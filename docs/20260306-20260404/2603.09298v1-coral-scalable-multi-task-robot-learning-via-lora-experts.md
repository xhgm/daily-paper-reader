---
title: "CORAL: Scalable Multi-Task Robot Learning via LoRA Experts"
title_zh: CORAL：基于 LoRA 专家的可扩展多任务机器人学习
authors: "Yuankai Luo, Woping Chen, Tong Liang, Zhenguo Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09298v1"
tags: ["query:q18"]
score: 10.0
evidence: 用于多任务机器人学习的视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在多任务学习中存在的任务干扰和负迁移问题，本文提出了CORAL框架。该框架通过冻结预训练骨干网络，并为每个任务分配独立的轻量化LoRA专家，实现了参数级别的任务隔离。CORAL Manager根据指令动态路由至对应专家，在避免干扰的同时支持新任务的持续扩展且无推理开销。实验证明，CORAL在真实机器人和仿真基准上均显著优于联合训练，为可扩展的终身机器人学习提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09298v1/fig-001.webp\", \"caption\": \"Fig. 4. Real-World Evaluation Tasks. Real-time inference trajectories successfully executed by CORALSimVLA during new capability acquisition.\", \"page\": 6, \"index\": 1, \"width\": 508, \"height\": 387}, {\"url\": \"assets/figures/arxiv/2603.09298v1/fig-002.webp\", \"caption\": \"TABLE III COMPARISON ON GOOGLE ROBOT TASKS; SUCCESS RATES (%).\", \"page\": 5, \"index\": 2, \"width\": 502, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2603.09298v1/fig-003.webp\", \"caption\": \"TABLE I COMPARISON ON THE LIBERO BENCHMARK. SUCCESS RATES (%) ON THE OFFICIAL TEST EPISODES FOR EACH SUITE. BOLD∗ DENOTES THE BEST PERFORMANCE.\", \"page\": 5, \"index\": 3, \"width\": 512, \"height\": 342}]"
motivation: 旨在解决多任务联合微调中的梯度冲突和负迁移问题，同时避免为每个任务存储完整模型的高昂成本。
method: 采用冻结骨干网络并为每个任务配备独立LoRA专家的架构，通过动态路由引擎实现零开销的任务切换与参数隔离。
result: 在真实双臂机器人及多个仿真基准测试中，CORAL克服了指令歧义，性能大幅超越传统的联合训练方法。
conclusion: CORAL提供了一种参数隔离且易于扩展的框架，有效解决了多任务机器人学习中的干扰与灾难性遗忘挑战。
---

## 摘要
在现实世界的机器人中部署视觉-语言-动作（VLA）模型暴露了一个核心的多任务学习挑战：如何协调多任务机器人学习中的任务干扰。当多个任务在单一阶段进行联合微调时，来自不同任务的梯度可能会发生冲突，导致负迁移并降低每个任务的性能。然而，为每个任务维护一个单独的完整检查点通常在存储和部署上是不可行的。为了解决这一困境，我们提出了 CORAL，这是一个与主干网络和具身形式无关的框架，旨在减轻多任务干扰，同时保持对连续新任务流的自然扩展性。CORAL 冻结单个预训练的 VLA 主干网络，并为每个任务附加一个轻量级的低秩自适应（LoRA）专家；在运行时，动态推理引擎（CORAL 管理器）将语言指令路由到相应的专家，并实时切换专家，且无推理开销。这种严格的参数隔离避免了复杂的门控网络，并从结构上防止了参数级的跨任务干扰；作为一项附加能力，它还支持按顺序引入新任务，而不会因灾难性遗忘导致参数覆盖。我们在现实世界的 Galaxea R1 双臂移动协作机器人以及三个仿真基准测试（LIBERO、WidowX、Google Robot）上验证了 CORAL。结果表明，CORAL 克服了细粒度的指令歧义，性能显著优于联合训练，为终身多任务机器人学习提供了一个实用且可扩展的系统。

## Abstract
Deploying Vision-Language-Action (VLA) models in real-world robotics exposes a core multi-task learning challenge: reconciling task interference in multi-task robotic learning. When multiple tasks are jointly fine-tuned in a single stage, gradients from different tasks can conflict, causing negative transfer and reducing per-task performance. Yet maintaining a separate full checkpoint per task is often storage- and deployment-prohibitive. To address this dilemma, we present CORAL, a backbone- and embodiment-agnostic framework designed primarily to mitigate multi-task interference while remaining naturally extensible to a continuous stream of new tasks. CORAL freezes a single pre-trained VLA backbone and attaches one lightweight Low-Rank Adaptation (LoRA) expert per task; at runtime, a dynamic inference engine (the CORAL Manager) routes language instructions to the appropriate expert and swaps experts on the fly with zero inference overhead. This strict parameter isolation avoids complex gating networks and prevents parameter-level cross-task interference by construction; as an added capability, it also enables sequentially introducing new tasks without parameter overwriting caused by catastrophic forgetting. We validate CORAL on a real-world Galaxea R1 dual-arm mobile manipulator and three simulation benchmarks (LIBERO, WidowX, Google Robot), where CORAL overcomes fine-grained instructional ambiguity and substantially outperforms joint training, yielding a practical and scalable system for lifelong multi-task robot learning. Website: https://frontierrobo.github.io/CORAL

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CORAL** 的框架，旨在解决视觉-语言-动作（VLA）模型在多任务机器人学习中的干扰、存储开销及遗忘问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **多任务干扰（Negative Transfer）：** 在单一阶段对多个任务进行联合微调时，不同任务的梯度往往会发生冲突，导致模型在特定任务上的表现不如单独微调。
*   **存储与部署瓶颈：** 为每个任务保存一个完整的模型检查点（Full Fine-tuning）虽然能避免干扰，但在边缘设备上存储开销巨大（O(N) 复杂度）。
*   **灾难性遗忘：** 在终身学习（Lifelong Learning）场景下，顺序学习新任务会覆盖旧任务的参数。
*   **研究目标：** 提出一种既能实现参数隔离（避免干扰和遗忘），又具备极高存储效率和实时切换能力的可扩展框架。

### 2. 方法论
CORAL 采用了一种“冻结主干+轻量级专家”的两阶段学习范式：
*   **具身感知通用预训练（Stage 1）：** 在多样化的机器人数据集上预训练或微调 VLA 主干网络（如 SimVLA 或 π0.5），使其掌握基本的运动学和视觉语义理解，随后永久冻结该主干参数（$\theta_{base}$）。
*   **任务特定 LoRA 专家（Stage 2）：** 为每个新任务训练一个独立的低秩自适应（LoRA）模块。LoRA 注入到 VLM 编码器和动作头的注意力层中。
*   **CORAL 管理器（动态推理引擎）：** 
    *   **确定性路由：** 利用机器人指令本身包含的任务信息，直接将指令映射到对应的 LoRA 专家索引，无需复杂的学习型门控网络。
    *   **零开销切换：** 在推理时，管理器将选定的 LoRA 权重直接合并（Merge）到冻结的主干中。切换过程在 100ms 内完成，且不增加推理时的计算量（FLOPs）。

### 3. 实验设计
*   **仿真基准测试：**
    *   **LIBERO：** 包含 40 个操作任务，分为 Spatial、Object、Goal 和 Long 四个子集。
    *   **WidowX (Simpler-Bridge)：** 评估真实到仿真的迁移能力。
    *   **Google Robot (Simpler-Fractal)：** 测试模型在不同变体下的鲁棒性。
*   **真实世界评估：**
    *   使用 **Galaxea R1** 双臂移动协作机器人。
    *   **任务类型：** 8 个复杂的双臂协同任务（如合书、拆笔帽、折叠 T 恤等）以及新能力获取实验（开门、按电梯按钮）。
*   **对比方法：** 联合全量微调（Joint FT）、独立全量微调（Independent FT）、以及多种 SOTA 模型（如 π0, RT-2-X, X-VLA 等）。

### 4. 资源与算力
*   **模型规模：** 验证了 0.8B（SimVLA）和 3B（π0.5）两种规模的主干网络。
*   **存储效率：** 对于 0.8B 模型，一个 Rank-16 的 LoRA 专家仅占 **26MB**，比全量模型缩小了约 **100 倍**。
*   **训练开销：** 论文提到专家训练非常轻量，仅需 50 个优化步或 1-5 个 Epoch。
*   **算力细节：** 文中未明确给出具体的 GPU 型号和总训练时长，但强调了其在消费级硬件和边缘设备上的部署可行性。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 40 个仿真任务和 10 余个真实世界复杂任务，实验组数较多。
*   **跨架构验证：** 在 SimVLA 和 π0.5 两个不同的 VLA 主干上均取得了提升，证明了方法的通用性（Backbone-agnostic）。
*   **客观性：** 实验对比了联合训练和独立训练两种极端情况，并进行了新任务获取与遗忘分析，逻辑闭环较完整。

### 6. 主要结论与发现
*   **性能领先：** CORAL 在 LIBERO 基准上达到了 **99.3%** 的成功率，刷新了 SOTA 记录。
*   **解决干扰：** 相比于联合微调（性能大幅下降），CORAL 通过参数隔离保持了极高的任务成功率。
*   **零遗忘：** 由于每个任务使用独立的 LoRA 专家，新任务的加入完全不会影响旧任务，从结构上消除了灾难性遗忘。
*   **部署友好：** 100ms 的切换延迟和极小的存储占用使其非常适合终身学习和实时部署。

### 7. 优点
*   **架构简洁高效：** 巧妙利用指令作为路由索引，规避了 MoE 架构中常见的门控训练难和负载均衡问题。
*   **参数隔离：** 彻底解决了多任务学习中的负迁移问题。
*   **极高的扩展性：** 存储开销极低，理论上可以在一个机器人上存储数百个技能专家。

### 8. 不足与局限
*   **任务定义依赖：** 依赖于清晰的任务划分和指令路由，如果指令存在歧义或任务边界模糊，路由准确性可能受影响。
*   **知识共享受限：** 严格的参数隔离虽然避免了干扰，但也限制了相关任务之间可能存在的正向知识迁移（即专家之间完全不共享更新）。
*   **预训练依赖：** 框架的效果高度依赖于第一阶段“具身感知预训练”的质量，如果主干网络未见过类似的运动模式，仅靠轻量级 LoRA 可能难以补偿。

（完）
