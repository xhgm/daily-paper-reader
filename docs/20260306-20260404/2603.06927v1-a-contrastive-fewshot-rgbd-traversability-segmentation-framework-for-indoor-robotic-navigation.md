---
title: A Contrastive Fewshot RGBD Traversability Segmentation Framework for Indoor Robotic Navigation
title_zh: 一种用于室内机器人导航的对比式少样本 RGB-D 可通行性分割框架
authors: "Qiyuan An, Tuan Dang, Fillia Makedon"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06927v1"
tags: ["query:q14"]
score: 9.0
evidence: 用于机器人导航的小样本RGBD可通行性分割
tldr: 针对室内机器人导航中纯视觉模型难以识别细小障碍物且依赖大量标注数据的问题，本文提出一种对比式少样本RGB-D可通行区域分割框架。该框架结合RGB图像与稀疏1D激光深度信息，通过两阶段注意力机制实现模态对齐。同时，引入负对比学习分支利用障碍物原型优化预测，显著提升了模型在极少样本下的泛化能力和对复杂障碍物的检测精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.06927v1/fig-001.webp\", \"caption\": \"TABLE II: Quantitative results on the indoor traversability dataset under 1-shot and 5-shot settings. Our proposed NCL achieves the best mIoU across all settings while requiring only a small fraction of trainable parameters, since most backbone weights are frozen during adaptation.\", \"page\": 6, \"index\": 1, \"width\": 910, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2603.06927v1/fig-002.webp\", \"caption\": \"Fig. 5: Qualitative results on indoor traversability segmentation. Each row shows (1) the query RGB image, (2) the corresponding depth vector, (3) predictions without the two-stage depth attention module and without NCL, (4) predictions with the depth module but without NCL, and (5) predictions from the full model. The proposed depth module helps separate floors from walls/ceilings, while the NCL branch further improves recognition of thin obstacles (e.g., chair legs).\", \"page\": 7, \"index\": 2, \"width\": 1032, \"height\": 635}, {\"url\": \"assets/figures/arxiv/2603.06927v1/fig-003.webp\", \"caption\": \"Fig. 2: Proposed contrastive few-shot RGB-D segmentation framework. RGB and depth inputs are embedded with modalityspecific backbones, fused into unified support and query features, and refined through prototype-based contrastive learning. A lightweight decoder then predicts the query segmentation mask for indoor freespaces.\", \"page\": 3, \"index\": 3, \"width\": 1021, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2603.06927v1/fig-004.webp\", \"caption\": \"Fig. 3: Two-stage attention depth backbone. It transforms 1D depth vectors into spatially aligned embeddings by applying horizontal attention (beam alignment) followed by vertical attention (height projection), producing refined depth features for multi-modal fusion.\", \"page\": 3, \"index\": 4, \"width\": 487, \"height\": 360}]"
motivation: 纯视觉模型难以检测细小障碍物且标注成本高，需结合深度信息并降低对大规模数据集的依赖。
method: 提出结合1D深度与RGB的多模态框架，利用负对比学习分支和两阶段注意力深度模块提升分割性能。
result: "在自建室内RGB-D数据集上，该方法在1-shot和5-shot设置下比现有基准模型mIoU最高提升了9%。"
conclusion: 结合负样本原型与稀疏深度信息能有效增强少样本场景下室内机器人可通行区域分割的鲁棒性。
---

## 摘要
室内可通行性分割旨在为自主智能体识别安全、可导航的空闲空间，这对于机器人导航至关重要。纯视觉模型通常难以检测到细小障碍物（如椅腿），这可能带来严重的安全风险。我们提出了一种多模态分割框架，利用 RGB 图像和稀疏一维（1D）激光深度信息来捕捉几何交互，并提高对挑战性障碍物的检测能力。为了减少对大规模标注数据集的依赖，我们采用了少样本分割（FSS）范式，使模型能够从有限的标注样本中进行泛化。传统的 FSS 方法仅关注正向原型，这往往导致对支持集的过拟合以及泛化能力不足。为解决这一问题，我们引入了一个负向对比学习（NCL）分支，利用负向原型（障碍物）来优化空闲空间的预测。此外，我们设计了一个两阶段注意力深度模块，在水平和垂直方向上将一维深度向量与 RGB 图像进行对齐。在我们自行采集的室内 RGB-D 可通行性数据集上进行的广泛实验表明，我们的方法优于最先进的 FSS 和 RGB-D 分割基准模型，在 1-shot 和 5-shot 设置下，mIoU 提升了高达 9%。这些结果突显了利用负向原型和稀疏深度进行鲁棒且高效的可通行性分割的有效性。

## Abstract
Indoor traversability segmentation aims to identify safe, navigable free space for autonomous agents, which is critical for robotic navigation. Pure vision-based models often fail to detect thin obstacles, such as chair legs, which can pose serious safety risks. We propose a multi-modal segmentation framework that leverages RGB images and sparse 1D laser depth information to capture geometric interactions and improve the detection of challenging obstacles. To reduce the reliance on large labeled datasets, we adopt the few-shot segmentation (FSS) paradigm, enabling the model to generalize from limited annotated examples. Traditional FSS methods focus solely on positive prototypes, often leading to overfitting to the support set and poor generalization. To address this, we introduce a negative contrastive learning (NCL) branch that leverages negative prototypes (obstacles) to refine free-space predictions. Additionally, we design a two-stage attention depth module to align 1D depth vectors with RGB images both horizontally and vertically. Extensive experiments on our custom-collected indoor RGB-D traversability dataset demonstrate that our method outperforms state-of-the-art FSS and RGB-D segmentation baselines, achieving up to 9\% higher mIoU under both 1-shot and 5-shot settings. These results highlight the effectiveness of leveraging negative prototypes and sparse depth for robust and efficient traversability segmentation.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于室内机器人导航的对比式少样本 RGB-D 可通行性分割框架

#### 1. 核心问题与研究动机
室内机器人（如清洁、送货机器人）在导航时需要准确识别“可通行区域”（Freespace）。现有的纯视觉模型（如 SegFormer）在处理复杂室内环境时存在两大痛点：
*   **细小障碍物检测难：** 诸如椅腿等细长物体在图像中像素占比极低，视觉特征不明显，容易被误判为可通行区域，导致碰撞风险。
*   **标注成本高：** 深度学习模型依赖大量精细标注的数据，而室内环境多变，获取大规模标注数据耗时耗力。
*   **传感器局限性：** 许多商用机器人仅配备低成本的一维（1D）激光雷达而非昂贵的 3D 深度相机，如何利用稀疏的 1D 深度信息辅助视觉分割是一个挑战。

#### 2. 提出的方法论
本文提出了一种结合 RGB 图像和 1D 稀疏深度信息的少样本分割（FSS）框架，核心技术包括：
*   **两阶段注意力深度模块（Two-stage Attention Depth Module）：** 针对 1D 深度向量与 2D 图像不对应的映射问题，首先通过“水平自注意力”实现光束对齐，再通过“垂直注意力”将深度信息投影至图像高度，生成空间对齐的深度特征。
*   **负对比学习分支（Negative Contrastive Learning, NCL）：** 传统的少样本分割仅提取目标类（正样本）的原型。本文额外提取了障碍物（负样本）的原型，通过计算查询图像特征与负原型的余弦相似度，显式地从预测结果中剔除障碍物区域，增强了模型对未见环境的泛化能力。
*   **多模态融合与少样本范式：** 采用元学习（Meta-learning）协议，仅需 1 个或 5 个标注样本（Support Set）即可对查询图像（Query Set）进行分割。模型仅更新深度模块和解码器，保持轻量化。

#### 3. 实验设计
*   **数据集：** 使用 Summit-XL Steel 机器人平台在多个校园建筑（教室、食堂、走廊等）采集的自建数据集。包含 91,951 对 RGB-1D 深度数据，其中 2,553 对经过人工精细标注。
*   **Benchmark 与对比方法：** 
    *   **融合骨干网络：** 对比了 CMX 和 DFormer 两种主流的多模态融合架构。
    *   **少样本基准：** 对比了 PANet、CWT 和 BAM 等最先进的少样本分割算法。
*   **实验设置：** 采用 1-shot 和 5-shot 设置，评估指标为平均交并比（mIoU）。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节：** 提到了使用 AdamW 优化器，初始学习率为 $6 \times 10^{-5}$，采用 WarmUpPolyLR 调度策略，每个 episode 适配训练 120 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 1-shot 和 5-shot 的完整对比实验，并在 10,000 对随机采样的测试集上进行评估，每组实验运行 5 次取平均值（标准差 < 0.4%），具有较好的统计说服力。
*   **消融实验：** 针对“两阶段注意力模块”的水平/垂直分支以及“负对比学习分支”分别做了消融研究，验证了各组件的贡献。
*   **充分性评价：** 实验设计较为充分，涵盖了不同骨干网络和多种 SOTA 方法的对比，客观地展示了该方法在处理细小障碍物方面的优势。

#### 6. 主要结论与发现
*   **性能提升：** NCL 框架在 1-shot 和 5-shot 设置下，比现有最强基准（如 BAM）的 mIoU 提升了约 8%-9%。
*   **深度信息的价值：** 引入 1D 深度信息能显著减少地板与墙壁/天花板的混淆。
*   **负原型的关键作用：** 显式建模障碍物原型（NCL）对于识别椅腿等细小物体至关重要，相比仅使用正向原型，障碍物类别的 IoU 提升了 11.4%。

#### 7. 优点
*   **实用性强：** 针对低成本 1D 激光雷达设计，符合工业界移动机器人的主流配置。
*   **创新性：** 提出了非参数化的负对比学习路径，在不增加额外训练参数负担的情况下提升了泛化性能。
*   **数据集贡献：** 贡献了一个大规模的室内 RGB-1D 深度可通行性数据集，填补了该领域的空白。

#### 8. 不足与局限
*   **深度退化问题：** 1D 深度在垂直方向上是退化的（只有一个维度），虽然使用了注意力机制投影，但在缺乏视觉线索的区域可能会产生“幻觉”或误导。
*   **伪标签依赖：** 测试集的评估依赖于一个强监督教师模型生成的伪标签，虽然教师模型准确率高（98.5% mIoU），但仍可能存在系统性偏差。
*   **算力细节缺失：** 缺乏具体的硬件运行效率和推理延迟数据，对于实时机器人应用来说，推理速度是一个关键指标。

（完）
