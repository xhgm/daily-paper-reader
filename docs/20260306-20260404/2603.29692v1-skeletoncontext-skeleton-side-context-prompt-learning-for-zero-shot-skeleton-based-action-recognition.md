---
title: "SkeletonContext: Skeleton-side Context Prompt Learning for Zero-Shot Skeleton-based Action Recognition"
title_zh: SkeletonContext：用于零样本基于骨架动作识别的骨架端上下文提示学习
authors: "Ning Wang, Tieyue Wu, Naeha Sharif, Farid Boussaid, Guangming Zhu, Lin Mei, Mohammed Bennamoun, zhang liang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29692v1"
tags: ["query:q14"]
score: 9.0
evidence: 利用语言驱动的上下文语义进行零样本骨骼动作识别
tldr: 本研究针对零样本骨骼动作识别中缺乏上下文信息（如交互物体）导致难以区分相似动作的问题，提出了SkeletonContext框架。该框架通过跨模态上下文提示模块，利用大语言模型引导的上下文重构，将语言层面的语义信息引入骨骼编码器，并结合关键部位解耦模块提取运动相关特征。实验证明，该方法在多个基准测试中达到了SOTA水平，有效提升了对细粒度动作的辨析能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29692v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison between existing ZSSAR methods and our proposed SkeletonContext. Conventional ZSSAR methods directly align skeleton features with textual descriptions, but the absence of contextual cues creates a semantic gap that hinders discrimination between similar actions. In contrast, SkeletonContext reconstructs language-driven contextual semantics (e.g., objects) and injects them into skeleton representations, enabling finegrained, context-aware zero-shot action recognition.\", \"page\": 1, \"index\": 1, \"width\": 444, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.29692v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of generalized zero-shot action recognition accuracy on the NTU RGB+D datasets. SkeletonContext achieves competitive or superior harmonic-mean (H) performance across most evaluation splits, indicating balanced generalization from seen (S) to unseen (U) classes.\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2603.29692v1/fig-003.webp\", \"caption\": \"Table 2. Comparison of zero-shot learning (ZSL) accuracy on the NTU RGB+D datasets. SkeletonContext achieves strong or comparable results across multiple splits, demonstrating effective generalization to unseen action categories.\", \"page\": 6, \"index\": 3, \"width\": 502, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.29692v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of SkeletonContext. The model enriches skeleton features via Cross-Modal Context Prompt Module (Sec 3.3) and Key-Part Decoupling Module (Sec 3.4) for context-aware zero-shot action recognition. Both modules are guided by LLM–derived contextual knowledge (Sec 3.2) during training, enabling semantic grounding and cross-modal alignment.\", \"page\": 4, \"index\": 4, \"width\": 823, \"height\": 358}]"
motivation: 现有的零样本骨骼动作识别方法因缺乏物体等上下文线索，导致骨骼特征与语义表示之间存在鸿沟，难以区分视觉相似的动作。
method: 提出SkeletonContext框架，利用跨模态上下文提示模块重构LLM引导的语义信息，并结合关键部位解耦模块提取鲁棒的运动特征。
result: 在多个基准测试的常规和广义零样本设置下，该方法均取得了最先进的性能，验证了其在上下文推理方面的有效性。
conclusion: 通过将语言驱动的上下文语义融入骨骼表示，SkeletonContext成功弥合了模态间隙，显著增强了对细粒度动作的识别能力。
---

## 摘要
零样本基于骨架的动作识别旨在通过语义描述将知识从已见类别迁移到未见类别，从而识别未见动作。大多数现有方法通常在共享潜空间内将骨架特征与文本嵌入进行对齐。然而，由于缺乏上下文线索（例如动作涉及的物体），骨架表示与语义表示之间存在固有鸿沟，导致难以区分视觉上相似的动作。为了解决这一问题，我们提出了 SkeletonContext，这是一个基于提示的框架，利用语言驱动的上下文语义来丰富骨架运动表示。具体而言，我们引入了一个跨模态上下文提示模块，该模块在大型语言模型（LLM）的引导下，利用预训练语言模型来重构掩码上下文提示。该设计有效地将语言上下文迁移到骨架编码器中，以实现实例级语义落地并改进跨模态对齐。此外，我们还加入了一个关键部位解耦模块，用于解耦与运动相关的关节特征，从而确保在缺乏显式物体交互的情况下也能实现稳健的动作理解。在多个基准数据集上的广泛实验表明，SkeletonContext 在常规和广义零样本设置下均达到了最先进的性能，验证了其在上下文推理和区分细粒度、视觉相似动作方面的有效性。

## Abstract
Zero-shot skeleton-based action recognition aims to recognize unseen actions by transferring knowledge from seen categories through semantic descriptions. Most existing methods typically align skeleton features with textual embeddings within a shared latent space. However, the absence of contextual cues, such as objects involved in the action, introduces an inherent gap between skeleton and semantic representations, making it difficult to distinguish visually similar actions. To address this, we propose SkeletonContext, a prompt-based framework that enriches skeletal motion representations with language-driven contextual semantics. Specifically, we introduce a Cross-Modal Context Prompt Module, which leverages a pretrained language model to reconstruct masked contextual prompts under guidance derived from LLMs. This design effectively transfers linguistic context to the skeleton encoder for instance-level semantic grounding and improved cross-modal alignment. In addition, a Key-Part Decoupling Module is incorporated to decouple motion-relevant joint features, ensuring robust action understanding even in the absence of explicit object interactions. Extensive experiments on multiple benchmarks demonstrate that SkeletonContext achieves state-of-the-art performance under both conventional and generalized zero-shot settings, validating its effectiveness in reasoning about context and distinguishing fine-grained, visually similar actions.

---

## 论文详细总结（自动生成）

以下是对论文《SkeletonContext: Skeleton-side Context Prompt Learning for Zero-Shot Skeleton-based Action Recognition》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在零样本基于骨架的动作识别（ZSSAR）中，骨架数据仅包含关节坐标，缺乏视觉背景（如物体、环境）。这导致骨架特征与包含丰富语义的文本描述之间存在巨大的“语义鸿沟”，使得模型难以区分动作模式相似但上下文不同的类别（例如“打字”与“写字”）。
*   **研究背景**：现有的 ZSSAR 方法多侧重于全局对齐或增强文本端，而忽略了从骨架端主动重构缺失的上下文信息。

### 2. 方法论
论文提出了 **SkeletonContext** 框架，核心思想是通过语言驱动的提示学习，在骨架特征中注入“虚拟”的上下文语义。
*   **LLM 上下文生成**：利用 GPT-4 生成结构化的动作描述，包含环境（Environment）、使用的物体（Used object）和目标物体（Target object）。
*   **跨模态上下文提示模块 (Cross-Modal Context Prompt Module)**：
    *   **差异化关节编码器 (DJE)**：通过计算关节间的成对差异来捕捉细粒度的空间拓扑和姿态依赖。
    *   **渐进式局部掩码 (PPM)**：采用课程学习策略，随训练进程逐渐增加上下文槽位的掩码比例，强制模型从纯骨架运动中推理出完整的上下文。
    *   **语义上下文落地**：利用预训练的 BERT 模型，通过跨模态注意力机制（Cross-Attention）将骨架特征与文本提示融合，重构被掩码的上下文单词。
*   **关键部位解耦模块 (KPD)**：针对不依赖物体的动作（如“挥手”），通过预测关节重要性图来突出关键身体部位，并利用 LLM 提供的部位先验进行监督。
*   **损失函数**：总损失由对齐损失（Contrastive Alignment）、上下文重构损失 ($L_{ccr}$) 和关键部位解耦损失 ($L_{kpd}$) 组成。

### 3. 实验设计
*   **数据集**：
    *   **NTU RGB+D 60**：包含 60 类动作，采用 55/5 和 48/12 划分。
    *   **NTU RGB+D 120**：目前最大的骨架数据集，采用 110/10 和 96/24 划分。
    *   **PKU-MMD**：中等规模数据集，采用随机 46/5 划分。
*   **Benchmark 与对比方法**：在常规零样本（ZSL）和广义零样本（GZSL）设置下进行评估。对比了包括 ReViSE、SynSE、STAR、SA-DVAE、SCoPLe、Neuron 和 FS-VAE 在内的十余种最先进方法。
*   **评估指标**：Top-1 准确率、已见/未见类别的调和平均值（H-mean）。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **NVIDIA GeForce RTX 3090 GPU** 的系统上运行。
*   **软件/参数**：使用 PyTorch 框架，优化器为 SGD，初始学习率为 0.1，Batch Size 为 64。
*   **训练时长**：文中未明确提及具体的总训练时长，但给出了学习率衰减的周期（第 10 和 20 个 epoch）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个主流数据集上进行了详尽测试，涵盖了多种类别划分方案（固定划分与随机划分）。
*   **消融实验**：非常充分。分别验证了 DJE、SCG（语义落地）、PPM 和 KPD 模块的有效性，并分析了不同损失函数项以及不同上下文槽位（环境 vs. 物体）对性能的影响。
*   **客观性**：通过在相似动作簇（Easy/Medium/Hard 难度）上的对比实验，客观证明了该方法在处理细粒度歧义动作上的优势。

### 6. 主要结论与发现
*   **上下文重构至关重要**：在骨架端引入语言驱动的上下文语义，能显著提升跨模态对齐的质量。
*   **性能领先**：SkeletonContext 在所有测试数据集上均达到了 SOTA 水平，尤其是在更具挑战性的 GZSL 设置下，其调和平均值（H）提升显著，证明了模型在已见和未见类别间的平衡能力。
*   **可视化验证**：定性分析显示，模型能够准确从骨架序列中推断出“键盘”、“笔”、“眼镜”等缺失的上下文物体。

### 7. 优点
*   **创新性视角**：不同于以往只做特征对齐的方法，该方法通过“重构缺失信息”来增强表示能力。
*   **细粒度辨析力**：通过 DJE 和上下文注入，有效解决了骨架识别中长期存在的“视觉相似动作难以区分”的问题。
*   **可解释性**：KPD 模块和上下文重构结果提供了直观的语义解释，证明模型确实学到了动作与物体/部位的关联。

### 8. 不足与局限
*   **对 LLM 的依赖**：模型性能部分取决于 GPT-4 生成描述的质量和准确性，若 LLM 提供的先验存在偏差，可能会影响重构效果。
*   **计算复杂度**：引入了 BERT 和跨模态注意力机制，相比于纯轻量级的 GCN 方法，推理时的计算开销会有所增加。
*   **场景局限**：虽然在实验室采集的数据集上表现优异，但在背景极其复杂或多人交互干扰严重的真实野外场景（In-the-wild）下的鲁棒性仍有待验证。

（完）
