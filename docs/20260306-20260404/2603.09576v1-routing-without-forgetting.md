---
title: Routing without Forgetting
title_zh: 无遗忘路由
authors: "Alessio Masano, Giovanni Bellitto, Dipam Goswani, Joost Van de Weijer, Concetto Spampinato"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09576v1"
tags: ["query:q15"]
score: 9.0
evidence: 通过动态路由实现Transformer的在线持续学习
tldr: 针对在线持续学习（OCL）中传统参数高效微调方法依赖梯度优化且难以应对非平稳数据流的问题，本文提出Routing without Forgetting (RwF)。该方法将持续学习建模为路由问题，利用现代霍普菲尔德网络的能量关联检索机制，在Transformer各层动态生成输入条件化的Prompt。RwF无需任务标识或迭代优化，在多个类增量基准测试中显著优于现有方法，为OCL提供了一种基于能量路由的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.09576v1/fig-001.webp\", \"caption\": \"Table 1: OCL performance comparison. Final Average Accuracy AFinal(↑) and Forgetting (↓) on Split-CIFAR100, Split-ImageNet-R, and Split-ImageNet-S. Best: bold, second: underline, third: italic. RwF achieves top performance on large-scale ImageNet benchmarks while ranking competitively on CIFAR100.\", \"page\": 7, \"index\": 1, \"width\": 980, \"height\": 622}, {\"url\": \"assets/figures/arxiv/2603.09576v1/fig-002.webp\", \"caption\": \"Table 2: Performance under few-shot data regimes (Split-ImageNet-R). Final Average Accuracy (AFinal(↑)) as the fraction of available training samples decreases from 100% to 20%. Best: bold, second: underline, third: italic. RwF maintains the highest accuracy across all evaluated data regimes.\", \"page\": 8, \"index\": 2, \"width\": 686, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2603.09576v1/fig-003.webp\", \"caption\": \"Table 3: Effect of routing depth on Split-CIFAR100, Split-ImageNet-R and Split-ImageNet-S. Final Average Accuracy (AFinal(↑)) as the number of HopfieldPooling (HP) layers k increases. First-k inserts HP layers in the first k transformer block; Last-k inserts them in last k blocks (12 . . . 12− k + 1). Best: bold, second: underline, third: italic.\", \"page\": 9, \"index\": 3, \"width\": 879, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.09576v1/fig-004.webp\", \"caption\": \"Figure 2: Performance scaling with increasing task fragmentation (Split-ImageNet-R). Final Average Accuracy (AFinal) (↑) as the number of sequential tasks t increases from 5 to 40.\", \"page\": 9, \"index\": 4, \"width\": 686, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2603.09576v1/fig-005.webp\", \"caption\": \"Figure 1: RwF layer: routing-augmented transformer block. Given input tokens Zℓ, a Hopfield-based associative retrieval module generates input-conditioned routing prompts Pℓ via energy-based pooling over token features. The retrieved Pℓ are concatenated with the Zℓ tokens and processed by the standard Multi-Head Self-Attention (MHSA). After MHSA, only the backbone tokens Z̃ℓ are propagated to the MLP blocks and then to the next RwF transformer layer ℓ+ 1, while P̃ℓ are discarded.\", \"page\": 5, \"index\": 5, \"width\": 979, \"height\": 409}]"
motivation: 现有的Prompt或Adapter方法在单次观察数据的在线持续学习场景下，因过度依赖梯度优化和缺乏动态路由机制而表现不佳。
method: 引入基于现代霍普菲尔德网络的能量关联检索层，通过闭式解最小化自由能量函数，在前向传播中为每个输入动态生成Prompt。
result: 在Split-ImageNet-R和Split-ImageNet-S等挑战性基准上，RwF大幅超越了现有的Prompt类方法，且在少样本学习场景下表现优异。
conclusion: 将基于能量的关联路由直接嵌入Transformer架构，是解决在线持续学习中灾难性遗忘问题的一种原则性且有效的方案。
---

## 摘要
Transformer 中的持续学习通常通过参数高效适配来解决：提示词（prompts）、适配器（adapters）或 LoRA 模块针对每个任务进行专门化，而骨干网络保持冻结。尽管这些方法在受控的多轮（multi-epoch）设置中有效，但它们依赖于基于梯度的渐进式专门化，在在线持续学习（OCL）中表现不佳，因为在 OCL 中，数据以非平稳流的形式到达，且每个样本可能仅被观察一次。我们将 Transformer 中的持续学习重新定义为一个路由问题：在严格的在线约束下，模型必须为每个输入动态选择合适的表示子空间，而无需显式的任务标识符或重复优化。因此，我们提出了“无遗忘路由”（Routing without Forgetting, RwF），这是一种受现代 Hopfield 网络启发、增强了基于能量的关联检索层的 Transformer 架构。RwF 不存储或合并特定于任务的提示词，而是通过在每一层对 Transformer 标记嵌入进行单步关联检索来生成动态提示词。检索对应于严格凸自由能泛函的闭式解最小化，从而在每次前向传播中实现输入条件路由，且独立于迭代梯度细化。在具有挑战性的类增量基准测试中，RwF 优于现有的基于提示词的方法。在 Split-ImageNet-R 和 Split-ImageNet-S 上，RwF 的表现大幅超过了先前的基于提示词的方法，即使在少样本学习场景下也是如此。这些结果表明，将基于能量的关联路由直接嵌入 Transformer 骨干网络，为在线持续学习提供了一个有原则且有效的基础。

## Abstract
Continual learning in transformers is commonly addressed through parameter-efficient adaptation: prompts, adapters, or LoRA modules are specialized per task while the backbone remains frozen. Although effective in controlled multi-epoch settings, these approaches rely on gradual gradient-based specialization and struggle in Online Continual Learning (OCL), where data arrive as a non-stationary stream and each sample may be observed only once. We recast continual learning in transformers as a routing problem: under strict online constraints, the model must dynamically select the appropriate representational subspace for each input without explicit task identifiers or repeated optimization. We thus introduce Routing without Forgetting (RwF), a transformer architecture augmented with energy-based associative retrieval layers inspired by Modern Hopfield Networks. Instead of storing or merging task-specific prompts, RwF generates dynamic prompts through single-step associative retrieval over the transformer token embeddings at each layer. Retrieval corresponds to the closed-form minimization of a strictly convex free-energy functional, enabling input-conditioned routing within each forward pass, independently of iterative gradient refinement. Across challenging class-incremental benchmarks, RwF improves over existing prompt-based methods. On Split-ImageNet-R and Split-ImageNet-S, RwF outperforms prior prompt-based approaches by a large margin, even in few-shot learning regimes. These results indicate that embedding energy-based associative routing directly within the transformer backbone provides a principled and effective foundation for OCL.

---

## 论文详细总结（自动生成）

这是一份关于论文《Routing without Forgetting (RwF)》的结构化深度分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：如何在**在线持续学习（Online Continual Learning, OCL）**场景下，使 Transformer 模型既能快速吸收新知识（塑性），又能有效防止旧知识遗忘（稳定性）。
*   **研究背景**：
    *   现有的参数高效适配方法（如 Prompt-tuning, LoRA, Adapters）通常依赖多次迭代的梯度更新来使参数“专门化”。
    *   在严格的 OCL 场景中，数据以非平稳流形式出现，每个样本仅被观察一次（Single-pass）。这种“单次观察”限制了梯度优化的充分性，导致模型在任务切换时反应滞后，难以实时调整表示空间。
    *   **动机**：将持续学习重新定义为**路由问题**，通过输入驱动的动态机制而非缓慢的参数更新来选择表示子空间。

### 2. 方法论：RwF (Routing without Forgetting)
*   **核心思想**：在 Transformer 层中嵌入基于**现代 Hopfield 网络（Modern Hopfield Networks）**的能量关联检索机制。通过输入特征自身的几何结构动态生成“路由 Prompt”，实现表示空间的即时重构。
*   **关键技术细节**：
    *   **关联检索层（HopfieldPooling）**：在每一层 Transformer 块的自注意力（MHSA）之前，插入一个关联检索操作。它利用一组可学习的查询向量 $Q$ 对当前输入 Token 序列 $Z$ 进行检索。
    *   **闭式解优化**：路由过程被建模为最小化一个严格凸的自由能泛函（Free-energy Functional）。其数学形式表现为 Softmax 检索：$P = \text{softmax}(\beta \tilde{Q} K^\top) V$。这意味着路由决策是在前向传播中通过解析计算完成的，无需迭代优化。
    *   **动态 Prompt 生成**：检索得到的 $P$ 作为动态 Prompt 与原 Token 拼接进入 MHSA。在注意力计算后，这些 Prompt 会被丢弃，仅保留更新后的 Token 传向下一层。
    *   **架构平滑性**：由于 Softmax 操作是 Lipschitz 连续的，微小的输入变化只会引起路由权重的平滑偏移，从而在结构上缓解了分布漂移导致的剧烈表示跳变。

### 3. 实验设计
*   **数据集与场景**：
    *   **Split-CIFAR100**：10个任务，每个任务10类。
    *   **Split-ImageNet-R**：10个任务，每个任务20类（大规模、多样化）。
    *   **Split-ImageNet-S**：10个任务，每个任务100类（极具挑战性的全量 ImageNet 划分）。
*   **Benchmark 与对比方法**：
    *   **回放类**：ER, MIR, DER++, PCR 等。
    *   **提示词类**：L2P, DualPrompt, CODA-Prompt。
    *   **适配器/LoRA 类**：Online-LoRA, InfLoRA, APER, EASE。
*   **评估协议**：严格的单次通过（Single-pass）在线类增量学习（Class-IL），不提供任务标识符（Task ID）。

### 4. 资源与算力
*   **硬件与环境**：论文提到使用 **ViT-B/16** 作为预训练骨干网络（ImageNet-21k 预训练）。
*   **具体算力说明**：文中**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到 Batch Size 为 64，且由于是单次通过（Single-pass），其计算开销主要集中在推理和单次梯度更新上。
*   **参数效率**：RwF 仅引入了约 **2.13%** 的额外可学习参数，处于参数高效微调（PEFT）的典型范围内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型（CIFAR）到大规模（ImageNet-R/S）的多个基准，实验设计较为全面。
*   **消融实验**：
    *   **路由深度分析**：研究了在不同层（前几层 vs 后几层）插入 Hopfield 层的效果，发现前几层插入对缓解干扰最有效。
    *   **少样本鲁棒性**：测试了在仅有 20% 训练数据情况下的表现，验证了方法的样本效率。
    *   **任务碎片化分析**：将任务数从 5 增加到 40，测试模型在频繁分布切换下的稳定性。
*   **客观性**：作者对所有基准方法进行了针对在线环境的超参数重新调优，确保了对比的公平性（避免了基准方法因默认参数不适用 OCL 而表现过差）。

### 6. 主要结论与发现
*   **性能领先**：在 ImageNet-R 上达到 74.09% 的准确率，在 ImageNet-S 上达到 61.37%，显著超越了 CODA-Prompt 和 InfLoRA 等强基线。
*   **即时适配**：RwF 的路由机制在参数尚未收敛前就能根据输入调整表示，这使其在数据稀缺和频繁切换任务的场景下表现尤为稳健。
*   **层级效应**：在 Transformer 的早期层进行关联路由比在深层更有效，说明在底层拦截特征干扰对持续学习至关重要。

### 7. 优点与亮点
*   **理论完备**：将路由过程与能量模型（Hopfield 网络）结合，为动态 Prompt 的生成提供了坚实的变分法解释。
*   **无需任务标识**：完全不依赖 Task ID，符合真实的在线学习场景。
*   **解耦优化与路由**：通过闭式解检索，将“表示选择”的速度与“参数更新”的速度解耦，解决了 OCL 中梯度更新滞后的痛点。

### 8. 不足与局限
*   **细粒度分类限制**：在 CUB-200 等细粒度数据集上效果一般。原因在于 Hopfield 检索本质上是一种加权聚合，可能会平滑掉区分细微类别所需的局部显著特征。
*   **计算复杂度**：虽然 $m \ll L$（路由 Prompt 数量远小于 Token 长度），但每层增加的关联检索仍会带来额外的矩阵运算开销。
*   **依赖预训练质量**：作为一种基于表示空间重构的方法，其效果高度依赖于冻结的骨干网络（ViT）提取高质量初始特征的能力。

（完）
