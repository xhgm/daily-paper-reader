---
title: "SegVGGT: Joint 3D Reconstruction and Instance Segmentation from Multi-View Images"
title_zh: SegVGGT：基于多视图图像的联合三维重建与实例分割
authors: "Jinyuan Qu, Hongyang Li, Lei Zhang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19926v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 基于多视图图像的联合3D重建与实例分割
tldr: SegVGGT是一个端到端的统一框架，旨在直接从多视图RGB图像中同时实现3D重建与实例分割。针对现有方法依赖高质量点云且重建与语义解耦的问题，该方法通过引入与几何特征交互的对象查询，并利用帧级注意力分布对齐（FADA）策略解决注意力分散问题。实验证明其在ScanNet系列数据集上达到了SOTA性能，具有极强的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19926v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of SegVGGT with point cloud-based methods. Input modalities are denoted as follows: P (point cloud from the benchmark), I (RGB images), D (depth maps), and C (camera parameters). † indicates that the method first performs 3D reconstruction from the input images with VGGT [52], and then performs 3D instance segmentation on the reconstructed point cloud.\", \"page\": 11, \"index\": 1, \"width\": 673, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-002.webp\", \"caption\": \"Table 5: Scalability and efficiency analysis of SegVGGT. We report the runtime breakdown, including aggregator, decoding heads, post-processing, total runtime, and peak reserved memory under different numbers of input frames.\", \"page\": 21, \"index\": 2, \"width\": 679, \"height\": 185}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-003.webp\", \"caption\": \"Fig. 7: Qualitative comparison with IGGT on ScanNet++. The blue and green bounding boxes highlight spatially adjacent instances of the same semantic class. While the clustering-based IGGT struggles to separate these closely located objects, our SegVGGT accurately disambiguates and segments them into distinct instances.\", \"page\": 30, \"index\": 3, \"width\": 706, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-004.webp\", \"caption\": \"Fig. 1: Comparison of different 3D instance segmentation pipelines. (a) Segmentation methods that rely on high-quality point clouds require complex and timeconsuming processing, limiting their applicability. (b) Feed-forward 3D reconstruction improves efficiency, but leads to severely degraded segmentation performance. (c) Our proposed SegVGGT performs 3D reconstruction and instance segmentation simultaneously in a single forward pass, achieving superior segmentation quality. (d) 3D instance segmentation performance on the ScanNet200 validation set.\", \"page\": 2, \"index\": 4, \"width\": 706, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-005.webp\", \"caption\": \"Fig. 6: Qualitative 3D results on ScanNet200. By directly unprojecting the 2D predictions, our method yields high-quality 3D scene reconstructions and precise instance segmentations. Note that instance colors are randomly assigned for visualization purposes and do not necessarily correspond between the prediction and ground truth.\", \"page\": 29, \"index\": 5, \"width\": 710, \"height\": 446}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-006.webp\", \"caption\": \"Fig. 5: Qualitative 2D results on ScanNet200. Given multi-view RGB inputs, SegVGGT simultaneously predicts dense depth maps, cross-view consistent instancelevel feature maps, and 2D instance masks. Spatially adjacent objects of the same class are clearly separated both in the feature space and in the resulting instance masks.\", \"page\": 28, \"index\": 6, \"width\": 702, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-007.webp\", \"caption\": \"Fig. 3: Visualization of the FADA module’s effect. We visualize the similarity map between the query corresponding to the TV instance and the image tokens in the last cross-attention layer. Without the FADA module, the query attention is dispersed across the entire scene. After introducing FADA, the query focuses much more accurately on the TV, indicating that FADA improves the model’s ability to attend to target instances and thereby enhances instance segmentation performance.\", \"page\": 13, \"index\": 7, \"width\": 711, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-008.webp\", \"caption\": \"Fig. 4: Visualization of the FADA module’s effect. We visualize the similarity maps between the query corresponding to the TV instance and the image tokens in the 15th and 23th cross-attention layers. Without the FADA module, the query attention is dispersed across the entire scene. After introducing FADA, the query focuses much more accurately on the TV, indicating that FADA improves the model’s ability to attend to target instances and thereby enhances instance segmentation performance.\", \"page\": 24, \"index\": 8, \"width\": 703, \"height\": 487}, {\"url\": \"assets/figures/arxiv/2603.19926v1/fig-009.webp\", \"caption\": \"Fig. 2: Overall architecture of SegVGGT. SegVGGT takes multi-view unposed RGB images as input and outputs the camera parameters, depth maps, and instance masks in a single forward pass. In the visual geometry grounded transformer, image tokens and object queries are progressively refined to capture both multi-view geometry and instance semantics. The green arrows indicate the components related to the 3D instance segmentation. During training, the Frame-level Attention Distribution Alignment (FADA) module regularizes the cross-attention weights via Jensen-Shannon Divergence loss Ljs, effectively mitigating the attention dispersion problem.\", \"page\": 5, \"index\": 9, \"width\": 706, \"height\": 313}]"
motivation: 现有的3D实例分割方法过度依赖高质量点云且与重建过程解耦，导致处理流程复杂且对噪声敏感。
method: 提出SegVGGT框架，通过对象查询与多级几何特征交互，并结合FADA策略引导模型在训练中关注实例相关的图像帧。
result: 在ScanNetv2和ScanNet200上超越了现有的联合模型及基于RGB-D的方法，并在ScanNet++上展现出优异的泛化性。
conclusion: 该研究成功将3D重建与语义理解深度集成，为从纯RGB图像进行高效、准确的3D场景理解提供了新方案。
---

## 摘要
三维实例分割方法通常依赖于高质量的点云或带有位姿的 RGB-D 扫描，需要复杂的多阶段处理流程，且对重建噪声高度敏感。尽管近期的前馈 Transformer 彻底改变了多视图三维重建，但它们仍与高层语义理解相脱节。在这项工作中，我们提出了 SegVGGT，这是一个统一的端到端框架，可直接从多视图 RGB 图像中同时进行前馈三维重建和实例分割。通过引入与多级几何特征交互的对象查询（object queries），我们的方法将实例识别深度集成到基于视觉几何的 Transformer 中。为了解决由海量全局图像 token 引起的严重注意力分散问题，我们提出了帧级注意力分布对齐（FADA）策略。FADA 在训练期间显式引导对象查询关注与实例相关的帧，在不增加额外推理开销的情况下提供结构化监督。大量实验表明，SegVGGT 在 ScanNetv2 和 ScanNet200 上达到了最先进的性能，优于近期的联合模型和基于 RGB-D 的方法，同时在 ScanNet++ 上展现出强大的泛化能力。

## Abstract
3D instance segmentation methods typically rely on high-quality point clouds or posed RGB-D scans, requiring complex multi-stage processing pipelines, and are highly sensitive to reconstruction noise. While recent feed-forward transformers have revolutionized multi-view 3D reconstruction, they remain decoupled from high-level semantic understanding. In this work, we present SegVGGT, a unified end-to-end framework that simultaneously performs feed-forward 3D reconstruction and instance segmentation directly from multi-view RGB images. By introducing object queries that interact with multi-level geometric features, our method deeply integrates instance identification into the visual geometry grounded transformer. To address the severe attention dispersion problem caused by the massive number of global image tokens, we propose the Frame-level Attention Distribution Alignment (FADA) strategy. FADA explicitly guides object queries to attend to instance-relevant frames during training, providing structured supervision without extra inference overhead. Extensive experiments demonstrate that SegVGGT achieves the state-of-the-art performance on ScanNetv2 and ScanNet200, outperforming both recent joint models and RGB-D-based approaches, while exhibiting strong generalization capabilities on ScanNet++.

---

## 论文详细总结（自动生成）

### SegVGGT：基于多视图图像的联合三维重建与实例分割 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 实例分割方法高度依赖高质量的点云或带有精确位姿的 RGB-D 扫描，导致处理流程复杂（涉及配准、融合、去噪等多个阶段），且对重建过程中的几何噪声极其敏感。
*   **研究动机**：虽然前馈 Transformer（如 VGGT）在多视图 3D 重建上取得了突破，但它们通常只关注几何估计，与高层语义理解（如实例分割）脱节。
*   **整体含义**：本文旨在提出一个统一的端到端框架 **SegVGGT**，直接从多视图 RGB 图像中同时实现 3D 重建和实例分割，消除对深度传感器和复杂后处理的依赖。

#### 2. 论文提出的方法论
*   **核心思想**：将实例识别深度集成到视觉几何 Transformer 中，通过可学习的对象查询（Object Queries）与多级几何特征进行交互，实现几何与语义的协同进化。
*   **关键技术细节**：
    *   **几何感知 Transformer**：在每一层 Transformer 中，对象查询通过交叉注意力（Cross-Attention）机制从全局图像 token 中提取特征，从而同时捕获低级空间细节和高级几何抽象。
    *   **帧级注意力分布对齐（FADA）**：针对海量图像 token（$10^4 \sim 10^5$）导致的注意力分散问题，FADA 策略在训练时利用 Jensen-Shannon (JS) 散度，强制对象查询的注意力权重与实例在各帧中的实际可见度分布对齐。
    *   **同步 3D 解码**：模型包含多个头，分别预测相机参数、深度图和实例级特征图。通过对象查询与特征图的点积生成 2D 掩码，再结合预测的几何信息直接投影回 3D 空间。
*   **算法流程**：输入 $N$ 张无位姿 RGB 图像 $\rightarrow$ DINO 提取特征 $\rightarrow$ Transformer 联合更新图像 token 和对象查询 $\rightarrow$ 解码头输出几何与实例掩码 $\rightarrow$ 3D 投影得到最终结果。

#### 3. 实验设计
*   **数据集**：
    *   **ScanNetv2**：标准室内场景，20 个类别。
    *   **ScanNet200**：更具挑战性的长尾分布数据集，包含 200 个细粒度类别。
    *   **ScanNet++**：用于评估模型的跨数据集**泛化能力**（Zero-shot 评估）。
*   **Benchmark 与对比方法**：
    *   **点云基准**：对比 Mask3D、OneFormer3D、Relation3D 等直接处理点云的方法。
    *   **RGB-D 基准**：对比 ODIN 等依赖深度图的方法。
    *   **联合模型基准**：对比 PanSt3R、IGGT 等同样从 RGB 图像进行重建与分割的最新方法。

#### 4. 资源与算力
*   **硬件设备**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练时长**：在 ScanNetv2 和 ScanNet200 上分别训练了约 **2 天**。
*   **推理效率**：处理 100 帧图像的总耗时约为 **15.9 秒**（包含后处理），显著快于需要复杂聚类的 IGGT（约 1121 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了详尽测试，涵盖了从 20 类到 200 类的不同难度。
*   **消融实验**：针对 FADA 模块的作用（损失函数 vs 匹配成本）、查询交互的层数位置、联合优化 vs 冻结骨干、统一架构 vs 解耦架构等进行了多组对比实验。
*   **客观性**：实验对比了不同输入模态（纯 RGB vs RGB-D vs 点云），并使用了标准的 mAP 指标。通过在 ScanNet++ 上的零样本测试，客观地验证了模型的鲁棒性和泛化性能。

#### 6. 主要结论与发现
*   **性能领先**：SegVGGT 在纯 RGB 输入下，性能不仅远超现有的联合模型（如在 ScanNet200 上 mAP 达到 31.9），甚至在某些指标上超越了依赖 RGB-D 扫描的方法。
*   **FADA 的必要性**：实验证明，如果没有 FADA 引导，对象查询在海量 token 中会迷失，导致分割精度大幅下降（mAP 从 31.9 降至 26.7）。
*   **深度集成优势**：将实例查询嵌入 Transformer 每一层，比在末端添加解码器的解耦架构效果更好，证明了几何与语义特征的协同学习至关重要。

#### 7. 优点
*   **端到端统一性**：实现了从图像到 3D 实例掩码的一体化预测，无需复杂的 SfM 或点云后处理。
*   **高效性**：FADA 策略仅在训练时引入，**推理时零开销**，且避免了 IGGT 等方法所需的耗时聚类步骤。
*   **泛化能力强**：在未见过的 ScanNet++ 数据集上表现优异，证明模型学习到了通用的几何与语义关联。

#### 8. 不足与局限
*   **尺度缺失**：继承了 VGGT 的特性，无法直接恢复真实世界的**绝对度量尺度**（Metric Scale），这限制了其在机器人抓取等精确物理交互任务中的应用。
*   **闭集限制**：目前仍基于预定义的语义类别训练，尚未实现真正的**开放词汇（Open-vocabulary）**理解。
*   **边界噪声**：在跨数据集评估时，由于缺乏特定领域的训练数据，物体边缘的几何重建仍存在一定噪声。

（完）
