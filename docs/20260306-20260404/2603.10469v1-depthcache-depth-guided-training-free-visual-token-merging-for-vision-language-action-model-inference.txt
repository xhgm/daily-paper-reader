Title: DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference

URL Source: https://arxiv.org/pdf/2603.10469v1

Published Time: Thu, 12 Mar 2026 00:35:55 GMT

Number of Pages: 8

Markdown Content:
# DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference 

Yuquan Li 1, Lianjie Ma 1, Han Ding 2 and Lijun Zhu 1,†

Abstract — Vision-Language-Action (VLA) models enable generalist robotic manipulation but suffer from high inference latency. This bottleneck stems from the massive number of visual tokens processed by large language backbones. Existing methods either prune or merge tokens uniformly, degrading the spatial reasoning essential for robotic control. We present DepthCache, a training-free framework that leverages depth as a structural prior for visual token compression. It partitions observations into depth-based regions and applies spatially differentiated merge ratios, preserving the near-field workspace while compressing the distant background. To exploit temporal redundancy, DepthCache distributes the merging process across consecutive frames, ensuring consistent representations while reducing per-step computation. A motion-adaptive pipeline further optimizes auxiliary view compression based on end-effector dynamics. The framework requires no model mod-ification, generalizing across diverse VLA architectures. On the LIBERO benchmark, DepthCache achieves up to 1.28 ×

inference speedup with less than 1% average success rate degra-dation across three VLA models ( π0.5, OpenVLA, GR00T), whereas pruning and merging baselines incur 4–24% degra-dation at comparable compression. Real-world experiments on a physical manipulator demonstrate that DepthCache enables faster task throughput and more responsive closed-loop control in latency-sensitive scenarios. 

I. INTRODUCTION Vision-Language-Action (VLA) models have emerged as a promising paradigm for generalist robotic manipula-tion, leveraging the world knowledge encoded in pretrained vision-language backbones to map free-form language in-structions and raw visual observations directly to low-level motor commands [1], [2]. This integration, however, comes at a steep computational cost: each camera view generates hundreds of visual patch tokens that are subsequently pro-cessed by a billion-scale language model, creating inference latencies that fundamentally conflict with the real-time de-mands of reactive manipulation [3], [4]. As multi-view setups become standard for dexterous tasks, the visual token budget grows proportionally, further widening this latency gap. To bridge this gap, a growing body of work has pursued inference-time token pruning , which selectively discards vi-sual tokens that score low on task relevance, and reported en-              

> †Corresponding author.
> 1School of Artificial Intelligence and Automation, Huazhong Uni-versity of Science and Technology, Wuhan, China.

{yuquanli, yingyi1048596, ljzhu }@hust.edu.cn 

> 2School of Mechanical Science and Engineering, Huazhong University of Science and Technology, Wuhan, China. This work has been submitted to the IEEE for possible publication. Copyright may be transferred without notice, after which this version may no longer be accessible. Less
> hesitation
> More
> adaptability

Fig. 1: DepthCache enables training-free inference accel-eration for VLA models with negligible performance loss. 

Top: speedup and success rate on LIBERO across three VLA architectures. Bottom: directly transplanting token merging (ToSA, left) causes hesitation during grasping; DepthCache (right) maintains fluid execution with reduced latency. couraging speedups [5]–[8]. However, pruning is inherently lossy: regardless of how sophisticatedly tokens are scored, each discarded token severs spatial relationships with its neighbours. Recent evidence confirms that such destruction degrades fine-grained spatial reasoning in vision-language models [9], a risk amplified in manipulation where precise geometric understanding of the workspace is indispensable. Token merging , which aggregates rather than eliminates, of-fers an alternative that reduces token count while preserving aggregated information [10], [11]. Yet existing formulations operate within the vision encoder on a per-layer basis. Since different VLA models employ distinct vision encoders (e.g., SigLIP, DaViT), each requiring a tailored implementation, such methods lack cross-architecture portability. Moreover, they apply uniform merge ratios across the entire image, disregarding the spatial disparity between task-relevant near-field objects and distant backgrounds. An instructive parallel arises from human visuomotor control: during grasping, foveal vision concentrates high-acuity processing on the tar-

> arXiv:2603.10469v1 [cs.RO] 11 Mar 2026

get and its boundaries, while peripheral regions are perceived at reduced resolution—compressed but never discarded— providing the spatial context essential for online motor coordination [12]. What remains absent is a compression mechanism that, like biological vision, modulates its inten-sity both spatially and temporally : preserving full resolution where precision matters, compressing aggressively where it does not, and maintaining a stable compressed representation across consecutive observations rather than reprocessing ev-ery frame from scratch. A key observation is that robotic manipulation scenes already carry the structural signal needed for such spatially differentiated compression: the depth map , available directly from simulators or onboard RGB-D sensors. Depth naturally delineates the near-field workspace from the distant back-ground and reveals object boundaries—yet no prior work leverages this readily available signal for guiding inference-time compression; existing methods that incorporate depth treat it exclusively as a perceptual input requiring model training [13]–[15]. Building on this observation, we propose DepthCache, a training-free visual token compression framework that repurposes depth from a perceptual modality into a structural prior for region-aware token merging. Unprotected tokens are partitioned into depth-based regions, each assigned a merge ratio proportional to its distance from the workspace, compressing distant backgrounds aggressively while preserv-ing near-field detail. Merging is distributed progressively across the temporal axis, exploiting the high inter-frame visual overlap inherent in sequential robotic control rather than concentrating all reduction within a single forward pass. A dual protection mechanism, combining the language model’s cross-attention as a semantic importance signal with depth-gradient edge detection for geometric boundary preser-vation, shields task-critical tokens from any compression. For wrist-mounted cameras, a lightweight motion-adaptive state machine adjusts compression to end-effector dynamics. The entire framework operates outside the vision encoder, re-quires no model modification or retraining, and is applicable to any VLA model that accepts visual token inputs. Our contributions are summarized as follows:  

> •

We present DepthCache, a training-free framework that, to our knowledge, is the first to leverage depth as an external structural prior for spatially differentiated visual token compression in VLA inference.  

> •

We introduce a temporally coherent compression pipeline that distributes token merging across con-secutive frames and couples depth-proportional region partitioning with dual semantic-geometric protection, eliminating the inter-frame instability and uniform-ratio limitations of existing merge methods.  

> •

We validate DepthCache on three architecturally di-verse VLA models ( π0.5, OpenVLA, GR00T) across the LIBERO benchmark, achieving up to 1.28 × infer-ence speedup with less than 1% average SR degrada-tion. Real-world experiments on a physical manipulator demonstrate 1.33 × speedup across three manipulation tasks and two extended latency-sensitive scenarios. II. RELATED WORK 

Visual Token Optimization for VLA Inference. The computational bottleneck of VLA models stems largely from the volume of visual tokens processed by the LLM backbone: a single camera view typically produces 256–576 patch to-kens, and multi-view setups multiply this proportionally [3]. A growing line of work addresses this through inference-time token reduction, predominantly via pruning —selectively dis-carding tokens deemed less relevant to the current task. These methods generally derive importance scores from one of two sources: cross-modal attention between language instructions and visual tokens [5], [16], [17], or temporal consistency across consecutive frames [6], [8]. Several approaches further condition pruning on the robot’s action state, increasing retention during precision-critical phases and pruning more aggressively during coarse motion [7], [17]. However, prun-ing irreversibly discards spatial relationships among tokens. N¨ uwa [9] quantifies this risk, showing 7–47% degradation on visual grounding tasks due to disrupted spatial reference frames. This motivates the exploration of alternatives that reduce token count while preserving aggregated information. 

Depth and 3D Representations in VLA Models. Aseparate but active research direction seeks to enhance VLA models’ spatial reasoning by incorporating depth or 3D geometric information. Existing approaches broadly fall into two paradigms: explicit 3D input , where point clouds (e.g., PointVLA [13]) or depth maps [15], [18], [19] are provided as additional input channels, and implicit geometric encod-ing , where 3D priors are injected through spatial position encoding (e.g., SpatialVLA [14]), stereo feature fusion [20], or knowledge distillation from geometry-aware foundation models [21], [22]. Despite their methodological diversity, these approaches share a common assumption: depth serves as a perceptual signal—an additional modality that the model must learn to interpret. Consequently, all require pretraining or fine-tuning to acquire geometric representations, preclud-ing training-free deployment. No prior work has explored depth as an external structural prior for guiding inference-time compression decisions. 

From Pruning to Merging in VLA Settings. The in-formation loss inherent in pruning naturally motivates token merging , which reduces token count through aggregation rather than elimination. ToMe [10] established the foun-dational framework via bipartite soft matching within ViT layers, later extended to video [23] and VLA settings [24]; ToSA [11] further incorporated depth-derived spatial tokens to improve merge quality. However, these methods require architecture-specific modifications to the vision encoder and apply a uniform merge ratio that treats foreground and back-ground identically. DepthCache addresses both limitations through depth-guided, spatially differentiated compression that operates entirely outside the vision encoder without retraining (Sec. III). Pick up the black bowl in the top drawer of the wooden cabinet and place it on the plate. Compressed Visual Tokens Language Tokens 

VLA Backbone RGB Depth Primary View Pipeline (Third-Person) Scene Init 

> Warmup in N Frames Attn Protect Edge Protect
> Depth Partition
> K Regions
> Progressive Merge Change Detection

Auxiliary View Pipeline (Wrist Camera)  

> RGB Merge State
> Arm Transit / Transport
> Full-View State
> Fine Manipulation

Action Aware        

> ①Attn. protection disruption caused by the movement of the target object.
> ②The state of the fused region is broken. Complete within W steps
> Actions Re-initialization

Fig. 2: Overview of DepthCache. The primary view pipeline (top) forms a cyclic process: initialization computes dual protection sets, depth-based partitioning assigns differentiated merge ratios, and progressive merging reduces tokens across frames until scene change triggers re-initialization. The auxiliary view pipeline (bottom) employs a two-state machine to gate wrist camera compression by end-effector motion. III. METHOD 

A. Overview 

We present DepthCache, a training-free visual token re-duction framework for VLA models. Our approach is mo-tivated by a key observation: robotic manipulation scenes possess inherent structural regularities that remain largely unexploited by existing token reduction methods [10]. Specifically, we identify three such regularities: (i) spatial structure —patches residing at similar depth levels exhibit high inter-token redundancy, while distant regions carry substantially lower information density than the near-field workspace; (ii) temporal continuity —under typical manip-ulation control frequencies, the majority of scene regions remain visually static across consecutive inference steps; and 

(iii) multi-view asymmetry —wrist-mounted cameras yield motion-blurred, low-value imagery during arm transit but provide indispensable close-up views during fine manipu-lation phases. To systematically exploit these properties, DepthCache organizes its processing into two camera-specific pipelines (Fig. 2):  

> •

Primary View Pipeline (Sec. III-B): handles the third-person camera through depth-guided region partitioning, progressive cross-frame token merging, and change-triggered re-initialization.  

> •

Auxiliary View Pipeline (Sec. III-C): governs wrist cameras via a lightweight state machine that adapts token reduction to end-effector motion dynamics. 

B. Primary View: Depth-Guided Progressive Token Merging 

The primary view pipeline constitutes the core of Depth-Cache. It operates as a cyclic process: scene initialization →

depth-based partitioning → progressive merging → change detection → re-initialization , enabling continuous adaptation to evolving task phases. 

1) Scene Initialization: During the first N warmup frames, the system executes standard forward passes while collecting two complementary protection signals. 

Attention-based Semantic Protection. Since single-frame cross-attention maps can be noisy, we accumulate the LLM’s cross-attention weights over image tokens across N

warmup frames and average them over all heads to obtain a robust estimate of task-relevant regions. Patches whose aggregated attention exceeds a dynamic threshold τatt =

μ+σ, where μ and σ are the mean and standard deviation of the per-patch attention scores, form the semantic protection set Patt , identifying task-relevant entities such as the target object and receptacle. 

Depth-based Geometric Protection. We extract geomet-ric boundaries from the depth map via spatial gradient computation. Patches with depth gradients above threshold 

τedge form the geometric protection set Pedge , preserving object boundaries and occlusion contours. 

Combined Protection Set. The union P = Patt ∪ P edge 

integrates semantic and geometric signals: attention captures what matters for the task, while depth edges capture where objects are delineated . P is computed once during initial-ization and refreshed only upon re-initialization (Sec. III-B.4), avoiding per-frame attention extraction (illustrated in the initialization stage of Fig. 2). 

2) Depth-Based Region Partitioning: Following initializa-tion, unprotected patches ( i /∈ P ) are partitioned into K

regions via K-Means clustering on their depth values. Each region Rk is assigned a merge ratio that scales linearly with its mean depth ¯dk:

rk = rmin + ( rmax − rmin ) · ¯dk − dmin 

dmax − dmin 

(1) where rmin and rmax define the merge ratio range. Pro-tected tokens are assigned r=0 and remain at full resolution throughout. 

3) Progressive Token Merging: DepthCache distributes token merging across the temporal axis rather than com-pleting it within a single inference step. 

Motivation. Existing approaches such as ToMe [10] per-form all merging within one forward pass across ViT lay-ers. While effective for image classification, this one-shot paradigm raises three concerns in sequential manipulation control. First, merge group assignments are re-computed independently per frame; the resulting inter-frame incon-sistencies can cause action hesitation in dynamic control. Second, concentrating all merges in a single step com-pounds matching errors within one forward pass, whereas distributing merges across frames allows each step to benefit from fresh visual observations. Third, at typical control frequencies (5–20 Hz), consecutive frames share substantial visual overlap that can be exploited for temporally coherent token reduction. 

Merge Pair Construction. Within each depth region Rk,we construct merge pairs via bipartite soft matching [10] on patch embeddings. Cosine similarity determines greedy assignments between source and target token subsets; the target merge count per region is determined by the depth-assigned ratio rk and the region size |Rk|. These pairings remain relatively stable across frames to ensure consistent cross-frame merging topology. 

Temporal Spreading. Rather than executing all mk

merges instantaneously, DepthCache distributes them linearly over a window of W consecutive frames: 

m(t) 

> k

=

 min( t − t0, W )

W · mk



(2) where t0 denotes the first post-initialization frame and mk

the target merge count for region Rk. The per-step merge fraction η = 1 /W controls how quickly compression con-verges to its target. Each frame merges only the highest-similarity pairs scheduled for that step, using size-weighted averaging to maintain unbiased token representations across multiple merge rounds. This design is conceptually analogous to how ToMe dis-tributes merging across ViT layers—but DepthCache oper-ates outside the ViT architecture along the temporal dimen-sion, requiring no model modification and incurring no per-layer synchronization overhead (see the progressive merging stage in Fig. 2). 

Depth Variation Detection and Dynamic Recovery. 

Progressive merging must account for scene dynamics. We monitor depth variation for each patch over a sliding window: a patch is deemed static if its depth variation remains below threshold ϵ across recent frames. When the fraction of non-static patches within region Rk exceeds threshold γ, the entire region is restored to full token resolution: Restore Rk if |{ i ∈ Rk | ¬ static i}| |Rk| > γ (3) Restored regions restart progressive merging from scratch upon re-convergence. This yields adaptive behaviour: static backgrounds are gradually merged, while dynamic regions promptly recover full resolution. 

4) Re-initialization: Scene initialization is not a one-time operation. Although the target object naturally moves with the end-effector during coordinated manipulation, it may also be displaced independently—e.g., by an external perturbation or after placement at a new location. To detect such independent displacement, DepthCache monitors the mean depth change within Patt while the gripper is not 

actively transporting an object: 

¯∆d = 1

|P att |

X

> i∈P att

d(t) 

> i

− d(tinit ) 

> i

> δ reinit (4) where tinit denotes the most recent initialization frame. This condition is evaluated only when the gripper is not carrying an object, ensuring that coordinated arm–object motion does not spuriously trigger re-initialization. Upon triggering, the system re-enters initialization (Sec. III-B.1), refreshes P, re-partitions depth regions, and restarts progressive merging. 

C. Auxiliary View: Motion-Adaptive Token Reduction 

The wrist-mounted camera presents fundamentally differ-ent dynamics. During arm transit and object transport, it produces motion-blurred imagery of limited informational value; during fine manipulation, it provides close-up views indispensable for precise control. DepthCache employs a two-state machine governed jointly by gripper aperture sta-bility and end-effector motion magnitude, evaluated over the predicted action chunk to enable anticipatory switching. The system enters Merge when the gripper aperture remains sta-ble and end-effector motion is significant, indicating transit or transport phases. It transitions to Full-View when the grip-per undergoes an opening or closing transition with low end-effector motion, signaling the onset of a precision-critical grasp or release. By inspecting the entire action chunk, the system detects upcoming motion trends and switches state preemptively, eliminating temporal lag. 

D. Overall Token Budget 

After progressive merging converges, typically within W

frames, the system reaches a steady-state token retention ratio. In our dual-camera configuration, the combined 512 TABLE I MAIN RESULTS ON LIBERO SIMULATION BENCHMARK .                                                                                              

> Success Rate (%) Efficiency Method Spatial Object Goal Long Avg. Speedup ρ
> OpenVLA 83.3 91.2 78.6 53.7 76.7 1.00 ×100 OpenVLA + FastV 75.8 70.3 67.3 42.5 64.0 −12 .71.39 ×75.0 OpenVLA + SP-VLA †80.0 82.4 73.6 51.6 71.9 −4.81.50 ×dyn. ‡
> OpenVLA + DepthCache 83.1 90.7 77.8 51.2 75.7 −1.01.21 ×78.9
> π0.599.0 97.0 99.0 96.5 97.9 1.00 ×100
> π0.5+ FastV 73.4 89.4 79.1 68.5 77.6 −20 .31.30 ×68.0
> π0.5+ ToSA 79.3 82.0 71.6 62.4 73.8 −24 .10.94 ×57.8
> π0.5+ DepthCache 98.9 97.1 98.3 96.1 97.6 −0.31.28 ×68.2 GR00T 91.7 97.6 93.0 90.2 93.1 1.00 ×100 GR00T + DepthCache 93.3 96.4 92.5 89.6 92.9 −0.21.07 ×87.5 All methods are training-free and require no model modification. ρ: token retention ratio (lower =more reduction). †: cited from the original publication.
> ‡:ρvaries dynamically with end-effector velocity.

patch tokens are reduced to approximately 300 in steady state, yielding substantial reductions in VLM computation while preserving task-critical visual information. IV. EXPERIMENTS We evaluate DepthCache across multiple VLA architec-tures on both simulated and real-world manipulation tasks. Our experiments are designed to answer the following re-search questions:  

> •

RQ1 : Can DepthCache reduce VLA inference latency while preserving task success rates across different model architectures?  

> •

RQ2 : Does DepthCache transfer effectively to real-world robotic manipulation?  

> •

RQ3 : What is the contribution of each design compo-nent to the overall performance? 

A. Experimental Setup 

Simulation Benchmark. We evaluate on LIBERO [25], a widely adopted manipulation benchmark comprising four task suites (Spatial, Object, Goal, Long) with 40 tasks of increasing difficulty. Each task is evaluated over 100 episodes; we report the average success rate (SR) per suite and the grand average. 

VLA Baselines. To validate the generality of DepthCache as a model-agnostic compression framework, we apply it to three architecturally diverse VLA models: (1) π0.5 [2] (3.3B), pairing a SigLIP vision encoder with a Gemma language backbone and a flow-matching action expert; (2) Open-VLA [26] (7B), fusing SigLIP and DINOv2 encoders with a Llama 2 backbone for autoregressive action prediction; and (3) GR00T [27] (2.2B), adopting an Eagle vision-language model with a Diffusion Transformer action head. These models differ in vision encoders, language backbones, and action generation paradigms, providing a rigorous testbed for cross-architecture generality. All checkpoints are sourced from Hugging Face and fine-tuned on LIBERO. 

Metrics. We report: (i) task success rate (SR, %), (ii) end-to-end inference latency per action step (ms), (iii) inference speedup relative to the uncompressed baseline (×), and (iv) average token compression ratio ( ρ, fraction of tokens retained after compression). Latency is measured as the wall-clock time from image observation to action output, averaged over all evaluation episodes. 

Real-World Platform. For real-world validation, we de-ploy on a PIPER 6-DoF manipulator with two Intel Re-alSense D435 RGB-D cameras providing an external static view and a wrist-mounted eye-in-hand view. Depth obser-vations are acquired directly from the onboard stereo depth sensor. 

Implementation Details. Both simulation and real-world experiments are conducted on a single NVIDIA RTX 4090 GPU (24 GB). Unless otherwise noted, DepthCache uses 

N =5 warmup frames, K=3 depth clusters, and a progressive merge window of W =5 steps. 

B. Simulation Results 

To contextualize DepthCache’s design, we compare against baselines spanning pruning and merging paradigms (Table I). For pruning, we evaluate FastV [28] on both 

π0.5 and OpenVLA, and include SP-VLA [17] results on OpenVLA. FastV drops average SR by 20.3% on π0.5 and 12.7% on OpenVLA, with particularly severe losses on spatial tasks (Spatial −25 .6% on π0.5; Object −20 .9% on OpenVLA). SP-VLA, which adds action-aware scheduling to semantic pruning, achieves a higher speedup ( 1.50 ×)but still incurs a 4.8% average SR loss. These results confirm that pruning visual tokens, regardless of the selection criterion, systematically degrades manipulation performance by destroying spatial context. We note that ADP [7] aug-ments pruning with action-aware gating on OpenVLA-OFT, a parallel-decoding variant with a different baseline, preclud-ing direct numerical comparison; its pruning core is the same mechanism evaluated via FastV. For merging, ToSA [11] applies uniform intra-ViT merging on π0.5, reducing SR by 24.1% with no speedup ( 0.94 ×) due to integration overhead. Three structural limitations explain this result: a uniform merge ratio across all patches, per-frame re-partitioning that TABLE II REAL -WORLD CORE TASK RESULTS WITH π0.5 .              

> Task Successes / 20 Baseline + Ours Pick & Place 20 / 20 20 / 20 Stack Blocks 18 / 20 17 / 20 Drawer & Place 17 / 20 15 / 20 Total 55 / 60 52 / 60 Avg. Latency (ms) 191 143 Speedup 1.00 ×1.33 ×

introduces temporal discontinuity, and frequent merging of the gripper with the target object during grasping. 

Cross-architecture results. Across all three architectures, DepthCache achieves 1.07 –1.28 × speedup with less than 

1% average SR loss. In every case, DepthCache loses less than 1% average SR while all pruning and merging baselines exceed 4% degradation at comparable or lower ρ.

C. Real-World Experiments 

To validate that DepthCache transfers to physical ma-nipulation, we evaluate on five real-world tasks using a PIPER robotic arm with dual RealSense D435 cameras (Sec. IV-A). We organize the evaluation into two tiers: three core tasks that assess fundamental manipulation competence under compression, and two extended scenarios that probe the practical benefits of reduced inference latency. 

1) Core Tasks: Task Design. We select three tasks that exercise distinct aspects of DepthCache: 1) Pick & Place : grasp a cube from the table and place it at a designated bin. Tests basic manipulation robustness under token compression. 2) Stack Blocks : pick up a cube and stack it on top of another cube. Requires precise spatial alignment, ex-ercising the dual protection mechanism that preserves object boundaries during fine positioning. 3) Open Drawer & Place : open a drawer, grasp an object, and place it inside. A multi-phase task whose scene-structure changes (drawer opening, object relo-cation) exercise the depth variation detection mecha-nism, testing whether token reduction adapts correctly across distinct manipulation phases. Each task is evaluated over 20 trials for both the baseline and DepthCache-augmented π0.5 policy. As shown in Table II, DepthCache maintains comparable success rates to the uncompressed baseline across all three core tasks (Fig. 3), with an average inference speedup of 1.33 ×.

2) Extended Scenarios: Sequential Multi-Object Sort-ing. We place 3 colored cubes on the table and instruct the robot to sort them into designated bin one by one. This task chains multiple pick-and-place cycles, accumulating latency differences into a visible gap in total task completion time (Fig. 4). Each trial is recorded as a side-by-side synchronized D435  

> (a) Stack Blocks D435 (b) Open Drawer & Place

Fig. 3: Real-world core task execution with DepthCache-augmented π0.5. Perturbation recovery is shown in Fig. 1. video; we report the average total elapsed time across 15 trials. 

Perturbation Recovery. During a pick-and-place trial, while the arm is moving toward the target cube, we manually push the cube approximately 2–3 cm away from its original position. The reduced inference latency provided by Depth-Cache enables more frequent re-planning, allowing the policy to detect and correct for the displacement more promptly. As shown in Table III, DepthCache reduces multi-object sorting time by 22.7% (28.6 s → 22.1 s) while maintaining a comparable success rate, confirming that per-step inference savings accumulate into a substantial throughput gain over repeated manipulation cycles. In the perturbation scenario, the faster re-planning loop improves recovery time by 21.3% (17.4 s → 13.7 s) and slightly raises the success rate (11/15 

→ 12/15), indicating that the policy benefits from receiving corrective observations sooner. 

D. Ablation Study 

We conduct ablation experiments on LIBERO using π0.5

to quantify the contribution of each component. 

1) Component Analysis: Table IV shows that the two 

spatial mechanisms, Depth Partitioning and Progressive Merge, have the largest impact on task performance ( ∆ = 

−18 .2% and −16 .6% ), while the remaining components serve complementary roles in safety, adaptation, and multi-view acceleration. Across all ablations, Object and Long suites are consistently the most affected, as both require sustained precision over fine-grained spatial features and are therefore most sensitive to token information loss. Depth Partitioning is the single most critical compo-nent. Replacing depth-proportional allocation with a uni-form merge ratio removes the inductive bias that near-field tokens carry denser task-relevant information than distant TABLE III EXTENDED SCENARIO RESULTS (15 TRIALS EACH ).              

> Scenario Successes / 15 Time (s) Baseline + Ours Baseline + Ours Multi-Object Sorting 15 / 15 13 / 15 28.6 22.1 Perturbation Recovery 11 / 15 12 / 15 17.4 13.7

With DepthCache         

> Initial 3s 6s 9s 12s 15s 18s 21s 24s

Baseline Only           

> Initial 3s 6s 9s 12s 15s 18s 21s 24s 27s 29s

Fig. 4: Sequential multi-object sorting comparison. Top: baseline π0.5. Bottom: π0.5 + DepthCache. The time gap accumulates across successive pick-and-place cycles. background. Without this prior, compression erodes gripper-object geometry and target features equally, causing severe failure on Object ( −22 .0% ) and Long ( −25 .0% ). Progressive Merge ranks second: one-shot application of the full merge ratio introduces a step discontinuity in the visual condition-ing signal, which is especially destructive for Object tasks (−29 .0% ) where grasp trajectories depend on smooth inter-chunk feature consistency. Among the remaining components, Dual Protection re-moval yields a moderate SR drop ( −7.3% ) while increas-ing speedup to 1.48 ×, confirming that protection trades compression headroom for safety. Re-initialization primarily benefits Spatial ( −7.1% ) and Long ( −6.7% ) tasks where scene geometry evolves during execution, with negligible effect on static-scene Object tasks. The Auxiliary View Pipeline contributes a speedup gain from 1.06 × to 1.28 ×

with near-zero SR impact ( +0 .2% ), validating the auxiliary-view state machine as a safe acceleration mechanism. 

2) Parameter Sensitivity: We investigate the sensitivity of DepthCache to its two key hyperparameters (Fig. 5). 

Maximum merge ratio rmax modulates the overall com-pression rate via the depth-proportional mapping rk ∈

[rmin , r max ]. Across a broad range ( rmax ∈ [0 .3, 0.7] ), SR remains nearly invariant, demonstrating a wide and flat safe operating plateau. This robustness stems from the dual 0.3 0.4 0.5 0.6 0.7 0.8 0.9                         

> Maximum Merge Ratio rmax
> 92
> 93
> 94
> 95
> 96
> 97
> 98
> 99
> Avg. Success Rate (%)
> Low Efficiency
> Zone
> Accuracy
> Drop Zone
> (a) Effect of rmax on SR and Speedup
> Avg. SR (%)
> Speedup ( £)
> Default
> rmax = 0 :7
> 97.6%
> 0.05 0.10 0.15 0.20 0.30 0.50 1.00
> Progressive Merge Step Ratio ´(log scale)
> 80.0
> 82.5
> 85.0
> 87.5
> 90.0
> 92.5
> 95.0
> 97.5
> 100.0
> Avg. Success Rate (%)
> Slow Convergence Performance Cliff
> (b) Effect of ´on SR (stability)
> Default
> ´= 0 :2,W= 5
> One-shot merge
> (no progressive)
> 97.6%
> 81.0%
> 94.0%
> 1.0
> 1.1
> 1.2
> 1.3
> 1.4
> 1.5
> Speedup ( £)
> 1.28x
> W=20 W=10 W=7 W=5 W=3 W=2 W=1

Fig. 5: Parameter sensitivity analysis on π0.5 (LIBERO). (a) Effect of maximum merge ratio rmax on success rate and speedup. (b) Effect of progressive merge step ratio η on success rate (log-scale x-axis). Default values ( rmax = 0 .7,

η = 0 .2) are highlighted. protection mechanism, which unconditionally shields task-critical tokens from merging, and the depth-proportional as-signment, which concentrates compression on low-value dis-tant regions. Degradation appears only beyond rmax ≈ 0.7,where even the moderately compressed near-field regions begin to lose spatial resolution. The default rmax = 0 .7 thus represents the Pareto-optimal trade-off: maximum speedup at the boundary of the safe zone. 

Progressive merge step ratio η controls the transition speed to target compression ( W ≈ 1/η frames). In VLA inference, each action chunk is conditioned on visual tokens from the preceding step; a step change in token count between chunks introduces a discontinuity in the condition-ing signal, disrupting the smoothness of generated trajecto-ries. Progressive merging amortizes this transition, allowing consecutive action chunks to be conditioned on gradually varying inputs. At the extreme ( η = 1 .0, one-shot merge), the system degenerates to the w/o Progressive Merge ablation (SR 81 .0% ), confirming that temporal smoothing is essential rather than optional. V. CONCLUSION We showed that depth, commonly treated as a perceptual input for VLA models, can instead serve as a structural prior for inference-time visual token compression. The result-ing framework, DepthCache, applies spatially differentiated and temporally coherent merging without model modifica-tion, generalizing across three architecturally distinct VLA backbones with less than 1% success rate degradation on the LIBERO benchmark. Real-world experiments further confirm that inference savings translate into faster task throughput and more responsive closed-loop control on a physical manipulator. Two principal limitations remain. First, as a purely inference-time method, DepthCache compresses visual tokens but does not accelerate the action decoding stage (e.g., flow-matching denoising), bounding achievable speedup by Amdahl’s law. Second, our evaluation covers three VLA architectures on the LIBERO benchmark and a single 6-DoF arm; extending to more diverse benchmarks, embodiments, and real-world settings would further validate TABLE IV ABLATION STUDY ON DEPTH CACHE COMPONENTS USING π0.5 .

Configuration Success Rate (%) Efficiency Spatial Object Goal Long Avg. ∆ Speedup ρ

Full DepthCache 98.9 97.1 98.3 96.1 97.6 — 1.28 × 68.2 w/o Depth Partitioning 92.9 75.1 78.4 71.1 79.4 -18.2 1.34 × 67.5 w/o Dual Protection 96.0 85.7 92.1 87.2 90.3 -7.3 1.48 × 55.6 w/o Progressive Merge 94.7 68.1 82.5 78.6 81.0 -16.6 1.32 × 65.4 w/o Re-initialization 91.8 97.2 92.6 89.4 92.8 -4.8 1.25 × 72.5 w/o Auxiliary View Pipeline 98.2 98.0 98.6 96.2 97.8 +0.2 1.06 × 88.0 Each row removes one component from the full method. ∆: change in average SR relative to the full method (percentage points). 

generality. Incorporating depth-aware compression into the training pipeline and combining token merging with com-plementary strategies such as KV-cache optimization are promising directions for lifting these ceilings. REFERENCES [1] K. Black, N. Brown, D. Driess, A. Esmail, M. Equi, C. Finn, N. Fusai, L. Groom, K. Hausman, B. Ichter et al. , “ π0: A vision-language-action flow model for general robot control,” arXiv preprint arXiv:2410.24164 , 2024. [2] K. Black, N. Brown, J. Darpinian, K. Dhabalia, D. Driess, A. Esmail, M. Equi, C. Finn, N. Fusai, M. Y. Galliker et al. , “ π0.5: A vision-language-action model with open-world generalization,” in Annual Conference on Robot Learning (CoRL) , 2025. [3] Z. Yu, B. Wang, P. Zeng, H. Zhang, J. Zhang, Z. Wang, L. Gao, J. Song, N. Sebe, and H. T. Shen, “A survey on efficient vision-language-action models,” arXiv preprint arXiv:2510.24795 , 2025. [4] K. Black, M. Y. Galliker, and S. Levine, “Real-time execution of action chunking flow policies,” in Advances in Neural Information Processing Systems (NeurIPS) , 2025. [5] Y. Yang, Y. Wang, Z. Wen, L. Zhong, C. Zou, Z. Zhang, C. Wen, and L. Zhang, “EfficientVLA: Training-free acceleration and compression for vision-language-action models,” in Advances in Neural Information Processing Systems (NeurIPS) , 2025. [6] H. Wang, J. Xu, Y. Xiang, J. Pan, Y. Zhou, Y.-L. Li, and G. Dai, “SpecPrune-VLA: Accelerating vision-language-action mod-els via action-aware self-speculative pruning,” arXiv preprint arXiv:2509.05614 , 2025. [7] X. Pei, Y. Chen, S. Xu, Y. Wang, Y. Shi, and C. Xu, “Action-aware dynamic pruning for efficient vision-language-action manipulation,” in 

International Conference on Learning Representations (ICLR) , 2026. [8] S. Xu, Y. Wang, C. Xia, D. Zhu, T. Huang, and C. Xu, “VLA-Cache: Efficient vision-language-action manipulation via adaptive to-ken caching,” in Advances in Neural Information Processing Systems (NeurIPS) , 2025. [9] Y. Huang, F. Ma, Y. Shao, J. Guo, Z. Yu, L. Cui, and Q. Tian, “N¨ uwa: Mending the spatial integrity torn by VLM token pruning,” in 

International Conference on Learning Representations (ICLR) , 2026. [10] D. Bolya, C.-Y. Fu, X. Dai, P. Zhang, C. Feichtenhofer, and J. Hoff-man, “Token merging: Your ViT but faster,” in International Confer-ence on Learning Representations (ICLR) , 2023. [11] H.-W. Huang, W. Chai, K.-M. Chen, C.-Y. Yang, and J.-N. Hwang, “ToSA: Token merging with spatial awareness,” in IEEE/RSJ Inter-national Conference on Intelligent Robots and Systems (IROS) , 2025, pp. 9654–9660. [12] M. A. Goodale and A. D. Milner, “Action without perception in human vision,” Cognitive Neuropsychology , vol. 25, no. 7-8, pp. 891–919, 2008. [13] C. Li, J. Wen, Y. Peng, Y. Peng, F. Feng, and Y. Zhu, “PointVLA: Injecting the 3D world into vision-language-action models,” arXiv preprint arXiv:2503.07511 , 2025. [14] D. Qu, H. Song, Q. Chen, Y. Yao, X. Ye, Y. Ding, Z. Wang, J. Gu, B. Zhao, D. Wang et al. , “SpatialVLA: Exploring spa-tial representations for visual-language-action model,” arXiv preprint arXiv:2501.15830 , 2025. [15] T. Yuan, Y. Liu, C. Lu, Z. Chen, T. Jiang, and H. Zhao, “Depthvla: Enhancing vision-language-action models with depth-aware spatial reasoning,” arXiv preprint arXiv:2510.13375 , 2025. [16] X. Tan, Y. Yang, P. Ye, J. Zheng, B. Bai, X. Wang, J. Hao, and T. Chen, “Think twice, act once: Token-aware compression and action reuse for efficient inference in vision-language-action models,” arXiv preprint arXiv:2505.21200 , 2025. [17] Y. Li, Y. Meng, Z. Sun, K. Ji, C. Tang, J. Fan, X. Ma, S.-T. Xia, Z. Wang, and W. Zhu, “SP-VLA: A joint model scheduling and token pruning approach for VLA model acceleration,” in International Conference on Learning Representations (ICLR) , 2026. [18] V. Bhat, Y.-H. Lan, P. Krishnamurthy, R. Karri, and F. Khor-rami, “3D CAVLA: Leveraging depth and 3D context to generalize vision language action models for unseen tasks,” arXiv preprint arXiv:2505.05800 , 2025. [19] H. Zhen, X. Qiu, P. Chen, J. Yang, X. Yan, Y. Du, Y. Hong, and C. Gan, “3D-VLA: A 3D vision-language-action generative world model,” in 

Proc. International Conference on Machine Learning (ICML) , 2024, pp. 61 229–61 245. [20] S. Deng, M. Yan, Y. Zheng, J. Su, W. Zhang, X. Zhao, H. Cui, Z. Zhang, and H. Wang, “Stereovla: Enhancing vision-language-action models with stereo vision,” arXiv preprint arXiv:2512.21970 , 2025. [21] M. Guo, M. Cao, J. Tao, R. Xu, Y. Yan, X. Liang, I. Laptev, and X. Chang, “Glad: Geometric latent distillation for vision-language-action models,” arXiv preprint arXiv:2512.09619 , 2025. [22] Q. Fan, Z. Li, Y. Lu, W. Chen, Q. Shen, X.-x. Long, Y. Cai, T. Lu, S. Wang, and X. Cao, “PEAfowl: Perception-enhanced multi-view vision-language-action for bimanual manipulation,” arXiv preprint arXiv:2601.17885 , 2026. [23] J. Choi, S. Lee, J. Chu, M. Choi, and H. J. Kim, “vid-tldr: Training free token merging for light-weight video transformer,” in Proc. IEEE/CVF Conf. Computer Vision and Pattern Recognition (CVPR) , 2024, pp. 18 771–18 781. [24] Y. Ye, J. Ma, J. Cen, and Z. Lu, “Token expand-merge: Training-free token compression for vision-language-action models,” arXiv preprint arXiv:2512.09927 , 2025. [25] B. Liu, Y. Zhu, C. Gao, Y. Feng, Q. Liu, Y. Zhu, and P. Stone, “LIBERO: Benchmarking knowledge transfer for lifelong robot learning,” in Advances in Neural Information Processing Systems (NeurIPS) , vol. 36, 2023, pp. 44 776–44 791. [26] M. J. Kim, K. Pertsch, S. Karamcheti, T. Xiao, A. Balakrishna, S. Nair, R. Rafailov, E. P. Foster, P. R. Sanketi, Q. Vuong, T. Kollar, B. Burchfiel, R. Tedrake, D. Sadigh, S. Levine, P. Liang, and C. Finn, “OpenVLA: An open-source vision-language-action model,” in Proc. Conference on Robot Learning (CoRL) , 2025, pp. 2679–2713. [27] J. Bjorck, F. Casta˜ neda, N. Cherniadev, X. Da, R. Ding, L. Fan, Y. Fang, D. Fox, F. Hu, S. Huang et al. , “GR00T N1: An open foundation model for generalist humanoid robots,” arXiv preprint arXiv:2503.14734 , 2025. [28] L. Chen, H. Zhao, T. Cai, Y. Cao, Y. Chen, and Y. Lu, “An image is worth 1/2 tokens after layer 2: Plug-and-play inference acceleration for large vision-language models,” in European Conference on Computer Vision (ECCV) , 2024.