---
title: Toward Unified Multimodal Representation Learning for Autonomous Driving
title_zh: 迈向自动驾驶的统一多模态表示学习
authors: "Ximeng Tao, Dimitar Filev, Gaurav Pandey"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07874v1"
tags: ["query:q20"]
score: 9.0
evidence: 自动驾驶的统一多模态表示学习
tldr: 针对自动驾驶中多模态对齐不一致的问题，本文提出了一种对比张量预训练（CTP）框架。该框架通过将传统的成对余弦相似度矩阵扩展为多模态相似度张量，并引入张量损失函数，实现了文本、图像和点云在统一嵌入空间中的联合对齐。实验表明，该方法在3D编码器对齐和从头预训练任务中均表现优异，显著提升了端到端自动驾驶的场景理解能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07874v1/fig-001.webp\", \"caption\": \"Fig. 2. Overview of the multimodal language model–based end-to-end autonomous driving system. The multimodal encoder is pretrained to align all modalities within a unified embedding space, enabling the LLM to jointly understand cross-modal information and generate reasoning, scene descriptions, and future trajectory predictions. In this work, we primarily focus on training the “Multimodal Encoder” that can be used in an endto-end driving system as shown here.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.07874v1/fig-002.webp\", \"caption\": \"TABLE V COMPARISON OF DIFFERENT METHODS FOR MEASURING SIMILARITY\", \"page\": 6, \"index\": 2, \"width\": 508, \"height\": 253}, {\"url\": \"assets/figures/arxiv/2603.07874v1/fig-003.webp\", \"caption\": \"Fig. 3. Overview of CTP framework. Triplet dataset: LiDAR point clouds, cropped images, and annotations are extracted from autonomous driving datasets to form triplet samples. The annotation expanded into a detailed caption using a VLM. similarity tensor: Image, text, and point cloud features are arranged along the x, y, and z axes to form a 3D similarity tensor. Each element represents a unique combination of three features, and the similarity measures their relationships. During training, the similarity scores of the matched triplets (small purple cubes) are maximized using a cross-entropy loss.\", \"page\": 3, \"index\": 3, \"width\": 512, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2603.07874v1/fig-004.webp\", \"caption\": \"TABLE I ZERO-SHOT CLASSIFICATION ACCURACY (%) ON THE NUSCENES TRIPLET DATASET\", \"page\": 5, \"index\": 4, \"width\": 958, \"height\": 180}]"
motivation: 传统的成对相似度对齐方法仅考虑模态两两之间的关系，难以确保所有模态在统一空间内实现全局一致的对齐。
method: 提出CTP框架，将2D相似度矩阵扩展为多模态相似度张量，并引入张量损失以实现跨模态的联合对比学习。
result: 在构建的文本-图像-点云三元组数据集上，该方法在对齐现有编码器和从头预训练两种场景下均取得了优于成对对齐的效果。
conclusion: 通过张量化方法实现统一的多模态表征学习，能有效提升自动驾驶系统对复杂场景的理解能力。
---

## 摘要
对比语言-图像预训练 (CLIP) 在对齐视觉和文本表示方面表现出了卓越的性能。最近的研究已将这一范式扩展到 3D 视觉，以提升自动驾驶的场景理解能力。一种常见的策略是利用模态间的成对余弦相似度来指导 3D 编码器的训练。然而，仅考虑单个模态对之间的相似度，而非联合考虑所有模态，无法确保整个多模态空间内一致且统一的对齐。在本文中，我们提出了一种对比张量预训练 (CTP) 框架，该框架在统一的嵌入空间中同时对齐多个模态，以增强端到端自动驾驶。与成对余弦相似度对齐相比，我们的方法将 2D 相似度矩阵扩展为多模态相似度张量。此外，我们引入了一种张量损失，以实现跨所有模态的联合对比学习。为了对我们的框架进行实验验证，我们基于现有的自动驾驶数据集构建了一个文本-图像-点云三元组数据集。结果表明，我们提出的统一多模态对齐框架在以下两种场景中均取得了良好的性能：(i) 将 3D 编码器与预训练的 CLIP 编码器对齐，以及 (ii) 从头开始预训练所有编码器。

## Abstract
Contrastive Language-Image Pre-training (CLIP) has shown impressive performance in aligning visual and textual representations. Recent studies have extended this paradigm to 3D vision to improve scene understanding for autonomous driving. A common strategy is to employ pairwise cosine similarity between modalities to guide the training of a 3D encoder. However, considering the similarity between individual modality pairs rather than all modalities jointly fails to ensure consistent and unified alignment across the entire multimodal space. In this paper, we propose a Contrastive Tensor Pre-training (CTP) framework that simultaneously aligns multiple modalities in a unified embedding space to enhance end-to-end autonomous driving. Compared with pairwise cosine similarity alignment, our method extends the 2D similarity matrix into a multimodal similarity tensor. Furthermore, we introduce a tensor loss to enable joint contrastive learning across all modalities. For experimental validation of our framework, we construct a text-image-point cloud triplet dataset derived from existing autonomous driving datasets. The results show that our proposed unified multimodal alignment framework achieves favorable performance for both scenarios: (i) aligning a 3D encoder with pretrained CLIP encoders, and (ii) pretraining all encoders from scratch.

---

## 论文详细总结（自动生成）

### 论文总结：迈向自动驾驶的统一多模态表示学习 (CTP)

#### 1. 核心问题与整体含义
*   **研究动机**：虽然 CLIP 在视觉-文本对齐上取得了巨大成功，且已有研究将其扩展到 3D 点云领域，但现有的多模态对齐方法大多依赖于**成对（Pairwise）**的相似度计算（如文本-图像、文本-点云、图像-点云）。
*   **核心问题**：这种成对对齐方式仅考虑了模态间的局部关系，缺乏全局一致性，无法确保所有模态在统一的嵌入空间中实现真正的联合对齐。这限制了自动驾驶系统在处理图像、点云和文本等异构输入时的综合理解能力。

#### 2. 方法论
*   **核心思想**：提出**对比张量预训练（Contrastive Tensor Pre-training, CTP）**框架，将传统的 2D 相似度矩阵扩展为 **n 维相似度张量**（本文重点讨论 3D 张量：文本-图像-点云），实现多模态的同步对齐。
*   **关键技术细节**：
    *   **相似度张量构建**：将三个模态的特征向量排列在张量的三个轴上，每个元素代表三个特征的组合相似度。
    *   **相似度度量**：对比了余弦相似度和 **L2 范数相似度**。研究发现，在单位超球面上，L2 范数相似度在衡量多个向量间的距离时比余弦相似度更有效。
    *   **张量损失（Tensor Loss）**：引入“平面损失（Plane Loss）”，将张量沿某一轴切片成平面并展平为向量，通过交叉熵损失最大化匹配三元组的相似度。
    *   **掩码策略（Masking Strategy）**：在展平张量平面时，屏蔽掉包含重复特征的条目，以降低计算复杂度并避免优化干扰。
    *   **三元组数据增强**：利用 VLM（如 Qwen3-VL）将原始简短的类别标注扩展为详细的描述性伪标题（Pseudo Captions），增强语义丰富度。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：基于 **nuScenes** 构建的文本-图像-点云三元组数据集（约 32.2 万组）。
    *   **测试集**：nuScenes 验证集、**KITTI** 以及 **Waymo Open Perception Dataset (WOD-P)**，用于评估模型的零样本（Zero-shot）分类能力和泛化性。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括 PointCLIP, PointCLIP V2, CLIP2Point（基于深度图的方法），以及 LidarCLIP, CLIP² 和 ULIP（基于对比学习的方法）。
    *   **实验场景**：(i) 冻结 CLIP 编码器仅训练点云编码器；(ii) 从头联合预训练所有模态编码器。

#### 4. 资源与算力
*   **仅训练点云编码器**：使用 **1 张 NVIDIA RTX 4090 GPU**，训练 20 个 epoch，Batch Size 为 192。
*   **预训练所有编码器**：使用 **2 张 NVIDIA A100 40G GPU**，训练 10 个 epoch，总 Batch Size 为 384。
*   **优化器**：采用 AdamW，初始学习率为 $5 \times 10^{-4}$，权重衰减为 0.2。

#### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流自动驾驶数据集上进行了广泛测试，涵盖了从单模态到多模态输入的多种组合。
*   **消融实验**：针对相似度度量方式（L2 vs Cosine）、张量展平策略（有无 Masking）进行了对比实验。
*   **充分性评价**：实验设计较为客观，通过在未见过的 KITTI 和 Waymo 数据集上进行测试，充分验证了模型的跨数据集泛化能力。对比基准（如 ULIP 和 CLIP²）均在相同数据集上进行了重新训练，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能提升**：在 nuScenes 上，CTP 在仅训练点云编码器时比成对对齐方法提升了 **5.42%**；在全编码器预训练场景下，提升幅度高达 **13.91%**。
*   **泛化能力**：在 KITTI 数据集上，全预训练模式下的 CTP 比 ULIP 提升了 **40.87%**，显示出极强的迁移能力。
*   **度量优势**：实验证明 L2 范数相似度在多模态张量对齐中显著优于传统的余弦相似度。
*   **联合对齐**：将所有模态向同一点对齐（CTP 模式）比两两对齐更高效，能学习到更稳健的全局表征。

#### 7. 优点
*   **理论创新**：打破了 CLIP 范式中长期的“成对对齐”思维定式，引入张量化方法实现真正的多模态联合学习。
*   **实用性强**：针对自动驾驶场景中点云稀疏、噪声大的特点，通过 VLM 增强文本语义，有效提升了 3D 场景理解。
*   **灵活性**：框架易于扩展到更多模态（如 Radar、音频等），只需增加张量的维度。

#### 8. 不足与局限
*   **计算开销**：随着模态数量 $q$ 的增加，张量大小按 $b^q$ 指数级增长，尽管有掩码策略，但在极多模态场景下的计算压力仍是挑战。
*   **数据依赖**：高度依赖于高质量的三元组数据，目前这类数据集仍需通过现有数据集人工合成或利用 VLM 生成，可能存在伪标题生成的偏差风险。
*   **类别不平衡**：实验中提到某些少数类别（如工程车、摩托车）的识别率仍然较低，受限于原始数据集的分布。

（完）
