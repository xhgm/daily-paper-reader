---
title: Uncertainty-aware Prototype Learning with Variational Inference for Few-shot Point Cloud Segmentation
title_zh: 基于变分推理的不确定性感知原型学习用于少样本点云分割
authors: "Yifei Zhao, Fanyu Zhao, Yinsheng Li"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19757v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 少样本三维点云语义分割
tldr: 针对少样本3D点云语义分割中确定性原型难以捕捉监督信息稀缺带来的不确定性问题，本文提出了不确定性感知原型学习（UPL）框架。该方法通过双流原型细化模块整合支持集与查询集信息，并将原型学习建模为变分推理问题，将类别原型视为潜在变量。这种概率化建模实现了显式的不确定性估计，显著提升了模型的鲁棒性和泛化能力，在ScanNet和S3DIS基准测试中达到了SOTA水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19757v1/fig-001.webp\", \"caption\": \"Fig. 3. Visualization of uncertainty analysis for the 1-way 1-shot setting. The left panel highlights the foreground class window (in blue), while the right panel focuses on the class chair (in red). Uncertainty is represented using a heatmap, where values transition from blue (low uncertainty) to red (high uncertainty). The number following @ denotes the sampling times T .\", \"page\": 4, \"index\": 1, \"width\": 950, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2603.19757v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of our UPL framework. UPL consists of two primary modules: (i) the Dual-stream Prototype Refinement (DPR) module, which enhances prototype discriminability by leveraging mutual information between support and query sets, and (ii) the Variational Prototype Inference Regularization(VPIR) module, which models class prototypes as latent variables to capture uncertainty and enable probabilistic inference. The solid lines indicate the training process, while the dashed lines represent the inference process.\", \"page\": 2, \"index\": 2, \"width\": 898, \"height\": 448}]"
motivation: 现有的确定性原型方法无法捕捉少样本场景下因监督信息匮乏而产生的内在不确定性，导致模型鲁棒性和泛化能力受限。
method: 提出UPL框架，通过双流模块融合支持与查询信息细化原型，并利用变分推理将原型建模为潜在变量以实现显式不确定性建模。
result: 在ScanNet和S3DIS基准测试中，该方法在不同设定下均取得了当前最先进的性能，并提供了可靠的不确定性估计。
conclusion: 通过引入概率化建模和变分推理，UPL有效解决了少样本点云分割中的不确定性问题，显著增强了模型的鲁棒性。
---

## 摘要
少样本3D语义分割旨在仅利用少量标注的支持样本，为查询点云生成准确的语义掩码。现有的基于原型的方法通常从支持集中构建紧凑且确定性的原型，以指导查询分割。然而，这种僵化的表示形式无法捕捉由稀疏监督引入的内在不确定性，这往往导致鲁棒性下降和泛化能力受限。在这项工作中，我们提出了 UPL（Uncertainty-aware Prototype Learning），这是一种旨在将不确定性建模融入少样本3D分割原型学习中的概率方法。我们的框架引入了两个关键组件：首先，UPL 引入了一个双流原型细化模块，通过联合利用来自支持样本和查询样本的有限信息来丰富原型表示；其次，我们将原型学习公式化为一个变分推理问题，将类别原型视为隐变量。这种概率公式化实现了显式的不确定性建模，提供了鲁棒且可解释的掩码预测。在广泛使用的 ScanNet 和 S3DIS 基准测试上进行的大量实验表明，我们的 UPL 在不同设置下均实现了一致的最先进性能，同时提供了可靠的不确定性估计。代码可在 https://fdueblab-upl.github.io/ 获取。

## Abstract
Few-shot 3D semantic segmentation aims to generate accurate semantic masks for query point clouds with only a few annotated support examples. Existing prototype-based methods typically construct compact and deterministic prototypes from the support set to guide query segmentation. However, such rigid representations are unable to capture the intrinsic uncertainty introduced by scarce supervision, which often results in degraded robustness and limited generalization. In this work, we propose UPL (Uncertainty-aware Prototype Learning), a probabilistic approach designed to incorporate uncertainty modeling into prototype learning for few-shot 3D segmentation. Our framework introduces two key components. First, UPL introduces a dual-stream prototype refinement module that enriches prototype representations by jointly leveraging limited information from both support and query samples. Second, we formulate prototype learning as a variational inference problem, regarding class prototypes as latent variables. This probabilistic formulation enables explicit uncertainty modeling, providing robust and interpretable mask predictions. Extensive experiments on the widely used ScanNet and S3DIS benchmarks show that our UPL achieves consistent state-of-the-art performance under different settings while providing reliable uncertainty estimation. The code is available at https://fdueblab-upl.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《Uncertainty-aware Prototype Learning with Variational Inference for Few-shot Point Cloud Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的少样本三维点云语义分割（FS-PCS）方法大多依赖于**确定性原型（Deterministic Prototypes）**。在监督信息极度匮乏的少样本场景下，这种僵化的表示方式难以捕捉类内差异和类间相似性带来的**内在不确定性**，导致模型鲁棒性差、泛化能力受限且缺乏预测的可解释性。
*   **研究背景**：3D点云标注成本极高，少样本学习成为必然趋势。虽然概率建模在2D分割中有所应用，但在3D点云少样本分割领域，如何显式建模不确定性仍是空白。

### 2. 核心方法论
论文提出了 **UPL（不确定性感知原型学习）** 框架，包含两个核心模块：
*   **双流原型细化模块（DPR）**：
    *   通过通道注意力机制实现支持集（Support）与查询集（Query）特征的互交互。
    *   利用共享注意力权重对原始原型进行值替换（Value-substitution）和残差更新，生成更具判别力的细化原型，从而缓解类内分歧。
*   **变分原型推理正则化模块（VPIR）**：
    *   **概率建模**：将类别原型视为服从高斯分布的隐变量。
    *   **变分推理**：在训练阶段，利用 KL 散度将基于支持数据的“先验分布”与基于查询数据的“后验分布”进行对齐。
    *   **重参数化采样**：通过从先验分布中采样多个随机原型，并与确定性原型进行自适应融合，增强模型对数据波动的容忍度。
*   **不确定性估计**：在推理阶段，通过多次蒙特卡洛采样（Monte Carlo Sampling）生成预测集成，利用预测方差和熵来量化点级别的预测不确定性。

### 3. 实验设计
*   **数据集与场景**：在两个主流的大型室内场景数据集 **S3DIS** 和 **ScanNet** 上进行评估。
*   **Benchmark 设置**：遵循标准化的 CoSeg 基准，采用 N-way K-shot 设定（包括 1-way 1-shot/5-shot 和 2-way 1-shot/5-shot）。
*   **对比方法**：对比了 AttMPTI、QGE、QPGA 和目前最强的基准 CoSeg。
*   **评价指标**：主要使用平均交并比（mIoU），并在不确定性分析中引入了期望校准误差（ECE）。

### 4. 资源与算力
*   **算力说明**：论文提到计算工作在复旦大学的 **CFFF 平台**上完成，并部署在跨境贸易支付智能体平台上。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100 或 V100）、显存占用、训练总时长或具体的硬件数量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 2 个数据集、4 种少样本设置、2 种随机种子（S0/S1）。
    *   进行了详细的**消融实验**，分别验证了 DPR 模块和 VPIR 模块的独立贡献。
    *   探讨了采样次数 $T$ 对性能和校准度的影响。
*   **充分性评价**：实验设计较为充分且客观。通过定量指标（mIoU）和定性可视化（不确定性热力图）双重验证了方法的有效性。对比实验均在统一的基准下进行，保证了公平性。

### 6. 主要结论与发现
*   **性能领先**：UPL 在所有设置下均达到了 SOTA 水平。在 ScanNet 的 2-way 5-shot 设置下，比最强基准 CoSeg 提升了 **3.72%** 的 mIoU。
*   **不确定性价值**：高不确定性区域主要集中在物体边界和遮挡区域。通过增加采样次数 $T$，可以显著降低预测方差并修正错误分类。
*   **适应性强**：随着支持样本数 $K$ 的增加，UPL 的性能提升幅度高于确定性方法，证明其能更好地利用变分推理捕捉类内变化。

### 7. 优点（亮点）
*   **首创性**：这是首个在少样本 3D 点云分割中引入不确定性建模和变分原型推理的工作。
*   **可解释性**：不仅提供分割掩码，还提供不确定性热力图，为模型决策提供了可靠的解释依据。
*   **双流融合**：DPR 模块有效地利用了查询集信息来增强支持原型，打破了传统方法单向利用支持集的局限。

### 8. 不足与局限
*   **计算开销**：推理阶段需要进行多次蒙特卡洛采样（如 $T=5$），这会成倍增加推理时的计算延迟，可能限制其在实时系统中的应用。
*   **场景局限**：实验主要集中在室内场景（S3DIS/ScanNet），尚未在室外大规模自动驾驶场景（如 SemanticKITTI）中验证其鲁棒性。
*   **超参数敏感性**：采样次数 $T$ 和 KL 散度的权重 $\beta$ 需要精细调优，不同数据集的最优值可能存在差异。

（完）
