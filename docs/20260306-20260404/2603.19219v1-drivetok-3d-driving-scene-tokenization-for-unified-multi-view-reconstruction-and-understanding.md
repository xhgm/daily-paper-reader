---
title: "DriveTok: 3D Driving Scene Tokenization for Unified Multi-View Reconstruction and Understanding"
title_zh: DriveTok：用于统一多视角重建与理解的 3D 驾驶场景令牌化
authors: "Dong Zhuo, Wenzhao Zheng, Sicheng Zuo, Siming Yan, Lu Hou, Jie Zhou, Jiwen Lu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19219v1"
tags: ["query:q13"]
score: 10.0
evidence: 用于视觉-语言-动作模型和世界模型的3D驾驶场景标记化
tldr: DriveTok 针对自动驾驶中多视图场景的高效表征问题，提出了一种 3D 驾驶场景分词器。它利用视觉基础模型提取特征，通过 3D 可变形交叉注意力机制将多视图图像转化为统一的场景 Token。该方法不仅支持高质量的多视图 RGB、深度和语义重建，还通过 3D 语义占用预测增强了空间感知能力。在 nuScenes 数据集上的实验证明，DriveTok 在重建和理解任务上均表现出色，为自动驾驶世界模型提供了高效的视觉接口。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.19219v1/fig-001.webp\", \"caption\": \"Table 4: 3D semantic occupancy prediction results on nuScenes.\", \"page\": 11, \"index\": 1, \"width\": 721, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.19219v1/fig-002.webp\", \"caption\": \"Table 7: Average runtime and GPU memory usage of each DriveTok module during forward inference.\", \"page\": 22, \"index\": 2, \"width\": 569, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2603.19219v1/fig-003.webp\", \"caption\": \"Fig. 2: Illustration of DriveTok. DriveTok processes multi-view images and scene queries through an encoder-decoder architecture to produce unified scene tokens and generate diverse autonomous driving scene reconstruction and understanding outputs.\", \"page\": 4, \"index\": 3, \"width\": 723, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2603.19219v1/fig-004.webp\", \"caption\": \"Fig. 4: Visualizations of BEV scene tokens and images. We visualize the BEV feature maps with PCA, the ground truth labels for semantic regularization and the decoded images v.s. ground truth images. The PCA result clearly shows that our BEV scene tokens not only learns the complex textures but also models the semantic structure of the driving scenes, avoiding radial patterns in conventional methods.\", \"page\": 12, \"index\": 4, \"width\": 718, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2603.19219v1/fig-005.webp\", \"caption\": \"Table 1: Comparison of image tokenizers on nuScenes dataset. ‡ denotes VQGAN was trained on Imagenet [7], † indicates trained on OpenImages [25]. Tok. denotes the tokenization stage only (w/o decoder).\", \"page\": 9, \"index\": 5, \"width\": 714, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2603.19219v1/fig-006.webp\", \"caption\": \"Fig. 3: Overview of DriveTok. Surround-view images are encoded by a 3D scene encoder. view tokens (with learnable and Plücker-ray embeddings) and scene tokens interact through a spatial-aware multi-view transformer with visibility-guided sceneview attention. Joint pretraining uses image reconstruction, depth prediction, semantic prediction, and occupancy prediction.\", \"page\": 5, \"index\": 6, \"width\": 723, \"height\": 262}]"
motivation: 现有的 2D 单目分词器在处理高分辨率多视图驾驶场景时存在效率低下和视图不一致的问题。
method: 采用 3D 可变形交叉注意力将视觉特征转化为场景 Token，并通过多头解码器实现 RGB、深度、语义及 3D 占用的统一重建。
result: 在 nuScenes 数据集上，DriveTok 在图像重建、语义分割、深度预测和 3D 占用预测等多个任务中均取得了优异表现。
conclusion: DriveTok 成功实现了集成语义、几何和纹理信息的统一 3D 场景 Token 化，为多视图驾驶场景的理解与重建提供了高效方案。
---

## 摘要
随着视觉-语言-动作模型和世界模型在自动驾驶系统中的日益普及，可扩展的图像令牌化作为视觉模态的接口变得至关重要。然而，大多数现有的令牌化器是针对单目和 2D 场景设计的，当应用于高分辨率多视角驾驶场景时，会导致效率低下和视角间的不一致性。为了解决这一问题，我们提出了 DriveTok，这是一种用于统一多视角重建与理解的高效 3D 驾驶场景令牌化器。DriveTok 首先从视觉基础模型中获取语义丰富的视觉特征，然后通过 3D 可变形交叉注意力机制将其转换为场景令牌。在解码阶段，我们采用多视角 Transformer 从场景令牌中重建多视角特征，并利用多个头部获取 RGB、深度和语义重建结果。我们还在场景令牌上直接添加了一个 3D 头部，用于 3D 语义占据预测，以增强空间感知能力。通过多个训练目标，DriveTok 学习到了集成了语义、几何和纹理信息的统一场景令牌，实现了高效的多视角令牌化。在广泛使用的 nuScenes 数据集上进行的实验表明，DriveTok 生成的场景令牌在图像重建、语义分割、深度预测和 3D 占据预测任务中均表现优异。

## Abstract
With the growing adoption of vision-language-action models and world models in autonomous driving systems, scalable image tokenization becomes crucial as the interface for the visual modality. However, most existing tokenizers are designed for monocular and 2D scenes, leading to inefficiency and inter-view inconsistency when applied to high-resolution multi-view driving scenes. To address this, we propose DriveTok, an efficient 3D driving scene tokenizer for unified multi-view reconstruction and understanding. DriveTok first obtains semantically rich visual features from vision foundation models and then transforms them into the scene tokens with 3D deformable cross-attention. For decoding, we employ a multi-view transformer to reconstruct multi-view features from the scene tokens and use multiple heads to obtain RGB, depth, and semantic reconstructions. We also add a 3D head directly on the scene tokens for 3D semantic occupancy prediction for better spatial awareness. With the multiple training objectives, DriveTok learns unified scene tokens that integrate semantic, geometric, and textural information for efficient multi-view tokenization. Extensive experiments on the widely used nuScenes dataset demonstrate that the scene tokens from DriveTok perform well on image reconstruction, semantic segmentation, depth prediction, and 3D occupancy prediction tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《DriveTok: 3D Driving Scene Tokenization for Unified Multi-View Reconstruction and Understanding》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着自动驾驶技术从单纯的“感知”转向基于“推理”的视觉-语言-动作模型（VLA）和世界模型，如何高效地将传感器输入转化为既包含底层重建信息（纹理、几何）又包含高层语义信息的视觉表示（Token）成为了核心挑战。
*   **现有痛点**：传统的图像分词器（如 VQ-VAE/GAN）多针对单目 2D 场景，直接应用于多相机系统时会导致：1) 视角间不一致；2) 忽略 3D 空间结构；3) 随着相机数量和分辨率增加，Token 数量激增导致后续大模型计算效率低下。
*   **研究目标**：提出一种统一的 3D 场景分词器 **DriveTok**，将多视角图像压缩为固定数量、与分辨率和相机布局无关的 3D 场景 Token，同时支持场景重建与理解。

### 2. 方法论：核心思想与技术细节
DriveTok 采用“编码-交互-解码”的架构，核心思想是将多视角 2D 特征“提升”到统一的 3D 空间进行表征。
*   **3D 场景编码器 (3D Scene Encoder)**：
    *   利用预训练的视觉基础模型（如 DINOv3）提取多视角特征。
    *   使用 **3D 可变形交叉注意力 (3D Deformable Cross-Attention)** 机制，根据相机内外参将图像特征采样并聚合到固定的 3D 场景网格（BEV Grid）中，生成统一的场景 Token。
*   **空间感知多视角解码器 (Spatial-Aware Multi-View Decoder)**：
    *   **可见性引导注意力 (Visibility-Guided Attention)**：为了保证物理一致性，引入掩码机制，使场景 Token 仅与物理上可见的相机视角 Token 进行交互，避免无效计算和特征污染。
    *   **Plücker 射线嵌入**：在视角 Token 中注入 Plücker 射线编码，提供相机感知的几何先验，辅助跨视角对齐。
*   **联合训练任务 (Joint Multi-Task Training)**：
    *   **图像重建**：通过 DPT 解码器重建 RGB 图像（结合 L1、LPIPS 和 GAN 损失）。
    *   **深度与语义预测**：利用 MoGe-2 生成的稠密深度伪标签和 LiDARSeg 稀疏标签进行监督。
    *   **3D 语义占用预测 (Occupancy)**：直接从场景 Token 预测 3D 空间中的语义占据情况。
    *   **语义正则化**：在潜空间对场景 Token 进行语义约束，防止结构坍塌。

### 3. 实验设计
*   **数据集**：在自动驾驶主流数据集 **nuScenes** 上进行验证，涵盖 6 个环视相机视角。
*   **Benchmark 与对比方法**：
    *   **图像重建**：对比 VQGAN、ViT-VQGAN、FlowMo、BEV-VAE。指标为 PSNR 和 SSIM。
    *   **深度预测**：对比单目方法（UniDepthV2、Metric3D-V2）和多视角方法（SurroundDepth、SelfOcc）。指标为 AbsRel 和 $\delta < 1.25$。
    *   **3D 占据预测**：对比 BEVFormer、TPVFormer、SurroundOcc、GaussianFormer 等 SOTA 方法。指标为 mIoU 和 IoU。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：总计训练约 **400k 次迭代 (iterations)**。
*   **优化技术**：启用了 FlashAttention-2 和 BFloat16 精度以加速训练并降低显存占用。
*   **推理效率**：场景分词模块（Tokenization）耗时约 88ms，全流程（含所有任务头）耗时约 672ms。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比，涵盖了四个核心任务（重建、深度、语义、占据预测）。
*   **消融实验**：
    1.  验证了**可见性引导注意力**的必要性：证明其能有效防止模型过拟合纹理而忽略几何结构。
    2.  验证了**联合训练策略**：展示了不同任务（深度、语义、占据）对增强 Token 空间感知能力的贡献。
    3.  验证了 **Backbone 的影响**：即使更换为较弱的 ResNet-101，DriveTok 依然表现出极强的竞争力。
*   **客观性**：实验对比了不同分辨率下的表现，并使用了冻结 Backbone 的公平设定，结果具有较高的参考价值。

### 6. 主要结论与发现
*   **统一性**：DriveTok 成功将多视角输入压缩为固定数量（如 $128 \times 128$）的场景 Token，且这些 Token 能够同时服务于底层重建和高层理解。
*   **性能领先**：在深度预测任务上显著优于现有方法（AbsRel 降低至 0.08）；在 3D 占据预测上达到了 33.32 IoU，处于 SOTA 水平。
*   **权衡关系**：实验发现引入 3D 占据预测任务虽然会轻微降低图像重建的 PSNR，但能显著提升 Token 的空间和语义表达能力，这对于自动驾驶推理更为重要。

### 7. 优点（亮点）
*   **架构优雅**：解耦了输入分辨率/相机数量与 Token 预算的关系，非常适合作为自动驾驶世界模型的视觉前端。
*   **几何一致性**：通过 3D 提升和可见性掩码，解决了传统 2D Tokenizer 常见的视角不一致和“径向伪影”问题。
*   **多功能性**：一个 Token 序列支持多种下游任务，体现了极高的信息密度。

### 8. 不足与局限
*   **计算开销**：虽然分词过程较快，但完整的解码过程（尤其是多个 DPT 头部）在实时性上仍有优化空间（总耗时 672ms）。
*   **数据依赖**：深度预测高度依赖于 MoGe-2 生成的伪标签质量，虽然有 LiDAR 锚点校准，但在极端长尾场景下的鲁棒性有待验证。
*   **时序缺失**：目前 DriveTok 主要关注单帧场景的 Token 化，尚未显式引入多帧时序建模，这对于预测未来场景（世界模型的核心）至关重要。

（完）
