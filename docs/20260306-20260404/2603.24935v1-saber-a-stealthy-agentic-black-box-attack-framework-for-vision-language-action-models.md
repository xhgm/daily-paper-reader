---
title: "SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models"
title_zh: SABER：一种针对视觉-语言-动作模型的隐蔽智能体化黑盒攻击框架
authors: "Xiyang Wu, Guangyao Shi, Qingzi Wang, Zongxia Li, Amrit Singh Bedi, Dinesh Manocha"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24935v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作模型的对抗性攻击框架
tldr: 针对视觉-语言-动作（VLA）模型对指令扰动的敏感性，本文提出了SABER框架。这是一种基于智能体的黑盒攻击方法，利用经过GRPO训练的ReAct攻击者，在有限的编辑预算内生成隐蔽且有效的指令修改。SABER能诱导机器人任务失败、增加执行时长或违反约束。在LIBERO基准测试中，SABER显著降低了六种主流VLA模型的成功率，且比GPT基线更高效，为机器人基础模型的红队测试提供了实用且可扩展的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24935v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of SABER. For each LIBERO task, we maintain two contrastive rollouts under a frozen target VLA. A clean baseline rollout (Green Box) is first executed and cached as reference. For the attack rollout, the instruction is passed to a red-team agent (Red Box), which uses an LLM backbone to reason over the instruction and available tools, then performs multi-turn FIND→APPLY edits in a ReAct-style loop. The perturbation toolbox (Blue Box) returns edited instructions from target positions and local context. The target VLA then executes the perturbed instruction to produce the attack rollout (Yellow Box). The reward function (Purple Box) compares the clean and attack rollouts, together with the agent’s tool-use traces, to compute rewards from task outcome, action inflation, constraint violations, and stealth signals, including character edits and tool calls.\", \"page\": 5, \"index\": 1, \"width\": 889, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2603.24935v1/fig-002.webp\", \"caption\": \"Fig. 3: Two-stage training procedure. We cold-start by caching clean baseline rollouts from target VLAs (Orange) and collecting initial attack trajectories with a frozen red-team agent (Red) via lightweight random exploration over tool-calling chains. These rollouts form the cold-start dataset for SFT before GRPO training. We then perform agentic RL in interactive scenarios, where the red-team agent attacks target VLAs through tool calling and learns from reward feedback (Purple) computed by comparing clean and attack rollouts, together with the agent’s tool-use traces, under different attack objectives.\", \"page\": 5, \"index\": 2, \"width\": 914, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2603.24935v1/fig-003.webp\", \"caption\": \"TABLE III: LIBERO category-level constraint violation results across victim VLA models. Columns are grouped by the four LIBERO suites (Spatial, Object, Goal, Long) and Overall. We report Base CV (average constraint violations per episode without attack), Attack CV (average constraint violations per episode under attack), and CVI (constraint violation inflation) ∆CV = CVattack/CVbase. The Average row reports the mean over victim models for each column.\", \"page\": 6, \"index\": 3, \"width\": 1044, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2603.24935v1/fig-004.webp\", \"caption\": \"TABLE I: LIBERO category-level attack results across victim VLA models (Task Failure). Columns are grouped by the four LIBERO suites (Spatial, Object, Goal, Long) and Overall. We report Base TER (no-attack task execution rate), Attack TER (task execution rate under attack), and ASR (attack success rate for task failure, computed as Base TER −Attack TER, in %). The Average row reports the mean over victim models for each column.\", \"page\": 6, \"index\": 4, \"width\": 1044, \"height\": 193}, {\"url\": \"assets/figures/arxiv/2603.24935v1/fig-005.webp\", \"caption\": \"TABLE II: LIBERO action-length statistics under attack across victim VLA models. Columns are grouped by the four LIBERO suites (Spatial, Object, Goal, Long) and Overall. We report Base |a| (average baseline action-sequence length), Attack |a| (average action-sequence length under attack), and AIR via action inflation ratio ∆|a| = |aattack|/|abase|. The Average row reports the mean over victim models for each column.\", \"page\": 6, \"index\": 5, \"width\": 1044, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2603.24935v1/fig-006.webp\", \"caption\": \"Fig. 1: SABER: An agent-centric black-box pipeline for stealthy, automated instruction-based attacks on VLAs. VLA models for robot manipulation are expected to achieve high task success, efficient action planning and execution, and safe behavior under physical constraints. However, even small instruction perturbations can induce VLA malfunctions. SABER (Dashed Box) applies stealthy edits to manipulation instructions through a ReAct-style tool-calling protocol (Red Box) with a two-stage FIND→APPLY workflow, using a perturbation toolbox (Blue Box) spanning character-, token-, and prompt-level attacks. After the perturbed instruction is fed to the target VLA, the robot exhibits degraded behaviors aligned with the attack objective, including task failure, action inflation, and increased constraint violations.\", \"page\": 2, \"index\": 6, \"width\": 981, \"height\": 479}]"
motivation: VLA模型容易受到自然语言指令中微小扰动的影响，需要一种系统性的黑盒攻击方法来评估其鲁棒性。
method: 提出SABER框架，使用GRPO训练的ReAct智能体，通过字符、Token和提示词级别的工具生成受限预算下的对抗性指令。
result: "在LIBERO基准上，SABER使任务成功率降低20.6%，执行长度增加55%，且编辑效率显著优于GPT基线。"
conclusion: 微小且合理的指令编辑足以大幅降低机器人执行性能，基于智能体的黑盒流水线是评估机器人模型安全性的有效手段。
---

## 摘要
视觉-语言-动作 (VLA) 模型使机器人能够遵循基于视觉观测的自然语言指令，但指令通道也引入了一个关键漏洞：微小的文本扰动即可改变下游机器人的行为。因此，系统的鲁棒性评估需要一种黑盒攻击者，能够在多种 VLA 模型中生成极小但有效的指令编辑。为此，我们提出了 SABER，这是一种以智能体为中心的方法，用于在受限的编辑预算下自动生成针对 VLA 模型的基于指令的对抗性攻击。SABER 使用经过 GRPO 训练的 ReAct 攻击者，在受限的编辑预算下，利用字符级、Token 级和提示词级工具生成微小且合理的对抗性指令编辑，从而诱导目标行为退化，包括任务失败、不必要的执行时长增加以及约束违反次数增多。在 LIBERO 基准测试中，针对六种最先进的 VLA 模型，SABER 使任务成功率降低了 20.6%，动作序列长度增加了 55%，约束违反增加了 33%，同时与强大的基于 GPT 的基准模型相比，所需的工具调用减少了 21.1%，字符编辑减少了 54.7%。这些结果表明，微小且合理的指令编辑足以显著降低机器人的执行性能，且智能体化黑盒流水线为机器人基础模型的红队测试提供了一种实用、可扩展且自适应的方法。

## Abstract
Vision-language-action (VLA) models enable robots to follow natural-language instructions grounded in visual observations, but the instruction channel also introduces a critical vulnerability: small textual perturbations can alter downstream robot behavior. Systematic robustness evaluation therefore requires a black-box attacker that can generate minimal yet effective instruction edits across diverse VLA models. To this end, we present SABER, an agent-centric approach for automatically generating instruction-based adversarial attacks on VLA models under bounded edit budgets. SABER uses a GRPO-trained ReAct attacker to generate small, plausible adversarial instruction edits using character-, token-, and prompt-level tools under a bounded edit budget that induces targeted behavioral degradation, including task failure, unnecessarily long execution, and increased constraint violations. On the LIBERO benchmark across six state-of-the-art VLA models, SABER reduces task success by 20.6%, increases action-sequence length by 55%, and raises constraint violations by 33%, while requiring 21.1% fewer tool calls and 54.7% fewer character edits than strong GPT-based baselines. These results show that small, plausible instruction edits are sufficient to substantially degrade robot execution, and that an agentic black-box pipeline offers a practical, scalable, and adaptive approach for red-teaming robotic foundation models.

---

## 论文详细总结（自动生成）

### 论文总结：SABER - 针对视觉-语言-动作（VLA）模型的隐蔽智能体化黑盒攻击框架

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型通过自然语言指令引导机器人执行任务，但这种语言接口引入了巨大的安全隐患。研究发现，指令中微小的文本扰动（如错别字或词语替换）可能导致机器人物理行为的剧烈变化，如任务失败、动作冗余或违反安全约束。
**研究动机：** 现有的攻击方法多依赖手动设计、固定启发式规则或昂贵的 GPT 搜索，缺乏针对黑盒、冻结机器人策略的通用自动攻击手段。此外，现实中的攻击需要满足“隐蔽性”（极小编辑）和“低成本”（少次查询），这要求攻击者具备自适应的策略生成能力。

#### 2. 方法论
SABER 提出了一种以智能体为中心的黑盒攻击框架，核心思想是将攻击生成建模为**受限预算下的强化学习问题**。
*   **核心架构：** 采用 ReAct（Reasoning and Acting）风格的智能体，使用 Qwen2.5-3B 作为骨干模型。
*   **两阶段工作流（FIND → APPLY）：**
    *   **FIND：** 识别指令中哪些位置或策略最适合进行扰动。
    *   **APPLY：** 调用工具执行具体编辑。
*   **扰动工具箱：**
    *   **字符级：** 模拟拼写错误（插入、删除、替换等）。
    *   **Token 级：** 词汇替换、移除或属性交换。
    *   **提示词级：** 注入验证子句、分解步骤或额外约束。
*   **训练算法：** 
    *   **第一阶段（冷启动）：** 使用 GPT-5-mini 生成的初始攻击轨迹进行有监督微调（SFT），使模型掌握工具调用规范。
    *   **第二阶段（GRPO 优化）：** 采用群体相对策略优化（Group Relative Policy Optimization, GRPO），直接利用机器人执行的反馈（如是否成功、动作长度）作为奖励信号进行强化学习，无需访问目标 VLA 模型的梯度。
*   **奖励函数：** 综合考虑攻击目标（任务失败、动作膨胀、约束违反）和隐蔽性惩罚（编辑距离、工具调用次数）。

#### 3. 实验设计
*   **数据集/场景：** 使用 **LIBERO** 机器人操纵基准测试，涵盖 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个任务套件。
*   **目标模型（受害者）：** 针对 6 种最先进的 VLA 模型进行测试，包括 $\pi_0$、$\pi_{0.5}$、GR00T-N1.5、X-VLA、InternVLA-M1 和 DeepThinkVLA。
*   **对比基准：** 与基于 **GPT-5-mini** 的冻结攻击者（使用相同的工具接口）进行对比。
*   **评估指标：** 攻击成功率（ASR）、动作膨胀率（AIR）、约束违反膨胀率（CVI）、工具调用次数和字符编辑数。

#### 4. 资源与算力
*   **模型规模：** 攻击者使用 Qwen2.5-3B，采用 LoRA（低秩自适应）进行微调。
*   **算力细节：** 论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了在训练过程中使用了 ART（Agent Reinforcement Trainer）框架，并针对每个攻击目标训练了独立的智能体。

#### 5. 实验数量与充分性
*   **实验规模：** 实验覆盖了 4 个任务套件、6 个主流 VLA 模型、3 种不同的攻击目标。
*   **消融实验：** 专门验证了“冷启动（Cold-start）”阶段的必要性，证明了没有 SFT 初始化，GRPO 难以在离散动作空间中发现有效的攻击策略。
*   **充分性评价：** 实验设计较为充分且客观。通过在多个不同架构（包括带有思维链推理的模型）的 VLA 上进行测试，验证了 SABER 的泛化能力。对比实验也考虑了攻击效果与隐蔽性之间的权衡。

#### 6 主要结论与发现
*   **攻击有效性：** SABER 平均使任务成功率降低 20.6%，动作序列长度增加 55%，约束违反增加 33%。
*   **隐蔽性优势：** 相比 GPT 基线，SABER 在达到更好攻击效果的同时，减少了 21.1% 的工具调用和 54.7% 的字符编辑。
*   **脆弱性分布：** 规划密集型任务（如长程任务）比简单的感知任务更容易受到指令扰动的影响。
*   **策略演变：** 经过强化学习，攻击者学会了从昂贵的提示词级扰动转向更精准、高杠杆的 Token 级编辑。

#### 7. 优点
*   **智能体化红队测试：** 首次将 ReAct 智能体与 GRPO 结合用于 VLA 模型的黑盒攻击，实现了自动化的漏洞挖掘。
*   **多维度攻击目标：** 不仅关注二元的“成功/失败”，还关注执行效率和物理安全约束，更符合机器人领域的实际需求。
*   **高效且隐蔽：** 通过强化学习优化了攻击的“性价比”，生成的扰动更难被人类察觉。

#### 8. 不足与局限
*   **环境局限：** 实验目前仅在 LIBERO 模拟器中进行，尚未在真实物理环境或更复杂的开放世界场景中验证。
*   **模态局限：** 攻击仅限于文本指令维度，未涉及视觉观测的对抗性扰动（如对抗性补丁）。
*   **防御缺失：** 论文主要侧重于攻击框架的构建，未深入探讨针对此类智能体化攻击的有效防御机制。

（完）
