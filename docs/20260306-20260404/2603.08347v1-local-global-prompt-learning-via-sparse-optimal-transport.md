---
title: Local-Global Prompt Learning via Sparse Optimal Transport
title_zh: 基于稀疏最优传输的局部-全局提示学习
authors: "Deniz Kizaroğlu, Ülku Tuncer Küçüktas, Emre Çakmakyurdu, Alptekin Temizel"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08347v1"
tags: ["query:q14"]
score: 9.0
evidence: 视觉语言模型的小样本适配
tldr: "本文提出SOT-GLP，旨在解决视觉语言模型在少样本适配中局部特征冗余和提示词重叠的问题。该方法结合全局对齐与基于稀疏最优传输的局部对齐，通过自注意力筛选显著补丁并利用平衡分配机制将其分配给类别特定的局部提示词。在11个基准数据集上，SOT-GLP不仅在少样本分类中取得了85.1%的平均准确率，还在分布外检测任务中表现优异，揭示了提示词学习中准确性与鲁棒性的平衡关系。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08347v1/fig-001.webp\", \"caption\": \"Table 3: ImageNet Accuracy and OOD Detection Performance.\", \"page\": 6, \"index\": 1, \"width\": 507, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-002.webp\", \"caption\": \"Table 2: Ablation study on local branch design (16-shot).\", \"page\": 6, \"index\": 2, \"width\": 527, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-003.webp\", \"caption\": \"Table 4: Detailed Few-Shot Performance (%) across all datasets.\", \"page\": 7, \"index\": 3, \"width\": 527, \"height\": 683}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-004.webp\", \"caption\": \"Figure 2: Learned Local Prompt Specialization. Patch–prompt similarity maps for two example classes. For each individual prompt, the top-3 patches are shown. The \\\"combined prompts\\\" column shows similarity between the mean of the local prompts and patch features, computed over top-10 patches. Optimal Transport constraint promotes prompt specialization on distinct visual regions, while their mean representation covers the most salient parts of the object.\", \"page\": 7, \"index\": 4, \"width\": 979, \"height\": 521}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of the SOT-GLP framework. The image encoder uses two parallel streams: a standard CLIP pathway (Q-K attention) for global features and a V-V attention pathway for local patch features. Global prompts generate class-agnostic text embeddings matched to the global [CLS] token, while class-specific local prompts align to Top-K selected patches via Optimal Transport. Both branches contribute to the final classification logits.\", \"page\": 3, \"index\": 5, \"width\": 1087, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-006.webp\", \"caption\": \"Figure 3: Left: Avg. FPR95 vs. ImageNet accuracy. Right: Avg. AUC vs. ImageNet accuracy. Our method (blue star) is in the high-accuracy / low-FPR95 corner (75.5, 28.1), achieving very similar FPR95 and AUC to GalLoP but at higher ImageNet accuracy, while the variant without local projection (orange star) further lowers FPR95 to 23.8 and increases AUC score to 94.2 with only a small drop in accuracy. No local projection variant shows state of art OOD performance compared to the other prompt-learning baselines, with FPR95 gains ranging from ↓ 4.9 (vs. LoCoOp–GL, 28.7 → 23.8) to ↓ 3.5 (vs. Gallop, 27.3 → 23.8), and AUC gains of ↑ 0.7 (vs. LoCoOp (GL-MCM), 93.5→ 94.2) up to ↑ 1.0 (vs. Gallop, 93.2→ 94.2).\", \"page\": 8, \"index\": 6, \"width\": 731, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2603.08347v1/fig-007.webp\", \"caption\": \"Table 1: Few-shot classification accuracy (%) on 11 benchmarks with 16 shots per class using CLIP ViT-B/16.\", \"page\": 5, \"index\": 7, \"width\": 527, \"height\": 471}]"
motivation: 现有的局部对齐方法通常独立选择图像区域，导致局部特征利用冗余且不同提示词之间存在语义重叠。
method: 引入全局-局部提示学习框架，利用稀疏补丁支持和平衡熵最优传输将显著视觉区域显式划分给类别特定的局部提示词。
result: "在16-shot实验中平均准确率达85.1%，且在OOD检测任务中以94.2%的AUC刷新了纪录。"
conclusion: 通过无投影的局部对齐保留CLIP原生特征空间，SOT-GLP在提升特定任务性能的同时保持了极高的分布外鲁棒性。
---

## 摘要
视觉语言模型（VLM）如 CLIP 的少样本自适应通常依赖于学习与全局图像嵌入相匹配的文本提示。近期的工作通过引入局部图像-文本对齐来捕捉细粒度视觉线索，从而扩展了这一范式。然而，这些方法通常为每个提示独立选择局部区域，导致局部特征使用冗余以及提示重叠。我们提出了 SOT-GLP，它引入了共享的稀疏补丁（patch）支撑和平衡最优传输分配，在保持全局对齐的同时，在类别特定的局部提示之间显式划分显著视觉区域。我们的方法学习共享的全局提示和类别特定的局部提示。全局分支维持标准的图像-文本匹配，以实现稳健的类别级对齐。局部分支利用 V-V 注意力构建类别条件的稀疏补丁集，并通过平衡熵最优传输将其与多个类别特定的提示对齐，从而产生补丁的软划分，防止提示重叠和崩溃。我们在两个互补的目标上评估了我们的方法：(i) 在 11 个标准基准上的少样本分类准确率，以及 (ii) 分布外（OOD）检测。在 16-shot ViT-B/16 的标准 11 数据集基准上，SOT-GLP 达到了 85.1% 的平均准确率，优于之前的提示学习方法。我们识别出提示学习中一种明显的准确率-稳健性权衡：虽然可学习的投影优化了分布内拟合，但它们改变了基础特征空间。我们证明了无投影的局部对齐保留了 CLIP 流形的原始几何结构，从而实现了超越全自适应模型的最先进 OOD 检测性能（94.2% AUC）。代码实现见：https://github.com/Deniz2304988/SOT-GLP。

## Abstract
Few-shot adaptation of vision-language models (VLMs) like CLIP typically relies on learning textual prompts matched to global image embeddings. Recent works extend this paradigm by incorporating local image-text alignment to capture fine-grained visual cues, yet these approaches often select local regions independently for each prompt, leading to redundant local feature usage and prompt overlap. We propose SOT-GLP, which introduces a shared sparse patch support and balanced optimal transport allocation to explicitly partition salient visual regions among class-specific local prompts while preserving global alignment. Our method learns shared global prompts and class-specific local prompts. The global branch maintains standard image-text matching for robust category-level alignment. The local branch constructs a class-conditioned sparse patch set using V-V attention and aligns it to multiple class-specific prompts via balanced entropic optimal transport, yielding a soft partition of patches that prevents prompt overlap and collapse. We evaluate our method on two complementary objectives: (i) few-shot classification accuracy on 11 standard benchmarks and (ii) out-of-distribution (OOD) detection. On the standard 11-dataset benchmark with 16-shot ViT-B/16, SOT-GLP achieves 85.1% average accuracy, outperforming prior prompt-learning methods. We identify a distinct accuracy-robustness trade-off in prompt learning: while learnable projections optimize in-distribution fit, they alter the foundational feature space. We demonstrate that a projection-free local alignment preserves the native geometry of the CLIP manifold, yielding state-of-the-art OOD detection performance (94.2% AUC) that surpasses fully adapted models. Implementation available at: https://github.com/Deniz2304988/SOT-GLP

---

## 论文详细总结（自动生成）

以下是对论文《Local-Global Prompt Learning via Sparse Optimal Transport》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉语言模型（VLM，如 CLIP）在少样本适配中主要依赖全局图像特征，忽略了细粒度的局部视觉线索。虽然已有研究尝试引入局部对齐，但存在两个主要缺陷：
    1.  **背景干扰**：全局对齐容易受到非判别性背景区域的影响。
    2.  **提示词重叠与冗余**：现有的局部方法通常为每个提示词独立选择图像区域，导致多个提示词可能同时关注相同的显著区域，缺乏对不同局部特征（如物体的不同部位、纹理）的专门化建模。
*   **研究背景**：在少样本场景下，如何有效地挖掘局部视觉证据并将其与类别特定的文本提示进行非冗余的匹配，是提升分类精度和分布外（OOD）检测能力的关键。

### 2. 方法论
SOT-GLP 提出了一个双分支架构，结合了全局对齐和基于**稀疏最优传输（Sparse Optimal Transport）**的局部对齐：
*   **双分支提示学习**：
    *   **全局分支**：学习一组共享的全局提示词，与图像的 [CLS] 标记对齐，保持类别的稳健泛化。
    *   **局部分支**：学习类别特定的局部提示词，捕捉细粒度属性。
*   **局部特征增强（V-V 注意力）**：采用值-值（Value-Value）注意力机制替代标准的 Q-K 注意力，以增强补丁（patch）之间的相互作用，产生更具判别力的局部特征。
*   **显著性引导的稀疏化**：计算图像补丁与局部提示词集的平均相似度，仅保留 Top-K 个显著补丁，从而过滤背景噪声。
*   **平衡熵最优传输（Balanced Entropic OT）**：
    *   将筛选出的 Top-K 补丁与多个局部提示词进行匹配。
    *   通过引入**平衡边际约束**，强制每个提示词分配到等量的补丁质量，从而实现补丁的“软划分”。这防止了所有提示词都坍缩到同一个最显著补丁上，促使不同提示词专门化于不同的视觉部位（如头、尾、翅膀）。
*   **推理与融合**：最终分类分数由全局和局部相似度加权融合而成。

### 3. 实验设计
*   **数据集**：在 11 个标准视觉基准上进行评估，涵盖通用物体（ImageNet, Caltech101）、细粒度分类（Cars, Aircraft, Pets, Flowers102, Food101）、纹理（DTD）、场景（SUN397）、遥感（EuroSAT）和动作识别（UCF101）。
*   **任务场景**：
    1.  **少样本分类**：分别测试 1, 2, 4, 8, 16-shot 性能。
    2.  **分布外（OOD）检测**：以 ImageNet 为分布内数据，测试在 iNaturalist、SUN、Places 和 Textures 上的检测能力。
*   **对比方法**：包括 Zero-shot CLIP、Linear Probe、CoOp、CoCoOp、MaPLe、PLOT、PromptSRC、LoCoOp 和 GalLoP 等前沿方法。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 3090 GPU**。
*   **训练细节**：采用混合精度训练和数据并行执行。
*   **算力开销**：由于引入了双流视觉编码器（V-V 注意力），推理成本增加了约 **24%**（从 44.9 GFLOPs 增至 58.8 GFLOPs）。最优传输（OT）匹配由于作用于稀疏的 Top-K 补丁，计算开销可忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：在 11 个数据集上进行了完整的少样本梯度测试，并进行了大规模的 OOD 检测对比。
*   **消融实验**：非常充分，分别验证了 V-V 注意力、局部投影层、类别特定提示词、超参数 $\lambda$（局部权重）和 $K$（补丁数量）的影响。
*   **客观性**：所有实验均遵循固定种子和固定划分协议，并在 16-shot 设置下报告了 3 次随机初始化的平均结果，确保了结果的可重复性和公平性。

### 6. 主要结论与发现
*   **性能领先**：SOT-GLP 在 16-shot 平均准确率达到 **85.1%**，在 11 个数据集中的 9 个上刷新了纪录。
*   **准确性-鲁棒性权衡**：研究发现，虽然可学习的局部投影层能提升 0.9% 的分类准确率，但会破坏 CLIP 原生的特征流形。**移除该投影层后，模型在 OOD 检测上达到了 SOTA 性能（94.2% AUC）**。
*   **OT 的有效性**：可视化证明，最优传输约束确实促进了提示词的专门化，使其能够覆盖物体的不同显著部位。

### 7. 优点
*   **创新的分配机制**：通过平衡最优传输解决了局部提示词学习中的“坍缩”和“重叠”问题，使模型能更精细地理解物体结构。
*   **双重任务优势**：不仅在分类任务上表现强劲，还通过“无投影”变体在 OOD 检测任务上展现了极高的鲁棒性。
*   **背景抑制**：稀疏化策略有效解决了局部对齐方法中常见的背景噪声干扰问题。

### 8. 不足与局限
*   **计算成本**：V-V 注意力分支虽然提升了性能，但也带来了额外的推理延迟和显存占用。
*   **特定领域限制**：在极度专业化的领域（如 Aircraft 飞机型号识别或 EuroSAT 卫星图像），由于 CLIP 预训练特征可能缺乏相应的局部判别力，该方法的提升幅度相对较小。
*   **融合策略简单**：目前采用的是简单的加权求和融合，未来可能需要更复杂的门控机制来动态调整全局与局部的权重。

（完）
