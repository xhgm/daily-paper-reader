---
title: "SLNet: A Super-Lightweight Geometry-Adaptive Network for 3D Point Cloud Recognition"
title_zh: SLNet：一种用于三维点云识别的超轻量级几何自适应网络
authors: "Mohammad Saeid, Amir Salarpour, Pedram MohajerAnsari, Mert D. Pesé"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07454v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 轻量级三维点云识别骨干网络
tldr: 本研究针对现有3D点云识别模型计算成本高的问题，提出了超轻量级骨干网络SLNet。该模型通过非参数化自适应点嵌入（NAPE）捕捉空间结构，并利用几何调制单元（GMU）进行高效特征调节。SLNet在保持极低参数量（如SLNet-S仅0.14M参数）的同时，在ModelNet40、ScanObjectNN和S3DIS等多个基准测试中展现出与重型模型相当的性能，实现了精度与效率的卓越平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07454v1/fig-001.webp\", \"caption\": \"Fig. 1: NetScore and NetScore+ across four datasets and two hardware platforms (RTX 3090 and Jetson Orin Nano), spanning 12 metric–dataset–hardware axes. SLNet-S and SLNet-M consistently occupy the outermost positions, establishing Pareto-optimal deployability across all evaluated configurations.\", \"page\": 1, \"index\": 1, \"width\": 440, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-002.webp\", \"caption\": \"TABLE IX: Effect of embedding choice and GMU placement on ModelNet40 OA (%). NAPE with post-embedding αβ modulation is optimal across both model sizes.\", \"page\": 6, \"index\": 2, \"width\": 408, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-003.webp\", \"caption\": \"Fig. 2: SLNet architecture: a NAPE+GMU front end, a four-stage hierarchical encoder with FPS, parameter-free normalization, and lightweight residual refinement, and task-specific heads for classification and segmentation.\", \"page\": 2, \"index\": 3, \"width\": 1002, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-004.webp\", \"caption\": \"Fig. 5: Qualitative comparison of saliency on ModelNet40 samples: top row—DGCNN stage-2 EdgeConv gradient saliency; middle and bottom—NAPE gradient saliency (channel widths 16 and 32). NAPE yields sharper, semantically focused responses despite operating at the input layer.\", \"page\": 7, \"index\": 4, \"width\": 475, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-005.webp\", \"caption\": \"TABLE II: ModelNet40 classification (supervised). Metrics: overall accuracy (OA), mean class accuracy (mAcc), parameters (Param, M), FLOPs (G), memory (MB), latency (ms), and composite NetScore / NetScore+. Our runs are 3-run averages.\", \"page\": 4, \"index\": 5, \"width\": 890, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-006.webp\", \"caption\": \"TABLE VI: ShapeNetPart part segmentation (supervised). Metrics: instance IoU (ins-IoU), class-average IoU (cls-IoU), Param (M), FLOPs (G), Mem (MB), Time (ms), and NetScore/NetScore+.\", \"page\": 5, \"index\": 6, \"width\": 960, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.07454v1/fig-007.webp\", \"caption\": \"TABLE V: ScanObjectNN classification (supervised). Metrics: OA, mAcc, Param (M), FLOPs (G), Mem (MB), Time (ms), and NetScore/NetScore+.\", \"page\": 5, \"index\": 7, \"width\": 794, \"height\": 269}]"
motivation: 旨在解决现有3D点云识别模型因过度依赖注意力机制或深层MLP而导致的计算开销过大、难以在资源受限设备部署的问题。
method: 提出由非参数化自适应点嵌入（NAPE）和极简几何调制单元（GMU）构成的四阶段分层编码器架构。
result: SLNet在多个基准测试中以极少的参数量（较同类模型减少5x至28x）实现了领先的识别准确率和分割性能。
conclusion: SLNet证明了通过精巧的几何自适应设计，极小规模的网络也能在3D识别任务中实现性能与效率的最优平衡。
---

## 摘要
我们提出了 SLNet，这是一种用于三维点云识别的轻量级骨干网络，旨在实现强大的性能，同时避免了近期许多基于注意力机制、图卷积和深层 MLP 模型的高昂计算成本。该模型基于两个简单的想法：NAPE（非参数化自适应点嵌入），它通过结合高斯径向基函数（RBF）和余弦基函数，并利用输入自适应的带宽和混合权重来捕捉空间结构；以及 GMU（几何调制单元），这是一种逐通道的仿射调制器，仅增加了 2D 可学习参数。这些组件被应用于一个四阶段的分层编码器中，该编码器包含 FPS+kNN 分组、非参数化归一化以及共享残差 MLP。实验表明，SLNet 证明了极小的模型在多项三维识别任务中仍能保持极强的竞争力。在 ModelNet40 数据集上，参数量为 0.14M、计算量为 0.31 GFLOPs 的 SLNet-S 达到了 93.64% 的总体准确率，在参数量减少 5 倍的情况下优于 PointMLP-elite；而参数量为 0.55M、计算量为 1.22 GFLOPs 的 SLNet-M 达到了 93.92%，在参数量减少 24 倍的情况下超过了 PointMLP。在 ScanObjectNN 数据集上，SLNet-M 实现了 84.25% 的总体准确率，与 PointMLP 的差距在 1.2 个百分点以内，而参数量仅为其 1/28。对于大规模场景分割，SLNet-T 通过引入局部 Point Transformer 注意力机制扩展了骨干网络，在 S3DIS Area 5 上仅凭 2.5M 参数就达到了 58.2% 的 mIoU，参数量比 Point Transformer V3 少 17 倍以上。我们还引入了 NetScore+，它通过整合延迟和峰值内存扩展了 NetScore，从而能够以更面向部署的方式评估效率。在多个基准测试和硬件设置下，SLNet 在准确率和效率之间实现了强大的整体平衡。代码开源地址：https://github.com/m-saeid/SLNet。

## Abstract
We present SLNet, a lightweight backbone for 3D point cloud recognition designed to achieve strong performance without the computational cost of many recent attention, graph, and deep MLP based models. The model is built on two simple ideas: NAPE (Nonparametric Adaptive Point Embedding), which captures spatial structure using a combination of Gaussian RBF and cosine bases with input adaptive bandwidth and blending, and GMU (Geometric Modulation Unit), a per channel affine modulator that adds only 2D learnable parameters. These components are used within a four stage hierarchical encoder with FPS+kNN grouping, nonparametric normalization, and shared residual MLPs. In experiments, SLNet shows that a very small model can still remain highly competitive across several 3D recognition tasks. On ModelNet40, SLNet-S with 0.14M parameters and 0.31 GFLOPs achieves 93.64% overall accuracy, outperforming PointMLP-elite with 5x fewer parameters, while SLNet-M with 0.55M parameters and 1.22 GFLOPs reaches 93.92%, exceeding PointMLP with 24x fewer parameters. On ScanObjectNN, SLNet-M achieves 84.25% overall accuracy within 1.2 percentage points of PointMLP while using 28x fewer parameters. For large scale scene segmentation, SLNet-T extends the backbone with local Point Transformer attention and reaches 58.2% mIoU on S3DIS Area 5 with only 2.5M parameters, more than 17x fewer than Point Transformer V3. We also introduce NetScore+, which extends NetScore by incorporating latency and peak memory so that efficiency can be evaluated in a more deployment oriented way. Across multiple benchmarks and hardware settings, SLNet delivers a strong overall balance between accuracy and efficiency. Code is available at: https://github.com/m-saeid/SLNet.

---

## 论文详细总结（自动生成）

以下是对论文《SLNet: A Super-Lightweight Geometry-Adaptive Network for 3D Point Cloud Recognition》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的高性能三维点云识别模型（如基于注意力机制、图卷积或深层MLP的模型）计算成本过高，难以在资源受限的边缘设备（如自动驾驶、机器人、AR设备）上实时运行。
*   **研究背景**：
    *   **重量级模型**：PointMLP、Point Transformer等虽然精度高，但参数量大、延迟高。
    *   **轻量级/非参数化模型**：如Point-NN、NPNet虽然效率极高，但在复杂基准测试上的准确率往往落后于监督学习模型。
*   **目标**：设计一种“超轻量级”且“几何自适应”的骨干网络，在极低参数预算下实现与重型模型相当的性能。

### 2. 方法论
SLNet 采用四阶段分层编码器架构，其核心创新在于两个组件：
*   **NAPE（非参数化自适应点嵌入）**：
    *   **核心思想**：无需学习参数，直接将原始XYZ坐标映射到高维特征空间。
    *   **技术细节**：结合了**高斯径向基函数（RBF）**（捕捉局部细节）和**余弦基函数**（捕捉大尺度平滑响应）。
    *   **自适应机制**：根据点云的全局离散度（尺度）动态调整核带宽（$\sigma_{adapt}$）和两种基函数的混合比例（$\beta$）。
*   **GMU（几何调制单元）**：
    *   **核心思想**：在NAPE之后引入极少量的可学习参数进行特征微调。
    *   **技术细节**：一种逐通道的仿射变换（$Y = \alpha X + \beta$），每个维度仅需2个标量参数（总参数量仅为2D，如32或64个）。
*   **整体架构**：
    *   使用最远点采样（FPS）和k近邻（kNN）进行分组。
    *   采用**非参数化归一化**（计算相对坐标和特征）。
    *   **轻量级残差块（LRB）**：使用共享MLP进行特征精炼，通过瓶颈比例（$r=0.25$）进一步压缩计算量。
    *   **SLNet-T变体**：针对大规模场景，将NAPE替换为线性投影，并引入局部Point Transformer注意力机制。

### 3. 实验设计
*   **数据集**：
    *   **物体分类**：ModelNet40、ModelNet-R（标签精炼版）、ScanObjectNN（含背景噪声的真实世界数据）。
    *   **少样本学习**：ModelNet40 (5-way/10-way, 10-shot/20-shot)。
    *   **零件分割**：ShapeNetPart。
    *   **场景分割**：S3DIS (Area 5)。
*   **Benchmark（对比方法）**：包括 PointNet/++、DGCNN、PointMLP、CurveNet、Point Transformer (V1/V2/V3) 以及非参数化模型 Point-NN、NPNet 等。
*   **评价指标**：除了OA、mIoU外，重点引入了 **NetScore+**，该指标综合考虑了准确率、参数量、FLOPs、**硬件延迟**和**峰值内存**。

### 4. 资源与算力
*   **硬件平台**：
    *   **训练/测试**：NVIDIA RTX 3090, GTX 1080。
    *   **大规模场景训练**：NVIDIA RTX 5090（用于 SLNet-T）。
    *   **边缘部署评估**：NVIDIA Jetson Orin Nano (8GB)。
*   **软件环境**：Ubuntu 22.04, CUDA 11.8/12.6, PyTorch。
*   **训练细节**：ModelNet40训练300轮，使用SGD优化器，余弦退火学习率，EMA（指数移动平均）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从物体级到场景级、从全监督到少样本学习的6大基准测试。
*   **消融实验**：非常详尽，包括：
    *   NAPE与不同嵌入方式（纯高斯、纯余弦、MLP）的对比。
    *   GMU放置位置及组合方式的影响。
    *   邻域大小 $K$、残差块瓶颈比例 $r$、采样策略（FPS vs 随机）的敏感性分析。
    *   针对 S3DIS 的损失函数（加权交叉熵 vs Focal Loss）和训练策略（EMA、标签平滑）的专项消融。
*   **客观性**：通过在不同硬件（3090 vs Jetson）上实测延迟和内存，并使用 NetScore+ 进行量化，评估过程较为客观且贴近实际应用。

### 6. 主要结论与发现
*   **极致效率**：SLNet-S 仅需 **0.14M** 参数，在 ModelNet40 上达到 93.64% OA，参数量比 PointMLP-elite 少 5 倍，精度更高。
*   **性能领先**：SLNet-M 在参数量仅为 PointMLP 的 **1/24** 时，准确率反而更高（93.92%）。
*   **部署优势**：在 NetScore+ 指标下，SLNet 在所有评估配置中均处于 Pareto 最优位置，证明了其卓越的部署能力。
*   **少样本能力**：在 10-way 20-shot 任务中，SLNet 显著超过了所有非参数化模型（如 NPNet），提升约 6 个百分点。

### 7. 优点
*   **设计精巧**：NAPE 充分利用了几何先验，通过非参数化方式实现了高效的特征初始化。
*   **硬件友好**：不仅关注理论上的 FLOPs，还实测了边缘设备上的延迟和内存，引入 NetScore+ 具有实际工程指导意义。
*   **通用性强**：模型变体（S, M, T）覆盖了从简单物体分类到复杂室内场景分割的多种任务。
*   **可视化支持**：通过梯度显著性图证明了 NAPE 能够比 DGCNN 等模型更精准地捕捉语义特征。

### 8. 不足与局限
*   **绝对精度上限**：在 S3DIS 等大规模场景中，SLNet-T 虽然效率极高，但绝对 mIoU（58.2%）仍低于参数量巨大的重型模型（如 PT V3 的 73% 级别）。
*   **采样依赖**：依然依赖 FPS 和 kNN 算法，虽然这是主流做法，但在极大规模点云上，这些预处理步骤本身可能成为新的计算瓶颈。
*   **超参数微调**：虽然 NAPE 参数固定，但邻域大小 $K$ 仍需根据数据集（干净 vs 噪声）进行手动调整。

（完）
