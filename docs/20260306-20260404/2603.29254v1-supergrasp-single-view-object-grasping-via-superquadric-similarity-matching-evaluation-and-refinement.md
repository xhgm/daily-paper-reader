---
title: "SuperGrasp: Single-View Object Grasping via Superquadric Similarity Matching, Evaluation, and Refinement"
title_zh: SuperGrasp：通过超二次曲面相似性匹配、评估与精细化实现的单视图物体抓取
authors: "Lijingze Xiao, Jinhong Du, Yang Cong, Supeng Diao, Yu Ren"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29254v1"
tags: ["query:q9"]
score: 10.0
evidence: 通过超二次曲面相似性匹配和精细化的单视图物体抓取
tldr: 针对单视角下几何信息缺失导致的抓取不稳定问题，本文提出SuperGrasp框架。该框架分为两阶段：首先通过超二次曲面相似性匹配从预构建基元库中快速检索初始抓取候选；随后利用E-RNet网络对抓取区域进行局部锚定、评估与精细化调整。实验证明该方法在仿真和现实场景中均具有极强的泛化能力和稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29254v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the task pipeline. Under dense clutter and singleview observation constraints, the tabletop decluttering task is accomplished through a two-stage pipeline consisting of grasp candidate generation, grasp evaluation, and refinement.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2603.29254v1/fig-002.webp\", \"caption\": \"Fig. 6. Simulation experiment settings. (a) Scene with 10 seen objects. (b) Scene with 20 seen objects. (c) Scene with 10 novel objects.\", \"page\": 6, \"index\": 2, \"width\": 529, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2603.29254v1/fig-003.webp\", \"caption\": \"Fig. 7. Real-world experiment settings. We design two real-world test scenarios: (1) the objects within the green box are similar to those in the training set; and (2) the objects within the red box differ significantly from the training set in both shape and size.\", \"page\": 7, \"index\": 3, \"width\": 1076, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2603.29254v1/fig-004.webp\", \"caption\": \"Fig. 4. Overview: Our framework consists of two stages. In the first stage, we first estimate the superquadric coefficients of the target object and compute their similarity to those of the objects in the database. We then select the top-N most similar objects and transfer their grasp poses to the target object through a transformation matrix T , followed by a coarse filtering step to remove noisy candidates. In the second stage, we enlarge the perception region of each initial grasp and feed the cropped regional point cloud into a PointNet++ network for feature extraction. We then further crop the features corresponding to the points within the initial gripper closing region and aggregate them via max pooling to obtain the grasp anchor feature. Finally, the anchor feature is fed into an evaluation network and a refinement network to predict grasp feasibility and refinement feasibility, respectively.\", \"page\": 4, \"index\": 4, \"width\": 948, \"height\": 375}]"
motivation: 解决单视角观测下由于物体几何信息不完整而难以生成稳定有效抓取位姿的挑战。
method: 提出一种结合超二次曲面相似性匹配检索初始位姿，并利用E-RNet进行局部区域评估与精细化调整的两阶段框架。
result: 在包含10万个标注的大规模数据集上训练，并在仿真及实物实验中展现出优异的抓取成功率和对新物体的泛化性。
conclusion: SuperGrasp通过几何基元匹配与深度学习精细化的结合，显著提升了单视角机器人抓取的可靠性。
---

## 摘要
从单视图观测中进行机器人抓取仍然是操作领域中的一个关键挑战。现有方法在面对不完整的几何信息时，仍难以生成稳定且有效的抓取位姿。为解决这些局限性，我们提出了 SuperGrasp，这是一个用于平行爪夹持器单视图抓取的创新两阶段框架，它将抓取过程分解为初始抓取位姿生成以及随后的抓取评估与精细化。在第一阶段，我们引入了一个相似性匹配模块，通过将输入的单视图点云与基于超二次曲面系数预计算的原语数据集进行匹配，从而高效地检索候选抓取。在第二阶段，我们提出了 E-RNet，这是一个端到端网络，它扩展了抓取感知区域，并将初始抓取闭合区域作为局部锚点区域，从而实现对候选抓取更准确、更可靠的评估与精细化。为了增强泛化能力，我们构建了一个包含 1500 个原语的用于相似性匹配的原语数据集，并收集了一个包含来自 124 个物体的 10 万个稳定抓取标签的大规模点云数据集，用于网络训练。在仿真和真实世界环境中的广泛实验表明，我们的方法在不同场景和新颖物体上均实现了稳定的抓取性能和强大的泛化能力。

## Abstract
Robotic grasping from single-view observations remains a critical challenge in manipulation. Existing methods still struggle to generate stable and valid grasp poses when confronted with incomplete geometric information. To address these limitations, we propose SuperGrasp, a novel two-stage framework for single-view grasping with parallel-jaw grippers that decomposes the grasping process into initial grasp pose generation and subsequent grasp evaluation and refinement. In the first stage, we introduce a Similarity Matching Module that efficiently retrieves grasp candidates by matching the input single-view point cloud with a pre-computed primitive dataset based on superquadric coefficients. In the second stage, we propose E-RNet, an end-to-end network that expands the graspaware region and takes the initial grasp closure region as a local anchor region, enabling more accurate and reliable evaluation and refinement of grasp candidates. To enhance generalization, we construct a primitive dataset containing 1.5k primitives for similarity matching and collect a large-scale point cloud dataset with 100k stable grasp labels from 124 objects for network training. Extensive experiments in both simulation and realworld environments demonstrate that our method achieves stable grasping performance and strong generalization across varying scenes and novel objects.

---

## 论文详细总结（自动生成）

这是一份关于论文《SuperGrasp: Single-View Object Grasping via Superquadric Similarity Matching, Evaluation, and Refinement》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在单视图（Single-view）观测下，由于遮挡导致的几何信息缺失和传感器噪声，机器人难以生成稳定且有效的 6-DoF 抓取位姿。
*   **研究动机**：
    *   现有的基于几何采样的方法在点云不完整时生成的候选位姿质量差。
    *   端到端深度学习方法虽然高效，但往往需要海量数据，且对未见过的物体（Unseen Objects）泛化能力有限。
    *   超二次曲面（Superquadrics）作为一种紧凑的几何原语表示，能够很好地补全物体形状，但直接拟合单视图点云存在尺度和形状偏差。

### 2. 方法论：SuperGrasp 两阶段框架
该方法将抓取任务分解为“候选生成”和“评估精细化”两个阶段：

*   **第一阶段：超二次曲面相似性匹配（Candidate Generation）**
    *   **原语数据库构建**：预先构建包含 1.5k 个具有稳定抓取标签的几何原语（如长方体、圆柱体、椭圆柱等）数据库。
    *   **快速匹配算法**：利用 EMS（期望最大化搜索）算法拟合目标物体的超二次曲面参数（形状参数 $\epsilon$ 和尺度参数 $a$）。
    *   **相似度度量**：通过计算形状差异（$d_{shape}$）和绝对尺度差异（$d_{scale}, d_{abs}$）的加权和，从数据库中检索最相似的 Top-N 个原语。
    *   **位姿迁移**：将原语上的高质量抓取位姿通过变换矩阵 $T$ 迁移至目标物体，并进行初步的碰撞过滤。

*   **第二阶段：E-RNet 评估与精细化网络（Evaluation & Refinement）**
    *   **核心思想**：不仅关注抓取闭合区域，还扩展感知范围以包含周围空间上下文。
    *   **网络结构**：以 PointNet++ 为骨干网络。
    *   **抓取锚点特征（Grasp Anchor Feature）**：将初始抓取闭合区域定义为“锚点”，提取该区域特征并与周围几何特征聚合，增强对抓取稳定性的判断。
    *   **多头输出**：一个分支预测抓取可行性分数（分类）；另一个分支预测三种预设精细化动作（加深抓取、左右旋转 $\pm 15^\circ$）的可行性。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 YCB、KIT 和 DexNet 中选取 124 个物体，在 PyBullet 仿真中生成 10 万个带有稳定标签（经受扰动测试）的抓取样本。
    *   **原语库**：1.5k 个缩放后的几何原语。
*   **实验场景**：
    *   **仿真**：设置了中等杂乱（10个物体）、高度杂乱（20个物体）以及未见过物体（10个新物体）三种场景。
    *   **现实世界**：使用 xMate ER7Pro 机械臂、AG-105 夹爪和 ZED 2i 相机，测试了相似物体和全新物体的抓取。
*   **对比基准（Benchmarks）**：对比了经典的 **PointNetGPD**（两阶段）和 **Contact-GraspNet**（端到端）。
*   **评估指标**：抓取成功率（GSR）、任务成功率（TSR）和抓取尝试次数（GA）。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：训练了 100 个 epoch，Batch Size 为 128。
*   **优化器**：Adam 优化器，初始学习率为 0.001。
*   **推理耗时**：总耗时约 0.789 秒（第一阶段 0.356s + 第二阶段 0.433s），优于 PointNetGPD（约 10s），但略慢于端到端的 Contact-GraspNet（0.333s）。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每个场景进行了 30 次试验；现实世界中每个场景进行了 10 次试验（共 200 个物体抓取尝试）。
*   **消融实验**：设计了三组消融实验（Stage-1 替换、Stage-2 替换、移除精细化分支），分别验证了相似性匹配模块和 E-RNet 的有效性。
*   **充分性评价**：实验覆盖了从简单到复杂、从已知到未知的多种维度，对比方法具有代表性，数据量充足，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：在所有测试场景中，SuperGrasp 的抓取成功率和任务成功率均显著优于对比方法。在仿真未见过物体场景下，任务成功率比 Contact-GraspNet 高出 25%。
*   **现实迁移**：模型在仿真中训练，**无需任何现实数据微调**即可直接迁移至真实环境，表现出极强的 Sim-to-Real 泛化能力。
*   **精细化的作用**：在高度杂乱和复杂几何物体场景下，精细化分支（Refinement）对提升成功率至关重要（提升约 8.39%）。

### 7. 优点与亮点
*   **几何与学习的结合**：巧妙利用超二次曲面这一经典几何工具解决了单视图下的“补全”问题，降低了对大规模复杂标注数据的依赖。
*   **局部锚点设计**：E-RNet 通过锚定抓取区域并感知周围环境，比单纯看闭合区域的方法更具鲁棒性。
*   **高效检索**：基于哈希表和超二次曲面系数的匹配机制，极大地加速了两阶段框架中的候选生成过程。

### 8. 不足与局限
*   **实时性仍有提升空间**：虽然比传统两阶段方法快，但相比最先进的端到端方法，其推理延迟仍高出约一倍，可能限制其在极高速动态场景的应用。
*   **遮挡风险**：实验观察到，主要的失败原因仍源于严重的自遮挡或环境遮挡导致的碰撞，该方法尚未完全解决极端遮挡下的盲区推理。
*   **原语限制**：对于形状极度不规则、无法用凸面原语近似的物体，第一阶段的匹配质量可能会下降。

（完）
