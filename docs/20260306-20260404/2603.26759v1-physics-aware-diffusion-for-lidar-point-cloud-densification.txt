Title: Physics-Aware Diffusion for LiDAR Point Cloud Densification

URL Source: https://arxiv.org/pdf/2603.26759v1

Published Time: Tue, 31 Mar 2026 00:10:30 GMT

Number of Pages: 6

Markdown Content:
# Physics-Aware Diffusion for LiDAR Point Cloud Densification 

Zeping Zhang 1 and Robert Lagani` ere 2

Abstract — LiDAR perception is severely limited by the distance-dependent sparsity of distant objects. While diffusion models can recover dense geometry, they suffer from pro-hibitive latency and physical hallucinations manifesting as ghost points. We propose Scanline-Consistent Range-Aware Diffusion, a framework that treats densification as probabilistic refine-ment rather than generation. By leveraging Partial Diffusion (SDEdit) on a coarse prior, we achieve high-fidelity results in just 156 ms. Our novel Ray-Consistency loss and Negative Ray Augmentation enforce sensor physics to suppress artifacts. Our method achieves state-of-the-art results on KITTI-360 and nuScenes, directly boosting off-the-shelf 3D detectors without retraining. Code will be made available. 

I. INTRODUCTION LiDAR-based 3D perception is fundamentally limited by sensor resolution. The quadratic decay of point density with distance results in severe sparsity for distant objects. To address this, generative densification methods have been pro-posed, but they often hallucinate geometry, creating bleeding artifacts or ’ghost trails’ (see Fig. 1) behind objects where the sensor beam should pass through unimpeded. These hallucinated artifacts severely degrade the performance of downstream tasks like 3D object detection [16], [2]. To address this, LiDAR densification aims to recover dense geometry from sparse inputs. While deterministic methods (e.g., depth completion [4]) are efficient, they often produce over-smoothed results that fail to capture high-frequency ge-ometric details. Recently, Denoising Diffusion Probabilistic Models (DDPMs) [3] have achieved state-of-the-art fidelity in 3D generation [19], [12]. However, applying standard diffusion to real-world perception pipelines faces two un-resolved conflicts: 

1. The Efficiency Gap: Standard diffusion requires it-erative denoising from pure Gaussian noise (often 1000+ 

steps), resulting in high latency (few seconds per frame) that is prohibitive for real-time systems [12]. 

2. The Physics Gap: LiDAR data is governed by ray-casting physics. Existing generative models often treat points as generic coordinates, leading to ”hallucinations” (ghost points) in known free space [19], [15]. These artifacts, while visually plausible, can trigger phantom braking in autonomous systems. In this work, we propose Scanline-Consistent Range-Aware Diffusion, a framework that bridges the gap between generative fidelity and physical constraints. We argue that densification should be treated as probabilistic refinement  

> 1,2Zeping Zhang and Robert Lagani` ere are with the VIVA Research Lab, Department of Electrical and Computer Engineering, University of Ottawa, Ottawa, Ontario, Canada

zeping.zhang@uottawa.com, laganier@uottawa.ca   

> Fig. 1. Bleeding Artifacts in LiDAR Densification. An example of a densified result generated by the baseline method LiDPM. Note that without physical constraints, existing methods often generate severe ghost artifacts extending behind objects into free space.

rather than generation from scratch. By initializing with a coarse structural prior and employing Partial Diffusion (SDEdit) [10], we reduce the search space significantly. Our contributions are:  

> •

Efficiency: We propose a ”Densify Before You Detect” front-end using Partial Diffusion, achieving high-fidelity densification in just 156ms.  

> •

Physics-Awareness: We introduce Negative Ray Aug-mentation and Isotropic Probabilistic Consistency, forc-ing the model to respect sensor line-of-sight and elim-inate ghost points.  

> •

Performance: We achieve SOTA results on KITTI-360 and nuScenes. Crucially, our densified clouds can improve off-the-shelf 3D detection (such as Voxel-NeXt [2], CenterPoint [16]) without retraining. II. RELATED WORK 

A. Deterministic Completion 

Early methods treated densification as a regression prob-lem. Non-learning approaches like IP-Basic [4] use mor-phological operations for fast upsampling but lack semantic coherence. Deep learning approaches, including sparse-to-dense networks, regress missing depth values. While com-      

> arXiv:2603.26759v1 [cs.CV] 23 Mar 2026 Fig. 2. The proposed Scanline-Consistent Range-Aware Diffusion framework. (Left) Stage-0 constructs a coarse structural prior. (Middle) Stage-1 performs Partial Diffusion. The Probabilistic Range Head predicts both range noise and an uncertainty scale ˆb. (Right) Physics-Aware Constraints. We employ an Isotropic Probabilistic Consistency loss. The penalty is weighted by a spatial Gaussian kernel (Lateral Weight) scaled by ˆb, allowing the uncertainty to dynamically determine the valid geometric tolerance for both lateral misalignment ( d⊥) and radial occlusion ( d∥).

putationally inexpensive, these methods minimize mean-squared error, thus inherently favoring blurry, mean-seeking predictions that degrade object boundaries. 

B. Generative LiDAR Modeling 

To recover sharp details, recent works leverage genera-tive models. PVD [18] introduced point-voxel diffusion for object completion. Scaling to scenes, LiDARGen [19] and LiDiff [12] utilize stochastic denoising to hallucinate realistic structures. More recently, the field has expanded toward foundational world models using latent flow matching [8] and vision-guided feature diffusion [7]. However, these meth-ods often suffer from high inference latency and lack explicit free-space constraints, leading to ”ghost points” in open re-gions. In contrast to recent works such as DiffRefine [14] that focuses on proposal-level densification, we target full-scene consistency. While concurrent residual-based approaches like ResGen[5] attempt to improve stability, by formulating re-finement using unconstrained 3D residual vectors generation. This formulation still permits points to drift into physically invalid free space. In contrast, our method employs a physics-constrained Range-Only Diffusion that strictly updates scalar depths along fixed sensor rays, enforcing geometric validity via our Ray Consistency Loss. 

C. Our Approach 

We diverge from previous works by formulating densifi-cation as a conditional refinement task. Instead of generating from noise, we adopt the SDEdit paradigm [10], effectively ”warm-starting” the diffusion process with a deterministic prior. This allows the model to focus on high-frequency geometric recovery (improving efficiency) while our novel ray-consistency losses ensure that the output adheres to sensor physics (improving safety). III. M ETHODOLOGY 

A. Problem Formulation: Range-Manifold Densification 

Given a sparse LiDAR sweep Pin = {pi}Nin  

> i=1

, our goal is to generate a dense, physically valid point cloud Pout 

that recovers the underlying geometry. Unlike previous works that treat point cloud completion as a generic 3D generation problem (x, y, z ) ∼ pθ (x), we leverage the projective nature of LiDAR sensors. We decompose any point p ∈ R3 into a ray direction d ∈ S2 and a scalar range r ∈ R+.Instead of a deterministic regression, We parameterize the predictive distribution along each ray as a Laplacian 

p(r|d) = 12ˆb exp( − |r−μ| 

> ˆb

). Here, r represents the estimated depth (geometry), while b represents the aleatoric uncertainty (confidence). Crucially, as visualized in Fig. 2 (Right), we interpret ˆb not merely as 1D range noise, but as an isotropic uncertainty scale in 3D Euclidean space. This allows the model to learn a flexible spatial tolerance: points with high uncertainty are permitted to exhibit larger lateral and radial fluctuations, while high-confidence predictions must strictly adhere to the sensor’s line-of-sight geometry. 

B. Architecture Overview 

Our framework, illustrated in Figure 2, adopts a coarse-to-fine strategy comprising two stages:  

> •

Stage-0 (Deterministic Structural Prior): Efficiently aggregates a coarse set of candidate rays Pcoarse to define the query directions d.Fig. 3. Negative Ray Augmentation Strategy. (Panel A) On positive rays, the model learns a probabilistic range distribution. The uncertainty ˆb 

> (visualized as the green confidence region) represents the isotropic spatial tolerance learned by the network. (Panel B) On negative rays sampled from known free space, we enforce a zero-occupancy constraint. This explicitly teaches the Occupancy Head to suppress ghost points in free space where the sensor beam should pass through unimpeded.
> •

Stage-1 (Physics-Aware Range Refinement): Utilizes a conditional diffusion model to refine the range values 

r along these directions. A key aspect of our method is that, instead of generating from pure Gaussian noise, we employ a Partial Diffusion 

(SDEdit, Stochastic Differential Editing) process initialized from the coarse prior, ensuring high fidelity and low latency. 

C. Stage-0: Deterministic Structural Prior 

The first stage generates a structural skeleton Pcoarse that serves as both the geometric condition and the starting point for diffusion. To handle the sparsity of single-sweep LiDAR, we employ two complementary heuristics: 1) KNN Jittering: To densify local surfaces, we sample 

M neighbors for each input point pi via a Gaussian kernel N (pi, σ 2

> jit

), capturing local surface variations. 2) BEV Morphological Expansion: To recover missing structures (e.g., occluded vehicle parts), we project the raw sparse input Pin onto a binary bird-eye-view(BEV) grid, apply morphological dilation to close holes, and back-project active cells to 3D using local height statistics from Pin .The union of these sets forms Pcoarse . Note that Pcoarse 

is noisy and artifact-prone; its primary role is to provide a comprehensive set of candidate ray directions dcoarse =⃗p/ ∥⃗p∥ for the subsequent refinement stage. 

D. Stage-1: Hybrid Point-BEV Range Diffusion a) Partial Diffusion via SDEdit.: Standard diffusion models denoise from a standard Gaussian distribution 

N (0 , I ), requiring hundreds of steps to form structure. Since we already start from a reasonable initialization Pcoarse , we adopt the SDEdit formulation. We define the range state 

r0 = ∥pcoarse ∥. We first diffuse r0 to an intermediate timestep T ′ < T by adding Gaussian noise: 

rT ′ = √¯αT ′ r0 + √1 − ¯αT ′ ϵ, ϵ ∼ N (0 , I ) (1) , where ¯αt′ is the cumulative noise schedule parameter at timestep t′. The neural network then performs the reverse denoising process from t = T ′ to 0. This Range-Only Partial Diffusion focuses the model’s capacity on refining geometry rather than generating global structure from scratch. 

b) Hybrid Point-BEV Architecture.: To effectively pro-cess LiDAR data which is sparse in 3D but structured in BEV, we propose a Scatter-Conv-Gather architecture. The point encoder processes individual points augmented with scanline encodings (Ring ID, Azimuth, Time). Simultane-ously, a BEV branch facilitates long-range interaction: (1) Scatter: Point features are projected onto a high-resolution BEV feature grid using max-pooling within grid cells. (2) Conv: A lightweight 2D CNN processes the grid to propagate local context and complete shapes. (3) Gather: The enhanced grid features are bilinearly sampled back to the 3D points. This hybrid design captures fine-grained depth-wise details via the point branch and holistic scene structure via the BEV branch. 

c) Dual-Head Prediction.: The network terminates with two specialized heads:  

> •

Probabilistic Range Head: Predicts both the denoising component ˆϵ = ϵθ (rt, t, C) and the aleatoric uncertainty parameter ˆb. 

> •

Occupancy Head: Predicts a logit o ∈ R indicating whether the ray hits a valid surface or traverses free space. This is critical for filtering out ghost points generated by the Stage-0 dilation. 

E. Physics-Aware Training Objectives 

Our training strategy is strictly grounded in LiDAR physics. We optimize a composite objective L = Ldif f +

λocc Locc + λf ree Lf ree .

a) Range Denoising Loss.: Standard simple diffusion loss applied only to the scalar range channel: 

Ldif f = Et,ϵ 

∥ϵ − ϵθ (rt, t, C)∥2 (2) , where rt represents the latent range state at timestep t, ini-tialized from the coarse prior r0, and C = {P coarse , Escan }

represents the conditioning context, comprising the coarse prior and scanline encodings. 

b) Occupancy and Negative Ray Augmentation.: Acommon failure mode in densification is ”hallucination” in free space. To mitigate this, we introduce a Negative Ray Augmentation strategy during training. For each batch, we replace a fraction of valid rays with randomly sampled negative rays directing into known free space. We enforce a Binary Cross Entropy (BCE) loss on the Occupancy Head: 

Locc = BCE (σ(opred ), ⊮valid ) (3) where ⊮valid is 1 for real points and 0 for negative rays. This explicitly teaches the model to distinguish between valid geometry and empty space. 

c) Isotropic Probabilistic Consistency.: Ideally, the un-certainty scale ˆb should represent a 3D geometric confidence region. Instead of applying hard angular thresholds, we for-mulate a fully probabilistic constraint based on spatial soft-association. We interpret the predicted point as a probabilistic volume centered at ˆp with scale ˆb. For a neighbor ray R∗

> in

,Fig. 4. Qualitative Comparison of Densification Results. LiDPM preserves local detail but introduces scattered outlier points near object edges and occasional ghost points in free space. Our method generates sharp structures with clean free space in the demonstration. 

we calculate two geometric metrics shown in Fig. 2: the lateral distance d⊥ (point-to-line deviation) and the radial occlusion depth d∥ = ReLU (r∗ 

> in

− ˆrproj ). The free-space loss is weighted by the spatial alignment probability: 

Lf ree = 1

N

X

> ˆp

exp 



− d2

> ⊥

2ˆb2

| {z }

> Lateral Weight

·

 d∥

ˆb + λ log ˆb



(4) Here, the exponential term acts as a learned attention mech-anism: (1) If the model is uncertain ( ˆb is large), it tolerates larger lateral deviations d⊥ without enforcing the occlusion penalty. (2) If the model is confident ( ˆb is small), the spatial weight vanishes unless the point is perfectly aligned with the ray. This effectively treats ˆb as the isotropic geometric scale, unifying both radial and lateral consistency. 

d) Curriculum Density Schedule.: To stabilize training, we employ a curriculum schedule ρ(e) that linearly increases the target point density ratio from ×2 to ×8 over the training epochs. This prevents the model from collapsing when attempting to learn high-frequency details early in training. IV. EXPERIMENTS 

A. Experimental Setup 

Datasets. We use two autonomous-driving benchmarks: 

KITTI-360 [6] (Velodyne HDL-64E, 64 beams, ∼120K pts/sweep; sequence 03 for validation [19]; dense GT: 10-sweep accumulation, ∼250K pts, 2.1 × ratio) and 

nuScenes [1] (HDL-32E, 32 beams, ∼34.5K pts/sweep; offi-cial 700/150/150 split; dense GT: 5-sweep aggregation [16], 

∼150K pts, 4.3 × ratio). Table II gives point-count statistics. 

Metrics. We report chamfer distance (CD, in meters) and earth mover’s distance (EMD) for geometry, Free-Space Violation Ratio (FSVR) for free-space safety (ghost points), and Relative Error of Average number of Points (REAP) [5] (%) for density consistency. To calculate FSVR and account for the mathematical impossibility of exact point-to-ray intersection in continuous space, a generated point is classified as a free-space violation if its lateral distance to a ground-truth ray is strictly within a tolerance threshold of 0.1 m, and its radial depth is shorter than the ground-truth return. This 0.1 m threshold is empirically chosen to accommodate the physical beam divergence of typical LiDAR sensors and the slight registration noise inherent in multi-sweep ground-truth accumulation. 

Baselines. We compare against PVD [18], PCDM [17], LiDARGen [19], R2DM [11], LiDM [13], LiDPM [9], LiDiff [12], and IP-Basic [4]. Some implementation details are given in Table III. 

B. Quantitative Results 

Our method ranks first on all eight metric–dataset combi-nations. On KITTI-360, we achieve CD = 0.1512 m, which is 3.3% lower than LiDPM (0.1563 m). On nuScenes (sparser 32-beam, 4.3 × densification), CD = 0.1671 m, representing a 6.4% reduction over LiDPM (0.1785 m). These results demonstrate consistent gains across sensor types and sparsity regimes. The largest gain is observed on FSVR. On KITTI-360, FSVR decreases from 3.84% to 2.77%, an absolute reduction of 1.07% (27.9% relative reduction). On nuScenes, FSVR drops from 4.21% to 3.96%. Validating the Ray Consistency Loss Lfree . REAP (5.27% / 6.21%) is also best across methods. 

C. Qualitative Analysis 

Due to limited space, we show only our method and the second-best baseline (LiDPM[9]) on nuScenes and KITTI 360 (Figure 4). Our results show clean free space with fewer ghost points than LiDPM[9], sharp object boundaries (per-ray range denoising avoids cross-contamination), and consistent quality; LiDPM degrades more where input is sparser. 

D. Ablation Studies 

We investigate the contribution of each key component. All ablation experiments are conducted on the validation sets TABLE I 

QUANTITATIVE COMPARISON ON KITTI-360 AND NU SCENES VALIDATION SETS . ↓ INDICATES LOWER IS BETTER . B EST RESULTS ARE IN BOLD 

> AND SECOND

-BEST ARE UNDERLINED . PVD AND PCDM ARE CAPPED AT 8K POINTS DUE TO ATTENTION MEMORY LIMITS .                                                                                                         

> KITTI-360 (64-beam, ∼120K → ∼ 250K) nuScenes (32-beam, ∼34.5K → ∼ 150K)
> Method Venue CD (m) ↓EMD ↓FSVR (%) ↓REAP (%) ↓CD (m) ↓EMD ↓FSVR (%) ↓REAP (%) ↓
> Non-Generative
> IP-Basic [4] IROS’18 0.2614 0.3427 4.87 10.42 0.2973 0.3846 5.21 11.63
> Object-level Diffusion
> PVD [18] ICCV’21 0.3782 0.4965 11.53 17.28 0.4136 0.5374 12.74 18.95 PCDM [17] NPL’25 0.2735 0.3581 7.64 12.35 0.3024 0.3918 8.26 13.41
> LiDAR Scene Generation
> LiDARGen [19] ECCV’22 0.2948 0.3852 8.42 13.18 0.3287 0.4243 9.18 14.27 R2DM [11] ICRA’24 0.2416 0.3169 5.37 7.95 0.2745 0.3521 5.94 8.67 LiDM [13] CVPR’24 0.1937 0.2584 4.92 7.46 0.2193 0.2907 5.46 8.13
> Scene Completion / General
> LiDPM [9] IV’25 0.1563 0.2158 3.84 5.91 0.1785 0.2436 4.21 6.58 LiDiff [12] CVPR’24 0.2189 0.2876 5.71 9.23 0.2478 0.3194 6.37 10.12
> Ours —0.1512 0.1734 2.77 5.27 0.1671 0.2293 3.96 6.21

TABLE II 

DATASET STATISTICS . AVG . POINT COUNTS FOR SPARSE INPUT 

(SINGLE SWEEP ), DENSE GROUND TRUTH (MULTI -SWEEP ACCUMULATION ), AND OUR MODEL ’S TYPICAL OUTPUT .            

> Dataset Sparse Input Generated (Ours) Dense GT Ratio
> KITTI-360 (64-beam) ∼120K ∼240K ∼250K 2.1 ×
> nuScenes (32-beam) ∼34.5K ∼142K ∼150K 4.3 ×

TABLE III IMPLEMENTATION DETAILS OF THE PROPOSED METHOD .

Category Parameter Value 

Architecture Hidden size H 256 Layers 6BEV Resolution 64 × 64 

Training Total Epochs 200 Batch Size 8Optimizer AdamW ( lr = 10 −4)Schedule Cosine ( T = 1000 )Self-conditioning 0.5 Data/Stage Curriculum Density 2× → 8×

Stage-0 ( k, Jitter) 8, 0.10 m Inference DDIM Steps 50 SDEdit α 0.25 

of both datasets using the same training configuration. The results are shown in TABLE V. Removing Lfree raises FSVR by ∼3× on both datasets and degrades CD by ∼18%; removing Stage-0 degrades CD by ∼39% and doubles inference time; removing BEV layers degrades CD by ∼25–27% for only 34 ms overhead; scanline encoding gives ∼10–12% CD gain. 

E. Computational Efficiency 

Table IV compares efficiency across all methods. All timings are measured on a single NVIDIA RTX 4090 GPU with batch size 1, 50 DDIM steps, and 150K target points. Ours achieves the lowest latency (156 ms), smallest model (8.6M) thanks to 1D diffusion. While the non-learning base-line IP-Basic achieves lower latency through local interpola-tion, our generative approach justifies the computational cost by enabling the recovery of large-scale occlusions that are intractable for interpolation-based methods. (Table I). 

F. Downstream Application: 3D Object Detection 

We evaluate densification on nuScenes using two state-of-the-art LiDAR detectors: VoxelNeXt [2] (fully sparse, top-tier on nuScenes) and CenterPoint [16] (voxel-based anchor-free). Detectors are applied off-the-shelf (no retraining on densified data). Table VI reports overall mAP and NDS only (no per-class breakdown). The consistent gains across both detec-tors indicate that densification improves detection quality in aggregate. Our method yields higher mAP/NDS than LiDPM-densified input, which shows our point clouds qual-ity matches the statistics detectors were trained on. Densification consistently improves both detectors; gains are largest for distant/small objects (pedestrians, cones) where single-sweep data is sparse. Our method yields higher mAP/NDS than LiDPM-densified input and avoids the false positives from LiDPM’s free-space violations. Off-the-shelf gains (no detector fine-tuning) show our point clouds match the statistics detectors were trained on. V. CONCLUSIONS In this paper, we presented a scanline-consistent, physics-aware diffusion framework for LiDAR densification. By for-mulating a two-stage coarse-to-fine pipeline and a range-only diffusion manifold, we successfully integrated sensor-specific scanline geometry and physical free-space constraints into a generative process. Our hybrid Point-BEV architecture and ray-consistency mechanism enable the recovery of sharp object structures while effectively suppressing hallucinations in empty space. Extensive evaluations on the KITTI-360 and nuScenes benchmarks demonstrate that our method achieves state-of-the-art performance in both geometric fidelity and free-space safety, with significantly lower latency than exist-ing generative baselines. Furthermore, the consistent perfor-mance gains observed in off-the-shelf 3D detectors confirm the effectiveness of our approach as a robust and efficient front-end for autonomous driving perception stacks. TABLE IV 

COMPUTATIONAL EFFICIENCY . WE EVALUATE ALL METHODS WITH BATCH SIZE 1. † PVD ENCOUNTERED OOM WHEN PROCESSING 150K POINTS .

Method Ours LiDiff ‡ LiDPM R2DM LiDARGen PVD † LiDM IP-Basic 

#Params 8.6M 38.2M 50.7M 45.5M 80.8M 13.6M 75.6M CPU Infer. (ms) ↓ 156 2,340 1,559 434 823 OOM 20 13 

TABLE V 

ABLATION STUDY ON KITTI-360 AND NU SCENES . IMPACT OF THE 

RAY CONSISTENCY LOSS , S CANLINE ENCODING , C OARSE 

INITIALIZATION , AND BEV B RANCH .                                     

> KITTI-360 nuScenes Configuration CD ↓FSVR ↓CD ↓FSVR ↓Infer. (ms) Full Model (Ours) 0.1512 2.77 0.1671 3.96 156
> w/o Lfree (Ray Loss) 0.179 9.0 0.196 12.4 156 w/o Scanline Encoding 0.169 4.2 0.188 5.9 149 w/o Stage-0 (Pure Noise Init) 0.211 4.9 0.231 7.0 314 w/o BEV Branch (Point-only) 0.193 4.4 0.209 6.1 122

TABLE VI 

DOWNSTREAM 3D D ETECTION ON NU SCENES . OFF -THE -SHELF SOTA  

> DETECTORS ON RAW SPARSE

(1 SWEEP ), L IDPM-DENSIFIED , AND OUR DENSIFIED SCANS . A LL VALUES ARE OVERALL M AP AND NDS. 

Detector Input Data mAP ↑ NDS ↑

VoxelNeXt 

Raw Sparse (1 sweep) 61.2 68.4 Densified (LiDPM) 64.1 70.8 

Densified (Ours) 65.6 71.9 CenterPoint 

Raw Sparse (1 sweep) 56.3 64.5 Densified (LiDPM) 59.5 66.2 

Densified (Ours) 61.1 67.6 

REFERENCES [1] Holger Caesar, Varun Bankiti, Alex H. Lang, Sourabh Vora, Venice Erin Liong, Qiang Xu, Anush Krishnan, Yu Pan, Giancarlo Baldan, and Oscar Beijbom. nuscenes: A multimodal dataset for autonomous driving. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) . IEEE, June 2020. [2] Yukang Chen, Jianhui Liu, Xiangyu Zhang, Xiaojuan Qi, and Jiaya Jia. Voxelnext: Fully sparse voxelnet for 3d object detection and tracking. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 21674–21683, 2023. [3] Jonathan Ho, Ajay Jain, and Pieter Abbeel. Denoising diffusion probabilistic models. In Advances in Neural Information Processing Systems (NeurIPS) , volume 33, pages 6840–6851, 2020. [4] Jason Ku, Ali Harakeh, and Steven L. Waslander. In defense of classical image processing: Fast depth completion on the CPU. In 

2018 15th Conference on Computer and Robot Vision (CRV) , pages 16–22. IEEE, 2018. [5] Minxing Li, Dingbang Wu, Zhenghong Li, Xiaojuan Jin, Lue Fan, binwang, Shuqi Mei, Chao Zheng, and Zhaoxiang Zhang. Resgen: Residual diffusion model for lidar-based point cloud generation, 2026. Under review at ICLR 2026. [6] Yiyi Liao, Jun Xie, and Andreas Geiger. KITTI-360: A novel dataset and benchmarks for urban scene understanding in 2d and 3d. IEEE Transactions on Pattern Analysis and Machine Intelligence ,45(3):3292–3310, 2023. [7] Tianran Liu, Morteza Mousa Pasandi, and Robert Laganiere. Fsmdet: Vision-guided feature diffusion for fully sparse 3d detector. In 

International Conference on Image Analysis and Processing , pages 375–389. Springer, 2025. [8] Tianran Liu, Shengwen Zhao, and Nicholas Rhinehart. Towards foundational lidar world models with efficient latent flow matching. 

arXiv preprint arXiv:2506.23434 , 2025. [9] Tetiana Martyniuk, Gilles Puy, Alexandre Boulch, Renaud Marlet, and Raoul de Charette. LiDPM: Rethinking point diffusion for LiDAR scene completion, 2025. [10] Chenlin Meng, Yutong He, Yang Song, Jiaming Song, Jiajun Wu, Jun-Yan Zhu, and Stefano Ermon. Sdedit: Guided image synthesis and editing with stochastic differential equations. In International Conference on Learning Representations (ICLR) , 2022. [11] Kazuto Nakashima and Ryo Kurazume. Lidar data synthesis with denoising diffusion probabilistic models. In Proceedings of the IEEE International Conference on Robotics and Automation (ICRA) . IEEE, 2024. [12] Lucas Nunes, Rodrigo Marcuzzi, Benedikt Mersch, Jens Behley, and Cyrill Stachniss. Scaling diffusion models to real-world 3d LiDAR scene completion. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 14770– 14780, 2024. [13] Haoxi Ran, Vitor Guizilini, and Yue Wang. Towards realistic scene generation with LiDAR diffusion models. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 14738–14748, 2024. [14] Sangyun Shin, Yuhang He, Xinyu Hou, Samuel Hodgson, Andrew Markham, and Niki Trigoni. Diffrefine: Diffusion-based proposal specific point cloud densification for cross-domain object detection. In 

Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , 2025. [15] Z. Wang et al. Physics-informed diffusion models. In International Conference on Learning Representations (ICLR) , 2025. [16] Tianwei Yin, Xingyi Zhou, and Philipp Kr¨ ahenb¨ uhl. Center-based 3d object detection and tracking. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) ,pages 11784–11793, 2021. [17] Cheng Zhang, Zhiqiang Qi, Wenwen Yuan, Wanlong Qi, Zhengzheng Yang, and Zhaobing Su. PCDM: Point cloud completion by condi-tional diffusion model. Neural Processing Letters , 57(51), 2025. [18] Linqi Zhou, Yilun Du, and Jiajun Wu. 3d shape generation and completion through point-voxel diffusion. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) ,pages 5826–5835, 2021. [19] Vlas Zyrianov, Xiyue Zhu, and Shenlong Wang. Learning to generate realistic lidar point cloud. In The European Conference on Computer Vision (ECCV) , 2022.