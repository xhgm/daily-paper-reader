---
title: Adversarial Prompt Injection Attack on Multimodal Large Language Models
title_zh: 针对多模态大语言模型的对抗性提示注入攻击
authors: "Meiwen Ding, Song Xia, Chenqi Kong, Xudong Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29418v1"
tags: ["query:q11"]
score: 9.0
evidence: 针对多模态大语言模型的对抗性提示注入
tldr: 本研究针对多模态大语言模型（MLLM）的指令遵循特性，提出了一种隐蔽的视觉提示注入攻击方法。不同于以往可见的文本或视觉提示，该方法通过在图像中嵌入不可见的对抗性扰动，将恶意指令隐藏在视觉模态中。通过结合有界文本叠加和多粒度特征对齐优化，该方法能有效诱导闭源MLLM执行恶意任务，在多个多模态理解任务上展现出超越现有方法的攻击性能和迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29418v1/fig-001.webp\", \"caption\": \"Table 1. Performance under the soft criterion (target-image caption) on the image captioning task against different closed-source MLLMs.\", \"page\": 6, \"index\": 1, \"width\": 981, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.29418v1/fig-002.webp\", \"caption\": \"Table 2. Performance under the hard criterion (target text) on the image captioning task against different closed-source MLLMs.\", \"page\": 6, \"index\": 2, \"width\": 981, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2603.29418v1/fig-003.webp\", \"caption\": \"Table 3. Performance under the hard criterion (target text) on the VQA task against different closed-source MLLMs.\", \"page\": 7, \"index\": 3, \"width\": 983, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.29418v1/fig-004.webp\", \"caption\": \"Figure 3. Visualization of adversarial examples and corresponding perturbations generated by different attacks.\", \"page\": 7, \"index\": 4, \"width\": 840, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2603.29418v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of our proposed CoTTA. The modification on the source image comprises an adaptive covert trigger and an adversarial perturbation, which are both optimized to align the attacked image with both the target text and the target image Gimg . Gimg is iteratively updated (via added perturbations) to better match the target text and stay separated from the attacked image.\", \"page\": 4, \"index\": 5, \"width\": 1002, \"height\": 452}]"
motivation: 现有的提示注入攻击多依赖于易被察觉的文本或视觉提示，缺乏针对闭源多模态大语言模型的隐蔽攻击手段。
method: 该方法通过有界文本叠加提供语义引导，并迭代优化不可见的视觉扰动，使图像特征在多粒度水平上与恶意文本及图像目标对齐。
result: 在多个闭源多模态大语言模型上的实验证明，该方法在攻击成功率和跨模型迁移性方面均显著优于现有攻击技术。
conclusion: 研究揭示了多模态大语言模型在面对隐蔽视觉提示注入时的严重安全漏洞，强调了开发针对性防御机制的必要性。
---

## 摘要
尽管多模态大语言模型（MLLMs）在现实应用中的部署日益广泛，但其指令遵循行为使其容易受到提示注入攻击。现有的提示注入方法主要依赖于文本提示或人类用户可观察到的显性视觉提示。在这项工作中，我们研究了针对强大的闭源 MLLMs 的不可感知视觉提示注入，即将对抗性指令嵌入到视觉模态中。我们的方法通过有界文本叠加将恶意提示自适应地嵌入到输入图像中以提供语义引导。同时，通过迭代优化不可感知的视觉扰动，使受攻击图像的特征表示在粗粒度和细粒度层面上与恶意视觉及文本目标的特征表示对齐。具体而言，视觉目标被实例化为文本渲染图像，并在优化过程中逐步细化，以更忠实地呈现所需语义并提高可迁移性。在多个闭源 MLLMs 上针对两项多模态理解任务进行的广泛实验表明，与现有方法相比，我们的方法具有更优越的性能。

## Abstract
Although multimodal large language models (MLLMs) are increasingly deployed in real-world applications, their instruction-following behavior leaves them vulnerable to prompt injection attacks. Existing prompt injection methods predominantly rely on textual prompts or perceptible visual prompts that are observable by human users. In this work, we study imperceptible visual prompt injection against powerful closed-source MLLMs, where adversarial instructions are embedded in the visual modality. Our method adaptively embeds the malicious prompt into the input image via a bounded text overlay to provide semantic guidance. Meanwhile, the imperceptible visual perturbation is iteratively optimized to align the feature representation of the attacked image with those of the malicious visual and textual targets at both coarse- and fine-grained levels. Specifically, the visual target is instantiated as a text-rendered image and progressively refined during optimization to more faithfully represent the desired semantics and improve transferability. Extensive experiments on two multimodal understanding tasks across multiple closed-source MLLMs demonstrate the superior performance of our approach compared to existing methods.

---

## 论文详细总结（自动生成）

以下是对论文《Adversarial Prompt Injection Attack on Multimodal Large Language Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：多模态大语言模型（MLLM）如 GPT-4o、Gemini 等在视觉问答和图像描述任务中表现卓越，但其“指令遵循”特性带来了安全隐患。
*   **核心问题**：现有的提示注入攻击（Prompt Injection）通常依赖于人类肉眼可见的文本或图像修改，容易被察觉。而传统的对抗性攻击虽然隐蔽，但难以精确控制模型生成特定的恶意指令字符串。
*   **研究目标**：探索一种**不可感知（Imperceptible）**的视觉提示注入攻击，使攻击者能通过微小的像素扰动，诱导闭源 MLLM 执行任意指定的恶意命令（如“搜索某恶意网站”或“忽略之前指令”）。

### 2. 核心方法论：CoTTA 框架
论文提出了 **CoTTA (Covert Triggered dual-Target Attack)**，其核心思想是结合“隐蔽触发器”与“双目标特征对齐”：
*   **隐蔽文本触发器 (Covert Text Trigger)**：
    *   在图像中心嵌入一个有界的、可学习的文本叠加层。
    *   通过仿射变换（缩放 $s$ 和旋转 $\theta$）自适应调整文本形状，使其在满足 $L_\infty$ 范数约束（不可见）的同时，为模型提供初步的语义引导。
*   **双目标对齐 (Dual-Target Alignment)**：
    *   **动态目标图像 ($G_{img}$)**：不使用静态图像，而是迭代更新一个目标图像。该图像在特征空间中被“推”离原始图像，同时被“拉”向目标恶意文本。
    *   **粗到细的对齐**：同时在全局特征（Global features）和令牌级特征（Token-level features）上进行对齐，确保模型能捕捉到细粒度的语义信息。
*   **优化目标**：通过最大化受攻击图像与“目标文本”及“动态目标图像”之间的余弦相似度，利用代理模型（如 CLIP）生成可迁移至闭源模型的对抗扰动。

### 3. 实验设计
*   **评估任务**：
    1.  **图像描述 (Image Captioning)**：诱导模型生成特定的行动建议（如 "Search ICML"）。
    2.  **视觉问答 (VQA)**：诱导模型给出预设的错误答案（如 "None of the above"）。
*   **数据集**：NIPS 2017 对抗攻击竞赛数据集（100/1000张图）、ScienceQA 数据集。
*   **受试模型（Victim Models）**：GPT-4o, GPT-5, Gemini-2.5, Claude-4.5（注：论文日期标注为2026年，模型版本号反映了对未来版本的预测或模拟）。
*   **对比方法**：AttackVLM, AnyAttack, M-Attack, FOA-Attack, Agent-Attack。
*   **评估指标**：攻击成功率 (ASR) 和平均语义相似度 (AvgSim)，采用 LLM-as-a-judge 框架进行评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了优化周期为 300 个 epoch，扰动约束 $\epsilon=16/255$，使用了 CLIP-B/16、CLIP-B/32 和 LAION 作为代理特征提取器进行集成攻击。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖了 4 种主流闭源模型和 2 种任务。
    *   为了验证统计可靠性，在 1000 张图像的大规模样本上进行了扩展测试。
    *   进行了详细的消融实验（验证触发器、图像对齐、文本对齐、动态更新等组件的有效性）。
*   **评价**：实验设计较为充分，对比了多种 SOTA 对抗攻击方法，且涵盖了不同厂商的闭源模型，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **攻击有效性**：CoTTA 在 GPT-4o 和 Gemini-2.5 上表现极强，ASR 普遍超过 70%-80%，显著优于现有方法。
*   **模型鲁棒性差异**：Claude-4.5 被证明是测试中最具鲁棒性的模型，所有攻击方法的成功率均处于较低水平。
*   **关键组件**：消融实验表明，“图像对特征对齐”对攻击成功贡献最大，而“隐蔽触发器”能显著提升硬准则（精确字符串匹配）下的表现。

### 7. 优点与亮点
*   **隐蔽性与表达力的平衡**：成功在严格的像素扰动限制下实现了精确的文本指令注入，解决了传统对抗攻击“词不达意”的问题。
*   **动态目标策略**：提出的动态更新目标图像策略有效缓解了跨模态对齐中的“模态鸿沟”问题，增强了攻击的可迁移性。
*   **自适应触发器**：通过优化仿射参数而非固定位置，使隐蔽文本能更好地被模型视觉编码器捕获。

### 8. 不足与局限
*   **防御研究缺失**：论文主要侧重于攻击的实现，未深入探讨针对此类隐蔽注入的有效防御手段（如对抗训练或输入净化）。
*   **黑盒依赖性**：攻击仍然依赖于开源代理模型（CLIP等）的迁移性，如果闭源模型使用了完全不同的视觉编码器，攻击效果可能会打折扣。
*   **应用限制**：虽然在实验中表现优异，但在实时交互系统或具有严格输入过滤器的生产环境中，其稳定性仍有待验证。

（完）
