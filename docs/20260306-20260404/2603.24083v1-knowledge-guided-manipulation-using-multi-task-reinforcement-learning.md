---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 统一了感知、3D场景图和多任务机器人操作
tldr: 本文提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操作难题。该方法通过构建动态3D场景图来整合空间、包含及功能关系，并利用图神经网络将结构化知识与多模态感知信息融合。通过端到端强化学习训练，使关系特征直接服务于控制决策，显著提升了机器人在复杂场景中的操作性能、学习效率及泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24083v1/fig-001.webp\", \"caption\": \"TABLE II: Short- & long-horizon performance (M3PO vs. KG-M3PO).\", \"page\": 6, \"index\": 1, \"width\": 498, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2603.24083v1/fig-002.webp\", \"caption\": \"Fig. 6: Single-task results. Average normalized score vs. timesteps. Adding the KG improves sample efficiency and final score. For the M3PO backbone, the camera-only variant corresponds to M3PO, while camera+KG corresponds to KGM3PO.\", \"page\": 7, \"index\": 2, \"width\": 1054, \"height\": 538}]"
motivation: 针对机器人操作中因部分可观测性、遮挡和环境多变导致的决策鲁棒性不足问题，探索如何利用结构化知识引导策略学习。
method: 开发了KG-M3PO框架，通过动态维护的3D场景图提取语义关系，并将其作为归纳偏置与视觉、本体及语言特征共同输入强化学习策略。
result: 实验证明，该方法在多任务操作中取得了更高的成功率和采样效率，且在面对新物体和未知场景布局时表现出更强的泛化性。
conclusion: 将持续维护的结构化世界知识引入强化学习计算图，能有效对齐关系表示与控制需求，是实现通用机器人操作的关键。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务模型化策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识与策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一人称视角视觉，将开放词汇检测落地为度量关系表示。动态关系机制在每一步更新空间、包含和功能（affordance）边，并且图神经网络编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的模态）被编码进一个共享潜空间，强化学习智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供紧凑且具有语义信息的决策状态。在包含遮挡、干扰项和布局变化的系列操纵任务上的实验表明，该方法相较于强基线模型具有持续的提升：以知识为条件的智能体实现了更高的成功率、更好的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提：结构化、持续维护的世界知识是实现可扩展、泛化操纵的强大归纳偏置；当知识模块参与强化学习计算图时，关系表示与控制相一致，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在非结构化日常环境中进行操纵时，普遍面临**部分可观测性（Partial Observability）**问题（如物体被遮挡、置于容器内或因光照变化导致视觉失效）。
*   **研究动机**：
    *   纯反应式策略（像素到转矩）缺乏持久的世界模型，难以处理长程任务中的信用分配。
    *   传统基于模型的几何方法过于脆弱，难以适应多任务和不同机器人形态。
    *   现有的 3D 场景图多为静态或离线生成，无法实时引导强化学习（RL）策略。
*   **整体含义**：提出 KG-M3PO 框架，旨在通过动态维护的结构化知识（3D 场景图）来弥补感知与策略之间的鸿沟，使机器人能在复杂、有遮挡的环境中实现鲁棒的多任务操纵。

### 2. 方法论
核心思想是将**感知（Perception）**、**知识（Knowledge）**与**策略（Policy）**统一在一个端到端的强化学习框架下。
*   **动态 3D 场景图（Online 3DSG）**：利用 BBQ 算法从第一人称视角流中提取物体节点（包含 3D 边界框、CLIP 语义嵌入）和关系边（空间、包含、功能关系）。
*   **动态更新机制**：每 10 步刷新受影响的子图，通过轻量级检测器识别移动物体，确保知识的时效性。
*   **端到端图编码器**：
    *   使用图神经网络（GNN）将场景图 $K_t$ 编码为紧凑的知识向量 $k_t$。
    *   **关键技术**：GNN 编码器直接参与 RL 的计算图。策略梯度会通过策略损失函数回传至 GNN 参数，使得关系表示直接由控制性能（Reward）驱动，而非辅助监督信号。
*   **多模态融合**：将视觉嵌入（腕部相机）、知识嵌入（GNN 输出）、语言目标嵌入和本体感受数据融合进共享潜空间，驱动基于 M3PO 的控制循环。

### 3. 实验设计
*   **实验场景**：基于 NVIDIA Isaac Lab 构建的物理仿真环境，具有光照、纹理随机化和物理一致性。
*   **机器人平台**：Franka Emika Panda（7自由度）和 UR5（6自由度）。
*   **任务设置**：
    *   **全可观测（FO）**：抓取方块、放置方块、开柜子。
    *   **部分可观测（PO）**：抓取墙后的隐藏物体、移除遮挡物（圆锥）后抓取并放置物体。
*   **基线模型（Benchmarks）**：
    *   **无知识引导**：PPO, SAC, DreamerV3, TD-MPC2, IMPALA, M3PO (Camera-only)。
    *   **知识引导**：KG-M3PO（本文方法）。

### 4. 资源与算力
*   **硬件设施**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的平铺渲染 API，在单个进程中并行运行 **1024 个环境**。
*   **训练时长**：
    *   纯相机模式（M3PO）：4亿步训练约需 0.61 GPU-天。
    *   知识引导模式（KG-M3PO）：4亿步训练约需 0.79 GPU-天（由于增加了图更新和 GNN 计算，开销增加约 30%）。
    *   **效率对比**：虽然单步开销大，但 KG-M3PO 达到目标分数所需的总步数远少于基线。

### 5. 实验数量与充分性
*   **实验组数**：涵盖了 5 种不同难度的操纵任务，并在两种不同的机器人形态上进行了验证。
*   **消融实验**：进行了**场景图噪声消融实验**，测试了在边界框中心位移（0-5%）、尺寸缩放（0-10%）和旋转偏差（0-5°）下的性能表现。
*   **充分性评价**：实验设计较为充分。通过对比单任务与多任务、全可观测与部分可观测，清晰地展示了知识图谱的作用。噪声消融实验证明了该方法对现实感知误差具有一定的鲁棒性。

### 6. 主要结论与发现
*   **知识的重要性**：在全可观测任务中，加入知识图谱显著提升了采样效率；在部分可观测任务中，知识图谱是解决任务的关键，纯相机策略几乎无法完成。
*   **端到端对齐**：将 GNN 纳入 RL 损失函数进行训练，比使用预训练或分离的图特征效果更好，因为关系表示能自动对齐控制需求。
*   **多任务泛化**：单一策略能够同时处理多种操纵任务，证明了融合状态表示（感知+关系上下文）的有效性。

### 7. 优点
*   **架构统一**：成功将非结构化的视觉信息转化为结构化的图知识，并服务于连续控制。
*   **动态性强**：在线更新机制解决了传统场景图易过时的问题。
*   **鲁棒性**：噪声消融实验显示，即使在感知存在中度几何不确定性时，性能下降也较为平缓。
*   **高效训练**：利用大规模向量化仿真，在消费级 GPU 上实现了复杂任务的快速训练。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真（Sim）中完成，未在真实物理机器人上进行测试，存在 Sim-to-Real 的鸿沟。
*   **感知依赖**：虽然有噪声消融，但系统仍高度依赖底层检测器的质量，若检测器在大规模遮挡下彻底失效，知识图谱将无法构建。
*   **动态环境限制**：对于物体极速运动或频繁接触导致拓扑结构剧烈变化的场景，图的一致性维护可能面临挑战。
*   **计算延迟**：图更新和 GNN 推理增加了每一步的计算延迟，在对实时性要求极高的真机部署中可能成为瓶颈。

（完）
