---
title: Semantic Modeling for World-Centered Architectures
title_zh: 以世界为中心架构的语义建模
authors: "Andrei Mantsivoda, Darya Gavrilina"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01359v1"
tags: ["query:q13"]
score: 9.0
evidence: 多智能体系统的共享世界表示与以世界为中心的架构
tldr: 针对传统以智能体为中心架构在复杂领域中存在的语义不一致和难以解释等问题，本文提出了以世界为中心的多智能体系统（WMAS）。该架构通过共享的显式世界模型替代孤立的局部表示，确保了全局一致性与可验证行为。研究引入了语义模型作为数学形式化手段，并开发了 Ontobox 平台作为 WMAS 的具体实现，为构建稳定、可解释的复杂系统提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01359v1/fig-001.webp\", \"caption\": \"Figure 1: A Semantic Model for the World-Centered Architecture\", \"page\": 8, \"index\": 1, \"width\": 596, \"height\": 652}]"
motivation: 传统以智能体为中心的架构难以满足企业等结构化领域对语义一致性、可解释性和长期稳定性的严苛要求。
method: 提出 WMAS 架构，通过共享的显式语义模型对世界进行数学形式化建模，并依据本体显式性和规范性等维度对世界进行分类。
result: 实现了智能体在统一世界模型上的学习与协作，确保了系统行为的全局一致性及可验证性。
conclusion: WMAS 及其 Ontobox 平台为构建高度结构化且需严格监管的多智能体系统提供了一种更具优势的替代架构。
---

## 摘要
我们引入了以世界为中心的多智能体系统 (WMAS) 作为传统以智能体为中心架构的替代方案，并认为诸如企业和机构系统等结构化领域需要共享且显式的世界表示，以确保语义一致性、可解释性和长期稳定性。我们根据本体显式性、规范性等维度对世界进行了分类。在 WMAS 中，学习与协作是在共享的世界模型上运行，而非孤立的智能体局部表示，从而实现了全局一致性和可验证的系统行为。我们提出了语义模型作为表示此类世界的数学形式化方法。最后，我们介绍了 Ontobox 平台作为 WMAS 的一种实现。

## Abstract
We introduce world-centered multi-agent systems (WMAS) as an alternative to traditional agent-centered architectures, arguing that structured domains such as enterprises and institutional systems require a shared, explicit world representation to ensure semantic consistency, explainability, and long-term stability. We classify worlds along dimensions including ontological explicitness, normativity, etc. In WMAS, learning and coordination operate over a shared world model rather than isolated agent-local representations, enabling global consistency and verifiable system behavior. We propose semantic models as a mathematical formalism for representing such worlds. Finally, we present the Ontobox platform as a realization of WMAS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic Modeling for World-Centered Architectures》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的多智能体系统（MAS）通常采用“以智能体为中心”的架构，每个智能体维护私有的环境表示。在企业、金融、医疗等高度结构化的领域，这种模式会导致智能体间的认知分歧、语义不一致、决策逻辑不可追溯以及系统行为难以验证。
*   **研究动机**：为了解决复杂机构环境下的语义一致性、可解释性和长期稳定性问题，作者提出了一种“以世界为中心”的架构（WMAS）。在这种架构中，世界本身（而非个体智能体）是主要的表示、学习和协调基质。

### 2. 论文提出的方法论
*   **核心思想**：将“世界”定义为一个显式的、共享的数学形式化模型 $W = (E, R, S, A, T, C)$（包含实体、关系、状态空间、动作、转换函数和约束）。智能体在操作上从属于这个共享模型。
*   **关键技术细节——双层语义模型**：
    1.  **基础语义层（对象本体）**：存储事实性知识、操作参数和领域结构。它强制执行事务正确性，并作为所有智能体的共享信念上下文。
    2.  **因果知识层**：在本体之上构建逻辑-概率推理系统，捕捉世界演化中的概率规律和因果依赖。
*   **语义机器学习（SML）**：一种增量式、持续学习机制。当基础语义层发生状态转换（如业务交易）时，SML 会自动更新因果知识层，确保知识与现实同步，无需全局重新训练。
*   **智能体集成**：支持异构智能体（包括大语言模型 LLM）。LLM 智能体被视为“受限推理器”，其输入、动作和解释都受到共享语义世界的约束，从而减少幻觉并增强可验证性。

### 3. 实验设计
*   **实验场景**：论文通过 **Ontobox**（原名 bSystem）平台实现了该架构，并在多个实际领域进行了验证，包括：
    *   私人诊所管理系统。
    *   汽车贷款管理系统。
    *   水培农场自动控制系统。
*   **Benchmark 与对比**：本文属于架构论证类论文，未提供标准化的公开数据集 Benchmark 对比。它更多地是作为一种“存在证明”（Existence Proof），证明该架构在处理大规模企业级多智能体管理任务时的可行性。

### 4. 资源与算力
*   **算力说明**：文中**未明确提到**具体的 GPU 型号、数量或训练时长。
*   **计算特性**：作者强调 SML 算法具有“局部感知”和“增量更新”的特性，旨在通过缩小搜索空间来降低计算复杂度，使其在大型演化环境中具有可管理性。

### 5. 实验数量与充分性
*   **实验规模**：提到了至少三个不同领域的工业级应用案例。
*   **充分性评价**：从系统工程和架构验证的角度看，实验涵盖了医疗、金融和农业等不同性质的规范化领域，具有一定的代表性。但从机器学习论文的标准来看，缺乏定量的消融实验和与传统 MAS 架构在相同任务下的性能指标（如延迟、吞吐量、错误率）对比，实验的客观性和公平性主要建立在功能实现和逻辑一致性上。

### 6. 论文的主要结论与发现
*   **架构适用性**：以世界为中心的架构最适合“本体显式、结构稳定、规则驱动（规范性强）、状态可访问”的领域（如企业管理）。
*   **语义一致性**：通过共享的语义模型，可以有效消除智能体间的信念冲突，简化行为验证。
*   **LLM 落地**：语义模型为 LLM 提供了坚实的“地面真理”（Ground Truth），是实现可解释 AI 和人类在环控制的有效途径。

### 7. 优点
*   **理论严谨**：为“世界”和“语义模型”提供了清晰的数学定义。
*   **解决痛点**：直接针对 MAS 中常见的语义漂移和黑盒决策问题。
*   **实用性强**：Ontobox 平台的成功应用证明了该理论可以转化为生产力工具，提高开发效率（语义模型可视为可执行的技术规范）。

### 8. 不足与局限
*   **应用范围受限**：不适用于感知主导、语义模糊或高度动态的领域（如开放环境下的自动驾驶或未知空间的机器人探索）。
*   **初期成本高**：构建高质量的对象本体需要深厚的领域专家知识，初期建模工作量巨大。
*   **缺乏定量评估**：论文未提供与主流 MAS 框架在计算效率或决策准确率方面的量化对比数据。

（完）
