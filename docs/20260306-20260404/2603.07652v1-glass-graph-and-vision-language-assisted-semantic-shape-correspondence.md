---
title: "GLASS: Graph and Vision-Language Assisted Semantic Shape Correspondence"
title_zh: GLASS：图与视觉语言辅助的语义形状对应
authors: "Qinfeng Xiao, Guofeng Mei, Qilong Liu, Chenyuan Yi, Fabio Poiesi, Jian Zhang, Bo Yang, Yick Kit-lun"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07652v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于机器人操作的语义形状对应
tldr: 针对非等距变形和跨类别3D形状匹配中几何特征模糊的难题，本文提出GLASS框架。该方法通过融合几何谱分析与视觉语言大模型，引入了视图一致的多视图特征提取、基于零样本分割的语义嵌入以及图辅助对比损失。GLASS在无需人工监督的情况下，实现了全局一致且语义准确的密集匹配，在SNIS、SMAL等多个挑战性数据集上显著超越了现有SOTA模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.07652v1/fig-001.webp\", \"caption\": \"Table 2: Average geodesic errors ×100 (↓) of GLASS and baselines on non-isometric benchmarks. The best and the second-best are shown in blue and orange respectively.\", \"page\": 11, \"index\": 1, \"width\": 683, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2603.07652v1/fig-002.webp\", \"caption\": \"Table 1: Average geodesic errors (↓) of GLASS and baselines on the inter-class benchmark. The best and the second-best are shown in blue and orange respectively.\", \"page\": 10, \"index\": 2, \"width\": 610, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2603.07652v1/fig-003.webp\", \"caption\": \"Table 4: Ablation study on novel components. Average geodesic errors (×100) comparing different design choices on SMAL and TOPKIDS.\", \"page\": 13, \"index\": 3, \"width\": 683, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2603.07652v1/fig-004.webp\", \"caption\": \"Table 3: Average geodesic errors ×100 (↓) of GLASS and baselines on near-isometric benchmarks. The best and the second-best are shown in blue and orange respectively.\", \"page\": 12, \"index\": 4, \"width\": 698, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2603.07652v1/fig-005.webp\", \"caption\": \"Fig. 2: The overall structure of our GLASS. It consists of three key stages: (1) View-consistent feature lifting, where we synthesize coherent textures and lift SDDINO features [48] onto the 3D surface; (2) Language-guided semantic injection, which enriches visual descriptors with linguistic priors using sentence encoder (e.g., SigLip [44]) embeddings derived from zero-shot region proposals; and (3) Regionaware map optimization, where we employ a functional map guided by a novel semantic graph-aware contrastive loss to ensure structural and semantic consistency.\", \"page\": 5, \"index\": 5, \"width\": 721, \"height\": 341}]"
motivation: 传统功能映射方法过度依赖等距性假设，难以处理非等距变形及跨类别形状匹配中的语义模糊问题。
method: 结合视觉语言模型的语义先验与几何谱分析，利用视图一致性特征、语言嵌入及图辅助对比损失来增强区域间的结构一致性。
result: "在SNIS、SMAL和TOPKIDS等基准测试中，GLASS将平均测地误差分别降低了57%、25%和37%，刷新了SOTA性能。"
conclusion: GLASS证明了将视觉语言大模型的语义知识与几何结构约束相结合，是解决复杂3D形状匹配问题的有效途径。
---

## 摘要
在 3D 形状之间建立密集对应关系对于纹理传递、形状插值和机器人操作等基础下游任务至关重要。然而，在没有人工监督的情况下学习这些映射仍然是一个巨大的挑战，特别是在严重的非等距形变以及几何线索模糊的跨类别场景下。传统的函数映射（functional map）方法虽然设计精巧，但由于依赖等距性，通常在这些情况下表现不佳。为此，我们提出了 GLASS，这是一个通过将几何谱分析与来自视觉语言基础模型的丰富语义先验相结合来弥补这一差距的框架。GLASS 引入了三项关键创新：(i) 一种视图一致性策略，能够从强大的视觉基础模型中提取鲁棒的多视图视觉特征；(ii) 通过零样本 3D 分割将语言嵌入注入顶点描述符，从而捕捉高层级的部件语义；(iii) 一种图辅助的对比损失，通过利用区域间的测地线和拓扑关系，强制实现区域间的结构一致性（例如，源形状的“头部” $\leftrightarrow$ 目标形状的“头部”）。这种设计使 GLASS 能够在没有真值监督的情况下学习全局连贯且语义一致的映射。大量实验表明，GLASS 在所有方案中均达到了最先进的性能，在保持标准近等距任务高精度的同时，显著提升了在挑战性设置下的表现。具体而言，它在跨类别基准 SNIS 以及非等距基准 SMAL 和 TOPKIDS 上分别实现了 0.21、4.5 和 5.6 的平均测地误差，相比 URSSM 基准（分别为 0.49、6.0 和 8.9）误差分别降低了 57%、25% 和 37%。

## Abstract
Establishing dense correspondence across 3D shapes is crucial for fundamental downstream tasks, including texture transfer, shape interpolation, and robotic manipulation. However, learning these mappings without manual supervision remains a formidable challenge, particularly under severe non-isometric deformations and in inter-class settings where geometric cues are ambiguous. Conventional functional map methods, while elegant, typically struggle in these regimes due to their reliance on isometry. To address this, we present GLASS, a framework that bridges the gap by integrating geometric spectral analysis with rich semantic priors from vision-language foundation models. GLASS introduces three key innovations: (i) a view-consistent strategy that enables robust multi-view visual feature extraction from powerful vision foundation models; (ii) the injection of language embeddings into vertex descriptors via zero-shot 3D segmentation, capturing high-level part semantics; and (iii) a graph-assisted contrastive loss that enforces structural consistency between regions (e.g., source's head'' $\leftrightarrow$ target's head'') by leveraging geodesic and topological relationships between regions. This design allows GLASS to learn globally coherent and semantically consistent maps without ground-truth supervision. Extensive experiments demonstrate that GLASS achieves state-of-the-art performance across all regimes, maintaining high accuracy on standard near-isometric tasks while significantly advancing performance in challenging settings. Specifically, it achieves average geodesic errors of 0.21, 4.5, and 5.6 on the inter-class benchmark SNIS and non-isometric benchmarks SMAL and TOPKIDS, reducing errors from URSSM baselines of 0.49, 6.0, and 8.9 by 57%, 25%, and 37%, respectively.

---

## 论文详细总结（自动生成）

### 论文总结：GLASS —— 图与视觉语言辅助的语义形状对应

#### 1. 核心问题与研究动机
建立 3D 形状间的密集对应关系（Dense Correspondence）是纹理传递、形状插值和机器人操作等任务的基础。
*   **研究背景**：传统的函数映射（Functional Map）方法依赖于“等距性”（Isometry）假设，即形状在变形过程中表面距离保持不变。
*   **核心问题**：当面对**非等距变形**（如不同体型的动物）或**跨类别匹配**（如人到马）时，纯几何线索变得模糊或具有误导性。
*   **动机**：现有的视觉基础模型（VFM）虽能提供语义特征，但存在多视图不一致、缺乏语言语义指导以及忽略高层级拓扑结构等问题。

#### 2. 方法论
GLASS 框架通过整合几何谱分析与视觉语言大模型的语义先验，提出了以下核心技术：
*   **视图一致性特征提升（View-consistent Feature Lifting）**：
    *   针对无纹理模型，采用 SyncMVD 算法生成高质量、跨视图一致的纹理。
    *   利用 SD-DINO（结合 Stable Diffusion 和 DINOv2）提取 2D 语义特征，并通过多视图投影融合到 3D 顶点上，确保特征场的稳定性。
*   **语言引导的语义注入（Language-guided Semantic Injection）**：
    *   利用 SATR 进行零样本 3D 语义分割，将形状划分为“头”、“躯干”、“腿”等区域。
    *   使用 SigLip 编码器获取这些部件名称的语言嵌入（Language Embeddings），并将其与视觉特征拼接，增强描述符的辨识度。
*   **图辅助对比损失（Graph-Assisted Contrastive Loss, GAC）**：
    *   构建语义区域图，节点为语义部件，边代表拓扑连接关系。
    *   **核心公式逻辑**：定义一种基于图最短路径的语义距离 $D_{sem}$。在对比学习中，拉近同一区域内的顶点特征，并根据 $D_{sem}$ 动态调整不同区域间的排斥力，强制实现结构一致性。
*   **函数映射优化**：使用轻量级的 DiffusionNet 作为学习适配器，在谱域内优化功能映射矩阵，确保映射的全局连贯性和平滑性。

#### 3. 实验设计
*   **数据集**：
    *   **SNIS**：跨类别匹配（人、动物、类人生物），含 34 个稀疏关键点标注。
    *   **SMAL / TOPKIDS**：非等距变形及含有拓扑噪声的挑战性场景。
    *   **FAUST / SCAPE / SHREC19**：标准的近等距人像匹配。
*   **对比方法**：
    *   **公理化方法**：ZoomOut, Smooth Shells 等。
    *   **函数映射方法**：URSSM, SURFMNet 等。
    *   **语义方法**：Diff3F, ZSC, DenseMatcher 等。
*   **评价指标**：平均测地误差（Average Geodesic Error）。

#### 4. 资源与算力
*   **算力需求**：论文提到该框架兼容单张高端 GPU（如文中实验可能使用的级别）。
*   **耗时统计**：
    *   **预处理**：纹理合成约 30s/形状，语义分割约 80s/形状，特征提取约 5s/形状。
    *   **推理**：单对形状匹配耗时小于 1s。
*   **内存**：峰值显存占用约 19GB（主要在特征提取阶段）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从近等距到极端非等距、再到跨类别的全场景测试。
*   **消融实验**：针对纹理策略（有无纹理、视图一致性）、特征组成（WKS、XYZ、视觉、语言）以及损失函数（有无 GAC 损失）进行了详细对比。
*   **充分性评价**：实验设计非常全面且客观。通过在多个异构数据集上取得一致的 SOTA 性能，证明了该方法在处理几何歧义方面的鲁棒性。

#### 6. 主要结论与发现
*   **性能飞跃**：在 SNIS 跨类别任务上，误差比基准 URSSM 降低了 **57%**；在非等距任务 SMAL 和 TOPKIDS 上分别降低了 **25%** 和 **37%**。
*   **语义的重要性**：实验证明，注入语言嵌入能有效区分几何相似但语义不同的部位（如前腿与后腿）。
*   **结构约束的价值**：图辅助对比损失能有效纠正局部误匹配，使结果在拓扑上更合理。

#### 7. 优点与亮点
*   **多模态融合**：成功将视觉（VFM）、语言（VLM）与经典几何谱分析（Functional Maps）统一在一个框架内。
*   **无需人工监督**：全流程基于零样本（Zero-shot）先验和自监督优化，无需手动标注对应关系。
*   **鲁棒性强**：对拓扑噪声和大幅度形变具有极强的抵抗力，解决了传统方法在跨物种匹配上的痛点。

#### 8. 不足与局限
*   **计算开销**：依赖多个预训练大模型（SyncMVD, SATR, SD-DINO, SigLip），预处理流程较长，且显存要求较高。
*   **局部匹配限制**：目前主要针对完整形状匹配，对于大面积缺失的局部形状匹配（Partial Shape Matching）仍有待探索。
*   **流水线依赖**：性能高度依赖于前端分割和纹理生成的质量，若预处理模型失效，会影响最终匹配精度。

（完）
