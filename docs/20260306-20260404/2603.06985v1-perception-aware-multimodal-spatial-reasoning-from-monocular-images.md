---
title: Perception-Aware Multimodal Spatial Reasoning from Monocular Images
title_zh: 基于单目图像的感知感知多模态空间推理
authors: "Yanchun Cheng, Rundong Wang, Xulei Yang, Alok Prakash, Daniela Rus, Marcelo H Ang, ShiJie Li"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.06985v1"
tags: ["query:q11"]
score: 9.0
evidence: 具有对象定位能力的视觉语言模型多模态空间推理框架
tldr: 针对自动驾驶中单目图像空间推理的挑战（如尺度变化和外观歧义），本文提出一种感知感知的多模态推理框架。该框架通过视觉参考标记（VRT）实现显式的以物体为中心的接地能力，并构建了多模态思维链（MM-CoT）数据集以增强跨模态交互。通过确定性排序策略，该方法在SURDS基准测试中显著超越了现有模型，证明了感知与推理结合对提升单目驾驶场景理解的重要性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.06985v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the perception-aware multimodal reasoning framework. Visual tokens from a ViT encoder are projected into the LLM, while a Dynamic Embedding Module injects object tokens and index tokens to enable explicit object-centric grounding. Grounding markers delimit visual reference spans, allowing the LLM to jointly reason over text, visual cues, and object instances. Right: examples of detection and grounding, region understanding, and grounded image conversation supported by the framework.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 447}, {\"url\": \"assets/figures/arxiv/2603.06985v1/fig-002.webp\", \"caption\": \"TABLE I COMPARISON RESULTS ON VISUAL SPATIAL REASONING TASKS.\", \"page\": 5, \"index\": 2, \"width\": 860, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2603.06985v1/fig-003.webp\", \"caption\": \"Fig. 2. Illustrative examples of the benchmark QA pairs on both single-object and multiobject\", \"page\": 6, \"index\": 3, \"width\": 1002, \"height\": 408}]"
motivation: 现有的视觉语言模型在处理单目图像的细粒度几何感知和复杂空间推理时仍存在困难。
method: 提出利用视觉参考标记（VRT）表示物体，并结合多模态思维链（MM-CoT）数据集和确定性排序策略进行监督微调。
result: 在SURDS基准测试的单物体和多物体任务中均大幅领先，甚至优于经过强化学习后训练的方法。
conclusion: 准确的感知能力与多模态推理相互促进，是实现鲁棒的单目驾驶场景空间理解的关键。
---

## 摘要
单目图像的空间推理对于自动驾驶至关重要，然而当前的视觉语言模型（VLM）在细粒度几何感知方面仍面临挑战，尤其是在大尺度变化和目标外观模糊的情况下。我们提出了一种简单而有效的感知感知多模态推理框架，赋予了 VLM 显式的以对象为中心的定位能力。该框架不再依赖文本形式的边界框输出，而是利用其空间范围内的所有视觉参考标记（VRT）来表示每个被引用的对象，从而使视觉证据和文本推理能够在统一的标记空间中进行联合处理。为了进一步增强跨模态交互，我们构建了一个多模态思维链（MM-CoT）数据集，其中注入了对齐的视觉和文本推理信号。我们引入了一种确定性排序策略，使得对本质上无序的 VRT 集合的监督能够与 VLM 的自回归下一标记预测完全兼容。仅通过标准的监督微调，我们的方法就在 SURDS 基准测试中取得了显著提升，在单对象和多对象任务上均大幅超越了以往的方法，包括那些采用基于强化学习（RL）后训练的方法。这些结果表明，准确的感知与多模态推理是相辅相成的，二者共同构成了在挑战性单目驾驶场景中实现鲁棒空间理解的关键。

## Abstract
Spatial reasoning from monocular images is essential for autonomous driving, yet current Vision-Language Models (VLMs) still struggle with fine-grained geometric perception, particularly under large scale variation and ambiguous object appearance. We propose a simple yet effective perception-aware multimodal reasoning framework that equips VLMs with explicit object-centric grounding ability. Instead of relying on textual bounding-box outputs, each referred object is represented using all Visual Reference Tokens (VRTs) within its spatial extent, enabling visual evidence and textual reasoning to be processed jointly in a unified token space. To further strengthen cross-modal interaction, we construct a Multimodal Chain-of-Thought (MM-CoT) dataset that injects aligned visual and textual reasoning signals. A deterministic ordering strategy is introduced to make supervision over inherently unordered VRT sets fully compatible with the VLM's autoregressive next-token prediction. With only standard supervised fine-tuning, our method achieves substantial improvements on the SURDS benchmark, outperforming previous approaches - including those using RL-based post-training - by a large margin across both single-object and multi-object tasks. These results demonstrate that accurate perception and multimodal reasoning are mutually reinforcing, and together form the key to robust spatial understanding in challenging monocular driving scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：基于单目图像的感知感知多模态空间推理

#### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，从单目图像中进行空间推理（如推断物体的 3D 位置、距离和朝向）至关重要。然而，现有的视觉语言模型（VLM）在处理此类任务时面临两大挑战：
*   **细粒度几何感知不足**：在单目设置下，物体尺度变化剧烈且外观存在歧义，VLM 难以精确感知。
*   **表示形式缺陷**：传统方法依赖文本形式的边界框坐标（Bounding Box），这种表示缺乏语义表达力，且与视觉特征空间存在隔阂。
本文旨在通过一种“感知优先”的框架，增强 VLM 对物体的显式定位能力，从而提升其在复杂驾驶场景下的空间推理水平。

#### 2. 方法论：核心思想与关键技术
论文提出了一种**感知感知（Perception-Aware）的多模态推理框架**，核心思想是将物体定位与文本推理统一在同一个标记（Token）空间内。
*   **视觉参考标记（VRT）表示法**：不再预测文本坐标，而是将物体表示为其空间范围内所有视觉补丁（Patch）对应的 VRT 集合。这些标记直接源自图像编码器，具有丰富的语义和空间信息。
*   **多模态思维链（MM-CoT）**：构建了包含 `<loc>`（定位）、`<think>`（推理过程）和 `<answer>`（最终答案）的数据集。在推理链中注入视觉证据，使模型在回答前先显式地“看到”并定位目标物体。
*   **确定性排序策略**：由于 VLM 是自回归的（按顺序预测下一个标记），而物体对应的 VRT 集合本质上是无序的，论文引入了一种预定义的排序规则，将无序集合转化为确定性序列，从而使监督训练与自回归预测完全兼容。
*   **损失函数**：结合了文本的交叉熵损失（$L_{text}$）和基于 PaDT 的视觉接地损失（$L_{vrt}$）。

#### 3. 实验设计
*   **数据集与场景**：主要在 **SURDS** 基准上进行评估。该基准基于 **nuScenes** 自动驾驶数据集构建，包含 27,152 张训练图像和 5,919 张验证图像。
*   **任务类型**：涵盖六大空间推理任务：偏航角（Yaw）、像素定位、深度范围、成对距离比较、左右排序、前后关系。
*   **对比方法**：
    *   通用大模型：GPT-4o, GPT-4o-mini, Gemini-1.5-pro, Gemini-2.0-flash。
    *   开源 VLM：Qwen2.5-VL 系列（3B/7B/72B）。
    *   空间专用模型：SpatialBot, SpatialRGPT, SURDS-3B。

#### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：总训练过程耗时约 **47 GPU-hours**。
*   **模型配置**：以 Qwen2.5-VL-3B 为基础架构，学习率为 $1 \times 10^{-5}$，采用恒定调度器。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的基准对比（Tab I）和详细的消融实验（Tab II）。
*   **充分性与公平性**：
    *   对比了从 3B 到 72B 不同参数规模的模型，甚至包括闭源的最强模型，证明了其方法的优越性。
    *   消融实验分别验证了感知先验（PaDT 初始化）和多模态思维链（MM-CoT）的贡献，逻辑严密。
    *   实验设置遵循 SURDS 官方标准，确保了结果的可比性和客观性。

#### 6. 主要结论与发现
*   **感知是推理的基础**：通过显式预测 VRT 标记，模型在单物体几何属性（如深度、偏航角）上的表现大幅提升，其中深度估计得分从 27.68 飙升至 95.39。
*   **多模态交互的威力**：MM-CoT 允许文本推理与视觉证据在统一空间交互，显著增强了多物体关系理解（如距离比较和方位排序）。
*   **SFT 优于 RL**：仅通过简单的监督微调（SFT），该方法就超越了许多经过昂贵强化学习（RL）后训练的模型，证明了架构设计的有效性。

#### 7. 优点（亮点）
*   **架构简洁高效**：通过 VRT 统一了视觉定位和文本生成，避免了复杂的坐标解码器。
*   **解决序冲突**：提出的确定性排序策略巧妙地解决了无序视觉集合与有序自回归模型之间的矛盾。
*   **性能卓越**：在具有挑战性的单目驾驶场景中刷新了多项任务的 SOTA 记录。

#### 8. 不足与局限
*   **缺乏 RL 探索**：虽然 SFT 效果很好，但论文未探讨强化学习是否能进一步提升长程推理能力（作者将其列为未来工作）。
*   **单目局限性**：尽管针对单目优化，但在极极端遮挡或超远距离场景下，单目视觉固有的深度歧义仍可能导致失效。
*   **推理开销**：由于需要预测大量的 VRT 标记作为定位依据，可能会增加推理时的 Token 序列长度，从而影响推理速度。

（完）
