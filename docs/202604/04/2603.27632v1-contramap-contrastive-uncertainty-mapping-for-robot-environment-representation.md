---
title: "ContraMap: Contrastive Uncertainty Mapping for Robot Environment Representation"
title_zh: ContraMap：用于机器人环境表示的对比不确定性映射
authors: "Chi Cuong Le, Weiming Zhi"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27632v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于机器人环境表示的对比不确定性映射
tldr: 针对机器人感知中环境表示的可靠性问题，本文提出ContraMap。该方法通过引入合成噪声样本训练一个显式的“不确定性”类别，将未观测区域视为对比类，实现了无需贝叶斯推理的实时环境预测与空间不确定性估计。在2D/3D建图及场景重建实验中，ContraMap在保持建图质量的同时，提供了空间连贯的不确定性估计，且计算效率显著优于传统贝叶斯核映射方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27632v1/fig-001.webp\", \"caption\": \"Fig. 6: Reconstruction results on SceneNet dataset.\", \"page\": 6, \"index\": 1, \"width\": 508, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.27632v1/fig-002.webp\", \"caption\": \"Fig. 2: Predictive uncertainty for a Gaussian Process (GP) and neural-network baselines on three toy datasets. Orange/Magenta points are in-distribution training samples, and Red points are out-of-distribution samples. Background shading indicates relative uncertainty (brighter = higher, darker = lower). Our method best matches the GP “gold standard”: it stays confident near observed data and becomes uncertain as inputs move away from the training distribution.\", \"page\": 2, \"index\": 2, \"width\": 898, \"height\": 452}, {\"url\": \"assets/figures/arxiv/2603.27632v1/fig-003.webp\", \"caption\": \"Fig. 8: Representative scene reconstructions. Col. 1: RGB. Col. 2: segmented point cloud. Col. 3: our reconstructed mesh. Col. 4: our uncertainty (softmax auxiliary head; lighter = higher). Col. 5: V-PRISM mesh. Col. 6: V-PRISM uncertainty heatmap (lighter = higher). Following [10], Cols. 4 and 6 show uncertainty on a 2D scene slice (bottom = nearer the camera). Object boundaries from the top-view projection are overlaid for spatial context.\", \"page\": 7, \"index\": 3, \"width\": 512, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2603.27632v1/fig-004.webp\", \"caption\": \"Fig. 7: Reconstruction results on SemanticKITTI dataset.\", \"page\": 7, \"index\": 4, \"width\": 514, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2603.27632v1/fig-005.webp\", \"caption\": \"Fig. 3: Overview of ContraMap. Our method employs a softmax classifier network with an additional output node to jointly represent the environment and estimate uncertainty. Observed data (e.g., LiDAR scan or segmented point cloud) are augmented with negative samples and noise, where the noise is labeled as an additional (C+1)-th or ”uncertain” class. The augmented data are then projected into a feature vector space using a set of reference points, forming the training dataset Dtrain. The model is trained on this dataset using a first-order optimization method. Once trained, we can query the model at arbitrary locations in the environment to obtain either environment mapping results or uncertainty estimates via the additional ”uncertain” node.\", \"page\": 3, \"index\": 5, \"width\": 1000, \"height\": 294}]"
motivation: 传统的机器人感知方法难以在保证实时性的同时，有效识别因观测稀疏或缺失导致的不确定区域。
method: 提出一种对比连续映射方法，通过引入合成噪声样本训练显式的不确定性类别，将环境预测转化为判别式分类问题。
result: 实验证明ContraMap在2D占据地图、3D语义地图和桌面重建任务中，均能生成空间连贯的不确定性估计，且计算效率大幅提升。
conclusion: ContraMap通过对比学习框架成功实现了高效、实时的环境表示与不确定性量化，是贝叶斯核映射方法的有力替代方案。
---

## 摘要
可靠的机器人感知不仅需要预测场景结构，还需要识别由于观测稀疏或缺失而导致预测不可靠的区域。我们提出了 ContraMap，这是一种对比连续映射方法，它通过使用合成噪声样本训练的显式不确定性类别，增强了基于核的判别式地图。该公式将未观测区域视为对比类，从而在无需贝叶斯推理的情况下，实现实时的联合环境预测和空间不确定性估计。在简单的混合模型视角下，我们证明了分配给不确定性类别的概率是距离感知不确定性代理指标的单调函数。在二维占据地图、三维语义地图和桌面场景重建中的实验表明，ContraMap 在保持建图质量的同时，产生了空间相干的不确定性估计，并且比贝叶斯核地图基准方法效率显著更高。

## Abstract
Reliable robot perception requires not only predicting scene structure, but also identifying where predictions should be treated as unreliable due to sparse or missing observations. We present ContraMap, a contrastive continuous mapping method that augments kernel-based discriminative maps with an explicit uncertainty class trained using synthetic noise samples. This formulation treats unobserved regions as a contrastive class, enabling joint environment prediction and spatial uncertainty estimation in real time without Bayesian inference. Under a simple mixture-model view, we show that the probability assigned to the uncertainty class is a monotonic function of a distance-aware uncertainty surrogate. Experiments in 2D occupancy mapping, 3D semantic mapping, and tabletop scene reconstruction show that ContraMap preserves mapping quality, produces spatially coherent uncertainty estimates, and is substantially more efficient than Bayesian kernelmap baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ContraMap** 的新型机器人环境表示方法，旨在通过对比学习（Contrastive Learning）框架，在保证实时性的前提下，为连续地图提供可靠的空间不确定性估计。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人感知不仅需要预测环境结构（如占据情况或语义），还需要识别哪些区域因观测稀疏或遮挡而不可靠。
*   **研究背景**：
    *   传统的占据网格地图（Occupancy Grids）分辨率受限且扩展性差。
    *   基于高斯过程（GP）或贝叶斯核映射（BHM）的连续表示方法虽然能提供不确定性，但计算成本极高（涉及矩阵求逆，复杂度通常为 $O(H^3)$），难以满足大规模场景的实时需求。
    *   深度学习方法在未观测区域往往表现出“过度自信”。
*   **动机**：开发一种既能保持核映射（Kernel Maps）的高效性，又能显式建模空间不确定性的方法，且无需复杂的贝叶斯后验推理。

### 2. 方法论
*   **核心思想**：将未观测到的空间视为一个显式的“不确定性”类别（第 $C+1$ 类），通过引入合成噪声样本进行对比训练。
*   **关键技术细节**：
    *   **特征投影**：利用核函数（如高斯核）将空间坐标 $x$ 投影到由参考点（Hinge Points）定义的特征空间 $\phi(x)$ 中。
    *   **对比噪声采样（NCE）**：在环境中均匀采样或在物体周围采样合成噪声点，并将其标记为“不确定”类。
    *   **分类模型**：使用带有 Softmax 激活函数的判别式模型（如线性分类器或浅层神经网络）。模型输出 $C$ 个已知类别（如空闲、占据、语义标签）和 1 个不确定性类别。
    *   **理论支撑**：论文证明了在混合模型假设下，Softmax 输出的不确定类概率是“距离感知不确定性代理指标”的单调函数。即离观测数据越远，该类别的概率越高。
*   **算法流程**：收集观测数据 $\rightarrow$ 生成合成噪声 $\rightarrow$ 特征投影 $\rightarrow$ 使用交叉熵损失进行一阶梯度优化训练 $\rightarrow$ 实时查询任意位置的类别和不确定性。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **2D 占据建图**：使用 Radish 仓库的 5 个标准数据集（Intel, Edmonton, Fhw, Mexico, Belgioioso）。
    *   **3D 语义建图**：使用 SemanticKITTI（室外大规模）和 SceneNet（室内）。
    *   **桌面场景重建**：使用 YCB 物体数据集生成的 100 个模拟场景。
    *   **玩具数据集**：Two Ovals, Two Moons, Two Circles 用于定性分析。
*   **对比方法（Benchmarks）**：
    *   **建图基准**：Hilbert Maps (HM), Bayesian Hilbert Maps (BHM), V-PRISM (最新的桌面概率建图方法)。
    *   **不确定性基准**：高斯过程 (GP)、MC Dropout、Deep Ensembles、SNGP。
*   **评估指标**：AUC（2D）、mIoU（3D）、Chamfer 距离（重建质量）、训练与推理耗时。

### 4. 资源与算力
*   **硬件平台**：所有实验均在单块 **NVIDIA Tesla P100 GPU** 上运行。
*   **效率表现**：
    *   2D 任务训练时间约为 2-3 秒，推理时间约 0.2-0.7 秒。
    *   3D 任务训练时间约 35 秒。
    *   相比贝叶斯方法（BHM），ContraMap 的训练和推理速度提升了数百倍（例如在 Intel 数据集上，BHM 推理需 65 秒，ContraMap 仅需 0.2 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 到 3D、从大规模室外到精细桌面重建的多种任务，总计处理了数百个场景。
*   **充分性**：实验设计较为全面，包含了消融实验（如增加隐藏层的影响）和扩展性分析（参考点数量对性能的影响）。
*   **客观性**：通过与“金标准”高斯过程对比不确定性分布，证明了该方法在 OOD（分布外）检测上的有效性，对比实验均在相同硬件和特征配置下进行，具有较好的公平性。

### 6. 主要结论与发现
*   **性能对等**：ContraMap 在建图精度（AUC/mIoU）上与复杂的贝叶斯方法持平甚至略优。
*   **效率飞跃**：由于避免了矩阵求逆，计算复杂度从 $O(H^3)$ 降至线性复杂度，极大地提升了实时性。
*   **不确定性质量**：该方法能准确识别遮挡区域和未探索区域，生成的空间不确定性图与高斯过程高度一致。
*   **架构潜力**：引入 1-2 层隐藏层可以显著提升模型在特征较少时的表达能力。

### 7. 优点
*   **简洁高效**：将复杂的不确定性估计简化为多分类问题，易于实现和部署。
*   **实时性强**：非常适合资源受限的移动机器人平台。
*   **空间连贯性**：生成的不确定性在空间上是连续且平滑的，有利于下游的路径规划和主动感知。
*   **理论完备**：为“为什么噪声类可以代表不确定性”提供了数学解释。

### 8. 不足与局限
*   **噪声采样依赖**：不确定性的质量高度依赖于噪声采样的策略（如采样密度、范围）。在桌面场景中，噪声比例过高会干扰正常类别的学习。
*   **非严格概率校准**：虽然输出具有单调性，但它是一个“代理指标”，并非严格意义上的贝叶斯后验概率，可能在某些极端边缘情况下缺乏精确的概率物理含义。
*   **超参数敏感**：参考点（Hinge Points）的数量和带宽参数仍需根据场景规模进行手动或启发式调整。

（完）
