---
title: "OVI-MAP:Open-Vocabulary Instance-Semantic Mapping"
title_zh: OVI-MAP：开放词汇实例语义建图
authors: "Zilong Deng, Federico Tombari, Marc Pollefeys, Johanna Wald, Daniel Barath"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26541v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 基于RGB-D的开放词汇三维实例语义地图构建
tldr: OVI-MAP是一个实时增量式开放词汇3D实例语义地图构建系统。针对现有方法在处理复杂环境时面临的实时性差和一致性低的问题，该研究提出将实例重建与语义推理相解耦。通过构建类别无关的3D实例地图，并利用视觉语言模型从自动选择的关键视图中提取语义特征，实现了高效的零样本语义标注。该系统在标准基准测试中表现优于现有SOTA方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26541v1/fig-001.webp\", \"caption\": \"Figure 1. Overview of OVI-MAP. Given a streaming RGB-D sequence with camera poses, OVI-MAP incrementally reconstructs a volumetric 3D scene while maintaining a class-agnostic instance map. Semantic information is then assigned in a zero-shot manner using selectively chosen views, enabling open-set object recognition. Our method supports real-time, open-world scene reconstruction with instance-level semantic understanding.\", \"page\": 1, \"index\": 1, \"width\": 494, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-002.webp\", \"caption\": \"Figure 4. Open-vocabulary 3D semantic maps aligned to the ground-truth label sets of the respective datasets. We compare our method with online [29] and offline [10, 14, 18, 36, 46] approaches on the Replica (a) and ScanNet (b) datasets. Colors correspond to the semantic classes defined in each dataset. Gray regions indicate unobserved areas. OVI-MAP produces spatially coherent and semantically accurate reconstructions, maintaining sharp instance boundaries and consistent semantics throughout incremental mapping. Minor discrepancies in color (e.g., pillows in (a) and the table in (b)) arise from closed-set label mapping, where the ground truth uses alternative class names such as “cushion” or “dining table”.\", \"page\": 6, \"index\": 2, \"width\": 1036, \"height\": 528}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-003.webp\", \"caption\": \"Table 3. Comparison of open-vocabulary 3D semantic and instance segmentation performance on Replica [45] and ScanNet [7]. OM3D denotes OpenMask3D [46]. The 30 FPS rows correspond to experiments where only every n-th frame is processed to match real-time performance. Our method achieves the best open-vocabulary semantic-instance accuracy among online systems, and maintains performance comparable to offline approaches even under real-time constraints.\", \"page\": 6, \"index\": 3, \"width\": 491, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-004.webp\", \"caption\": \"Figure 5. Instance highlighting from arbitrary text queries. Given natural language prompts, our system retrieves and highlights corresponding 3D instances based on the learned vision-language embeddings. The examples demonstrate zero-shot grounding of both concrete (“pillow”, “toilet”) and abstract (“where to sleep”, “where is the music”) concepts in reconstructed scenes. Darker tones indicate higher cosine similarity between an object and the query.\", \"page\": 7, \"index\": 4, \"width\": 1026, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-005.webp\", \"caption\": \"Figure 8. Heat map visualizations of the semantic queries. The color closer to red indicates the instance is more similar to the query semantic label, while the color closer to blue indicates the instance is less similar to the query semantic label. Unobserved areas are shown in black.\", \"page\": 14, \"index\": 5, \"width\": 1026, \"height\": 592}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-006.webp\", \"caption\": \"Figure 2. Overview of the proposed pipeline. (a) Class-agnostic instance map reconstruction: each RGB-D frame is segmented into entity proposals and refined with geometry-aware depth segmentation. The fused segments are lifted into 3D and incrementally integrated into a global TSDF-based instance map via super-point registration and spatial voting. (b) Incremental semantic feature aggregation: given the global instance map, each instance is re-projected into new frames via depth-guided ray casting. A view selection module identifies informative viewpoints based on object-centric coverage. Selected views are cropped at multiple scales and masked before being passed to a Vision-Language Model (VLM). The resulting features are aggregated per instance, producing stable open-set semantic embeddings.\", \"page\": 3, \"index\": 6, \"width\": 912, \"height\": 608}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-007.webp\", \"caption\": \"Figure 7. We compare our method with online [29] and offline [18, 43] approaches on the Replica (a) and ScanNet (b) datasets. Colors are randomly assigned for all instance maps according to the instance labels. Gray regions indicate unobserved areas for online methods (Ours and OVO-SLAM), and unlabeled for offline methods (Segment3D, Mask3D). OVI-MAP produces spatially coherent accurate reconstructions, maintaining sharp instance boundaries throughout incremental mapping.\", \"page\": 13, \"index\": 7, \"width\": 1026, \"height\": 1069}, {\"url\": \"assets/figures/arxiv/2603.26541v1/fig-008.webp\", \"caption\": \"Figure 6. Incremental performance on Replica as the number of observed views increases. We compare our incremental semantic aggregation using the proposed view coverage strategy against pixel-counting and other baselines. Our method achieves comparable semantic accuracy (mIoU, mAcc, AP25) while requiring significantly fewer VLM queries per instance, demonstrating efficient and scalable open-vocabulary mapping during online exploration.\", \"page\": 8, \"index\": 8, \"width\": 477, \"height\": 433}]"
motivation: 现有的开放词汇地图方法受限于闭集假设或密集的逐像素语言融合，难以兼顾实时性、可扩展性和时间一致性。
method: 提出将实例重建与语义推理相解耦，通过RGB-D输入增量构建类别无关的3D实例图，并仅从少量自动选择的视图中提取VLM语义特征。
result: 该系统实现了实时运行，并在标准基准测试中显著超越了现有的开放词汇建图基准方法。
conclusion: OVI-MAP通过解耦策略有效提升了在线探索中的实例跟踪稳定性和语义标注准确性，为自主智能体提供了高效的3D环境感知方案。
---

## 摘要
增量式开放词汇 3D 实例语义建图对于在复杂日常环境中运行的自主智能体至关重要。然而，由于需要鲁棒的实例分割、实时处理以及灵活的开集推理，这仍然是一项具有挑战性的任务。现有方法通常依赖于闭集假设或密集的逐像素语言融合，这限制了可扩展性和时间一致性。我们提出了 OVI-MAP，将实例重建与语义推理相解耦。我们建议构建一个类别无关的 3D 实例地图，该地图由 RGB-D 输入增量构建，而语义特征仅通过视觉语言模型从一小组自动选择的视图中提取。这种设计在整个在线探索过程中实现了稳定的实例跟踪和零样本语义标注。我们的系统能够实时运行，并在标准基准测试中优于最先进的开放词汇建图基准方法。

## Abstract
Incremental open-vocabulary 3D instance-semantic mapping is essential for autonomous agents operating in complex everyday environments. However, it remains challenging due to the need for robust instance segmentation, real-time processing, and flexible open-set reasoning. Existing methods often rely on the closed-set assumption or dense per-pixel language fusion, which limits scalability and temporal consistency. We introduce OVI-MAP that decouples instance reconstruction from semantic inference. We propose to build a class-agnostic 3D instance map that is incrementally constructed from RGB-D input, while semantic features are extracted only from a small set of automatically selected views using vision-language models. This design enables stable instance tracking and zero-shot semantic labeling throughout online exploration. Our system operates in real time and outperforms state-of-the-art open-vocabulary mapping baselines on standard benchmarks.

---

## 论文详细总结（自动生成）

### OVI-MAP: 开放词汇实例语义建图论文总结

#### 1. 核心问题与研究动机
论文针对的是**增量式开放词汇 3D 实例语义建图**问题。在自动驾驶或机器人导航中，智能体需要实时理解环境中的物体实例及其语义，但现有方法存在以下痛点：
*   **闭集限制**：传统方法依赖固定类别的预测器，无法识别训练集之外的物体。
*   **计算开销大**：直接将高维视觉语言模型（VLM）特征存储在每个体素或点上，会导致巨大的内存和计算负担。
*   **一致性差**：逐帧运行分割模型（如 SAM）速度慢，且由于遮挡和视角变化，跨帧的语义融合往往充满噪声。
*   **耦合弊端**：现有系统通常将实例分割与语义标签耦合，导致在缺乏语义先验时实例跟踪不稳定。

#### 2. 方法论
OVI-MAP 的核心思想是**将实例重建与语义推理相解耦**。
*   **类别无关的实例地图重建**：
    *   **2D 实体分割与几何细化**：使用 CropFormer 生成类别无关的掩码，并结合深度图的几何不连续性进行细化，减少过分割或欠分割。
    *   **3D 提升与空间投票**：将 2D 掩码投影到 3D 空间，通过在 TSDF（截断符号距离场）体素网格中进行空间投票，实现跨帧的实例关联。
    *   **增量融合**：使用超点（Super-point）注册和合并机制，动态维护全局一致的实例地图。
*   **视角自适应的语义特征聚合**：
    *   **对象中心视角覆盖（View Coverage）**：为每个实例维护一个单位球面的视角图。只有当新视角提供了显著的“新观测面积”时，才触发 VLM 查询。
    *   **多尺度裁剪与特征提取**：对选定视角的物体进行原图裁剪和掩码裁剪，输入 SigLIP 模型提取特征。
    *   **加权融合**：根据可见像素数对多视角特征进行加权平均，生成稳定的实例级语义嵌入。

#### 3. 实验设计
*   **数据集**：使用了 **Replica**（8个场景）和 **ScanNet**（18个场景）。
*   **Benchmark**：评估了实例分割（mIoU, AP25/50/75）和开放词汇语义分割（mIoU, mAcc, AP）。
*   **对比方法**：
    *   **在线系统**：OVO-SLAM, OpenFusion。
    *   **离线系统**：Mask3D, Segment3D, OpenScene, OpenMask3D, Semantic Gaussians 等。
*   **实时性测试**：特别设置了 30 FPS 的约束环境，测试系统在丢帧处理下的性能表现。

#### 4. 资源与算力
*   **硬件**：实验在一台配备 **Nvidia RTX 3090 GPU** 和 **Intel Core i7-12700K CPU** 的工作站上运行。
*   **运行效率**：系统采用多线程并行设计（分割、关联、视角选择、特征提取各占独立线程）。在 30 FPS 约束下，通过跳帧处理仍能保持高性能。
*   **训练时长**：由于是基于预训练模型（CropFormer, SigLIP）的在线建图系统，**无需针对特定场景进行重新训练**。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **性能对比**：在两个主流数据集上与 10 余种 SOTA 方法进行了对比。
*   **消融实验**：
    *   对比了不同的**视角选择策略**（随机、像素计数、视角覆盖），证明了视角覆盖策略在减少 53% VLM 查询的同时保持了精度。
    *   对比了不同的 **2D 分割器**（SAM2, CropFormer, GT）。
    *   对比了不同的 **VLM 后干网络**（CLIP, SigLIP 及其变体）。
    *   对比了不同的**特征融合方式**（平均、加权平均、聚类）。
*   **定性分析**：提供了语义地图、实例地图、热力图以及自然语言查询（如“哪里可以睡觉”）的可视化结果。

#### 6. 主要结论与发现
*   **解耦策略有效**：先构建几何稳定的实例再进行语义标注，比直接融合逐像素语义特征更鲁棒且高效。
*   **视角选择至关重要**：并非所有帧都对语义理解有贡献，基于物体表面覆盖率的选择机制能大幅降低计算压力。
*   **实时性与精度兼得**：OVI-MAP 在在线运行模式下的表现甚至优于部分离线全局优化方法，尤其在 AP75 等高精度指标上领先。

#### 7. 优点
*   **高效性**：通过视角过滤机制，极大地减少了昂贵的 VLM 调用次数。
*   **稳定性**：类别无关的实例关联避免了语义预测错误对地图结构的影响。
*   **灵活性**：支持开放词汇查询，能够处理抽象指令（如“寻找听音乐的地方”）。
*   **实时性**：系统架构支持多线程并行，适合部署在实际机器人平台上。

#### 8. 不足与局限
*   **依赖 2D 分割质量**：如果前端 2D 分割器（如 CropFormer）在复杂或微小物体上失败，3D 实例地图也会受损。
*   **背景偏差**：从裁剪图像中提取特征时，虽然使用了掩码，但仍可能受到背景像素的干扰。
*   **VLM 局限性**：目前的视觉语言模型在视觉特征与文本空间的对齐上仍存在弱点，可能导致某些细粒度标签的误判。
*   **动态环境**：目前主要针对静态室内场景，未深入探讨动态物体对建图的影响。

（完）
