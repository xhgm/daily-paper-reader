---
title: "GeoGuide: Hierarchical Geometric Guidance for Open-Vocabulary 3D Semantic Segmentation"
title_zh: GeoGuide：用于开放词汇 3D 语义分割的层级几何引导
authors: "Xujing Tao, Chuxin Wang, Yubo Ai, Zhixin Cheng, Zhuoyuan Li, Liangsheng Liu, Yujia Chen, Xinjun Li, Qiao Li, Wenfei Yang, Tianzhu Zhang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26260v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 用于开放词汇三维语义分割的分层几何引导
tldr: 针对开放词汇3D语义分割中过度依赖2D蒸馏导致几何特征缺失和误差继承的问题，本文提出GeoGuide框架。该框架利用预训练3D模型引入分层几何语义一致性，通过超点蒸馏、实例级掩码重建和实例间关系一致性模块，有效融合几何与语义信息，抑制2D噪声并增强跨实例一致性。在ScanNet v2等多个数据集上取得了领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26260v1/fig-001.webp\", \"caption\": \"Figure 1. (a) Occlusions and ambiguous boundaries in 2D predictions lead to inaccurate 3D segmentation, whereas our geometryguided modeling effectively rectifies these errors. (b) The baseline suffers from the lack of geometric relation modeling, whereas our method exploits inter-instance geometric constraints to produce accurate predictions.\", \"page\": 1, \"index\": 1, \"width\": 493, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2603.26260v1/fig-002.webp\", \"caption\": \"Table 1. Open-vocabulary 3D semantic segmentation results. Comparison between GeoGuide and existing fully-supervised and zero-shot methods. SAS∗ denotes using the same 2D features as in SAS [42], ‡ denotes the use of the 2D-3D ensemble strategy, LS: LSeg, OS: OpenSeg. Best results are shown in bold.\", \"page\": 6, \"index\": 2, \"width\": 494, \"height\": 696}, {\"url\": \"assets/figures/arxiv/2603.26260v1/fig-003.webp\", \"caption\": \"Table 3. Cross-domain generalization evaluation. Models are trained on ScanNet v2 and evaluated on Matterport3D (21/40/80/160 categories), † denotes results reproduced by us using officially provided weights, ‡ denotes the use of the 2D-3D ensemble strategy. SAS∗ denotes using the same 2D features as in SAS [42].\", \"page\": 8, \"index\": 3, \"width\": 748, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.26260v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of our proposed GeoGuide framework. GeoGuide integrates hierarchical geometric priors to achieve geometry–semantic consistent open-vocabulary 3D segmentation\", \"page\": 4, \"index\": 4, \"width\": 1030, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.26260v1/fig-005.webp\", \"caption\": \"Figure 3. Preliminary Experiments. Comparison between OpenScene and our preliminary attempt under different 2D models. LS: LSeg, OS: OpenSeg.\", \"page\": 4, \"index\": 5, \"width\": 472, \"height\": 260}]"
motivation: 现有方法过度依赖2D模型蒸馏，限制了3D几何特征学习并容易继承2D预测误差。
method: 提出包含基于不确定性的超点蒸馏、实例级掩码重建和实例间关系一致性三个模块的分层几何引导框架。
result: 在ScanNet v2、Matterport3D和nuScenes等主流数据集上均展现出优于现有方法的性能。
conclusion: GeoGuide通过深度整合3D几何先验与语义一致性，显著提升了开放词汇3D语义分割的准确性和鲁棒性。
---

## 摘要
开放词汇 3D 语义分割旨在分割训练集之外的任意类别。现有方法主要依赖于从 2D 开放词汇模型中蒸馏知识。然而，将 3D 特征对齐到 2D 表示空间限制了内在的 3D 几何学习，并继承了 2D 预测中的误差。为了解决这些局限性，我们提出了 GeoGuide，这是一个利用预训练 3D 模型来整合层级几何-语义一致性的新型框架，用于开放词汇 3D 分割。具体而言，我们引入了一个基于不确定性的超点蒸馏模块，用于融合几何和语义特征以估计逐点不确定性，自适应地加权超点内的 2D 特征，在抑制噪声的同时保留判别性信息，从而增强局部语义一致性。此外，我们的实例级掩码重建模块利用几何先验，通过重建完整的实例掩码来强制执行实例内的语义一致性。另外，我们的实例间关系一致性模块对齐几何和语义相似度矩阵，以校准同类别物体间的跨实例一致性，从而缓解由视角引起的语义漂移。在 ScanNet v2、Matterport3D 和 nuScenes 上的广泛实验证明了 GeoGuide 的卓越性能。

## Abstract
Open-vocabulary 3D semantic segmentation aims to segment arbitrary categories beyond the training set. Existing methods predominantly rely on distilling knowledge from 2D open-vocabulary models. However, aligning 3D features to the 2D representation space restricts intrinsic 3D geometric learning and inherits errors from 2D predictions. To address these limitations, we propose GeoGuide, a novel framework that leverages pretrained 3D models to integrate hierarchical geometry-semantic consistency for open-vocabulary 3D segmentation. Specifically, we introduce an Uncertainty-based Superpoint Distillation module to fuse geometric and semantic features for estimating per-point uncertainty, adaptively weighting 2D features within superpoints to suppress noise while preserving discriminative information to enhance local semantic consistency. Furthermore, our Instance-level Mask Reconstruction module leverages geometric priors to enforce semantic consistency within instances by reconstructing complete instance masks. Additionally, our Inter-Instance Relation Consistency module aligns geometric and semantic similarity matrices to calibrate cross-instance consistency for same-category objects, mitigating viewpoint-induced semantic drift. Extensive experiments on ScanNet v2, Matterport3D, and nuScenes demonstrate the superior performance of GeoGuide.

---

## 论文详细总结（自动生成）

这是一份关于论文《GeoGuide: Hierarchical Geometric Guidance for Open-Vocabulary 3D Semantic Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的开放词汇 3D 语义分割方法过度依赖从 2D 预训练模型（如 CLIP、OpenSeg）向 3D 模型的知识蒸馏。
*   **研究动机**：
    *   **几何缺失**：简单的 2D-3D 对齐忽略了 3D 空间固有的几何结构特征。
    *   **误差继承**：2D 模型在处理遮挡、光照变化和边界模糊时存在预测错误，3D 模型在蒸馏过程中会继承这些噪声。
    *   **语义漂移**：同一类别的物体在不同视角下的 2D 特征可能存在显著差异，导致 3D 空间内的语义表示不一致。

### 2. 方法论与关键技术
GeoGuide 提出了一个**层级几何引导框架**，通过三个核心模块在不同尺度上强制执行几何-语义一致性：

*   **基于不确定性的超点蒸馏 (USD)**：
    *   **核心思想**：利用超点（几何同质区域）内的几何一致性来过滤 2D 噪声。
    *   **技术细节**：融合预训练 3D 特征与 2D 语义特征，通过一个轻量级 MLP 预测逐点“可靠性权重”。利用该权重对超点内的特征进行加权聚合，抑制噪声点，增强局部语义的纯净度。
*   **实例级掩码重建 (IMR)**：
    *   **核心思想**：利用几何先验恢复完整的实例结构，确保物体内部语义统一。
    *   **技术细节**：采用类无关的 3D 实例分割生成提案，随机遮盖部分区域后，利用剩余部分的特征尝试重建完整的实例掩码。这迫使模型学习到物体整体的几何与语义关联。
*   **实例间关系一致性 (IIRC)**：
    *   **核心思想**：利用 3D 几何相似性校准语义相似性。
    *   **技术细节**：计算实例间（及超点间）的几何相似度矩阵和语义相似度矩阵，通过 MSE 损失函数使两者对齐。即使 2D 视角导致语义特征偏移，只要几何结构相似，模型仍会将其识别为同类。

### 3. 实验设计
*   **数据集与场景**：
    *   **室内场景**：ScanNet v2（20类）、Matterport3D（21类及扩展长尾类别）。
    *   **室外场景**：nuScenes（23类，自动驾驶场景）。
*   **Benchmark 与对比方法**：
    *   对比了全监督方法（如 MinkowskiNet, PTv3）和零样本/开放词汇方法（如 OpenScene, SAS, GGSD, OV3D 等）。
    *   使用了多种 2D 特征提取器（LSeg, OpenSeg, SEEM）进行公平对比。
*   **评价指标**：平均交并比 (mIoU) 和平均准确率 (mAcc)。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA A6000 GPU** 上完成。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：使用了 AdamW 优化器，室内和室外场景的体素大小分别设为 2cm 和 5cm。
*   **说明**：文中未明确给出具体的总训练时长，但提到推理阶段不需要几何引导模块，仅需纯 3D 模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在三个主流数据集上验证了 SOTA 性能。
    *   **长尾实验**：在 Matterport3D 上测试了从 21 类到 160 类的扩展能力。
    *   **跨域泛化**：进行了 ScanNet $\leftrightarrow$ Matterport3D 的双向迁移实验（不进行微调）。
    *   **消融实验**：详细验证了 USD、IMR、IIRC 每个模块以及预训练权重对结果的贡献。
*   **充分性评价**：实验设计非常全面，涵盖了室内外、长尾分布、跨数据集泛化等多个维度，对比基准清晰，消融实验逻辑严密，足以证明所提方法的有效性和鲁棒性。

### 6. 主要结论与发现
*   **几何引导至关重要**：单纯引入 3D 预训练特征而不加几何约束，性能提升有限甚至会下降；而层级几何引导能显著纠正 2D 蒸馏带来的偏差。
*   **性能突破**：在 ScanNet v2 上达到 64.8 mIoU，在 nuScenes 上达到 50.3 mIoU，显著优于之前的 SAS 等强基准。
*   **泛化性强**：GeoGuide 在处理未见类别和跨场景迁移时表现出极强的稳定性，尤其在类别数量增加时，性能下降幅度远小于同类方法。

### 7. 优点
*   **层级化设计**：从局部（超点）到物体（实例）再到全局（实例间关系）构建了完整的约束体系。
*   **不确定性建模**：通过预测权重自适应地处理 2D 噪声，比传统的平均池化更科学。
*   **即插即用**：推理时不需要额外的几何模块或 2D 模型，保持了 3D 模型的推理效率。

### 8. 不足与局限
*   **依赖外部算法**：IMR 和 IIRC 模块依赖于类无关的 3D 实例分割算法（如 SAI3D），如果实例分割质量较差，可能会影响一致性建模的效果。
*   **预训练依赖**：性能在很大程度上受限于所使用的 3D 预训练骨干网络（如 Sonata）和 2D 模型的质量。
*   **计算开销**：虽然推理高效，但训练阶段引入多个相似度矩阵计算和掩码重建任务，可能会增加训练时的显存占用和计算耗时。

（完）
