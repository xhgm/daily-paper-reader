---
title: "JRM: Joint Reconstruction Model for Multiple Objects without Alignment"
title_zh: JRM：无需对齐的多物体联合重建模型
authors: "Qirui Wu, Yawar Siddiqui, Duncan Frost, Samir Aroudj, Armen Avetisyan, Richard Newcombe, Angel X. Chang, Jakob Engel, Henry Howard-Jenkins"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.25985v1"
tags: ["query:q10"]
score: 9.0
evidence: 利用重复性进行多物体联合3D重建
tldr: 针对物体中心化重建中忽略重复观测信号以及现有联合重建方法依赖显式对齐的问题，本文提出联合重建模型（JRM）。JRM 采用 3D 流匹配生成模型，通过在潜空间隐式聚合未对齐的多个观测值，将重建视为个性化生成任务。该方法无需显式对齐或匹配，能有效处理非刚性形变（如关节运动），在合成和真实数据集上均展现出优于独立重建和基于对齐方法的鲁棒性与重建质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25985v1/fig-001.webp\", \"caption\": \"Figure 5. Qualitative results on temporal instance repetition. Objects are colored from faded to solid tones reflecting chamfer distance to the ground truth. The reconstruction from JRM improves with more rescans, while those from FM degrade due to inaccurate alignments.\", \"page\": 6, \"index\": 1, \"width\": 1034, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-002.webp\", \"caption\": \"Figure 10. Illustration of a single-stream block taking coupled latents as input.\", \"page\": 11, \"index\": 2, \"width\": 494, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-003.webp\", \"caption\": \"Figure 18. Comparison with MIDI-3D and SceneGen, both are conditioned on the single image displayed. Best viewed zoomed.\", \"page\": 15, \"index\": 3, \"width\": 1024, \"height\": 523}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-004.webp\", \"caption\": \"Figure 2. Comparison between different approaches to objectcentric reconstruction. JRM offers a relaxation of explicit alignment and registration techniques. Objects are jointly reconstructed, allowing information flow between them, but without imposing hard constraints on similarity.\", \"page\": 2, \"index\": 4, \"width\": 615, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-005.webp\", \"caption\": \"Figure 15. Reconstruction with similar source objects.\", \"page\": 14, \"index\": 5, \"width\": 496, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-006.webp\", \"caption\": \"Figure 3. (a) JRM jointly reconstructs two nightstands k and k′ that appear in a single scan. (b) Our proposed coupled fusion block, in which the denoised tokens of two distinct objects attend to each other in the latent space to implicitly aggregate unaligned observations.\", \"page\": 3, \"index\": 6, \"width\": 1034, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-007.webp\", \"caption\": \"Table 7. Quantitative results of temporal instance repetition with predicted object matching and alignment by MORE2. Numbers in gray color represent results of only applying the oracle object alignment to the condition inputs of FM.\", \"page\": 13, \"index\": 7, \"width\": 1022, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-008.webp\", \"caption\": \"Figure 14. We show examples of generated synthetic scenes in scenarios of temporal instance repetition and articulation dynamics. In the last column, we visualize SLAM points (blue points), camera trajectory (gray diamonds) and 3D bounding boxes.\", \"page\": 13, \"index\": 8, \"width\": 1032, \"height\": 653}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-009.webp\", \"caption\": \"Figure 9. Qualitative results on real-world scenes, including ScanNet++ and Replica. We use the same color for objects within the same match group. For each scene, we produce two close-up views focusing on the matched objects.\", \"page\": 8, \"index\": 9, \"width\": 1015, \"height\": 679}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-010.webp\", \"caption\": \"Table 6. Qualitative results on two JRM variants.\", \"page\": 12, \"index\": 10, \"width\": 1029, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-011.webp\", \"caption\": \"Table 1. Quantitative results of temporal instance repetition with ground truth object matching. Numbers in gray color represent results of applying the oracle object alignment to the condition inputs of FM.\", \"page\": 5, \"index\": 11, \"width\": 860, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2603.25985v1/fig-012.webp\", \"caption\": \"Figure 19. Additional qualitative results on real-world scenes, including ScanNet++ and Replica. We use the same color for objects within the same match group. For each scene, we produce two close-up views focusing on the matched objects.\", \"page\": 16, \"index\": 12, \"width\": 1023, \"height\": 835}]"
motivation: 现有的物体联合重建方法过度依赖显式的刚性对齐和匹配，难以处理非刚性变换且对关联错误敏感。
method: 提出一种基于 3D 流匹配的生成模型，通过在潜空间隐式聚合多个未对齐的观测点云，实现一致且忠实的物体重建。
result: 实验表明 JRM 无需显式对齐即可提升重建质量，对错误的关联具有更强的鲁棒性，并能自然处理关节运动等非刚性变化。
conclusion: JRM 证明了通过隐式潜空间聚合进行联合重建的有效性，为多观测场景下的 3D 物体建模提供了更具鲁棒性的新方案。
---

## 摘要
以物体为中心的重建旨在通过独立物体的组合来恢复场景的 3D 结构。虽然这种独立性可以简化建模，但它忽略了能够改进重建的强信号，特别是重复性，即同一个物体模型在场景中或跨扫描中多次出现。我们提出了联合重建模型（JRM），通过将物体重建定义为一种个性化生成任务来利用重复性：多个观测值共享一个共同的主体，该主体在所有观测值中应保持一致，同时仍遵循每个观测值的特定姿态和状态。该方向的先前方法依赖于观测值之间的显式匹配和刚性对齐，这使得它们对误差敏感，且难以扩展到非刚性变换。相比之下，JRM 是一种 3D 流匹配（flow-matching）生成模型，它在其潜空间中隐式聚合未对齐的观测值，学习以数据驱动的方式生成一致且忠实的重建，而无需显式约束。在合成数据和真实世界数据上的评估表明，JRM 的隐式聚合消除了对显式对齐的需求，提高了对错误关联的鲁棒性，并能自然地处理诸如关节连接等非刚性变化。总的来说，JRM 在重建质量上优于独立重建和基于对齐的基准模型。

## Abstract
Object-centric reconstruction seeks to recover the 3D structure of a scene through composition of independent objects. While this independence can simplify modeling, it discards strong signals that could improve reconstruction, notably repetition where the same object model is seen multiple times in a scene, or across scans. We propose the Joint Reconstruction Model (JRM) to leverage repetition by framing object reconstruction as one of personalized generation: multiple observations share a common subject that should be consistent for all observations, while still adhering to the specific pose and state from each. Prior methods in this direction rely on explicit matching and rigid alignment across observations, making them sensitive to errors and difficult to extend to non-rigid transformations. In contrast, JRM is a 3D flow-matching generative model that implicitly aggregates unaligned observations in its latent space, learning to produce consistent and faithful reconstructions in a data-driven manner without explicit constraints. Evaluations on synthetic and real-world data show that JRM's implicit aggregation removes the need for explicit alignment, improves robustness to incorrect associations, and naturally handles non-rigid changes such as articulation. Overall, JRM outperforms both independent and alignment-based baselines in reconstruction quality.

---

## 论文详细总结（自动生成）

### JRM: 无需对齐的多物体联合重建模型论文总结

#### 1. 核心问题与研究动机
论文关注的是**以物体为中心的 3D 场景重建**。传统的独立重建方法（如对每个检测到的物体单独建模）忽略了场景中普遍存在的**重复性（Repetition）**信号，例如：
*   **空间重复**：同一房间内多个相同的椅子。
*   **时间重复**：在不同时间点对同一移动物体的多次扫描。
*   **关节运动**：同一物体在不同状态（如柜门开启/关闭）下的观测。

现有利用重复性的方法通常依赖于**显式的几何对齐和配准**，这导致它们对匹配误差极其敏感，且难以处理非刚性形变（如家具的开关）。JRM 的动机在于通过生成模型的潜空间隐式聚合信息，实现无需显式对齐的联合重建。

#### 2. 方法论：核心思想与技术细节
JRM 将联合重建视为一种**个性化生成任务**，核心思想是在 3D 生成模型的潜空间中耦合多个物体的特征。
*   **基础架构**：基于 **ShapeR**（一种多模态条件 3D 流匹配模型）。它将 3D 形状表示为潜令牌（Latent Tokens），并使用扩散转换器（DiT）进行去噪。
*   **耦合融合模块（Coupled Fusion Block）**：这是 JRM 的核心创新。在 DiT 的单流块中引入耦合注意力机制，让一组物体（K 个）的潜令牌在潜空间中相互关注（Self-attention over concatenated tokens）。
*   **隐式聚合**：不同于在原始点云空间进行刚性变换对齐，JRM 允许不同实例的令牌在生成过程中交换信息。这使得模型能从其他观测更全的实例中“借用”几何细节，同时保持自身观测的姿态和状态。
*   **配对训练策略**：模型在物体对上进行训练。训练样本包括“正对”（相似物体，相似度 > 0.9）和“负对”（不同物体）。通过以 0.1 的概率引入负对，训练模型学会自适应地选择性聚合相关信息，增强对错误匹配的鲁棒性。

#### 3. 实验设计
论文在合成和真实场景中进行了广泛评估：
*   **数据集**：
    *   **合成数据**：基于 Objaverse-XL、ABO 等 400k 个 3D 资产构建。设计了时间重复（100 个场景）、空间重复（100 个场景）和关节运动（100 个场景）三种实验设置。
    *   **真实数据**：Replica 和 ScanNet++ 场景，以及使用 Project Aria 眼镜采集的真实公寓数据。
*   **对比基准（Baselines）**：
    *   **FM (Flow Matching)**：独立重建基准（ShapeR 的变体）。
    *   **MORE 2**：基于显式对齐和配准的先进多物体重建方法。
    *   **DPRecon**：基于神经隐式表示和 SDS 损失的优化方法。
*   **评估指标**：倒角距离 (CD)、法线一致性 (NC) 和 F-Score。

#### 4. 资源与算力
论文明确列出了庞大的算力需求：
*   **基础模型 (FM)**：使用 **128 张 NVIDIA H100 GPU** 训练 300K 步，有效批大小为 1,536。
*   **3D VAE**：使用 **64 张 NVIDIA H100 GPU** 训练 200K 步。
*   **JRM 微调**：在预训练 FM 基础上，使用 **64 张 NVIDIA H100 GPU** 训练 200K 步，有效批大小为 256。

#### 5. 实验数量与充分性
实验设计非常充分且具有针对性：
*   **多场景覆盖**：涵盖了刚性、非刚性、时间跨度等多种重复性场景。
*   **鲁棒性测试**：专门测试了在存在对齐误差和匹配错误（误报）时模型的表现，证明了 JRM 在错误率上升时性能下降远比基准模型平缓。
*   **消融实验**：对负样本对的训练比例、模型架构变体（替换 vs 插入耦合块）进行了详细消融。
*   **客观性**：使用了 Oracle（真值）匹配和预测匹配两种设置进行对比，确保了实验的公平性。

#### 6. 主要结论与发现
*   **联合优于独立**：通过耦合多个观测值，JRM 在所有指标上均优于独立重建模型，尤其是在观测稀疏或存在遮挡时。
*   **无需对齐的优势**：JRM 对显式对齐误差具有极强的鲁棒性。在关节运动物体上，JRM 表现显著优于强制刚性对齐的方法。
*   **泛化能力**：虽然仅在物体对上训练，但 JRM 可以自然扩展到推理时同时重建更多数量（如 18 个）的物体组。
*   **隐式分割改进**：联合重建有助于模型更好地识别哪些几何属于目标物体，减少了因遮挡导致的伪影。

#### 7. 优点
*   **架构优雅**：通过简单的注意力机制实现了复杂的隐式信息聚合，避开了脆弱的显式几何配准步骤。
*   **处理非刚性**：是少数能有效利用重复性来提升关节运动物体重建质量的方法之一。
*   **鲁棒性强**：对上游任务（如实例关联、姿态估计）的容错度更高。

#### 8. 不足与局限
*   **依赖上游输入**：重建质量仍受限于检测、分割和相机位姿估计的准确性。
*   **计算开销**：虽然推理是前馈的，但联合处理多个物体会增加显存占用和计算延迟。
*   **幻觉风险**：虽然比独立模型好，但在极端缺乏观测时，模型仍可能生成符合先验但非真实的几何细节。
*   **训练成本极高**：依赖大量 H100 GPU 和高质量 3D 数据集，普通研究机构难以复现。

（完）
