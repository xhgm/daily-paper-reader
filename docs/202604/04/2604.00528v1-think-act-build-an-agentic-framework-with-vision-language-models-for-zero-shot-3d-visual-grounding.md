---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
title_zh: 思考、行动、构建：一种基于视觉语言模型的零样本 3D 视觉定位智能体框架
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v1"
tags: ["query:q21"]
score: 10.0
evidence: 使用视觉语言模型进行零样本3D视觉定位
tldr: "本文提出Think, Act, Build (TAB)框架，旨在解决零样本3D视觉定位中过度依赖预处理点云的问题。该框架将任务重构为生成式2D到3D重建过程，利用VLM智能体在原始RGB-D流中动态追踪目标，并通过语义锚定几何扩展机制克服多视角覆盖不足。实验证明，该方法在ScanRefer和Nr3D上显著优于现有零样本方法，甚至超越了部分全监督基准。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00528v1/fig-001.webp\", \"caption\": \"Figure 1: (1) Top: Previous methods rely on preprocessed 3D point clouds, degrading the task into proposal matching. (2) Bottom: Our TAB operates directly on RGB-D streams. Through an iterative Think-Act-Build process, the agent reconstructs the target object.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 198}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-002.webp\", \"caption\": \"Table 1: 3D Visual Grounding results on ScanRefer. \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input. † means results with two stage training.\", \"page\": 7, \"index\": 2, \"width\": 822, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-003.webp\", \"caption\": \"Table 2: 3D Visual Grounding results on Nr3D (w/o GT object class). \\\"w/o PC\\\" denotes methods that do not rely on 3D point clouds as input.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-004.webp\", \"caption\": \"Figure 2: The \\\"Think, Act, Build (TAB)\\\" framework. Guided by an expert 3D-VG Skill, the VLM agent reasons about the task, invokes visual tools, and reconstructs the target.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.00528v1/fig-005.webp\", \"caption\": \"Figure 4: Qualitative comparison of different components in TAB.\", \"page\": 9, \"index\": 5, \"width\": 417, \"height\": 352}]"
motivation: 现有方法通常将3D视觉定位简化为预处理点云的提议匹配，忽略了2D VLM强大的语义解析能力与多视角几何在构建3D结构中的潜力。
method: 提出一种动态智能体框架，通过VLM调用视觉工具在2D帧中追踪目标，并利用语义锚定几何扩展机制将多视角特征聚合为精确的3D坐标。
result: 在ScanRefer和Nr3D数据集上的实验显示，该框架仅使用开源模型即大幅领先于此前的零样本方法，并展现出超越全监督基准的性能。
conclusion: 通过解耦空间语义理解与几何结构实例化，TAB框架为实现高效、鲁棒的零样本3D视觉定位提供了一种全新的生成式重建范式。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLMs) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选区域匹配。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLMs 解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。在此启发下，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。至关重要的是，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展机制，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基准模型。

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Think, Act, Build (TAB) 框架

#### 1. 核心问题与整体含义
*   **研究动机**：传统的 3D 视觉定位（3D-VG）方法高度依赖预先扫描并处理好的 3D 点云数据，这使得任务退化为简单的“提议匹配（Proposal Matching）”。在现实场景（如机器人导航）中，预处理的点云往往不可用。
*   **核心问题**：现有的零样本（Zero-shot）方法要么依赖 3D 先验，要么在仅使用 2D 图像时因缺乏几何约束而导致跟踪脆弱，难以处理视角剧烈变化或遮挡。
*   **整体含义**：本文提出了 **TAB (Think, Act, Build)** 框架，将 3D-VG 重新定义为一个动态的、由智能体驱动的“语义推理+几何重建”过程。它直接作用于原始 RGB-D 视频流，无需预处理点云。

#### 2. 方法论
TAB 框架遵循 **ReAct** 范式，通过一个专有的“3D-VG 技能蓝图”引导视觉语言模型（VLM）智能体：
*   **Think（思考）**：VLM 根据查询（Query）和当前视觉上下文进行推理，规划下一步行动（如：解析查询、过滤帧、识别目标）。
*   **Act（行动）**：调用视觉工具库（如 Grounding DINO 进行检测、SAM3 进行分割、Query Analysis 进行语义解析）。
*   **Build（构建）**：这是核心技术环节，包含**语义锚定几何扩展（SAGE）**机制：
    *   **语义时序扩展（STE）**：在参考帧附近进行双向语义跟踪，构建初步的 3D 几何中心（Centroid）。
    *   **多视角几何扩展（MGE）**：利用确定的相机内外参，将 3D 中心投影回所有视频帧。通过深度检查和遮挡剔除，在语义跟踪失效（如视角太近缺乏上下文）的帧中精准找回目标。
    *   **重建**：将所有获取的 2D 掩码通过逆投影（Inverse Projection）映射回 3D 空间，聚合生成点云并计算 3D 边界框。

#### 3. 实验设计
*   **数据集**：使用了 **ScanRefer** 和 **Nr3D**（均基于 ScanNet 室内场景）。
*   **Benchmark 优化**：作者指出原始基准存在指代歧义、类别错误和空间描述错误，并手动修正了这些噪声数据以确保评估严谨。
*   **对比方法**：
    *   **全监督方法**：如 3DVG-T, EDA, G3-LQ, LLaVA-3D 等。
    *   **零样本方法**：如 LLM-Grounder, SeeGround, VLM-Grounder, SeqVLM, SPAZER 等。
*   **评估指标**：Acc@0.25 和 Acc@0.5（预测框与真值的 IoU 阈值准确率）。

#### 4. 资源与算力
*   **模型使用**：核心智能体采用 **Qwen3-VL-32B**（开源模型），辅助工具包括 Grounding DINO 和 SAM3。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或推理总时长。由于该方法是零样本推理框架，不涉及大规模 3D 预训练，主要开销在于 VLM 的多轮 API 调用或本地推理。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集的多个子集（Unique/Multiple, Easy/Hard, View-Dep/Indep）上进行了全面测试。
*   **消融实验**：针对 STE（语义扩展）和 MGE（几何扩展）模块进行了详细的消融分析，验证了各组件对提升多视角覆盖率的贡献。
*   **充分性评价**：实验设计较为充分，对比了从传统模型到最新大模型驱动的各类方法。通过手动修正基准测试集，进一步提升了实验的客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先**：TAB 在无需预处理点云的情况下，在 ScanRefer 上达到了 71.2% (Acc@0.25)，显著优于现有的零样本方法，甚至在某些指标上超过了全监督基准。
*   **几何的重要性**：单纯依靠 VLM 的语义跟踪在复杂视角下极易失败，引入确定性的几何投影（MGE）是实现鲁棒 3D 定位的关键。
*   **容错性**：智能体能够通过“动态调整”策略（如降低检测阈值）从中间步骤的失败中恢复。

#### 7. 优点
*   **摆脱 3D 先验**：直接处理 RGB-D 流，更符合具身智能和机器人的实际应用场景。
*   **解耦设计**：巧妙地将 VLM 的语义推理能力与多视角几何的结构化能力结合。
*   **基准修正**：对现有数据集噪声的识别和修正为后续研究提供了更高质量的测试环境。
*   **高度鲁棒**：具备自动纠错和阈值自适应调整能力。

#### 8. 不足与局限
*   **传感器依赖**：高度依赖深度图（Depth Map）的质量，在深度噪声剧烈或传感器缺失的区域表现受限。
*   **计算延迟**：作为智能体框架，涉及多轮 VLM 推理和工具调用，其实时性可能难以满足高速运动场景。
*   **场景限制**：目前主要在室内场景（ScanNet）验证，在室外大规模场景或动态物体较多的环境中，几何扩展的稳定性有待进一步验证。

（完）
