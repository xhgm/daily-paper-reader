---
title: "RAP: Retrieve, Adapt, and Prompt-Fit for Training-Free Few-Shot Medical Image Segmentation"
title_zh: RAP：面向无需训练的少样本医学图像分割的检索、自适应与提示拟合框架
authors: "Zhihao Mao, Bangpu Chen"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27705v1"
tags: ["query:q14"]
score: 9.0
evidence: 使用 SAM2 的免训练少样本医学图像分割
tldr: 针对少样本医学图像分割中解剖结构形态利用不足的问题，本文提出RAP框架。该框架无需训练，首先利用DINOv3检索形态兼容的支撑样本，接着通过边界感知结构拟合生成解剖一致的预掩码，最后采用Voronoi划分和扇区采样生成提示点驱动SAM2进行精细化分割。实验证明RAP在多个医学分割基准上达到SOTA，为稳健的零样本医学分割提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27705v1/fig-001.webp\", \"caption\": \"Fig. 3: Qualitative results on Card-MRI for three cardiac structures. Our method produces more accurate boundaries, especially for the thin myocardium (LV-MYO).\", \"page\": 6, \"index\": 1, \"width\": 529, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2603.27705v1/fig-002.webp\", \"caption\": \"TABLE II: Quantitative comparison (Dice %) on Card-MRI dataset.\", \"page\": 5, \"index\": 2, \"width\": 512, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.27705v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of the proposed RAP framework. Given a query image, we first retrieve the most similar support from a database using DINOv3 features (Retrieve). Then, we adapt the support shape to the query through oriented chamfer matching with semantic gating (Adapt). Finally, we generate Voronoi-based point prompts and feed them to SAM2 for segmentation (Prompt-Fit).\", \"page\": 3, \"index\": 3, \"width\": 1076, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2603.27705v1/fig-004.webp\", \"caption\": \"Fig. 2: Frequency-domain style adaptation via wavelet transform. The support’s low-frequency (LL) subband is replaced with the query’s LL to align global appearance while preserving high-frequency edge details.\", \"page\": 3, \"index\": 4, \"width\": 494, \"height\": 305}]"
motivation: 现有方法主要依赖语义对应，未能充分利用医学图像中解剖目标具有的可重复高频形态特征（如边界几何和空间布局）。
method: 提出一种结合形态检索、结构适配和提示词拟合的无训练框架，通过优化提示点生成策略并结合SAM2实现精准分割。
result: 在多个医学分割基准测试中，RAP一致超越了现有的少样本分割基准，并取得了当前最先进的性能表现。
conclusion: 显式的结构拟合结合检索增强的提示策略，为实现稳健且无需训练的少样本医学图像分割提供了一种简单有效的技术路线。
---

## 摘要
少样本医学图像分割（FSMIS）已取得显著进展，但现有方法大多主要依赖于稀缺标注中的语义对应关系，而未能充分利用医学影像的一个关键特性：解剖目标在不同患者和采集过程中表现出可重复的高频形态学特征（如边界几何形状和空间布局）。我们提出了 RAP，这是一个无需训练的框架，通过检索、自适应和提示 Segment Anything Model 2 (SAM2) 来实现 FSMIS。首先，RAP 利用 DINOv3 特征从存档中检索形态兼容的支撑样本，以降低单一支撑样本选择的脆弱性。其次，它通过拟合边界感知结构线索，使检索到的支撑掩码自适应于查询样本，从而在领域偏移下生成解剖一致的预掩码。第三，RAP 通过 Voronoi 划分采样正点，并通过基于扇区的采样方式采样负点，将预掩码转换为提示，并将其输入 SAM2 进行最终细化，无需任何微调。在多个医学分割基准上的广泛实验表明，RAP 持续超越了先前的 FSMIS 基准方法，并达到了最先进的性能。总的来说，RAP 表明显式结构拟合结合检索增强提示，为实现鲁棒的无需训练少样本医学分割提供了一条简单且有效的途径。

## Abstract
Few-shot medical image segmentation (FSMIS) has achieved notable progress, yet most existing methods mainly rely on semantic correspondences from scarce annotations while under-utilizing a key property of medical imagery: anatomical targets exhibit repeatable high-frequency morphology (e.g., boundary geometry and spatial layout) across patients and acquisitions. We propose RAP, a training-free framework that retrieves, adapts, and prompts Segment Anything Model 2 (SAM2) for FSMIS. First, RAP retrieves morphologically compatible supports from an archive using DINOv3 features to reduce brittleness in single-support choice. Second, it adapts the retrieved support mask to the query by fitting boundary-aware structural cues, yielding an anatomy-consistent pre-mask under domain shifts. Third, RAP converts the pre-mask into prompts by sampling positive points via Voronoi partitioning and negative points via sector-based sampling, and feeds them into SAM2 for final refinement without any fine-tuning. Extensive experiments on multiple medical segmentation benchmarks show that RAP consistently surpasses prior FSMIS baselines and achieves state-of-the-art performance. Overall, RAP demonstrates that explicit structural fitting combined with retrieval-augmented prompting offers a simple and effective route to robust training-free few-shot medical segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《RAP: Retrieve, Adapt, and Prompt-Fit for Training-Free Few-Shot Medical Image Segmentation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：少样本医学图像分割（FSMIS）面临标注稀缺、跨领域/跨模态泛化差以及现有方法对解剖结构先验（如形状、位置、拓扑）利用不足的问题。
*   **研究动机**：
    *   医学图像中的解剖目标具有高度可重复的形态学特征（边界几何和空间布局）。
    *   现有方法大多需要针对特定任务进行训练，限制了其在未知领域的部署。
    *   旨在开发一种**无需训练（Training-Free）**的框架，通过结合视觉大模型（Foundation Models）与显式的几何形状拟合，实现稳健的少样本分割。

### 2. 方法论（RAP 框架）
RAP 框架分为三个阶段：**检索（Retrieve）**、**自适应（Adapt）**和**提示拟合（Prompt-Fit）**。

*   **检索阶段 (Retrieve)**：
    *   利用 **DINOv3** 提取图像的全局语义特征。
    *   从支撑数据库中检索与查询图像（Query）语义最相似的支撑样本（Support），以减少对单一支撑样本选择的依赖。
*   **自适应阶段 (Adapt)**：
    *   **频域风格迁移**：通过离散小波变换（DWT）交换支撑图像与查询图像的低频分量（LL），消除不同扫描仪导致的亮度/对比度差异，同时保留边缘细节。
    *   **DINO 语义门控**：对支撑特征进行 K-Means 聚类，计算查询图像的语义相似度图，生成门控掩码以缩小形状匹配的搜索空间。
    *   **定向倒角匹配 (Oriented Chamfer Matching, OCM)**：提取边缘并根据梯度方向划分为不同角度桶，通过搜索最优的平移、缩放和旋转参数，将支撑掩码对齐到查询图像，生成初步掩码（Pre-mask）。
*   **提示拟合阶段 (Prompt-Fit)**：
    *   **正点采样**：在预掩码上应用最远点采样（FPS）并进行 **Voronoi 划分**，取各区域质心作为正提示点，确保几何分布均匀。
    *   **负点采样**：基于边界扇区采样，结合 DINO 语义不相似度选取背景点。
    *   **SAM2 推理**：将生成的正负点提示、边界框（Bbox）及查询图像输入 **SAM2**，获得最终精细分割结果。

### 3. 实验设计
*   **数据集**：
    *   **Abd-MRI** (CHAOS 挑战赛)：腹部 MRI，包含肝、肾、脾。
    *   **Abd-CT** (BTCV 挑战赛)：腹部 CT，包含相同器官。
    *   **Card-MRI** (MS-CMRSeg 挑战赛)：心脏 MRI，包含左心室血池、心肌层和右心室。
*   **Benchmark 与对比方法**：
    *   在 **1-way 1-shot** 设置下进行评估。
    *   对比了 6 种基于训练的方法（如 PANet, SSL-ALPNet, FAMNet 等）和 1 种最新的无需训练方法（MAUP）。
*   **评价指标**：Dice 相似系数 (DSC)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 3090 GPU** (24GB 显存)。
*   **训练时长**：由于该方法是 **Training-Free**（无需训练），因此不存在训练耗时，仅涉及推理过程。
*   **模型库**：使用了 PyTorch 框架，集成了 DINOv3-ViT-L/16 和 SAM2-ViT-H。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同模态（MRI/CT）和解剖部位的数据集上进行了广泛测试。
*   **消融实验**：针对定向倒角匹配（OCM）、语义门控（SG）和 Voronoi 提示（VP）分别进行了消融研究，验证了各模块对性能的提升。
*   **客观性**：采用了留一法（Leave-one-out）评估，并在检索时选择 Rank-2 匹配以避免自匹配偏差，实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：RAP 在所有数据集上均达到了 SOTA 性能，平均 Dice 分数在 Abd-MRI 上为 69.16%，Abd-CT 上为 69.56%，Card-MRI 上为 75.27%。
*   **超越训练模型**：作为一个无需训练的方法，RAP 竟然超越了许多经过特定任务训练的少样本模型（如 FAMNet）。
*   **几何先验的重要性**：显式的形状对齐（OCM）和几何感知的提示点生成（Voronoi）是提升 SAM2 在医学影像中表现的关键。

### 7. 优点
*   **即插即用**：无需任何梯度更新或微调，能够直接部署于新领域。
*   **鲁棒性强**：通过频域适配和定向倒角匹配，有效处理了医学影像中常见的领域偏移和微弱边界问题。
*   **协同效应**：成功将自监督特征（DINOv3）的语义能力与分割大模型（SAM2）的几何控制能力结合。

### 8. 不足与局限
*   **数据库依赖**：性能受限于支撑数据库的质量和多样性，对于极罕见的解剖畸形或病理情况，检索可能失效。
*   **维度限制**：目前主要针对 **2D 切片**进行操作，虽然论文提到未来会扩展到 3D，但当前未充分利用 3D 空间一致性。
*   **计算开销**：虽然无需训练，但推理过程中涉及多尺度 LoG 滤波、角度桶距离变换及多次 SAM2 提示尝试，可能存在一定的推理延迟。

（完）
