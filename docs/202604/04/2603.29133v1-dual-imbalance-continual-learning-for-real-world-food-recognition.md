---
title: Dual-Imbalance Continual Learning for Real-World Food Recognition
title_zh: 针对真实场景食物识别的双重不平衡持续学习
authors: "Xiaoyan Zhang, Jiangpeng He"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29133v1"
tags: ["query:q15"]
score: 9.0
evidence: 真实场景食品识别的持续学习
tldr: "针对真实场景下食物识别存在的“双重不平衡”问题（类别内样本分布不均及各增量阶段新类别数量不均），本文提出DIME框架。该框架利用参数高效微调技术为各任务学习轻量化适配器，并通过类别计数引导的光谱合并策略及秩阈值调制机制进行动态整合。实验证明，DIME在长尾食物基准测试中显著优于现有持续学习方法，提升幅度超过3%，且推理效率高。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29133v1/fig-001.webp\", \"caption\": \"Table 1. Last task accuracy (AT ), average accuracy (Ā), and weighted average accuracy (wĀ) on VFN186-LT, VFN186-Insulin, VFN186T2D, and Food101-LT with ρ = 0.01. Results are averaged over 5 runs. The best results are in bold and the second-best results are underlined.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2603.29133v1/fig-002.webp\", \"caption\": \"Figure 3. Performance comparison under different step-imbalance ratios ρ on VFN186-LT and Food101-LT with T = 10 incremental steps. Our method consistently achieves higher and more stable performance across tasks under all imbalance settings.\", \"page\": 7, \"index\": 2, \"width\": 1036, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2603.29133v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of DIME. (a) Sequential food datasets are learned with lightweight adapters on a frozen backbone using Balanced Softmax. After each step, the new adapter is merged with the accumulated base adapter to produce a unified model. (b) The merging module performs spectral alignment via SVD, class-count guided weighting, and rank-wise threshold modulation.\", \"page\": 4, \"index\": 3, \"width\": 980, \"height\": 361}]"
motivation: 现有持续学习研究忽略了真实场景中食物类别样本分布不均以及各增量阶段新类别数量差异巨大的“双重不平衡”挑战。
method: 提出DIME框架，利用参数高效微调学习适配器，并结合类别计数引导的光谱合并与秩阈值调制机制实现知识的稳定整合。
result: "在现实的长尾食物数据集上，该方法在步骤不平衡的设定下比最强的持续学习基准模型性能提升了3%以上。"
conclusion: DIME通过创新的适配器合并策略有效解决了持续学习中的双重不平衡问题，为构建高效、稳健的真实世界食物识别系统提供了新思路。
---

## 摘要
真实世界饮食记录场景中的视觉食物识别天然地表现出严重的数据不平衡，即少数食物类别频繁出现，而许多其他类别则极少出现，从而导致长尾类别分布。在实践中，食物识别系统通常运行在持续学习设置下，新类别随时间顺序引入。然而，现有研究通常假设每个增量步骤引入的新食物类别数量相近，这在现实世界中很少发生，因为不同步骤中新观察到的类别数量可能存在显著差异，导致学习动态高度不均。因此，持续食物识别呈现出双重不平衡：每个食物类别内的样本不平衡，以及每个增量学习步骤中待学习的新食物类别数量不平衡。在这项工作中，我们提出了 DIME，一个用于持续食物识别的双重不平衡感知适配器合并框架。DIME 利用参数高效微调为每个任务学习轻量级适配器，并通过类别计数引导的光谱合并策略逐步整合这些适配器。此外，一种逐秩阈值调制机制通过保留主导知识并允许自适应更新，进一步稳定了合并过程。最终模型在推理时仅维持一个合并后的适配器，实现了高效部署，无需累积特定任务模块。在步骤不平衡设置下的真实长尾食物基准测试表明，所提方法较现有的最强持续学习基准模型持续提升了 3% 以上。代码可在 https://github.com/xiaoyanzhang1/DIME 获取。

## Abstract
Visual food recognition in real-world dietary logging scenarios naturally exhibits severe data imbalance, where a small number of food categories appear frequently while many others occur rarely, resulting in long-tailed class distributions. In practice, food recognition systems often operate in a continual learning setting, where new categories are introduced sequentially over time. However, existing studies typically assume that each incremental step introduces a similar number of new food classes, which rarely happens in real world where the number of newly observed categories can vary significantly across steps, leading to highly uneven learning dynamics. As a result, continual food recognition exhibits a dual imbalance: imbalanced samples within each food class and imbalanced numbers of new food classes to learn at each incremental learning step. In this work, we introduce DIME, a Dual-Imbalance-aware Adapter Merging framework for continual food recognition. DIME learns lightweight adapters for each task using parameter-efficient fine-tuning and progressively integrates them through a class-count guided spectral merging strategy. A rank-wise threshold modulation mechanism further stabilizes the merging process by preserving dominant knowledge while allowing adaptive updates. The resulting model maintains a single merged adapter for inference, enabling efficient deployment without accumulating task-specific modules. Experiments on realistic long-tailed food benchmarks under our step-imbalanced setup show that the proposed method consistently improves by more than 3% over the strongest existing continual learning baselines. Code is available at https://github.com/xiaoyanzhang1/DIME.

---

## 论文详细总结（自动生成）

### 论文分析报告：针对真实场景食物识别的双重不平衡持续学习 (DIME)

#### 1. 核心问题与整体含义（研究动机和背景）
本文关注的是真实世界中**食物识别（Food Recognition）**系统在持续学习（Continual Learning）过程中面临的严峻挑战。研究者指出，现有的持续学习研究通常假设每个增量阶段引入的新类别数量是均衡的，但在实际饮食记录场景中，存在**“双重不平衡”（Dual-Imbalance）**现象：
*   **类别间不平衡（Class Imbalance）：** 食物消费遵循长尾分布，少数热门食物样本极多，大量冷门食物样本极少。
*   **步骤间不平衡（Step Imbalance）：** 随着时间推移，每次新引入的食物类别数量差异巨大（例如，有时一次增加几十种新菜，有时只增加几种）。
这种双重不平衡会导致学习动态高度不稳定：大步骤和头部类别产生的梯度会淹没小步骤和尾部类别的知识，导致严重的灾难性遗忘或学习不足。

#### 2. 方法论：核心思想与关键技术
论文提出了 **DIME (Dual-Imbalance-aware Adapter Merging)** 框架，其核心思想是在冻结的预训练模型基础上，通过参数高效微调（PEFT）学习适配器，并利用光谱分析技术进行智能合并。

*   **任务内平衡优化：** 采用 **Balanced Softmax** 作为损失函数，通过引入类别频率先验来调整 Logits，缓解单个任务内部的长尾分布偏见。
*   **类别计数感知的光谱合并（Spectral Merging）：** 
    *   为每个新任务训练一个轻量级适配器（Adapter）。
    *   利用**奇异值分解（SVD）**将新旧适配器的参数矩阵对齐到共享的正交基空间。
    *   根据新旧任务涵盖的类别数量比例（Class-Count）计算权重，进行加权合并。
*   **秩阈值调制机制（Rank-Wise Threshold Modulation, RTM）：** 
    *   在 SVD 空间中，根据奇异值大小区分“主导方向”和“次要方向”。
    *   对代表核心视觉特征的主导方向（大奇异值）采用较小的更新系数以保持稳定性；对代表细节或新特征的次要方向（小奇异值）允许更大的更新灵活性。
*   **高效推理：** 最终将所有知识整合进单一适配器，推理时无需存储多个任务模块，也不需要任务 ID。

#### 3. 实验设计
*   **数据集：** 使用了四个具有挑战性的长尾食物基准：**VFN186-LT**、**VFN186-Insulin**（胰岛素用户数据）、**VFN186-T2D**（2型糖尿病患者数据）以及经典的 **Food101-LT**。
*   **Benchmark 设置：** 引入了步骤不平衡因子 $\rho$，模拟从轻微到极端的步骤不平衡场景（$\rho \in \{0.1, 0.05, 0.01, 0.001\}$）。
*   **对比方法：** 涵盖了 9 种代表性持续学习方法，包括 SLCA、InfLoRA、EASE、SEMA、CL-LoRA、ACMap、MOS 和 TUNA 等。
*   **评价指标：** 除了最终准确率 ($A_T$) 和平均准确率 ($\bar{A}$)，还专门引入了**加权平均准确率 ($w\bar{A}$)**，以更公平地衡量步骤不平衡下的性能。

#### 4. 资源与算力
*   **硬件：** 实验在一台 **NVIDIA L40S GPU** 上完成。
*   **软件：** 基于 PyTorch 框架，使用 ViT-B/16 作为预训练主干网络。
*   **训练细节：** 每个任务训练 20 个 Epoch，Batch Size 为 16，学习率为 0.07。虽然未明确给出总训练时长，但考虑到适配器微调的参数量极小（适配器维度仅 64），其计算开销远低于全参数微调。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 4 个数据集上进行了详尽测试，每个实验均运行 5 次取平均值。
*   **消融实验：** 针对光谱合并（SM）、类别计数权重（CCW）、秩阈值调制（RTM）和 Balanced Softmax（BSM）分别做了剥离实验，验证了各组件的有效性。
*   **敏感性分析：** 探讨了超参数（如头方向比例 $r_h$ 和保护系数 $\gamma$）对结果的影响。
*   **公平性：** 所有基线方法均在相同的步骤不平衡协议下重新评估，确保了对比的客观性。

#### 6. 主要结论与发现
*   **性能领先：** DIME 在所有数据集和不平衡设置下均显著优于现有方法，在 $A_T$ 指标上通常领先最强基线 **3% 以上**。
*   **稳健性：** 在极端不平衡（$\rho=0.001$）的情况下，DIME 的性能曲线比其他方法更平滑，证明其合并策略能有效抵御小任务带来的噪声干扰。
*   **效率优势：** DIME 在推理时的 FLOPs 和耗时与最轻量的方法持平，但准确率大幅领先，实现了极佳的效能平衡。

#### 7. 优点（亮点）
*   **场景真实：** 首次系统性地定义并解决了持续学习中“双重不平衡”这一极具现实意义的问题。
*   **数学严谨：** 引入 SVD 进行适配器合并，比简单的参数平均更具理论支撑，能更好地处理特征空间的对齐。
*   **部署友好：** 最终模型保持单一适配器结构，避免了随着任务增加而导致的参数量爆炸。

#### 8. 不足与局限
*   **超参数依赖：** 秩阈值调制机制引入了额外的超参数（如 $r_h, \gamma_{head}, \gamma_{tail}$），虽然实验证明其不敏感，但在全新领域应用时可能需要微调。
*   **任务类型局限：** 目前仅验证了分类任务，尚未扩展到更复杂的食物成分识别或多标签识别场景。
*   **预训练模型依赖：** 该方法高度依赖于高质量的预训练主干网络（如 ImageNet-21K 预训练的 ViT），若主干网络与食物领域差异过大，适配器的迁移能力可能受限。

（完）
