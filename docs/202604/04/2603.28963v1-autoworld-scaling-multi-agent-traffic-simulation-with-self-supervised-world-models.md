---
title: "AutoWorld: Scaling Multi-Agent Traffic Simulation with Self-Supervised World Models"
title_zh: AutoWorld：利用自监督世界模型扩展多智能体交通仿真
authors: "Mozhgan Pourkeshavatz, Tianran Liu, Nicholas Rhinehart"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28963v1"
tags: ["query:q13"]
score: 10.0
evidence: 从无标签LiDAR数据中学习的世界模型用于交通模拟
tldr: AutoWorld是一个利用大规模无标签传感器数据提升多智能体交通模拟真实性的框架。针对现有模拟器过度依赖标注数据且难以扩展的问题，该研究提出了一种基于自监督学习的世界模型，通过LiDAR数据的占用表示学习场景动态。结合粗到细的预测场景上下文、多样性引导采样及运动感知潜在监督，AutoWorld在WOSAC基准测试中排名第一，证明了利用无标签数据实现模拟性能持续扩展的可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28963v1/fig-001.webp\", \"caption\": \"Fig. 5: Qualitative analysis of multimodal behavior coverage. The ground-truth ego trajectory, candidate SDC paths from WOMD, and 32 rollouts generated by AutoWorld are visualized (left to right). The model produces diverse behaviors within a single scenario, including alternative turning directions and lane changes, with diversity that also reflects different kinematic profiles.\", \"page\": 21, \"index\": 1, \"width\": 664, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-002.webp\", \"caption\": \"Table 1: Results on the WOSAC leaderboard. RMM (Realism Meta Metric) is the primary ranking metric. The “†” denotes technical reports for the Waymo challenge.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of AutoWorld. A LiDAR-based world model is first trained on unlabeled sequences to learn latent scene dynamics. At simulation time, the trained model predicts future occupancies from the observed LiDAR history, which condition the motion generation module. At inference, we sample from both the world model and the motion generator using a training-free cascaded latent diversity strategy.\", \"page\": 4, \"index\": 3, \"width\": 614, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2603.28963v1/fig-004.webp\", \"caption\": \"Table 2: Ablation study on WOSAC 2% validation split. The “†” denotes the model we used to submit on the leaderbord.\", \"page\": 12, \"index\": 4, \"width\": 725, \"height\": 296}]"
motivation: 现有多智能体交通模拟器高度依赖昂贵的标注数据，难以利用海量无标签传感器数据进行性能扩展。
method: 提出AutoWorld框架，利用无标签LiDAR占用表示学习世界模型，并结合级联行列式点过程（DPP）和运动感知潜在监督来生成多样且真实的交通场景。
result: 在WOSAC基准测试中位列榜首，且实验证明模拟性能随无标签LiDAR数据的增加而持续提升。
conclusion: 该研究为在无需额外标注的情况下，通过大规模无标签数据提升交通模拟的真实性开辟了新路径。
---

## 摘要
多智能体交通仿真是开发和测试自动驾驶系统的核心。近期的训练驱动型仿真器已取得显著成果，但严重依赖于对标注轨迹或语义注释的监督学习，这使得扩展其性能的成本十分高昂。与此同时，虽然可以大规模采集海量的无标注传感器数据，但现有的交通仿真框架在很大程度上仍未利用这些数据。这提出了一个关键问题：如何利用无标注数据来提升交通仿真的性能？在这项工作中，我们提出了 AutoWorld，这是一个利用从激光雷达（LiDAR）数据的无标注占用表示中学习到的世界模型的交通仿真框架。给定世界模型样本，AutoWorld 构建了一个由粗到精的预测场景上下文，作为多智能体运动生成模型的输入。为了提升样本多样性，AutoWorld 采用级联行列式点过程（DPP）框架来引导世界模型和运动模型的采样过程。此外，我们设计了一个运动感知的潜空间监督目标，增强了 AutoWorld 对场景动力学的表示能力。在 WOSAC 基准测试上的实验表明，根据主要的真实性元指标（RMM），AutoWorld 在排行榜上排名第一。我们进一步证明，随着无标注激光雷达数据的加入，仿真性能持续提升，并通过消融实验研究了各组件的有效性。我们的方法为在无需额外标注的情况下提升交通仿真的真实性铺平了道路。我们的项目主页包含额外的可视化效果和发布的源代码。

## Abstract
Multi-agent traffic simulation is central to developing and testing autonomous driving systems. Recent data-driven simulators have achieved promising results, but rely heavily on supervised learning from labeled trajectories or semantic annotations, making it costly to scale their performance. Meanwhile, large amounts of unlabeled sensor data can be collected at scale but remain largely unused by existing traffic simulation frameworks. This raises a key question: How can a method harness unlabeled data to improve traffic simulation performance? In this work, we propose AutoWorld, a traffic simulation framework that employs a world model learned from unlabeled occupancy representations of LiDAR data. Given world model samples, AutoWorld constructs a coarse-to-fine predictive scene context as input to a multi-agent motion generation model. To promote sample diversity, AutoWorld uses a cascaded Determinantal Point Process framework to guide the sampling processes of both the world model and the motion model. Furthermore, we designed a motion-aware latent supervision objective that enhances AutoWorld's representation of scene dynamics. Experiments on the WOSAC benchmark show that AutoWorld ranks first on the leaderboard according to the primary Realism Meta Metric (RMM). We further show that simulation performance consistently improves with the inclusion of unlabeled LiDAR data, and study the efficacy of each component with ablations. Our method paves the way for scaling traffic simulation realism without additional labeling. Our project page contains additional visualizations and released code.

---

## 论文详细总结（自动生成）

以下是对论文《AutoWorld: Scaling Multi-Agent Traffic Simulation with Self-Supervised World Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多智能体交通仿真模型（如基于扩散模型或自回归模型）高度依赖于**人工标注的轨迹数据或语义地图**。这种监督学习范式面临标注成本高昂、难以利用海量原始传感器数据进行性能扩展（Scaling）的问题。
*   **核心问题**：如何利用海量**无标注**的传感器数据（如 LiDAR 点云）来提升交通仿真的真实感和多样性？
*   **整体含义**：论文提出了一种名为 **AutoWorld** 的半监督框架，通过自监督学习构建 LiDAR 占用世界模型，并以此引导多智能体行为生成，实现了仿真性能随无标注数据量增加而持续提升。

### 2. 方法论
AutoWorld 采用两阶段架构，结合了世界模型（World Model）和条件扩散策略：
*   **运动感知潜在占用预测（第一阶段）**：
    *   使用 VAE 将 LiDAR 点云编码为潜在占用表示（Latent Occupancy）。
    *   采用**整流流匹配（Rectified Flow Matching）**训练世界模型，预测未来潜在占用序列。
    *   **关键技术：运动感知潜在监督**。针对占用网格中静态背景占主导的问题，通过补偿自我运动（Ego-motion compensation）计算体素级的占用变化，生成权重图，强制模型关注动态变化的区域。
*   **世界模型引导的行为生成（第二阶段）**：
    *   **粗到细预测上下文**：将世界模型预测的未来潜在序列通过多头交叉注意力（MHCA）聚合为全局“预测场景上下文”，并结合每一步的局部占用特征，作为扩散模型生成智能体动作的条件。
*   **级联潜在多样性采样（推理阶段）**：
    *   引入**行列式点过程（DPP）**。在推理时，通过计算样本间的相似度内核矩阵，利用其对数行列式的梯度引导采样过程。
    *   **级联机制**：在世界模型采样（场景级）和运动生成采样（行为级）中分别应用 DPP，并结合质量权重（如碰撞率、越野率等约束），确保生成的场景既多样又符合物理真实性。

### 3. 实验设计
*   **数据集**：使用 **Waymo Open Dataset (WOD)** 训练世界模型（利用其无标注 LiDAR 序列），使用 **Waymo Open Motion Dataset (WOMD)** 训练运动生成模块。
*   **Benchmark**：**WOSAC (Waymo Open Sim Agents Challenge)**，这是目前最权威的闭环多智能体行为生成基准。
*   **对比方法**：包括 InfGen、LLM2AD、SMART-R1、UniMotion、TrajTok、DecompGAIL 等近年来在排行榜领先的监督学习或强化学习微调方法。
*   **评价指标**：主要指标为真实性元指标（RMM），涵盖运动学（Kinematic）、交互性（Interactive）和地图遵从性（Map-based）。

### 4. 资源与算力
*   **算力支持**：文中提到使用了 Digital Research Alliance of Canada、NVIDIA 学术捐赠计划以及 **Google TPU Research Cloud (TRC)**。
*   **具体细节**：
    *   VAE 训练 100 个 epoch（Batch size 16）。
    *   世界模型训练 100 个 epoch，随后进行 50 个 epoch 的运动感知微调（Batch size 8）。
    *   扩散模型推理步数 $K_f=50$。
    *   **注**：文中未明确给出具体的 GPU 型号数量或总训练小时数，但从使用的 TPU 资源来看，该模型具有一定的计算规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **排行榜对比**：在 WOSAC 完整测试集上进行了提交，并取得第一名。
    *   **消融实验**：在 2% 的验证集上进行了详尽的消融，包括采样预算分配（$N \times M$）、多样性策略组合、预测上下文的作用等。
    *   **扩展性实验**：测试了从 1x 到 5x 不同规模的无标注数据对 RMM 指标的影响。
*   **充分性评价**：实验设计非常充分且客观。通过控制变量法验证了世界模型在没有语义标签的情况下依然能提供有效的结构化先验，且通过数据扩展曲线有力证明了其 Scaling 能力。

### 6. 主要结论与发现
*   **性能领先**：AutoWorld 在 WOSAC 排行榜上 RMM 达到 0.7865，位列第一，尤其在交互性（Interactive）得分上表现突出。
*   **Scaling 效应**：仿真真实性随无标注 LiDAR 训练数据的增加而单调提升，证明了自监督世界模型是扩展仿真性能的有效路径。
*   **多样性平衡**：级联 DPP 采样能有效利用世界模型提供的多模态假设，在不牺牲真实性的前提下显著提升场景覆盖度。
*   **运动感知的重要性**：相比均匀权重的监督，运动感知监督能显著提升动态目标的预测精度（IoU 提升约 13%）。

### 7. 优点
*   **数据效率**：打破了对大规模轨迹标注的依赖，开辟了利用原始传感器数据提升仿真的新方向。
*   **架构优雅**：将世界模型作为“未来先验”集成到扩散模型中，实现了从粗（场景占用）到细（智能体轨迹）的引导。
*   **训练无须多样性微调**：通过推理时的 DPP 引导实现多样性，无需复杂的对抗训练或强化学习微调。

### 8. 不足与局限
*   **身份缺失**：世界模型仅预测占用（Occupancy），不区分具体的智能体身份，这可能导致在极度拥堵或复杂交互场景下，扩散模型对特定智能体的引导不够精准。
*   **计算开销**：推理阶段需要运行世界模型生成多个潜在序列，并计算 DPP 梯度，相比纯轨迹预测模型，其计算延迟更高。
*   **拓扑覆盖**：定性分析显示，模型对某些极端复杂的地图拓扑路径（如非常规的转向）覆盖仍有不足。

（完）
