---
title: "Knowledge is Power: Advancing Few-shot Action Recognition with Multimodal Semantics from MLLMs"
title_zh: 知识就是力量：利用多模态大语言模型（MLLMs）的多模态语义推进少样本动作识别
authors: "Jiazheng Xing, Chao Xu, Hangjie Yuan, Mengmeng Wang, Jun Dan, Hangwei Qian, Yong Liu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26033v1"
tags: ["query:q11"]
score: 9.0
evidence: 利用多模态大模型作为多模态知识库进行少样本动作识别
tldr: 针对少样本动作识别（FSAR）中多模态大模型（MLLM）利用不充分的问题，本文提出FSAR-LLaVA。该方法直接将MLLM作为多模态知识库，通过多模态特征增强模块提取并解耦视觉与文本特征，利用任务导向的原型构建缓解分布偏移，并引入免训练的多模态原型匹配度量。实验证明，该方法在极少可训练参数下实现了性能的显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26033v1/fig-001.webp\", \"caption\": \"Fig. 3: Overview of our CTPCM, which is divided into local prototype construction and global prototype construction.\", \"page\": 6, \"index\": 1, \"width\": 449, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-002.webp\", \"caption\": \"Fig. 4: Visualization of the attention map for the data-driven method Hyrsm [7] and our FSAR-LLaVA on HMDB51. We also provide the Video-LLaVA’s QA results for these videos using “Unknown” prompts.\", \"page\": 11, \"index\": 2, \"width\": 944, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-003.webp\", \"caption\": \"Fig. 6: Performance comparison between our knowledge-driven FSAR-LLaVA and the datadriven baseline of the 5-way 1-shot task on five widely used datasets. All methods share the same vision encoder.\", \"page\": 15, \"index\": 3, \"width\": 454, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-004.webp\", \"caption\": \"Fig. 1: (a), (b), and (c) indicate the pipeline of the data-driven unimodal/multimodal method, knowledge-driven CapFSAR, and our FSARLLaVA, respectively. Our FSAR-LLaVAUnknown, which uses the fixed input prompt: “What’s the action of the video?” without introducing additional textual label information, fully leverages the multimodal features of MLLM and achieves state-of-the-art performance that requires minimal parameters, as depicted in part (d), which refers to the performance comparison in the HMDB51 5-way 1-shot task.\", \"page\": 2, \"index\": 4, \"width\": 448, \"height\": 420}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-005.webp\", \"caption\": \"Table 1: Comparison under 5-way k-shot settings on five widely-used benchmarks including HMDB51, UCF101, Kinetics, SSv2-Small, and SSv2-Full.\", \"page\": 10, \"index\": 5, \"width\": 945, \"height\": 615}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-006.webp\", \"caption\": \"Table 12: Performance comparison of the hidden layer features from Video-LLaVA. The boldfacen and underline font indicate the highest and the second highest results. Note that “Avg.” denotes Average.\", \"page\": 18, \"index\": 6, \"width\": 454, \"height\": 590}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-007.webp\", \"caption\": \"Fig. 5: Direct qualitative analysis of the Video-LLaVA’s output with different types of prompts. For clarity, we highlight words in blue to represent accurate action labels, in red to reveal the question posed in Video-LLaVA, and in green to indicate inaccurate action descriptions.\", \"page\": 12, \"index\": 7, \"width\": 950, \"height\": 824}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-008.webp\", \"caption\": \"Fig. 2: Overview of our FSAR-LLaVA: Visual inputs and text prompts are processed through our knowledge base to extract multimodal tokens Tm from the multimodal decoder’s hidden layer. These tokens are downsampled and decoupled into visual tokens Tv and textual tokens Tt. The tokens are then passed through the Multimodal Feature-enhanced Module, and features from both branches are processed in the Composite Task-oriented Prototype Construction Module to obtain the enhanced support set class prototypes P̃S v and P̃S t . Finally, the class prototypes and query features are fed into the Multimodal Matching Metric to yield the probability distribution pQ2S and loss LQ2S .\", \"page\": 5, \"index\": 8, \"width\": 906, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2603.26033v1/fig-009.webp\", \"caption\": \"Table 8: Complexity analysis for 5-way 1-shot HMDB51 evaluation. Our FSAR-LLaVA utilizes the “Unknown” prompts here. “Time” represents the training time cost of each 5-way 1-shot task. “Acc” means the 5-way 1-shot accuracy on HMDB51. All experiments are carried out on one Nvidia H20 GPU.\", \"page\": 16, \"index\": 9, \"width\": 879, \"height\": 210}]"
motivation: 现有FSAR方法主要依赖次优的“特征-描述-特征”流程且仅在视觉空间进行度量学习，未能充分挖掘MLLM的语义知识。
method: 提出FSAR-LLaVA框架，包含多模态特征增强模块、复合任务导向原型构建以及免训练的多模态原型匹配度量。
result: 在多个FSAR基准测试中取得了优于现有方法的性能，且仅需极少量的可训练参数。
conclusion: 本研究证明了直接利用MLLM作为多模态知识库能有效增强FSAR的语义表征能力和泛化性能。
---

## 摘要
多模态大语言模型（MLLMs）推动了少样本动作识别（FSAR）领域的发展。然而，该领域的初步探索主要集中在生成字幕，以形成一种次优的“特征->字幕->特征”流水线，并仅在视觉空间内采用度量学习。在本文中，我们提出了 FSAR-LLaVA，这是首个将 MLLMs（如 Video-LLaVA）作为多模态知识库来直接增强 FSAR 的端到端方法。首先，在特征层面，我们利用 MLLM 的多模态解码器提取时空和语义丰富的表示，随后通过我们的多模态特征增强模块将其解耦并增强为独立的视觉和文本特征，从而充分利用其语义知识进行 FSAR。其次，我们利用 MLLMs 的通用性来构建灵活适应各种场景的输入提示，并利用其对齐的输出驱动我们设计的复合任务导向原型构建，从而有效弥合元训练集和元测试集之间的分布差异。最后，为了使多模态特征能够共同引导度量学习，我们引入了一种无需训练的多模态原型匹配度量，该度量能够自适应地选择最具决定性的线索，并高效利用 MLLMs 产生的解耦特征表示。大量实验表明，该方法在各种任务中均表现出优越的性能，且仅需极少的待训练参数。

## Abstract
Multimodal Large Language Models (MLLMs) have propelled the field of few-shot action recognition (FSAR). However, preliminary explorations in this area primarily focus on generating captions to form a suboptimal feature->caption->feature pipeline and adopt metric learning solely within the visual space. In this paper, we propose FSAR-LLaVA, the first end-to-end method to leverage MLLMs (such as Video-LLaVA) as a multimodal knowledge base for directly enhancing FSAR. First, at the feature level, we leverage the MLLM's multimodal decoder to extract spatiotemporally and semantically enriched representations, which are then decoupled and enhanced by our Multimodal Feature-Enhanced Module into distinct visual and textual features that fully exploit their semantic knowledge for FSAR. Next, we leverage the versatility of MLLMs to craft input prompts that flexibly adapt to diverse scenarios, and use their aligned outputs to drive our designed Composite Task-Oriented Prototype Construction, effectively bridging the distribution gap between meta-train and meta-test sets. Finally, to enable multimodal features to guide metric learning jointly, we introduce a training-free Multimodal Prototype Matching Metric that adaptively selects the most decisive cues and efficiently leverages the decoupled feature representations produced by MLLMs. Extensive experiments demonstrate superior performance across various tasks with minimal trainable parameters.

---

## 论文详细总结（自动生成）

### 论文总结：Knowledge is Power: Advancing Few-shot Action Recognition with Multimodal Semantics from MLLMs

#### 1. 核心问题与整体含义（研究动机和背景）
少样本动作识别（FSAR）旨在利用极少量标注样本识别新类别动作。传统方法分为**数据驱动的单模态/多模态方法**（依赖视觉或CLIP等预训练模型）和**知识驱动方法**（如CapFSAR）。
*   **研究动机**：现有的知识驱动方法（如CapFSAR）通常采用“特征→字幕→特征”的次优流程，在字幕生成过程中存在严重的语义信息降级，且度量学习仅局限于视觉空间，未能充分挖掘多模态大语言模型（MLLM）内部丰富的世界知识。
*   **核心目标**：提出一种端到端的框架 **FSAR-LLaVA**，直接利用 MLLM 的中间层隐藏特征作为多模态知识库，避免信息损失，并实现视觉与文本语义的深度融合。

#### 2. 方法论：核心思想与关键技术
论文提出了三个核心模块，旨在高效迁移 MLLM 的知识：
*   **多模态特征增强模块 (MFM)**：
    *   从 MLLM（如 Video-LLaVA）的解码器隐藏层提取多模态 Token。
    *   **解耦与增强**：将混合 Token 还原为视觉和文本分支。通过空间/时间自注意力增强视觉表征，并利用交叉注意力机制实现双模态间的语义互补（视觉引导文本，文本引导视觉）。
*   **复合任务导向原型构建模块 (CTPCM)**：
    *   针对元训练和元测试集之间的分布偏移，设计了**局部原型构建 (LPC)** 和 **全局原型构建 (GPC)**。
    *   利用查询集（Query Set）的特征动态调整支持集（Support Set）的类中心，使原型更具任务针对性。
*   **多模态原型匹配度量 (MPMM)**：
    *   一种**免训练**的度量方法。它基于 Hausdorff 距离，但引入了自适应筛选机制（Top-u 距离求和），从双分支中选择最具辨识度的 Token 进行匹配，有效抑制了动作无关的噪声。

#### 3. 实验设计
*   **数据集**：涵盖了空间相关（Kinetics, HMDB51, UCF101）和时间相关（SSv2-Full, SSv2-Small）五大主流基准数据集。
*   **Benchmark 与对比方法**：
    *   **单模态**：OTAM, TRX, HyRSM, STRM 等。
    *   **多模态（CLIP 基础）**：CLIP-FSAR, Task-Adapter, MVP-shot 等。
    *   **知识驱动**：CapFSAR（基于 BLIP）。
*   **实验设置**：采用标准的 5-way 1-shot 和 5-shot 设置。设计了“Unknown”（无标签提示词）和“Known”（含标签提示词）两种 Prompt 模式。

#### 4. 资源与算力
*   **硬件**：实验明确提到使用了 **Nvidia H20 GPU** 进行测试。
*   **效率分析**：论文对比了“离线”（预存特征）和“在线”（实时推理）两种模式。
    *   **参数量**：可训练参数量极小（约 7.2M - 9.3M），远低于全量微调。
    *   **耗时**：在离线模式下，单次任务推理时间约为 0.18s；在线模式受限于 MLLM 主干网络，延迟较高（约 4.4s/item）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估（5个数据集 × 2种 Shot × 多种模型主干）。
*   **消融实验**：非常详尽，包括：直接利用隐藏特征的必要性、特征解耦的作用、MFM/CTPCM 各组件的贡献、不同匹配度量的对比、MLLM 不同隐藏层的性能差异、超参数（$\alpha, u, D'$）的敏感性分析。
*   **客观性**：通过引入 Qwen2-VL 和 Qwen3-VL 作为主干网络，验证了框架的通用性；同时进行了失败案例分析（Failure Analysis），客观指出了模型在复杂时间动作上的局限。

#### 6. 主要结论与发现
*   **性能领先**：FSAR-LLaVA 在所有数据集上均达到 SOTA 性能，尤其在“Known”模式下大幅领先。
*   **知识的力量**：即使在不提供类别标签的“Unknown”模式下，直接利用 MLLM 的多模态特征也优于大多数专门设计的单模态 FSAR 方法。
*   **模态偏好**：空间相关数据集更依赖文本分支语义，而时间相关数据集（如 SSv2）更依赖视觉分支。
*   **层级特性**：MLLM 的倒数第二层（31层）通常包含最强的语义表征能力。

#### 7. 优点（亮点）
*   **端到端知识迁移**：首个直接利用 MLLM 解码器特征进行 FSAR 的工作，避免了字幕生成的瓶颈。
*   **轻量化**：仅需微调极少量的注意力层参数，即可适配强大的大模型知识。
*   **通用性强**：框架不绑定特定模型，可无缝切换至 Qwen 等其他 MLLM。
*   **自适应性**：提出的 MPMM 度量和 CTPCM 模块能有效处理多模态噪声和分布偏移。

#### 8. 不足与局限
*   **时间推理瓶颈**：虽然引入了 MLLM，但在处理 SSv2 等强时间相关任务时，若无标签引导（Unknown 模式），MLLM 仍会出现理解偏差。
*   **在线推理开销**：由于依赖巨大的 MLLM 主干（7B/8B 参数），实时在线推理的计算成本和延迟仍然是工业部署的挑战。
*   **Prompt 依赖**：实验显示 Prompt 的微小改动会对结果产生非平凡的影响，如何自动化寻找最优 Prompt 尚待研究。

（完）
