---
title: "HiFlow: Tokenization-Free Scale-Wise Autoregressive Policy Learning via Flow Matching"
title_zh: HiFlow：通过流匹配实现的无需分词的跨尺度自回归策略学习
authors: "Daichi Yashima, Koki Seno, Shuhei Kurita, Yusuke Oda, Komei Sugiura"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27281v1"
tags: ["query:q12"]
score: 9.0
evidence: 机器人视觉运动策略学习
tldr: 本研究针对机器人视觉运动策略学习，提出了HiFlow框架。传统方法借鉴图像生成，使用离散分词器将动作序列映射为索引，但这在处理低维连续动作时会产生量化误差且训练复杂。HiFlow摒弃了分词器，直接在连续动作空间操作，通过时间池化构建多尺度目标，并结合流匹配技术实现从粗到细的自回归生成。该方法支持单阶段端到端训练，在模拟和真实环境实验中均展现出超越扩散模型和离散自回归模型的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27281v1/fig-001.webp\", \"caption\": \"Fig. 1: Tokenized vs tokenization-free scale-wise policy learning. Prior next-scale autoregressive policies discretize multi-scale action chunks using a VQ-VAE tokenizer and learn to predict code indices with cross-entropy, which introduces a separate tokenizer pretraining stage and incurs quantization error. HIFLOW instead refines raw continuous actions from coarse to fine temporal scales. A scalewise autoregressive transformer provides coarse-to-fine conditioning, and a shared conditional flow matching module generates continuous actions at each scale, eliminating the tokenizer while preserving fine-grained control.\", \"page\": 1, \"index\": 1, \"width\": 512, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.27281v1/fig-002.webp\", \"caption\": \"Fig. 5: Qualitative results of HIFLOW on the Orange→Plate task from the real-world experiments. Note that the third-person view are not included in the observation of the model.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2603.27281v1/fig-003.webp\", \"caption\": \"Fig. 6: Visualization of coarse-to-fine action generation. Each panel shows the predicted cumulative end-effector displacement trajectory in Cartesian task space (position components only) at a different scale level on the Mimicgen benchmark. ◦,⋆, and ⋄ indicate token boundaries, trajectory start point and the end point, respectively.\", \"page\": 7, \"index\": 3, \"width\": 1054, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2603.27281v1/fig-004.webp\", \"caption\": \"Fig. 2: Architecture overview of HIFLOW. Given visual observations, proprioceptive states, and a task identifier, the scale-wise autoregressive Transformer (ScaleAR) produces conditioning features at progressively finer temporal scales via a scale-wise causal mask. A shared ActionFlowNet then generates continuous actions at each scale through conditional flow matching, progressively refining the trajectory from a single-token global summary (scale 1) to the full T -step action chunk. The entire pipeline operates in continuous action space without any discrete tokenization.\", \"page\": 3, \"index\": 4, \"width\": 1052, \"height\": 464}, {\"url\": \"assets/figures/arxiv/2603.27281v1/fig-005.webp\", \"caption\": \"Fig. 3: Task overview across the three evaluation benchmarks. Top: 8 single-arm manipulation tasks from MimicGen. Middle: 3 dual-arm tasks from RoboTwin 2.0. Bottom: 5 real-world tasks with a mobile manipulator, spanning object grasping, relocation, and target placement.\", \"page\": 5, \"index\": 5, \"width\": 512, \"height\": 441}]"
motivation: 现有自回归策略使用的离散分词器在处理低维机器人动作时会引入量化误差，且导致训练流程过于复杂。
method: 提出一种无分词器的分层流策略，利用时间池化构建多尺度连续动作目标，并通过流匹配实现端到端的自回归学习。
result: 实验结果显示，HiFlow在MimicGen、RoboTwin 2.0及实物测试中均优于扩散模型和基于分词的自回归策略。
conclusion: HiFlow通过在连续空间进行多尺度建模，简化了机器人策略训练流程并显著提升了操作任务的精度与效率。
---

## 摘要
从粗到细的自回归建模最近在视觉运动策略学习中展现出强大的潜力，它结合了自回归方法的推理效率与基于扩散策略的全局轨迹连贯性。然而，现有方法依赖于离散动作分词器，将连续动作序列映射到码本索引，这种设计继承自图像生成领域，在图像生成中，学习压缩对于高维像素数据是必要的。我们观察到机器人动作本质上是低维连续向量，对此类数据进行分词会引入不必要的量化误差和多阶段训练流程。在这项工作中，我们提出了分层流策略（HiFlow），这是一种无需分词的从粗到细的自回归策略，直接作用于原始连续动作。HiFlow 通过简单的时序池化，从每个动作块中构建多尺度连续动作目标。具体而言，它对连续的动作窗口取平均值以生成粗略摘要，并在更精细的时序分辨率下进行细化。整个模型在单一阶段内进行端到端训练，消除了对独立分词器的需求。在 MimicGen、RoboTwin 2.0 和真实环境中的实验表明，HiFlow 的表现始终优于现有方法，包括基于扩散和基于分词的自回归策略。

## Abstract
Coarse-to-fine autoregressive modeling has recently shown strong promise for visuomotor policy learning, combining the inference efficiency of autoregressive methods with the global trajectory coherence of diffusion-based policies. However, existing approaches rely on discrete action tokenizers that map continuous action sequences to codebook indices, a design inherited from image generation where learned compression is necessary for high-dimensional pixel data. We observe that robot actions are inherently low-dimensional continuous vectors, for which such tokenization introduces unnecessary quantization error and a multi-stage training pipeline. In this work, we propose Hierarchical Flow Policy (HiFlow), a tokenization-free coarse-to-fine autoregressive policy that operates directly on raw continuous actions. HiFlow constructs multi-scale continuous action targets from each action chunk via simple temporal pooling. Specifically, it averages contiguous action windows to produce coarse summaries that are refined at finer temporal resolutions. The entire model is trained end-to-end in a single stage, eliminating the need for a separate tokenizer. Experiments on MimicGen, RoboTwin 2.0, and real-world environments demonstrate that HiFlow consistently outperforms existing methods including diffusion-based and tokenization-based autoregressive policies.

---

## 论文详细总结（自动生成）

### 论文总结：HiFlow —— 基于流匹配的无需分词跨尺度自回归策略学习

#### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人视觉运动策略（Visuomotor Policy）在结合自回归（AR）的高效推理与扩散模型（DM）的全局连贯性时，通常依赖**离散动作分词器**（如 VQ-VAE）。
*   **研究动机**：
    *   **量化误差**：机器人动作通常是低维连续向量（7-14自由度），强行将其离散化会引入不必要的量化误差。
    *   **流程复杂**：离散分词需要多阶段训练（先训练分词器，再训练策略），增加了系统复杂度和超参数调优难度。
    *   **领域局限**：分词器设计往往针对特定机器人硬件，缺乏通用性。
*   **整体含义**：HiFlow 旨在通过**流匹配（Flow Matching）**技术，在连续动作空间内实现从粗到细的自回归生成，从而彻底摆脱分词器。

#### 2. 方法论
HiFlow 由三个核心组件构成，支持单阶段端到端训练：
*   **无需分词的多尺度动作表示**：
    *   不使用学习的分词器，而是通过简单的**时间维度平均池化（Temporal Averaging）**构建多尺度目标。
    *   例如，将 $T$ 步动作块下采样为 $1, 2, 4, \dots, T$ 个尺度的连续向量，保留了动作的欧几里得几何特性。
*   **跨尺度自回归 Transformer (ScaleAR)**：
    *   负责生成每一尺度的条件特征。
    *   采用跨尺度因果掩码（Scale-wise Causal Mask），确保精细尺度仅依赖于较粗尺度的信息。
*   **共享动作流网络 (ActionFlowNet)**：
    *   **核心思想**：在每个尺度上，使用流匹配模型将高斯噪声转化为目标动作分布。
    *   **技术细节**：学习一个速度场（Velocity Field），通过常微分方程（ODE）将先验分布输送到数据分布。
    *   **条件注入**：通过时间自适应层归一化（AdaLN）将 ScaleAR 的特征注入流网络，确保轨迹的时间连贯性。
*   **损失函数**：采用跨尺度的流匹配均方误差（MSE），并根据尺度精细程度进行加权（尺度越细，权重越大）。

#### 3. 实验设计
*   **实验场景与数据集**：
    1.  **MimicGen**：单臂多任务操作（8个任务，如咖啡机、锤子清理、穿线等），包含1K-10K演示数据。
    2.  **RoboTwin 2.0**：双臂协同操作（3个任务，如移动罐子、放置篮子），涉及14自由度的高维动作空间。
    3.  **真实世界实验**：使用丰田 HSR 移动机器人完成5类任务（抓取、重定位、精准放置）。
*   **对比方法 (Benchmarks)**：
    *   **扩散类**：Diffusion Policy (DP), TCD, SDP。
    *   **自回归类**：ACT, CARP（基于分词的跨尺度 AR 领先模型）。

#### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA H200 SXM GPU** (每台 141GB 显存) 进行并行训练。
*   **推理算力**：在单张 **GeForce RTX 4090** (24GB 显存) 上进行推理。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到采用单阶段端到端训练，相比多阶段方法更高效。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了模拟环境（11个任务）和真实世界（5个任务），总计超过 16 组对比实验。
*   **消融/敏感性分析**：针对时间尺度的配置（如 $\{1, 8\}$ vs $\{1, 2, 4, 8\}$）进行了专门的敏感性实验。
*   **充分性评价**：实验设计较为充分，涵盖了单臂、双臂、移动机器人等多种形态，且对比了当前最先进的扩散和自回归基准，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：HiFlow 在所有基准测试中均优于现有方法。在 MimicGen 上平均成功率达到 88%（超过 CARP 的 85%）；在复杂的双臂任务中提升尤为显著。
*   **精细控制能力**：在“穿线（Threading）”等需要极高精度的任务中，HiFlow 表现远超扩散模型，证明流匹配能更好地捕捉微小运动细节。
*   **连续空间的优势**：实验证明，直接在连续空间操作不仅简化了流程，还通过消除量化误差提升了轨迹的平滑度和准确性。

#### 7. 优点
*   **架构简洁**：摒弃了复杂的 VQ-VAE 分词器，实现了真正的端到端学习。
*   **多尺度建模**：结合了全局轨迹结构（粗尺度）与局部细节（精细尺度），平衡了效率与质量。
*   **通用性强**：无需针对不同机器人设计专门的码本，易于迁移到不同自由度的机器人平台。

#### 8. 不足与局限
*   **推理延迟**：虽然比标准扩散模型快，但由于需要跨尺度迭代（每个尺度还要进行多步 ODE 采样），其推理步数仍多于简单的单步自回归模型。
*   **尺度依赖**：敏感性分析显示，尺度的选择对性能有一定影响，过于激进的下采样可能导致误差累积。
*   **长时程限制**：实验主要集中在短时程动作块（Action Chunking），对于极长序列的全局一致性仍有待进一步验证。

（完）
