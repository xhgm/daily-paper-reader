---
title: "FAST3DIS: Feed-forward Anchored Scene Transformer for 3D Instance Segmentation"
title_zh: FAST3DIS：用于三维实例分割的前馈锚定场景 Transformer
authors: "Changyang Li, Xueqing Huang, Shin-Fang Chng, Huangying Zhan, Qingan Yan, Yi Xu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.25993v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 绕过聚类的端到端3D实例分割Transformer
tldr: 针对现有3D实例分割方法依赖非微分聚类、扩展性差且训练目标不一致的问题，本文提出FAST3DIS。这是一种端到端的Feed-forward架构，基于3D锚点查询的Transformer，通过将3D对象查询投影到多视图特征图中实现高效上下文采样。结合双层正则化策略，该方法在保持高精度的同时，显著提升了内存扩展性和推理速度，实现了视图一致的3D实例分割。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25993v1/fig-001.webp\", \"caption\": \"Fig. 2: Comparisons with IGGT [16]. Left: Inference time comparison (log scale). Right: Qualitative segmentation results. Red circles highlight small objects that IGGT fails to isolate and incorrectly merges into the supporting background.\", \"page\": 11, \"index\": 1, \"width\": 725, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2603.25993v1/fig-002.webp\", \"caption\": \"Fig. 4: Qualitative ablation of our proposed regularization strategies. Left: Impact of explicit feature and spatial regularization. The full model successfully maintains consistent instance identities across different viewpoints (highlighted in green). Without Lgeo and Loverlap (see Section 3.6), the model suffers from severe cross-view association failures (the table and the pillow, highlighted in read) and “query hijacking”, resulting in physically overlapping masks. Right: Impact of the dynamic overlap penalty. A fixed, static penalty forces the network to conservatively predict artificial background gaps (black regions) between adjacent objects. In contrast, our dynamic scheduling ensures more precise and contiguous instance boundaries.\", \"page\": 14, \"index\": 2, \"width\": 710, \"height\": 493}, {\"url\": \"assets/figures/arxiv/2603.25993v1/fig-003.webp\", \"caption\": \"Fig. 3: Qualitative comparison with IGGT. Red circles highlight adjacent objects that IGGT incorrectly merges into a single instance.\", \"page\": 12, \"index\": 3, \"width\": 725, \"height\": 283}]"
motivation: 传统的“提升并聚类”范式存在非微分聚类扩展性差以及表示学习与分割目标脱节的问题。
method: 提出一种基于3D锚点查询的Transformer架构，利用学习到的3D锚点生成器和跨注意力机制，结合对比学习与重叠惩罚进行端到端训练。
result: 在复杂室内3D数据集上，该方法在保证竞争性准确率的前提下，显著优于现有基于聚类的方法的内存效率和推理速度。
conclusion: FAST3DIS通过端到端查询机制取代了传统的后处理聚类，为高效、可扩展的3D场景理解提供了新方案。
---

## 摘要
虽然最近的前馈三维重建模型为场景理解提供了强大的几何基础，但将其扩展到三维实例分割通常依赖于一种脱节的“提升与聚类”（lift-and-cluster）范式。通过不可微聚类对密集的像素级嵌入进行分组，其可扩展性随视图数量增加而变差，且使表示学习与最终的分割目标脱节。在本文中，我们提出了一种用于三维实例分割的前馈锚定场景 Transformer（FAST3DIS），这是一种能够有效绕过事后聚类的端到端方法。我们引入了一种基于三维锚定查询的 Transformer 架构，该架构构建在基础深度骨干网络之上，经过高效适配以学习特定实例的语义，同时保留其零样本几何先验。我们制定了一个学习的三维锚点生成器，并结合锚点采样交叉注意力机制，以实现视图一致的三维实例分割。通过将三维物体查询直接投影到多视图特征图中，我们的方法能够高效地采样上下文。此外，我们引入了一种双层正则化策略，将多视图对比学习与动态调度的空间重叠惩罚相结合，以显式防止查询冲突并确保精确的实例边界。在复杂室内三维数据集上的实验表明，与最先进的基于聚类的方法相比，我们的方法在实现具有竞争力的分割准确率的同时，显著提高了内存可扩展性和推理速度。

## Abstract
While recent feed-forward 3D reconstruction models provide a strong geometric foundation for scene understanding, extending them to 3D instance segmentation typically relies on a disjointed "lift-and-cluster" paradigm. Grouping dense pixel-wise embeddings via non-differentiable clustering scales poorly with the number of views and disconnects representation learning from the final segmentation objective. In this paper, we present a Feed-forward Anchored Scene Transformer for 3D Instance Segmentation (FAST3DIS), an end-to-end approach that effectively bypasses post-hoc clustering. We introduce a 3D-anchored, query-based Transformer architecture built upon a foundational depth backbone, adapted efficiently to learn instance-specific semantics while retaining its zero-shot geometric priors. We formulate a learned 3D anchor generator coupled with an anchor-sampling cross-attention mechanism for view-consistent 3D instance segmentation. By projecting 3D object queries directly into multi-view feature maps, our method samples context efficiently. Furthermore, we introduce a dual-level regularization strategy, that couples multi-view contrastive learning with a dynamically scheduled spatial overlap penalty to explicitly prevent query collisions and ensure precise instance boundaries. Experiments on complex indoor 3D datasets demonstrate that our approach achieves competitive segmentation accuracy with significantly improved memory scalability and inference speed over state-of-the-art clustering-based methods.

---

## 论文详细总结（自动生成）

### FAST3DIS：用于三维实例分割的前馈锚定场景 Transformer 论文总结

#### 1. 核心问题与研究背景
*   **研究动机**：当前的三维场景理解正从多阶段优化转向前馈（Feed-forward）模型。虽然现有的几何重建模型（如 DUSt3R, DA3）能恢复密集几何，但将其扩展到**三维实例分割**时，通常依赖于“提升并聚类”（lift-and-cluster）范式。
*   **核心痛点**：现有的聚类方法（如 HDBSCAN）是非微分的，且计算复杂度随视图数量呈平方级增长，导致内存消耗巨大、推理速度慢，且表示学习与最终分割目标脱节。
*   **论文目标**：提出一种完全端到端、无需事后聚类、具备高扩展性的前馈三维实例分割框架。

#### 2. 方法论
FAST3DIS 采用基于查询（Query-based）的 Transformer 架构，核心思想是将实例分割建模为连续三维空间中的直接集合预测问题。
*   **双路径 LoRA 适配骨干网络**：使用预训练的 **Depth Anything V3 (DA3)** 作为冻结的几何骨干。通过引入 **LoRA（低秩自适应）**，在保留几何先验的同时，提取具有实例辨别力的语义特征。
*   **几何注入像素解码器**：将预测的密集三维点图注入到特征金字塔中，生成三维感知的特征图。
*   **学习的三维锚点生成器**：定义 $N_q$ 个可学习的三维锚点（3D Anchors），作为物体在物理空间中的中心先验。
*   **锚点采样交叉注意力**：
    *   **3D-to-2D 投影**：不进行全局密集注意力计算，而是将三维锚点投影到各个 2D 视图中，仅在投影点附近采样局部特征。
    *   **线性复杂度**：该机制将复杂度从 $O(HW)$ 降低到 $O(1)$，使计算量随视图数量线性增长。
*   **双层正则化策略**：
    *   **多视图对比损失 ($L_{geo}$)**：在特征空间拉近同一实例的特征，推开不同实例。
    *   **动态空间重叠惩罚 ($L_{overlap}$)**：显式惩罚多个查询占用同一物理区域的行为。通过动态调度（先升后降），在训练初期分离查询，后期确保边界精确。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：Aria Synthetic Environments（高度写实的室内模拟环境）。
    *   **测试集（零样本泛化）**：ScanNet V2（真实室内）、ScanNet++（高分辨率激光扫描）、Replica（高保真合成场景）。
*   **对比方法（Baselines）**：
    *   基于几何的聚类：HDBSCAN, Felzenszwalb。
    *   2D 驱动模型：SAM3D, Segment3D。
    *   前馈模型：PanSt3R, IGGT（最主要的对比对象，代表聚类范式）。
*   **评估指标**：平均精度（AP, $AP_{50}$, $AP_{25}$）以及推理延迟和内存占用。

#### 4. 资源与算力
*   **硬件环境**：单台计算节点，配备 **32 核 AMD EPYC 9654 处理器** 和 **单张 NVIDIA H200 GPU (144GB VRAM)**。
*   **训练细节**：在 Aria 数据集中抽取了 40% 的场景（约 4 万个场景）进行训练。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到使用了高性能 H200 显卡以应对大规模数据。

#### 5. 实验数量与充分性
*   **实验规模**：在三个主流三维数据集上进行了定量评估，并针对 2D/3D 效果进行了定性对比。
*   **消融实验**：针对特征正则化、空间重叠惩罚以及动态权重调度进行了详细的消融研究，验证了各组件的必要性。
*   **公平性**：所有对比方法均在相同的分辨率和硬件环境下测试，通过 Sim(3) 轨迹对齐确保了不同坐标系下评估的客观性。实验涵盖了从 10 视图到 350 视图的扩展性测试，充分展示了方法的性能边界。

#### 6. 主要结论与发现
*   **效率飞跃**：相比基于聚类的 IGGT，FAST3DIS 在 30 视图下实现了 **115 倍**的加速，在 50 视图下加速达 **250 倍**，且完全避免了 GPU 聚类时的显存溢出（OOM）问题。
*   **分割质量**：在 ScanNet V2 和 Replica 上表现优异，尤其在**小物体识别**和**相邻物体分离**方面显著优于聚类方法。
*   **扩展性**：能够单次前馈处理多达 350 张图像，证明了其在大型复杂场景中的实用性。

#### 7. 优点与亮点
*   **端到端架构**：彻底摒弃了不可微的后处理聚类，使模型能够直接优化分割目标。
*   **高效注意力机制**：通过 3D-to-2D 投影采样，解决了多视图 Transformer 处理高分辨率图像时的计算瓶颈。
*   **动态正则化**：创新的动态重叠惩罚解决了类无关（Class-agnostic）分割中容易出现的“查询劫持”和边界模糊问题。

#### 8. 不足与局限
*   **查询数量限制**：模型预设了固定数量的查询（$N_q=80$），在物体极度密集的场景（如 ScanNet++，常有 100-200 个物体）中会出现漏检。
*   **依赖几何骨干**：分割精度高度依赖于底层 DA3 模型的深度估计质量，若几何恢复失败，分割也会受限。
*   **任务范围**：目前仅专注于类无关的实例分割，尚未集成语义类别预测（虽然架构上易于扩展）。

（完）
