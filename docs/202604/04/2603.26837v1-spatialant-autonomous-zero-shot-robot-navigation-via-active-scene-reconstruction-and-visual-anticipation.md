---
title: "SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation"
title_zh: SpatialAnt：通过主动场景重建与视觉预测实现的自主零样本机器人导航
authors: "Jiwen Zhang, Xiangyu Shi, Siyuan Wang, Zerui Li, Zhongyu Wei, Qi Wu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26837v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 通过主动场景重建和多模态大模型实现零样本机器人导航
tldr: SpatialAnt是一个零样本机器人导航框架，旨在解决自主构建的场景重建不完整且多噪点的问题。它通过物理接地策略恢复单目重建的绝对尺度，并引入视觉预测机制，利用噪声点云渲染未来观测以进行反事实推理和路径剪枝。该方法在R2R-CE和RxR-CE基准测试中表现优异，并在真实机器人部署中验证了其有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26837v1/fig-001.webp\", \"caption\": \"Table 1. Comparison in continuous environments on R2R-CE and RxR-CE Val-Unseen splits. The best supervised results are highlighted in bold, while the best zero-shot results are underlined. “Pre-Exp” denotes whether the zero-shot agent adopts the preexploration based navigation settings.\", \"page\": 7, \"index\": 1, \"width\": 987, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.26837v1/fig-002.webp\", \"caption\": \"Figure 2. Overview of our proposed SpatialAnt framework. We firstly let the agent to pre-explore the environment, gathering the coordinates of navigable places. Then, we use these information to plan visibility-aware tours to collect geometrically coherent RGB videos and IMU traces for subsequent regional point cloud reconstruction. The reconstructed point clouds are then aligned with physical scale and merged into a global point cloud. Afterall, this global point cloud provides extra global context by a novel visual anticipation mechanism for MLLM to conduct sub-path based high-level reasoning.\", \"page\": 4, \"index\": 2, \"width\": 1013, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2603.26837v1/fig-003.webp\", \"caption\": \"Figure 4. Representative cases in real environments. We show frames of the agent egocentric front view, panoramic observations (from right to left), and candidate actions on the spatial map together with our anticipated views.\", \"page\": 9, \"index\": 3, \"width\": 492, \"height\": 645}]"
motivation: 现有的零样本导航方法依赖高质量的人工场景重建，而机器人自主生成的重建往往存在噪声和不完整性，限制了实际应用。
method: 提出了物理接地策略以恢复度量尺度，并结合视觉预测机制利用噪声点云渲染未来视图，辅助路径决策。
result: "在R2R-CE和RxR-CE上分别达到66%和50.8%的成功率，且在真实环境部署中实现了52%的成功率。"
conclusion: SpatialAnt通过弥补不完美自建重建与稳健执行之间的差距，显著提升了机器人在未知环境中的零样本导航能力。
---

## 摘要
视觉语言导航（VLN）近期受益于多模态大语言模型（MLLM），实现了零样本导航。虽然近期基于探索的零样本方法通过利用全局场景先验取得了显著成果，但它们依赖于高质量的人工场景重建，这在现实世界的机器人部署中并不切实际。当遇到陌生环境时，机器人应通过预探索建立自己的先验。然而，这些自主构建的重建结果不可避免地存在不完整和噪声问题，严重降低了依赖高质量场景重建方法的性能。为解决这些问题，我们提出了 SpatialAnt，这是一个旨在弥合不完美的自主重建与鲁棒执行之间差距的零样本导航框架。SpatialAnt 引入了一种物理落地策略，用于恢复基于单目重建的绝对度量尺度。此外，我们提出了一种新颖的视觉预测机制，而不是将带有噪声的自主重建场景视为绝对的空间参考。该机制利用含噪点云渲染未来观测结果，使智能体能够进行反事实推理，并剪除与人类指令相悖的路径。在模拟和真实环境中的广泛实验表明，SpatialAnt 显著优于现有的零样本方法。我们在 R2R-CE 基准测试中实现了 66% 的成功率（SR），在 RxR-CE 基准测试中实现了 50.8% 的成功率。在 Hello Robot 上的物理部署进一步证实了我们框架的效率和有效性，在具有挑战性的现实场景中实现了 52% 的成功率。

## Abstract
Vision-and-Language Navigation (VLN) has recently benefited from Multimodal Large Language Models (MLLMs), enabling zero-shot navigation. While recent exploration-based zero-shot methods have shown promising results by leveraging global scene priors, they rely on high-quality human-crafted scene reconstructions, which are impractical for real-world robot deployment. When encountering an unseen environment, a robot should build its own priors through pre-exploration. However, these self-built reconstructions are inevitably incomplete and noisy, which severely degrade methods that depend on high-quality scene reconstructions. To address these issues, we propose SpatialAnt, a zero-shot navigation framework designed to bridge the gap between imperfect self-reconstructions and robust execution. SpatialAnt introduces a physical grounding strategy to recover the absolute metric scale for monocular-based reconstructions. Furthermore, rather than treating the noisy self-reconstructed scenes as absolute spatial references, we propose a novel visual anticipation mechanism. This mechanism leverages the noisy point clouds to render future observations, enabling the agent to perform counterfactual reasoning and prune paths that contradict human instructions. Extensive experiments in both simulated and real-world environments demonstrate that SpatialAnt significantly outperforms existing zero-shot methods. We achieve a 66% Success Rate (SR) on R2R-CE and 50.8% SR on RxR-CE benchmarks. Physical deployment on a Hello Robot further confirms the efficiency and efficacy of our framework, achieving a 52% SR in challenging real-world settings.

---

## 论文详细总结（自动生成）

### 论文总结：SpatialAnt —— 基于主动场景重建与视觉预测的自主零样本机器人导航

#### 1. 论文的核心问题与整体含义（研究动机和背景）
现有的零样本视觉语言导航（VLN）方法（如 SpatialNav）通常假设机器人可以获得高质量、人工标注的 3D 场景重建（如 Matterport3D 数据集中的完美点云）。然而，在现实部署中，机器人必须通过单目相机自主探索并构建地图。这带来了两个核心挑战：
*   **尺度歧义（Scale Ambiguity）：** 单目 RGB 序列重建的场景缺乏绝对物理尺度（米），无法直接用于导航。
*   **重建噪声与不完整性：** 机器人视角受限且存在遮挡，导致自建点云充满噪声和空洞，直接作为空间参考会导致导航失败。
**SpatialAnt** 的核心目标是弥合“不完美的自建重建”与“稳健的导航执行”之间的差距，使机器人能在未知环境中自主探索、建模并执行复杂指令。

#### 2. 论文提出的方法论
SpatialAnt 提出了一个三阶段的闭环框架：
*   **主动场景发现（Active Scene Discovery）：**
    *   利用前沿探索（Frontier-based DFS）确定可行驶区域。
    *   通过“能见度感知路径规划”（Visibility-Aware Path Planning），利用 2-opt 算法规划出覆盖率高、视角重叠度大的扫描路径，以收集高质量的 RGB 视频和 IMU 数据。
*   **物理落地场景重建（Physically-grounded Scene Reconstruction）：**
    *   使用神经 SLAM（SLAM3R）从视频中恢复局部点云。
    *   **物理接地策略：** 将重建点云渲染的深度图与 Depth-Anything-2 预测的度量深度进行对比，计算缩放因子 $\delta$，从而恢复绝对物理尺度。
*   **预测引导导航（Anticipation-Guided Navigation）：**
    *   **视觉预测机制：** 不再将噪声点云视为绝对坐标参考，而是利用高斯泼溅（Gaussian Splatting）从点云中渲染出候选路径终点的“未来视图”。
    *   **反事实推理：** 将当前观测、历史路径、拓扑地图以及“预测的未来视图”输入多模态大模型（MLLM，如 GPT-5.1）。模型通过对比“如果我走这条路会看到什么”与指令的语义一致性，剪除错误路径。

#### 3. 实验设计
*   **数据集/场景：** 
    *   **仿真环境：** 在 Habitat 模拟器中使用 R2R-CE 和 RxR-CE 数据集（连续环境）。
    *   **真实世界：** 使用 Hello Robot (Stretch) 移动协作机器人，在包含 25 条跨房间路径的真实室内场景中测试。
*   **Benchmark 与对比方法：**
    *   **监督学习方法：** NavFoM, Efficient-VLN。
    *   **零样本方法：** Open-Nav, Smartway, STRIDER（无预探索）；VLN-Zero, SpatialNav（有预探索）。
    *   **对比重点：** 重点对比了在“自建噪声点云”而非“理想点云”条件下的性能表现。

#### 4. 资源与算力
*   **硬件：** 真实机器人部署使用了单块 **NVIDIA RTX A6000 GPU** 进行在线推理。
*   **模型：** 默认使用 **GPT-5.1-2025-11-13** 作为 MLLM 后端，同时也测试了 Qwen3.5-Plus 和 Gemini-2.5-Pro。
*   **耗时：** 场景重建在 A6000 上约需 200 秒；每步全局感知（投影地图和渲染预测视图）仅需 0.57 秒，具有较高的实时性。

#### 5. 实验数量与充分性
*   **实验规模：** 在 R2R-CE 采样了 100 个情节，RxR-CE 采样了 195 个情节（符合 MLLM 类论文因 API 成本而采用的常规采样规模）。
*   **消融实验：** 针对视觉预测机制（VA）、预测视野（HFOV）、不同 MLLM 主干模型、以及“人工点云 vs. 自建点云”的鲁棒性进行了详细对比。
*   **充分性评价：** 实验设计较为全面，特别是通过对比 SpatialNav 在噪声环境下的性能大幅下降，有力证明了 SpatialAnt 视觉预测机制在处理不完美重建时的优越性，实验结果客观且具有说服力。

#### 6. 论文的主要结论与发现
*   **性能领先：** 在 R2R-CE 上达到 66% SR，在 RxR-CE 上达到 50.8% SR，显著超过之前的零样本 SOTA。
*   **噪声鲁棒性：** 相比于依赖精确地图的方法，SpatialAnt 在面对含噪自建地图时性能下降极小（SR 仅下降 2-3%），证明了“视觉预测”比“绝对坐标参考”更适合现实机器人。
*   **模型可扩展性：** 随着 MLLM 推理能力的增强（如从 Qwen 升级到 GPT-5），SpatialAnt 的导航表现随之提升，显示出良好的算法潜力。

#### 7. 优点
*   **闭环系统：** 实现了从原始 RGB 输入、主动探索、尺度恢复到最终执行的完整闭环，不依赖外部地图。
*   **创新的预测机制：** 巧妙地将不完美的 3D 几何转化为 MLLM 易于理解的“未来视觉证据”，降低了幻觉风险。
*   **物理尺度恢复：** 解决了单目 SLAM 在导航应用中的核心痛点（尺度缺失）。

#### 8. 不足与局限
*   **预探索成本：** 导航前需要约 10 分钟的预探索和数分钟的重建时间，对于极度追求即时响应的任务可能受限。
*   **依赖高性能 API：** 核心推理依赖于顶尖 MLLM（如 GPT-5），在无网络或对隐私敏感的离线部署场景中存在挑战。
*   **动态环境：** 论文主要针对静态场景重建，未深入讨论环境中存在移动物体（如行人）时对重建和预测的影响。

（完）
