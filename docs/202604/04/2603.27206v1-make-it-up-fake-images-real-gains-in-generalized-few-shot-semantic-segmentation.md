---
title: "Make It Up: Fake Images, Real Gains in Generalized Few-shot Semantic Segmentation"
title_zh: Make It Up：广义小样本语义分割中伪图像带来的真实增益
authors: "Guohuan Xie, Xin He, Dingying Fan, Le Zhang, Ming-Ming Cheng, Yun Liu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27206v1"
tags: ["query:q14"]
score: 9.0
evidence: 使用扩散模型的广义少样本语义分割
tldr: 针对广义少样本语义分割（GFSS）中新类样本覆盖不足及伪标签质量低的问题，本文提出Syn4Seg框架。该框架利用扩散模型生成多样化图像，通过去重提示词库扩大覆盖范围，并采用两阶段伪标签精炼技术（结合全局支持信息与局部统计）及受限SAM边界优化，显著提升了合成数据的监督质量。实验证明，该方法在PASCAL-5i和COCO-20i上均取得显著提升，为GFSS提供了可扩展的合成数据路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27206v1/fig-001.webp\", \"caption\": \"Fig. 1. Pipelines of classical GFSS works and our Syn4Seg framework. (a) Classical GFSS methods follow three stages: base class learning, novel class registration, and evaluation, where novel classes rely on limited manual annotations, resulting in low diversity and constrained performance. (b) Our Syn4Seg framework synthesizes diverse, realistic novel class images and pseudo labels guided by support annotations. These synthesized samples, together with base data, are used to train a semantic segmentation model, enabling robust recognition of both base and novel classes during evaluation.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2603.27206v1/fig-002.webp\", \"caption\": \"TABLE I COMPARISON WITH STATE-OF-THE-ART METHODS ON PASCAL-5i AND COCO-20i UNDER THE GFSS SETTING. BOLD AND UNDERLINED INDICATE THE BEST AND SECOND BEST METHODS. MEAN DENOTES THE AVERAGE OF THE MIOU OVER BASE AND NOVEL CLASSES, WHILE HARMONIC REPRESENTS THEIR HARMONIC MEAN. ALL METHODS USE A RESNET-50 BACKBONE AND FOLLOW THE SAME TRAINING/TEST SPLIT.\", \"page\": 6, \"index\": 2, \"width\": 1079, \"height\": 635}, {\"url\": \"assets/figures/arxiv/2603.27206v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative comparison between images generated directly using classname prompts (a) and those generated with our HDIG method (b).\", \"page\": 4, \"index\": 3, \"width\": 520, \"height\": 287}]"
motivation: GFSS受限于新类样本的多样性，且利用扩散模型生成的合成图像往往存在覆盖不足和伪标签噪声大的问题。
method: 提出Syn4Seg框架，通过构建去重提示词库、两阶段支持引导的伪标签精炼以及基于SAM的边界优化来提升合成数据的质量。
result: 在PASCAL-5i和COCO-20i数据集的1-shot和5-shot设置下，该方法均实现了性能的持续提升。
conclusion: 通过生成多样化图像并结合精确的伪标签技术，合成数据能够有效解决GFSS中的数据稀缺问题。
---

## 摘要
广义小样本语义分割（GFSS）从根本上受限于标注稀缺情况下新类别外观的覆盖范围。虽然扩散模型可以大规模合成新类别图像，但在掩码不可用或不可靠时，覆盖范围不足和噪声监督往往会阻碍实际增益。我们提出了 Syn4Seg，这是一个生成增强的 GFSS 框架，旨在扩大新类别覆盖范围的同时提高伪标签质量。Syn4Seg 首先通过为每个新类别构建嵌入去重的提示库来最大化提示空间覆盖，从而生成多样化且类别一致的合成图像。随后，它通过两阶段细化进行支持引导的伪标签估计：i) 过滤低一致性区域以获得高精度种子点；ii) 使用结合了全局（支持集）和局部（图像）统计信息的图像自适应原型对不确定像素进行重新标注。最后，我们使用基于受限 SAM 的更新仅细化边界带和未标记像素，以在不覆盖高置信度内部区域的情况下提高轮廓保真度。在 PASCAL-$5^i$ 和 COCO-$20^i$ 上的广泛实验表明，在 1-shot 和 5-shot 设置下均有持续改进，突显了合成数据作为实现具有可靠掩码和精确边界的 GFSS 的一条可扩展路径。

## Abstract
Generalized few-shot semantic segmentation (GFSS) is fundamentally limited by the coverage of novel-class appearances under scarce annotations. While diffusion models can synthesize novel-class images at scale, practical gains are often hindered by insufficient coverage and noisy supervision when masks are unavailable or unreliable. We propose Syn4Seg, a generation-enhanced GFSS framework designed to expand novel-class coverage while improving pseudo-label quality. Syn4Seg first maximizes prompt-space coverage by constructing an embedding-deduplicated prompt bank for each novel class, yielding diverse yet class-consistent synthetic images. It then performs support-guided pseudo-label estimation via a two-stage refinement that i) filters low-consistency regions to obtain high-precision seeds and ii) relabels uncertain pixels with image-adaptive prototypes that combine global (support) and local (image) statistics. Finally, we refine only boundary-band and unlabeled pixels using a constrained SAM-based update to improve contour fidelity without overwriting high-confidence interiors. Extensive experiments on PASCAL-$5^i$ and COCO-$20^i$ demonstrate consistent improvements in both 1-shot and 5-shot settings, highlighting synthetic data as a scalable path for GFSS with reliable masks and precise boundaries.

---

## 论文详细总结（自动生成）

以下是对论文《Make It Up: Fake Images, Real Gains in Generalized Few-shot Semantic Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：广义小样本语义分割（GFSS）旨在同时识别基类（Base）和仅有少量标注的新类（Novel）。其主要瓶颈在于新类标注稀缺，导致模型对新类外观的覆盖范围不足，泛化能力差。
*   **研究动机**：虽然扩散模型（Diffusion Models）能合成新类图像，但直接应用面临三大挑战：
    1.  **多样性不足**：简单提示词生成的图像往往高度冗余。
    2.  **伪标签噪声**：预训练模型生成的初始掩码存在语义错误和对齐问题。
    3.  **边界粗糙**：分割模型生成的掩码在物体边缘处精度极低，影响细粒度学习。

### 2. 方法论（Syn4Seg 框架）
论文提出了 **Syn4Seg** 框架，通过“生成-增强-精炼”的流程提升 GFSS 性能：
*   **高质量多样化图像生成 (HDIG)**：
    *   利用大语言模型（DeepSeek-V3）迭代生成候选提示词。
    *   **嵌入去重**：使用 Stable Diffusion 3.5 的文本编码器计算提示词嵌入的余弦相似度，过滤掉相似度高于阈值 $\lambda$ 的提示词，确保生成图像在语义空间上的广泛覆盖。
*   **自适应伪标签增强 (APE)**：
    *   **自适应过滤 (APF)**：利用支持集的类别原型（Prototype），计算合成图像中各区域的特征一致性，剔除低置信度的噪声区域。
    *   **自适应重标注 (APR)**：结合全局支持原型和图像局部统计信息，构建自适应原型，对过滤后的“空白”像素进行重新分类，提高语义可靠性。
*   **基于 SAM 的边界精炼 (SABR)**：
    *   利用 Segment Anything Model (SAM) 强大的定位能力。
    *   **受限更新**：仅在物体边界带和未标记区域内利用 SAM 进行精炼，防止 SAM 误改高置信度的物体内部区域，从而获得锐利且对齐的轮廓。

### 3. 实验设计
*   **数据集**：在 GFSS 标准基准 **PASCAL-5i** 和 **COCO-20i** 上进行评估。
*   **Benchmark 与对比方法**：
    *   对比了包括 CAPL、BAM、DIaM、POP、AlignDiff、VP 和 BCM 在内的多种最先进（SOTA）方法。
    *   评估指标：平均交并比 (mIoU) 和基类/新类的调和平均值 (hIoU)。
*   **设置**：涵盖了 1-shot 和 5-shot 两种极具挑战性的设置。

### 4. 资源与算力
*   **模型组件**：使用了 Stable Diffusion 3.5-Large（图像生成）、SAM-ViT-H（边界精炼）、DeepSeek-V3（提示词生成）以及 Mask2Former（下游分割模型）。
*   **算力说明**：文中提到计算资源由南开大学超算中心（NKSC）支持。
*   **训练细节**：Mask2Former 在 PASCAL-5i 上训练 100 轮，在 COCO-20i 上训练 20 轮；Batch Size 为 16；优化器为 AdamW。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号、数量以及总的训练/生成耗时。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集的 4 个 Split 上均进行了实验，并取平均值，符合学术规范。
*   **消融实验**：非常充分。分别验证了 HDIG、APF、APR 和 SABR 各个模块的增益，并对超参数 $\lambda$（多样性阈值）、$\beta$（原型融合系数）和 $\delta$（边界带宽度）进行了详细的敏感性分析。
*   **客观性**：通过 t-SNE 可视化证明了 HDIG 生成图像的特征分布确实比普通提示词更广，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：Syn4Seg 在所有设置下均显著超过 SOTA。例如在 PASCAL-5i 的 5-shot 设置下，mIoU 提升了 2.20%，hIoU 提升了 3.28%。
*   **合成数据的价值**：证明了只要能解决“多样性”和“标签质量”问题，合成图像可以作为 GFSS 的有效数据增强手段。
*   **边界的重要性**：SABR 模块的加入证明了精确的物体轮廓对于提升分割模型（尤其是 Mask2Former 这种基于 Query 的模型）至关重要。

### 7. 优点与亮点
*   **系统性解决噪声**：不仅关注图像生成，更通过 APE 和 SABR 形成了一套完整的伪标签质量控制闭环。
*   **提示词工程创新**：引入嵌入空间去重机制，从源头上解决了生成图像同质化的问题。
*   **即插即用**：该框架生成的合成数据集可以用于训练任何主流的语义分割架构。

### 8. 不足与局限
*   **计算开销**：由于涉及大规模图像生成、多阶段伪标签精炼以及 SAM 推理，整个离线数据准备过程可能非常耗时且耗费存储空间。
*   **模型依赖**：性能高度依赖于基础模型（如 SD 3.5 和 SAM）的质量，若基础模型对某些冷门类别理解不足，合成效果会受限。
*   **缺乏细粒度控制**：目前的提示词生成虽然保证了类别多样性，但尚未显式控制物体的姿态、遮挡程度或复杂的场景上下文。

（完）
