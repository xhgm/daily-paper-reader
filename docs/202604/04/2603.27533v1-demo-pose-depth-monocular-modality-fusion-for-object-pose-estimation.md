---
title: "Demo-Pose: Depth-Monocular Modality Fusion For Object Pose Estimation"
title_zh: DeMo-Pose：用于物体位姿估计的深度-单目模态融合
authors: "Rachit Agarwal, Abhishek Joshi, Sathish Chalasani, Woo Jin Kim"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27533v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 基于RGB-D输入的9自由度物体姿态估计与定位
tldr: 本研究针对类别级9-DoF物体位姿估计，提出了DeMo-Pose架构。该方法通过创新的多模态融合策略，将RGB图像的单目语义特征与基于深度的图卷积表示相结合，解决了现有方法忽视语义信息或融合不佳的问题。此外，引入了网格点损失（MPL）以增强几何推理能力。在REAL275基准测试中，DeMo-Pose在保持实时推理的同时，显著超越了现有SOTA模型，为机器人和AR/VR等应用提供了更鲁棒的位姿估计方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27533v1/fig-001.webp\", \"caption\": \"Fig. 3. (a) Comparison of predictions across video frames: GPV-Pose exhibits temporal instability for the laptop category (top row), while our DeMo-Pose fusion yields stable predictions (bottom row). (b) We represent predictions with green colored boxes and ground truth with black boxes, our method (bottom row) produces tighter and more accurate boxes for laptop and mug compared to GPV-Pose (top row).\", \"page\": 3, \"index\": 1, \"width\": 1056, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2603.27533v1/fig-002.webp\", \"caption\": \"Fig. 2. Fusion module architecture to leverage RGB features obtained from Monocular detection model and fuse with Depth-based GCN features. In inference, we achieve real-time performance, suitable for device deployment. We provide more details in proposed method sec.2\", \"page\": 2, \"index\": 2, \"width\": 1056, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2603.27533v1/fig-003.webp\", \"caption\": \"Table 1. Comparison with state-of-the-art methods on the REAL275 dataset. Bold indicates superior results. ↑ denotes higher is better for each metric.\", \"page\": 4, \"index\": 3, \"width\": 1020, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2603.27533v1/fig-004.webp\", \"caption\": \"Fig. 1. Impact of inaccurate rotation, scale, and translation when overlaying a virtual keyboard template on a physical keyboard in MR. The bottom image shows correct alignment using pose estimation for improved user experience, as seen through the HMD [2]\", \"page\": 1, \"index\": 4, \"width\": 510, \"height\": 387}]"
motivation: 现有方法在处理RGB-D输入时，往往忽视RGB语义线索或无法有效对齐跨模态特征，限制了类别级位姿估计的精度。
method: 提出一种融合单目语义特征与深度图卷积表示的混合架构，并引入无需推理开销的网格点损失（MPL）来优化几何推理。
result: "在REAL275基准上，该方法在3D IoU和位姿精度上分别比GPV-Pose提升了3.2%和11.1%，并实现了实时推理。"
conclusion: 通过深度-RGB融合与几何感知学习，DeMo-Pose显著提升了类别级3D位姿估计的性能和鲁棒性。
---

## 摘要
物体位姿估计是 3D 视觉中的一项基础任务，在机器人、AR/VR 和场景理解中具有广泛应用。我们解决了基于 RGB-D 输入的类别级 9 自由度（9-DoF）位姿估计（6D 位姿 + 3D 尺寸）挑战，且在推理过程中不依赖 CAD 模型。现有的仅深度方法虽然取得了显著成果，但忽略了来自 RGB 的语义线索；而许多 RGB-D 融合模型由于跨模态融合效果欠佳，未能将语义 RGB 线索与 3D 几何表示对齐，导致性能受限。我们提出了 DeMo-Pose，这是一种混合架构，通过一种新颖的多模态融合策略，将单目语义特征与基于深度的图卷积表示相结合。为了进一步提升几何推理能力，我们引入了一种新颖的网格-点损失（Mesh-Point Loss, MPL），该损失在训练期间利用网格结构，且不增加推理开销。我们的方法实现了实时推理，并在各类物体上显著优于现有最先进方法，在 REAL275 基准测试中，其 3D IoU 和位姿准确度分别比强基准模型 GPV-Pose 高出 3.2% 和 11.1%。实验结果突显了深度-RGB 融合和几何感知学习的有效性，为现实应用提供了鲁棒的类别级 3D 位姿估计。

## Abstract
Object pose estimation is a fundamental task in 3D vision with applications in robotics, AR/VR, and scene understanding. We address the challenge of category-level 9-DoF pose estimation (6D pose + 3Dsize) from RGB-D input, without relying on CAD models during inference. Existing depth-only methods achieve strong results but ignore semantic cues from RGB, while many RGB-D fusion models underperform due to suboptimal cross-modal fusion that fails to align semantic RGB cues with 3D geometric representations. We propose DeMo-Pose, a hybrid architecture that fuses monocular semantic features with depth-based graph convolutional representations via a novel multimodal fusion strategy. To further improve geometric reasoning, we introduce a novel Mesh-Point Loss (MPL) that leverages mesh structure during training without adding inference overhead. Our approach achieves real-time inference and significantly improves over state-of-the-art methods across object categories, outperforming the strong GPV-Pose baseline by 3.2\% on 3D IoU and 11.1\% on pose accuracy on the REAL275 benchmark. The results highlight the effectiveness of depth-RGB fusion and geometry-aware learning, enabling robust category-level 3D pose estimation for real-world applications.

---

## 论文详细总结（自动生成）

### 论文总结：DeMo-Pose —— 用于物体位姿估计的深度-单目模态融合

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**类别级 9-DoF 物体位姿估计**（包含 3D 旋转、平移和绝对尺寸），且要求在推理时不依赖于特定物体的 CAD 模型。
*   **研究动机**：现有的方法主要分为“仅深度”和“RGB-D 融合”两类。仅深度方法虽然几何推理强，但忽略了 RGB 图像中的语义线索（如遮挡关系、背景上下文）；而现有的 RGB-D 融合模型往往因为跨模态特征对齐不佳，导致性能反而不如纯深度模型。
*   **核心目标**：提出一种能够有效融合单目 RGB 语义特征与深度几何特征的混合架构，以提升在复杂现实场景下的位姿估计鲁棒性和精度。

#### 2. 论文提出的方法论
DeMo-Pose 采用了一种混合架构，主要包含以下核心组件：
*   **单目检测分支（Monocular Detection）**：
    *   使用 GhostNet 作为轻量化主干网络，配合路径聚合网络（PAN）提取多尺度特征。
    *   预测 3D 边界框的 8 个投影 2D 关键点、物体类别及相对尺寸。
    *   该分支旨在学习丰富的语义和空间线索，为后续融合提供“单目特征图”。
*   **深度分支与特征融合**：
    *   基于 3D 图卷积（3DGC）处理从深度图反投影得到的点云（采样 1028 个点）。
    *   **特征对齐**：利用点云在图像平面上的投影坐标，通过**双线性插值**从单目特征图中采样对应的语义特征。
    *   **融合策略**：将采样后的语义特征与 3DGC 提取的几何特征进行**拼接（Concatenation）**，输入回归头预测位姿参数。
*   **几何感知损失函数（Mesh-Point Loss, MPL）**：
    *   在训练阶段，从物体的 ground-truth 网格中采样顶点。
    *   **公式含义**：$L_{MPL} = \frac{1}{V} \sum_{i=1}^{V} \|R \cdot M^{GT}_i - M^{pred}_i\|^2$。
    *   通过直接监督 3D 点分布而非旋转参数，MPL 能够更好地处理物体的对称性问题，且在推理阶段不产生额外开销。

#### 3. 实验设计
*   **数据集**：使用类别级位姿估计的标准基准数据集 **REAL275**。该数据集包含 13 个真实场景，涵盖 6 类物体（瓶子、碗、相机、罐子、笔记本电脑、杯子）。
*   **基准（Benchmark）与对比方法**：
    *   对比了包括 NOCS、SPD、SGPA、DualPoseNet 以及强基准 **GPV-Pose**（纯深度 SOTA）在内的多种方法。
    *   评价指标包括 3D IoU（25/50/75 阈值）和位姿准确度（5°2cm, 5°5cm, 10°5cm, 10°10cm）。

#### 4. 资源与算力
*   **硬件与框架**：使用 PyTorch 实现。
*   **具体算力说明**：文中**未明确给出**具体的 GPU 型号、数量或总训练时长。但作者提到为了公平对比，采用了与 GPV-Pose 相同的硬件设置，并强调了系统在嵌入式设备上的潜力（如使用 GhostNet）。
*   **推理速度**：实现了约 **17.86 FPS** 的实时推理速度。

#### 5. 实验数量与充分性
*   **实验规模**：在 REAL275 的测试集（约 2.7k 图像）上进行了全面评估。
*   **消融实验**：
    1.  **MPL 损失函数的有效性**：对比了加入 MPL 前后的性能提升。
    2.  **融合策略对比**：对比了拼接（Concatenation）、MLP 融合及注意力机制融合的效果，证明了简单的拼接在当前架构下最有效。
*   **充分性评价**：实验设计较为客观，涵盖了定量指标对比、定性可视化分析（如预测的稳定性对比）以及关键模块的消融研究，足以支撑其技术主张。

#### 6. 主要结论与发现
*   **性能提升**：DeMo-Pose 在 REAL275 上显著超越了 GPV-Pose，其中 3D75 IoU 提升了 **3.2%**，5°5cm 位姿精度提升了 **11.1%**。
*   **语义互补性**：实验证明 RGB 语义线索能有效弥补深度数据在噪声、遮挡和采样不足时的缺陷，提高预测的稳定性（减少视频中的闪烁）。
*   **几何正则化**：引入 MPL 损失能显著增强模型对物体几何结构的感知，尤其在处理对称物体时表现更优。

#### 7. 优点
*   **高效融合**：通过双线性插值采样实现了 2D 语义与 3D 几何特征的精确对齐。
*   **实时性**：在提升精度的同时保持了接近 18 FPS 的速度，适合 AR/VR 和机器人实时应用。
*   **训练优化**：MPL 损失在不增加推理负担的前提下，通过利用网格结构信息提升了模型性能。

#### 8. 不足与局限
*   **依赖预分割**：系统仍依赖 Mask-RCNN 生成物体掩码，分割质量会直接影响后续位姿估计的精度。
*   **算力细节缺失**：未披露具体的训练资源消耗，不便于其他研究者评估训练成本。
*   **泛化范围**：虽然在 6 类物体上表现良好，但对于更广泛的开放集（Open-vocabulary）或极端形变物体的泛化能力尚待验证。

（完）
