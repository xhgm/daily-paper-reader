---
title: "Let Geometry GUIDE: Layer-wise Unrolling of Geometric Priors in Multimodal LLMs"
title_zh: 让几何来引导：多模态大语言模型中几何先验的逐层展开
authors: "Chongyu Wang, Ting Huang, Chunyu Sun, Xinyu Ning, Di Wang, Hao Tang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05695v1"
tags: ["query:q11"]
score: 9.0
evidence: 多模态大语言模型的渐进式几何先验注入框架
tldr: 针对多模态大模型在物理空间感知上的局限，本文提出GUIDE框架。该框架通过在几何编码器中进行多级采样以捕获多粒度特征，并将其逐层注入MLLM的早期层，结合上下文感知门控机制实现2D到3D转换的渐进式学习。实验证明，GUIDE在复杂空间推理和感知任务中表现优异，为集成3D几何先验提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05695v1/fig-001.webp\", \"caption\": \"Figure 2: Overall architecture of the proposed GUIDE framework.GUIDE employs progressive unrolling to inject multigranularity features from the 3D geometric encoder (left) into early LLM layers (middle) via a context-aware gate (right), enabling the model to gradually learn 2D-to-3D transitions and fetch geometric details on demand.\", \"page\": 3, \"index\": 1, \"width\": 919, \"height\": 655}, {\"url\": \"assets/figures/arxiv/2604.05695v1/fig-002.webp\", \"caption\": \"Table 1: The performance on VSI-Bench. We integrate recent general and spatial-enhanced models for comprehensive comparison. * denotes training using the S1+S2 dataset configuration from VG-LLM.\", \"page\": 6, \"index\": 2, \"width\": 1048, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.05695v1/fig-003.webp\", \"caption\": \"Table 2: Comparison of different models on VSI-Bench [41], MMSI-Bench [43], ViewSpatial [24], and CV-Bench [54].\", \"page\": 6, \"index\": 3, \"width\": 506, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2604.05695v1/fig-004.webp\", \"caption\": \"Table 4: The performance on Scan2Cap.\", \"page\": 7, \"index\": 4, \"width\": 498, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2604.05695v1/fig-005.webp\", \"caption\": \"Table 3: The performance on ScanRefer.\", \"page\": 7, \"index\": 5, \"width\": 498, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.05695v1/fig-006.webp\", \"caption\": \"Table 5: Comprehensive ablation study of injection layers and context-aware gating components on VSI-Bench. 𝐺𝑠𝑒𝑚 and 𝐺𝑔𝑙𝑜 denote the token-wise semantic gate and layer-wise global gate, respectively. Layers = 0 corresponds to the baseline (Input-Level Fusion).\", \"page\": 8, \"index\": 6, \"width\": 1048, \"height\": 404}]"
motivation: 现有几何感知模型多采用单层特征提取和输入级融合，导致局部几何细节丢失及早期层语义不匹配。
method: 提出GUIDE框架，通过多级采样捕获多粒度几何特征，并利用上下文感知门控将其逐层注入MLLM早期层进行对齐融合。
result: 实验结果显示，GUIDE在多项复杂空间推理和感知任务上显著优于现有基准模型。
conclusion: 该研究通过层级化展开几何先验，成功提升了多模态大模型的物理空间意识，为3D几何信息集成开辟了新路径。
---

## 摘要
多模态大语言模型（MLLMs）在 2D 视觉任务中取得了显著进展，但在处理现实世界视觉流时，其物理空间感知能力仍然有限。最近，能够隐式提取几何先验的前馈几何基础模型为解决这一问题提供了新途径。然而，现有的几何感知 MLLMs 主要受限于单深层提取和输入级融合的范式。这种扁平化的融合导致了局部几何细节的丢失，并在早期层中引起语义不匹配。为了打破这一瓶颈，我们提出了 GUIDE（多模态大模型早期层内的几何展开），这是一种渐进式几何先验注入框架。GUIDE 在几何编码器内进行多级采样，全面捕捉从局部边缘到全局拓扑的多粒度特征。随后，我们将这些多级几何先验与 MLLM 的早期层进行逐步的严格对齐与融合。基于多粒度几何信息的注入，该设计引导模型逐步学习从 2D 到 3D 的过渡过程。此外，我们引入了上下文感知门控机制，使模型能够根据当前语义获取所需的空间线索，从而最大化空间先验的利用效率，并有效抑制冗余的几何噪声。大量实验表明，GUIDE 在多个复杂的空间推理和感知任务上显著优于现有基准，为将 3D 几何先验集成到大模型中建立了一种新范式。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable progress in 2D visual tasks but still exhibit limited physical spatial awareness when processing real-world visual streams. Recently, feed-forward geometric foundation models, which implicitly extract geometric priors, have provided a new pathway to address this issue. However, existing geometry-aware MLLMs are predominantly constrained by the paradigm of single deep-layer extraction and input-level fusion. This flattened fusion leads to the loss of local geometric details and causes semantic mismatches in the early layers. To break this bottleneck, we propose GUIDE (Geometric Unrolling Inside MLLM Early-layers), a progressive geometric priors injection framework. GUIDE performs multi-level sampling within the geometric encoder, comprehensively capturing multi-granularity features ranging from local edges to global topologies. Subsequently, we rigorously align and fuse these multi-level geometric priors step-by-step with the early layers of the MLLM. Building upon the injection of multi-granularity geometric information, this design guides the model to progressively learn the 2D-to-3D transitional process. Furthermore, we introduce a context-aware gating that enables the model to fetch requisite spatial cues based on current semantics, thereby maximizing the utilization efficiency of spatial priors and effectively suppressing redundant geometric noise. Extensive experiments demonstrate that GUIDE significantly outperforms existing baselines on multiple complex spatial reasoning and perception tasks, establishing a novel paradigm for integrating 3D geometric priors into large models.

---

## 论文详细总结（自动生成）

这篇论文提出了 **GUIDE** 框架，旨在通过逐层展开几何先验来增强多模态大语言模型（MLLM）的物理空间感知能力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 MLLM 在 2D 视觉任务上表现优异，但在处理现实世界的 3D 空间推理（如距离估计、遮挡关系、场景布局）时能力有限。
*   **研究背景**：虽然前馈几何基础模型（如 VGGT）能提供 3D 先验，但现有方法多采用“输入级融合”和“深层特征提取”范式。
*   **研究动机**：作者认为这种“扁平化”的融合存在两个缺陷：1) 深层特征平滑导致局部几何细节（如边缘、深度不连续性）丢失；2) 单次注入无法让模型学习从 2D 到 3D 的渐进式转换，容易导致早期层的语义不匹配。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将几何先验的集成从“外部单次拼接”转变为“内部逐层展开”，引导模型在早期感知层中逐步内化 2D 到 3D 的物理转换。
*   **关键技术细节**：
    *   **多粒度几何提取**：从几何编码器（VGGT）的中后部均匀采样 $m$ 层特征，捕捉从局部边缘到全局拓扑的信息。
    *   **严格空间网格对齐**：通过调整输入图像分辨率，确保几何特征图与视觉特征图在物理感受野上完全一致。
    *   **逐层展开注入（Layer-wise Unrolling）**：将提取的多级几何特征按顺序注入 MLLM 的前 $m$ 个解码器层。
    *   **双重上下文感知门控（Context-Aware Gating）**：
        *   **Token 级语义门控**：利用 LLM 当前层的隐藏状态预测每个 Token 对几何信息的动态需求。
        *   **层级全局门控**：使用可学习的标量参数控制整层注入的强度。
    *   **输入级宏观锚定**：在输入端保留全局几何特征的加法融合，为模型提供初始的物理坐标参考。

### 3. 实验设计
*   **数据集与场景**：
    *   **空间推理**：VSI-Bench（核心基准）、MMSI-Bench、ViewSpatial、CV-Bench。
    *   **3D 场景理解**：ScanRefer（3D 视觉定位）、Scan2Cap（3D 密集描述）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-1.5/2.5-Pro, GPT-5。
    *   **开源通用模型**：Qwen3-VL, InternVL3。
    *   **空间增强模型**：Spatial-MLLM, VLM-3R, VG-LLM, Video-3D LLM 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 (80GB) GPU**。
*   **训练时长**：
    *   **GUIDE-5B**：空间推理任务约 14 小时。
    *   **GUIDE-9B**：空间推理任务约 85 小时；3D 场景理解任务约 37 小时。
*   **模型规模**：基于 Qwen3-VL 的 4B 和 8B 版本构建。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流基准测试，涉及多项选择、数值预测、目标定位和文本生成等多种任务形式。
*   **消融实验**：针对注入深度（0, 3, 6, 9, 24 层）和门控组件（无门控、仅语义门控、双重门控）进行了详尽测试。
*   **公平性**：在相同训练数据配置（如 VG-LLM*）下进行了直接对比，确保了结果的客观性。实验设计较为充分，验证了从推理到感知的全面能力。

### 6. 主要结论与发现
*   **性能领先**：GUIDE-9B 在 VSI-Bench 上达到 64.2 的高分，超越了 VLM-3R 和 VG-LLM，甚至在多项指标上优于 GPT-5。
*   **注入深度的敏感性**：实验发现“早期注入”最有效（6 层为佳），过深注入（如 24 层）反而会干扰 LLM 的高层抽象语义逻辑。
*   **门控的重要性**：若无门控机制，直接注入几何特征会导致性能下降，证明了过滤冗余几何噪声、按需获取空间线索的必要性。
*   **无需 3D 输入**：仅凭单目 2D 图像序列，GUIDE 在 3D 定位任务上达到了与使用点云数据的模型（如 LLaVA-3D）相当甚至更好的水平。

### 7. 优点
*   **范式创新**：打破了传统的输入级融合瓶颈，提出了渐进式展开的新范式。
*   **细节保留**：通过多级采样有效回收了深层特征丢失的高频空间细节。
*   **高效性**：在推理时无需昂贵的 3D 传感器数据或离线 3D 重建，具有极强的可扩展性。
*   **鲁棒性**：双重门控机制保护了 LLM 原有的语义空间不被几何噪声破坏。

### 8. 不足与局限
*   **依赖预训练编码器**：性能高度依赖于冻结的几何编码器（如 VGGT）的质量。
*   **计算开销**：虽然编码器冻结，但双流结构（视觉+几何）在特征提取阶段仍增加了额外的计算负担。
*   **应用限制**：目前主要针对单目视频/图像流，对于需要极高精度绝对度量的极端工业场景，可能仍需结合显式 3D 数据。

（完）
