---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本槽位级放置的目标条件视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v1"
tags: ["query:q18"]
score: 10.0
evidence: 用于零样本机器人放置的视觉-语言-动作策略
tldr: AnySlot 针对机器人视觉-语言-动作（VLA）策略在精确插槽级放置任务中的不足，提出了一种分层框架。该框架通过生成场景标记将复杂的语言指令转化为显式的空间视觉目标，并利用目标条件 VLA 策略进行执行。此外，研究者还推出了 SlotBench 基准测试。实验证明，AnySlot 在零样本插槽级放置任务中显著优于现有基准，实现了高精度的语义定位与空间控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10432v1/fig-001.webp\", \"caption\": \"Fig. 2: AnySlot overview. We formulate slot-level placement as goal-conditioned control. High-level goal construction uses the Nano-Banana image generator to render a blue-sphere goal from the language prompt, lifting it to a view-consistent multi-view overlay via depth and camera calibration. Low-level control uses a goal-conditioned VLA policy (π0.5) with a PaliGemma-3B backbone and action expert, mapping goalaugmented multi-view observations to continuous actions At.\", \"page\": 6, \"index\": 1, \"width\": 646, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2604.10432v1/fig-002.webp\", \"caption\": \"Fig. 1: Overview of flat (a), modular (b), and (c) our goal-conditioned policy.\", \"page\": 2, \"index\": 2, \"width\": 644, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.10432v1/fig-003.webp\", \"caption\": \"Fig. 4: Comparison between AnySlot and a VLM-based method. AnySlot accurately grounds the target slot and executes successful placement, while the VLM-based method mislocalizes the target and fails.\", \"page\": 10, \"index\": 3, \"width\": 675, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.10432v1/fig-004.webp\", \"caption\": \"Table 2: Ablation on high-level grounding module. Report Coarse Accuracy (CA, %), Instruction Accuracy (IA, %) and Latency (s). † means inference performed on a single H200 GPU. The yellow rows denote the module used by AnySlot.\", \"page\": 13, \"index\": 4, \"width\": 702, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.10432v1/fig-005.webp\", \"caption\": \"Table 1: Performance comparison of different methods on the SlotBench benchmark. Each entry reports SR (Success Rate; higher is better, %) and IA (Instruction Accuracy; higher is better, %). The yellow rows denote our proposed AnySlot, while the blue rows indicate the oracle upper bound.\", \"page\": 12, \"index\": 5, \"width\": 690, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.10432v1/fig-006.webp\", \"caption\": \"Fig. 3: SlotBench. A slot-level benchmark with nine challenges for spatial reasoning and language grounding in manipulation.\", \"page\": 9, \"index\": 6, \"width\": 716, \"height\": 532}]"
motivation: 现有的 VLA 策略在处理复杂的组合语言指令时，难以同时实现可靠的插槽定位和亚厘米级的执行精度。
method: 提出 AnySlot 框架，通过生成场景标记作为中间视觉目标，将高层插槽选择与底层控制执行解耦。
result: 在包含九类任务的 SlotBench 基准测试中，AnySlot 在零样本插槽级放置任务上的表现显著优于传统的扁平化 VLA 模型。
conclusion: 通过引入显式视觉目标作为中间表示，可以有效提升 VLA 策略在复杂空间推理和高精度放置任务中的鲁棒性与准确性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。槽位级任务既需要可靠的槽位定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记（scene marker）将语言转化为显式视觉目标，随后利用目标条件 VLA 策略执行该目标。这种分层设计有效地将高层槽位选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估槽位级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本槽位级放置方面显著优于扁平化 VLA 基线和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这是一份关于论文《AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）策略在处理“槽位级”（Slot-level）精确放置任务时存在巨大挑战。这类任务要求机器人不仅要理解复杂的组合语言指令（如“放在左数第二个槽位”），还要具备亚厘米级的执行精度。
*   **研究动机**：
    *   **扁平化模型（Flat VLA）的局限**：将语言理解与动作控制耦合在一起，导致模型在面对未见过的空间布局和复杂指令时，推理能力不足且精度低下。
    *   **模块化模型（VLM-based）的局限**：通常将目标简化为单一坐标点，丢失了槽位的几何边界信息，且视觉语言模型（VLM）生成的坐标往往不够精确。
*   **整体含义**：论文旨在通过引入一种“显式空间视觉目标”作为中间表示，将高层语义推理与底层运动控制解耦，从而实现零样本（Zero-shot）的高精度机器人放置。

### 2. 方法论：核心思想与技术细节
AnySlot 采用两阶段的分层架构：
*   **核心思想**：不直接输出动作或坐标，而是先将语言指令转化为图像中的一个“视觉标记”（如蓝色球体），再让底层策略去“追随”这个标记。
*   **关键技术流程**：
    1.  **第一阶段：高层目标构建（Goal Construction）**：
        *   使用现成的图像生成模型（如 Nano-Banana）在原始观测图像中根据指令生成一个蓝色球体标记。
        *   利用深度图和相机内参将该标记中心反投影到 3D 空间，获得世界坐标系下的锚点。
        *   将该 3D 锚点重新投影到所有相机视角（如手眼相机、侧边相机），生成视角一致的视觉覆盖层（Overlay）。
    2.  **第二阶段：底层目标条件策略（Goal-Conditioned Policy）**：
        *   使用连续生成式 VLA 策略（基于 $\pi_{0.5}$ 框架，采用 PaliGemma-3B 主干网络）。
        *   策略输入包含带有视觉标记的多视图图像，指令被固定为简单的“移动物体到蓝色球体引导的槽位”。
        *   通过流匹配（Flow-matching）生成连续动作序列。

### 3. 实验设计
*   **基准测试（SlotBench）**：研究者专门开发了 SlotBench 仿真基准，包含 9 类挑战：
    *   序数推理、尺寸比较、高度推理、距离推理、组合关系、逻辑否定、模糊语言、适宜性推理、世界知识。
*   **数据集**：
    *   **合成数据集 ($D_{syn}$)**：在 SAPIEN 模拟器中收集，包含随机化的物体位姿、槽位几何形状及对应的视觉目标标记，用于训练底层策略。
*   **对比方法**：
    *   **扁平化 VLA**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle**：由人类专家提供完美目标位置的上限基准。

### 4. 资源与算力
*   **算力设备**：实验使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：
    *   对 $\pi_{0.5}$ 进行了全参数微调。
    *   训练步数为 **20,000 步**。
    *   Batch Size 为 **64**。
*   **推理延迟**：高层图像生成模块（Nano-Banana 2）的推理延迟约为 11 秒，这在实时性要求极高的场景下是一个考虑因素。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SlotBench 的 9 个类别中，每个任务进行 **50 次测试**。
    *   包含 45 种以上的独特场景布局和多种语言指令变体。
*   **充分性与公平性**：
    *   **消融实验**：对比了不同的高层推理模块（GPT-4o, Gemini, Molmo 等）和不同的底层策略（Diffusion Policy 等）。
    *   **泛化性测试**：在完全未见过的槽位布局和指令下进行零样本评估。
    *   **真实世界验证**：在 UR10e 机器人平台上验证了目标重构的几何一致性。
    *   实验设计较为全面，通过 Oracle 基线明确了性能瓶颈所在，对比具有客观性。

### 6. 主要结论与发现
*   **AnySlot 表现卓越**：在 SlotBench 的平均成功率接近 **90%**，而传统的扁平化 VLA 模型在绝大多数任务上几乎完全失败（成功率接近 0%）。
*   **视觉目标优于坐标**：使用图像生成模型产生的空间标记比 VLM 回归的坐标点更具鲁棒性，能提供更好的几何上下文。
*   **解耦的必要性**：将复杂的语义推理交给强大的生成模型，将精确控制交给专门训练的 VLA，是解决高精度操控任务的有效路径。

### 7. 优点（亮点）
*   **创新的中间表示**：通过“图像编辑”的方式定义任务目标，巧妙地解决了 VLM 空间定位不准的问题。
*   **强大的零样本能力**：无需针对特定槽位布局进行微调，利用预训练生成模型的常识即可处理复杂逻辑（如逻辑否定、世界知识）。
*   **视角一致性**：通过 3D 锚点重投影技术，确保了多摄像头系统下的目标一致性。

### 8. 不足与局限
*   **推理延迟**：图像生成模型（如 Nano-Banana）的推理速度较慢（>10s），限制了其在动态环境或高频交互任务中的应用。
*   **依赖外部模型**：系统的性能高度依赖于第三方图像生成模型的质量，若生成模型无法正确渲染标记，后续执行将失败。
*   **深度图依赖**：第一阶段依赖高质量的深度图进行 3D 重建，在透明物体或强反光环境下可能失效。
*   **任务范围**：目前主要集中在“放置”这一特定动作，对于更复杂的长程操作（Long-horizon）尚待验证。

（完）
