---
title: "MV3DIS: Multi-View Mask Matching via 3D Guides for Zero-Shot 3D Instance Segmentation"
title_zh: MV3DIS：通过 3D 引导进行多视图掩码匹配的零样本 3D 实例分割
authors: "Yibo Zhao, Yigong Zhang, Jin Xie"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.08916v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 利用多视图 2D 掩码和 3D 引导进行零样本 3D 实例分割
tldr: 本文提出MV3DIS，一个旨在解决零样本3D实例分割中多视图不一致问题的由粗到精框架。针对现有方法独立处理各帧导致分割碎片化的问题，MV3DIS引入3D引导的掩码匹配策略，利用粗略3D先验作为参考来对齐跨视图2D掩码，并结合深度一致性加权处理遮挡。该方法在ScanNetV2等多个数据集上刷新了性能记录，显著提升了分割的连贯性与准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08916v1/fig-001.webp\", \"caption\": \"Figure 1. View inconsistency in existing methods versus view consistency with MV3DIS. Top-right: 3D instance segmentations from existing approaches and MV3DIS. Bottom blue and yellow strips: 2D segmentation maps from different views produced by existing approaches and MV3DIS. Existing methods yield view-inconsistent masks and fragmented 3D instances, whereas MV3DIS enforces view consistency and produces coherent 3D instance segmentations.\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-002.webp\", \"caption\": \"Table 1. Class-agnostic 3D instance segmentation on ScanNetV2. Best and second-best results are bold and underlined, respectively. VFM represents the 2D vision foundation model, and Sem-SAM represents Semantic-SAM [29].\", \"page\": 6, \"index\": 2, \"width\": 475, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-003.webp\", \"caption\": \"Table 4. Semantic instance segmentation on ScanNet200. Head (AP), Common (AP), and Tail (AP) represent AP scores evaluated on frequent, common, and rare classes, respectively. “Sup.” and “OV.” represent “Supervised” and “Open-vocabulary”. Best results are in bold, and the second-best results are underlined.\", \"page\": 7, \"index\": 3, \"width\": 483, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-004.webp\", \"caption\": \"Table 3. Class-agnostic 3D instance segmentation on ScanNet++. Best and second-best results are bold and underlined, respectively. VFM represents the 2D vision foundation model, and SAM-HQ refers to [22]. Note that Mask3D is trained on the ScanNetV2 dataset.\", \"page\": 7, \"index\": 4, \"width\": 471, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-005.webp\", \"caption\": \"Table 2. Class-agnostic 3D instance segmentation on ScanNet200. Best and second-best results are bold and underlined, respectively. VFM represents the 2D vision foundation model, SemSAM represents Semantic-SAM, and CropFormer refers to [41].\", \"page\": 7, \"index\": 5, \"width\": 500, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-006.webp\", \"caption\": \"Figure 4. Qualitative results of our method compared to SAM3D [61] and SAI3D [65] on the ScanNetV2 and ScanNet++ datasets.\", \"page\": 8, \"index\": 6, \"width\": 882, \"height\": 515}, {\"url\": \"assets/figures/arxiv/2604.08916v1/fig-007.webp\", \"caption\": \"Figure 2. Method overview. Our method consists of two stages: (a) Coarse 3D Instance Segmentation and (b) 3D Instance Refinement. We first over-segment the point cloud into superpoints and use SAM to generate per-frame 2D masks, which are organized into a global mask collection. These masks are consolidated into coarse 2D segmentation maps that guide the merging of superpoints into coarse 3D segments. We then project the coarse 3D segments onto images and perform 3D-guided mask matching to associate consistent 2D masks from the collection. Finally, we refine the coarse 3D segments into coherent 3D instances using the consistent 2D segmentation maps.\", \"page\": 4, \"index\": 7, \"width\": 1020, \"height\": 387}]"
motivation: 现有方法缺乏多视图关联和3D先验的引导，导致2D掩码在不同视角下不一致，进而造成3D分割结果破碎。
method: 提出一种利用粗略3D段作为引导的掩码匹配策略，并结合深度一致性加权机制来优化多视图掩码的融合与精细化。
result: 在ScanNetV2、ScanNet200、ScanNet++、Replica和Matterport3D等多个数据集上均取得了优于现有方法的性能。
conclusion: 通过显式整合3D先验和多视图一致性，MV3DIS为零样本3D实例分割提供了一种更具鲁棒性和扩展性的解决方案。
---

## 摘要
传统的 3D 实例分割方法依赖于劳动密集型的 3D 标注进行监督训练，这限制了它们的可扩展性以及对新物体的泛化能力。最近的方法利用来自 Segment Anything Model (SAM) 的多视图 2D 掩码来引导 3D 几何原语的合并，从而实现零样本 3D 实例分割。然而，这些方法通常独立处理每一帧，并仅依赖于 2D 指标（如 SAM 预测分数）来生成分割图。这种设计忽略了多视图相关性和固有的 3D 先验，导致跨视图的 2D 掩码不一致，并最终导致 3D 分割碎片化。在本文中，我们提出了 MV3DIS，这是一个用于零样本 3D 实例分割的由粗到精的框架，它显式地结合了 3D 先验。具体而言，我们引入了一种 3D 引导的掩码匹配策略，该策略使用粗糙的 3D 分段作为共同参考来匹配跨视图的 2D 掩码，并通过 3D 覆盖分布巩固多视图掩码的一致性。在这些视图一致的 2D 掩码引导下，粗糙的 3D 分段被进一步细化为精确的 3D 实例。此外，我们引入了一种深度一致性加权方案，通过量化投影可靠性来抑制物体间遮挡带来的歧义，从而提高 3D 到 2D 对应关系的鲁棒性。在 ScanNetV2、ScanNet200、ScanNet++、Replica 和 Matterport3D 数据集上的广泛实验证明了 MV3DIS 的有效性，其性能优于以往的方法。

## Abstract
Conventional 3D instance segmentation methods rely on labor-intensive 3D annotations for supervised training, which limits their scalability and generalization to novel objects. Recent approaches leverage multi-view 2D masks from the Segment Anything Model (SAM) to guide the merging of 3D geometric primitives, thereby enabling zero-shot 3D instance segmentation. However, these methods typically process each frame independently and rely solely on 2D metrics, such as SAM prediction scores, to produce segmentation maps. This design overlooks multi-view correlations and inherent 3D priors, leading to inconsistent 2D masks across views and ultimately fragmented 3D segmentation. In this paper, we propose MV3DIS, a coarse-to-fine framework for zero-shot 3D instance segmentation that explicitly incorporates 3D priors. Specifically, we introduce a 3D-guided mask matching strategy that uses coarse 3D segments as a common reference to match 2D masks across views and consolidates multi-view mask consistency via 3D coverage distributions. Guided by these view-consistent 2D masks, the coarse 3D segments are further refined into precise 3D instances. Additionally, we introduce a depth consistency weighting scheme that quantifies projection reliability to suppress ambiguities from inter-object occlusions, thereby improving the robustness of 3D-to-2D correspondence. Extensive experiments on the ScanNetV2, ScanNet200, ScanNet++, Replica, and Matterport3D datasets demonstrate the effectiveness of MV3DIS, which achieves superior performance over previous methods

---

## 论文详细总结（自动生成）

这是一份关于论文《MV3DIS: Multi-View Mask Matching via 3D Guides for Zero-Shot 3D Instance Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无需 3D 人工标注的情况下，利用 2D 视觉大模型（如 SAM）实现高质量、连贯的 3D 实例分割。
*   **研究背景**：传统的 3D 实例分割依赖昂贵的 3D 标注。现有零样本（Zero-shot）方法（如 SAM3D, SAI3D）通常独立处理每一帧图像，仅依赖 2D 指标（如 SAM 预测分数）生成分割图。
*   **研究动机**：由于缺乏跨视图关联和 3D 几何先验的引导，现有方法在不同视角下生成的 2D 掩码往往不一致（例如同一物体在 A 视角被分割完整，在 B 视角被碎片化），这直接导致最终生成的 3D 实例出现严重的碎片化或过分割现象。

### 2. 方法论
MV3DIS 采用了一个**由粗到精（Coarse-to-fine）**的框架，核心思想是利用 3D 几何信息作为“锚点”来对齐多视图的 2D 预测。

*   **核心流程**：
    1.  **粗略 3D 实例生成**：首先将点云过分割为超点（Superpoints），利用 SAM 生成的初步 2D 分割图引导超点合并，得到粗略的 3D 段（Coarse 3D segments）。
    2.  **3D 引导的掩码匹配（关键创新）**：将粗略 3D 段投影回各个 2D 视图。这些投影作为共同参考，用于在不同视图的 SAM 掩码池中匹配属于同一物体的掩码。
    3.  **3D 覆盖分布一致性**：通过计算 2D 掩码对 3D 段的覆盖向量，利用余弦相似度评估跨视图掩码的一致性分数，剔除不一致的碎片化掩码。
    4.  **深度一致性加权（DCW）**：引入公式计算投影点与深度图的匹配度，量化投影的可靠性。这能有效识别并抑制因物体间遮挡（Occlusion）导致的 3D-2D 错误关联。
    5.  **区域精细化（Region Refinement）**：利用筛选出的视图一致性掩码重新计算超点间的亲和力，通过迭代调整边界超点的归属，修正合并错误。

### 3. 实验设计
*   **数据集**：涵盖了五个主流 3D 室内场景数据集：**ScanNetV2**（基础）、**ScanNet200**（长尾类别）、**ScanNet++**（高分辨率）、**Replica** 和 **Matterport3D**。
*   **Benchmark（基准设置）**：
    *   **类别无关（Class-agnostic）**：仅评估实例分割的几何准确性。
    *   **语义实例分割（Semantic）**：结合 CLIP 等模型为实例分配标签。
*   **对比方法**：
    *   **有监督方法**：Mask3D（SOTA 闭集模型）。
    *   **零样本/开放词汇方法**：SAM3D, SAI3D, Open3DIS, SAM2Object, Any3DIS 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练/推理总时长。
*   **效率提示**：作者提到为了平衡性能与效率，在 ScanNet 系列数据集上采用了采样策略（仅使用 5%-10% 的视图），并指出该方法在稀疏视图输入下依然具有很强的鲁棒性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个不同特性的数据集上进行了全面测试。
    *   **消融实验**：详细验证了区域精细化（RR）、3D 引导掩码匹配（3DG-MM）和深度一致性加权（DCW）三个核心组件的增益。
    *   **超参数分析**：对深度容差 $\alpha$ 和合并阈值 $\tau_{merge}$ 进行了敏感度分析。
    *   **视图稀疏性测试**：测试了从 1% 到 20% 采样率下的性能变化。
*   **评价**：实验设计非常充分，涵盖了从几何到语义、从密集到稀疏视图的多个维度，对比基准涵盖了最新的 2024/2025 年方法，结果具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：MV3DIS 在所有测试数据集上均刷新了零样本 3D 实例分割的记录。在 ScanNet200 上，其 AP50 甚至超过了完全监督的 Mask3D 模型。
*   **解决不一致性**：定性结果显示，MV3DIS 能有效合并被其他方法误分割的沙发、椅子等大型物体。
*   **泛化能力**：在处理尾部类别（稀有物体）时，MV3DIS 表现出比传统有监督方法更强的泛化性能。
*   **稀疏视图鲁棒性**：仅使用 2% 的图像输入，其表现就优于使用 20% 图像输入的 SAI3D。

### 7. 优点
*   **3D 先验的深度融合**：不同于以往“2D 升 3D”的单向流程，MV3DIS 实现了“3D 引导 2D 匹配，2D 细化 3D 分割”的闭环，显著提升了视图一致性。
*   **遮挡处理机制**：通过深度一致性加权解决了投影中的“穿透”问题，提高了 3D-2D 关联的鲁棒性。
*   **细粒度分割**：能够分割出比 Ground Truth 标注更细致的物体（如墙上的插座）。

### 8. 不足与局限
*   **依赖超点质量**：第一步的超点生成（Graph-cut）如果出现严重错误，可能会影响后续的合并效果。
*   **计算链路较长**：由粗到精的多阶段处理虽然效果好，但相比端到端方法，其系统复杂度和中间存储需求可能较高。
*   **2D 模型限制**：性能上限仍受限于底层 2D 分割模型（如 SAM/SAM2）的质量，若 2D 模型在某些光照或材质下失效，3D 引导也难以完全弥补。

（完）
