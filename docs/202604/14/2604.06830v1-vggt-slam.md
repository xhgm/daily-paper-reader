---
title: VGGT-SLAM++
title_zh: VGGT-SLAM++
authors: "Avilasha Mandal, Rajesh Kumar, Sudarshan Sunil Harithas, Chetan Arora"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06830v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于精确大规模建图和环境表示的视觉 SLAM 系统
tldr: VGGT-SLAM++ 是一个基于视觉几何接地 Transformer (VGGT) 的完整视觉 SLAM 系统。它通过融合 VGGT 前馈网络与 Sim(3) 解算构建前端，并引入基于数字高程模型 (DEM) 的图构建模块。针对前作在短时位姿漂移上的不足，该系统利用 DINOv2 特征和视觉地点识别 (VPR) 实现了高频局部平差 (LBA) 和空间校正后端。该系统在保持全局一致性的同时，显著降低了漂移并提升了大规模建图的精度与效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06830v1/fig-001.webp\", \"caption\": \"Figure 4. Preservation of geometric cues in the DEMs, alongside semantics help in accurate trajectory estimation in planar scenes like the TUM RGB-D foor scene as shown in the figure.\", \"page\": 6, \"index\": 1, \"width\": 329, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2604.06830v1/fig-002.webp\", \"caption\": \"Figure 5. (A), (B), (C): zero-shot object detection from DEMs proving structure preservation. (D) DEM of TUM-teddy and (E) color coding.\", \"page\": 6, \"index\": 2, \"width\": 466, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.06830v1/fig-003.webp\", \"caption\": \"Figure 6. VGGT-SLAM++ results for : (A) custom data (406.8m) recorded by GoPro HERO10 camera with GPS groundtruth with 2m precision. (ATE RMSE 18± 2 m); (B) custom data (1.8m) recorded by a OAK-1 camera with a Humanoid robot kinematics groundtruth (ATE RMSE 0.02m); (C) custom data (1.8m) recorded by a OAK-1 camera with Cobot forward kinematics groundtruth (ATE RMSE 0.01m); (D) KITTI Odometry 06 sequence (1230 m; ATE RMSE 13.65m); (E)TUM RGB-D 360 scene (5.82m; ATE RMSE 0.042m); (F) custom data (287.381m) path recorded by the GoPro HERO10 camera with GPS groundtruth with 2m precision. (ATE RMSE 7.17± 2 m).\", \"page\": 7, \"index\": 3, \"width\": 976, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2604.06830v1/fig-004.webp\", \"caption\": \"Figure 2. (A) DEM-based scene representation on the TUM RGB-D freiburg1 teddy dataset. The DEMs provide a compact 2.5D encoding retaining geometric structure. (B) DEM visualizations from the 7-Scenes dataset. (C) DEMs generated for the Virtual KITTI (Sequence 01) dataset. (D) A full KITTI Odometry (Sequence 09) sequence demonstrating a complete loop, illustrating the ability of DEMs and our SLAM back-end pipeline to maintain global consistency.\", \"page\": 3, \"index\": 4, \"width\": 976, \"height\": 451}, {\"url\": \"assets/figures/arxiv/2604.06830v1/fig-005.webp\", \"caption\": \"Table 5. DEM hyperparameter ablations on KITTI Odometry.\", \"page\": 8, \"index\": 5, \"width\": 467, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2604.06830v1/fig-006.webp\", \"caption\": \"Figure 3. Complete pipeline overview. The proposed VGGT-SLAM++ system comprises three main components: (a) Front-end: A Sim(3) odometry module that optimizes the relative poses of submaps generated by the feed-forward VGGT network. (b) Covisibility graph construction: A DEM-based map representation is used to compute structure-aware embeddings leveraging DINOv2, and an averaged tile score is used to insert spatially consistent nodes and edges into the covisibility graph. (c) Back-end: An optimization module that organizes submaps into a spatial graph and performs optimization over the detected spatial constraints.\", \"page\": 4, \"index\": 6, \"width\": 976, \"height\": 539}]"
motivation: 旨在解决现有基于 Transformer 的 SLAM 系统因过度依赖稀疏回环而导致的短时位姿漂移和局部一致性不足的问题。
method: 融合 VGGT 前端与基于 DEM 的图构建模块，利用 DINOv2 嵌入和 VPR 模块在共视窗口内实现频繁的局部束调整优化。
result: 在多个标准 SLAM 基准测试中取得了最先进的精度，显著降低了短期漂移并加快了全局一致性图的收敛。
conclusion: VGGT-SLAM++ 证明了结合几何感知 Transformer 与空间校正后端可以实现高精度、低内存占用且鲁棒的大规模视觉定位与建图。
---

## 摘要
我们介绍了 VGGT-SLAM++，这是一个完整的视觉 SLAM 系统，它利用了视觉几何接地 Transformer（Visual Geometry Grounded Transformer, VGGT）丰富的几何输出。该系统由融合了 VGGT 前馈 Transformer 和 Sim(3) 解算的视觉里程计（前端）、基于数字高程模型（DEM）的图构建模块以及后端组成，共同实现了在有限内存下的精确大规模建图。虽然以往基于 Transformer 的 SLAM 流水线（如 VGGT-SLAM）主要依赖稀疏回环检测或全局 Sim(3) 流形约束，从而导致短时位姿漂移，但 VGGT-SLAM++ 通过空间校正后端恢复了高频次的局部光束法平差（LBA）。对于每个 VGGT 子图，我们构建一个稠密的平面规范化 DEM，将其划分为补丁，并计算其 DINOv2 嵌入，以将子图集成到共视图中。在共视窗口内，利用视觉地点识别（VPR）模块检索空间邻居，触发频繁的局部优化以稳定轨迹。在标准 SLAM 基准测试中，VGGT-SLAM++ 达到了最先进的精度，显著减少了短期漂移，加速了图收敛，并利用紧凑的 DEM 瓦片和亚线性检索保持了全局一致性。

## Abstract
We introduce VGGT-SLAM++, a complete visual SLAM system that leverages the geometry-rich outputs of the Visual Geometry Grounded Transformer (VGGT). The system comprises a visual odometry (front-end) fusing the VGGT feed-forward transformer and a Sim(3) solution, a Digital Elevation Map (DEM)-based graph construction module, and a back-end that jointly enable accurate large-scale mapping with bounded memory. While prior transformer-based SLAM pipelines such as VGGT-SLAM rely primarily on sparse loop closures or global Sim(3) manifold constraints - allowing short-horizon pose drift - VGGT-SLAM++ restores high-cadence local bundle adjustment (LBA) through a spatially corrective back-end. For each VGGT submap, we construct a dense planar-canonical DEM, partition it into patches, and compute their DINOv2 embeddings to integrate the submap into a covisibility graph. Spatial neighbors are retrieved using a Visual Place Recognition (VPR) module within the covisibility window, triggering frequent local optimization that stabilizes trajectories. Across standard SLAM benchmarks, VGGT-SLAM++ achieves state-of-the-art accuracy, substantially reducing short-term drift, accelerating graph convergence, and maintaining global consistency with compact DEM tiles and sublinear retrieval.

---

## 论文详细总结（自动生成）

以下是对论文 **《VGGT-SLAM++》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉 SLAM 系统（如 ORB-SLAM）依赖于手工设计的特征，而新兴的基于 Transformer 的 SLAM 系统（如早期的 VGGT-SLAM）虽然在几何感知上表现出色，但存在**短时位姿漂移（Short-horizon drift）**的问题。这是因为它们往往过度依赖稀疏的回环检测或全局约束，缺乏高频次的局部优化机制。
**VGGT-SLAM++** 的核心目标是构建一个完整的、具备全局一致性的视觉 SLAM 系统，通过引入**数字高程模型（DEM）**作为中间表示，并结合**空间校正后端**，解决 Transformer SLAM 的局部不稳定性，实现大规模环境下的高精度定位与建图。

### 2. 论文提出的方法论
该系统由三个主要模块组成：
*   **前端（Front-end）：** 融合了 VGGT 前馈网络与 Sim(3) 求解器。VGGT 负责从图像中提取丰富的几何特征，Sim(3) 模块则通过优化子图间的相对位姿来提供视觉里程计。
*   **共视网格构建（Covisibility Graph Construction）：** 
    *   将 VGGT 生成的子图转换为**数字高程模型（DEM）**。DEM 是一种 2.5D 的紧凑表示，能够保留几何结构并节省内存。
    *   将 DEM 划分为多个瓦片（Tiles），并利用 **DINOv2** 提取结构感知嵌入（Embeddings）。
    *   通过计算瓦片得分，将具有空间一致性的节点和边插入共视图中。
*   **后端（Back-end）：** 
    *   引入**视觉地点识别（VPR）**模块，在共视窗口内检索空间邻居。
    *   触发**高频局部束调整（LBA）**，通过检测到的空间约束对子图进行频繁的局部优化，从而抑制漂移并加速图收敛。

### 3. 实验设计
*   **数据集：** 涵盖了多种尺度和环境，包括：
    *   **室外大规模：** KITTI Odometry（序列 06, 09）、Virtual KITTI。
    *   **室内场景：** TUM RGB-D（如 teddy, floor 序列）、7-Scenes。
    *   **自定义数据：** 使用 GoPro HERO10（带 GPS 基准）和 OAK-1 相机（带机器人运动学基准）采集的长距离（达 400m+）和短距离数据。
*   **Benchmark（基准指标）：** 主要使用绝对轨迹误差（ATE RMSE）。
*   **对比方法：** 与原始 VGGT-SLAM 以及其他主流 SLAM 方案进行对比，验证其在减少漂移和提升精度方面的优势。

### 4. 资源与算力
*   **算力说明：** 论文摘要和提取文本中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **效率特征：** 文中强调了系统具有“受限的内存占用（bounded memory）”和“亚线性检索（sublinear retrieval）”能力，暗示其在处理大规模场景时具有较好的计算效率。

### 5. 实验数量与充分性
*   **实验规模：** 论文在多个标准基准数据集和真实世界自定义数据上进行了测试，轨迹长度从 1.8 米到 1230 米不等。
*   **消融实验：** 针对 DEM 的超参数（如分辨率、瓦片大小等）在 KITTI 数据集上进行了专门的消融研究（见 Table 5）。
*   **充分性评价：** 实验覆盖了室内/室外、模拟/真实、短程/长程等多种维度，验证了系统在不同光照和运动条件下的鲁棒性，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **精度提升：** VGGT-SLAM++ 在多个基准测试中达到了最先进（SOTA）的精度，显著优于前作。
*   **漂移抑制：** 通过引入基于 DEM 和 DINOv2 的空间校正后端，成功解决了 Transformer SLAM 常见的短期位姿漂移问题。
*   **全局一致性：** 系统能够有效识别回环并进行全局优化，即使在长达 1.2 公里的 KITTI 序列中也能保持轨迹的闭合与一致。
*   **表示优势：** DEM 不仅压缩了地图数据，还保留了足够的几何和语义信息，甚至能支持零样本（Zero-shot）目标检测。

### 7. 优点
*   **创新的表示法：** 使用 DEM 作为 SLAM 的中间表示，既保留了 3D 结构信息，又比原始点云更轻量。
*   **强强联手：** 结合了 Transformer 的特征提取能力、DINOv2 的语义理解能力以及传统图优化的稳定性。
*   **鲁棒性：** 在平面场景（如 TUM floor）和复杂室外场景中均表现稳定。

### 8. 不足与局限
*   **DEM 的局限性：** DEM 本质上是 2.5D 表示，在处理极端复杂的 3D 结构（如多层重叠建筑、复杂的洞穴系统）时，可能会丢失部分垂直方向的几何细节。
*   **实时性挑战：** 虽然提到了亚线性检索，但集成 DINOv2 和频繁的 LBA 优化可能会带来较高的计算开销，文中对实时帧率（FPS）的详细分析较少。
*   **传感器依赖：** 系统主要基于视觉几何，在极端弱光或无纹理环境下可能仍面临挑战。

（完）
