---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v2"
tags: ["query:q11"]
score: 9.0
evidence: 基于多模态大语言模型的高效视觉语言理解
tldr: Firebolt-VL 是一款高效的视觉语言模型，旨在解决传统 Transformer 架构计算成本高且难以捕捉细粒度视觉信息的问题。该模型采用 Liquid Foundation Model (LFM) 替代 Transformer 解码器，并引入 Token-Grid 相关性模块，通过状态空间模型（SSM）和 FiLM 调节实现轻量级的跨模态调制。这使得模型在保持线性推理复杂度的同时，能精准聚焦与文本相关的视觉区域，在多项基准测试中实现了高效且精准的细粒度理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04579v2/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison of the proposed Firebolt-VL model with existing MLLMs across seven benchmarks. The superscript p denotes the perception score on the MME benchmark, while SQAI refers the ScienceQA-IMG [30]. The best results are shown in bold, and the second-best results are underlined. “–” indicates results not reported in the original papers.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2604.04579v2/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of responses from Firebolt-VL with recent efficient vision–language models, including MobileVLM V2 [10] and SmolVLM2 [31], on detail-dependent question-answering tasks. Firebolt-VL demonstrates stronger fine-grained grounding and more accurate, instruction-aligned responses.\", \"page\": 6, \"index\": 2, \"width\": 891, \"height\": 588}, {\"url\": \"assets/figures/arxiv/2604.04579v2/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Firebolt-VL architecture. The Cross-Modal Modulator (CMM) fuses textual instructions with the visual representations of the query image to produce conditioned tokens, which are then processed by the Liquid Foundation Model (LFM). The model is trained in two stages: (1) CMM pre-training to initialize modulation parameters, and (2) end-to-end training of the full framework.\", \"page\": 4, \"index\": 3, \"width\": 912, \"height\": 430}]"
motivation: 现有的多模态大模型因 Transformer 的二次复杂度导致计算成本高昂，且小型模型在处理细粒度推理任务时难以精准捕捉关键视觉区域。
method: 采用 Liquid Foundation Model (LFM) 作为解码器，并结合 Token-Grid 相关性模块与 FiLM 调节的状态空间模型，实现高效的跨模态特征调制。
result: 实验结果表明，Firebolt-VL 在多个基准测试中展现了出色的细粒度理解能力，并显著提升了推理效率。
conclusion: Firebolt-VL 证明了通过结合线性复杂度的解码器与轻量级跨模态调制机制，可以在资源受限的环境下实现高性能的视觉语言理解。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在视觉-语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次方复杂度阻碍了效率。此外，小型视觉-语言模型往往难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了它们在现实世界中的有效性。为了解决这些问题，我们推出了 Firebolt-VL，这是一种高效的视觉-语言模型，它将基于 Transformer 的解码器替换为液体基础模型（Liquid Foundation Model, LFM）解码器。为了进一步增强视觉定位，我们提出了一个令牌-网格相关性模块（Token-Grid Correlation Module），该模块计算文本令牌与图像块之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型进行调制。这使得模型能够有选择地强调与文本提示相关的视觉区域，同时保持线性时间推理。在多个基准测试中的实验结果表明，Firebolt-VL 实现了准确、细粒度的理解，并显著提高了效率。我们的模型和代码可在以下网址获取：https://fireboltvl.github.io

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Firebolt-VL**，一种旨在平衡高性能与高效率的轻量级多模态大语言模型（MLLM）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的多模态大模型（如 LLaVA、Qwen-VL）大多基于 Transformer 架构，其注意力机制具有二次方计算复杂度，导致在资源受限设备（如智能手机、嵌入式摄像头）上推理缓慢且内存占用高。
*   **核心挑战**：
    1.  **效率瓶颈**：Transformer 的 KV 缓存和计算开销随序列长度剧增。
    2.  **感知精度不足**：小型视觉语言模型在处理需要细粒度视觉信息的任务（如 OCR、复杂图表分析）时，往往无法精准定位与任务相关的视觉区域。

### 2. 方法论：核心思想与技术细节
Firebolt-VL 的核心在于将线性复杂度的解码器与创新的跨模态调制机制相结合：
*   **Liquid Foundation Model (LFM) 解码器**：弃用传统的 Transformer，采用基于“液体”假设的 LFM2 作为语言解码器。LFM 具有线性时间复杂度，显著提升了长序列处理效率。
*   **跨模态调制器 (CMM)**：这是论文提出的关键模块，用于替代高成本的交叉注意力机制。
    *   **令牌-网格相关性（Token-Grid Correlation）**：计算文本令牌与图像特征网格之间的轻量级相关性矩阵，并仅保留前 $k$ 个最相关的视觉区域（Top-k）。
    *   **FiLM 调制**：利用特征线性调制（FiLM）技术，根据文本指令动态地对视觉特征进行缩放和偏移（Affine Transformation）。
    *   **状态空间模型 (SSM) 融合**：使用 S4 等结构化状态空间模型处理融合后的特征，以线性复杂度捕捉长程依赖，使模型能精准聚焦于任务相关的视觉细节。
*   **视觉编码器**：采用 SigLIP 提取图像特征，保持原生宽高比并生成网格化嵌入。

### 3. 实验设计
*   **数据集**：
    *   **阶段 1（预训练）**：使用 CC3M 数据集初始化 CMM 模块。
    *   **阶段 2（指令微调）**：使用 LLaVA-CoT 和处理后的 MMPR-v1.2 数据集，引入思维链（CoT）格式以增强推理能力。
*   **基准测试 (Benchmarks)**：在 7 个主流基准上进行评估，包括 VQAv2（视觉问答）、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、SQA-Image（科学问答）和 MMB（综合能力）。
*   **对比方法**：
    *   **大型模型 (>7B)**：IDEFICS, OpenFlamingo v2, MiniGPT-4, Chameleon, FastV。
    *   **小型模型 (<3B)**：Kosmos-2, MobileVLM (v1/v2), MoE-LLaVA, LLaVA-Phi, SmolVLM2。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H100 80GB GPU**。
*   **训练配置**：
    *   阶段 1：Batch size 128，学习率 $5 \times 10^{-4}$。
    *   阶段 2：Batch size 8，学习率 $1 \times 10^{-4}$。
*   **时长**：每个阶段训练 5 个 epoch，通常在第 2 个 epoch 即可选出最佳模型。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的定量对比（7 个基准测试）和定性分析（可视化对比）。
*   **消融实验**：针对核心组件做了三组深入实验：
    1.  **SSM 变体对比**：对比了 Mamba、S4D 和 S4，发现 S4 在捕捉几何空间关系上表现最优。
    2.  **融合策略对比**：对比了直接拼接（Prepend）、交叉注意力（Cross-attend）和 CMM，证明了 CMM 在感知分数上的巨大优势。
    3.  **Top-k 参数分析**：测试了不同视觉网格数量对性能的影响，确定 $k=4$ 为平衡点。
*   **评价**：实验设计客观、公平，对比对象涵盖了从早期经典到 2025 年最新的同类模型（如 SmolVLM2），具有很强的说服力。

### 6. 主要结论与发现
*   **性能卓越**：仅拥有 **0.8B 参数** 的 Firebolt-VL 在 MME 感知基准上取得了 1376.2 的高分，超越了许多 7B 甚至 9B 规模的模型。
*   **效率极高**：吞吐量达到 **46.67 tokens/s**，在同类轻量级模型中处于领先地位，且推理延迟与准确率的权衡（Trade-off）表现极佳。
*   **细粒度感知**：定性结果显示，Firebolt-VL 能准确识别图像中的微小文字（如 APP 界面中的目的地）和复杂的生物食物链关系，而其他轻量级模型常会失败。

### 7. 优点与亮点
*   **架构创新**：成功将 LFM 和 SSM 引入多模态领域，打破了 Transformer 的复杂度限制。
*   **轻量且精准**：通过 CMM 模块实现了“四两拨千斤”的效果，用极小的计算代价换取了高精度的视觉定位能力。
*   **开源贡献**：提供了代码和预训练模型，有助于推动端侧多模态技术的发展。

### 8. 不足与局限
*   **任务局限**：目前仅支持单图输入，尚未扩展到多图或视频序列。
*   **幻觉问题**：在 POPE 基准（物体幻觉测试）上表现略逊于部分基于注意力的模型，作者推测这可能是由于 SSM 的隐式状态传播在抑制幻觉方面不如显式注意力机制有效。
*   **应用限制**：虽然推理速度快，但在处理极度复杂的长文本上下文时，SSM 架构是否能完全替代 Transformer 仍需更多验证。

（完）
