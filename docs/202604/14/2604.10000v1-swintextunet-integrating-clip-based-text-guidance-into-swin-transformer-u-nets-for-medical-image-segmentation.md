---
title: "SwinTextUNet: Integrating CLIP-Based Text Guidance into Swin Transformer U-Nets for Medical Image Segmentation"
title_zh: SwinTextUNet：将基于 CLIP 的文本引导集成到 Swin Transformer U-Net 中用于医学图像分割
authors: "Ashfak Yeafi, Parthaw Goswami, Md Khairul Islam, Ashifa Islam Shamme"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10000v1"
tags: ["query:q11"]
score: 9.0
evidence: 将基于 CLIP 的文本引导集成到 Swin Transformer U-Net 中，用于多模态医学图像分割。
tldr: 针对传统医学图像分割模型在处理模糊或低对比度图像时表现不佳的问题，本文提出 SwinTextUNet。该框架将 CLIP 文本嵌入集成到 Swin Transformer UNet 架构中，通过交叉注意力和卷积融合机制实现文本语义与视觉特征的对齐。在 QaTaCOV19 数据集上的实验表明，该方法显著提升了分割的鲁棒性和准确性，为多模态医学影像分析提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10000v1/fig-001.webp\", \"caption\": \"Fig. 7. Representative qualitative segmentation results. Each triplet shows (a) original CXR, (b) ground-truth mask, and (c) SwinTextUNet prediction.\", \"page\": 5, \"index\": 1, \"width\": 370, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-002.webp\", \"caption\": \"Fig. 4. Overall architecture of SwinTextUNet. The model integrates CLIP-based text embeddings with a hierarchical Swin U-Net through cross-attention and ConvFuse blocks.\", \"page\": 4, \"index\": 2, \"width\": 702, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-003.webp\", \"caption\": \"Fig. 5. Representative samples from the QaTa-COV19 dataset across training, validation, and test sets. Each sample shows (a) chest X-ray, (b) ground-truth lesion mask, and (c) textual annotation provided by the dataset authors [23].\", \"page\": 4, \"index\": 3, \"width\": 474, \"height\": 452}]"
motivation: 传统的纯视觉分割模型在面对医学图像中模糊或低对比度的模式时，往往难以实现精确分割。
method: 提出一种结合 CLIP 文本引导与 Swin Transformer UNet 的多模态框架，利用交叉注意力和卷积融合技术对齐文本与视觉特征。
result: "在 QaTaCOV19 数据集上，该模型达到了 86.47% 的 Dice 分数和 78.2% 的 IoU，在性能与复杂度之间取得了最佳平衡。"
conclusion: 视觉与语言信息的集成能有效增强医学图像分割的准确性，为临床诊断工具的开发提供了有力支持。
---

## 摘要
精确的医学图像分割是实现计算机辅助诊断和有效治疗规划的基础。仅依赖视觉特征的传统模型在面对模糊或低对比度模式时往往难以取得理想效果。为了克服这些局限性，我们提出了 SwinTextUNet，这是一种多模态分割框架，它将对比语言-图像预训练（CLIP）衍生的文本嵌入集成到 Swin Transformer U-Net 骨干网络中。通过整合交叉注意力（cross attention）和卷积融合，该模型有效地将语义文本引导与分层视觉表示对齐，从而增强了鲁棒性和准确性。我们在 QaTaCOV19 数据集上评估了我们的方法，其中所提出的四阶段变体在性能和复杂性之间实现了最佳平衡，Dice 和 IoU 分数分别达到 86.47% 和 78.2%。消融研究进一步验证了文本引导和多模态融合的重要性。这些研究结果强调了视觉-语言集成在推进医学图像分割和支持具有临床意义的诊断工具方面的巨大潜力。

## Abstract
Precise medical image segmentation is fundamental for enabling computer aided diagnosis and effective treatment planning. Traditional models that rely solely on visual features often struggle when confronted with ambiguous or low contrast patterns. To overcome these limitations, we introduce SwinTextUNet, a multimodal segmentation framework that incorporates Contrastive Language Image Pretraining (CLIP), derived textual embeddings into a Swin Transformer UNet backbone. By integrating cross attention and convolutional fusion, the model effectively aligns semantic text guidance with hierarchical visual representations, enhancing robustness and accuracy. We evaluate our approach on the QaTaCOV19 dataset, where the proposed four stage variant achieves an optimal balance between performance and complexity, yielding Dice and IoU scores of 86.47% and 78.2%, respectively. Ablation studies further validate the importance of text guidance and multimodal fusion. These findings underscore the promise of vision language integration in advancing medical image segmentation and supporting clinically meaningful diagnostic tools.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SwinTextUNet**，一种将视觉与语言信息融合的医学图像分割框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的医学图像分割模型（如 U-Net 及其变体）主要依赖视觉特征。然而，在面对低对比度、边界模糊或噪声严重的医学影像时，纯视觉模型往往难以准确区分病变区域。
*   **核心思想**：临床医生在诊断时通常会结合放射报告等文本信息。本文旨在通过引入 **CLIP（对比语言-图像预训练）** 提取的文本语义嵌入，引导 **Swin Transformer U-Net** 进行分割，从而利用多模态信息增强模型的鲁棒性和准确性。

### 2. 方法论
SwinTextUNet 结合了分层视觉编码和语义文本引导，核心组件包括：
*   **文本编码器**：使用预训练且冻结的 CLIP 文本编码器。将诊断提示（如“双肺感染，左上肺和右上肺有两个感染区”）转化为文本嵌入向量，并通过线性投影对齐到视觉特征维度。
*   **视觉编码器**：采用分层的 Swin Transformer 结构，通过移动窗口自注意力机制（Shifted-Window Self-Attention）捕捉多尺度全局上下文。
*   **文本引导交叉注意力（Cross-Attention）**：在编码器的每个阶段，视觉 Token 作为 Query，文本 Token 作为 Key 和 Value。通过交叉注意力机制，文本语义能够“指导”视觉特征的提取，强化与诊断描述相关的区域。
*   **卷积融合模块（ConvFuse）**：在解码器阶段，将上采样特征与跳跃连接（Skip Connection）特征拼接，通过 3×3 卷积层进行细化，以补偿 Transformer 在局部空间细节上的不足，改善边界重建。
*   **损失函数**：采用混合损失函数，结合了 Dice Loss（关注区域重叠）和 Cross-Entropy Loss（关注像素级分类），以应对医学图像中的类别不平衡问题。

### 3. 实验设计
*   **数据集**：使用 **QaTa-COV19** 数据集，包含 9,258 张胸部 X 光片（CXR）及其对应的 COVID-19 病变掩码。该数据集的特点是每张图像都配有由医学专家编写的文本注释。
*   **对比基准（Benchmark）**：
    *   **CNN 模型**：U-Net, Attention U-Net。
    *   **多模态/Transformer 模型**：CLIP（基准实现）, LViT（Language-Vision Transformer）。
*   **评估指标**：Dice 相似系数（Dice）和交并比（IoU）。

### 4. 资源与算力
*   **软件框架**：PyTorch。
*   **训练参数**：使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，权重衰减为 $1 \times 10^{-2}$，采用余弦退火学习率调度和预热（Warmup），训练 100 个 Epoch，Batch Size 为 8。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **架构搜索**：对比了 3 阶段、4 阶段和 5 阶段的变体，确定了 4 阶段为性能与复杂度的平衡点。
    *   **消融实验**：分别验证了“无文本引导”、“无 ConvFuse”和“无交叉注意力”的情况，证明了各组件的必要性。
    *   **基准对比**：与 4 种主流模型进行了定量对比。
*   **充分性评价**：实验设计较为标准，消融实验充分展示了多模态融合的优势。但实验仅限于单一数据集（QaTa-COV19），缺乏在其他病种或多中心数据集上的泛化性验证。

### 6. 主要结论与发现
*   **性能领先**：SwinTextUNet（4 阶段版）在 QaTa-COV19 上达到了 **86.47% 的 Dice** 和 **78.2% 的 IoU**，优于所有对比基准。
*   **文本的重要性**：消融实验显示，移除文本引导后 Dice 分数下降了 7.3%，说明语义先验对处理复杂病变至关重要。
*   **深度平衡**：增加到 5 阶段虽然能微幅提升性能，但参数量翻倍（从 57.6M 增至 105.4M），因此 4 阶段是实际应用的最佳选择。

### 7. 优点
*   **多模态对齐**：成功将 CLIP 的强大语义理解能力引入到像素级的医学分割任务中。
*   **架构优化**：ConvFuse 模块有效结合了 Transformer 的全局建模能力和 CNN 的局部特征提取能力，使边缘分割更清晰。
*   **临床相关性**：利用放射报告文本作为输入，符合临床实际诊断逻辑。

### 8. 不足与局限
*   **数据局限性**：仅在 2D X 光图像上进行了测试，未验证其在 3D 影像（如 CT、MRI）上的表现。
*   **文本依赖性**：在实际临床流程中，高质量的实时文本标注并不总是可用；如果文本描述有误，可能会对分割结果产生负面干扰（论文未讨论鲁棒性风险）。
*   **算力细节缺失**：缺乏训练效率和推理速度的具体数据，难以评估其在实时诊断系统中的部署潜力。

（完）
