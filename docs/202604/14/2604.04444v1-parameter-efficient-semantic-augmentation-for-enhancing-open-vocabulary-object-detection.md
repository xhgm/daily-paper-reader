---
title: Parameter-Efficient Semantic Augmentation for Enhancing Open-Vocabulary Object Detection
title_zh: 增强开放词汇目标检测的参数高效语义增强方法
authors: "Weihao Cao, Runqi Wang, Xiaoyue Duan, Jinchao Zhang, Ang Yang, Liping Jing"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04444v1"
tags: ["query:q14"]
score: 9.0
evidence: 利用语义增强提升针对未见类别的开放词汇目标检测
tldr: 针对开放词汇目标检测（OVOD）在特定领域因领域偏移和语义缺失导致的性能下降，本文提出 HSA-DINO 框架。该框架利用多尺度提示库从图像特征中提取层级语义，并结合语义感知路由动态选择增强策略。HSA-DINO 在保持预训练模型泛化性的同时，显著提升了垂直领域的检测性能，实现了领域适应性与泛化能力的优异平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04444v1/fig-001.webp\", \"caption\": \"Table 1. Comparison of different methods across various downstream tasks. “ZS” denotes the zero-shot performance of the pre-trained models, while “FFT” and “PEFT” denote the models’ performance after full fine-tuning or parameter-efficient fine-tuning on downstream tasks, respectively. mAPtgt denotes the performance of models on corresponding target downstream datasets (i.e., ArTaxOr, DIOR, UODD); mAPcoco denotes the performance of models on the OV-COCO. H denotes the harmonic mean value defined in Eq. 10.\", \"page\": 6, \"index\": 1, \"width\": 956, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2604.04444v1/fig-002.webp\", \"caption\": \"Table 2. Comparison of mAP across different methods on OVCOCO+. “w” indicates the additional dataset included for joint evaluation (e.g., “w ArTaxOr” means OV-COCO evaluated together with ArTaxOr).\", \"page\": 6, \"index\": 2, \"width\": 509, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.04444v1/fig-003.webp\", \"caption\": \"Figure 2. Method motivation. (a) Previous methods use predefined templates or learnable vectors prepended to the category label embeddings, ignoring detailed semantics from image features. Our multi-scale prompt bank uses hierarchical semantics from multi-scale feature pyramid to select auxiliary prompts for the category labels. (b) The dynamic routing method [33] uses the reconstruction error of an input, obtained from multiple autoencoders, as an indicator of its domain. However, the large overlap in reconstruction errors across different domains can confuse the model, leading to incorrect parameter selection. Our method explicitly models both the content and the domain of the inputs, effectively reducing this overlap and enabling more accurate routing decisions.\", \"page\": 2, \"index\": 3, \"width\": 753, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.04444v1/fig-004.webp\", \"caption\": \"Figure 3. Overview of the proposed HSA-DINO framework. We incorporate LoRA into the image encoder during training on downstream datasets. We also introduce a multi-scale prompt bank to further enhance the model’s adaptability to downstream tasks. In the test stage, we propose a semantic-aware router that can accurately identify different data distributions, enabling more precise selection between pretrained semantics and augmented semantics for detection.\", \"page\": 4, \"index\": 4, \"width\": 848, \"height\": 424}]"
motivation: 现有 OVOD 模型在处理领域偏移较大的下游任务时，因类别标签语义稀疏且无法捕捉细粒度辅助信息而导致性能严重下滑。
method: 提出 HSA-DINO 框架，通过多尺度提示库捕捉层级语义并利用语义感知路由在推理时动态选择最优的增强策略。
result: 实验证明 HSA-DINO 在 OV-COCO 和多个垂直领域数据集上均优于现有方法，在领域适应性与泛化性之间达到了最佳平衡。
conclusion: 该研究表明参数高效的语义增强能有效弥补类别标签的语义不足，是提升开放词汇检测模型领域迁移能力的有效途径。
---

## 摘要
开放词汇目标检测（OVOD）使模型能够检测任何物体类别，包括未见过的类别。得益于大规模预训练，现有的 OVOD 方法在通用场景（如 OV-COCO）上取得了强大的检测性能，但在迁移到具有显著领域偏移的下游任务时，性能会出现严重下降。这种退化源于特定领域任务中类别标签的稀缺性和弱语义性，以及现有模型无法捕捉粗粒度类别标签之外的辅助语义。为了解决这些问题，我们提出了 HSA-DINO，这是一个用于增强开放词汇目标检测的参数高效语义增强框架。具体而言，我们提出了一个多尺度提示库，利用图像特征金字塔来捕捉层级语义并选择特定领域的局部语义提示，从而将文本表示从粗粒度到细粒度逐步丰富。此外，我们引入了一个语义感知路由器，在推理过程中动态选择合适的语义增强策略，从而防止参数更新降低预训练 OVOD 模型的泛化能力。我们在 OV-COCO、多个垂直领域数据集以及修改后的基准设置上对 HSA-DINO 进行了评估。结果表明，HSA-DINO 的表现优于之前的最先进方法，在领域适应性和开放词汇泛化能力之间实现了卓越的权衡。

## Abstract
Open-vocabulary object detection (OVOD) enables models to detect any object category, including unseen ones. Benefiting from large-scale pre-training, existing OVOD methods achieve strong detection performance on general scenarios (e.g., OV-COCO) but suffer severe performance drops when transferred to downstream tasks with substantial domain shifts. This degradation stems from the scarcity and weak semantics of category labels in domain-specific task, as well as the inability of existing models to capture auxiliary semantics beyond coarse-grained category label. To address these issues, we propose HSA-DINO, a parameter-efficient semantic augmentation framework for enhancing open-vocabulary object detection. Specifically, we propose a multi-scale prompt bank that leverages image feature pyramids to capture hierarchical semantics and select domain-specific local semantic prompts, progressively enriching textual representations from coarse to fine-grained levels. Furthermore, we introduce a semantic-aware router that dynamically selects the appropriate semantic augmentation strategy during inference, thereby preventing parameter updates from degrading the generalization ability of the pre-trained OVOD model. We evaluate HSA-DINO on OV-COCO, several vertical domain datasets, and modified benchmark settings. The results show that HSA-DINO performs favorably against previous state-of-the-art methods, achieving a superior trade-off between domain adaptability and open-vocabulary generalization.

---

## 论文详细总结（自动生成）

以下是对论文《Parameter-Efficient Semantic Augmentation for Enhancing Open-Vocabulary Object Detection》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的开放词汇目标检测（OVOD）模型虽然在通用场景（如 COCO）表现优异，但在迁移到**垂直领域**（如遥感、水下、昆虫分类）时性能大幅下降。
*   **研究背景**：
    *   **语义稀疏性**：垂直领域的类别标签（如特定物种名）在预训练阶段极少出现，导致文本表示能力弱。
    *   **细粒度缺失**：现有方法多采用固定模板（如 "a photo of a [CLS]"），无法捕捉图像中丰富的层级语义。
    *   **泛化性冲突**：对下游任务进行全参数微调（FFT）或简单的参数高效微调（PEFT）往往会导致模型“忘记”通用的开放词汇能力，造成在通用数据集上的性能崩溃。

### 2. 核心方法论
论文提出了 **HSA-DINO** 框架，其核心思想是通过图像特征驱动的语义增强来弥补文本端的不足，并利用动态路由保护泛化性。
*   **多尺度提示库 (Multi-Scale Prompt Bank, MSPB)**：
    *   利用视觉骨干网络（Swin Transformer）输出的多尺度特征图（$z_s$）。
    *   构建一个包含 $N$ 个 (Key, Prompt) 对的库。通过计算图像特征与 Key 的余弦相似度，为每个尺度选择最相关的 Prompt。
    *   将选中的多尺度提示词与类别标签拼接，输入文本编码器，生成更具领域针对性的细粒度文本特征。
*   **语义感知路由器 (Semantic-Aware Router, SAR)**：
    *   **核心逻辑**：在推理时动态决定使用“增强语义”还是“原始预训练语义”。
    *   **技术细节**：为了避免领域信息干扰，SAR 显式建模**内容嵌入**（Content Embedding）和**领域统计数据**（均值和标准差）。通过自编码器重构内容特征，计算重构误差 $d_{err}$。
    *   **决策机制**：若 $d_{err}$ 低于阈值 $\tau$，判定为下游领域，使用增强语义；否则使用原始语义。
*   **优化目标**：结合了 DINO 的标准检测损失、提示词匹配损失（$L_m$）和提示词正交损失（$L_p$，用于保证提示词的多样性）。

### 3. 实验设计
*   **数据集与场景**：
    *   **通用领域**：OV-COCO（评估开放词汇泛化能力）。
    *   **垂直领域**：ArTaxOr（节肢动物检测）、DIOR（光学遥感检测）、UODD（水下生物检测）。
    *   **综合评估**：提出了 **OV-COCO+** 基准，将通用类与垂直类混合进行联合评估。
*   **对比方法**：
    *   **SOTA 模型**：GLIP, Grounding DINO, YOLO-World, OV-DINO。
    *   **微调策略**：零样本（ZS）、全参数微调（FFT）、参数高效微调（PEFT，如 LoRA、ZiRa、MR-GDINO）。
*   **评价指标**：采用调和平均值 $H$（平衡目标领域 mAP 和 COCO mAP）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：
    *   下游任务微调 24 个 epoch，Batch Size 为 16。
    *   使用 AdamW 优化器，学习率为 1e-3。
    *   提示库大小 $N=10$，提示词长度 $M=12$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个垂直领域数据集上进行了详尽的对比实验。
    *   **消融实验**非常充分：涵盖了 V-LoRA、MSPB、SAR 各组件的有效性，以及提示库大小 $N$、提示词长度 $M$、损失权重 $\lambda$ 和路由阈值 $\tau$ 的敏感度分析。
    *   **可视化分析**：通过 Grad-CAM 可视化了提示词的热力图，并展示了重构误差的概率分布图。
*   **客观性**：实验对比了多种微调策略和最新的 OVOD 模型，使用了调和平均值 $H$ 这一较为公平的指标来衡量“性能-泛化”权衡，结果具有说服力。

### 6. 主要结论与发现
*   **性能提升**：HSA-DINO 在所有下游任务中均取得了最高的 $H$ 值，显著优于之前的 PEFT 方法（如 ZiRa）。
*   **泛化保护**：在大幅提升垂直领域检测能力的同时，HSA-DINO 几乎没有损失在 OV-COCO 上的性能（例如在 ArTaxOr 任务中，COCO mAP 仅从 50.6 降至 49.9，而其他方法通常降至 20-30 左右）。
*   **路由有效性**：显式建模内容与领域的 SAR 路由比传统的 DDAS 路由能更准确地识别数据分布，减少了重构误差的重叠。

### 7. 优点与亮点
*   **设计巧妙**：将视觉特征反馈给文本端进行“语义补全”，解决了 OVOD 在特定领域“词穷”的问题。
*   **参数高效**：仅通过 LoRA 和轻量级提示库实现增强，无需重新训练庞大的视觉-语言模型。
*   **动态自适应**：SAR 路由机制解决了“微调即退化”的痛点，使模型具备了在通用和专业场景间自动切换的能力。

### 8. 不足与局限
*   **阈值依赖**：路由决策高度依赖超参数 $\tau$，虽然论文给出了推荐值，但在面对全新领域时可能需要重新调优。
*   **任务特定性**：虽然论文尝试了“统一路由”，但实验显示针对特定任务训练的路由效果更好，这在一定程度上增加了部署时的复杂性。
*   **算力信息缺失**：缺乏训练时长和硬件需求的具体数据，不便于其他研究者评估其训练成本。

（完）
