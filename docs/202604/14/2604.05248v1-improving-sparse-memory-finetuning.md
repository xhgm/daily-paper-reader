---
title: Improving Sparse Memory Finetuning
title_zh: 改进稀疏记忆微调
authors: "Satyam Goyal, Anirudh Kanchi, Garv Shah, Prakhar Gupta"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05248v1"
tags: ["query:q15"]
score: 9.0
evidence: 通过将更新定位到稀疏记忆模块来实现有效的持续学习
tldr: 本研究针对大语言模型在持续学习中面临的灾难性遗忘问题，提出了一种改进的稀疏记忆微调（SMF）方法。通过在预训练模型（如Qwen-2.5-0.5B）中嵌入稀疏记忆模块，并引入基于KL散度的槽位选择机制，优先更新对模型而言具有“信息增量”的令牌。该方法在开源流水线上实现了高效的知识更新，显著降低了对已有能力的干扰，为在消费级硬件上实现模型持续进化提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05248v1/fig-001.webp\", \"caption\": \"Figure 2: Recovery phase training loss on the instruction dataset (using TF-IDF).\", \"page\": 4, \"index\": 1, \"width\": 498, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-002.webp\", \"caption\": \"Figure 3: Sparse update mechanism: only retrieved key/value entries receive gradients.\", \"page\": 4, \"index\": 2, \"width\": 582, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-003.webp\", \"caption\": \"Figure 4: Finetuning performance across datasets. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse memory variants.\", \"page\": 6, \"index\": 3, \"width\": 826, \"height\": 228}, {\"url\": \"assets/figures/arxiv/2604.05248v1/fig-004.webp\", \"caption\": \"Figure 5: Finetuning performance when adapting to SimpleQA. (a) TriviaQA training F1, (b) Natural Questions evaluation F1 during recovery, and (c) GSM8K evaluation loss for full finetuning and sparse finetuning variants.\", \"page\": 6, \"index\": 4, \"width\": 826, \"height\": 229}]"
motivation: 传统的全量微调或LoRA方法因修改共享密集表示，容易导致模型在学习新知识时出现严重的灾难性遗忘。
method: 开发了开源流水线为预训练模型植入稀疏记忆模块，并采用基于KL散度的机制来识别并更新信息量大的令牌。
result: 实验证明该方法能使模型在获取新事实知识的同时，极大地减少对原有能力的遗忘。
conclusion: 该研究验证了稀疏更新假设在实际应用中的有效性，为大模型的持续适应提供了高效且低损耗的路径。
---

## 摘要
大语言模型（LLMs）在训练后通常是静态的，但实际应用要求模型能够持续适应新知识，且不降低现有能力。标准的模型更新方法，如全量微调或参数高效微调方法（如 LoRA），面临着一个根本性的权衡：灾难性遗忘。这些方法修改了共享的密集表示，从而导致任务间的干扰。稀疏记忆微调（SMF）提供了一种极具前景的替代方案，它通过将更新局域化到显式记忆层中的一小部分参数子集来实现。在本研究中，我们提出了一个开源流水线，用于为现有的预训练模型（Qwen-2.5-0.5B）加装稀疏记忆模块，从而在消费级硬件上实现有效的持续学习。我们通过引入一种基于 Kullback-Leibler (KL) 散度的、具有理论依据的插槽选择机制扩展了前人的工作，该机制优先为相对于背景分布而言具有信息“惊奇性”的标记（tokens）进行记忆更新。我们的实验表明，加装该模块的模型能够获取新的事实性知识，同时对保留能力的遗忘极小，从而在实际场景中验证了稀疏更新假设。

## Abstract
Large Language Models (LLMs) are typically static after training, yet real-world applications require continual adaptation to new knowledge without degrading existing capabilities. Standard approaches to updating models, like full finetuning or parameter-efficient methods (e.g., LoRA), face a fundamental trade-off: catastrophic forgetting. They modify shared dense representations, causing interference across tasks. Sparse Memory Finetuning (SMF) offers a promising alternative by localizing updates to a small subset of parameters in explicit memory layers. In this work, we present an open-source pipeline to retrofit existing pretrained models (Qwen-2.5-0.5B) with sparse memory modules, enabling effective continual learning on consumer hardware. We extend prior work by introducing a theoretically grounded slot-selection mechanism based on Kullback-Leibler (KL) divergence, which prioritizes memory updates for informationally "surprising" tokens relative to a background distribution. Our experiments demonstrate that our retrofitted models can acquire new factual knowledge with minimal forgetting of held-out capabilities, validating the sparse update hypothesis in a practical setting.

---

## 论文详细总结（自动生成）

这篇论文《Improving Sparse Memory Finetuning》针对大语言模型（LLM）在持续学习中的“灾难性遗忘”问题提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：LLM 在训练后通常是静态的，但在实际应用中需要不断吸收新知识。传统的全量微调或参数高效微调（如 LoRA）会修改共享的密集表示，导致新旧知识相互干扰，产生严重的**灾难性遗忘**。
*   **研究背景**：现有的稀疏记忆微调（SMF）虽然通过局部更新减少了干扰，但往往依赖于简单的启发式方法（如 TF-IDF）来选择更新位置，且通常绑定在特定的专有架构上。

### 2. 方法论
论文提出了一个开源的改造流水线，将预训练模型转换为支持稀疏更新的记忆增强模型。
*   **核心思想**：将 Transformer 中的前馈网络（FFN）替换为稀疏的键值（Key-Value）记忆层。在前向传播时，仅激活前 $k$ 个最相关的记忆槽位（Slots），在反向传播时，仅更新这些被激活的参数。
*   **关键技术细节**：
    1.  **三阶段流水线**：
        *   **改造（Retrofitting）**：移除 Qwen-2.5-0.5B 的部分 FFN 层，替换为随机初始化的记忆层。
        *   **恢复（Healing）**：在通用指令数据集（OpenAssistant）上微调记忆参数，使模型恢复基础对话能力。
        *   **任务特定微调**：在目标任务上进行稀疏更新。
    2.  **KL 散度槽位选择机制**：这是本文的核心创新。通过计算当前批次（Batch）的槽位使用分布与背景分布之间的 **KL 散度**，识别出那些相对于背景知识而言具有“信息增量”或“惊奇性”的标记，并优先更新这些槽位。
*   **公式逻辑**：通过梯度掩码（Gradient Masking）确保只有选定的 $T$ 个槽位接收梯度更新，其余参数保持冻结。

### 3. 实验设计
*   **模型与数据集**：
    *   **基础模型**：Qwen-2.5-0.5B-Instruct。
    *   **目标任务（可塑性测试）**：TriviaQA（事实问答）、SimpleQA（文档级问答）。
    *   **保留任务（稳定性测试）**：GSM8K（数学推理）、Natural Questions（通用知识）。
*   **对比基准（Benchmark）**：
    *   **全量微调（Full Finetuning）**：更新模型所有密集权重。
    *   **SMF (TF-IDF)**：基于词频-逆文档频率启发式方法的稀疏更新。
    *   **SMF (KL-Divergence)**：本文提出的基于信息论的稀疏更新。

### 4. 资源与算力
*   **算力说明**：论文强调该方法旨在使持续学习能在**消费级硬件**上运行。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练时长，但提到在恢复阶段使用了 20,000 个样本，在任务微调阶段使用了 1,000 个样本。由于模型规模较小（0.5B），推测计算需求较低。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了两种不同的目标任务场景（TriviaQA 和 SimpleQA），并分别在两个保留任务上监测遗忘情况。
*   **充分性评价**：实验涵盖了“学习新知识”与“保留旧能力”的双重维度，验证了 KL 散度机制在不同任务下的表现。虽然实验在 0.5B 模型上进行，但对于验证“稀疏更新减少干扰”这一假设是充分且客观的。

### 6. 主要结论与发现
*   **稀疏更新的优越性**：相比全量微调，SMF 在学习新事实知识时速度更快，且对 GSM8K 等推理能力的破坏显著更小。
*   **KL 机制的有效性**：基于 KL 散度的槽位选择在处理复杂任务（如 SimpleQA）时表现出更好的稳定性，能有效防止噪声更新导致的模型漂移。
*   **权衡关系**：实验发现存在“稳定性-可塑性”的权衡，KL 机制在某些简单任务中可能因约束过强而略微减缓学习速度，但在保持模型长期稳定性方面更具优势。

### 7. 优点
*   **开源贡献**：提供了可复现的流水线，允许研究者将现有 Transformer 模型改造为记忆增强架构。
*   **理论支撑**：将槽位选择从简单的启发式方法提升到了信息论层面（KL 散度），更具科学性。
*   **高效性**：通过梯度掩码实现极低参数量的更新，适合资源受限的场景。

### 8. 不足与局限
*   **模型规模限制**：实验仅在 0.5B 的小模型上验证，尚未在大规模模型（如 7B 或 70B）上展示扩展性。
*   **层选择启发式**：改造时选择替换哪些 FFN 层（如第 8, 12, 16 层）仍基于前人的经验法则，缺乏自动化的层选择机制。
*   **任务覆盖面**：虽然测试了推理和问答，但对于长文本生成、代码编写等更复杂能力的保留情况探讨较少。

（完）
