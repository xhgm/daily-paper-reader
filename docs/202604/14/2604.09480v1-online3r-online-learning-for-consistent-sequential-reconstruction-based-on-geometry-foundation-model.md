---
title: "Online3R: Online Learning for Consistent Sequential Reconstruction Based on Geometry Foundation Model"
title_zh: Online3R：基于几何基础模型的连贯序列重建在线学习
authors: "Shunkai Zhou, Zike Yan, Fei Xue, Dong Wu, Yuchen Deng, Hongbin Zha"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09480v1"
tags: ["query:q10"]
score: 9.0
evidence: 用于一致性序列三维重建的在线学习
tldr: Online3R是一个基于几何基础模型的在线学习序列重建框架，旨在解决新场景下的重建不一致性。它通过在冻结的基础模型中引入轻量级视觉提示，并结合局部-全局自监督学习策略，在无需真值的情况下实现高效在线适配。该方法在多个基准测试中超越了现有SOTA，显著提升了长序列重建的质量与一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09480v1/fig-001.webp\", \"caption\": \"Figure 3. Qualitative results for reconstruction of non-overlapping image pairs.\", \"page\": 8, \"index\": 1, \"width\": 477, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.09480v1/fig-002.webp\", \"caption\": \"Table 3. Quantitative 3D reconstruction results on 7-scenes and NRGBD datasets. We categorize methods into “Offline” and “Online” [29], and use “GA” to mark methods with global alignment. The best and the second best results of all methods are highlighted. Our method achieves competitive or better performance than those offline or online methods.\", \"page\": 7, \"index\": 2, \"width\": 879, \"height\": 294}, {\"url\": \"assets/figures/arxiv/2604.09480v1/fig-003.webp\", \"caption\": \"Figure 2. Qualitative Comparison on 3D Reconstruction Consistency. We present reconstruction results on two sequences, with 7-Scenes-heads on the left and NRGBD-staircase on the right, separated by dashed lines. The first row shows the global point cloud reconstruction from a far viewpoint. The second row zooms in to view the near viewpoint. The third row highlights details using bounding boxes. It is evident that our method achieves better consistency compared to MASt3R-SLAM*.\", \"page\": 7, \"index\": 3, \"width\": 1010, \"height\": 440}, {\"url\": \"assets/figures/arxiv/2604.09480v1/fig-004.webp\", \"caption\": \"Table 1. Absolute trajectory error (ATE (m)↓) on TUM RGB-D [23]. We evaluate methods with and without ground-truth camera intrinsics as inputs (noted as “Calibrated” and “Uncalibrated”). The best and the second best results of each type are highlighted.\", \"page\": 6, \"index\": 4, \"width\": 1018, \"height\": 427}, {\"url\": \"assets/figures/arxiv/2604.09480v1/fig-005.webp\", \"caption\": \"Figure 1. Overview of our proposed Online3R. The core of our Online3R lies in constructing self-supervised methods and online prompt tuning, enabling the model to adapt to the current scene and ensuring consistent reconstruction results. We leverage a local consistency loss derived from temporally fused geometry to enhance the accuracy of subsequent predictions, and a global consistency loss that enforces geometric invariance across distant views to mitigate long-term drift, guaranteeing both consistency and efficiency.\", \"page\": 3, \"index\": 5, \"width\": 956, \"height\": 587}]"
motivation: 针对现有序列重建方法在面对新场景时存在的一致性差以及难以进行高效在线适配的问题。
method: 在冻结的几何基础模型中引入可学习视觉提示，并利用基于伪标签的局部约束和基于稀疏关键帧的全局约束进行自监督在线更新。
result: 实验结果表明，Online3R 在多个主流基准测试中均取得了优于现有最先进方法的性能表现。
conclusion: 该研究证明了结合基础模型与在线自监督学习能有效提升序列重建的几何一致性与环境适应能力。
---

## 摘要
我们提出了 Online3R，这是一种全新的序列重建框架，能够通过在线学习适应新场景，从而有效解决不一致性问题。具体而言，我们在预训练且冻结的几何基础模型中引入了一组可学习的轻量级视觉提示（visual prompts），以捕获新环境的知识，同时保留基础模型在几何预测方面的基本能力。为了解决测试阶段更新这些视觉提示时面临的真值（groundtruth）缺失和高效性要求问题，我们提出了一种局部-全局自监督学习策略，通过对预测结果施加局部和全局一致性约束来实现。局部一致性约束作用于中间结果和先前的局部融合结果，使模型能够利用高质量的伪真值信号进行训练；全局一致性约束则作用于跨越长距离的稀疏关键帧而非逐帧进行，从而使模型能够以高效的方式从长轨迹上的连贯预测中学习。我们的实验表明，Online3R 在各种基准测试中均优于现有的最先进方法。

## Abstract
We present Online3R, a new sequential reconstruction framework that is capable of adapting to new scenes through online learning, effectively resolving inconsistency issues. Specifically, we introduce a set of learnable lightweight visual prompts into a pretrained, frozen geometry foundation model to capture the knowledge of new environments while preserving the fundamental capability of the foundation model for geometry prediction. To solve the problems of missing groundtruth and the requirement of high efficiency when updating these visual prompts at test time, we introduce a local-global self-supervised learning strategy by enforcing the local and global consistency constraints on predictions. The local consistency constraints are conducted on intermediate and previously local fused results, enabling the model to be trained with high-quality pseudo groundtruth signals; the global consistency constraints are operated on sparse keyframes spanning long distances rather than per frame, allowing the model to learn from a consistent prediction over a long trajectory in an efficient way. Our experiments demonstrate that Online3R outperforms previous state-of-the-art methods on various benchmarks. Project page: https://shunkaizhou.github.io/online3r-1.0/

---

## 论文详细总结（自动生成）

以下是对论文《Online3R: Online Learning for Consistent Sequential Reconstruction Based on Geometry Foundation Model》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的几何基础模型（如 DUSt3R, MASt3R）虽然在静态图像对上表现优异，但在处理连续视频流（序列重建）时，由于模型参数固定，难以适应全新的场景特征，导致重建结果在时间序列上缺乏一致性，容易产生漂移和重影。
*   **研究动机**：作者认为不可能训练一个能完美适配所有场景的通用模型。因此，赋予模型在测试阶段（Test-time）针对特定环境进行“在线适配”的能力至关重要。

### 2. 方法论
Online3R 建立在冻结的 MASt3R 基础模型之上，核心思想是通过**在线提示微调（Online Prompt Tuning）**实现场景自适应。
*   **视觉提示（Visual Prompts）**：在基础模型的 ViT 编码器层中插入少量可学习的轻量级参数（Prompts）。这些提示充当任务特定的指令，引导冻结的模型捕获当前场景的几何先验，而无需修改原始模型的庞大权重。
*   **局部-全局自监督学习策略**：
    *   **局部一致性约束（Local Consistency）**：利用 SLAM 系统中历史帧融合生成的“置信度加权平均点图”作为伪真值（Pseudo GT），监督当前帧的预测，将多视图融合的知识蒸馏回前馈网络。
    *   **全局一致性约束（Global Consistency）**：随机采样历史关键帧，强制要求模型对同一关键帧的几何预测在不同参考视角下保持一致（几何不变性），从而抑制长距离漂移。
*   **算法流程**：每当系统识别到新关键帧时，触发一次在线优化，通过 AdamW 优化器更新视觉提示参数。

### 3. 实验设计
*   **数据集**：
    *   **相机位姿评估**：TUM RGB-D、NRGBD 数据集。
    *   **稠密几何重建评估**：7-Scenes、NRGBD 数据集。
*   **Benchmark 与对比方法**：
    *   **传统/深度 SLAM**：ORB-SLAM3, DROID-SLAM, GO-SLAM 等。
    *   **基于基础模型的序列重建**：Spann3R, CUT3R, Point3R, MASt3R-SLAM 等。
*   **评估模式**：区分了“已知内参（Calibrated）”和“未知内参（Uncalibrated）”两种模式，重点展示了在无内参情况下的鲁棒性。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100 GPU** 上完成。
*   **效率**：系统运行速度约为 **10 FPS**。
*   **参数量**：视觉提示长度为 32，维度为 1024，相比于基础模型，更新的参数量极小（通常小于 1%）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了位姿估计（ATE 指标）和稠密重建（Accuracy, Completion, Chamfer 指标）的多维度定量分析。
*   **消融实验**：专门验证了“仅局部约束”、“仅全局约束”以及“全约束”的效果，证明了两者结合的必要性。
*   **充分性评价**：实验设计较为充分，对比了从传统几何方法到最新基于 Transformer 的多种 SOTA 方法。通过可视化点云对比（如 Figure 2）直观展示了解决“重影”问题的效果，客观证明了在线学习的价值。

### 6. 主要结论与发现
*   **在线适配的必要性**：即使是强大的几何基础模型，在面对未见过的场景时也需要通过在线微调来消除预测偏差。
*   **性能领先**：Online3R 在位姿精度和重建质量上均超越了现有的在线重建方法，甚至在某些指标上优于离线的全局优化方法。
*   **隐式先验学习**：实验发现，经过在线学习的提示能够编码场景的隐式先验，甚至能帮助模型在无重叠视角的图像对上恢复出更合理的几何结构。

### 7. 优点
*   **参数高效**：采用 Prompt Tuning 避免了全参数微调的高昂代价，适合在线实时应用。
*   **无需真值**：完全基于自监督机制，利用时空一致性产生监督信号，解决了测试阶段无标签的问题。
*   **端到端一致性**：通过局部融合与全局采样的结合，兼顾了重建的细节精度和全局结构的稳定性。

### 8. 不足与局限
*   **计算开销**：虽然提示微调很轻量，但相比于纯前馈推理（如原始 MASt3R-SLAM），在线优化步骤仍带来了额外的计算负担（FPS 从 13.2 降至 10.0）。
*   **场景限制**：目前仅适用于**静态场景**的序列重建，无法处理包含移动物体的动态环境。
*   **初始化依赖**：系统性能仍受限于基础模型（MASt3R）的初始预测质量，若初始误差过大，自监督优化可能陷入局部最优。

（完）
