---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v1"
tags: ["query:q11"]
score: 9.0
evidence: 用于细粒度推理和理解的高效视觉语言模型
tldr: Firebolt-VL 是一款高效的视觉语言模型，旨在解决多模态大模型计算成本高及细粒度理解能力不足的问题。该模型采用 Liquid Foundation Model (LFM) 替代传统的 Transformer 解码器，并引入 Token-Grid 相关性模块，通过 SSM 和 FiLM 条件化实现轻量级的跨模态调制。这使得模型能在保持线性推理复杂度的同时，精准捕捉与文本相关的视觉区域，在多个基准测试中实现了高效且准确的细粒度推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04579v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison of the proposed Firebolt-VL model with existing MLLMs across seven benchmarks. The superscript p denotes the perception score on the MME benchmark, while SQAI refers the ScienceQA-IMG [30]. The best results are shown in bold, and the second-best results are underlined. “–” indicates results not reported in the original papers.\", \"page\": 6, \"index\": 1, \"width\": 1038, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of responses from Firebolt-VL with recent efficient vision–language models, including MobileVLM V2 [10] and SmolVLM2 [31], on detail-dependent question-answering tasks. Firebolt-VL demonstrates stronger fine-grained grounding and more accurate, instruction-aligned responses.\", \"page\": 6, \"index\": 2, \"width\": 891, \"height\": 589}, {\"url\": \"assets/figures/arxiv/2604.04579v1/fig-003.webp\", \"caption\": \"Figure 2. Overview of the Firebolt-VL architecture. The Cross-Modal Modulator (CMM) fuses textual instructions with the visual representations of the query image to produce conditioned tokens, which are then processed by the Liquid Foundation Model (LFM). The model is trained in two stages: (1) CMM pre-training to initialize modulation parameters, and (2) end-to-end training of the full framework.\", \"page\": 4, \"index\": 3, \"width\": 949, \"height\": 448}]"
motivation: 现有的多模态大模型因 Transformer 的二次复杂度导致计算成本高昂，且小型模型在处理细粒度视觉推理任务时难以精准定位关键区域。
method: 采用 Liquid Foundation Model (LFM) 作为解码器，并结合 Token-Grid 相关性模块与 FiLM 条件化的状态空间模型（SSM）来增强跨模态调制。
result: 实验结果表明，Firebolt-VL 在多个基准测试中实现了准确的细粒度理解，并显著提升了推理效率。
conclusion: Firebolt-VL 证明了通过结合线性复杂度的解码器和高效的跨模态调制机制，可以在资源受限的环境下实现高性能的视觉语言理解。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在视觉-语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次方复杂度阻碍了效率。此外，小型视觉-语言模型往往难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了它们在现实世界中的有效性。为了解决这些问题，我们推出了 Firebolt-VL，这是一种高效的视觉-语言模型，它将基于 Transformer 的解码器替换为液体基础模型（Liquid Foundation Model, LFM）解码器。为了进一步增强视觉定位，我们提出了一个令牌-网格相关性模块（Token-Grid Correlation Module），该模块计算文本令牌与图像块之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型进行调制。这使得模型能够在保持线性时间推理的同时，选择性地强调与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 在显著提高效率的同时，实现了准确的细粒度理解。我们的模型和代码可在以下网址获取：https://fireboltvl.github.io

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

以下是对论文《Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）主要面临两大挑战：
    1.  **计算成本高昂**：大多数模型依赖 Transformer 架构，其注意力机制具有二次方计算复杂度，导致在资源受限设备（如智能手机、摄像头）上推理缓慢且内存占用高。
    2.  **细粒度理解不足**：小型视觉语言模型在处理需要精确视觉定位的任务（如 OCR、复杂图表推理）时，往往无法准确捕捉与任务相关的局部视觉区域。
*   **研究背景**：为了在保持高效的同时提升性能，研究者们开始探索非 Transformer 架构（如状态空间模型 SSM）以及更高效的跨模态对齐机制。

### 2. 论文提出的方法论
Firebolt-VL 的核心思想是结合**线性复杂度解码器**与**动态跨模态调制机制**。
*   **核心架构**：
    *   **视觉编码器**：采用 SigLIP 提取图像的网格级（Grid-level）嵌入。
    *   **语言解码器**：放弃 Transformer，采用 **Liquid Foundation Model (LFM)**，这是一种基于液体假设的序列模型，具有线性推理复杂度。
    *   **跨模态调制器 (CMM)**：这是本文的核心创新，包含以下步骤：
        1.  **令牌-网格相关性（Token-Grid Correlation）**：计算文本令牌与图像网格之间的相关性矩阵，并仅保留前 $k$ 个最相关的视觉区域（Top-k），以减少噪声。
        2.  **FiLM 调制**：利用特征线性调制（FiLM）技术，根据视觉上下文对文本表示进行动态缩放和偏移。
        3.  **SSM 处理**：使用结构化状态空间模型（如 S4）对调制后的特征进行序列建模，以 $O(T)$ 的线性时间复杂度捕捉长距离依赖。
        4.  **残差与前馈网络**：进一步稳定和增强融合后的多模态表示。

### 3. 实验设计
*   **数据集**：
    *   **预训练阶段**：使用 CC3M 数据集初始化 CMM 模块。
    *   **指令微调阶段**：使用 LLaVA-CoT 和处理后的 MMPR-v1.2 数据集进行端到端训练。
*   **基准测试 (Benchmarks)**：涵盖了 7 个主流榜单，包括 VQAv2（视觉问答）、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、SQA-I（科学问答）和 MMB（综合能力）。
*   **对比方法**：
    *   **大型模型 (>7B)**：IDEFICS, OpenFlamingo v2, MiniGPT-4, Chameleon, FastV 等。
    *   **小型高效模型 (<3B)**：Kosmos-2, MobileVLM (V1/V2), MoE-LLaVA, LLaVA-Phi, SmolVLM2。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA H100 80GB GPU**。
*   **训练细节**：
    *   第一阶段（CMM 预训练）：Batch size 为 128，学习率 $5 \times 10^{-4}$。
    *   第二阶段（全模型微调）：Batch size 为 8，学习率 $1 \times 10^{-4}$。
    *   两个阶段各训练 5 个 Epoch，通常在第 2 个 Epoch 即可获得最佳模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个不同的基准数据集上进行了定量评估，并提供了丰富的定性可视化对比。
*   **消融实验**：
    1.  **SSM 变体对比**：对比了 Mamba、S4D 和 S4，发现结构化 SSM（S4/S4D）在视觉任务中优于 Mamba。
    2.  **融合策略对比**：对比了直接拼接（Prepend）、交叉注意力（Cross-attention）和 CMM，证明了 CMM 的优越性。
    3.  **Top-k 参数分析**：测试了 $k=1$ 到 $5$ 的影响，确定 $k=4$ 为最佳平衡点。
*   **充分性评价**：实验设计较为全面，不仅对比了同量级的轻量级模型，还跨级对比了 7B 以上的大模型，验证了其在效率和精度上的双重优势。

### 6. 论文的主要结论与发现
*   **性能卓越**：Firebolt-VL（仅 0.8B 参数）在 MME 和 VQAv2 等感知任务上超越了许多 7B 级别的模型，并在 MME 感知评分上达到了 1376.2 的高分。
*   **效率极高**：其吞吐量达到 **46.67 tokens/s**，是对比模型中最快的，显著优于 SmolVLM2 和 MobileVLM V2。
*   **细粒度能力强**：通过 CMM 模块，模型能更精准地定位图像中的微小细节（如搜索栏文字、食品成分标签），减少了小型模型常见的“泛泛而谈”问题。

### 7. 优点
*   **架构创新**：成功将 Liquid Foundation Model 和 SSM 应用于多模态领域，打破了 Transformer 的复杂度瓶颈。
*   **高效对齐**：提出的 CMM 模块通过 Top-k 机制和 FiLM 调制，实现了比传统交叉注意力更轻量且更具针对性的特征融合。
*   **精度与速度平衡**：在极小的参数量下实现了极高的推理速度，非常适合端侧部署。

### 8. 不足与局限
*   **任务局限**：目前仅支持单图输入，尚未扩展到多图或长视频序列。
*   **物体幻觉风险**：在 POPE 基准测试中表现略逊于部分模型，论文推测这可能是由于 SSM 的隐式状态传播在抑制幻觉方面不如显式注意力机制有效。
*   **训练数据规模**：相比于一些动辄使用数亿数据的模型，其预训练数据量（CC3M）相对较小，可能限制了其开放域知识的上限。

（完）
