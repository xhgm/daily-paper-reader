---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明、开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于机器人操作的高效视觉-语言-动作模型
tldr: A1是一个开源且高效的视觉-语言-动作（VLA）模型框架，旨在解决大模型在机器人操控中推理成本高、延迟大的问题。该研究通过引入预算感知的自适应推理方案，结合中间层动作一致性监控实现提前终止，并利用跨层截断流匹配加速动作生成。A1在保持甚至超越SOTA性能的同时，显著降低了计算开销，为低成本硬件上的实时机器人控制提供了全栈开源解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05672v1/fig-001.webp\", \"caption\": \"Table 5 Adaptive early-exit inference with A1-MLP. Average accuracy, per-episode computation cost (TFLOPs) and model inference time (second) under different exit criteria c on the LIBERO benchmark. † Full-layer training. ‡ Multi-exit training.\", \"page\": 11, \"index\": 1, \"width\": 736, \"height\": 199}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-002.webp\", \"caption\": \"Table 6 Adaptive early-exit inference with A1-FM. Accuracy, per-episode computation cost (TFLOPs) and model inference time (second) under different exit criteria c and denoising steps δ on the LIBERO benchmark. ∗ Layer i+ 1 denoising initialized with layer i output (Eq. (10)). † Full-layer training. ‡ Multi-exit training.\", \"page\": 11, \"index\": 2, \"width\": 718, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-003.webp\", \"caption\": \"Figure 1 Overview of A1. A1 comprises a Vision Language Model (VLM) backbone and an action head, where the VLM provides semantically rich and affordance-aware representations for downstream action prediction. We instantiate the latter in two forms: a flow-matching head and an MLP head. To reduce end-to-end inference latency, we introduce a budget-aware acceleration scheme that is compatible with both action-head designs and jointly reduces backbone computation and action-head iterations. Extensive experiments in simulators, on real hardware, and on RoboChallenge demonstrate that A1 achieves strong manipulation performance with substantially improved efficiency.\", \"page\": 2, \"index\": 3, \"width\": 982, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-004.webp\", \"caption\": \"Table 1 Experimental Results of Simulation Benchmarks (%).\", \"page\": 7, \"index\": 4, \"width\": 944, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-005.webp\", \"caption\": \"Table 4 Comparison with state-of-the-art open-source VLA models on Table30 benchmark in RoboChallenge. List in success rate. Bold for highest. §Denotes fully open-source models with complete training stack, weights, and data pipelines.\", \"page\": 10, \"index\": 5, \"width\": 986, \"height\": 656}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-006.webp\", \"caption\": \"Table 8 Hyperparameters for pretraining A1.\", \"page\": 18, \"index\": 6, \"width\": 527, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-007.webp\", \"caption\": \"Figure 2 Training and adaptive inference of A1. During training, for each layer i of VLM, the shared action head generates actions A (i) t . For flow matching, the model is executed with the same number of layers i to generate predictions. All actions are supervised simultaneously. At inference time, we adaptively activate an appropriate size of VLM based on an exit criterion c. The threshold ηi is generated by training set and c. When the VLM reaches the i-th layer, we computes the discrepancy between the current actions and those from the previous layer, and determines whether to terminate via ηi. We propose Inter-Layer Truncated Flow Matching to accelerate early-exit inference.\", \"page\": 4, \"index\": 7, \"width\": 962, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.05672v1/fig-008.webp\", \"caption\": \"Figure 4 Example of adaptive inference visualization for the A1 model with the exit criterion c = 0.6. A successful execution episode from the LIBERO-Long task (instruction: turn on the stove and put the moka pot on it). Green numbers indicate the layer indices where the A1 model exits (the model has 28 layers in total). At each frame, the model outputs actions for the next 8 time steps (action chunk = 8).\", \"page\": 9, \"index\": 8, \"width\": 986, \"height\": 181}]"
motivation: 针对现有VLA模型因大规模骨干网络和复杂动作生成头导致的实时控制成本高昂且延迟大的痛点。
method: 采用自适应推理机制，通过监控中间层一致性实现骨干网络提前终止，并结合跨层截断流匹配优化去噪迭代。
result: "在RoboChallenge等基准测试中超越了pi0和RDT-1B等模型，同时最高减少了72%的推理延迟和76.6%的计算量。"
conclusion: A1通过全栈开源和高效的推理优化，实现了高性能与低成本的平衡，推动了VLA模型在实际机器人任务中的部署。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数的 VLM 骨干网络和基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算开销，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操控成功率的前提下实现低成本、高吞吐量的推理。我们的方法利用预训练的 VLM 为动作生成提供隐式的示能性（affordance）先验。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了单独优化 VLM，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能同时加速骨干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），通过在层间热启动去噪过程，从而以显著减少的有效去噪迭代次数实现精确的动作生成。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的每回合延迟降低高达 72%，骨干网络计算量减少高达 76.6%，且性能下降极小），达到了最先进的成功率。在 RoboChallenge 上，A1 实现了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision--Language--Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by \emph{cost}: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the \emph{action head}. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

### A1: 完全透明、开源、自适应且高效的截断式视觉-语言-动作模型总结

#### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 $\pi_0$、OpenVLA）虽然在开放世界操控中表现出色，但面临严重的**推理成本瓶颈**。其高昂的开销源于两方面：一是数十亿参数的视觉语言模型（VLM）骨干网络，二是需要多次迭代去噪的扩散/流匹配动作头。这使得在普通硬件上实现实时控制（低延迟、高吞吐）变得非常困难且昂贵。
*   **核心目标**：开发一个名为 **A1** 的全栈开源框架，通过自适应推理机制，在不损失操控成功率的前提下，大幅降低计算开销和推理延迟。

#### 2. 方法论
*   **核心思想**：**“只在动作发生变化时消耗计算资源”**。通过监控 VLM 中间层的动作预测一致性，实现计算路径的动态截断。
*   **关键技术细节**：
    *   **骨干网络与动作头**：采用 Molmo-7B 作为 VLM 骨干，提供丰富的视觉语义和示能性（Affordance）先验。动作头提供两种实现：流匹配（A1-FM）和 MLP（A1-MLP）。
    *   **自适应提前终止（Early-Exit）**：在训练时对 VLM 的所有层进行动作预测监督。推理时，计算第 $i$ 层与第 $i-1$ 层预测动作之间的差异（Discrepancy），若差异低于预设阈值 $\eta_i$，则停止后续层的计算直接输出动作。
    *   **层间截断流匹配（Inter-Layer Truncated Flow Matching）**：针对 FM 动作头，提出“热启动”策略。每一层的去噪不再从随机噪声开始，而是继承上一层的预测结果，并将每层的去噪步数 $\delta$ 压缩至极低（如 2 步），从而避免了动作头成为新的计算瓶颈。
    *   **阈值校准**：利用训练集数据分布，通过指数、高斯或伽马分布来设定不同计算预算下的退出阈值。

#### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：整合了 DROID、AgiBot、RoboCOIN、RoboMind、GM-100 等开源数据集，并加入 15,951 条自主采集的多机器人轨迹。
    *   **仿真基准**：LIBERO（侧重长程操控）、VLABench（侧重视觉语言理解）。
    *   **真实机器人**：在 Franka、AgiBot、ARX、UR5 等 4 种不同平台上进行验证。
    *   **综合评测**：RoboChallenge (Table30)，包含 30 个真实的机器人任务。
*   **对比方法**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、RDT-1B、X-VLA、Octo 等当前最先进的 VLA 模型。

#### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，预训练阶段全局 Batch Size 为 1024，总步数为 200K。
*   **微调配置**：针对不同任务（如 LIBERO, RoboChallenge），Batch Size 在 32 到 128 之间，步数为 50K-100K。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体数量，但从 1024 的 Batch Size 和 7B 参数规模来看，该模型需要在高性能计算集群上进行大规模并行训练。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从仿真到真实世界、从单臂到双臂、从固定基座到移动底座的多种场景，总计测试了超过 3000 条真实轨迹。
*   **消融实验**：针对退出准则（$c$ 值）、去噪步数（$\delta$）、热启动策略等关键变量做了详尽的对比实验。
*   **充分性与公平性**：实验设计较为客观，不仅对比了成功率，还详细分析了 FLOPs 和实际推理时间（Wall-clock time）。通过在 RoboChallenge 这一第三方基准上的表现，证明了其在开源模型中的领先地位。

#### 6. 主要结论与发现
*   **性能领先**：A1 在 RoboChallenge 上获得 29.00% 的平均成功率，超越了 $\pi_0$ 和 RDT-1B 等知名开源模型。
*   **极高的效率提升**：通过自适应推理，在 LIBERO 任务中，骨干网络计算量最高可减少 **76.6%**，FM 推理延迟降低 **72%**，且成功率仅有轻微下降。
*   **冗余性发现**：实验证明 VLM 的深层计算在许多简单动作中是冗余的，中间层已足以提供精确的控制信号；只有在遇到复杂或关键动作（如精准对准）时，模型才会自动运行至更深层。

#### 7. 优点
*   **全栈透明开源**：不仅公开权重，还公开了完整的训练代码、数据处理流水线和评估脚本，极大地促进了社区复现。
*   **端到端优化**：不同于以往只优化 VLM 或只优化动作头的研究，A1 实现了骨干网络与动作头的联合加速。
*   **部署友好**：自适应机制允许模型在不同算力的硬件上灵活调整性能与速度的平衡。

#### 8. 不足与局限
*   **数据依赖性**：预训练仍高度依赖高质量的机器人演示数据，未来需探索如何利用无标签的人类视频数据。
*   **模仿学习通病**：作为基于模仿学习的模型，在极长程任务中仍可能面临累积误差导致的鲁棒性问题。
*   **系统延迟**：虽然模型推理变快了，但在实际部署中，云端推理与本地控制之间的网络延迟、异步执行的平滑性仍是待解决的工程挑战。

（完）
