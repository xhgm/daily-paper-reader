---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:q11"]
score: 9.0
evidence: 多模态Transformer中的线性注意力机制用于视觉语言模型
tldr: 本研究探讨了线性注意力（LA）在多模态Transformer中的应用，旨在解决传统Softmax注意力随序列长度呈平方级增长的计算瓶颈。通过在LAION-400M数据集上训练不同规模的ViT模型，证明了线性注意力能将计算复杂度降至线性，同时遵循与标准注意力相同的缩放法则，为大规模多模态任务提供了高效且可扩展的替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 针对多模态Transformer中标准注意力机制计算复杂度过高、难以扩展到长序列的问题，寻求更高效的替代方案。
method: 在ViT-S/B/L等不同规模的架构中引入线性注意力机制，并在LAION-400M大规模数据集上进行训练与验证。
result: 实验表明线性注意力显著降低了计算开销，且在ImageNet-21K零样本准确率上表现出与标准注意力相似的缩放特性。
conclusion: 线性注意力是构建下一代高效、可扩展多模态Transformer的稳健解决方案，适用于处理日益增长的复杂数据集。
---

## 摘要
多模态 Transformer 是最先进视觉-语言模型的核心架构，但其注意力的二次方复杂度仍是限制可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（Linear Attention, LA）作为多模态框架内高效替代方案的可行性。通过集成 LA，我们将相对于序列长度的计算开销从二次方降低到线性，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，验证重点在于 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 softmax 注意力相同的缩放法则。这些发现将线性注意力定位为一种稳健且可扩展的解决方案，适用于处理日益庞大且复杂数据集的下一代多模态 Transformer。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这篇论文对线性注意力（Linear Attention, LA）在多模态 Transformer 中的应用进行了深入研究，旨在解决传统注意力机制在处理长序列时的计算瓶颈。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态 Transformer（如 CLIP）在处理视觉和文本信息时，其标准 Softmax 注意力机制的计算复杂度随序列长度 $N$ 呈二次方增长（$O(N^2)$）。这限制了模型处理高分辨率图像或长文本序列的能力。
*   **研究动机**：虽然线性注意力在纯语言或纯视觉任务中有所应用，但在大规模多模态表示学习中的有效性尚未得到充分验证。本文旨在探索 LA 是否能在降低计算开销的同时，保持与标准注意力相当的性能，并验证其是否遵循相同的缩放法则（Scaling Laws）。

### 2. 方法论
*   **核心思想**：利用矩阵乘法的结合律，将注意力计算顺序从 $(QK^T)V$ 改为 $Q(K^T V)$，从而将复杂度降为 $O(ND^2)$。
*   **关键技术细节**：
    *   **仿射核函数**：采用 $f(x) = 1 + x$ 作为核函数。
    *   **Q/K 归一化**：为了防止仿射核产生负值（导致行为异常），对 Query ($q_i$) 和 Key ($k_i$) 进行 L2 归一化，确保其点积在 $[-1, 1]$ 之间，从而使核输出非负。
    *   **改进的公式**：作者发现传统线性注意力公式中的分母（归一化项）会导致随序列增长而出现的“过度平滑”现象，使模型难以捕捉显著特征。
    *   **去分母策略**：作者提出移除分母，直接计算 $o_{ij} = \sum (1 + q_i \cdot k_n) v_{nj} / 2$。这种修改增强了注意力权重的对比度，提升了模型的表达能力。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 LAION-400M（包含 4 亿图文对的大规模数据集）。
    *   **验证/基准**：ImageNet-21K 零样本（Zero-shot）分类准确率。
*   **对比方法**：
    *   标准 Softmax 注意力（Standard Attention）。
    *   常规线性注意力（Regular Linear Attention）。
    *   本文改进的线性注意力（Proposed Linear Attention）。
*   **模型架构**：评估了三种规模的 Vision Transformer：ViT-S/16（22M 参数）、ViT-B/16（86M 参数）和 ViT-L/16（304M 参数）。

### 4. 资源与算力
*   **训练设备**：使用了 **4 台 NVIDIA A5500 GPU**。
*   **推理/运行时间测试**：在 **NVIDIA H200 GPU** 上进行，对比了 FlashAttention-2 与优化后的线性注意力内核。
*   **训练细节**：ViT-S、ViT-B、ViT-L 的全局 Batch Size 分别为 64、16 和 4。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 22M 到 304M 参数的三种模型规模，并在 4 亿规模的数据集上进行了训练。
*   **充分性评价**：实验设计较为充分。作者不仅对比了最终性能，还绘制了完整的训练曲线，并进行了运行时间随序列长度变化的定量分析。此外，通过拟合幂律公式验证了缩放法则，证明了结论在不同模型规模下的普适性。

### 6. 主要结论与发现
*   **性能对等**：改进后的线性注意力在最终收敛性能上与标准注意力基本持平。
*   **计算优势**：在序列长度达到 $10^3$ 时，LA 的效率优势开始显现；在极长序列（$4 \times 10^6$）下，计算时间比标准注意力低约 3 个数量级。
*   **缩放法则一致性**：实验证明线性注意力遵循与标准注意力相似的幂律缩放规律（$L(N) \approx aN^{-\alpha}$），这意味着 LA 具有良好的可扩展性。
*   **收敛速度**：虽然 LA 最终能达到相似精度，但其收敛速度比标准注意力慢，需要更多的训练轮数（Epochs）。

### 7. 优点
*   **解决了过度平滑问题**：通过简单的公式改进（去分母），显著提升了线性注意力在多模态任务中的训练稳定性和表达力。
*   **极高的扩展性**：线性复杂度的特性使其成为处理超高分辨率图像或超长上下文多模态任务的理想选择。
*   **严谨的验证**：通过 Scaling Law 的拟合，为 LA 在更大规模模型上的应用提供了理论支撑。

### 8. 不足与局限
*   **收敛效率**：LA 需要更长的训练时间才能达到与 Softmax 注意力相同的精度，这在一定程度上抵消了其单步计算速度的优势。
*   **评估维度单一**：主要关注 ImageNet-21K 的零样本准确率，未在更多样化的多模态任务（如视觉问答 VQA、图像检索等）上进行广泛验证。
*   **超参数敏感性**：虽然文中提到了归一化，但未深入讨论不同核函数或归一化策略对多模态对齐的具体影响。

（完）
