---
title: A Physical Agentic Loop for Language-Guided Grasping with Execution-State Monitoring
title_zh: 一种带有执行状态监测的语言引导抓取物理智能体循环
authors: "Wenze Wang, Mehdi Hosseinzadeh, Feras Dayoub"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07395v1"
tags: ["query:q9"]
score: 10.0
evidence: 带有执行状态监测的语言引导抓取的物理智能体循环
tldr: 针对机器人语言引导抓取中常见的单次执行且缺乏反馈的问题，本文受数字智能体循环启发，提出一种物理智能体循环框架。该框架通过Watchdog监控层将机械臂遥测数据转化为离散执行状态，并结合确定性策略实现失败后的自动重试或用户澄清。实验证明，该方法在不改变底层抓取模型的情况下，显著提升了系统在复杂环境下的鲁棒性和可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07395v1/fig-001.webp\", \"caption\": \"TABLE I WATCHDOG OUTCOME LABELS USED AS DISCRETE EXECUTION STATES (HIGH-LEVEL DEFINITIONS).\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2604.07395v1/fig-002.webp\", \"caption\": \"Fig. 2. Agent-centric architecture with a structured event interface. A lightweight language control layer specifies a semantic goal, which the Agent Core parses into structured task constraints G. Perception conditioning produces goal-consistent RGB-D input for the unmodified manipulation primitive, while the outcome-aware execution engine runs the grasp and performs physical outcome monitoring. Execution evidence is summarized as a structured event consisting of a Watchdog outcome label (SUCCESS, EMPTY, SLIP, WEAK, STALL, TIMEOUT) and an execution status (SUCCESS, FAIL, WAIT CLARIFY). The Agent Core consumes these events to realize the bounded agentic loop (observe–act–evaluate–decide), selecting to finalize, retry/reselect, or request clarification/terminate.\", \"page\": 3, \"index\": 2, \"width\": 840, \"height\": 447}, {\"url\": \"assets/figures/arxiv/2604.07395v1/fig-003.webp\", \"caption\": \"TABLE III COMPONENT ABLATION ACROSS AMBIGUITY SCENARIOS. RESULTS ARE REPORTED AS SUCCESSFUL GRASPS OVER TOTAL TRIALS.\", \"page\": 6, \"index\": 3, \"width\": 402, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.07395v1/fig-004.webp\", \"caption\": \"Fig. 6. End-to-end execution workflow of the proposed system across representative task scenarios. Each row illustrates a complete execution trace, and each column corresponds to a stage of the agentic grasping pipeline. Column 1: Scene observation. The robot observes the scene and receives a natural-language instruction specifying the target object or constraint. Column 2: Target grounding and perception conditioning. The system parses the instruction and applies perception conditioning to isolate the intended target. Depending on the task, non-target objects may be suppressed (black regions) using RGB-D conditioning or instance selection. Column 3: Grasping execution. The robot executes the visual servoing policy to approach and grasp the selected target object while maintaining target consistency. Column 4: Outcome evaluation and final grasp state. The watchdog monitors gripper signals and system state to determine the outcome, enabling bounded decisions such as finalize, retry, or clarification if needed. The four rows correspond to different task scenarios: (top) toy grasping under distractor presence, (second) multiple diverse cups under varying background conditions, (third) two identical cups with the target positioned farther away, and (bottom) selecting the target-colored cup from two differently colored cups. Across scenarios, the system maintains consistent target selection through semantic grounding and outcome-aware execution.\", \"page\": 7, \"index\": 4, \"width\": 1048, \"height\": 595}]"
motivation: 现有的语言引导抓取系统多为开环执行，无法有效识别并处理抓取过程中的物理失败或语义错误。
method: 引入名为Watchdog的执行监控层，利用触觉融合和时间稳定技术将传感器数据转化为状态标签，并配合确定性策略构建闭环控制。
result: 在移动协作机器人上的实验表明，该循环能有效应对视觉歧义、干扰物及诱发性执行失败，比开环执行更稳健。
conclusion: 通过显式的执行状态监控和有界恢复机制，可以在不增加架构负担的前提下，显著增强机器人操作任务的可靠性。
---

## 摘要
遵循语言指令的机器人操纵系统通常以单次尝试（single-shot）的方式执行抓取原语：模型提出动作，机器人执行，而诸如空抓、打滑、停滞、超时或语义错误抓取等故障并未以结构化方式反馈至决策层。受数字工具使用智能体中智能体循环（agentic loops）的启发，我们将语言引导的抓取重新表述为在具身执行状态上运行的有界具身智能体，其中物理动作会暴露显式的工具状态流。我们引入了一种物理智能体循环，它通过以下两部分封装了一个未经修改的学习型操纵原语（抓取并提升）：(i) 一个基于事件的接口；(ii) 一个名为 Watchdog 的执行监测层，该层利用接触感知融合和时间稳定技术，将带有噪声的夹爪遥测数据转换为离散的结果标签。这些结果事件（可选择性地结合抓取后的语义验证）由一个确定性有界策略处理，该策略负责完成、重试或上报给用户以寻求澄清，从而保证有限终止。我们在配备手眼 D405 相机的移动协作机器人上验证了该循环，在保持底层抓取模型不变的情况下，评估了涉及视觉歧义、干扰物和诱发执行故障的代表性场景。结果表明，显式的执行状态监测和有界恢复能够实现比开环执行更具鲁棒性和可解释性的行为，同时增加的架构开销极小。源代码和演示请参考我们的项目页面：https://wenzewwz123.github.io/Agentic-Loop/

## Abstract
Robotic manipulation systems that follow language instructions often execute grasp primitives in a largely single-shot manner: a model proposes an action, the robot executes it, and failures such as empty grasps, slips, stalls, timeouts, or semantically wrong grasps are not surfaced to the decision layer in a structured way. Inspired by agentic loops in digital tool-using agents, we reformulate language-guided grasping as a bounded embodied agent operating over grounded execution states, where physical actions expose an explicit tool-state stream. We introduce a physical agentic loop that wraps an unmodified learned manipulation primitive (grasp-and-lift) with (i) an event-based interface and (ii) an execution monitoring layer, Watchdog, which converts noisy gripper telemetry into discrete outcome labels using contact-aware fusion and temporal stabilization. These outcome events, optionally combined with post-grasp semantic verification, are consumed by a deterministic bounded policy that finalizes, retries, or escalates to the user for clarification, guaranteeing finite termination. We validate the resulting loop on a mobile manipulator with an eye-in-hand D405 camera, keeping the underlying grasp model unchanged and evaluating representative scenarios involving visual ambiguity, distractors, and induced execution failures. Results show that explicit execution-state monitoring and bounded recovery enable more robust and interpretable behavior than open-loop execution, while adding minimal architectural overhead. For the source code and demo refer to our project page: https://wenzewwz123.github.io/Agentic-Loop/

---

## 论文详细总结（自动生成）

### 论文总结：一种带有执行状态监测的语言引导抓取物理智能体循环

#### 1. 核心问题与整体含义
论文针对机器人操纵领域中“语言引导抓取”任务的可靠性问题展开研究。目前的系统大多采用**开环（Open-loop）**或单次执行模式：即根据指令生成一个动作并执行，但对于执行过程中出现的物理失败（如空抓、打滑、停滞）或语义错误（抓错了物体），系统缺乏结构化的反馈和纠正机制。

受数字世界中 AI Agent（如工具调用循环）的启发，本文提出将物理抓取重新定义为一个**有界智能体循环**。其核心含义在于：通过实时监测物理执行状态，将连续的传感器信号转化为离散的“工具状态事件”，从而使机器人能够像软件智能体处理 API 错误一样，自主决定是“完成”、“重试”还是“向用户求助”。

#### 2. 方法论
论文提出了一个名为 **Physical Agentic Loop** 的框架，其核心思想是在不修改底层抓取模型（如 ForceSight）的前提下，为其封装一个智能体外壳。

*   **核心流程（Observe-Act-Evaluate-Decide）**：
    *   **Observe（观察）**：接收语言指令并进行语义解析，通过感知调节（如实例分割、颜色过滤）锁定目标。
    *   **Act（行动）**：调用预训练的操纵原语（抓取并提升）。
    *   **Evaluate（评估）**：引入 **Watchdog（监控层）**。它融合夹爪的遥测数据（如电流、位置、力矩），利用时间稳定技术输出离散状态标签：`SUCCESS`（成功）、`EMPTY`（空抓）、`SLIP`（打滑）、`WEAK`（抓取不稳）、`STALL`（停滞）、`TIMEOUT`（超时）。
    *   **Decide（决策）**：采用**有界恢复策略（Bounded Recovery Policy）**。例如，若状态为 `EMPTY` 且在重试预算内，则触发 `RETRY`；若多次失败或语义模糊，则触发 `WAIT_CLARIFY`（请求用户澄清）。
*   **关键技术细节**：
    *   **事件驱动接口**：所有模块通过标准化的事件模式通信，确保了系统的模块化和可解释性。
    *   **有界性保证**：通过严格限制重试次数（如最多 1 次自动重试）和明确的终止条件，防止机器人在复杂环境下陷入无限循环。

#### 3. 实验设计
*   **硬件平台**：Hello Robot Stretch 3 移动协作机器人，配备 D405 手眼相机。
*   **底层模型**：使用未经重新训练的 **ForceSight** 作为抓取原语。
*   **Benchmark 与场景**：设计了 7 种具有挑战性的真实机器人场景：
    1.  **颜色/空间歧义**：如“抓取左边的杯子”或“抓取红色的杯子”。
    2.  **干扰物鲁棒性**：目标附近存在显著的非目标物体。
    3.  **领域偏移**：改变背景、光照或放置平台。
    4.  **诱发性空抓**：人为干预制造失败，测试重试逻辑。
    5.  **无有效目标**：测试系统是否会安全终止而非盲目抓取。
    6.  **目标修正**：执行中途更改指令。
*   **对比方法**：以传统的**开环执行（Open-loop）**作为基准（Baseline），即只执行一次抓取而不进行状态监测和恢复。

#### 4. 资源与算力
*   **模型使用**：使用了轻量级的本地大语言模型 **TinyLlama-1.1B** 用于指令解析。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号、数量或训练时长。这主要是因为该研究侧重于系统集成与闭环控制逻辑，而非从头训练大型视觉-语言-动作模型（VLA）。其核心组件 Watchdog 是基于规则和轻量级信号处理的，具有极低的延迟。

#### 5. 实验数量与充分性
*   **实验规模**：在系统开发和评估过程中共进行了超过 **290 次** 真实机器人实验，其中 **180 次** 用于最终系统的定量评估（每个场景 10 次试验）。
*   **充分性与公平性**：
    *   包含了**消融实验**，分别验证了 Watchdog 监控、感知调节和重试逻辑对成功率的贡献。
    *   实验设计涵盖了物理失败和语义歧义两个维度，较为全面。
    *   对比实验在相同的物理场景下进行，确保了客观性。

#### 6. 主要结论与发现
*   **性能提升**：在存在歧义和干扰的场景下，该智能体循环显著优于开环系统（例如在干扰物场景下成功率从 0% 提升至 100%）。
*   **低开销**：引入监控和决策逻辑带来的端到端运行时间增加极小（约 1 秒左右），证明了该框架的实用性。
*   **可靠性**：Watchdog 对“空抓”的检测准确率较高（约 86%），能够有效触发有界重试，使系统在物理不确定性面前表现得更稳健。

#### 7. 优点
*   **非侵入式封装**：不需要重新训练复杂的端到端模型，可以作为现有任何抓取算法的“插件”使用。
*   **可解释性强**：通过离散的状态标签（如 SLIP, EMPTY），用户和开发者能清晰知道机器人为何失败以及为何选择重试。
*   **安全性**：通过“有界”策略和“请求澄清”机制，解决了机器人盲目尝试导致的潜在碰撞或损坏风险。

#### 8. 不足与局限
*   **语义验证依赖性**：目前的语义一致性检查仍依赖于上游感知模型的准确性，如果视觉模型本身判定错误，系统仍可能抓错物体。
*   **恢复策略较为保守**：目前对于 `SLIP` 或 `WEAK` 等复杂状态多采取安全终止，尚未实现更精细的“调整姿态后重抓”等高级恢复动作。
*   **Watchdog 精度限制**：虽然能有效区分空抓，但对于更细微的物理状态（如物体在指尖滑动）的识别仍有提升空间。

（完）
