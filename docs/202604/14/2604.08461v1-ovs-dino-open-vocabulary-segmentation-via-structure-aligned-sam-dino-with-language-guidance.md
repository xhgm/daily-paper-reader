---
title: "OVS-DINO: Open-Vocabulary Segmentation via Structure-Aligned SAM-DINO with Language Guidance"
title_zh: OVS-DINO：通过语言引导的结构对齐 SAM-DINO 实现开放词汇分割
authors: "Haoxi Zeng, Qiankun Liu, Yi Bin, Haiyue Zhang, Yujuan Ding, Guoqing Wang, Deqiang Ouyang, Heng Tao Shen"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08461v1"
tags: ["query:q6"]
score: 9.0
evidence: 利用语言引导和视觉基础模型进行开放词汇分割
tldr: 本研究针对开放词汇分割（OVS）中边缘感知不足的问题，提出OVS-DINO框架。通过分析发现DINO深层特征存在边界衰减，研究者引入SAM的结构先验进行对齐。该框架包含结构感知编码器和调制解码器，并利用SAM生成的伪掩码进行监督。实验证明，该方法在多个基准测试中达到SOTA，显著提升了复杂场景下的分割精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08461v1/fig-001.webp\", \"caption\": \"Figure 1: Overall performance comparison. We evaluate our proposed method against state-of-the-art weakly-supervised approaches. The results illustrate that while existing methods struggle with fine-grained segmentation on challenging datasets such as Cityscapes [9] and COCO-Stuff [4], our OVSDINO consistently achieves superior performance across all benchmarks, with particularly significant improvements on complex scenarios.\", \"page\": 1, \"index\": 1, \"width\": 531, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-002.webp\", \"caption\": \"Table 1: Ablation study of using different visual backbones.\", \"page\": 11, \"index\": 2, \"width\": 1048, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-003.webp\", \"caption\": \"Figure 5: Additional qualitative results on the Cityscapes [9] dataset.\", \"page\": 14, \"index\": 3, \"width\": 1056, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-004.webp\", \"caption\": \"Figure 6: Additional qualitative results on the COCO-Stuff [4] dataset.\", \"page\": 14, \"index\": 4, \"width\": 1026, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-005.webp\", \"caption\": \"Figure 2: Frequency-domain analysis of DINO layers. (a) The high-to-low frequency energy ratio exhibits a consistent downward trend across layers, indicating a progressive transition from high-frequency local details to low-frequency global semantics during inference. (b) The azimuthally averaged power spectrum 𝐸 (𝑟 ) shows that at higher frequencies (large 𝑟 ), deeper layers exhibit lower energy compared to shallow ones, indicating that shallow layers effectively preserve more fine-grained edge information.\", \"page\": 3, \"index\": 5, \"width\": 1039, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-006.webp\", \"caption\": \"Figure 4: Additional qualitative results on the ADE20K [54] dataset.\", \"page\": 13, \"index\": 6, \"width\": 468, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.08461v1/fig-007.webp\", \"caption\": \"Figure 3: Visualization of failure cases.\", \"page\": 13, \"index\": 7, \"width\": 472, \"height\": 383}]"
motivation: 现有的开放词汇分割方法在利用DINO等模型时，由于深层特征的边界信息衰减，难以实现高精度的边缘感知。
method: 提出OVS-DINO框架，通过结构感知编码器（SAE）和结构调制解码器（SMD）将SAM的结构先验引入DINO，并结合伪掩码监督。
result: "在多个弱监督OVS基准上取得领先，平均得分提升2.1%，在Cityscapes复杂场景下提升达6.3%。"
conclusion: 通过将SAM的结构化能力与DINO的语义特征相结合，有效解决了开放词汇分割中的精细边缘提取难题。
---

## 摘要
开放词汇分割 (OVS) 旨在利用语义描述来分割超出预定义类别集合的图像区域。虽然基于 CLIP 的方法在语义泛化方面表现出色，但它们通常缺乏密集预测所需的细粒度空间感知。最近的研究引入了 DINO 等视觉基础模型 (VFM) 来缓解这些局限性。然而，这些方法在实现高保真分割所需的精确边缘感知方面仍然面临困难。在本文中，我们分析了 DINO 的内部表示，并发现其固有的边界意识并非不存在，而是随着特征向更深的 Transformer 块过渡而逐渐衰减。为了解决这个问题，我们提出了 OVS-DINO，这是一个通过与 Segment Anything Model (SAM) 进行结构对齐来激活 DINO 潜在边缘敏感性的新颖框架。具体而言，我们引入了结构感知编码器 (SAE) 和结构调制解码器 (SMD)，利用 SAM 的结构先验有效激活 DINO 的边界特征，并辅以利用 SAM 生成的伪掩码的监督策略。大量实验表明，我们的方法在多个弱监督 OVS 基准测试中达到了最先进的性能，平均得分提高了 2.1%（从 44.8% 提升至 46.9%）。值得注意的是，我们的方法显著增强了复杂、拥挤场景下的分割精度，在 Cityscapes 数据集上提升了 6.3%（从 36.6% 提升至 42.9%）。

## Abstract
Open-Vocabulary Segmentation (OVS) aims to segment image regions beyond predefined category sets by leveraging semantic descriptions. While CLIP based approaches excel in semantic generalization, they frequently lack the fine-grained spatial awareness required for dense prediction. Recent efforts have incorporated Vision Foundation Models (VFMs) like DINO to alleviate these limitations. However, these methods still struggle with the precise edge perception necessary for high fidelity segmentation. In this paper, we analyze internal representations of DINO and discover that its inherent boundary awareness is not absent but rather undergoes progressive attenuation as features transition into deeper transformer blocks. To address this, we propose OVS-DINO, a novel framework that revitalizes latent edge-sensitivity of DINO through structural alignment with the Segment Anything Model (SAM). Specifically, we introduce a Structure-Aware Encoder (SAE) and a Structure-Modulated Decoder (SMD) to effectively activate boundary features of DINO using SAM's structural priors, complemented by a supervision strategy utilizing SAM generated pseudo-masks. Extensive experiments demonstrate that our method achieves state-of-the-art performance across multiple weakly-supervised OVS benchmarks, improving the average score by 2.1% (from 44.8% to 46.9%). Notably, our approach significantly enhances segmentation accuracy in complex, cluttered scenarios, with a gain of 6.3% on Cityscapes (from 36.6% to 42.9%).

---

## 论文详细总结（自动生成）

这是一份关于论文《OVS-DINO: Open-Vocabulary Segmentation via Structure-Aligned SAM-DINO with Language Guidance》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：开放词汇分割（OVS）旨在分割图像中任意类别的物体。虽然 CLIP 等模型具备强大的语义泛化能力，但缺乏密集预测所需的细粒度空间感知；而 DINO 等视觉基础模型虽有较强的空间定位能力，但其深层特征由于“低通滤波”效应，会导致边界信息逐渐衰减，难以处理复杂场景下的精确边缘分割。
*   **研究动机**：作者发现 DINO 的浅层其实保留了边界意识，只是在深层被语义抽象掩盖了。因此，研究目标是利用 Segment Anything Model (SAM) 的结构化先验来“重新激活”DINO 的边缘敏感性，同时保留其与语言对齐的语义空间。

### 2. 方法论
核心思想是通过结构对齐将 SAM 的边界感知能力迁移到 DINO 框架中，主要包含以下关键组件：
*   **结构感知编码器 (SAE)**：聚合 DINO 的多层特征（如第 2, 4, 6, 8, 10, 12 层），通过卷积层进行线性重组，并将其投影到 SAM 的潜空间进行对齐。
*   **结构化对齐 (Structural Alignment)**：使用余弦相似度损失（语义对齐）和均方误差 MSE 损失（结构对齐），强制 DINO 的中间特征吸收 SAM 的细粒度纹理和边界信息。
*   **结构调制解码器 (SMD) 与保存门 (Preservation Gate, PG)**：
    *   SMD 将增强后的特征映射回 DINO 的语义流。
    *   **PG 机制**：为了防止 SAM 的结构信息破坏 DINO 原有的语义一致性，引入了一个基于 `tanh` 激活函数的门控残差结构，确保输出特征在增强边缘的同时，仍处于 DINO 的跨模态对齐空间内。
*   **弱监督训练流程**：利用 NLTK 提取文本中的名词，结合 Grounding DINO 和 SAM 生成伪掩码（Pseudo-masks），以此作为监督信号进行训练。

### 3. 实验设计
*   **数据集**：涵盖了 8 个主流基准测试，包括 PASCAL VOC 20/21、PASCAL Context 59/60、COCO-Stuff、COCO-Object、Cityscapes 和 ADE20K。
*   **Benchmark 与对比方法**：对比了包括 GroupViT、TCL、CoDe、CLIP-DINOiser、Talk2DINO 和 DSGS 在内的多种最先进（SOTA）的弱监督 OVS 方法。
*   **评估指标**：采用平均交并比 (mIoU)。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：提到了 Batch size 为 64，在 COCO Captions 2017 数据集上训练了 25 个 epoch，使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$（编码器/解码器）和 $1 \times 10^{-5}$（微调 DINOv2 后三层）。

### 5. 实验数量与充分性
*   **实验规模**：作者在 8 个不同难度的任务上进行了测试，并进行了详尽的消融实验。
*   **消融实验内容**：验证了 SAM 引导的有效性、保存门 (PG) 的必要性、不同层选择的影响、以及微调策略的作用。
*   **充分性评价**：实验设计非常充分且客观。通过频率域分析（DFT）解释了 DINO 的特征衰减，通过 CKA 热图分析了特征空间差异，并提供了大量的定性可视化对比，证明了方法在复杂场景（如 Cityscapes）下的优越性。

### 6. 主要结论与发现
*   **性能提升**：OVS-DINO 在 8 个基准测试上均取得了 SOTA 性能，平均 mIoU 提升了 2.1%。在复杂场景（Cityscapes）中提升尤为显著，达 6.3%。
*   **知识迁移**：通过训练时的结构对齐，DINO 成功内化了 SAM 的边界知识。
*   **推理效率**：在推理阶段，模型**不再需要运行 SAM**，仅靠增强后的 DINO 即可实现高精度分割，极大地降低了计算开销。

### 7. 优点与亮点
*   **理论支撑扎实**：通过 DFT 频率分析揭示了 DINO 边界信息丢失的本质原因，为方法设计提供了科学依据。
*   **保存门 (PG) 设计**：巧妙地解决了结构增强与语义保持之间的冲突，避免了“灾难性遗忘”原有的跨模态对齐能力。
*   **推理高效**：实现了“训练时引导，推理时解耦”，兼顾了性能与速度。

### 8. 不足与局限
*   **小物体识别**：在失败案例分析中提到，模型对细长结构、小规模物体（如灯杆、旗帜）的分割仍不够鲁棒。
*   **语义混淆**：在极度拥挤的室内场景中，外观相似的类别（如柱子与墙壁）仍存在分类错误的风险。
*   **伪标签依赖**：性能在一定程度上受限于 Grounding DINO 和 SAM 生成的伪掩码质量，若初始检测出错，会影响后续学习。

（完）
