---
title: "ZeD-MAP: Bundle Adjustment Guided Zero-Shot Depth Maps for Real-Time Aerial Imaging"
title_zh: ZeD-MAP：用于实时航空成像的光束法平差引导零样本深度图
authors: "Selim Ahmet Iz, Francesco Nex, Norman Kerle, Henry Meissner, Ralf Berger"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04667v1"
tags: ["query:q10"]
score: 9.0
evidence: 利用零样本扩散模型从无人机图像中进行实时深度重建
tldr: 针对无人机高分辨率影像实时深度重建中存在的宽基线、弱纹理及计算受限等挑战，本文提出ZeD-MAP框架。该方法将零样本扩散深度模型与增量式集群平差（BA）相结合，利用BA生成的稀疏3D点作为度量引导，解决了扩散模型缺乏绝对尺度和时空一致性的问题。实验表明，该方法在保持实时性的同时，实现了亚米级的重建精度，为灾害响应等时效性地理空间任务提供了高效的3D制图方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04667v1/fig-001.webp\", \"caption\": \"Figure 1. Integration of cluster-based bundle adjustment and diffusion model.\", \"page\": 2, \"index\": 1, \"width\": 1023, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2604.04667v1/fig-002.webp\", \"caption\": \"Figure 4. Point clouds generated by (a) VGGT, (b) Map Anything, (c) COLMAP, (d) proposed method. (i) Enlarged views of target regions from corresponding point clouds (a* to d*). (ii) The perspective view of selected regions in the point cloud generated by the proposed method.\", \"page\": 7, \"index\": 2, \"width\": 1019, \"height\": 863}, {\"url\": \"assets/figures/arxiv/2604.04667v1/fig-003.webp\", \"caption\": \"Figure 2 . Ground-marker locations on test scene. 9m\", \"page\": 5, \"index\": 3, \"width\": 514, \"height\": 564}]"
motivation: 传统摄影测量计算耗时且零样本扩散模型缺乏度量准确性，难以满足无人机实时高精度制图的需求。
method: 通过增量式集群平差获取相机位姿和稀疏3D点，并将其作为度量引导融入扩散深度模型，实现一致性深度估计。
result: 在50米航高影像下实现了0.87米水平和0.12米垂直精度，单帧处理时间缩短至1.47-4.91秒。
conclusion: 平差引导的度量约束使扩散模型能兼顾处理速度与重建精度，为实时3D地图生成提供了可行路径。
---

## 摘要
从超高分辨率无人机（UAV）图像中进行实时深度重建对于灾害响应等时间敏感的地理空间任务至关重要，但由于宽基线视差、大图像尺寸、低纹理或镜面反射表面、遮挡以及严格的计算限制，这仍然具有挑战性。最近的零样本扩散模型提供了快速的单图稠密预测，无需针对特定任务进行重新训练，且与基于 Transformer 的预测器相比，所需的标注数据集更少，同时避免了经典多视图立体视觉（MVS）对严苛采集几何条件的限制。然而，它们的概率推理特性阻碍了在序列帧和重叠瓦片之间实现可靠的度量精度和时间一致性。我们提出了 ZeD-MAP，这是一个集群级框架，通过集成增量式集群光束法平差（BA），将测试时扩散深度模型转换为具有度量一致性的、类似于 SLAM 的制图流水线。流式传输的无人机帧被分组为重叠的集群；周期性的 BA 产生度量一致的位姿和稀疏 3D 连接点，这些点被重投影到选定帧中，并作为基于扩散的深度估计的度量引导。在约 50 米高度拍摄的地面标记飞行数据（地面采样距离 GSD 约为 0.85 厘米/像素，对应每帧 2,650 平方米的地面覆盖范围）上，使用 DLR 模块化航空相机系统（MACS）进行的验证表明，我们的方法实现了亚米级精度，水平（XY）平面误差约为 0.87 米，垂直（Z）方向误差约为 0.12 米，同时单图运行时间保持在 1.47 至 4.91 秒之间。结果受手动点云标注带来的轻微噪声影响。这些研究结果表明，基于 BA 的度量引导提供了与经典摄影测量方法相当的一致性，同时显著加快了处理速度，从而实现了实时 3D 地图生成。

## Abstract
Real-time depth reconstruction from ultra-high-resolution UAV imagery is essential for time-critical geospatial tasks such as disaster response, yet remains challenging due to wide-baseline parallax, large image sizes, low-texture or specular surfaces, occlusions, and strict computational constraints. Recent zero-shot diffusion models offer fast per-image dense predictions without task-specific retraining, and require fewer labelled datasets than transformer-based predictors while avoiding the rigid capture geometry requirement of classical multi-view stereo. However, their probabilistic inference prevents reliable metric accuracy and temporal consistency across sequential frames and overlapping tiles. We present ZeD-MAP, a cluster-level framework that converts a test-time diffusion depth model into a metrically consistent, SLAM-like mapping pipeline by integrating incremental cluster-based bundle adjustment (BA). Streamed UAV frames are grouped into overlapping clusters; periodic BA produces metrically consistent poses and sparse 3D tie-points, which are reprojected into selected frames and used as metric guidance for diffusion-based depth estimation. Validation on ground-marker flights captured at approximately 50 m altitude (GSD is approximately 0.85 cm/px, corresponding to 2,650 square meters ground coverage per frame) with the DLR Modular Aerial Camera System (MACS) shows that our method achieves sub-meter accuracy, with approximately 0.87 m error in the horizontal (XY) plane and 0.12 m in the vertical (Z) direction, while maintaining per-image runtimes between 1.47 and 4.91 seconds. Results are subject to minor noise from manual point-cloud annotation. These findings show that BA-based metric guidance provides consistency comparable to classical photogrammetric methods while significantly accelerating processing, enabling real-time 3D map generation.

---

## 论文详细总结（自动生成）

### 论文总结：ZeD-MAP —— 光束法平差引导的实时航空成像零样本深度图

#### 1. 核心问题与研究动机
*   **核心问题**：如何在灾害响应等时间敏感任务中，利用无人机（UAV）超高分辨率影像实现**实时、具有度量准确性且时空一致**的稠密3D重建。
*   **研究背景**：
    *   **传统方法（如SGM/MVS）**：几何精度高，但计算量巨大，难以处理5000万像素以上的实时流数据。
    *   **零样本扩散模型**：泛化能力强，无需特定训练，但存在概率推理导致的尺度不确定性（非度量）和帧间不一致（闪烁/漂移）问题。
    *   **需求**：需要一种结合几何约束的确定性和深度学习模型的高效性的混合方案。

#### 2. 方法论
ZeD-MAP 提出了一种将**增量式集群光束法平差（BA）**与**测试时扩散深度模型**相结合的框架：
*   **重叠感知集群形成**：根据GNSS数据或固定窗口将影像流分组。若GNSS可用，动态计算重叠度；若不可用，采用三帧滑动窗口。
*   **轻量化三视图BA（N-Frame Selection）**：为了平衡速度与精度，仅对集群中的中间帧及其相邻帧进行BA优化。这能产生度量一致的相机位姿和稀疏3D连接点。
*   **度量引导的扩散推理**：
    *   将BA生成的稀疏3D点重投影到中间帧，形成稀疏深度图。
    *   使用 **Murre**（一种基于扩散的深度估计模型）作为骨干，将稀疏深度点作为条件输入（Conditioning），引导模型生成具有绝对物理尺度的稠密深度图。
*   **增量融合**：利用截断符号距离函数（TSDF）将各帧深度图在线融合，生成全局点云、深度图和真正射影像（True-ortho）。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **地面标记数据集**：在50米航高拍摄，包含人工放置的地面参考标记，用于定量评估绝对度量精度。
    2.  **地震灾害数据集**：2023年土耳其地震灾区影像（60张，约325米航高），用于评估大规模场景下的鲁棒性和多航带一致性。
*   **Benchmark（对比方法）**：
    *   **COLMAP**：经典的离线摄影测量基准（高精度但非实时）。
    *   **VGGT & MapAnything**：最先进的基于Transformer的前馈几何预测模型。
    *   **单目深度基准**：DINOv2, DepthPro, Marigold, Depth Anything v2（用于一致性对比）。

#### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：NVIDIA RTX A6000（负责扩散模型推理）。
    *   **CPU**：Intel Core i7 (13th Gen, 20核) + 32GB RAM（负责BA和几何预处理）。
*   **运行效率**：
    *   扩散推理：单张图约10秒（10次迭代，下采样至1024px）。
    *   整体流程：单对影像处理时间在 **1.47s 至 4.91s** 之间（取决于集群大小），满足准实时需求。

#### 5. 实验数量与充分性
*   **实验规模**：论文通过三个阶段进行验证：帧间一致性定性分析、地面标记定量精度分析、大规模地震场景应用分析。
*   **充分性评价**：实验设计较为全面，涵盖了从微观精度到宏观稳定性的评估。通过与离线SOTA（COLMAP）和在线SOTA（VGGT等）对比，客观展示了该方法在速度与精度平衡点上的优势。但实验场景主要集中在航空测绘，对于更复杂的低空避障或室内场景未做探讨。

#### 6. 主要结论与发现
*   **精度表现**：在50米航高下，水平（XY）误差约 **0.87m**，垂直（Z）误差约 **0.12m**，相对误差低于2.2%。
*   **一致性**：BA引导显著消除了扩散模型的随机性，使生成的深度图在序列帧间保持了极高的几何连续性。
*   **性能平衡**：ZeD-MAP 在保持接近前馈模型速度的同时，达到了接近离线摄影测量软件的局部精度（局部标准差仅差0.001m）。

#### 7. 优点
*   **零样本度量化**：无需针对特定场景重新训练，通过BA实时注入物理尺度。
*   **高效的N帧选择策略**：将复杂的全局BA简化为轻量级集群BA，解决了高分辨率影像处理的计算瓶颈。
*   **端到端流程**：集成了从原始RGB输入到真正射影像输出的完整在线制图能力。

#### 8. 不足与局限
*   **计算开销**：尽管已优化，但扩散模型的迭代特性仍比纯前馈网络慢，对高性能GPU有依赖。
*   **分辨率限制**：为了保证实时性，扩散推理时需对图像进行下采样，可能损失极细微的结构特征。
*   **累积误差**：虽然有集群间的位姿锚定，但在极长距离且无GNSS辅助的情况下，仍可能存在全局漂移风险。
*   **特定伪影**：在航机转弯或大角度旋转区域，深度图可能会出现小规模的“高度膨胀”现象。

（完）
