---
title: Scene-Agnostic Object-Centric Representation Learning for 3D Gaussian Splatting
title_zh: 面向 3D 高斯泼溅的场景无关以物体为中心的表示学习
authors: "Tsuheng Hsu, Guiyu Liu, Juho Kannala, Janne Heikkilä"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09045v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 三维场景理解的以物体为中心的表示学习
tldr: 本研究针对现有3D场景理解中掩码监督信号非对象中心化且难以跨场景泛化的问题，提出了一种面向3D高斯泼溅（3DGS）的场景无关对象中心化表示学习方法。通过引入预训练的槽注意全局对象中心学习模块和对象代码本，实现了跨视图和跨场景的一致性身份表示。该方法无需额外的掩码预处理或多视图对齐，即可直接监督3D高斯的身份特征，显著提升了模型在机器人交互和场景理解等任务中的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09045v1/fig-001.webp\", \"caption\": \"Figure 3. Visualization of cross-scene identity on the OCTA dataset (a) and GSO dataset (b). Our method achieves cross-scene identification using the learned global codebook: as highlighted, the same object is assigned a consistent ID across different scenes, whereas VFM supervised methods remain scene-dependent.\", \"page\": 6, \"index\": 1, \"width\": 1023, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.09045v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of our proposed pipeline. (b) GOLD learns a global, scene-agnostic codebook storing a total of C object intrinsic features while preserving the underlying functionality of slot attention.During scene optimization (a), for each view, we render Gaussian identity features and feed the input image into the trained GOLD module to obtain K object masks and mask-to-codebook probabilities. We assign a codebook feature to each mask, broadcast it to image resolution, and blend the masks to construct dense supervision for Gaussian identity features.\", \"page\": 2, \"index\": 2, \"width\": 1024, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.09045v1/fig-003.webp\", \"caption\": \"Figure 5. Qualitative comparison of reconstruction and rendered masks for our method and uOCF on the GSO dataset. As highlighted, when uOCF fails to discover an object, the reconstructed scene may miss that object, whereas our reconstruction remains intact even when object discovery is imperfect.\", \"page\": 7, \"index\": 3, \"width\": 488, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.09045v1/fig-004.webp\", \"caption\": \"Table 3. Quantitative comparison of our proposed pipeline and prior methods on the OCTA and GSO datasets, where the prior methods’ original supervision masks are replaced with SAM2-generated masks.\", \"page\": 8, \"index\": 4, \"width\": 922, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2604.09045v1/fig-005.webp\", \"caption\": \"Table 1. Quantitative comparison of our method and prior works on the OCTA and GSO datasets, evaluated across test scenes. Best results are in bold, second best are underlined.\", \"page\": 5, \"index\": 5, \"width\": 820, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.09045v1/fig-006.webp\", \"caption\": \"Figure 2. Qualitative comparison of rendered Gaussian feature masks on the OCTA dataset (left) and GSO dataset (right), where each object ID is visualized with a distinct color generated through a deterministic mapping. VFM-based supervision lacks object-level consistency, as highlighted in the images, where different surfaces of the cube or the rim of the vessel are often identified as separate instances.\", \"page\": 5, \"index\": 6, \"width\": 984, \"height\": 550}]"
motivation: 现有的3D分割方法依赖于场景相关的2D掩码监督，存在身份冲突且缺乏跨场景的泛化能力。
method: 结合槽注意机制构建全局对象中心学习模块和场景无关代码本，为3DGS提供一致的身份锚定表示。
result: 实现了无需逐场景微调的无监督对象监督与识别，在跨场景泛化和结构化表示方面表现优异。
conclusion: 该方法成功将无监督对象中心学习引入3DGS，为更通用的3D场景理解和机器人交互奠定了基础。
---

## 摘要
最近在 3D 场景理解方面的工作利用来自视觉基础模型 (VFM) 的 2D 掩码来监督辐射场，从而实现实例级 3D 分割。然而，来自基础模型的监督信号在本质上并非以物体为中心，通常需要额外的掩码预处理/后处理，或专门的训练和损失设计来解决跨视图的掩码身份冲突。所学习的 3D 场景身份具有场景依赖性，限制了跨场景的泛化能力。因此，我们提出了一种数据集级别的、以物体为中心的监督方案，用于在 3D 高斯泼溅 (3DGS) 中学习物体表示。基于预训练的、基于槽注意力 (slot attention) 的全局以物体为中心学习 (GOCL) 模块，我们学习了一个场景无关的物体码本，该码本提供了跨视图和跨场景的一致且身份锚定的表示。通过将码本与该模块的无监督物体掩码相结合，我们可以直接监督 3D 高斯的身份特征，而无需额外的掩码预处理/后处理或显式的多视图对齐。所学习的场景无关码本无需针对每个场景进行微调或重新训练即可实现物体监督和识别。因此，我们的方法将无监督以物体为中心学习 (OCL) 引入 3DGS，为机器人交互、场景理解和跨场景泛化等下游任务带来了更具结构化的表示和更好的泛化性能。

## Abstract
Recent works on 3D scene understanding leverage 2D masks from visual foundation models (VFMs) to supervise radiance fields, enabling instance-level 3D segmentation. However, the supervision signals from foundation models are not fundamentally object-centric and often require additional mask pre/post-processing or specialized training and loss design to resolve mask identity conflicts across views. The learned identity of the 3D scene is scene-dependent, limiting generalizability across scenes. Therefore, we propose a dataset-level, object-centric supervision scheme to learn object representations in 3D Gaussian Splatting (3DGS). Building on a pre-trained slot attention-based Global Object Centric Learning (GOCL) module, we learn a scene-agnostic object codebook that provides consistent, identity-anchored representations across views and scenes. By coupling the codebook with the module's unsupervised object masks, we can directly supervise the identity features of 3D Gaussians without additional mask pre-/post-processing or explicit multi-view alignment. The learned scene-agnostic codebook enables object supervision and identification without per-scene fine-tuning or retraining. Our method thus introduces unsupervised object-centric learning (OCL) into 3DGS, yielding more structured representations and better generalization for downstream tasks such as robotic interaction, scene understanding, and cross-scene generalization.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **GOLD-3DGS** 的方法，旨在解决 3D 高斯泼溅（3DGS）在物体级场景理解中存在的跨视图不一致和缺乏跨场景泛化能力的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 场景理解方法（如基于 NeRF 或 3DGS 的方法）主要依赖 2D 视觉基础模型（VFM，如 SAM）生成的掩码进行监督。
*   **研究动机**：
    *   **视图不一致性**：VFM 生成的掩码在不同视角下 ID 经常跳变，需要复杂的预处理或后处理（如跟踪算法）来对齐。
    *   **过度分割**：VFM 基于外观而非物理实体进行分割，容易将一个物体拆分为多个部分。
    *   **场景依赖性**：学习到的物体身份（Identity）仅限于单个场景，无法在不同场景间识别相同的物体。
*   **整体含义**：引入无监督以物体为中心学习（OCL）机制，通过学习一个“全局物体代码本”，实现场景无关、跨视图一致的 3D 物体表示。

### 2. 方法论
*   **核心思想**：将全局以物体为中心学习模块（GOLD）与 3DGS 结合，利用预训练的场景无关代码本直接监督高斯点的身份特征。
*   **关键技术细节**：
    1.  **全局代码本学习（GOLD）**：利用槽注意力（Slot Attention）机制，将物体表示分解为“内在特征”（从全局代码本中通过 Gumbel-Softmax 选取）和“外在特征”（位置、缩放等）。
    2.  **身份特征嵌入**：为每个 3D 高斯点增加一个低维身份特征向量 $f_i$。
    3.  **监督流程**：
        *   输入图像通过预训练的 GOLD 模块生成 $K$ 个物体掩码及对应的代码本概率。
        *   根据概率将代码本中的特征分配给掩码，构建密集的身份特征图。
        *   使用 MSE 损失直接监督 3DGS 渲染出的身份特征图。
    4.  **3D 正则化损失**：通过 KL 散度约束空间邻近的高斯点具有相似的物体类别分布，增强成组效果。

### 3. 实验设计
*   **数据集**：
    *   **OCTScene-A (OCTA)**：真实的桌面场景数据集，包含 3100 个训练场景和 100 个测试场景。
    *   **Google Scanned Objects (GSO)**：合成物体数据集，包含 1900 个训练场景和 90 个测试场景。
*   **对比方法**：
    *   **3DGS 类**：Gaussian Grouping、Unified Lift、ObjectGS（均基于 VFM 监督）。
    *   **NeRF 类**：uOCF（无监督 3D OCL 的代表作）。
*   **评估指标**：ARI（调整兰德指数）、AMI（调整互信息）、mIoU（分割质量）、PSNR/SSIM/LPIPS（重建质量）以及 FPS（渲染速度）。

### 4. 资源与算力
*   **算力设备**：实验在 **NVIDIA H200 GPU** 上运行。
*   **训练时长**：
    *   GOLD 模块：在 GSO 上训练 40 epochs，在 OCTA 上训练 30 epochs。
    *   3DGS 优化：每个场景进行 **300k 次迭代**。
*   **推理速度**：渲染速度达到 **106.1 FPS**，远高于 NeRF 类方法的 6.2 FPS。

### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集（共近 200 个测试场景）上进行了详尽的定量评估。
*   **实验类型**：
    *   **定性对比**：展示了分割掩码的连贯性。
    *   **跨场景识别实验**：验证了相同物体在不同场景下 ID 是否一致。
    *   **消融实验**：将监督信号替换为更先进的 SAM2，证明了即使有更好的 VFM，本方法在物体发现和跨场景泛化上仍具优势。
*   **评价**：实验设计客观、公平，对比了从隐式表示到显式表示的多种前沿基准，充分验证了方法的有效性。

### 6. 主要结论与发现
*   **跨场景一致性**：该方法是首个在 3DGS 中实现跨场景物体身份锚定的方法，相同物体在不同场景中被分配相同的代码本索引。
*   **物体级完整性**：相比 VFM 容易产生的过度分割，本方法能更完整地识别物理实体。
*   **解耦优势**：与 NeRF 类 OCL 方法不同，本方法的物体发现失败不会导致场景重建质量下降（3DGS 的显式优势）。
*   **性能领先**：在 OCL 相关指标（ARI/AMI）上显著优于基于 VFM 的 3DGS 方法。

### 7. 优点
*   **无需预处理**：摆脱了对掩码跟踪、多视图对齐或复杂对比学习损失的依赖。
*   **泛化性强**：学习到的代码本是场景无关的，支持零样本（Zero-shot）的跨场景物体识别。
*   **高效性**：继承了 3DGS 的实时渲染能力，且身份特征学习过程简洁。

### 8. 不足与局限
*   **数据依赖**：目前仅在物体种类较少、背景相对简单的桌面场景（OCTA/GSO）中验证，在复杂、高动态的真实世界大规模场景中的表现尚待观察。
*   **超参数敏感**：需要预先设定槽（Slot）的数量和代码本的大小，这在处理未知物体数量的场景时可能存在限制。
*   **计算开销**：训练大型全局代码本对计算资源有一定要求。

（完）
