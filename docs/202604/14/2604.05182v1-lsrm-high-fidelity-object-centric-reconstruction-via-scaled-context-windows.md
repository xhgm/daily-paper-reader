---
title: "LSRM: High-Fidelity Object-Centric Reconstruction via Scaled Context Windows"
title_zh: LSRM：通过扩展上下文窗口实现高保真以物体为中心的重建
authors: "Zhengqin Li, Cheng Zhang, Jakob Engel, Zhao Dong"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05182v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 通过扩展上下文窗口实现高保真以物体为中心的三维重建
tldr: 本研究提出大型稀疏重建模型（LSRM），旨在通过扩展Transformer上下文窗口提升前馈3D重建质量。针对现有方法在精细纹理恢复上的不足，LSRM利用原生稀疏注意力机制，处理比以往多20倍的对象标记和2倍的图像标记。其核心贡献包括高效的粗到细流水线、基于几何距离的3D感知空间路由以及块感知序列并行策略。实验表明，LSRM在多项指标上显著超越现有SOTA，甚至在逆渲染任务中达到或超过了密集视图优化方法的水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05182v1/fig-001.webp\", \"caption\": \"Fig. 2: Network architecture of LSRM. Our method employs a two-stage coarseto-fine pipeline. In Stage 1, a Dense Reconstruction Transformer generates a coarse, low-resolution volume. Stage 2 utilizes this coarse volume to initialize the active sparse volume tokens and predicts high-resolution sparse residuals, constructing the final 3D representation.\", \"page\": 6, \"index\": 1, \"width\": 721, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.05182v1/fig-002.webp\", \"caption\": \"Fig. 1: High-fidelity 3D reconstruction. Given 12–18 images (left), LSRM adapts Native Sparse Attention (NSA) to generate explicit meshes and textures in a single feed-forward pass with minimal compute overhead. As an extension, LSRM can also predict BRDF maps for inverse rendering (right). Zoom in for details.\", \"page\": 2, \"index\": 2, \"width\": 719, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2604.05182v1/fig-003.webp\", \"caption\": \"Fig. 8: Qualitative Inverse Rendering Results. Visual comparisons against optimization-based and feed-forward baselines demonstrate that LSRM consistently recovers higher-fidelity textures and finer geometric details.\", \"page\": 14, \"index\": 3, \"width\": 719, \"height\": 282}, {\"url\": \"assets/figures/arxiv/2604.05182v1/fig-004.webp\", \"caption\": \"Fig. 6: Qualitative NVS Results on the GSO Dataset. LSRM significantly improves texture fidelity over prior SOTA [41]. Zoomed-in comparisons show that our method successfully recovers legible text (bottom left), complex geometry (top left) and sharp facial structures (right), whereas the baseline produces blurred artifacts.\", \"page\": 12, \"index\": 4, \"width\": 720, \"height\": 302}]"
motivation: 现有的前馈3D重建方法在恢复精细纹理和外观细节方面仍显著落后于基于密集视图的优化方法。
method: 通过原生稀疏注意力机制扩展上下文窗口，并引入粗到细的残差预测、3D感知空间路由及块感知序列并行策略。
result: "在标准基准测试中，LSRM比现有SOTA方法提升了2.5 dB PSNR并降低了40%的LPIPS，在逆渲染任务中表现优异。"
conclusion: 大幅扩展Transformer的上下文窗口并结合稀疏注意力机制，是实现高保真前馈3D对象重建和逆渲染的有效途径。
---

## 摘要
我们引入了大稀疏重建模型（Large Sparse Reconstruction Model，LSRM），旨在研究扩展 Transformer 上下文窗口对前馈 3D 重建的影响。尽管最近以物体为中心的前馈方法能够提供鲁棒且高质量的重建，但在恢复细粒度纹理和外观方面，它们仍落后于密集视图优化（dense-view optimization）方法。我们表明，通过大幅增加活跃的物体和图像 token 数量来扩展上下文窗口，可以显著缩小这一差距，并实现高保真的 3D 物体重建和逆渲染。为了实现有效的扩展，我们在架构设计中采用了原生稀疏注意力机制，通过以下三项核心贡献释放了其在 3D 重建方面的潜力：(1) 一种高效的由粗到精（coarse-to-fine）流水线，通过预测稀疏的高分辨率残差，将计算集中在信息丰富的区域；(2) 一种 3D 感知的空间路由机制，利用显式的几何距离而非标准的注意力分数来建立准确的 2D-3D 对应关系；(3) 一种定制的块感知序列并行策略，利用 All-gather-KV 协议在 GPU 之间平衡动态的稀疏工作负载。结果表明，与之前的最先进（SOTA）方法相比，LSRM 处理的物体 token 数量增加了 20 倍，图像 token 数量增加了 2 倍以上。在标准新视角合成基准测试上的广泛评估显示，该模型相较于当前的 SOTA 有显著提升，PSNR 提高了 2.5 dB，LPIPS 降低了 40%。此外，将 LSRM 扩展到逆渲染任务时，在广泛使用的基准测试上的定性和定量评估表明，其在纹理和几何细节方面有一致的改进，实现的 LPIPS 达到或超过了 SOTA 密集视图优化方法。代码和模型将在我们的项目页面上发布。

## Abstract
We introduce the Large Sparse Reconstruction Model to study how scaling transformer context windows impacts feed-forward 3D reconstruction. Although recent object-centric feed-forward methods deliver robust, high-quality reconstruction, they still lag behind dense-view optimization in recovering fine-grained texture and appearance. We show that expanding the context window -- by substantially increasing the number of active object and image tokens -- remarkably narrows this gap and enables high-fidelity 3D object reconstruction and inverse rendering. To scale effectively, we adapt native sparse attention in our architecture design, unlocking its capacity for 3D reconstruction with three key contributions: (1) an efficient coarse-to-fine pipeline that focuses computation on informative regions by predicting sparse high-resolution residuals; (2) a 3D-aware spatial routing mechanism that establishes accurate 2D-3D correspondences using explicit geometric distances rather than standard attention scores; and (3) a custom block-aware sequence parallelism strategy utilizing an All-gather-KV protocol to balance dynamic, sparse workloads across GPUs. As a result, LSRM handles 20x more object tokens and >2x more image tokens than prior state-of-the-art (SOTA) methods. Extensive evaluations on standard novel-view synthesis benchmarks show substantial gains over the current SOTA, yielding 2.5 dB higher PSNR and 40% lower LPIPS. Furthermore, when extending LSRM to inverse rendering tasks, qualitative and quantitative evaluations on widely-used benchmarks demonstrate consistent improvements in texture and geometry details, achieving an LPIPS that matches or exceeds that of SOTA dense-view optimization methods. Code and model will be released on our project page.

---

## 论文详细总结（自动生成）

以下是对论文《LSRM: High-Fidelity Object-Centric Reconstruction via Scaled Context Windows》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前以物体为中心的前馈（Feed-forward）3D重建模型虽然速度快、鲁棒性强，但在恢复**精细纹理和外观细节**（如清晰的文字、Logo、面部特征）方面，仍显著落后于基于密集视图的优化方法（Optimization-based methods）。
*   **研究动机**：作者假设这种质量差距源于Transformer上下文窗口的限制。简单增加3D或2D表示的分辨率会导致显存和计算量呈平方或立方级增长。因此，研究如何通过**扩展上下文窗口**（增加活跃的物体和图像token数量）来提升重建保真度是本文的核心驱动力。

### 2. 方法论
LSRM（Large Sparse Reconstruction Model）采用了一种基于原生稀疏注意力（NSA）的架构，核心技术包括：
*   **由粗到精的二阶段流水线**：
    *   **阶段1（密集重建）**：训练一个低分辨率的密集Transformer，识别出信息最丰富的体积区域（靠近表面的点）。
    *   **阶段2（稀疏重建）**：利用阶段1的先验，仅对活跃的稀疏体积token进行高分辨率处理。该阶段不从零预测，而是预测**高分辨率稀疏残差**，显著提升了收敛效率。
*   **3D感知空间路由机制**：
    *   传统的稀疏注意力依赖压缩token的注意力分数进行块选择，在训练初期往往无法建立准确的对应关系。
    *   LSRM引入了显式的几何约束：利用阶段1建立的几何结构和已知相机参数，通过**3D欧几里得距离**直接检索相关的图像/体积块，确保了纹理抓取的准确性。
*   **定制化块感知序列并行策略**：
    *   针对稀疏token分布不均导致的GPU负载失衡问题，设计了**All-gather-KV协议**。它在保持空间块局部性的同时，通过广播轻量级的KV缓存实现全局上下文感知，确保了大规模并行训练的效率。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用了60万个精心挑选的3D模型进行大规模预训练。
    *   **评估集**：GSO (Google Scanned Objects) 用于新视角合成（NVS）；StanfordORB、DigitalTwinCatalogue (DTC) 和 ObjectsWithLighting (OWL) 用于逆渲染任务。
*   **Benchmark与对比方法**：
    *   **前馈基准**：LIRM、MeshLRM、GS-LRM、RelitLRM等。
    *   **优化基准**：NVDiffrecMc、InvRender、NeuralPBIR等。
*   **任务类型**：涵盖了新视角合成（NVS）、显式网格提取、材质属性（BRDF）预测及重打光。

### 4. 资源与算力
*   **硬件设施**：使用了 **128块 NVIDIA H100 GPU**。
*   **训练时长**：
    *   阶段1（密集模型）：5天。
    *   阶段2（稀疏模型，中分辨率）：7天。
    *   阶段3（稀疏模型，高分辨率）：3天。
    *   **总计**：约15天的大规模集群训练。
*   **扩展规模**：相比SOTA方法（如LIRM），LSRM处理的物体token增加了**20倍**，图像token增加了**2倍以上**。

### 5. 实验数量与充分性
*   **实验规模**：论文在四个主流基准数据集上进行了详尽的定量评估，并提供了大量定性对比图（包括缩放细节对比）。
*   **消融实验**：针对“上下文窗口缩放”和“3D感知路由”分别做了消融研究，验证了各组件对提升纹理清晰度的独立贡献。
*   **公平性**：使用了相同的输入视图数量（12-18张）和评估脚本，并测试了模型在更少输入视图（如6-8张）下的泛化能力，实验设计客观且充分。

### 6. 主要结论与发现
*   **规模效应**：扩展上下文窗口是提升前馈重建质量的关键，LSRM在GSO数据集上比SOTA提升了 **2.4 dB PSNR**，并降低了 **40% 的 LPIPS**。
*   **性能跨越**：在逆渲染任务中，LSRM作为前馈模型，其感知质量（LPIPS）首次达到甚至超过了需要长时间迭代的密集视图优化方法。
*   **几何路由的重要性**：显式的3D空间路由比纯数据驱动的注意力机制更能有效提升纹理的锐度。

### 7. 优点
*   **高保真度**：成功解决了前馈模型长期存在的“纹理模糊”痛点，实现了接近照片级的重建效果。
*   **硬件友好**：通过NSA和定制的并行策略，在不按比例增加计算资源的情况下实现了超大规模token的处理。
*   **多功能性**：不仅支持NVS，还能输出高质量的PBR材质贴图，直接适配标准图形学渲染管线。

### 8. 不足与局限
*   **极端细节挑战**：尽管进步巨大，但对于极小字体的成分表等超精细纹理，模型仍可能无法完全还原。
*   **材质解耦难题**：在逆渲染中，准确估计粗糙度（Roughness）和金属度（Metallic）等属性依然存在病态性挑战，有时会出现估计偏差。
*   **算力门槛**：128块H100的训练成本极高，普通研究机构难以复现或微调该模型。

（完）
