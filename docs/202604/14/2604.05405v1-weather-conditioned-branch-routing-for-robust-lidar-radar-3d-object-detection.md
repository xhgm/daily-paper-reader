---
title: Weather-Conditioned Branch Routing for Robust LiDAR-Radar 3D Object Detection
title_zh: 面向鲁棒激光雷达-雷达 3D 目标检测的天气感知分支路由
authors: "Hongsheng Li, Lingfeng Zhang, Zexian Yang, Liang Li, Rong Yin, Xiaoshuai Hao, Wenbo Ding"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05405v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: LiDAR-雷达3D目标检测与多模态感知
tldr: 针对恶劣天气下传感器可靠性波动的问题，本文提出一种基于天气条件的动态分支路由框架。该方法通过并行维护纯LiDAR、纯4D毫米波雷达及融合分支，并利用轻量级路由器根据环境提示动态调整各分支权重，实现了在不同天气下的鲁棒3D目标检测。实验证明该方法在K-Radar基准上达到SOTA，并具有极高的可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05405v1/fig-001.webp\", \"caption\": \"Table 4: Ablation study on key hyperparameters of the proposed method. We evaluate the impact of the auxiliary weather supervision weight (𝜆aux), diversity regularization weight (𝜆div), and entropy regularization weight (𝜆ent). Best results are highlighted in bold.\", \"page\": 8, \"index\": 1, \"width\": 482, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.05405v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative results and interpretable routing behavior under diverse weather conditions. From top to bottom: Normal, Rain, Overcast, Sleet and Heavy Snow scenarios. For each scene, we visualize the camera view with 3D bounding box predictions (left), the adaptive branch weights𝑤𝐿,𝑤𝐹 ,𝑤𝑅 predicted by the weather-conditioned router (middle), and the corresponding radar and LiDAR BEV representations (right).\", \"page\": 8, \"index\": 2, \"width\": 456, \"height\": 712}, {\"url\": \"assets/figures/arxiv/2604.05405v1/fig-003.webp\", \"caption\": \"Figure 2: Framework overview of the proposed weather-conditioned branch routing method. The model takes LiDAR, 4D radar, and camera images as inputs. First, a condition token is generated by fusing visual features and semantic text prompts, which is further refined by sensor context. Next, LiDAR and radar features are processed through three parallel streams: a LiDAR-only branch, a radar-only branch, and a condition-gated fusion branch. A branch router then utilizes the condition token to predict adaptive weights (𝑤𝐿,𝑤𝐹 ,𝑤𝑅). These weights softly aggregate the branch-wise BEV features for the final 3D detection head. During training, auxiliary weather classification and diversity losses are applied to ensure meaningful routing behavior.\", \"page\": 3, \"index\": 3, \"width\": 998, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2604.05405v1/fig-004.webp\", \"caption\": \"Table 1: Quantitative results of LiDAR and 4D radar-based 3D object detection methods on K-Radar dataset. We present the modality of each method (L: LiDAR, 4DR: 4D radar) and detailed performance for each weather condition. Best in bold with blue background (best), second in light blue background (second). * denotes our reproduced results.\", \"page\": 7, \"index\": 4, \"width\": 1044, \"height\": 790}]"
motivation: 现有的LiDAR与4D雷达融合方法多采用固定或弱自适应流程，无法根据天气变化动态调整对不同模态的依赖。
method: 提出一种天气条件分支路由机制，利用视觉和语义提示引导路由器动态聚合LiDAR、4D雷达及融合分支的特征，并引入天气监督学习策略。
result: 在K-Radar基准测试中取得了最先进的性能，并清晰展示了模型在不同天气下对模态偏好的动态切换。
conclusion: 该研究通过动态路由有效提升了多模态感知在极端天气下的鲁棒性，为自动驾驶感知系统提供了透明且可靠的决策支持。
---

## 摘要
在恶劣天气下进行鲁棒的 3D 目标检测极具挑战性，因为不同传感器的可靠性会随环境变化。虽然现有的激光雷达-4D 雷达融合方法提高了鲁棒性，但它们主要依赖于固定或弱自适应的流水线，无法根据环境条件的变化动态调整模态偏好。为了弥补这一差距，我们将多模态感知重新定义为一个天气感知的分支路由问题。我们的框架并非计算单一的融合输出，而是显式地维持了三个并行的 3D 特征流：纯激光雷达分支、纯 4D 雷达分支以及一个条件门控融合分支。在从视觉和语义提示中提取的条件 token 的引导下，一个轻量级路由器动态预测样本特定的权重，以对这些表示进行软聚合。此外，为了防止分支崩溃，我们引入了一种结合辅助分类和多样性正则化的天气监督学习策略，以强制实现独特的、依赖于条件的路由行为。在 K-Radar 基准测试上的大量实验表明，我们的方法达到了最先进的性能。此外，该方法为模态偏好提供了显式且高度可解释的见解，透明地揭示了自适应路由如何在各种恶劣天气场景下，在激光雷达和 4D 雷达之间稳健地切换依赖重心。源代码将会公开发布。

## Abstract
Robust 3D object detection in adverse weather is highly challenging due to the varying reliability of different sensors. While existing LiDAR-4D radar fusion methods improve robustness, they predominantly rely on fixed or weakly adaptive pipelines, failing to dy-namically adjust modality preferences as environmental conditions change. To bridge this gap, we reformulate multi-modal perception as a weather-conditioned branch routing problem. Instead of computing a single fused output, our framework explicitly maintains three parallel 3D feature streams: a pure LiDAR branch, a pure 4D radar branch, and a condition-gated fusion branch. Guided by a condition token extracted from visual and semantic prompts, a lightweight router dynamically predicts sample-specific weights to softly aggregate these representations. Furthermore, to prevent branch collapse, we introduce a weather-supervised learning strategy with auxiliary classification and diversity regularization to enforce distinct, condition-dependent routing behaviors. Extensive experiments on the K-Radar benchmark demonstrate that our method achieves state-of-the-art performance. Furthermore, it provides explicit and highly interpretable insights into modality preferences, transparently revealing how adaptive routing robustly shifts reliance between LiDAR and 4D radar across diverse adverse-weather scenarios. The source code with be released.

---

## 论文详细总结（自动生成）

以下是对论文《Weather-Conditioned Branch Routing for Robust LiDAR-Radar 3D Object Detection》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：自动驾驶中的 3D 目标检测在恶劣天气（雨、雪、雾）下极具挑战。激光雷达（LiDAR）提供精确几何结构但在恶劣天气下信号衰减严重；4D 毫米波雷达（Radar）虽鲁棒但空间精度较低。
*   **核心问题**：现有的 LiDAR-Radar 融合方法大多采用**固定或弱自适应**的融合流水线。这种“一刀切”的设计无法根据环境变化动态调整对不同传感器的依赖，且缺乏决策过程的可解释性（即无法得知模型在特定天气下为何选择某种模态）。
*   **研究动机**：将多模态融合重新定义为“天气感知的分支路由问题”，通过显式维护多个表示分支并动态加权，实现环境自适应的鲁棒感知。

### 2. 方法论
核心思想是构建一个**天气感知的分支路由框架**，其关键技术细节包括：
*   **条件 Token 生成（Condition Token）**：
    *   **视觉提示**：通过轻量级 CNN 从相机图像提取视觉特征。
    *   **语义提示**：利用预训练的 CLIP 文本编码器，将 7 种天气描述（如“大雪场景”）转化为语义向量。
    *   **传感器上下文**：融合 LiDAR 和 Radar 的全局统计特征，最终生成统一的条件 Token $\hat{c}$。
*   **多分支架构**：
    *   **纯 LiDAR 分支**：保留精确的几何信息。
    *   **纯 4D Radar 分支**：提供全天候的鲁棒性。
    *   **条件门控融合分支**：利用条件 Token 引导的 KNN 注意力机制，将 Radar 特征动态注入 LiDAR 特征。
*   **分支路由器（Branch Router）**：
    *   使用 MLP 根据 $\hat{c}$ 预测三个分支的权重 $w = [w_L, w_R, w_F]$。
    *   通过**软聚合（Soft Aggregation）**方式合并 BEV 特征图，确保流程可微。
*   **天气监督学习策略**：
    *   **辅助分类损失 ($L_{aux}$)**：强制条件 Token 包含判别性的天气信息。
    *   **天气多样性正则化 ($L_{div}$)**：通过类内方差和类间距离约束，防止路由权重坍缩，确保不同天气下有明显的路由偏好差异。
    *   **分支熵正则化 ($L_{ent}$)**：防止训练早期模型过度依赖单一分支。

### 3. 实验设计
*   **数据集**：使用大规模自动驾驶基准 **K-Radar**，该数据集包含多种天气（晴天、雾、雨、雪、雨夹雪、阴天）。
*   **Benchmark 与指标**：针对 Sedan（轿车）类别，采用官方的 $AP_{3D}$ 和 $AP_{BEV}$ 指标，分别在 IoU=0.3 和 0.5 下评估。
*   **对比方法**：对比了纯 4D Radar 方法（RTNH）、纯 LiDAR 方法（PointPillars、RTNH-L）以及主流融合方法（InterFusion、3D-LRF）。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量。
*   **训练细节**：使用了 Adam 优化器，初始学习率为 $5 \times 10^{-4}$，采用余弦退火策略，Batch Size 为 4，共训练 20 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **SOTA 对比**：在 7 种天气条件下进行了详尽的性能对比。
    *   **消融实验**：验证了分支路由、天气监督损失（$L_{aux}, L_{div}$）的有效性。
    *   **设计对比**：对比了不同路由器架构（MLP vs. Transformer vs. Cross-Attention）及超参数敏感性。
*   **充分性评价**：实验设计较为充分，特别是在恶劣天气下的细分评估展示了该方法的优势。通过复现 3D-LRF 等强基线进行对比，保证了实验的客观性与公平性。

### 6. 主要结论与发现
*   **性能领先**：在 K-Radar 上达到了 SOTA 水平。在 IoU=0.5 的严苛条件下，$AP_{3D}$ 提升显著（从 30.0 提升至 43.5）。
*   **恶劣天气表现优异**：在重雪、大雾等极端场景下，该方法通过动态抑制受损的 LiDAR 分支并增强 Radar/融合分支，表现出极强的鲁棒性。
*   **可解释性验证**：定性分析证明，路由权重随天气变化的趋势符合传感器物理特性（如雪天 $w_L$ 下降，$w_F$ 和 $w_R$ 上升）。

### 7. 优点
*   **动态自适应**：打破了固定融合模式，实现了样本级的模态偏好调整。
*   **高可解释性**：分支权重直观地反映了模型对传感器的信任度，对安全关键型应用（如自动驾驶）至关重要。
*   **监督策略有效**：引入多样性正则化有效解决了多分支训练中的“分支坍缩”难题。

### 8. 不足与局限
*   **计算开销**：虽然路由器是轻量级的，但同时维护三个特征提取分支（尽管部分权重共享）可能会增加推理时的显存占用和计算延迟。
*   **传感器依赖**：路由决策部分依赖相机图像来识别天气，若相机在极端黑暗或遮挡下失效，可能会影响路由的准确性。
*   **数据集单一**：目前仅在 K-Radar 上验证，尚未在其他包含 4D Radar 的大型数据集（如 View-of-Delft 或 TJ4DRadSet）上进行泛化性测试。

（完）
