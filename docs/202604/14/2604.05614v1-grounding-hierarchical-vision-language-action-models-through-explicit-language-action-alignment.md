---
title: Grounding Hierarchical Vision-Language-Action Models Through Explicit Language-Action Alignment
title_zh: 通过显式语言-动作对齐实现分层视觉-语言-动作模型的关联
authors: "Theodor Wulff, Federico Tavella, Rahul Singh Maharjan, Manith Adikari, Angelo Cangelosi"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05614v1"
tags: ["query:q12"]
score: 10.0
evidence: 分层VLA模型中的显式语言-动作对齐
tldr: 本文针对机器人透明度及语言-动作一致性问题，提出一种通过显式对齐增强分层视觉-语言-动作（VLA）模型接地能力的新框架。该框架利用对比模型评估生成的子任务描述与实际动作轨迹的对齐程度，并通过离线偏好学习优化模型。在LanguageTable基准测试中，该方法在减少昂贵数据标注需求的同时，达到了与全监督微调相当的性能，为实现更可靠的人机协作提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05614v1/fig-001.webp\", \"caption\": \"Figure 2. Action-Conditioned Grounding Model. We extend a pre-trained SigLIP 2 by conditioning the visual features of the SigLIP 2 Vision Encoder on the encoded trajectories. Using a contrastive loss, we align the vision-action pairs with the low-level instructions.\", \"page\": 6, \"index\": 1, \"width\": 470, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2604.05614v1/fig-002.webp\", \"caption\": \"Figure 4. Quantitative Evaluation of Generated Trajectories.\", \"page\": 7, \"index\": 2, \"width\": 482, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.05614v1/fig-003.webp\", \"caption\": \"Figure 3. Quantitative Evaluation of Generated Low-level Instructions.\", \"page\": 7, \"index\": 3, \"width\": 482, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2604.05614v1/fig-004.webp\", \"caption\": \"Table 4. Hyperparameters grouped by model variant.\", \"page\": 14, \"index\": 4, \"width\": 335, \"height\": 743}, {\"url\": \"assets/figures/arxiv/2604.05614v1/fig-005.webp\", \"caption\": \"Figure 1. Method Overview. We extend a regular VLA (left) into a hierarchical VLA by adding a high-level VLM module to break a high-level instruction down into executable low-level instructions (center), following recent trends on hierarchical VLAs [4, 45]. To align the intermediate low-level instruction and the generated trajectory, we invoke a separately trained ranking model, which ranks N sampled output pairs based on their grounding in the environment. Based on these scores, we select the chosen and rejected output pairs that serve as preference data to update our transparent VLA and increase the alignment of the multimodal outputs.\", \"page\": 3, \"index\": 5, \"width\": 968, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2604.05614v1/fig-006.webp\", \"caption\": \"Figure 5. t-SNE visualizations of all grounding models on the LanguageTable dataset using visual inputs and low-level instructions.\", \"page\": 8, \"index\": 6, \"width\": 1036, \"height\": 274}]"
motivation: 现有的分层VLA模型在生成语言描述和底层动作时缺乏显式的跨模态对齐，导致机器人的自然语言沟通与实际行为可能不一致。
method: 提出一种利用对比模型对语言-动作轨迹对进行评分排序，并结合离线偏好学习来精细化调整分层VLA模型接地的训练框架。
result: 在LanguageTable数据集上的实验证明，该框架在显著降低对昂贵数据标注依赖的情况下，取得了与全监督微调相当的性能表现。
conclusion: 通过显式的语言-动作对齐和偏好学习，可以有效提升VLA模型的透明度与接地能力，是实现高效人机协作的关键步骤。
---

## 摘要
实现机器人透明度是迈向有效人机协作的关键一步。为了实现透明度，机器人的自然语言交流必须与其动作保持一致，并显式地关联到任务和环境中。现有的分层视觉-语言-动作（VLA）模型可以生成语言（例如通过思维链）和底层动作。然而，目前的工作在训练过程中并未考虑这些模态之间的显式对齐。为了填补这一关键空白，我们提出了一种新颖的训练框架，该框架将分层 VLA 子任务描述与视觉观测和动作空间进行显式关联。我们的框架使用对比模型来评估生成的语言与相应动作轨迹之间的对齐程度。该对比模型能够根据对齐程度对不同的语言-轨迹对进行直接排序，使我们能够通过离线偏好学习来优化分层 VLA 的关联性。我们将该框架应用于 LanguageTable 数据集（一个由人类语言标注轨迹的基准数据集），并提供了关于多模态关联表示的关键见解，同时建立了一个强大的基准，其性能可与全监督微调相媲美，并最大限度地减少了对昂贵数据标注的需求。

## Abstract
Achieving robot transparency is a critical step toward effective human-robot collaboration. To be transparent, a robot's natural language communication must be consistent with its actions and explicitly grounded in the task and environment. Existing hierarchical Vision-Language-Action (VLA) models can generate language (e.g., through chain-of-thought) and low-level actions. However, current work does not consider explicit alignment between these modalities during training. To address this crucial gap, we propose a novel training framework that explicitly grounds hierarchical VLA sub-task descriptions with respect to the visual observation and action space. Our framework uses a contrastive model to assess the alignment between generated language and corresponding action trajectories. This contrastive model enables direct ranking of different language-trajectory pairs based on their alignment, allowing us to refine the grounding of our hierarchical VLA through offline preference learning. We apply our framework to the LanguageTable dataset, a benchmark dataset of human language-annotated trajectories, and provide critical insights into multimodal grounding representations, all while establishing a strong baseline that achieves performance comparable to fully supervised fine-tuning and minimizing the need for costly data annotations.

---

## 论文详细总结（自动生成）

这篇论文《Grounding Hierarchical Vision-Language-Action Models Through Explicit Language-Action Alignment》由曼彻斯特大学的研究团队提出，旨在解决分层视觉-语言-动作（VLA）模型中语言描述与实际动作不一致的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在人机协作中，机器人的透明度至关重要。现有的分层 VLA 模型虽然能生成中间子任务描述（语言）和底层动作，但两者在训练中往往是解耦的，导致机器人“言行不一”，即生成的语言说明与实际执行的动作轨迹缺乏显式的对齐（Grounding）。
*   **研究背景**：现有的 VLA 模型主要关注任务成功率，而忽略了中间语言输出的准确性。为了实现真正的透明度，需要确保机器人的自然语言交流能够准确反映其视觉感知和物理行为。

### 2. 方法论
该论文提出了一种名为 **GPLA（Grounded Preference-based Language-action Alignment）** 的框架，核心思想是通过对比学习和偏好学习来强化语言与动作的对齐。
*   **分层架构**：
    *   **高层 VLM**：使用 Gemma-3-4B 模型，将高层指令分解为低层子任务描述。
    *   **底层 VLA**：使用 SmolVLA 模型，根据子任务描述和视觉观测生成动作轨迹。
*   **关键技术细节**：
    1.  **动作条件关联模型（Action-Conditioned Grounding Model）**：基于 SigLIP 2 构建，通过 FiLM 层将动作轨迹注入视觉特征，利用对比学习（InfoNCE Loss）将“视觉+动作”对与“语言描述”映射到统一的嵌入空间。
    2.  **偏好对生成**：对于同一观测，模型生成 $N$ 个语言-动作候选对，利用关联模型进行评分，选出得分最高（Chosen）和最低（Rejected）的对。
    3.  **SimPO 优化**：采用无参考模型的简单偏好优化（SimPO）算法，直接对高层 VLM 进行微调，使其倾向于生成与动作更匹配的语言描述。

### 3. 实验设计
*   **数据集与场景**：使用 **LanguageTable** 基准数据集，这是一个包含人类语言标注的机器人（Franka 机械臂）推块任务数据集。
*   **对比方法**：
    *   全监督微调（Supervised Fine-Tuning, SFT）。
    *   仅底层 VLA（Low-Level Only）。
    *   基于 CLIP 或原始 SigLIP 2 的 GPLA 变体。
*   **评估指标**：
    *   **语言维度**：BLEU、ROUGE、METEOR、BERTScore。
    *   **动作维度**：均方误差（MSE）、平均绝对误差（MAE）、余弦相似度（CosSim）。

### 4. 资源与算力
*   **硬件**：使用了 **1 张 NVIDIA A100 GPU**。
*   **训练时长**：
    *   高层 VLM 微调：1,500 步。
    *   底层 VLA 微调：15,000 步。
    *   GPLA 偏好对齐：100 步（极短的微调步数即可见效）。
    *   关联模型预训练：50,000 步。
*   **批处理大小**：训练 Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 6 种不同的模型配置，涵盖了从纯监督学习到不同关联模型驱动的偏好学习。
*   **充分性评价**：实验设计较为客观，不仅提供了量化的性能指标，还通过 **t-SNE 可视化** 深入分析了嵌入空间的对齐情况，并提供了定性的案例分析。虽然仅在一个数据集（LanguageTable）上进行了验证，但对于证明“偏好学习可替代昂贵标注”这一核心论点是充分的。

### 6. 主要结论与发现
*   **性能对等**：GPLA 在动作轨迹生成性能上达到了与全监督微调（SFT）相当的水平，但显著降低了对高质量人工标注的依赖。
*   **语义稳定性**：虽然 GPLA 在 BLEU 等基于词汇重叠的指标上得分较低，但 BERTScore（语义相似度）保持稳定，说明模型生成了语义正确但表达多样的指令。
*   **对齐效果**：动作条件关联模型比传统的 CLIP/SigLIP 能更好地融合视觉和动作模态，使语言与物理行为在嵌入空间中重叠。

### 7. 优点
*   **降低标注成本**：通过自监督的对比模型生成偏好数据，解决了分层模型中间步骤难以大规模标注的痛点。
*   **提升透明度**：显式地优化了“言行一致性”，使机器人的解释更具可信度。
*   **算法高效**：采用 SimPO 算法，无需维护参考模型，节省了显存和计算开销。

### 8. 不足与局限
*   **空间推理局限**：模型在处理复杂的物体间空间关系（如“在...左侧”）时仍存在挑战，这受限于底层 VLM 的基础能力。
*   **泛化性验证不足**：实验仅限于 LanguageTable 模拟环境，尚未在更复杂的真实世界场景或多任务环境中验证其泛化能力。
*   **数据依赖**：对比学习的效果仍高度依赖于机器人轨迹数据的多样性。

（完）
