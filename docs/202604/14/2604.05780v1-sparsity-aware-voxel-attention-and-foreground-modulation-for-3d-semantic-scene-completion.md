---
title: Sparsity-Aware Voxel Attention and Foreground Modulation for 3D Semantic Scene Completion
title_zh: 面向三维语义场景补全的稀疏感知体素注意力与前景调制
authors: "Yu Xue, Longjun Gao, Yuanqi Su, HaoAng Lu, Xiaoning Zhang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05780v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 从单张 RGB 图像为机器人重建完整的 3D 语义场景
tldr: 针对单目3D语义场景补全中体素分布极度稀疏且前景类别长尾分布的问题，本文提出VoxSAMNet。该框架引入DSFR模块利用虚拟节点绕过空体素并细化占用体素，同时结合前景调制策略（FD和TGIF）缓解过拟合并增强类别特征。在SemanticKITTI等基准上达到SOTA性能，显著提升了补全效率与准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05780v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative results on the SemanticKITTI hidden test set. Bold denotes the best performance.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.05780v1/fig-002.webp\", \"caption\": \"Figure 1. Motivation for VoxSAMNet. (a) Current methods such as BEVFormer [17] and Et-former [19] use deformable attention to update features for both empty and occupied voxels. (b) The ratio of occupied and empty voxels in the SemanticKITTI [3] dataset. (c) We designed a branch that uses two different feature update methods for empty voxels and occupied voxels.\", \"page\": 2, \"index\": 2, \"width\": 882, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.05780v1/fig-003.webp\", \"caption\": \"Table 2. Quantitative results on the SSCBench-KITTI-360 test set. Bold denotes the best performance.\", \"page\": 7, \"index\": 3, \"width\": 960, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.05780v1/fig-004.webp\", \"caption\": \"Figure 2. Overview of VoxSAMNet and the structure of its modules. (a) The pipeline flow of the proposed VoxSAMNet. (b) The specific structure of the Text-Guided Image Filter. (c) The specific structure of Dummy Shortcut for Feature Refinement.\", \"page\": 4, \"index\": 4, \"width\": 932, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.05780v1/fig-005.webp\", \"caption\": \"Figure 3. Result of VoxSAMNet. Qualitative visual comparisons with Monoscene [4], Symphonies [10], VisHall3D [29], and Ground Truth (GT) on the SemanticKITTI [3] dataset.\", \"page\": 5, \"index\": 5, \"width\": 932, \"height\": 450}]"
motivation: "旨在解决单目SSC中体素分布严重不平衡（空体素占比超93%）以及前景类别稀少导致的计算冗余和模型泛化性差的问题。"
method: 提出一种稀疏感知体素注意力网络，通过虚拟节点快捷方式优化特征提取，并利用前景丢弃与文本引导图像滤波增强关键语义特征。
result: "在SemanticKITTI和SSCBench-KITTI-360基准上分别实现18.2%和20.2%的mIoU，性能优于现有的单目及双目基准方法。"
conclusion: 证明了显式建模体素稀疏性和语义不平衡对于高效、准确的3D场景补全至关重要，为未来研究提供了有效方向。
---

## 摘要
单目语义场景补全 (SSC) 旨在从单张 RGB 图像重建完整的三维语义场景，为自动驾驶和机器人技术提供了一种具有成本效益的解决方案。然而，体素分布固有的不平衡性（其中超过 93% 的体素为空，且前景类别稀少）带来了重大挑战。现有方法通常对无信息体素存在冗余关注，且对长尾类别的泛化能力较差。为了解决这些问题，我们提出了 VoxSAMNet（体素稀疏感知调制网络），这是一个显式建模体素稀疏性和语义不平衡的统一框架。我们的方法引入了：(1) 用于特征细化的虚拟捷径 (DSFR) 模块，该模块通过共享虚拟节点绕过空体素，同时利用可变形注意力细化被占据的体素；(2) 前景调制策略，结合了前景 Dropout (FD) 和文本引导图像滤波器 (TGIF)，以缓解过拟合并增强类别相关特征。在公共基准数据集 SemanticKITTI 和 SSCBench-KITTI-360 上的广泛实验表明，VoxSAMNet 达到了最先进的性能，其 mIoU 分数分别为 18.2% 和 20.2%，超越了之前的单目和双目基准模型。我们的结果强调了稀疏感知和语义引导设计对于高效且准确的三维场景补全的重要性，为未来的研究提供了一个有前景的方向。

## Abstract
Monocular Semantic Scene Completion (SSC) aims to reconstruct complete 3D semantic scenes from a single RGB image, offering a cost-effective solution for autonomous driving and robotics. However, the inherently imbalanced nature of voxel distributions, where over 93% of voxels are empty and foreground classes are rare, poses significant challenges. Existing methods often suffer from redundant emphasis on uninformative voxels and poor generalization to long-tailed categories. To address these issues, we propose VoxSAMNet (Voxel Sparsity-Aware Modulation Network), a unified framework that explicitly models voxel sparsity and semantic imbalance. Our approach introduces: (1) a Dummy Shortcut for Feature Refinement (DSFR) module that bypasses empty voxels via a shared dummy node while refining occupied ones with deformable attention; and (2) a Foreground Modulation Strategy combining Foreground Dropout (FD) and Text-Guided Image Filter (TGIF) to alleviate overfitting and enhance class-relevant features. Extensive experiments on the public benchmarks SemanticKITTI and SSCBench-KITTI-360 demonstrate that VoxSAMNet achieves state-of-the-art performance, surpassing prior monocular and stereo baselines with mIoU scores of 18.2% and 20.2%, respectively. Our results highlight the importance of sparsity-aware and semantics-guided design for efficient and accurate 3D scene completion, offering a promising direction for future research.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **VoxSAMNet** 的新型单目三维语义场景补全（SSC）框架，旨在解决自动驾驶场景中三维空间极度稀疏以及语义类别分布严重不平衡的核心挑战。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：单目 SSC 任务旨在仅凭单张 RGB 图像重建完整的 3D 语义场景。然而，该任务面临两个严峻的“不平衡”挑战：
    1.  **空间稀疏性不平衡**：在 3D 空间中，超过 93% 的体素是空的，现有方法对所有体素进行统一处理，导致计算资源在无信息区域严重浪费。
    2.  **语义分布不平衡**：前景类别（如行人、骑行者）极其稀少，导致模型容易对常见类别过拟合，对长尾类别泛化性差。
*   **研究背景**：现有的变形注意力（Deformable Attention）机制虽然能自适应采样，但往往忽略了体素是否被占据，且在 2D 到 3D 投影时存在特征歧义。

### 2. 论文提出的方法论
VoxSAMNet 提出了一个统一的框架，包含三个关键阶段：
*   **文本引导的 3D 初始化（Text-Guided 3D Initialization）**：
    *   **前景丢弃（Foreground Dropout, FD）**：在训练时随机掩盖部分前景类别的监督信号，强迫模型学习更具泛化性的特征。
    *   **文本引导图像滤波器（TGIF）**：利用 CLIP 风格的文本提示（如“道路、建筑”）与 2D 图像特征进行交叉注意力融合，抑制被丢弃类别的残余响应，增强相关类别的特征。
*   **虚拟捷径特征细化（Dummy Shortcut for Feature Refinement, DSFR）**：
    *   **稀疏感知处理**：首先通过一个轻量级分类器预测体素的占用状态。
    *   **虚拟节点（Dummy Node）**：对于预测为空的体素，直接映射到一个共享的、可学习的虚拟嵌入向量，绕过复杂的计算。
    *   **细化占用体素**：仅对被占据的体素应用 3D 变形注意力机制，从 2D 特征图中提取精细信息。
*   **补全与去噪**：采用基于掩码体素建模（MAE）的策略，通过 3D U-Net 进行上下文推理，恢复遮挡或未观测区域的几何与语义。

### 3. 实验设计
*   **数据集**：
    *   **SemanticKITTI**：包含 20 个语义类别，提供密集的体素级标注。
    *   **SSCBench-KITTI-360**：更具挑战性的长距离场景数据集，包含 19 个类别。
*   **对比方法**：对比了包括单目（MonoScene, VoxFormer, CG-Former, DISC 等）、时间序列（SOAP, FlowScene）以及双目/多视图（ScanSSC, VLScene）在内的多种最先进方法。
*   **评价指标**：主要使用平均交并比（mIoU）和几何交并比（IoU）。

### 4. 资源与算力
*   **硬件配置**：使用了 **4 台 NVIDIA RTX A100 GPU** 进行训练。
*   **训练细节**：Batch size 为 4，共训练 30 个 epoch。使用 AdamW 优化器，初始学习率为 $10^{-4}$。
*   **推理效率**：单帧推理时间为 **284ms**，优于 Symphonize (310ms) 和 OccFormer (338ms)。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准上进行了详尽测试，并在 SemanticKITTI 的**隐藏测试集**（Hidden Test Set）上提交了结果，确保了评估的客观性。
*   **消融实验**：针对 TGIF、FD、DSFR 模块分别做了组合实验，验证了各模块的增量贡献。此外还进行了主干网络（ResNet vs Swin Transformer）的对比实验。
*   **计算量分析**：专门对比了 DSFR 模块带来的 FLOPs 变化，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能领先**：VoxSAMNet 在 SemanticKITTI 上达到 **18.2% mIoU**，在 KITTI-360 上达到 **20.2% mIoU**，刷新了单目 SSC 的记录，甚至超越了部分利用多帧或双目信息的方法。
*   **高效性**：DSFR 模块将变形注意力的计算量降低了 **98.5%**，整体 FLOPs 降低了 **71.9%**，证明了显式建模稀疏性的巨大价值。
*   **语义增强**：文本引导和前景丢弃策略有效缓解了长尾分布下的过拟合问题。

### 7. 优点
*   **计算分配极度合理**：通过“虚拟捷径”巧妙避开了空体素的无效计算，是处理 3D 稀疏性的一种优雅方案。
*   **多模态融合创新**：将文本提示引入 2D 空间特征过滤，为 SSC 任务提供了新的语义约束思路。
*   **泛化能力强**：在处理小物体和稀有类别（如人、自行车）上表现出更好的鲁棒性。

### 8. 不足与局限
*   **依赖深度估计**：虽然是单目框架，但其 3D 初始化仍高度依赖预训练的深度网络（DepthNet），深度误差可能会传递至后续模块。
*   **文本提示的局限**：目前的文本引导主要基于类别标签，对于场景中复杂的动态交互或特定属性的描述利用尚浅。
*   **单帧限制**：尽管性能优异，但作为单帧方法，在处理视频序列时可能缺乏时间连续性约束。

（完）
