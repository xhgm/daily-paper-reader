---
title: Critical Patch-Aware Sparse Prompting with Decoupled Training for Continual Learning on the Edge
title_zh: 面向边缘侧持续学习的解耦训练关键补丁感知稀疏提示
authors: "Wonseon Lim, Jaesung Lee, Dae-Won Kim"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07399v1"
tags: ["query:q15"]
score: 9.0
evidence: 使用稀疏提示的边缘设备持续学习框架
tldr: 针对边缘设备上持续学习面临的内存和计算资源限制，本文提出CPS-Prompt框架。该方法通过关键补丁采样（CPS）实现任务感知的令牌减少，并利用解耦提示与分类器训练（DPCT）降低反向传播开销。在保证与最先进方法相当的准确率的同时，显著提升了训练时的内存、时间和能源效率，为边缘端高效持续学习提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07399v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison of accuracy and training-time efficiency on CIFAR-100 using PCL and token reduction methods, with efficiency metrics measured on a Jetson Orin Nano. Our method exhibits a more balanced accuracy–efficiency trade-off under edgedevice constraints.\", \"page\": 1, \"index\": 1, \"width\": 467, \"height\": 360}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of CL methods on three datasets. ▼/△ indicate that the corresponding method is statistically worse/better than the proposed method, based on a paired t-test (p < 0.05). Higher is better for Accuracy (↑), while lower is better for Forgetting (↓). All results are averaged over 10 runs.\", \"page\": 6, \"index\": 2, \"width\": 1026, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-003.webp\", \"caption\": \"Figure 3. Comparison of memory usage, training time, and energy consumption between our method and other PCL methods on three datasets on the Jetson Orin Nano. For clarity, method names are abbreviated (e.g., CODA = CODA-Prompt, CP = C-Prompt, OS++ = OS-Prompt++, OS = OS-Prompt).\", \"page\": 6, \"index\": 3, \"width\": 1005, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-004.webp\", \"caption\": \"Figure 4. Comparison of accuracy and memory usage between CPS-Prompt and other token reduction methods based on the CODAPrompt baseline under varying reduction ratios on the Jetson Orin Nano.\", \"page\": 7, \"index\": 4, \"width\": 1005, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-005.webp\", \"caption\": \"Figure 5. Comparison of training time and memory usage between CPS-Prompt and other token reduction methods based on the CODAPrompt baseline under varying reduction ratios on the Jetson Orin Nano.\", \"page\": 7, \"index\": 5, \"width\": 1005, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-006.webp\", \"caption\": \"Figure 2. Overview of the CPS-Prompt framework. Left: CPS selects a small subset of task-relevant patches to preserve accuracy while reducing memory usage. Right: DPCT mitigates representation mismatch through decoupled training, where the prompt is optimized with sparse patches and the classifier with full patches.\", \"page\": 3, \"index\": 6, \"width\": 1036, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2604.07399v1/fig-007.webp\", \"caption\": \"Figure 8. Qualitative comparison between deterministic top-k and stochastic sampling at a reduction ratio of 0.5 on CUB-200.\", \"page\": 8, \"index\": 7, \"width\": 396, \"height\": 311}]"
motivation: 现有的基于提示的持续学习方法主要关注推理性能，忽视了边缘设备在片上训练时的内存和计算成本。
method: 提出关键补丁采样以减少令牌数量，并采用解耦训练策略来降低反向传播的计算负担。
result: 在多个基准测试中，相比CODA-Prompt，该方法在峰值内存、训练时间和能效上提升了约1.6倍，且准确率保持竞争力。
conclusion: CPS-Prompt在保持高准确率的同时，有效解决了边缘设备持续学习的训练效率瓶颈。
---

## 摘要
边缘设备上的持续学习（CL）不仅需要高准确率，还需要训练效率，以支持在严格的内存和计算约束下的设备端自适应。虽然基于提示的持续学习（PCL）具有参数效率且能达到极具竞争力的准确率，但以往的工作主要集中在准确率或推理性能上，往往忽略了设备端训练的内存和计算成本。在本文中，我们提出了 CPS-Prompt，这是一种关键补丁感知稀疏提示框架，通过集成用于任务感知令牌（token）缩减的关键补丁采样（CPS）和用于减少反向传播开销的解耦提示与分类器训练（DPCT），明确针对训练时的内存使用和计算成本。在三个公共基准测试和真实边缘硬件上的实验表明，CPS-Prompt 在峰值内存、训练时间和能源效率方面比平衡的 CODA-Prompt 基准提高了约 1.6 倍，同时平均准确率保持在最先进的 C-Prompt 的 2% 以内，且在准确率上与 CODA-Prompt 保持相当的竞争力。代码可在 https://github.com/laymond1/cps-prompt 获取。

## Abstract
Continual learning (CL) on edge devices requires not only high accuracy but also training-time efficiency to support on-device adaptation under strict memory and computational constraints. While prompt-based continual learning (PCL) is parameter-efficient and achieves competitive accuracy, prior work has focused mainly on accuracy or inference-time performance, often overlooking the memory and computational costs of on-device training. In this paper, we propose CPS-Prompt, a critical patch-aware sparse prompting framework that explicitly targets training-time memory usage and computational cost by integrating critical patch sampling (CPS) for task-aware token reduction and decoupled prompt and classifier training (DPCT) to reduce backpropagation overhead. Experiments on three public benchmarks and real edge hardware show that CPS-Prompt improves peak memory, training time, and energy efficiency by about 1.6x over the balanced CODA-Prompt baseline, while maintaining accuracy within 2% of the state-of-the-art C-Prompt on average and remaining competitive with CODA-Prompt in accuracy. The code is available at https://github.com/laymond1/cps-prompt.

---

## 论文详细总结（自动生成）

这篇论文针对边缘设备上的持续学习（Continual Learning, CL）提出了一种高效的稀疏提示框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现代边缘设备（如机器人、无人机、智能手机）需要在本地进行持续学习以适应新任务，但受限于共享的 CPU-GPU 内存和有限的计算预算。
*   **核心问题**：现有的基于提示的持续学习（PCL）方法虽然参数效率高，但主要关注推理性能或准确率，忽视了**训练阶段**的资源消耗。特别是训练时的中间激活值（Activations）会导致内存峰值过高，且反向传播计算开销大，难以在资源受限的边缘硬件上稳定运行。

### 2. 方法论：核心思想与关键技术
论文提出了 **CPS-Prompt** 框架，包含两个核心模块：
*   **关键补丁采样（Critical Patch Sampling, CPS）**：
    *   **核心思想**：利用 PCL 结构中固有的“冻结查询编码器”生成的信号，识别并保留对当前任务最重要的图像补丁（Patches），丢弃冗余补丁。
    *   **技术细节**：从查询编码器的最后一个 Transformer 块提取类令牌（Class Token）对各补丁的注意力权重（Attention）和特征范数（Value Norm）。计算得分 $s = A \odot \|V\|$，通过带温度系数的 Softmax 转化为概率分布，并使用多项式采样（Multinomial Sampling）选择补丁。
*   **解耦提示与分类器训练（Decoupled Prompt and Classifier Training, DPCT）**：
    *   **核心思想**：解决稀疏训练与全量推理之间的表示失配问题，并减少计算量。
    *   **流程**：
        1.  **提示训练阶段**：使用 CPS 选出的稀疏补丁，同时优化提示参数和分类器。
        2.  **分类器对齐阶段**：冻结提示参数，使用全量补丁仅微调分类器，以对齐推理时的特征分布。

### 3. 实验设计
*   **数据集**：CIFAR-100、ImageNet-R、CUB-200（均划分为 10 个任务）。
*   **Benchmark（基准）**：
    *   **传统 CL 方法**：LwF、ER。
    *   **PCL 方法**：L2P、DualPrompt、CODA-Prompt、C-Prompt（SOTA）、OS-Prompt。
    *   **令牌缩减方法**：ToMe（Token Merging）、PatchDropout。
*   **评估指标**：平均准确率（ACC）、遗忘率（FGT）、GPU 峰值内存、每任务训练时间、能源消耗。

### 4. 资源与算力
*   **硬件平台**：
    *   **边缘端评估**：NVIDIA Jetson Orin Nano（用于测量内存、时间和能效）。
    *   **常规训练**：NVIDIA RTX 4090 GPU。
*   **模型骨干**：ViT-Tiny/16（在 ImageNet-21K 上预训练）。
*   **训练细节**：ImageNet-R 训练 50 轮，其余数据集 20 轮；Batch Size 为 16。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准数据集上进行了完整对比，每组实验均运行 10 次并取平均值，使用了配对 t 检验（p < 0.05）来验证统计显著性。
*   **消融实验**：详细分析了 CPS 与 DPCT 的组合效果、采样温度 $\tau$、训练阶段比例 $\lambda$ 以及随机采样与确定性采样的对比。
*   **充分性评价**：实验设计客观、公平，不仅对比了准确率，还深入探讨了边缘设备最关心的资源效率指标，论证充分。

### 6. 主要结论与发现
*   **效率提升**：相比基准方法 CODA-Prompt，CPS-Prompt 在峰值内存、训练时间和能效上均提升了约 **1.6 倍**。
*   **准确率保持**：在大幅降低资源消耗的同时，准确率仅比最先进但极其耗时的 C-Prompt 低约 2%，且与 CODA-Prompt 相当。
*   **采样策略**：带有控制随机性的随机采样（Stochastic Sampling）比简单的 Top-K 确定性选择具有更好的泛化能力。

### 7. 优点
*   **训练感知优化**：不同于多数只关注推理的研究，该方法直接解决了边缘端“训练难”的痛点。
*   **任务感知稀疏化**：利用冻结的查询分支进行零成本（Training-free）的补丁筛选，设计巧妙。
*   **软硬件结合**：在真实的边缘硬件（Jetson）上验证了能效，具有很强的实用参考价值。

### 8. 不足与局限
*   **超参数敏感性**：温度系数 $\tau$ 和训练阶段比例 $\lambda$ 在不同数据集上需要微调以达到最优效果。
*   **架构限制**：该方法高度依赖于 Vision Transformer（ViT）的注意力机制和 PCL 的两阶段架构，难以直接迁移到 CNN 等其他架构。
*   **极端稀疏下的损失**：当补丁缩减比例极高（如超过 60%）时，准确率仍会出现较明显的下滑。

（完）
