---
title: "PointTPA: Dynamic Network Parameter Adaptation for 3D Scene Understanding"
title_zh: PointTPA：用于三维场景理解的动态网络参数自适应
authors: "Siyuan Liu, Chaoqun Zheng, Xin Zhou, Tianrui Feng, Dingkang Liang, Xiang Bai"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04933v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 场景级点云理解的测试时参数自适应
tldr: "针对3D场景理解中几何多样性和布局复杂导致的静态网络参数适应性不足问题，本文提出PointTPA框架。该框架通过序列化邻域分组（SNG）形成局部一致的补丁，并利用动态参数投影器（DPP）为每个补丁生成输入感知的自适应权重。PointTPA以极低的参数开销（小于2%）显著提升了模型在动态场景下的表现，在ScanNet等多个基准测试中超越了现有的参数高效微调方法。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04933v1/fig-001.webp\", \"caption\": \"Table 2. Results on ScanNet Validation [8] with decoder.\", \"page\": 6, \"index\": 1, \"width\": 491, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.04933v1/fig-002.webp\", \"caption\": \"Figure 1. (a) Scene-level point clouds have more points and highly imbalanced category distributions compared to object-level point clouds. (b) Different from the existing PEFT methods, our PointTPA provides dynamic projection weights during inference and performs better in 3D semantic segmentation.\", \"page\": 2, \"index\": 2, \"width\": 1022, \"height\": 551}, {\"url\": \"assets/figures/arxiv/2604.04933v1/fig-003.webp\", \"caption\": \"Figure 2. More visualizations of the semantic segmentation results of our PointTPA on four large-scale scene datasets. (a) ScanNet [8], (b) ScanNet200 [39], (c) ScanNet++ [54], (d) S3DIS [3] with 3 views.\", \"page\": 13, \"index\": 3, \"width\": 1025, \"height\": 616}, {\"url\": \"assets/figures/arxiv/2604.04933v1/fig-004.webp\", \"caption\": \"Figure 5. Qualitative comparison against IDPT [58]. Green and red boxes indicate correct and incorrect segmentations, respectively, with GT denoting the ground truth.\", \"page\": 8, \"index\": 4, \"width\": 496, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.04933v1/fig-005.webp\", \"caption\": \"Figure 6. Visualization of the similarity of dynamic weights.\", \"page\": 8, \"index\": 5, \"width\": 496, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.04933v1/fig-006.webp\", \"caption\": \"Figure 3. Illustration of our mixed-insertion strategy. PointTPA is applied to the last block of each stage, while static adapters are adopted in the remaining blocks.\", \"page\": 5, \"index\": 6, \"width\": 492, \"height\": 382}]"
motivation: 现有的3D场景理解方法在推理阶段依赖静态网络参数，难以灵活应对多样化的几何结构和复杂的空间布局。
method: 提出一种测试时参数自适应框架，结合序列化邻域分组（SNG）和动态参数投影器（DPP）来生成输入感知的局部自适应权重。
result: "在ScanNet验证集上实现了78.4%的mIoU，且仅引入了不到骨干网络2%的轻量级模块，性能优于多种参数高效微调方法。"
conclusion: PointTPA证明了通过测试时动态调整网络参数，可以在保持极高参数效率的同时显著增强模型对复杂3D场景的理解能力。
---

## 摘要
由于几何形状多样、类别分布不均以及空间布局变化剧烈，场景级点云理解仍然具有挑战性。现有方法虽然提高了物体级性能，但在推理过程中依赖静态网络参数，限制了其对动态场景数据的适应能力。我们提出了 PointTPA，这是一种测试时参数自适应（Test-time Parameter Adaptation）框架，可为场景级点云生成输入感知的网络参数。PointTPA 采用基于序列化的邻域分组（SNG）来形成局部相干的补丁，并利用动态参数投影器（DPP）生成逐补丁的自适应权重，使骨干网络能够根据特定场景的变化调整其行为，同时保持较低的参数开销。通过集成到 PTv3 结构中，PointTPA 引入了两个参数量不到骨干网络 2% 的轻量级模块，展现了极高的参数效率。尽管参数开销极小，PointTPA 在 ScanNet 验证集上仍达到了 78.4% 的 mIoU，在多个基准测试中超越了现有的参数高效微调（PEFT）方法，凸显了我们的测试时动态网络参数自适应机制在增强三维场景理解方面的有效性。代码可在 https://github.com/H-EmbodVis/PointTPA 获取。

## Abstract
Scene-level point cloud understanding remains challenging due to diverse geometries, imbalanced category distributions, and highly varied spatial layouts. Existing methods improve object-level performance but rely on static network parameters during inference, limiting their adaptability to dynamic scene data. We propose PointTPA, a Test-time Parameter Adaptation framework that generates input-aware network parameters for scene-level point clouds. PointTPA adopts a Serialization-based Neighborhood Grouping (SNG) to form locally coherent patches and a Dynamic Parameter Projector (DPP) to produce patch-wise adaptive weights, enabling the backbone to adjust its behavior according to scene-specific variations while maintaining a low parameter overhead. Integrated into the PTv3 structure, PointTPA demonstrates strong parameter efficiency by introducing two lightweight modules of less than 2% of the backbone's parameters. Despite this minimal parameter overhead, PointTPA achieves 78.4% mIoU on ScanNet validation, surpassing existing parameter-efficient fine-tuning (PEFT) methods across multiple benchmarks, highlighting the efficacy of our test-time dynamic network parameter adaptation mechanism in enhancing 3D scene understanding. The code is available at https://github.com/H-EmbodVis/PointTPA.

---

## 论文详细总结（自动生成）

这是一份关于论文《PointTPA: Dynamic Network Parameter Adaptation for 3D Scene Understanding》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的3D场景理解模型（如语义分割）在推理阶段通常使用**静态网络参数**。然而，场景级点云具有极其复杂的特性：局部几何形状多样、类别分布严重失衡（如地板多、椅子少）、空间布局多变。静态参数难以灵活适应不同场景的特定几何特征。
*   **核心问题**：如何在保持参数高效（PEFT）的前提下，让模型在推理时能够根据输入点云的特征动态调整其网络权重，从而提升对复杂3D场景的泛化能力。

### 2. 方法论
PointTPA 框架主要由两个核心模块组成，旨在实现“输入感知”的动态参数调整：
*   **序列化邻域分组 (SNG)**：
    *   为了处理无序且大规模的点云，SNG 利用**空间填充曲线**（如 Hilbert 或 Z-order 曲线）将点云序列化。
    *   通过这种方式将空间邻近的点划分为局部相干的“补丁”（Patches），为后续的局部参数自适应提供结构化基础。
*   **动态参数投影器 (DPP)**：
    *   **参数基准集 (Parameter Base Set)**：预设一组可学习的权重基底 $P$。
    *   **权重路由 (Weight Router)**：通过轻量级 MLP 对每个补丁的特征进行处理，生成路由系数。
    *   **动态合成**：根据路由系数对权重基底进行线性组合，为每个补丁生成专属的投影权重。
*   **混合插入策略**：为了平衡效率与性能，PointTPA 仅在每个编码器阶段的最后一个块插入动态 DPP 模块，而在其他块使用静态适配器（Static Adapters）。

### 3. 实验设计
*   **数据集**：ScanNet (V2), ScanNet++, S3DIS (Area 5), 以及更具挑战性的 ScanNet200。
*   **基准模型 (Backbone)**：以 Point Transformer V3 (PTv3) 为基础，并加载了 Sonata 自监督预训练权重。
*   **对比方法**：
    *   **全量微调 (FFT)**：作为性能上限。
    *   **通用 PEFT 方法**：Adapter, Prefix Tuning, BitFit, LoRA, VeRA, RandLoRA。
    *   **点云专用 PEFT 方法**：IDPT, DAPT, PointGST。
*   **评估指标**：平均交并比 (mIoU)、全要素准确率 (allAcc)、平均准确率 (mAcc)。

### 4. 资源与算力
*   **硬件环境**：实验在 **2 台 NVIDIA RTX 4090 GPU** 上完成。
*   **训练配置**：总 Batch Size 为 4。
*   **训练时长**：文中未明确给出总训练小时数，但通过图表对比显示，PointTPA 的训练和推理耗时远低于全量微调（FFT），且比同类点云 PEFT 方法（如 PointGST）快约 4 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 4 个主流大规模场景数据集，并进行了详尽的消融实验。
*   **消融实验内容**：包括 SNG 和 DPP 的有效性、动态基底数量、分组数量、缩放因子、DPLayer 插入位置、空间填充曲线类型以及插入策略等。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集验证和针对不同组件的细粒度拆解，有力证明了动态参数机制在处理场景级差异时的优越性。

### 6. 主要结论与发现
*   **性能卓越**：在 ScanNet 验证集上达到 **78.4% mIoU**，超越了所有已知的 PEFT 方法，且非常接近全量微调（78.9%）的水平。
*   **参数高效**：可训练参数量仅为骨干网络的 **1.09%**（约 1.18M），相比全量微调减少了 98% 以上。
*   **动态优势**：可视化分析表明，模型确实为不同的几何结构（如墙面 vs. 家具）生成了差异化的权重，证明了动态推理机制的有效性。

### 7. 优点
*   **推理时动态性**：不同于以往只在训练时动态的方法，PointTPA 实现了推理时的输入感知自适应，这在 3D 场景理解领域具有创新性。
*   **极高的效率**：在显著提升性能的同时，保持了极低的计算开销和参数增量，适合资源受限的部署场景。
*   **即插即用**：作为一种 PEFT 策略，它可以轻松集成到现有的 Transformer 类 3D 骨干网络中。

### 8. 不足与局限
*   **分组超参数敏感**：SNG 模块中的分组数量对性能有显著影响，过细或过粗的分组都会导致性能下降，需要针对不同数据集进行调优。
*   **任务覆盖面**：目前主要集中在语义分割任务上，尚未在 3D 目标检测或实例分割等其他场景理解任务中广泛验证。
*   **对预训练权重的依赖**：作为 PEFT 方法，其最终效果高度依赖于预训练模型（如 Sonata）的质量。

（完）
