---
title: "G-EDF-Loc: 3D Continuous Gaussian Distance Field for Robust Gradient-Based 6DoF Localization"
title_zh: G-EDF-Loc：用于鲁棒梯度 6 自由度定位的 3D 连续高斯距离场
authors: "José E. Maese, Lucía Coto-Elena, Luis Merino, Fernando Caballero"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04525v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于鲁棒6DoF定位与重建的3D距离场
tldr: 本文提出G-EDF-Loc，一种基于CPU的鲁棒6自由度定位框架。该系统核心是G-EDF，一种新型连续且内存高效的3D距离场表示方法。通过块稀疏高斯混合模型和自适应空间划分，该方法实现了跨块转换的C1连续性，并利用解析梯度进行高效的扫描到地图配准。实验证明，该框架在里程计退化或无IMU先验的严苛条件下，依然能保持高精度的实时定位性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04525v1/fig-001.webp\", \"caption\": \"TABLE II: Robustness Study: Evaluation of G-EDF-Loc against Fast-GICP and NDT across trajectories (0.5 m resolution). Systems are tested under normal operation (Inertial), missing IMU, and degraded priors (Low/High Gaussian noise). Best and second-best results are bold and underlined [Pos: m, Rot: °, Time: ms].\", \"page\": 7, \"index\": 1, \"width\": 832, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2604.04525v1/fig-002.webp\", \"caption\": \"Fig. 3: Qualitative 2D trajectory comparison featuring the full bc sequence (top row) and a zoomed-in detail of the park sequence (bottom row). Trajectories are depicted as follows: ground truth (black), the proposed G-EDF-Loc (red), Fast-GICP (blue), and NDT (green).\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2604.04525v1/fig-003.webp\", \"caption\": \"Fig. 2: Cross-section analysis on a local subset of the New College dataset (z = 3.0 m). Parameters: 1.0 m3 blocks with δ = 0.25 m overlap. Left column (a, c): Discontinuities in the distance field and gradient without blending. Right column (b, d): The proposed G-EDF representation, ensuring global C1 continuity.\", \"page\": 4, \"index\": 3, \"width\": 463, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.04525v1/fig-004.webp\", \"caption\": \"Fig. 1: Overview of the Snail dataset environment reconstruction. Top: detailed perspectives of the large-scale outdoor structures and local elevation changes. Bottom: a top-down view showing the full 900 × 610 × 120 m extent of the environment.\", \"page\": 1, \"index\": 4, \"width\": 512, \"height\": 510}]"
motivation: 针对现有3D距离场表示在定位时面临的内存开销大、边界不连续以及对运动先验依赖性强等挑战。
method: 采用块稀疏高斯混合模型（GMM）构建连续欧几里得距离场，并利用自适应空间划分和解析梯度实现鲁棒的梯度下降配准。
result: 在大规模数据集测试中，该方法在定位精度上达到先进水平，且在完全缺失IMU先验的情况下表现出极强的鲁棒性。
conclusion: G-EDF-Loc证明了基于连续高斯距离场的解析梯度方法能有效提升6自由度定位的可靠性与计算效率。
---

## 摘要
本文提出了一种基于直接、基于 CPU 的扫描到地图（scan-to-map）配准流水线的鲁棒 6 自由度（6-DoF）定位框架。该系统利用了 G-EDF，这是一种新型的连续且内存高效的 3D 距离场表示。该方法使用具有自适应空间划分的块稀疏高斯混合模型（Block-Sparse Gaussian Mixture Model）对欧几里得距离场（EDF）进行建模，确保了跨块过渡的 $C^1$ 连续性并减轻了边界伪影。通过利用该连续地图的解析梯度（这些梯度保持了程函（Eikonal）一致性），所提方法实现了高保真空间重建和实时定位。在大规模数据集上的实验结果表明，G-EDF-Loc 的性能与现有最先进方法相比具有竞争力，即使在里程计严重退化或完全缺乏 IMU 先验的情况下，也表现出卓越的韧性。

## Abstract
This paper presents a robust 6-DoF localization framework based on a direct, CPU-based scan-to-map registration pipeline. The system leverages G-EDF, a novel continuous and memory-efficient 3D distance field representation. The approach models the Euclidean Distance Field (EDF) using a Block-Sparse Gaussian Mixture Model with adaptive spatial partitioning, ensuring $C^1$ continuity across block transitions and mitigating boundary artifacts. By leveraging the analytical gradients of this continuous map, which maintain Eikonal consistency, the proposed method achieves high-fidelity spatial reconstruction and real-time localization. Experimental results on large-scale datasets demonstrate that G-EDF-Loc performs competitively against state-of-the-art methods, exhibiting exceptional resilience even under severe odometry degradation or in the complete absence of IMU priors.

---

## 论文详细总结（自动生成）

### 论文总结：G-EDF-Loc —— 用于鲁棒梯度 6 自由度定位的 3D 连续高斯距离场

#### 1. 核心问题与研究动机
*   **核心问题**：如何在资源受限的平台（如仅靠 CPU）上，实现大规模环境下高精度、高鲁棒性的 6 自由度（6-DoF）实时定位。
*   **研究动机**：
    *   **传统方法的局限**：特征点法（如 LOAM）缺乏体积上下文；网格法（如 TSDF/ESDF）内存开销大且仅具备 $C^0$ 连续性，导致体素边界梯度不连续，影响优化收敛。
    *   **神经表示的局限**：隐式神经表示（INR）虽连续但计算量巨大，通常依赖昂贵的 GPU 硬件。
    *   **需求**：需要一种既能保证数学上的连续性（$C^1$ 连续）、具备解析梯度，又能高效压缩空间信息的地图表示方法。

#### 2. 方法论
*   **核心思想**：将欧几里得距离场（EDF）建模为**块稀疏高斯混合模型（GMM）**。高斯函数不作为概率密度，而是作为通用的几何函数近似器。
*   **关键技术细节**：
    *   **G-EDF 表示**：使用各向同性/轴对齐的高斯核叠加来逼近距离值。公式形式为 $\hat{d}(x) = \sum w_k \cdot g_k(x)$。允许权重 $w_k$ 为负，以精确刻画表面细节。
    *   **块稀疏架构与 $C^1$ 连续性**：将空间划分为独立立方体块，通过空间哈希管理。相邻块之间设置重叠区，并使用**三次埃尔米特多项式（Smoothstep 函数）**进行平滑混合，确保全局梯度连续且无边界伪影。
    *   **自适应复杂度**：根据重建误差（MAE）动态调整每个块内高斯核的数量。简单区域使用少量核，复杂结构增加核密度。
    *   **解析梯度**：推导出闭式解析梯度 $\nabla \hat{d}(x)$，实验证明其高度符合程函方程（Eikonal Equation, $\|\nabla \hat{d}\| \approx 1$），为优化提供了稳定的物理意义。
    *   **定位流水线**：结合误差状态卡尔曼滤波（ESKF）进行运动预测和点云去畸变，随后通过两阶段（粗到精）的非线性最小二乘优化实现扫描到地图（Scan-to-Map）的配准。

#### 3. 实验设计
*   **数据集**：
    *   **Newer College Dataset**：涵盖校园环境，包括 quad-medium、quad-hard（剧烈运动）和 park（大规模）序列。
    *   **Snail Dataset**：超大规模户外环境（900 × 610 × 120 m）。
*   **对比基准（Benchmark）**：
    *   **重建精度**：对比地面真值点云，测量平均绝对误差（MAE）和中值误差。
    *   **定位性能**：对比 **Fast-GICP** 和 **NDT**（基于 `hdl_localization` 框架的优化实现）。
*   **实验场景**：
    *   标准模式（有 IMU 先验）。
    *   无里程计模式（NoOdom，完全禁用 IMU）。
    *   噪声干扰模式（在初始位姿中注入不同等级的高斯噪声）。

#### 4. 资源与算力
*   **硬件平台**：HP Victus 16 笔记本电脑，CPU 为 **Intel Core i7-13700H**（13 代），内存 32GB。
*   **GPU 使用情况**：**明确指出未使用 GPU**。整个框架基于 C++17 开发，利用 OpenMP 进行多核 CPU 并行加速。
*   **实时性**：在 CPU 上实现了实时处理，扫描配准耗时通常在 40-120ms 之间（视环境复杂度而定）。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集的 7 个不同特征序列上进行了测试。
*   **充分性**：实验设计涵盖了从厘米级重建精度验证到极端运动下的定位鲁棒性测试。
*   **客观性**：通过统一的定位包装器（Wrapper）对比 Fast-GICP 和 NDT，确保了预处理、去畸变等外部因素一致，仅对比核心配准算法的差异，实验结果具有较高的说服力。

#### 6. 主要结论与发现
*   **高精度重建**：G-EDF 实现了厘米级（约 1.8cm 中值误差）的距离场重建，且具有极高的压缩率。
*   **卓越的鲁棒性**：在**完全缺失 IMU 先验**或**高噪声干扰**下，Fast-GICP 和 NDT 频繁出现位姿跳变或发散，而 G-EDF-Loc 凭借连续的解析梯度和宽阔的收敛盆地，依然能保持平滑且准确的跟踪。
*   **计算效率**：虽然是基于连续函数的优化，但其 CPU 耗时与高度优化的离散方法（如 Fast-GICP）相当，且在困难场景下不会像传统方法那样因搜索耗时而出现计算瓶颈。

#### 7. 优点
*   **数学完备性**：通过 Smoothstep 混合实现了全局 $C^1$ 连续，解决了网格地图梯度不连续的顽疾。
*   **无需 GPU**：为资源受限的移动机器人提供了一种高性能的连续地图方案。
*   **抗干扰能力强**：对初始位姿误差不敏感，非常适合传感器退化场景。
*   **开源贡献**：提供了地图构建和定位系统的完整源代码。

#### 8. 不足与局限
*   **预计算开销**：G-EDF 地图需要离线或异步构建（涉及非线性最小二乘拟合），虽然文中提到支持流式处理，但初始构建成本高于直接存储点云。
*   **各向异性限制**：为了计算效率使用了轴对齐协方差，这可能导致在处理某些倾斜表面时需要更多的高斯核来补偿旋转表达能力的不足。
*   **动态环境**：论文主要关注静态环境的重建与定位，对于高动态场景下的地图更新机制探讨较少。

（完）
