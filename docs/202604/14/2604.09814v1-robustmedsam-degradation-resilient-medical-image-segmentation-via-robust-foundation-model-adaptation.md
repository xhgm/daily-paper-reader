---
title: "RobustMedSAM: Degradation-Resilient Medical Image Segmentation via Robust Foundation Model Adaptation"
title_zh: RobustMedSAM：通过鲁棒基础模型适配实现抗退化的医学图像分割
authors: "Jieru Li, Matthew Chen, Micky C. Nnamdi, J. Ben Tamo, Benoit L. Marteau, May D. Wang"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09814v1"
tags: ["query:q6"]
score: 9.0
evidence: 针对医学图像分割的 SAM 模型鲁棒性适配
tldr: 针对医疗图像分割模型在噪声、模糊等真实退化场景下性能下降的问题，本文提出 RobustMedSAM。该方法基于“编码器保留医疗先验、解码器控制鲁棒性”的发现，融合了 MedSAM 的图像编码器与 RobustSAM 的掩码解码器，并在包含 12 种退化类型的 MedSegBench 数据集上进行针对性微调，显著提升了模型在复杂临床环境下的分割精度与可靠性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09814v1/fig-001.webp\", \"caption\": \"Figure 4. Per-degradation analysis. Dice comparison across degradation types under point and box prompts. RobustMedSAM shows the clearest gains under point prompting, while remaining competitive under box prompting.\", \"page\": 6, \"index\": 1, \"width\": 437, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2604.09814v1/fig-002.webp\", \"caption\": \"Table 2. Segmentation performance under various image degradations with point prompts. Values are reported as mean ± std across degradation types per dataset. Metrics: Dice ↑, IoU ↑, and boundary accuracy NSD ↑ (τ=2 px).\", \"page\": 6, \"index\": 2, \"width\": 1034, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.09814v1/fig-003.webp\", \"caption\": \"Figure 1. Overview of RobustMedSAM. During training, each medical image is paired with a degraded counterpart generated by medical degradation augmentation. Both the clean and degraded images are processed using the same frozen MedSAM image encoder and prompt encoder. The resulting features are decoded by a shared robust decoder initialized from RobustSAM. Finetuned modules are highlighted in color, while frozen modules are shown in gray. At inference time, RobustMedSAM requires only a single input image without any clean reference.\", \"page\": 2, \"index\": 3, \"width\": 1036, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2604.09814v1/fig-004.webp\", \"caption\": \"Figure 6. Prompt sensitivity. Dice versus the number of point prompts K. RobustMedSAM improves rapidly and saturates around K = 3, while box-prompted models are shown as reference baselines.\", \"page\": 7, \"index\": 4, \"width\": 432, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.09814v1/fig-005.webp\", \"caption\": \"Figure 7. Qualitative segmentation results under degradations. Each panel shows (left to right): degraded input with point prompts, ground truth, SAM, MedSAM, RobustMedSAM, and RobustMedSAM+SVD predictions. Green = true positive, red = false positive, blue = false negative.\", \"page\": 8, \"index\": 5, \"width\": 1036, \"height\": 694}, {\"url\": \"assets/figures/arxiv/2604.09814v1/fig-006.webp\", \"caption\": \"Figure 2. Tail robustness under degradations. Empirical CDF of degradation-level Dice across datasets. RobustMedSAM shows a consistent rightward shift under point prompts, indicating improved worst-case performance, while remaining comparable to MedSAM under box prompts.\", \"page\": 5, \"index\": 6, \"width\": 461, \"height\": 285}]"
motivation: 现有的医疗分割模型在面对真实世界的图像退化（如噪声、伪影）时鲁棒性不足，且缺乏同时兼顾领域适配与抗退化能力的方法。
method: 采用模块化检查点融合策略，结合 MedSAM 的编码器与 RobustSAM 的解码器，并仅在多模态退化医疗数据集上微调解码器。
result: 在退化图像上的 Dice 指标较原始 SAM 提升了 0.106（从 0.613 升至 0.719），在分布内和分布外基准测试中均表现优异。
conclusion: 结构化融合互补的预训练模型是实现鲁棒医疗图像分割的一种高效且实用的方法。
---

## 摘要
基于 Segment Anything Model (SAM) 构建的医学图像分割模型在干净的基准测试中表现出色，但在面对噪声、模糊、运动伪影和特定模态失真等现实图像退化时，其可靠性往往会下降。现有方法通常只针对医学领域适配或退化鲁棒性中的某一方面进行优化，而未能兼顾两者。在 SAM 中，我们发现这些能力集中在互补的模块中：图像编码器保留了医学先验，而掩码解码器则主导了抗退化鲁棒性。基于这一观察，我们提出了 RobustMedSAM，它采用模块化检查点融合策略，在共享的 ViT-B 架构下，分别从 MedSAM 和 RobustSAM 初始化图像编码器和掩码解码器。随后，我们在来自 MedSegBench 的 35 个医学数据集（涵盖 6 种成像模态和 12 种退化类型）上仅对掩码解码器进行微调，同时冻结其余组件以保留预训练的医学表示。此外，我们还研究了一种基于 SVD 的参数高效变体，用于有限的编码器适配。在分布内和分布外基准测试上的实验表明，RobustMedSAM 将退化图像的 Dice 系数从 SAM 的 0.613 提升至 0.719（+0.106），证明了互补预训练模型的结构化融合是实现鲁棒医学图像分割的一种有效且实用的方法。

## Abstract
Medical image segmentation models built on Segment Anything Model (SAM) achieve strong performance on clean benchmarks, yet their reliability often degrades under realistic image corruptions such as noise, blur, motion artifacts, and modality-specific distortions. Existing approaches address either medical-domain adaptation or corruption robustness, but not both jointly. In SAM, we find that these capabilities are concentrated in complementary modules: the image encoder preserves medical priors, while the mask decoder governs corruption robustness. Motivated by this observation, we propose RobustMedSAM, which adopts module-wise checkpoint fusion by initializing the image encoder from MedSAM and the mask decoder from RobustSAM under a shared ViT-B architecture. We then fine-tune only the mask decoder on 35 medical datasets from MedSegBench, spanning six imaging modalities and 12 corruption types, while freezing the remaining components to preserve pretrained medical representations. We additionally investigate an SVD-based parameter-efficient variant for limited encoder adaptation. Experiments on both in-distribution and out-of-distribution benchmarks show that RobustMedSAM improves degraded-image Dice from 0.613 to 0.719 (+0.106) over SAM, demonstrating that structured fusion of complementary pretrained models is an effective and practical approach for robust medical image segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《RobustMedSAM: Degradation-Resilient Medical Image Segmentation via Robust Foundation Model Adaptation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医学图像分割模型（如 MedSAM）虽然在标准清晰数据集上表现优异，但在面对临床实际中常见的**图像退化**（如噪声、模糊、运动伪影、模态特定失真）时，性能会大幅下降。
*   **研究背景**：
    *   **SAM**：通用分割能力强，但在医学领域和退化场景下表现不佳。
    *   **MedSAM**：通过大规模医学数据微调获得了医学先验，但对图像质量波动敏感。
    *   **RobustSAM**：针对自然图像退化进行了增强，但缺乏医学专业知识。
*   **动机**：作者发现 SAM 架构中，医学先验主要由**编码器（Encoder）**承载，而抗退化能力主要由**解码器（Decoder）**控制。因此，通过“模块化融合”互补的模型能力是解决该问题的关键。

### 2. 方法论
*   **核心思想**：采用**模块化检查点融合（Module-wise Checkpoint Fusion）**。
*   **关键技术细节**：
    *   **混合初始化**：图像编码器（Image Encoder）初始化自 **MedSAM**（保留医学特征提取能力）；掩码解码器（Mask Decoder）初始化自 **RobustSAM**（引入抗退化模块）。
    *   **训练策略**：冻结编码器和提示编码器，仅微调解码器。采用**清晰-退化对（Clean-Degraded Pair）**训练法，即同一图像的清晰版和退化版同时输入，共享权重。
    *   **损失函数**：
        *   **分割损失**：Dice 损失 + Focal 损失（仅针对退化分支）。
        *   **一致性约束**：掩码特征一致性（Lmfc）和输出 Token 一致性（Ltc），强制模型在面对退化图像时产生与清晰图像相似的内部表示。
    *   **SVD 变体**：提出一种基于奇异值分解（SVD）的参数高效编码器适配方法，仅更新编码器卷积层的奇异值，以极小代价微调特征空间。

### 3. 实验设计
*   **数据集**：使用 **MedSegBench** 基准，包含 35 个医学数据集，涵盖 6 种成像模态（超声、MRI、CT、X射线、内窥镜等）和 12 种退化类型（高斯噪声、瑞利噪声、运动模糊等）。
*   **对比方法**：原始 SAM、MedSAM、RobustSAM。
*   **评估设置**：
    *   **分布内（In-distribution）**：在 6 个代表性数据集上测试。
    *   **分布外（Out-of-distribution, OOD）**：在 6 个未见过的模态-解剖组合数据集上测试。
    *   **提示词设置**：主要评估点提示（Point Prompts, K=3），辅助评估框提示（Box Prompts）。

### 4. 资源与算力
*   **硬件**：使用单台 **NVIDIA H200 GPU**。
*   **训练参数**：训练 10 个 epoch，Adam 优化器，学习率 $5 \times 10^{-4}$，Batch Size 为 4。
*   **图像处理**：所有图像缩放至 $512 \times 512$。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 35 个数据集、6 种模态和 12 种退化，实验量非常庞大且具有代表性。
*   **充分性**：
    *   进行了**消融实验**（如 SVD 变体的效果对比）。
    *   进行了**提示词敏感性分析**（对比不同点提示数量 K 的影响）。
    *   同时评估了清晰图像和退化图像，验证了鲁棒性提升是否以牺牲清晰图像精度为代价。
*   **客观性**：通过 OOD 测试验证了泛化能力，对比基准涵盖了当前最主流的医疗 SAM 变体，实验设计公平。

### 6. 主要结论与发现
*   **性能提升**：在退化图像上，RobustMedSAM 的平均 Dice 达到 **0.719**，较 SAM（0.613）提升了 **0.106**，显著优于 MedSAM 和 RobustSAM。
*   **鲁棒性与精度的平衡**：通过冻结 MedSAM 编码器，模型在提升鲁棒性的同时，保持了与 SAM 相当的清晰图像分割精度。
*   **点提示效率**：RobustMedSAM 在点提示设置下表现尤为突出，解决了 MedSAM 在点提示下性能崩溃的问题。
*   **SVD 发现**：虽然 SVD 适配能微调特征，但在清晰图像上会导致性能下降，存在“鲁棒性-精度”权衡，因此默认推荐冻结编码器。

### 7. 优点
*   **架构优雅**：无需从头训练，通过融合现有预训练模型的优势模块，实现了“1+1>2”的效果。
*   **实用性强**：针对临床真实的图像质量问题设计，且在推理时不需要清晰图像作为参考。
*   **泛化性好**：在多种模态和未见过的 OOD 数据集上均表现出稳定的提升。

### 8. 不足与局限
*   **拓扑敏感结构表现有限**：在细小血管（如 DRIVE 数据集）或显微镜图像等对拓扑结构极度敏感的任务中，提升幅度较小。
*   **合成退化的偏差**：实验主要基于合成的退化算法，虽然涵盖面广，但与真实的临床硬件伪影（如金属伪影）可能仍存在细微差异。
*   **编码器限制**：模块化融合假设能力是完全解耦的，但在极端退化下，仅靠解码器调整可能无法完全弥补编码器提取特征的损失。

（完）
