---
title: "AnyImageNav: Any-View Geometry for Precise Last-Meter Image-Goal Navigation"
title_zh: AnyImageNav：用于精准“最后一米”图像目标导航的任意视角几何
authors: "Yijie Deng, Shuaihang Yuan, Yi Fang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05351v2"
tags: ["query:q9"]
score: 9.0
evidence: 针对抓取等下游任务的精确图像目标导航
tldr: 针对传统图像目标导航在最后1米定位精度不足的问题，本文提出AnyImageNav。这是一种无需训练的系统，将目标图像视为几何查询，通过像素级对应关系恢复精确的6自由度位姿。该方法采用语义到几何的级联策略，利用语义信号引导探索，并结合3D多视图基础模型实现高精度定位，在提升导航成功率的同时显著增强了位姿估计精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05351v2/fig-001.webp\", \"caption\": \"Fig. 2: AnyImageNav pipeline. (a) Frontier Exploration: The dense relevance map between the current observation and the goal image Ig is projected onto a BEV map, scoring candidate frontiers and yielding the proximity score Srelev. (b) Geometric Verification: When Srelev > θ, a short memory window and Ig are passed to a 3D multi-view foundation model; the model’s internal correspondence confidence Sconf gates the transition to localization. (c) Last-Meter Localization: Upon acceptance, the full long-memory cache is registered with Ig in a single forward pass; the recovered goal pose is aligned to the agent’s map via Sim(3)+orientation correction and continuously refined as confidence improves.\", \"page\": 6, \"index\": 1, \"width\": 723, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2604.05351v2/fig-002.webp\", \"caption\": \"Fig. 4: Navigation examples on HM3D (top) and Gibson (bottom). In both episodes, the BEV relevance map directs the agent toward goal-relevant regions well before the target is reached (shown at t = 70). At t = 111 (HM3D) and t = 135 (Gibson), the target pose is accurately recovered while the agent is still approaching, and the agent subsequently navigates to the estimated goal position with high precision.\", \"page\": 11, \"index\": 2, \"width\": 724, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2604.05351v2/fig-003.webp\", \"caption\": \"Fig. 3: BEV Relevance Map Construction. A dense pixel-level relevance map between the current observation and the goal image is calculated per step. Then a frustum-aware projection then maps the relevance map onto the top-down BEV grid: a 2D cone mask is combined with depth-contoured truncation to prevent relevance from bleeding through obstacles, and element-wise multiplied with a confidence cone that attenuates rays toward the field-of-view boundary. The resulting per-step relevance cone is accumulated into a persistent global BEV relevance map.\", \"page\": 7, \"index\": 3, \"width\": 723, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.05351v2/fig-004.webp\", \"caption\": \"Table 3: Ablation study on HM3D val set. Each row removes or replaces one component.\", \"page\": 13, \"index\": 4, \"width\": 464, \"height\": 260}]"
motivation: 现有的图像目标导航成功标准过于粗糙，无法满足抓取等需要精确对准的下游任务对高精度定位的需求。
method: 采用语义到几何的级联策略，利用语义相关性引导探索，并调用3D多视图基础模型通过像素匹配实现精确的6-DoF位姿恢复。
result: "在Gibson和HM3D数据集上分别实现了93.1%和82.6%的成功率，且位姿误差较现有基准方法降低了5至10倍。"
conclusion: AnyImageNav证明了通过几何配准可实现高精度的图像目标导航，为机器人执行复杂下游任务提供了可靠的定位支持。
---

## 摘要
图像目标导航（ImageNav）通常采用粗粒度的成功标准进行评估，即智能体必须停止在目标 1 米范围内。这对于寻找物体已经足够，但对于抓取等需要精确位姿的下游任务则力有不逮。我们推出了 AnyImageNav，这是一个无需训练的系统，旨在将 ImageNav 推向这一更具挑战性的设定。我们的核心见解是，目标图像可以被视为一种几何查询：任何物体、走廊或房间角落的照片都可以通过密集像素级对应关系与智能体的观测结果进行配准，从而恢复精确的 6 自由度（6-DoF）相机位姿。我们的方法通过“语义到几何”的级联方式实现：语义相关性信号引导探索并充当邻近闸门，仅当当前视图与目标图像高度相关时才调用 3D 多视图基础模型；随后，该模型在循环中对其配准进行自我验证，以获得精确的恢复位姿。我们的方法在 Gibson (93.1%) 和 HM3D (82.6%) 数据集上创下了最先进的导航成功率，并实现了先前方法无法提供的位姿恢复：在 Gibson 上位置误差为 0.27m，航向误差为 3.41 度；在 HM3D 上为 0.21m / 1.23 度，相比适配后的基准模型提升了 5-10 倍。

## Abstract
Image Goal Navigation (ImageNav) is evaluated by a coarse success criterion, the agent must stop within 1m of the target, which is sufficient for finding objects but falls short for downstream tasks such as grasping that require precise positioning. We introduce AnyImageNav, a training-free system that pushes ImageNav toward this more demanding setting. Our key insight is that the goal image can be treated as a geometric query: any photo of an object, a hallway, or a room corner can be registered to the agent's observations via dense pixel-level correspondences, enabling recovery of the exact 6-DoF camera pose. Our method realizes this through a semantic-to-geometric cascade: a semantic relevance signal guides exploration and acts as a proximity gate, invoking a 3D multi-view foundation model only when the current view is highly relevant to the goal image; the model then self-certifies its registration in a loop for an accurate recovered pose. Our method sets state-of-the-art navigation success rates on Gibson (93.1%) and HM3D (82.6%), and achieves pose recovery that prior methods do not provide: a position error of 0.27m and heading error of 3.41 degrees on Gibson, and 0.21m / 1.23 degrees on HM3D, a 5-10x improvement over adapted baselines.

---

## 论文详细总结（自动生成）

### AnyImageNav 论文深度总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的图像目标导航（ImageNav）任务通常将“成功”定义为智能体到达目标点 1 米范围内。然而，对于抓取、操作等实际下游任务，仅靠“邻近”是不够的，机器人需要知道目标图像拍摄的精确 6 自由度（6-DoF）位姿。
*   **核心问题**：如何弥补从“粗略导航”到“精确操作定位”之间的“最后一米”差距，在未知环境中仅凭一张参考照片实现高精度的位姿恢复。
*   **背景**：现有方法多依赖语义匹配或预构建的场景表示（如高斯泼溅），前者精度不足，后者泛化性差且成本高。

#### 2. 论文提出的方法论
AnyImageNav 是一个**无需训练（Training-free）**的系统，其核心思想是将目标图像视为一个**几何查询**，通过“语义到几何”的级联架构实现定位。
*   **前沿探索（语义阶段）**：
    *   利用 DINOv2 提取当前观测与目标图像的特征，计算像素级相关性。
    *   构建**俯视图（BEV）相关性地图**，通过融合语义相关性、地理距离、航向偏差和信息增益来为边界（Frontiers）评分，引导智能体向目标区域移动。
*   **几何验证与定位（几何阶段）**：
    *   **邻近触发**：当语义相关性得分超过阈值时，调用 3D 多视图基础模型（如 VGGT）。
    *   **自我验证**：利用模型内部的跨帧注意力权重和特征相似度生成“几何置信度” $S_{conf}$。只有当置信度达标时，才确认为进入了目标视域。
    *   **6-DoF 恢复**：使用 Pi3 等模型对长期记忆帧和目标图像进行联合 3D 重建，通过 **Sim(3) 变换**和**航向修正算法**将预测位姿对齐到智能体的全局坐标系。
    *   **循环精炼**：在靠近目标的过程中，持续监控置信度并更新最佳位姿估计。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Gibson**：用于通用图像目标导航（ImageNav），包含 14 个场景，4200 个评估回合。
    *   **HM3D**：用于实例级图像目标导航（Instance ImageNav），包含 36 个场景，1000 个评估回合。
*   **Benchmark 与对比方法**：
    *   对比了学习类方法（ZSON, OVRL-v2, FGPrompt, REGNav）和模块化/几何类方法（IEVE, GaussNav, GauScoreMap）。
    *   **评估指标**：除了标准的成功率（SR）和路径长度加权成功率（SPL），还引入了**位置误差（$\epsilon_{pos}$）**和**航向误差（$\epsilon_{head}$）**。

#### 4. 资源与算力
*   **训练算力**：论文明确指出该系统是 **Training-free** 的，因此**不需要**针对特定场景或任务进行大规模训练。
*   **推理算力**：文中未详细列出具体的 GPU 型号和推理耗时，但提到系统依赖于大型 3D 基础模型（如 Pi3, VGGT）的单次前向传播。这类模型通常需要具备显存容量较大的现代 GPU（如 A100 或 RTX 3090/4090 级别）来处理多帧图像序列。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了超过 5000 次回合的测试，实验规模较大。
*   **消融实验**：针对探索权重、安全路径规划（FMMPlanner）、相关性阈值、记忆窗口长度以及置信度门控等关键组件进行了详细的消融测试。
*   **客观性与公平性**：实验在标准的 Habitat 模拟器中进行，对比了多种开源的最先进方法，并使用了统一的评估协议，实验结果具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **性能突破**：AnyImageNav 在 Gibson 上实现了 **93.1%** 的成功率，在 HM3D 上实现了 **82.6%** 的成功率，均创下新纪录。
*   **精度飞跃**：在位姿恢复精度上，相比于现有最强基准，位置误差降低了约 5-10 倍（达到 0.2m 级别），航向误差降低至 1.23°-3.41°。
*   **几何查询的有效性**：证明了将目标图像作为几何查询而非单纯的语义标签，是解决“最后一米”定位问题的关键。

#### 7. 优点（亮点）
*   **无需训练**：极大地增强了系统的泛化能力，可直接部署于新环境。
*   **高精度定位**：首次在 ImageNav 任务中系统性地实现了可用于操作任务的 6-DoF 位姿恢复。
*   **级联效率**：通过语义信号作为“闸门”，避免了在探索阶段频繁调用昂贵的 3D 几何模型，平衡了性能与计算开销。
*   **自验证机制**：利用基础模型的内部特征进行置信度评估，提高了导航决策的鲁棒性。

#### 8. 不足与局限
*   **传感器依赖**：系统仍高度依赖精确的深度传感器和里程计（Odometry）来构建 BEV 地图。
*   **语义缺失风险**：在缺乏明显语义特征的区域（如纯白走廊），探索效率会退化为随机搜索。
*   **固定阈值**：系统中的相关性和置信度阈值目前是固定的，在不同纹理密度的场景中可能需要微调以平衡“误报”和“漏报”。
*   **路径效率（SPL）**：在某些简单场景下，为了积累足够的几何多样性以实现精确对齐，智能体可能会走一些“冤枉路”，导致 SPL 指标略低于某些端到端学习方法。

（完）
