---
title: "IROSA: Interactive Robot Skill Adaptation using Natural Language"
title_zh: IROSA：利用自然语言的交互式机器人技能自适应
authors: "Markus Knauer, Samuel Bustamante, Thomas Eiband, Alin Albu-Schäffer, Freek Stulp, João Silvério"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03897v1"
tags: ["query:q20"]
score: 9.0
evidence: 利用自然语言和基础模型进行机器人技能适配
tldr: 本研究提出IROSA框架，旨在结合大语言模型与模仿学习，实现工业机器人的自然语言技能自适应。该框架采用基于工具的架构，在LLM与机器人硬件间建立抽象层，利用预训练模型选择并参数化特定工具，无需微调即可调整机器人行为。实验在7自由度机器人上成功完成了轴承环插入任务，证明了其在速度调节、轨迹修正和避障方面的有效性，同时保证了系统的安全性与可解释性。
selection_source: fresh_fetch
motivation: 旨在结合大语言模型与模仿学习，解决工业机器人技能自适应中缺乏安全性、透明度和自然交互的问题。
method: 提出一种基于工具的架构，利用预训练LLM选择并参数化特定工具，在不直接接触硬件的情况下调整机器人技能。
result: 在7自由度机器人的轴承环插入任务中，成功通过自然语言指令实现了速度调整、轨迹修正和避障。
conclusion: 该框架为工业环境下的机器人技能自适应提供了一种安全、透明且无需微调的高效交互方案。
---

## 摘要
基础模型在多个领域展示了令人印象深刻的能力，而模仿学习则为从有限数据中进行机器人技能自适应提供了原则性方法。将这些方法结合起来对于机器人的直接应用具有巨大潜力，但这种结合目前受到的关注有限，特别是在工业部署方面。我们提出了一种新型框架，通过基于工具的架构实现开放词汇的技能自适应，并在语言模型与机器人硬件之间保持了一个保护性的抽象层。我们的方法利用预训练的大语言模型（LLM）来选择特定工具并对其进行参数化，以调整机器人技能，而无需进行微调或直接的模型与机器人交互。我们在执行工业轴承环插入任务的 7 自由度（7-DoF）力矩控制机器人上演示了该框架，展示了通过自然语言指令成功实现的技能自适应，包括速度调节、轨迹修正和避障，同时保持了安全性、透明度和可解释性。

## Abstract
Foundation models have demonstrated impressive capabilities across diverse domains, while imitation learning provides principled methods for robot skill adaptation from limited data. Combining these approaches holds significant promise for direct application to robotics, yet this combination has received limited attention, particularly for industrial deployment. We present a novel framework that enables open-vocabulary skill adaptation through a tool-based architecture, maintaining a protective abstraction layer between the language model and robot hardware. Our approach leverages pre-trained LLMs to select and parameterize specific tools for adapting robot skills without requiring fine-tuning or direct model-to-robot interaction. We demonstrate the framework on a 7-DoF torque-controlled robot performing an industrial bearing ring insertion task, showing successful skill adaptation through natural language commands for speed adjustment, trajectory correction, and obstacle avoidance while maintaining safety, transparency, and interpretability.

---

## 论文详细总结（自动生成）

这是一份关于论文《IROSA: Interactive Robot Skill Adaptation using Natural Language》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
工业机器人应用日益需要灵活且易于重新配置的系统。虽然大语言模型（LLM）在自然语言理解方面表现出色，但直接将其应用于工业机器人控制面临**安全性、可解释性和验证性**的挑战。
*   **核心问题**：如何在不进行模型微调、不直接让 LLM 控制硬件的前提下，利用自然语言直观地调整机器人已有的工业技能（如速度、轨迹、避障）？
*   **研究动机**：现有的端到端方法（如 VLA 模型）缺乏透明度且需要海量数据；而基于代码生成的方法（如 OVITA）在本地部署时可靠性较差。IROSA 旨在通过“工具化”架构，在语言理解与机器人控制之间建立一个保护性的抽象层。

### 2. 方法论
IROSA 框架结合了**核化运动基元（KMP）**和**基于工具的 LLM 架构**。
*   **核心思想**：将机器人技能的调整动作封装为一组预定义的、经过验证的“工具”（函数）。LLM 仅负责根据用户指令选择工具并提取参数，而不直接生成控制指令或代码。
*   **关键技术流程**：
    1.  **用户查询**：用户通过语音或文字输入指令（如“在到达工作站前减速 50%”）。
    2.  **工具选择与参数化**：利用 LLM 的函数调用（Function Calling）能力，从 JSON 模式定义的工具库中选择匹配工具（如 `SpeedUpRobot`），并提取参数（速度比例、起始/结束时间点）。
    3.  **KMP 适配执行**：
        *   **速度调节**：通过修改 KMP 轨迹点之间的时间间隔实现。
        *   **路点插入**：在 KMP 中添加新的约束点（Via-points）来修正轨迹。
        *   **斥力点生成**：利用有向距离场（SDF）检测碰撞，并在受影响的轨迹段自动插入避障路点。
    4.  **反馈循环**：机器人执行调整后的技能，用户可根据观察进行迭代修正。

### 3. 实验设计
*   **实验场景**：使用 7 自由度力矩控制机器人（DLR SARA）执行工业轴承环插入任务。
*   **基准测试（Benchmark）**：对比了 **OVITA**（一种基于代码生成的开放词汇轨迹适配方法）。
*   **对比配置**：
    *   OVITA + 云端 LLM (Gemini)。
    *   OVITA + 本地 LLM (Qwen2.5-VL-72B)。
    *   IROSA + 本地 LLM (Qwen2.5-VL-72B)。
*   **评估指标**：指令成功率 (CSR)、解释成功率 (ISR)、任务完成率 (TCR) 以及响应时间。

### 4. 资源与算力
*   **LLM 模型**：主要使用本地部署的 **Qwen2.5-VL-72B-Instruct**。此外还测试了 Qwen2.5-Omni-7B 和 Pixtral-12B。
*   **训练时长**：该方法是 **Zero-shot（零样本）** 的，不需要针对特定任务进行微调或离线训练。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量，但强调了该框架支持在工业环境常见的**本地离线部署**。

### 5. 实验数量与充分性
*   **实验规模**：针对三种主要适配类型（速度、轨迹修正、避障）进行了测试。每种类型收集了 10 条来自不同用户的提示词，每条提示词运行 5 次，总计进行了大量的重复实验以确保统计意义。
*   **充分性评价**：实验设计较为充分。通过对比不同 LLM 后端（云端 vs 本地）以及不同适配机制（工具化 vs 代码生成），客观地证明了在本地模型能力有限的情况下，工具化架构比代码生成更具鲁棒性。

### 6. 主要结论与发现
*   **工具化架构的优越性**：在相同本地 LLM 条件下，IROSA 的各项指标（CSR, ISR, TCR）均显著优于 OVITA。
*   **效率提升**：IROSA 的平均响应时间比 OVITA 缩短了约 **43%**（15.4s vs 27.1s）。
*   **鲁棒性**：KMP 框架能够很好地处理 LLM 提取的参数，即使在复杂的避障任务中也能保持轨迹的平滑性和任务的连续性。
*   **本地化可行性**：证明了即使不依赖云端高性能 API，通过合理的架构设计，本地中等规模模型也能胜任工业技能适配任务。

### 7. 优点
*   **安全性与可解释性**：通过抽象层隔离了 LLM 的随机性，所有修改都在预定义的物理安全范围内。
*   **无需微调**：利用预训练模型的通用语义能力，实现了真正的零样本适配。
*   **模块化**：易于扩展新工具，且不依赖特定的 LLM 后端。
*   **工业友好**：支持本地部署，满足工业数据隐私和实时性需求。

### 8. 不足与局限
*   **灵活性受限**：系统的能力上限取决于预定义的工具集，无法处理工具库之外的复杂创意指令（例如“走一个螺旋形轨迹”，除非预定义了螺旋工具）。
*   **环境感知依赖**：目前环境信息（物体位置、标签）仍需手动提供或依赖外部视觉系统，尚未实现完全闭环的感知-决策。
*   **多工具冲突风险**：当 LLM 在单条指令中误触发多个工具时，可能会产生非预期的行为，需要更严格的提示工程或后处理过滤。

（完）
