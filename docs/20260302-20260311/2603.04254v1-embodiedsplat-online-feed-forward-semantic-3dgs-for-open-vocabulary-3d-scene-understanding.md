---
title: "EmbodiedSplat: Online Feed-Forward Semantic 3DGS for Open-Vocabulary 3D Scene Understanding"
title_zh: EmbodiedSplat：面向开放词汇 3D 场景理解的在线前馈语义 3DGS
authors: "Seungjun Lee, Zihan Wang, Yunsong Wang, Gim Hee Lee"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04254v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 用于开放词汇三维场景理解的在线前馈 3DGS
tldr: EmbodiedSplat 是一种面向具身智能任务的在线前馈 3D 高斯泼溅（3DGS）方法，旨在从流式图像中实时构建具有开放词汇语义理解能力的 3D 场景。该方法通过在线稀疏系数场和 CLIP 全局码本将 2D 语义嵌入 3D 高斯，并利用 3D U-Net 增强几何感知能力。它克服了传统方法需离线优化或逐场景训练的局限，在 ScanNet 等数据集上展现了卓越的泛化性与实时性。
selection_source: fresh_fetch
motivation: 现有的开放词汇 3DGS 方法多局限于离线或逐场景优化，无法满足具身智能对在线、实时且可泛化的 3D 场景理解需求。
method: 提出在线稀疏系数场结合 CLIP 全局码本以降低内存并保留语义，并利用 3D U-Net 聚合点云特征以增强 2D 语言嵌入的 3D 几何感知。
result: 在 ScanNet、ScanNet++ 和 Replica 等多个室内数据集上的实验证明，该方法在处理超过 300 张流式图像时具有极高的效率和泛化性能。
conclusion: EmbodiedSplat 实现了高效的在线 3D 语义重建，为具身智能在未知环境中的实时场景理解提供了强有力的技术支撑。
---

## 摘要
在探索过程中立即理解 3D 场景对于具身任务至关重要，在这些任务中，智能体必须以在线且近乎实时的方式构建并理解 3D 场景。在本研究中，我们提出了 EmbodiedSplat，这是一种用于开放词汇场景理解的在线前馈 3DGS，能够从流式图像中同时实现在线 3D 重建和 3D 语义理解。与现有的通常局限于离线或逐场景优化设置的开放词汇 3DGS 方法不同，我们的目标有两个：1) 以在线方式从超过 300 张流式图像中重建整个场景的语义嵌入 3DGS。2) 通过前馈设计对新场景具有高度泛化性，并在与实时 2D 模型结合时支持近乎实时的 3D 语义重建。为了实现这些目标，我们提出了一种带有 CLIP 全局码本（Global Codebook）的在线稀疏系数场（Online Sparse Coefficients Field），它将 2D CLIP 嵌入绑定到每个 3D 高斯（Gaussian）上，同时最小化内存消耗并保留 CLIP 的完整语义泛化能力。此外，我们通过 3D U-Net 聚合 3DGS 的局部点云，生成具有 3D 几何感知能力的 CLIP 特征，以弥补面向 2D 的语言嵌入所缺失的 3D 几何先验。在包括 ScanNet、ScanNet++ 和 Replica 在内的多种室内数据集上进行的广泛实验证明了我们方法的有效性和效率。项目页面见 https://0nandon.github.io/EmbodiedSplat/。

## Abstract
Understanding a 3D scene immediately with its exploration is essential for embodied tasks, where an agent must construct and comprehend the 3D scene in an online and nearly real-time manner. In this study, we propose EmbodiedSplat, an online feed-forward 3DGS for open-vocabulary scene understanding that enables simultaneous online 3D reconstruction and 3D semantic understanding from the streaming images. Unlike existing open-vocabulary 3DGS methods which are typically restricted to either offline or per-scene optimization setting, our objectives are two-fold: 1) Reconstructs the semantic-embedded 3DGS of the entire scene from over 300 streaming images in an online manner. 2) Highly generalizable to novel scenes with feed-forward design and supports nearly real-time 3D semantic reconstruction when combined with real-time 2D models. To achieve these objectives, we propose an Online Sparse Coefficients Field with a CLIP Global Codebook where it binds the 2D CLIP embeddings to each 3D Gaussian while minimizing memory consumption and preserving the full semantic generalizability of CLIP. Furthermore, we generate 3D geometric-aware CLIP features by aggregating the partial point cloud of 3DGS through 3D U-Net to compensate the 3D geometric prior to 2D-oriented language embeddings. Extensive experiments on diverse indoor datasets, including ScanNet, ScanNet++, and Replica, demonstrate both the effectiveness and efficiency of our method. Check out our project page in https://0nandon.github.io/EmbodiedSplat/.

---

## 论文详细总结（自动生成）

这是一份关于论文《EmbodiedSplat: Online Feed-Forward Semantic 3DGS for Open-Vocabulary 3D Scene Understanding》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（如机器人）在探索未知环境时，需要实时构建并理解 3D 场景。现有的开放词汇 3D 场景理解方法（基于 NeRF 或 3DGS）存在以下痛点：
*   **非在线/非实时**：大多依赖离线处理或耗时的逐场景优化（Per-scene Optimization）。
*   **泛化性差**：模型通常无法直接应用于未见过的场景，需要重新训练。
*   **内存开销大**：将高维的 CLIP 语义特征直接存储在数百万个高斯点上会导致内存爆炸。
*   **缺乏几何先验**：2D 提取的语义特征往往缺乏 3D 空间的一致性和几何感知。

**EmbodiedSplat** 的核心目标是实现一个**在线、前馈（无需优化）、可泛化且内存高效**的 3D 语义重建框架。

### 2. 方法论：核心思想与关键技术
该方法基于前馈 3DGS 模型（FreeSplat++），并引入了以下创新：
*   **在线稀疏系数场与 CLIP 全局码本（Sparse Coefficient Field & Global Codebook）**：
    *   **核心思想**：场景中的语义类别远少于高斯点数量。
    *   **技术细节**：建立一个动态更新的全局码本，存储实例级的 CLIP 特征。每个 3D 高斯点不存储完整的特征向量，而是存储极少量的索引（Index）和权重（Weight）。
    *   **流程**：通过线性组合码本中的基向量来重建高斯点的语义，大幅降低内存（约 67 倍压缩率），同时保留 CLIP 的开放词汇能力。
*   **几何感知 3D 语义特征（Geometry-aware 3D Features）**：
    *   利用 **3D Sparse U-Net** 和**时间感知记忆适配器（Memory-based Adapter）**聚合 3DGS 的局部点云特征。
    *   将 2D CLIP 语义与 3D 几何特征融合，通过 3D 模块提供的几何先验来修正 2D 投影带来的噪声。
*   **EmbodiedSplat-fast（快速变体）**：
    *   使用实时 2D 模型（如 FastSAM 和 Mask-Adapter）。
    *   提出**基于码本的余弦相似度计算**：先计算码本条目与文本查询的相似度，再进行加权求和，将 3D 搜索复杂度从 $O(MD)$ 降至 $O(KD+M)$，实现 5-6 FPS 的处理速度。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet / ScanNet200**：大规模真实室内场景，包含 20/200 类标注。
    *   **ScanNet++**：高保真室内场景。
    *   **Replica**：合成室内场景，用于评估开放词汇性能。
*   **对比方法**：
    *   **2D 渲染法**：LangSplat, LEGaussians, Online-LangSplat。
    *   **3D 直接索引法**：OpenGaussian, Occam’s LGS, Dr. Splat, InstanceGaussian。
*   **评估指标**：3D 语义分割的 mIoU 和 mACC，以及场景重建的时间和内存占用。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 6000 Ada GPU**。
*   **训练时长**：
    *   第一阶段（单视图预热）：100,000 次迭代。
    *   第二阶段（带记忆适配器的微调）：300,000 次迭代。
*   **推理速度**：标准版约 0.75 FPS，快速版（Fast）可达 **5.18 FPS**。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集上进行了详尽测试，涵盖了从 10 类到 70 类的不同粒度分割。
*   **消融实验**：针对 3D 几何特征的有效性、码本加速效果、内存压缩率、缓存大小（L）等关键变量做了定量分析。
*   **跨域测试**：进行了 ScanNet ↔ ScanNet++ 的跨数据集泛化实验，以及 Real-2-Sim（真实到合成）的测试。
*   **评价**：实验设计客观、公平，对比了最新的 SOTA 方法，且涵盖了在线/离线、前馈/优化等多个维度的对比，论证充分。

### 6. 主要结论与发现
*   **性能领先**：在无需逐场景优化的前提下，EmbodiedSplat 在 3D 语义分割任务上显著优于现有的优化型方法（如 Dr. Splat）。
*   **效率极高**：重建时间从小时级（离线优化）缩短至分钟级（在线处理），Fast 版本支持近乎实时的交互。
*   **内存友好**：通过稀疏系数场，在保留 CLIP 完整表达能力的同时，内存占用远低于直接存储特征的方法。
*   **几何补偿**：引入 3D U-Net 聚合几何信息能有效提升语义理解的准确性，尤其是在处理 2D 视角遮挡或噪声时。

### 7. 优点（亮点）
*   **首个在线前馈语义 3DGS**：填补了具身智能在 3DGS 实时语义理解领域的空白。
*   **无需逐场景优化**：真正实现了“即插即用”，对新环境具有极强的适应能力。
*   **创新的内存管理**：全局码本+稀疏系数的设计非常巧妙，解决了高维语义特征存储的瓶颈。
*   **端到端流程**：从流式图像输入到 3D 语义高斯场输出，流程完整且高效。

### 8. 不足与局限
*   **依赖深度估计**：作为前馈模型，其 3D 重建质量高度依赖于底座模型（FreeSplat++）的深度预测准确性。在天花板或透明物体等深度难测区域，语义质量会下降。
*   **跨域性能波动**：在 Real-2-Sim（如 ScanNet 到 Replica）的极端域迁移中，性能虽优于部分基准，但仍存在明显下降。
*   **计算开销**：虽然达到了 5-6 FPS，但对于某些极高性能要求的机器人任务，仍有进一步优化的空间。

（完）
