---
title: Discrete World Models via Regularization
title_zh: 通过正则化实现的离散世界模型
authors: "Davide Bizzaro, Luciano Serafini"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01748v1"
tags: ["query:q13"]
score: 9.0
evidence: 用于动力学和状态的无监督布尔世界模型学习
tldr: 本研究提出DWMR，一种无需重构和对比学习的无监督布尔世界模型学习方法。DWMR通过结合潜空间预测与专门的正则化项（如方差、相关性和偏度惩罚）来最大化表示位的熵和独立性，并引入局部性先验。实验证明，DWMR在组合结构基准测试中比重构方法更准确，且能有效支持离散滚动预测。
selection_source: fresh_fetch
motivation: 现有的世界模型学习通常依赖重构或对比信号，难以高效学习适用于符号推理和规划的离散布尔表示。
method: 提出DWMR方法，通过结合潜空间预测损失与方差、相关性、偏度正则化项，并引入局部性先验和鲁棒的离散训练方案。
result: 在具有组合结构的基准测试中，DWMR在表示准确性和状态转移预测方面均优于基于重构的替代方案。
conclusion: DWMR证明了通过正则化而非重构或对比学习，可以有效地学习出高质量的离散世界模型。
---

## 摘要
世界模型旨在将环境的状态和动力学捕捉到一个紧凑的潜空间中。此外，使用布尔状态表示对于搜索启发式以及符号推理和规划特别有用。现有方法通过基于解码器的重构，或者通过对比或奖励信号来保持潜变量的信息量。在这项工作中，我们提出了通过正则化实现的离散世界模型（DWMR）：一种用于无监督布尔世界模型学习的无需重构且无需对比的方法。具体而言，我们引入了一种新颖的世界建模损失，将潜变量预测与专门的正则化项相结合。这些正则化项通过方差、相关性和共偏度惩罚来最大化表示位的熵和独立性，同时为稀疏动作变化强制执行局部性先验。为了实现有效的优化，我们还引入了一种新颖的训练方案，提高了对离散展开的鲁棒性。在两个具有底层组合结构的基准测试上的实验表明，DWMR 比基于重构的替代方案学习到了更准确的表示和转换。最后，DWMR 还可以与辅助重构解码器配对，这种组合带来了额外的性能提升。

## Abstract
World models aim to capture the states and dynamics of an environment in a compact latent space. Moreover, using Boolean state representations is particularly useful for search heuristics and symbolic reasoning and planning. Existing approaches keep latents informative via decoder-based reconstruction, or instead via contrastive or reward signals. In this work, we introduce Discrete World Models via Regularization (DWMR): a reconstruction-free and contrastive-free method for unsupervised Boolean world-model learning. In particular, we introduce a novel world-modeling loss that couples latent prediction with specialized regularizers. Such regularizers maximize the entropy and independence of the representation bits through variance, correlation, and coskewness penalties, while simultaneously enforcing a locality prior for sparse action changes. To enable effective optimization, we also introduce a novel training scheme improving robustness to discrete roll-outs. Experiments on two benchmarks with underlying combinatorial structure show that DWMR learns more accurate representations and transitions than reconstruction-based alternatives. Finally, DWMR can also be paired with an auxiliary reconstruction decoder, and this combination yields additional gains.

---

## 论文详细总结（自动生成）

以下是对论文《Discrete World Models via Regularization》（通过正则化实现的离散世界模型）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何学习**离散（布尔/比特）潜空间**的世界模型。世界模型旨在捕捉环境状态及其动力学，而布尔表示对于符号推理、启发式搜索和规划（如拼图、棋类）具有天然优势。
*   **研究动机**：现有的离散世界模型通常依赖**像素级重构**（容易关注无关噪声而非核心动力学）或**对比学习**（依赖数据增强和负采样）。
*   **核心问题**：能否仅通过**正则化**手段，在不使用解码器重构和对比信号的情况下，学习出信息丰富、非坍缩且符合动力学逻辑的布尔潜空间表示？

### 2. 方法论：DWMR 框架
论文提出了 **DWMR (Discrete World Models via Regularization)**，其核心思想是通过一系列专门设计的正则化项来约束潜空间。

*   **架构组成**：
    *   **编码器 (Encoder)**：将图像映射为 Bernoulli 概率向量 $p$，并通过阈值化得到硬比特 $b$。
    *   **预测器 (Predictor)**：输入当前潜状态和动作，预测下一时刻的潜状态 $\hat{p}'$。
    *   **目标编码器 (Target Encoder)**：使用在线编码器的指数移动平均 (EMA) 副本，用于提供稳定的训练目标。
*   **核心损失函数**：$L = L_{pred} + \lambda_{var}L_{var} + \lambda_{cor}L_{cor} + \lambda_{cos}L_{cos} + \lambda_{loc}L_{loc}$
    *   **预测损失 ($L_{pred}$)**：预测值与目标比特之间的二元交叉熵。
    *   **方差正则 ($L_{var}$)**：防止潜变量坍缩为常数，强制每个比特保持一定的标准差（最大化熵）。
    *   **相关性正则 ($L_{cor}$)**：惩罚比特间的成对相关性，促进表示的独立性。
    *   **共偏度正则 ($L_{cos}$)**：惩罚三阶跨阶矩，消除成对解耦后可能残留的高阶结构化依赖。
    *   **局部性正则 ($L_{loc}$)**：引入先验知识，即单个动作通常只改变少数比特（稀疏性），鼓励模型发现解耦的特征。
*   **训练方案**：采用**两步更新法**。第一步仅更新预测器，使其适应硬比特输入（模拟测试环境）；第二步联合更新编码器和预测器，使用连续概率进行反向传播。

### 3. 实验设计
*   **数据集/场景**：
    1.  **MNIST 8-puzzle**：3x3 滑动拼图，数字由 MNIST 样本组成，动作移动空格。测试模型对组合配置和未见数字实例的泛化能力。
    2.  **IceSlider**：8x8 冰面滑行谜题，智能体沿动作方向滑行直至撞墙。
*   **Benchmark 与对比方法**：
    *   **AE (Autoencoder)**：基于重构的基准。
    *   **$\beta$-VAE**：带 KL 散度约束的变分自编码器。
    *   **DeepCubeAI**：一种先进的离散自编码世界模型。
*   **评估指标**：使用**线性探针 (Linear Probes)**。冻结编码器，训练一个线性层从布尔代码中恢复原始符号状态（如拼图的具体数字位置），评估“编码准确度”和“一步想象准确度”。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：Batch size 为 256；MNIST 8-puzzle 训练 40 个 epoch，IceSlider 训练 20 个 epoch。
*   **计算复杂度**：提到共偏度正则项具有 $O(K^3)$ 的复杂度（$K$ 为比特数），但在实验设置下（$K=64$ 或 $192$）是可计算的，且可以通过随机采样三元组来进一步优化。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的组合基准上进行了测试，每个基准包含“干净”和“高斯噪声”两种环境。
*   **重复性**：所有实验均运行了 **10 次独立随机试验**，并报告了均值和标准差，具有较好的统计说服力。
*   **消融实验**：非常充分。分别验证了方差、相关性、共偏度、局部性正则项以及 EMA 和训练方案的必要性。
*   **客观性**：对比了多种主流的离散世界模型方法，并在相同架构下进行了超参数搜索，确保了对比的公平性。

### 6. 主要结论与发现
*   **正则化足以替代重构**：DWMR 在不使用解码器的情况下，学习到的表示准确度显著优于基于重构的方法（如 AE 和 DeepCubeAI）。
*   **布尔特化正则项至关重要**：方差正则防止了完全坍缩，而相关性和共偏度正则对于学习解耦的、有意义的表示必不可少。
*   **局部性先验的价值**：引入动作引起稀疏变化的先验，显著提升了模型在噪声环境下的鲁棒性。
*   **兼容性**：DWMR 可以作为辅助项与重构解码器结合，进一步提升性能。

### 7. 优点
*   **无需重构**：避免了生成模型在处理复杂视觉细节时的资源浪费和噪声干扰。
*   **高阶解耦**：创新性地引入了共偏度（Coskewness）正则化，比传统的二阶协方差解耦更彻底。
*   **训练稳定性**：两步训练方案和 EMA 目标编码器的结合，解决了离散潜变量在端到端优化中的不稳定性。

### 8. 不足与局限
*   **环境假设**：目前仅在**完全可观测**且**确定性**的环境中验证，尚未处理部分可观测（POMDP）或随机性动力学。
*   **动作空间**：仅限于离散动作空间。
*   **视觉复杂度**：实验场景虽然具有复杂的组合逻辑，但视觉背景相对简单（拼图块），在自然场景或高度非平稳背景下的表现尚待验证。
*   **计算开销**：高阶矩正则项随比特数增加而计算量剧增，在大规模潜空间扩展上存在挑战。

（完）
