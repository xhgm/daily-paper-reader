---
title: "AffordGrasp: Cross-Modal Diffusion for Affordance-Aware Grasp Synthesis"
title_zh: AffordGrasp：面向可交互性感知抓取合成的跨模态扩散模型
authors: "Xiaofei Wu, Yi Zhang, Yumeng Liu, Yuexin Ma, Yujiao Shi, Xuming He"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08021v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 基于三维几何的可供性感知抓取合成
tldr: 本研究针对AR/VR和具身智能中语义抓取面临的模态差异及物理不一致问题，提出了AffordGrasp框架。该框架利用扩散模型，结合自动生成的细粒度语言标签，通过双重条件扩散过程和分布调整模块，实现了兼顾物体几何、空间功能性及文本语义的高精度、物理稳定的人手抓取姿态合成，在多个基准数据集上显著提升了抓取质量和语义一致性。
selection_source: fresh_fetch
motivation: 现有语义抓取方法难以弥合3D物体与文本指令间的模态鸿沟，且缺乏显式的空间和语义约束，导致生成的抓取动作在物理或语义上不合理。
method: 提出一种基于扩散模型的框架，通过自动标注流水线获取交互意图标签，并结合功能感知表征与双重条件扩散过程，配合分布调整模块确保物理接触和语义对齐。
result: 在HO-3D、OakInk等四个增强基准上的实验表明，该方法在抓取质量、语义准确性和多样性方面均优于现有最先进技术。
conclusion: AffordGrasp通过跨模态扩散和功能性感知，有效解决了语义引导下的高精度人手抓取合成难题。
---

## 摘要
生成能够准确反映物体几何形状和用户指定交互语义的人类抓取姿态，对于 AR/VR 和具身智能（Embodied AI）中自然的手物交互至关重要。然而，现有的语义抓取方法难以处理 3D 物体表示与文本指令之间巨大的模态差异，且往往缺乏明确的空间或语义约束，导致生成的抓取在物理上无效或在语义上不一致。在这项工作中，我们提出了 AffordGrasp，这是一个基于扩散模型的框架，能够高精度地生成物理稳定且语义忠实的人类抓取。我们首先引入了一个可扩展的标注流水线，该流水线能够自动为手物交互数据集丰富细粒度的结构化语言标签，以捕捉交互意图。基于这些标注，AffordGrasp 将手部姿态的可交互性感知潜表示（affordance-aware latent representation）与双重条件扩散过程相结合，使模型能够对物体几何、空间可交互性（spatial affordances）和指令语义进行联合推理。分布调整模块进一步增强了物理接触的一致性和语义对齐。我们在源自 HO-3D、OakInk、GRAB 和 AffordPose 的四个指令增强基准测试上评估了 AffordGrasp，并观察到其在抓取质量、语义准确性和多样性方面较现有最先进方法有显著提升。

## Abstract
Generating human grasping poses that accurately reflect both object geometry and user-specified interaction semantics is essential for natural hand-object interactions in AR/VR and embodied AI. However, existing semantic grasping approaches struggle with the large modality gap between 3D object representations and textual instructions, and often lack explicit spatial or semantic constraints, leading to physically invalid or semantically inconsistent grasps. In this work, we present AffordGrasp, a diffusion-based framework that produces physically stable and semantically faithful human grasps with high precision. We first introduce a scalable annotation pipeline that automatically enriches hand-object interaction datasets with fine-grained structured language labels capturing interaction intent. Building upon these annotations, AffordGrasp integrates an affordance-aware latent representation of hand poses with a dual-conditioning diffusion process, enabling the model to jointly reason over object geometry, spatial affordances, and instruction semantics. A distribution adjustment module further enforces physical contact consistency and semantic alignment. We evaluate AffordGrasp across four instruction-augmented benchmarks derived from HO-3D, OakInk, GRAB, and AffordPose, and observe substantial improvements over state-of-the-art methods in grasp quality, semantic accuracy, and diversity.

---

## 论文详细总结（自动生成）

以下是对论文《AffordGrasp: Cross-Modal Diffusion for Affordance-Aware Grasp Synthesis》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在给定特定文本指令（如“握住杯柄”或“按压喷头”）的情况下，生成既符合物体几何形状、又满足语义意图，且物理上稳定的 3D 人手抓取姿态。
*   **研究背景**：传统的抓取生成仅依赖几何形状，无法区分同一物体的不同交互意图。现有的语义抓取方法面临两大挑战：一是 3D 几何与自然语言之间的**模态鸿沟**巨大，难以实现细粒度对齐；二是扩散模型在生成过程中缺乏显式的**空间和物理约束**，容易产生穿透物体或语义不符的姿态。

### 2. 方法论
AffordGrasp 提出了一个基于潜扩散模型（LDM）的框架，核心流程如下：
*   **自动标注流水线**：利用预训练的 PointBERT 分类器和大型语言模型（Qwen），为现有的手物交互数据集（如 OakInk, GRAB）自动生成细粒度的语义标签和交互指令，解决了语义抓取数据匮乏的问题。
*   **可供性生成器（Affordance Generator）**：输入物体点云和文本指令，预测点云中与指令相关的区域（Affordance Map）。该图作为中间表示，桥接了抽象语义与具体几何空间。
*   **跨模态潜扩散模型**：在压缩的潜空间内进行抓取姿态生成。扩散过程受三重条件引导：文本指令特征（RoBERTa 提取）、物体几何特征（PointNet 提取）和预测的可供性特征。
*   **分布调整模块（DAM）**：这是一个轻量级的后处理模块。它在扩散采样后，通过多头注意力机制（MHA）将生成的潜表示与物体几何、语义指令再次融合，并利用物理约束损失（如穿透损失、接触一致性损失）进行微调，确保最终姿态的物理真实性。

### 3. 实验设计
*   **数据集与场景**：
    *   **域内（In-domain）**：在 OakInk 和 GRAB 数据集上进行训练和测试。
    *   **跨域（Out-of-domain/Zero-shot）**：在未见过的 HO-3D 和 AffordPose 数据集上直接测试，验证泛化能力。
*   **对比方法**：对比了 TTA（测试时优化方法）、FastGrasp（高效扩散方法）、D-VQVAE（基于量化自编码器的方法）以及 ControlNet 的变体。
*   **评估指标**：
    *   物理性：穿透体积（Penetration Volume）、接触率（Contact Ratio）。
    *   稳定性：仿真位移（Simulation Displacement，在物理引擎中测试抓取后的物体稳定性）。
    *   多样性：聚类熵（Entropy）、聚类规模（Cluster Size）。
    *   语义准确性：分类准确率（ACC，使用预训练分类器判断生成的姿态是否符合指令）。

### 4. 资源与算力
*   **算力说明**：论文提到实验得到了上海科技大学（ShanghaiTech University）高性能计算（HPC）平台的支持。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 V100）、GPU 数量以及具体的训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数据集上进行了大规模实验，涵盖了 1700 多个物体和多种交互动作。
*   **消融实验**：详细验证了“物体可供性（Affordance）”和“DAM 模块”的必要性，证明了它们对减少物体穿透和提升语义对齐的关键作用。
*   **下游应用**：在 RaiSim 物理仿真环境和真实的 ShadowHand 灵巧手机器人上进行了验证。
*   **充分性评价**：实验设计非常全面，不仅有定量的指标对比，还有定性的可视化展示，且包含了跨数据集的泛化测试，客观性较强。

### 6. 主要结论与发现
*   **性能领先**：AffordGrasp 在所有评估指标上均显著优于现有 SOTA 方法，尤其是在减少物理穿透和提升指令遵循度方面表现突出。
*   **可供性的桥梁作用**：引入物体可供性作为中间特征，能有效缓解 3D 几何与文本之间的对齐难题。
*   **DAM 的高效性**：相比于传统的测试时优化（TTA），DAM 模块在保持高精度的同时，推理速度提升了约 15 倍（从 7.09s 降至 0.45s）。

### 7. 优点（亮点）
*   **端到端且高效**：通过 DAM 模块避免了耗时的测试时迭代优化，实现了单次推理生成高质量抓取。
*   **语义细粒度**：能够区分同一物体上的不同功能区域，生成的抓取动作具有高度的意图一致性。
*   **数据驱动的标注**：提出的自动标注引擎为社区提供了扩充语义抓取数据集的新思路。

### 8. 不足与局限
*   **物理先验缺失**：模型主要基于数据驱动学习，未显式建模重力、摩擦力等复杂的物理动力学先验，可能导致某些极端情况下的抓取不稳定。
*   **失败案例**：在某些复杂指令下，手指可能无法完全闭合或与物体表面仍有微小间隙（如论文附录图 16 所示）。
*   **应用限制**：目前主要针对单手抓取，尚未扩展到双手协作或动态物体抓取场景。

（完）
