---
title: Efficient RGB-D Scene Understanding via Multi-task Adaptive Learning and Cross-dimensional Feature Guidance
title_zh: 通过多任务自适应学习与跨维度特征引导的高效 RGB-D 场景理解
authors: "Guodong Sun, Junjie Liu, Gaoyang Zhang, Bo Wu, Yang Zhang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07570v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 包含语义和实例分割的RGB-D场景理解
tldr: 本研究针对机器人场景理解中的遮挡和边界模糊等挑战，提出了一种高效的RGB-D多任务学习模型。该模型通过增强型融合编码器整合RGB与深度信息，并引入归一化聚焦通道层和非瓶颈1D结构，分别优化语义与实例分割。结合多任务自适应损失函数，该方法在NYUv2等数据集上实现了精度与速度的双重提升。
selection_source: fresh_fetch
motivation: 传统RGB-D场景理解方法在处理遮挡、边界模糊及多任务协同方面存在局限，且难以根据样本变化动态调整注意力。
method: 提出包含增强型融合编码器、归一化聚焦通道层、非瓶颈1D结构及多任务自适应损失函数的综合框架。
result: 在NYUv2、SUN RGB-D和Cityscapes数据集上的实验表明，该方法在分割精度和处理速度上均优于现有主流方法。
conclusion: 通过跨维度特征引导和自适应学习策略，该模型有效提升了复杂场景下多任务理解的鲁棒性与效率。
---

## 摘要
场景理解在实现机器人系统的智能化与自主化方面起着至关重要的作用。传统方法通常面临遮挡、边界模糊以及无法根据特定任务需求和样本变化调整注意力等挑战。为了解决这些局限性，本文提出了一种高效的 RGB-D 场景理解模型，可执行语义分割、实例分割、朝向估计、全景分割和场景分类等一系列任务。该模型包含一个增强型融合编码器，能有效利用来自 RGB 和深度输入的冗余信息。针对语义分割，我们引入了归一化聚焦通道层和上下文特征交互层，旨在缓解浅层特征误导和局部-全局特征表示不足的问题。实例分割任务受益于非瓶颈 1D 结构，该结构以更少的参数实现了更优的轮廓表示。此外，我们提出了一种多任务自适应损失函数，可根据场景变化动态调整不同任务的学习策略。在 NYUv2、SUN RGB-D 和 Cityscapes 数据集上的大量实验表明，本方法在分割精度和处理速度上均优于现有方法。

## Abstract
Scene understanding plays a critical role in enabling intelligence and autonomy in robotic systems. Traditional approaches often face challenges, including occlusions, ambiguous boundaries, and the inability to adapt attention based on task-specific requirements and sample variations. To address these limitations, this paper presents an efficient RGB-D scene understanding model that performs a range of tasks, including semantic segmentation, instance segmentation, orientation estimation, panoptic segmentation, and scene classification. The proposed model incorporates an enhanced fusion encoder, which effectively leverages redundant information from both RGB and depth inputs. For semantic segmentation, we introduce normalized focus channel layers and a context feature interaction layer, designed to mitigate issues such as shallow feature misguidance and insufficient local-global feature representation. The instance segmentation task benefits from a non-bottleneck 1D structure, which achieves superior contour representation with fewer parameters. Additionally, we propose a multi-task adaptive loss function that dynamically adjusts the learning strategy for different tasks based on scene variations. Extensive experiments on the NYUv2, SUN RGB-D, and Cityscapes datasets demonstrate that our approach outperforms existing methods in both segmentation accuracy and processing speed.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Efficient RGB-D Scene Understanding** 的多任务学习框架，旨在通过跨维度特征引导和自适应学习策略，提升机器人对复杂环境的感知能力。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的场景理解方法通常专注于单一任务（如仅语义分割），难以提供机器人决策所需的全面信息。虽然 RGB-D 数据结合了颜色和几何信息，但现有方法在处理**遮挡、边界模糊、模态冗余**以及**多任务权重分配不均**等方面存在局限。
*   **核心问题**：如何在保证实时处理速度（Efficiency）的同时，有效地融合 RGB 与深度信息，并协调语义分割、实例分割、全景分割、朝向估计和场景分类这五个相互关联的任务。

### 2. 论文提出的方法论
该模型采用编码器-解码器架构，核心创新点包括：
*   **高效融合编码器 (Efficient Fusion Encoder)**：针对通道特征的冗余性，采用部分卷积（Partial Convolution）思想，仅对 1/4 的通道进行特征提取，其余通道保留，从而大幅减少计算量（FLOPs 降至常规卷积的 1/16）并提升推理速度。
*   **归一化聚焦通道层 (NFCL)**：在语义解码器中，利用批归一化（BN）中的缩放因子 $\gamma$ 来衡量通道重要性，通过学习到的权重重新分配注意力，强化浅层特征中的关键信息。
*   **上下文特征交互层 (CFIL)**：通过多尺度自适应平均池化（1x1 和 5x5）捕获全局与局部上下文，弥补 MLP 解码器在空间结构表示上的不足。
*   **非瓶颈 1D 实例解码器 (Non-bottleneck 1D)**：将 3x3 卷积分解为 3x1 和 1x3 卷积，在减少 30% 参数量的同时增强非线性表达能力，提升物体轮廓的分割精度。
*   **多任务自适应损失函数 (Multi-task Adaptive Loss)**：引入动态权重调整机制。算法根据每个任务的**历史相对损失（Relative Loss）**趋势，实时调整各任务在总损失中的权重，避免了固定权重导致的训练不稳定或任务间竞争。

### 3. 实验设计
*   **数据集**：
    *   **NYUv2**：室内场景，包含 40 个语义类别及朝向标注。
    *   **SUN RGB-D**：大规模室内场景，包含 37 个类别。
    *   **Cityscapes**：室外城市街道场景，用于验证模型的泛化性。
*   **Benchmark 与对比方法**：
    *   对比了 **EMSAFormer**（基于 Transformer 的 SOTA 方法）、**Swin Transformer v2**、**ConvNeXt v2**、**MPViT** 等主流骨干网络。
    *   在语义分割任务中，对比了 MaskSup、MMANet、SGACNet 等十余种方法。
*   **评价指标**：mIoU（语义）、PQ/RQ/SQ（全景/实例）、MAAE（朝向误差）、bAcc（场景分类精度）以及 FPS（速度）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX 3090 Ti GPU**。
*   **软件环境**：PyTorch 2.0.1, CUDA 12.1。
*   **训练细节**：使用 SGD 优化器，初始学习率 0.03，动量 0.9。NYUv2 和 SUN RGB-D 的输入分辨率为 640x480，Cityscapes 为 1024x512。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的对比实验和消融实验。
*   **消融实验**：分别验证了融合编码器、NFCL、CFIL、非瓶颈 1D 结构以及自适应损失函数的有效性。例如，针对自适应损失的调节因子 $\alpha$ 进行了多组梯度测试。
*   **充分性评价**：实验覆盖了室内与室外、白天与低光照等多种场景，对比对象既包括经典的卷积网络也包括最新的 Transformer 模型。实验设计客观、公平，通过可视化热力图和分割图直观展示了改进效果，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 NYUv2 上，语义分割 mIoU 达到 **49.82%**，优于 EMSAFormer。
*   **速度优势**：模型参数量为 71.82M，推理速度达到 **20.33 FPS**，显著快于基于 Transformer 的同类多任务模型（如 EMSAFormer 为 16.32 FPS）。
*   **自适应学习有效性**：动态损失权重调整使训练过程更加收敛且稳定，有效解决了多任务学习中的梯度冲突问题。

### 7. 优点
*   **高效性**：通过通道冗余利用和 1D 卷积分解，实现了精度与速度的平衡，适合嵌入式机器人平台。
*   **跨维度引导**：NFCL 和 CFIL 的设计巧妙地结合了通道注意力和空间上下文，提升了对细小物体和模糊边界的识别。
*   **鲁棒性**：在低光照、镜面反射和物体遮挡等挑战性条件下表现稳健。

### 8. 不足与局限
*   **高分辨率瓶颈**：虽然在 1024x512 下表现良好，但处理超高分辨率图像或视频流时的扩展性仍需进一步验证。
*   **传感器依赖**：模型假设 RGB 和深度图是完全对齐且无噪的，但在现实中，廉价深度传感器常存在空洞或噪声，论文未深入探讨噪声鲁棒性增强。
*   **时序一致性**：目前是单帧处理，未考虑视频序列中的时序信息，可能导致连续帧间的分割结果出现闪烁。

（完）
