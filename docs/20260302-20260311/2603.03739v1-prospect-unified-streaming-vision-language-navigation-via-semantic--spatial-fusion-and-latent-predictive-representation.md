---
title: "PROSPECT: Unified Streaming Vision-Language Navigation via Semantic--Spatial Fusion and Latent Predictive Representation"
title_zh: PROSPECT：通过语义-空间融合与潜空间预测表示实现的统一流式视觉语言导航
authors: "Zehua Fan, Wenqi Lyu, Wenxuan Song, Linge Zhao, Yifei Yang, Xi Wang, Junjie He, Lida Huang, Haiyan Liu, Bingchuan Sun, Guangjun Bao, Xuanyao Mao, Liang Xu, Yan Wang, Feng Gao"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03739v1"
tags: ["query:q18"]
score: 10.0
evidence: 用于视觉语言导航的流式VLA策略
tldr: 针对视觉语言导航（VLN）中语义理解与空间预测结合不足的问题，本文提出PROSPECT框架。该框架将流式视觉-语言-动作（VLA）策略与潜空间预测表示学习相结合，利用CUT3R提取长上下文3D空间特征，并与SigLIP语义特征融合。通过引入可学习的流查询令牌预测未来潜空间状态，在不增加推理开销的情况下强化了内部表示。实验证明其在VLN-CE及真实机器人任务中达到SOTA水平，具备极强的长程导航鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多模态大模型导航缺乏对环境动态和空间结构的预测建模能力，难以应对复杂的长程任务。
method: 融合3D空间与语义特征，并利用流查询令牌在潜空间中预测下一帧的2D/3D特征以强化表示学习。
result: 在VLN-CE基准测试和实机部署中均取得SOTA性能，并在多变光照环境下表现出优异的鲁棒性。
conclusion: 结合流式空间-语义融合与潜空间预测学习，显著提升了导航智能体在复杂动态环境中的感知与决策效能。
---

## 摘要
多模态大语言模型（MLLMs）推动了零样本端到端视觉语言导航（VLN）的发展，但鲁棒的导航不仅需要语义理解，还需要对环境动态和空间结构进行预测建模。我们提出了 PROSPECT，这是一个统一的流式导航智能体，它将流式视觉-语言-动作（VLA）策略与潜空间预测表示学习相结合。PROSPECT 使用 CUT3R 作为流式 3D 基础空间编码器，以生成长上下文、绝对尺度的空间特征，并通过交叉注意力机制将其与 SigLIP 语义特征融合。在训练过程中，我们引入了可学习的流查询令牌（stream query tokens），用于查询流式上下文并预测下一步的 2D 和 3D 潜空间特征（而非像素或显式模态），并在冻结的 SigLIP 和 CUT3R 教师模型的潜空间中进行监督。预测分支在不增加推理开销的情况下塑造了内部表示。在 VLN-CE 基准测试和真实机器人部署上的实验表明，该方法达到了最先进的性能，并在多种光照条件下提高了长程鲁棒性。我们将很快向社区发布代码。

## Abstract
Multimodal large language models (MLLMs) have advanced zero-shot end-to-end Vision-Language Navigation (VLN), yet robust navigation requires not only semantic understanding but also predictive modeling of environment dynamics and spatial structure. We propose PROSPECT, a unified streaming navigation agent that couples a streaming Vision-Language-Action (VLA) policy with latent predictive representation learning. PROSPECT uses CUT3R as a streaming 3D foundation spatial encoder to produce long-context, absolute-scale spatial features, and fuses them with SigLIP semantic features via cross-attention. During training, we introduce learnable stream query tokens that query the streaming context and predict next-step 2D and 3D latent features (rather than pixels or explicit modalities), supervised in the latent spaces of frozen SigLIP and CUT3R teachers. The predictive branch shapes internal representations without inference overhead. Experiments on VLN-CE benchmarks and real-robot deployment demonstrate state-of-the-art performance and improved long-horizon robustness under diverse lighting. We will release code for the community soon.

---

## 论文详细总结（自动生成）

这是一份关于论文《PROSPECT: Unified Streaming Vision-Language Navigation via Semantic–Spatial Fusion and Latent Predictive Representation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言导航（VLN）要求智能体根据自然语言指令在未知环境中移动。现有的多模态大模型（MLLM）虽然在语义理解上表现出色，但在**环境动态预测**和**空间结构建模**方面存在不足。
*   **研究动机**：现有的预测模型通常在显式的像素或深度空间进行监督，容易过拟合于任务无关的细节（如纹理、光照）；同时，许多模型仅利用短时历史，忽略了长程流式上下文。
*   **核心问题**：如何构建一个既能理解长程流式语义，又能预测未来空间/语义状态，且不增加推理负担的统一导航框架？

### 2. 方法论
PROSPECT 提出了一个结合流式视觉-语言-动作（VLA）策略与潜空间预测表示学习的统一架构。
*   **核心思想**：在训练阶段通过“流查询令牌”预测未来的潜空间特征，以此来塑造模型内部的表示能力，而在推理阶段移除预测分支以保持高效。
*   **关键技术细节**：
    *   **2D-3D 语义-空间融合**：使用 **SigLIP** 提取 2D 语义特征，使用 **CUT3R**（流式 3D 基础模型）提取具有绝对尺度的 3D 空间特征。两者通过交叉注意力机制（Cross-Attention）融合。
    *   **流查询令牌（Stream Query Tokens）**：在 LLM 输入中附加可学习的 $q_{2D}$ 和 $q_{3D}$ 令牌，用于反向查询流式上下文并预测下一时刻的特征。
    *   **潜空间预测（Latent Prediction）**：借鉴 JEPA 思想，不预测像素，而是预测冻结的 SigLIP（2D）和 CUT3R（3D）教师模型的输出特征。2D 使用余弦相似度损失，3D 使用 MSE 损失。
    *   **流式注意力掩码**：设计了特殊的掩码确保：1) 时间因果性；2) 不同回合的查询令牌相互隔离；3) 2D 与 3D 预测任务解耦，防止模态间信息泄露。

### 3. 实验设计
*   **数据集/场景**：
    *   **模拟器**：在 Habitat 模拟器中的 VLN-CE 基准上评估，涵盖 **R2R**（短程）和 **RxR**（长程、多语言、指令复杂）数据集。
    *   **真实世界**：部署在 **ARX-Lift2** 机器人上，测试办公区、仓库、走廊及室外街道（午后、黄昏、夜晚）等多种场景。
*   **Benchmark 与对比方法**：
    *   对比了 NaVid, NaVILA, StreamVLN 等最先进的（SOTA）端到端导航模型。
    *   评估指标包括成功率（SR）、路径长度加权成功率（SPL）、导航误差（NE）等。

### 4. 资源与算力
论文详细列出了算力消耗：
*   **硬件**：使用 **8 台 NVIDIA A800 GPU** 进行训练。
*   **训练时长**：
    *   第一阶段（SFT）：约 560 A800 GPU 小时。
    *   第二阶段（增强 SFT）：约 1900 A800 GPU 小时。
*   **推理性能**：在双 RTX-4090 或 A800 服务器上，推理频率约为 **4 Hz**（延迟约 0.25-0.27s/步）。

### 5. 实验数量与充分性
*   **实验规模**：训练集包含约 93.8 万个样本（71% 导航数据，29% VQA 数据）。
*   **消融实验**：
    *   验证了 2D/3D 预测分支的各自贡献（两者结合效果最好）。
    *   对比了不同空间编码器（CUT3R 优于 VGGT，且解决了长序列 OOM 问题）。
    *   分析了不同任务长度（短、中、长程）下的表现。
    *   验证了注意力掩码设计的必要性。
*   **充分性评价**：实验设计非常充分，不仅有标准基准测试，还进行了细致的层级分析和极具挑战性的跨光照实机部署，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **长程任务优势明显**：PROSPECT 在 RxR（长程）上的提升远高于 R2R，证明了流式上下文和预测表示对复杂指令跟随的有效性。
*   **预测塑造表示**：即使推理时不运行预测分支，训练时的预测任务也能显著提升 VLA 策略的鲁棒性。
*   **空间智能的重要性**：引入绝对尺度的 3D 特征（CUT3R）比仅使用 2D 语义特征或相对尺度特征（VGGT）能更有效地处理大视角变化。
*   **环境鲁棒性**：实机测试显示，模型在低光照（夜晚街道）和复杂室内环境下仍能保持较高的导航成功率。

### 7. 优点（亮点）
*   **统一性**：将世界模型的预测能力与 VLA 的动作生成能力完美统一，且推理零开销。
*   **绝对尺度感知**：通过 CUT3R 解决了导航中常见的尺度漂移和空间不一致问题。
*   **潜空间监督**：避免了生成像素带来的计算浪费和噪声干扰，聚焦于高层语义和几何特征。
*   **实战价值**：提供了从模拟器到真实机器人、从室内到室外的全场景验证。

### 8. 不足与局限
*   **算力门槛高**：训练过程需要大量的 A800 算力，对于小型研究团队而言成本较高。
*   **板载推理限制**：虽然支持远程推理，但在单张 RTX 4070 等移动端显卡上直接运行时的性能会有所下降。
*   **动态障碍物处理**：论文主要侧重于静态环境的空间结构和光照鲁棒性，对于行人、车辆等高度动态障碍物的避障策略未做深入探讨。

（完）
