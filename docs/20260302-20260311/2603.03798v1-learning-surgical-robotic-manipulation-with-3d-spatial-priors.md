---
title: Learning Surgical Robotic Manipulation with 3D Spatial Priors
title_zh: 基于 3D 空间先验的学习型手术机器人操作
authors: "Yu Sheng, Lidian Wang, Xiaomeng Chu, Jiajun Deng, Min Cheng, Yanyong Zhang, Bei Hua, Houqiang Li, Jianmin Ji"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03798v1"
tags: ["query:q9"]
score: 9.0
evidence: 具有三维空间意识的手术机器人操作
tldr: 本研究针对手术机器人操作中3D空间感知不足的问题，提出了Spatial Surgical Transformer (SST)。SST是一种端到端的视觉运动策略，通过直接从立体内窥镜图像中提取3D空间线索，克服了传统方法误差累积和临床受限的弊端。研究团队构建了包含3万对立体图像的大规模数据集Surgical3D，并利用几何Transformer和多级空间特征连接器实现精准操作。实验证明，该方法在打结和器官解剖等复杂任务中达到了领先水平，为临床部署迈出重要一步。
selection_source: fresh_fetch
motivation: 传统手术机器人操作方法在3D场景重建中易产生误差累积，且增加腕部摄像头的方法在临床实践中难以应用。
method: 提出SST模型，利用新构建的Surgical3D数据集微调几何Transformer以提取3D表征，并通过多级空间特征连接器将其与机器人动作空间对齐。
result: 在打结和离体器官解剖等复杂手术任务的实机实验中，SST展现了最先进的性能和极强的空间泛化能力。
conclusion: 通过引入3D空间先验和端到端优化，该研究显著提升了手术机器人在复杂环境下的操作精度，具有重要的临床应用价值。
---

## 摘要
实现 3D 空间感知对于需要精确且精细操作的手术机器人操作至关重要。现有方法要么在操作前显式重建手术场景，要么通过增加腕部摄像头来补充默认的立体内窥镜，从而增强多视图特征。然而，这两种范式都存在显著局限性：前者由于其多阶段性质，容易导致误差累积并阻碍端到端优化；而后者在临床实践中很少被采用，因为腕部摄像头会干扰手术机器人手臂的运动。在这项工作中，我们提出了空间手术 Transformer (SST)，这是一种端到端的视觉运动策略，通过直接探索嵌入在内窥镜图像中的 3D 空间线索，赋予手术机器人 3D 空间感知能力。首先，我们构建了 Surgical3D，这是一个包含 3 万对具有精确 3D 几何信息的写实立体内窥镜图像对的大规模数据集，解决了手术场景中 3D 数据稀缺的问题。基于 Surgical3D，我们微调了一个强大的几何 Transformer，以从立体内窥镜图像中提取鲁棒的 3D 潜表征。随后，这些表征通过轻量级的多级空间特征连接器 (MSFC) 与机器人的动作空间无缝对齐，且全部在以内窥镜为中心的坐标系内完成。广泛的真实机器人实验表明，SST 在打结和离体器官解剖等复杂手术任务中实现了最先进的性能和强大的空间泛化能力，标志着向实际临床部署迈出了重要一步。数据集和代码将被开源。

## Abstract
Achieving 3D spatial awareness is crucial for surgical robotic manipulation, where precise and delicate operations are required. Existing methods either explicitly reconstruct the surgical scene prior to manipulation, or enhance multi-view features by adding wrist-mounted cameras to supplement the default stereo endoscopes. However, both paradigms suffer from notable limitations: the former easily leads to error accumulation and prevents end-to-end optimization due to its multi-stage nature, while the latter is rarely adopted in clinical practice since wrist-mounted cameras can interfere with the motion of surgical robot arms. In this work, we introduce the Spatial Surgical Transformer (SST), an end-to-end visuomotor policy that empowers surgical robots with 3D spatial awareness by directly exploring 3D spatial cues embedded in endoscopic images. First, we build Surgical3D, a large-scale photorealistic dataset containing 30K stereo endoscopic image pairs with accurate 3D geometry, addressing the scarcity of 3D data in surgical scenes. Based on Surgical3D, we finetune a powerful geometric transformer to extract robust 3D latent representations from stereo endoscopes images. These representations are then seamlessly aligned with the robot's action space via a lightweight multi-level spatial feature connector (MSFC), all within an endoscope-centric coordinate frame. Extensive real-robot experiments demonstrate that SST achieves state-of-the-art performance and strong spatial generalization on complex surgical tasks such as knot tying and ex-vivo organ dissection, representing a significant step toward practical clinical deployment. The dataset and code will be released.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Surgical Robotic Manipulation with 3D Spatial Priors》的结构化深入总结：

### 1. 核心问题与背景
*   **核心问题**：如何在不增加额外硬件（如腕部摄像头）且不依赖复杂多阶段重建的情况下，赋予手术机器人精确的 **3D 空间感知能力**，以完成毫米级的精细操作。
*   **研究背景**：
    *   **传统重建法**：先重建场景再执行操作，存在误差累积且无法端到端优化。
    *   **多视角法**：在机械臂末端增加摄像头，但在临床中会受限于穿刺套管（Trocar）的物理约束，且干扰运动。
    *   **数据稀缺**：手术场景缺乏大规模、带 3D 标注的真实数据集。

### 2. 方法论
论文提出了 **Spatial Surgical Transformer (SST)**，其核心思想是直接从立体内窥镜图像中提取 3D 空间先验并融入视觉运动策略。
*   **Surgical3D 数据集构建**：利用 NVIDIA Omniverse 生成 30k 对具有精确深度和点云标注的写实立体图像，并通过伪标签技术引入真实手术视频数据以缩小领域差距（Domain Gap）。
*   **手术几何 Transformer**：基于 MASt3R 模型进行微调，输入立体图像对，通过 ViT 编码器和跨视图注意力机制提取 3D 潜嵌入（Latent Embeddings）。
*   **多级空间特征连接器 (MSFC)**：将几何 Transformer 不同层级的特征（底层细节+高层全局上下文）进行拼接，并通过轻量级 MLP 映射到动作空间。
*   **以内窥镜为中心的动作空间**：由于手术机器人运动学不精确，SST 在内窥镜坐标系下预测相对位姿变化（平移+欧拉角+夹持器角度），确保感知与动作的一致性。

### 3. 实验设计
*   **实验场景**：在 Torin 手术机器人上进行了三类任务：
    1.  **Peg Pickup（套圈拾取）**：在不规则硅胶肝脏模型上进行，测试空间泛化性。
    2.  **Knot Tying（缝合打结）**：双臂协作，涉及复杂的柔性物体操作。
    3.  **Ex-vivo Gallbladder Dissection（离体胆囊解剖）**：使用真实动物器官，测试在真实生物组织上的表现。
*   **对比方法 (Baselines)**：
    *   **SRT**：使用额外腕部摄像头的 SOTA 手术机器人模型。
    *   **ACT / Diffusion Policy (DP)**：通用的端到端视觉运动策略（仅使用内窥镜输入）。

### 4. 资源与算力
*   **硬件平台**：实验中提到了使用 **NVIDIA RTX-4090 GPU** 进行推理延迟测试。
*   **模型规模**：几何 Transformer 采用 ViT-Large 架构，策略解码器包含 12 层 Transformer。
*   **训练细节**：所有任务训练 100 个 epoch。MASt3R 推理延迟约为 56.2ms（约 18Hz），满足实时操作需求。

### 5. 实验数量与充分性
*   **数据规模**：每个任务收集了 180-200 条演示轨迹，总计约 600 条。
*   **评估强度**：每个任务进行 10 次独立测试。
*   **消融实验**：针对“Surgical3D 数据集微调的必要性”、“不同特征连接器设计（LFC vs MSC vs MSFC）”、“不同几何模型效率（MASt3R vs VGGT）”进行了详细对比。
*   **评价**：实验覆盖了从模拟环境到真实器官、从单臂到双臂的维度，对比了当前主流的机器人学习框架，实验设计较为客观、充分。

### 6. 主要结论与发现
*   **3D 先验至关重要**：在不使用腕部摄像头的情况下，SST 的表现远超 ACT 和 DP，证明了显式引入 3D 几何特征能显著提升操作精度。
*   **空间泛化性强**：SST 在未见过的物体位置和具有高度起伏的表面上表现稳健，而传统方法在位置偏移时容易失败。
*   **临床可行性**：SST 仅需标准立体内窥镜即可达到甚至超过配备额外传感器的模型性能，更符合临床实际。

### 7. 优点（亮点）
*   **端到端架构**：避免了显式 3D 重建带来的误差累积，同时保留了几何信息的解释性。
*   **数据集贡献**：填补了手术机器人领域大规模 3D 标注数据集的空白。
*   **动作空间创新**：通过以内窥镜为中心的相对动作表征，巧妙绕过了手术机器人正向运动学不准的硬件难题。

### 8. 不足与局限
*   **推理延迟挑战**：虽然 18Hz 可用，但对于极高频的闭环控制，ViT-Large 的计算开销仍然较大。
*   **任务复杂度**：虽然涵盖了解剖，但尚未涉及出血、烟雾干扰等极端复杂的手术环境。
*   **泛化边界**：虽然在离体器官上成功，但在具有生理运动（如呼吸、心跳）的在体（In-vivo）环境下的表现仍需验证。

（完）
