---
title: Structure-aware Prompt Adaptation from Seen to Unseen for Open-Vocabulary Compositional Zero-Shot Learning
title_zh: 面向开放词汇组合零样本学习的从已见到未见的结构感知提示适配
authors: "Yihang Duan, Jiong Wang, Pengpeng Zeng, Ji Zhang, Lei Zhao, Chong Wang, Jingkuan Song, Lianli Gao"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03815v1"
tags: ["query:q14"]
score: 9.0
evidence: 开集组合零样本学习
tldr: 本文针对开放词汇组合零样本学习（OV-CZSL）中模型难以泛化到未见属性和对象的问题，提出了一种结构感知提示适配（SPA）方法。该方法受人类类比学习启发，利用语义相关概念在嵌入空间中形成的局部结构一致性，通过训练阶段的结构一致性损失（SCL）和推理阶段的结构引导适配策略（SAS），实现了从已知到未知概念的有效迁移。SPA 是一种即插即用的方法，在保持闭集性能的同时显著提升了开集识别效果。
selection_source: fresh_fetch
motivation: 现有的提示微调方法在处理开放词汇场景下未见的属性和对象时泛化能力不足，而人类通常能通过类比相似语义的已知概念来推断未知概念。
method: 提出 SPA 方法，包含在训练阶段通过结构一致性损失（SCL）约束局部结构，以及在推理阶段利用结构引导适配策略（SAS）将未见概念与已知概念的结构进行对齐。
result: 在 OV-CZSL 基准测试上的实验表明，SPA 在保持竞争力的闭集性能基础上，显著提升了模型在开放词汇设置下的表现。
conclusion: SPA 是一种通用的即插即用方法，通过利用嵌入空间的局部结构一致性，有效解决了组合零样本学习中从已知到未知概念的泛化难题。
---

## 摘要
开放词汇组合零样本学习（OV-CZSL）的目标是在开放词汇设置下识别属性-物体组合，其中会对已见和未见的属性及物体的组合进行评估。最近，提示微调（prompt tuning）方法在封闭设置（即组合零样本学习，CZSL，仅评估已见属性和物体的组合）中展示了强大的泛化能力。然而，将这些方法直接应用于 OV-CZSL 可能不足以泛化到未见的属性、物体及其组合，因为它受限于已见的属性和物体。通常，当面对未见概念时，人类会与具有相似语义的已见概念进行类比，从而推断其含义（例如，“湿的”和“潮湿的”，“衬衫”和“夹克”）。在本文中，我们通过实验表明，语义相关的属性或物体在嵌入空间中的分布倾向于形成一致的局部结构。基于上述结构，我们提出了结构感知提示适配（SPA）方法，使模型能够从已见的属性和物体泛化到未见的属性和物体。具体而言，在训练阶段，我们设计了一种结构感知一致性损失（SCL），在每次迭代中鼓励已见属性和物体的局部结构一致性。在推理阶段，我们设计了一种结构引导适配策略（SAS），自适应地将未见属性和物体的结构与具有相似语义的已训练已见属性和物体的结构进行对齐。值得注意的是，SPA 是一种即插即用的方法，可以无缝集成到现有的 CZSL 提示微调方法中。在 OV-CZSL 基准测试上的大量实验表明，SPA 在显著提高开放词汇结果的同时，实现了具有竞争力的封闭集性能。

## Abstract
The goal of Open-Vocabulary Compositional Zero-Shot Learning (OV-CZSL) is to recognize attribute-object compositions in the open-vocabulary setting, where compositions of both seen and unseen attributes and objects are evaluated. Recently, prompt tuning methods have demonstrated strong generalization capabilities in the closed setting, where only compositions of seen attributes and objects are evaluated, i.e., Compositional Zero-Shot Learning (CZSL). However, directly applying these methods to OV-CZSL may not be sufficient to generalize to unseen attributes, objects and their compositions, as it is limited to seen attributes and objects. Normally, when faced with unseen concepts, humans adopt analogies with seen concepts that have the similar semantics thereby inferring their meaning (e.g., "wet" and "damp", "shirt" and "jacket"). In this paper, we experimentally show that the distribution of semantically related attributes or objects tends to form consistent local structures in the embedding space. Based on the above structures, we propose Structure-aware Prompt Adaptation (SPA) method, which enables models to generalize from seen to unseen attributes and objects. Specifically, in the training stage, we design a Structure-aware Consistency Loss (SCL) that encourages the local structure's consistency of seen attributes and objects in each iteration. In the inference stage, we devise a Structure-guided Adaptation Strategy (SAS) that adaptively aligns the structures of unseen attributes and objects with those of trained seen attributes and objects with similar semantics. Notably, SPA is a plug-and-play method that can be seamlessly integrated into existing CZSL prompt tuning methods. Extensive experiments on OV-CZSL benchmarks demonstrate that SPA achieves competitive closed-set performance while significantly improving open-vocabulary results.

---

## 论文详细总结（自动生成）

# 论文总结：面向开放词汇组合零样本学习的结构感知提示适配（SPA）

### 1. 核心问题与研究动机
*   **核心问题**：现有的组合零样本学习（CZSL）方法主要集中在“闭集”场景（即属性和物体在训练中都见过，仅组合未见）。但在**开放词汇（OV-CZSL）**场景下，模型需要处理训练中从未见过的属性、物体或两者皆未见的组合。
*   **研究动机**：
    *   直接对预训练视觉-语言模型（如 CLIP）进行提示微调（Prompt Tuning）容易导致模型过拟合于已见概念，破坏了 CLIP 原始的语义结构，导致对未见概念的泛化能力差。
    *   **人类认知启发**：人类通过类比学习，例如通过“湿的”理解“潮湿的”。作者发现语义相关的概念在 CLIP 嵌入空间中形成了稳定的**局部结构**，这种结构在微调过程中应当被保留并用于引导未见概念的学习。

### 2. 方法论（SPA 框架）
SPA 是一种即插即用的模块，包含两个核心组件：
*   **结构感知一致性损失（SCL, Training Stage）**：
    *   **核心思想**：在训练过程中，约束已见属性/物体的提示词嵌入（Prompt Embedding）在微调前后的局部结构保持一致。
    *   **技术细节**：首先计算 CLIP 原始空间中每个概念的 Top-K 邻居。在微调时，通过 **KL 散度** 约束当前嵌入空间与原始空间中邻居相似度分布的一致性，防止微调扭曲语义空间。
*   **结构引导适配策略（SAS, Inference Stage）**：
    *   **核心思想**：将已见概念在训练中学习到的“知识偏移”迁移给未见的语义近邻。
    *   **技术细节**：在推理时，计算未见概念与已见概念的语义相似度。未见概念的新提示词 = 原始提示词 + 已见近邻概念偏移量（微调后减去微调前）的加权平均。这使得未见概念能够自适应地对齐到微调后的任务空间。

### 3. 实验设计
*   **数据集**：
    *   **MIT-States**：包含 115 个属性和 245 个物体。
    *   **C-GQA**：较大规模，包含 413 个属性和 674 个物体。
    *   **VAW-CZSL**：大规模且极具挑战性，包含 533 个属性和 543 个物体。
    *   **UT-Zappos**：细粒度鞋类数据集。
*   **Benchmark 与对比方法**：
    *   对比了传统非 VLM 方法（如 LE, CompCos, OADis, SCEN, CANet）。
    *   对比了基于 CLIP 的提示微调基准（CSP, HPL, DFSP, Troika）。
    *   对比了专门针对 OV-CZSL 的 NEL 方法。

### 4. 资源与算力
*   **模型架构**：统一使用 **CLIP ViT-B/32** 作为骨干网络（为了降低开销，未采用更重的 ViT-L/14）。
*   **训练参数**：AdamW 优化器，学习率 $1\times10^{-5}$ 至 $5\times10^{-5}$，训练 20 个 epoch。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量，但提供了显存占用数据（训练时约 6.5GB 至 11.5GB，推理时约 4.5GB），并指出 SPA 带来的额外计算开销极小（训练时间仅增加约 5%，推理延迟增加忽略不计）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数据集上进行了全面测试，涵盖了从细粒度到大规模的多种场景。
*   **消融实验**：详细验证了 SCL 和 SAS 两个组件的各自贡献、邻居数量 $K$ 的敏感度、损失权重 $\lambda$ 和温度系数 $\tau$ 的影响。
*   **充分性评价**：实验设计非常充分且客观。作者不仅报告了整体指标（HM, AUC），还细化到了五种不同的组合类型（如 $A^*O^*$ 全未见组合），并进行了计算成本分析和定性可视化分析，证明了方法的鲁棒性。

### 6. 主要结论
*   **性能提升**：SPA 在所有基准测试中均显著提升了开集性能。在 C-GQA 数据集最难的 $A^*O^*$ 任务上，相对基准提升了 **55.1%**。
*   **即插即用**：SPA 可以无缝集成到多种现有的提示微调方法中，并均能带来增益。
*   **结构价值**：实验证明保留 CLIP 的局部结构一致性是实现从已见到未见知识迁移的关键。

### 7. 优点
*   **逻辑简洁高效**：利用类比学习的直觉，通过简单的线性偏移迁移解决了复杂的泛化问题。
*   **极低开销**：不需要复杂的生成模型或外部知识库，仅通过结构约束即可实现显著增益。
*   **通用性强**：作为插件不改变原有模型架构，易于在实际应用中部署。

### 8. 不足与局限
*   **语义孤岛问题**：如果某个未见概念在语义空间中极其孤立（没有相近的已见概念），SAS 的类比效果会受限。
*   **依赖预训练质量**：SPA 的效果高度依赖于 CLIP 原始嵌入空间的质量，如果原始空间中存在语义偏见，SPA 可能会继承这些偏见。
*   **视觉-语义鸿沟**：虽然 CLIP 缓解了此问题，但在某些极端情况下，语义相近的概念视觉差异可能巨大，导致结构迁移失效。

（完）
