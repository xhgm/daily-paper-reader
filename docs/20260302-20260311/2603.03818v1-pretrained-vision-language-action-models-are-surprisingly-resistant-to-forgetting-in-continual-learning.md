---
title: Pretrained Vision-Language-Action Models are Surprisingly Resistant to Forgetting in Continual Learning
title_zh: 预训练视觉-语言-动作模型在持续学习中表现出惊人的抗遗忘能力
authors: "Huihan Liu, Changyeon Kim, Bo Liu, Minghuan Liu, Yuke Zhu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03818v1"
tags: ["query:q18"]
score: 10.0
evidence: 预训练VLA模型中的持续学习
tldr: 本研究探讨了预训练视觉-语言-动作（VLA）模型在机器人持续学习中的表现。研究发现，与从零训练的小型策略模型相比，预训练VLA模型具有极强的抗遗忘能力。通过简单的经验回放（ER）机制，即使在回放数据量极小的情况下，VLA也能在学习新技能的同时有效保持旧技能，且预训练在缓解遗忘和增强前向学习能力方面起到了关键作用。
selection_source: fresh_fetch
motivation: 旨在探究大规模预训练VLA模型是否能解决机器人策略在持续学习过程中常见的灾难性遗忘问题。
method: 通过在持续学习任务中对比预训练VLA与小型从零训练模型，并评估简单经验回放机制在不同数据量下的效果。
result: 预训练VLA模型展现出惊人的抗遗忘性，仅需少量回放即可达到零遗忘，并能通过微调快速找回退化的旧技能。
conclusion: 大规模预训练改变了持续学习的动态，使模型能够通过简单的回放策略高效且持久地获取新技能。
---

## 摘要
持续学习是机器人策略学习中一个长期存在的挑战，即策略必须随着时间的推移习得新技能，同时不发生对先前已学技能的灾难性遗忘。虽然先前的研究已经广泛探讨了在从零开始训练的相对较小的行为克隆（BC）策略模型中的持续学习，但在现代大规模预训练视觉-语言-动作（VLA）模型中的表现仍有待探索。在这项工作中，我们发现与从零开始训练的小型策略模型相比，预训练的 VLA 模型表现出显著的抗遗忘能力。简单的经验回放（ER）在 VLA 上效果惊人，有时即使在回放数据量较小的情况下也能实现零遗忘。我们的分析表明，预训练在下游持续学习性能中起着至关重要的作用：大型预训练模型在保持强大的前向学习能力的同时，通过较小的回放缓冲区规模缓解了遗忘。此外，我们发现尽管在学习新任务期间性能有所下降，但 VLA 仍能保留来自先前任务的相关知识。这种知识保留使得通过微调可以快速恢复看似已遗忘的技能。总之，这些见解表明大规模预训练从根本上改变了持续学习的动态，使模型能够通过简单的回放随时间不断获取新技能。代码和更多信息可在 https://ut-austin-rpl.github.io/continual-vla 找到。

## Abstract
Continual learning is a long-standing challenge in robot policy learning, where a policy must acquire new skills over time without catastrophically forgetting previously learned ones. While prior work has extensively studied continual learning in relatively small behavior cloning (BC) policy models trained from scratch, its behavior in modern large-scale pretrained Vision-Language-Action (VLA) models remains underexplored. In this work, we found that pretrained VLAs are remarkably resistant to forgetting compared with smaller policy models trained from scratch. Simple Experience Replay (ER) works surprisingly well on VLAs, sometimes achieving zero forgetting even with a small replay data size. Our analysis reveals that pretraining plays a critical role in downstream continual learning performance: large pretrained models mitigate forgetting with a small replay buffer size while maintaining strong forward learning capabilities. Furthermore, we found that VLAs can retain relevant knowledge from prior tasks despite performance degradation during learning new tasks. This knowledge retention enables rapid recovery of seemingly forgotten skills through finetuning. Together, these insights imply that large-scale pretraining fundamentally changes the dynamics of continual learning, enabling models to continually acquire new skills over time with simple replay. Code and more information can be found at https://ut-austin-rpl.github.io/continual-vla

---

## 论文详细总结（自动生成）

这是一份关于论文《Pretrained Vision-Language-Action Models are Surprisingly Resistant to Forgetting in Continual Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了机器人策略学习中的一个核心难题：**持续学习（Continual Learning）中的灾难性遗忘**。当机器人按顺序学习新技能时，往往会迅速遗忘旧技能。
*   **背景**：以往研究多集中在从零开始训练的小型行为克隆（BC）模型上，这些模型通常需要复杂的正则化或巨大的回放缓冲区来缓解遗忘。
*   **核心问题**：现代大规模预训练的**视觉-语言-动作（VLA）模型**在持续学习中的表现是否与小型模型不同？预训练在其中扮演了什么角色？

### 2. 方法论：核心思想与技术细节
论文并没有提出复杂的算法，而是通过严谨的实验揭示了预训练 VLA 的内在特性。
*   **核心思想**：利用简单的**经验回放（Experience Replay, ER）**机制，配合 VLA 强大的预训练表征，实现高效的持续学习。
*   **关键技术细节**：
    *   **经验回放（ER）**：在学习新任务 $T_k$ 时，从旧任务的缓冲区中随机抽取少量样本（如每任务仅 10-1000 个 transition），与当前任务数据混合训练。
    *   **模型架构**：采用了基于流匹配（Flow Matching）的 **Pi0** 和 **GR00T N1.5**。这些模型通常包含冻结或微调的视觉编码器（如 SigLIP）、语言骨干（如 Gemma/Qwen）以及专门的动作头。
    *   **微调策略**：对于 Pi0，采用视觉编码器全量微调、语言和动作头使用 LoRA 微调的策略。

### 3. 实验设计
*   **数据集/场景**：使用了机器人持续学习的标准 Benchmark —— **LIBERO**。
    *   包含四个子集：`LIBERO-Spatial`（空间关系）、`LIBERO-Object`（物体变化）、`LIBERO-Goal`（目标多样性）、`LIBERO-10`（10个迥异的任务）。
*   **对比方法（Baselines）**：
    *   **非预训练模型**：BC-Transformer、BC-Diffusion Policy、BC-ViT。
    *   **持续学习算法**：Sequential（直接顺序微调）、EWC（弹性权重巩固，一种正则化方法）、ER（经验回放）。
*   **评估指标**：平均成功率（SR）和负向后向迁移（NBT，衡量遗忘程度，越低越好）。

### 4. 资源与算力
*   **算力说明**：论文致谢中提到使用了德克萨斯高级计算中心（TACC）和 UT Austin 生成式 AI 中心的计算资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量及总训练时长。但考虑到 Pi0 和 GR00T 均为 3B 参数规模的模型，且实验涵盖了多个 Benchmark 的多次重复实验，推测消耗了大量的 A100 或 H100 计算资源。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常充分且具有说服力。
    *   **跨模型验证**：对比了 Pi0 和 GR00T 两种不同架构的 VLA。
    *   **消融实验**：对比了“从零训练”、“仅视觉语言预训练”和“视觉语言动作全预训练”三种初始化状态。
    *   **缓冲区敏感度**：测试了从 0.2% 到 20% 不同比例的回放数据量。
    *   **知识保留实验**：通过“组件交换”和“恢复效率”实验深入探究了知识在模型内部的存储位置。
*   **客观性**：实验采用了多随机种子，并提供了详细的混淆矩阵（Confusion Matrix）和归一化指标，确保了结果的客观性。

### 6. 主要结论与发现
*   **惊人的抗遗忘性**：预训练 VLA 在极小回放数据（如 2% 甚至更低）下，遗忘率远低于小型模型，甚至能出现**正向后向迁移**（学习新任务后，旧任务表现反而提升）。
*   **预训练是关键**：预训练不仅提高了学习新任务的“塑性”（前向学习），还增强了保留旧知识的“稳定性”。
*   **知识并未消失**：即使在极端情况下旧任务性能下降，VLA 的内部表征仍保留了相关知识。实验证明，仅需不到 10% 的原始训练步数，VLA 就能快速找回“遗忘”的技能，而小型模型则需从头再来。
*   **视觉骨干是遗忘主因**：组件交换实验表明，视觉-语言骨干权重的更新是导致性能下降的主要原因，而非动作头。

### 7. 优点：亮点与贡献
*   **观念挑战**：挑战了传统持续学习中“稳定性-塑性”必须权衡的固有认知，证明大规模预训练改变了游戏规则。
*   **实用性强**：证明了在 VLA 时代，简单的经验回放（ER）可能优于复杂的持续学习算法，简化了机器人技能扩展的流程。
*   **深度分析**：不仅展示了结果，还通过“恢复效率”等实验深入挖掘了模型内部知识保留的机制。

### 8. 不足与局限
*   **环境局限**：所有实验均在 LIBERO 模拟器中完成，**缺乏真实机器人（Real-world）**环境下的验证。
*   **任务顺序**：虽然使用了标准 Benchmark，但未深入探讨任务顺序（Task Ordering）对 VLA 遗忘动态的潜在影响。
*   **算力门槛**：虽然结论是“简单回放即可”，但前提是拥有强大的预训练 VLA 模型，这对于算力受限的研究者仍是较高门槛。
*   **模型规模边界**：虽然讨论了模型大小的影响，但对于“多大规模的预训练才能触发这种抗遗忘特性”尚未给出精确的临界点。

（完）
