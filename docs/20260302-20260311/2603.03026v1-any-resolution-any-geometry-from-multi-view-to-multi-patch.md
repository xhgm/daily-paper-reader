---
title: "Any Resolution Any Geometry: From Multi-View To Multi-Patch"
title_zh: 任意分辨率，任意几何：从多视图到多补丁
authors: "Wenqing Cui, Zhenyu Li, Mykola Lavreniuk, Jian Shi, Ramzi Idoughi, Xiangjun Tang, Peter Wonka"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03026v1"
tags: ["query:q19"]
score: 9.0
evidence: 用于三维场景理解的高分辨率深度和法线估计
tldr: 针对高分辨率单目深度与法向估计中局部细节与全局一致性难以兼顾的问题，本文提出超分辨率几何变换器（URGT）。该方法将高分辨率图像划分为多个分块，并结合预训练模型的粗略先验，通过多分块变换器架构和跨分块注意力机制实现全局一致的几何推理。此外，引入GridMix采样策略增强空间鲁棒性。实验表明，URGT在UnrealStereo4K等数据集上显著提升了精度，具备极强的零样本泛化能力和高分辨率扩展性。
selection_source: fresh_fetch
motivation: 现有的高分辨率深度与法向估计方法在保留精细局部细节的同时，难以维持全局几何一致性。
method: 提出URGT框架，通过将图像分块并结合粗略先验，利用跨分块注意力机制和GridMix采样策略在单一前向传播中实现全局一致的几何细化。
result: 在UnrealStereo4K数据集上，深度AbsRel从0.0582降至0.0291，法向平均角度误差从23.36度降至18.51度，达到SOTA水平。
conclusion: 该多分块框架为高质量几何细化提供了一种高效、可扩展且具有强泛化能力的解决方案。
---

## 摘要
表面法线和深度的联合估计对于全面的 3D 场景理解至关重要，但由于在保留精细局部细节与维持全局一致性之间存在权衡，高分辨率预测仍然具有挑战性。为了应对这一挑战，我们提出了超分辨率几何 Transformer (URGT)，它将视觉几何基础 Transformer (VGGT) 适配为统一的多补丁 Transformer，用于单目高分辨率深度-法线估计。单张高分辨率图像被划分为多个补丁，并利用来自预训练模型的粗略深度和法线先验进行增强，然后在单次前向传播中进行联合处理，以预测精细化的几何输出。全局一致性通过跨补丁注意力机制（cross-patch attention）来强制执行，该机制在共享主干网络中实现了长程几何推理和补丁间的无缝信息传播。为了进一步增强空间鲁棒性，我们引入了 GridMix 补丁采样策略，在训练期间概率性地采样网格配置，从而提高补丁间的一致性和泛化能力。我们的方法在 UnrealStereo4K 上达到了最先进的结果，共同改进了深度和法线估计，将 AbsRel 从 0.0582 降低到 0.0291，RMSE 从 2.17 降低到 1.31，并将平均角度误差从 23.36 度降低到 18.51 度，同时生成了更清晰、更稳定的几何结构。所提出的多补丁框架还展示了强大的零样本和跨域泛化能力，并能有效扩展到极高分辨率，为高质量几何精细化提供了一种高效且可扩展的解决方案。

## Abstract
Joint estimation of surface normals and depth is essential for holistic 3D scene understanding, yet high-resolution prediction remains difficult due to the trade-off between preserving fine local detail and maintaining global consistency. To address this challenge, we propose the Ultra Resolution Geometry Transformer (URGT), which adapts the Visual Geometry Grounded Transformer (VGGT) into a unified multi-patch transformer for monocular high-resolution depth--normal estimation. A single high-resolution image is partitioned into patches that are augmented with coarse depth and normal priors from pre-trained models, and jointly processed in a single forward pass to predict refined geometric outputs. Global coherence is enforced through cross-patch attention, which enables long-range geometric reasoning and seamless propagation of information across patches within a shared backbone. To further enhance spatial robustness, we introduce a GridMix patch sampling strategy that probabilistically samples grid configurations during training, improving inter-patch consistency and generalization. Our method achieves state-of-the-art results on UnrealStereo4K, jointly improving depth and normal estimation, reducing AbsRel from 0.0582 to 0.0291, RMSE from 2.17 to 1.31, and lowering mean angular error from 23.36 degrees to 18.51 degrees, while producing sharper and more stable geometry. The proposed multi-patch framework also demonstrates strong zero-shot and cross-domain generalization and scales effectively to very high resolutions, offering an efficient and extensible solution for high-quality geometry refinement.

---

## 论文详细总结（自动生成）

### 论文总结：Any Resolution Any Geometry: From Multi-View To Multi-Patch

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对**高分辨率（如 4K 及以上）单目深度与表面法线联合估计**中的核心矛盾：**局部精细细节的保留与全局几何一致性的维持**。
*   **背景**：现有的深度/法线估计模型（如 Metric3D V2）受限于显存，通常在低分辨率下运行，导致丢失高频细节。
*   **动机**：现有的分块细化方法（如 PatchRefiner）往往独立处理每个补丁，导致补丁边界出现不连续性（接缝），且缺乏全局几何推理能力。
*   **核心思想**：借鉴多视图重建（Multi-View）的 Transformer 架构，将单张高分辨率图像的多个补丁（Patches）视为“虚拟视图”，通过统一的 Transformer 主干网络进行联合处理。

#### 2. 论文提出的方法论：超分辨率几何变换器（URGT）
*   **核心架构**：基于视觉几何基础 Transformer（VGGT）进行适配，构建了 **URGT** 模型。
*   **输入处理**：
    *   将高分辨率 RGB 图像划分为多个补丁。
    *   引入**粗略先验**：利用预训练模型（Depth-Anything V2 和 Metric3D V2）生成低分辨率的深度和法线图，上采样后作为条件输入。
    *   使用 DINOv2 提取视觉特征，并与深度/法线先验特征融合。
*   **关键技术细节**：
    *   **全局位置编码（Global RoPE）**：通过全局坐标偏移，确保不同补丁中的 Token 在统一的参考系下进行位置编码，解决边界对齐问题。
    *   **混合注意力机制**：交替使用**补丁内注意力（Intra-patch）**捕捉局部细节，以及**跨补丁注意力（Cross-patch）**实现长程几何推理和信息传播。
    *   **GridMix 采样策略**：训练时随机采样不同配置的网格（1×1 到 4×4），增强模型对不同分块方式的鲁棒性。
*   **预测头**：采用 DPT 风格的轻量级头部，预测相对于粗略先验的**偏移量（Offsets）**，从而实现几何细化。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：UnrealStereo4K（合成数据集，提供高质量 4K 深度和法线真值）。
    *   **测试/泛化集**：Booster、ETH3D、Middlebury 2014（用于零样本跨域评估）。
*   **Benchmark 与对比方法**：
    *   对比了 SOTA 基础模型：Depth-Anything V2、Metric3D V2。
    *   对比了高分辨率细化方法：PatchRefiner (V1/V2)、PatchFusion、PRO。
*   **评价指标**：AbsRel、RMSE、δ1、一致性误差（CE）、边界误差（PDBE）以及法线角度误差。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练参数**：
    *   迭代次数：80,000 次。
    *   Batch Size：1（由于高分辨率补丁序列较长）。
    *   优化器：AdamW，学习率 1e-5。
    *   技术手段：启用了梯度检查点（Gradient Checkpointing）以降低显存消耗。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   **定量对比**：在 UnrealStereo4K 上进行了深度和法线的全面精度对比。
    *   **零样本泛化**：在三个真实世界数据集上验证了模型的鲁棒性。
    *   **消融实验**：针对 GridMix 采样策略、全局位置编码（Global RoPE）、跨补丁注意力机制分别进行了对比实验。
*   **充分性评价**：实验设计较为充分且客观。通过引入 CE 和 PDBE 指标，专门量化了补丁间的一致性和边界锐度，这比单纯的 AbsRel 更能体现高分辨率模型的优势。

#### 6. 主要结论与发现
*   **性能突破**：在 UnrealStereo4K 上，URGT 将深度 AbsRel 从 0.0582 降至 **0.0291**（提升约 50%），法线平均角度误差从 23.36° 降至 **18.51°**。
*   **一致性**：跨补丁注意力结合全局位置编码显著消除了补丁接缝，CE 指标大幅下降。
*   **分辨率无关性**：模型展现了极强的扩展性，能够直接应用于 8K 图像，且在超高分辨率下依然能保持锐利的边缘和全局几何逻辑。
*   **联合学习增益**：深度和法线的联合训练比单任务训练具有更好的相互促进作用。

#### 7. 优点
*   **架构优雅**：将“多视图”逻辑转化为“多补丁”逻辑，利用 Transformer 的长程建模能力解决了传统分块方法的接缝问题。
*   **实用性强**：支持**任意分辨率**输入，无需针对特定分辨率重新训练。
*   **细节恢复极佳**：相比基础模型，能显著修复细小结构（如电线、植物枝叶）的几何形状。

#### 8. 不足与局限
*   **反射表面失效**：模型在处理强反射物体（如镜子）时表现不佳，容易将其误判为背景空间的延伸。
*   **依赖先验**：模型的最终效果在一定程度上受限于初始粗略预测模型（Depth-Anything/Metric3D）的质量。
*   **算力成本**：虽然推理效率尚可（4K 约 1 秒），但训练过程对 A100 等高性能显存设备有较高要求。

（完）
