---
title: "CORAL: Scalable Multi-Task Robot Learning via LoRA Experts"
title_zh: CORAL：通过 LoRA 专家实现的可扩展多任务机器人学习
authors: "Yuankai Luo, Woping Chen, Tong Liang, Zhenguo Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09298v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的可扩展多任务学习
tldr: 针对多任务机器人学习中存在的任务干扰和存储开销问题，本文提出CORAL框架。该方法冻结预训练的VLA骨干网络，为每个任务配备独立的轻量化LoRA专家，并通过动态路由引擎实现零开销切换。CORAL通过参数隔离彻底消除了负迁移，并支持在不发生灾难性遗忘的情况下顺序扩展新任务。实验证明，CORAL在真实双臂机器人和多个仿真基准上均显著优于传统的联合训练方法，展现了极佳的可扩展性。
selection_source: fresh_fetch
motivation: 旨在解决多任务微调中因梯度冲突导致的负迁移问题，以及为每个任务维护完整模型权重带来的存储与部署压力。
method: 采用冻结骨干网络结合单任务LoRA专家的架构，利用CORAL管理器根据语言指令动态路由并实时切换专家。
result: 在Galaxea R1真实机器人及LIBERO、Google Robot等仿真实验中，该方法有效克服了指令歧义，性能大幅超越联合训练。
conclusion: CORAL提供了一种通用且高效的终身多任务学习方案，通过参数隔离实现了高性能、可扩展且无遗忘的机器人控制。
---

## 摘要
在现实世界的机器人中部署视觉-语言-动作（VLA）模型暴露了一个核心的多任务学习挑战：如何协调多任务机器人学习中的任务干扰。当多个任务在单一阶段进行联合微调时，来自不同任务的梯度可能会发生冲突，导致负迁移并降低每个任务的性能。然而，为每个任务维护一个单独的完整检查点通常在存储和部署上是不可行的。为了解决这一困境，我们提出了 CORAL，这是一个与骨干网络和具身形式无关的框架，旨在减轻多任务干扰，同时保持对连续新任务流的自然扩展性。CORAL 冻结单个预训练的 VLA 骨干网络，并为每个任务附加一个轻量级的低秩自适应（LoRA）专家；在运行时，动态推理引擎（CORAL 管理器）将语言指令路由到相应的专家，并实时切换专家，且具有零推理开销。这种严格的参数隔离避免了复杂的门控网络，并从结构上防止了参数级的跨任务干扰；作为一项附加功能，它还允许按顺序引入新任务，而不会因灾难性遗忘导致参数覆盖。我们在现实世界的 Galaxea R1 双臂移动协作机器人和三个仿真基准测试（LIBERO、WidowX、Google Robot）上验证了 CORAL。结果表明，CORAL 克服了细粒度的指令歧义，且性能显著优于联合训练，为终身多任务机器人学习提供了一个实用且可扩展的系统。

## Abstract
Deploying Vision-Language-Action (VLA) models in real-world robotics exposes a core multi-task learning challenge: reconciling task interference in multi-task robotic learning. When multiple tasks are jointly fine-tuned in a single stage, gradients from different tasks can conflict, causing negative transfer and reducing per-task performance. Yet maintaining a separate full checkpoint per task is often storage- and deployment-prohibitive. To address this dilemma, we present CORAL, a backbone- and embodiment-agnostic framework designed primarily to mitigate multi-task interference while remaining naturally extensible to a continuous stream of new tasks. CORAL freezes a single pre-trained VLA backbone and attaches one lightweight Low-Rank Adaptation (LoRA) expert per task; at runtime, a dynamic inference engine (the CORAL Manager) routes language instructions to the appropriate expert and swaps experts on the fly with zero inference overhead. This strict parameter isolation avoids complex gating networks and prevents parameter-level cross-task interference by construction; as an added capability, it also enables sequentially introducing new tasks without parameter overwriting caused by catastrophic forgetting. We validate CORAL on a real-world Galaxea R1 dual-arm mobile manipulator and three simulation benchmarks (LIBERO, WidowX, Google Robot), where CORAL overcomes fine-grained instructional ambiguity and substantially outperforms joint training, yielding a practical and scalable system for lifelong multi-task robot learning. Website: https://frontierrobo.github.io/CORAL

---

## 论文详细总结（自动生成）

以下是对论文《CORAL: Scalable Multi-Task Robot Learning via LoRA Experts》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在部署视觉-语言-动作（VLA）模型时，研究者面临一个核心矛盾：**多任务干扰与部署开销的权衡**。
*   **任务干扰：** 在单一阶段对多个任务进行联合微调时，不同任务的梯度往往会发生冲突（负迁移），导致模型在特定任务上的表现不如专门微调的模型。
*   **存储瓶颈：** 为每个任务维护一个完整的模型检查点虽然能避免干扰，但在实际部署（尤其是边缘设备）中，存储开销随任务数量线性增长，难以承受。
*   **灾难性遗忘：** 顺序学习新任务会导致模型覆盖旧知识。
**CORAL** 旨在通过一种“骨干网络+轻量级专家库”的架构，实现高性能、可扩展且无遗忘的机器人多任务学习。

### 2. 方法论
CORAL 采用两阶段学习范式，核心思想是**参数隔离**：
*   **阶段一：具身感知通用预训练。** 在大规模多样化数据集上训练 VLA 骨干网络（如 SimVLA 或 $\pi_{0.5}$），使其掌握基本的运动学和视觉语义。训练完成后，永久冻结该骨干网络权重 $\theta_{base}$。
*   **阶段二：轻量级任务特定 LoRA 专家。** 为每个具体任务训练一个独立的低秩自适应（LoRA）模块。LoRA 模块被注入到编码器的注意力层和动作头中。由于每个专家只针对特定任务进行极短时间（1-5 epoch）的微调，且彼此参数独立，从而彻底消除了参数级的干扰。
*   **关键技术：CORAL 管理器（动态路由）。** 
    *   利用机器人任务中“语言指令即路由索引”的特性，无需复杂的门控网络。
    *   运行时，管理器根据指令识别任务 ID，从磁盘加载对应的 LoRA 权重并将其合并（Merge）进冻结的骨干网络。
    *   **零推理开销：** 合并操作在推理前完成，不增加前向传播的计算量（FLOPs）。

### 3. 实验设计
*   **仿真基准测试：**
    *   **LIBERO：** 包含 40 个操作任务，分为 Spatial、Object、Goal、Long 四个子集。
    *   **WidowX (Simpler-Bridge)：** 评估真实到仿真的迁移能力。
    *   **Google Robot (Simpler-Fractal)：** 评估模型在不同变体下的鲁棒性。
*   **真实世界评估：**
    *   使用 **Galaxea R1 Lite**（23 自由度双臂移动协作机器人）。
    *   测试了 8 个复杂的双臂协同任务（如折 T 恤、插吸管、拆笔帽等）以及新技能获取（开门、按电梯按钮）。
*   **对比方法：** 包括 $\pi_0$、SimVLA、X-VLA、RT-2-X 等 SOTA 模型，以及“联合全量微调”和“独立全量微调”两种基线。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号和数量。
*   但提到了训练效率：每个 LoRA 专家仅需训练 50 步或 1-5 个 epoch；专家切换时间在单 GPU 上小于 100ms。
*   **存储效率：** 对于 0.8B 参数的模型，一个 Rank-16 的专家仅占约 26MB，比全量模型缩小了约 100 倍。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 3 个主流仿真基准和 10+ 个真实世界复杂任务，总任务数超过 50 个。
*   **充分性：** 实验设计非常全面，不仅对比了成功率，还进行了跨场景泛化实验、新能力获取实验以及针对灾难性遗忘的对比分析。
*   **客观性：** 通过在不同的 VLA 骨干网络（SimVLA 和 $\pi_{0.5}$）上应用 CORAL，证明了该框架的通用性（Backbone-agnostic）。

### 6. 主要结论与发现
*   **性能领先：** CORAL 在 LIBERO 上达到了 99.3% 的平均成功率，刷新了纪录。
*   **解决干扰：** 在新技能获取实验中，联合全量微调因任务干扰仅有 24.5% 的成功率，而 CORAL 达到了与独立微调相当的高水平（接近 100%）。
*   **无遗忘扩展：** 严格的参数隔离确保了在引入新任务时，旧任务性能完全不受影响。
*   **高效部署：** 100 倍的存储压缩率使得在板载设备上维护数百个专家成为可能。

### 7. 优点
*   **架构优雅：** 巧妙利用语言指令作为天然路由，避开了 Mixture-of-Experts (MoE) 中复杂的门控训练问题。
*   **零推理延迟：** 通过权重合并技术，在不增加计算负担的前提下实现了专家切换。
*   **极佳的可扩展性：** 为终身学习（Lifelong Learning）提供了切实可行的系统方案，支持任务的无限增量添加。
*   **双臂协同能力：** 在真实机器人上展示了极高难度的细粒度双臂操作能力。

### 8. 不足与局限
*   **路由依赖性：** 极度依赖语言指令的唯一性。如果不同任务的指令存在严重歧义或重叠，确定性路由可能会失效。
*   **表征共享限制：** 虽然参数隔离了，但所有专家共享同一个冻结的骨干网络。如果骨干网络在预训练阶段未能捕获某些关键特征，仅靠轻量级 LoRA 可能无法完全弥补表征能力的缺失。
*   **专家库管理：** 随着任务增加到成千上万个，如何高效检索和管理海量专家文件可能成为新的系统挑战。

（完）
