---
title: "Dehallu3D: Hallucination-Mitigated 3D Generation from Single Image via Cyclic View Consistency Refinement"
title_zh: Dehallu3D：通过循环视图一致性细化实现单图像幻觉缓解的 3D 生成
authors: "Xiwen Wang, Shichao Zhang, Hailun Zhang, Ruowei Wang, Mao Li, Chenyu Zhou, Qijun Zhao, Ji-Zhe Zhou"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01601v1"
tags: ["query:q16"]
score: 9.0
evidence: 从单张图像进行幻觉缓解的3D生成
tldr: 针对3D重建模型中的幻觉问题（如结构异常），本文提出Dehallu3D框架。该方法通过即插即用的优化模块，利用相邻视图一致性和自适应平滑约束，在密集视角间强制执行几何连续性，从而消除异常并保留细节。此外，研究提出了ORM指标量化几何保真度，实验证明该方法显著提升了单图生成3D模型的质量。
selection_source: fresh_fetch
motivation: 现有的3D重建模型因多视图生成稀疏且不连续，容易产生空洞或突起等结构性幻觉。
method: 提出Dehallu3D框架，通过相邻视图一致性确保几何连续，并利用自适应平滑约束在消除异常值的同时保留精细特征。
result: 实验表明Dehallu3D能有效减少3D生成中的幻觉异常，并在保持结构细节方面优于现有方法。
conclusion: 该研究通过引入循环视图一致性优化和新的评估指标ORM，显著提升了单图3D生成的几何保真度和鲁棒性。
---

## 摘要
大型 3D 重建模型彻底改变了 3D 内容生成领域，在虚拟现实和游戏领域实现了广泛的应用。与其他大模型一样，大型 3D 重建模型也存在幻觉问题，会引入偏离输入数据的结构性离群值（例如，奇怪的孔洞或突起）。然而，与其他大模型不同，大型 3D 重建模型中的幻觉问题仍未得到充分探索，这导致了 3D 打印物体畸形或虚拟场景沉浸感不足。这种幻觉主要源于现有方法是从稀疏生成的多视图图像中重建 3D 内容，而这些图像存在较大的视角间隙和不连续性。为了通过消除离群值来缓解幻觉，我们提出了用于 3D 网格生成的 Dehallu3D。我们的核心思想是设计一种平衡的多视图连续性约束，以强制密集中间视角之间的平滑过渡，同时避免可能抹除尖锐几何特征的过度平滑。因此，Dehallu3D 采用了一个即插即用的优化模块，包含两个关键约束：(i) 相邻一致性，以确保跨视图的几何连续性；(ii) 自适应平滑，以保留精细细节。我们还进一步提出了离群值风险度量 (ORM) 指标，从离群值的角度量化 3D 生成中的几何保真度。大量实验表明，Dehallu3D 通过有效保留结构细节并移除幻觉离群值，实现了高保真 3D 生成。

## Abstract
Large 3D reconstruction models have revolutionized the 3D content generation field, enabling broad applications in virtual reality and gaming. Just like other large models, large 3D reconstruction models suffer from hallucinations as well, introducing structural outliers (e.g., odd holes or protrusions) that deviate from the input data. However, unlike other large models, hallucinations in large 3D reconstruction models remain severely underexplored, leading to malformed 3D-printed objects or insufficient immersion in virtual scenes. Such hallucinations majorly originate from that existing methods reconstruct 3D content from sparsely generated multi-view images which suffer from large viewpoint gaps and discontinuities. To mitigate hallucinations by eliminating the outliers, we propose Dehallu3D for 3D mesh generation. Our key idea is to design a balanced multi-view continuity constraint to enforce smooth transitions across dense intermediate viewpoints, while avoiding over-smoothing that could erase sharp geometric features. Therefore, Dehallu3D employs a plug-and-play optimization module with two key constraints: (i) adjacent consistency to ensure geometric continuity across views, and (ii) adaptive smoothness to retain fine details.We further propose the Outlier Risk Measure (ORM) metric to quantify geometric fidelity in 3D generation from the perspective of outliers. Extensive experiments show that Dehallu3D achieves high-fidelity 3D generation by effectively preserving structural details while removing hallucinated outliers.

---

## 论文详细总结（自动生成）

这是一份关于论文《Dehallu3D: Hallucination-Mitigated 3D Generation from Single Image via Cyclic View Consistency Refinement》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：单图像 3D 重建模型（如 LRM 或基于扩散的模型）在生成 3D 内容时，常因生成的多视图图像稀疏、视角间隙大且不连续，导致重建结果出现“幻觉”（Hallucinations）。
*   **核心问题**：这些幻觉表现为几何结构上的离群值（Outliers），如异常的突起、孔洞或畸形结构。这在 3D 打印中会导致制造失败，在虚拟现实中则会破坏沉浸感。
*   **研究动机**：目前针对 3D 重建中幻觉问题的研究较少，本文旨在通过增强视图间的连续性来消除这些几何异常。

### 2. 方法论
Dehallu3D 提出了一个两阶段的优化框架，核心是**循环视图一致性细化（CVCR）**模块：
*   **核心思想**：通过在 360° 范围内插入密集的中间视角，强制执行相邻视角间的几何一致性，从而平滑掉因视角不连续产生的幻觉。
*   **关键技术细节**：
    1.  **粗糙网格重建**：利用生成的 4 个正交视图及其法线贴图初始化网格，并引入**表面曝光加权法线损失（LSE）**，根据顶点的可见区域大小动态调整权重，快速修正全局拓扑。
    2.  **CVCR 模块（即插即用）**：
        *   **循环深度一致性（LDC）**：在 360° 旋转中渲染密集视角（如每 5° 一个视角），利用 SSIM 和余弦相似度约束相邻深度图的一致性。
        *   **自适应平滑（LDS）**：为了防止过度平滑抹除真实的尖锐特征，利用彩色图像的梯度来动态调整平滑权重。在图像梯度大的地方（代表有边缘/细节），减弱平滑约束。
    3.  **ORM 指标**：提出**离群值风险度量（Outlier Risk Measure）**，基于条件风险价值（CVaR）和点云密度分析，专门量化 3D 模型中离群值的严重程度。

### 3. 实验设计
*   **数据集**：主要使用 Google Scanned Objects (GSO) 数据集进行评估。
*   **Benchmark 与对比方法**：
    *   对比了 6 种 SOTA 方法：SF3D, Unique3D, CRM, InstantMesh, TripoSR, Wonder3D。
    *   评估指标涵盖外观（PSNR, SSIM, LPIPS, Clip-Sim）和几何（Chamfer Distance, F-Score, ORM）。

### 4. 资源与算力
*   **硬件环境**：实验在一台单张 **NVIDIA GeForce RTX 4090 GPU** 上完成。
*   **耗时情况**：文中提到 CVCR 模块在 5° 间隔下的优化耗时约为 **163.3 秒**。虽然比前向推理模型慢，但该方法侧重于高保真度而非极致速度。

### 5. 实验数量与充分性
*   **实验规模**：进行了全面的定量对比（表 1）、定性视觉对比（图 3）、以及针对不同损失函数组件（LSE, LDC, LDS）的消融实验（表 2）。
*   **充分性与公平性**：
    *   实验涵盖了从 3° 到 15° 不同视角密度的对比，验证了 5° 是质量与效率的最佳平衡点。
    *   通过将 CVCR 模块集成到 Unique3D 中（图 6），证明了该模块的通用性和即插即用特性。
    *   引入 ORM 指标并验证其与人类感知的相关性，增强了实验的客观性。

### 6. 主要结论与发现
*   **性能领先**：Dehallu3D 在所有外观和几何指标上均优于现有方法，尤其在减少离群值方面表现卓越。
*   **幻觉抑制**：通过密集视角的循环约束，可以有效消除因稀疏视图生成带来的结构性错误。
*   **细节保留**：自适应平滑机制成功解决了“消除噪声”与“保留尖锐特征”之间的矛盾。

### 7. 优点
*   **即插即用**：CVCR 模块不依赖特定的初始化方法，可轻松集成到现有的 3D 网格重建管线中。
*   **评估创新**：提出的 ORM 指标为 3D 生成领域的“幻觉”评估提供了量化工具。
*   **几何保真度高**：相比于直接推理模型，该方法通过优化手段显著提升了网格的物理合理性。

### 8. 不足与局限
*   **时间开销**：由于涉及密集的视图渲染和迭代优化，其生成速度（约 2-3 分钟）远慢于 TripoSR 等秒级生成的模型，不适用于实时性要求极高的场景。
*   **计算成本**：密集视角的渲染增加了显存和计算资源的消耗。
*   **依赖初始质量**：如果初始生成的 2D 多视图存在严重的语义错误（而非仅仅是不连续），CVCR 模块可能难以完全修复。

（完）
