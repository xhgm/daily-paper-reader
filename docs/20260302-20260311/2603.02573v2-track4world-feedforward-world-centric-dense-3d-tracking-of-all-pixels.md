---
title: "Track4World: Feedforward World-centric Dense 3D Tracking of All Pixels"
title_zh: Track4World：全像素的前馈式以世界为中心的稠密三维跟踪
authors: "Jiahao Lu, Jiayi Xu, Wenbo Hu, Ruijie Zhu, Chengfeng Zhao, Sai-Kit Yeung, Ying Shan, Yuan Liu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02573v2"
tags: ["query:q19"]
score: 9.0
evidence: 以世界为中心的稠密3D追踪与场景表示
tldr: 针对单目视频中全像素3D轨迹估计的挑战，本文提出了Track4World，这是一个前馈式模型，旨在实现世界坐标系下的高效全像素3D跟踪。该方法利用VGGT风格的ViT编码全局3D场景，并通过创新的3D相关性方案同步估计2D和3D密集流。实验证明，Track4World在2D/3D流估计和3D跟踪任务上均优于现有方法，为实时4D重建提供了稳健且可扩展的解决方案。
selection_source: fresh_fetch
motivation: 现有的单目3D跟踪方法要么仅限于稀疏点跟踪，要么依赖于速度较慢的优化框架，难以实现高效的全像素密集跟踪。
method: 提出一种基于VGGT风格ViT的前馈模型，通过新型3D相关性方案同时估计任意帧间的像素级2D/3D密集流及场景几何。
result: 在多个基准测试中，该方法在2D/3D流估计和3D跟踪性能上均显著超越了现有技术。
conclusion: Track4World为世界坐标系下的全像素3D跟踪提供了一种高效、稳健且具有扩展性的新途径，有力推动了4D重建技术的发展。
---

## 摘要
从单目视频中估计每个像素的三维轨迹，对于全面理解视频的三维动态至关重要且具有广阔前景。近期的单目三维跟踪工作展示了令人印象深刻的性能，但局限于仅跟踪首帧的稀疏点，或是采用基于优化的缓慢框架进行稠密跟踪。在本文中，我们提出了一种名为 Track4World 的前馈模型，能够在以世界为中心的坐标系中实现对每个像素的高效整体三维跟踪。Track4World 构建于由 VGGT 风格 ViT 编码的全局三维场景表示之上，采用了一种新颖的三维相关方案，以同时估计任意帧对之间的像素级二维和三维稠密流。估计的场景流与重建的三维几何结构相结合，使得后续能够对该视频的每个像素进行高效的三维跟踪。在多个基准测试上的广泛实验表明，我们的方法在二维/三维流估计和三维跟踪方面始终优于现有方法，突显了其在现实世界四维重建任务中的鲁棒性和可扩展性。

## Abstract
Estimating the 3D trajectory of every pixel from a monocular video is crucial and promising for a comprehensive understanding of the 3D dynamics of videos. Recent monocular 3D tracking works demonstrate impressive performance, but are limited to either tracking sparse points on the first frame or a slow optimization-based framework for dense tracking. In this paper, we propose a feedforward model, called Track4World, enabling an efficient holistic 3D tracking of every pixel in the world-centric coordinate system. Built on the global 3D scene representation encoded by a VGGT-style ViT, Track4World applies a novel 3D correlation scheme to simultaneously estimate the pixel-wise 2D and 3D dense flow between arbitrary frame pairs. The estimated scene flow, along with the reconstructed 3D geometry, enables subsequent efficient 3D tracking of every pixel of this video. Extensive experiments on multiple benchmarks demonstrate that our approach consistently outperforms existing methods in 2D/3D flow estimation and 3D tracking, highlighting its robustness and scalability for real-world 4D reconstruction tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Track4World: Feedforward World-centric Dense 3D Tracking of All Pixels》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：从单目视频中重建所有像素的 3D 轨迹（4D 动态理解）在机器人、动画制作和物理推理中具有重要意义。
*   **核心挑战**：
    1.  **稀疏性限制**：现有前馈模型大多仅能跟踪首帧的稀疏点，无法处理后续帧新出现的像素。
    2.  **效率瓶颈**：基于优化的稠密跟踪方法速度极慢；而直接预测全像素轨迹会导致计算和内存开销爆炸。
    3.  **数据稀缺**：高质量的 3D 运动标注（Ground Truth）极度匮乏。
*   **整体含义**：本文提出了 **Track4World**，这是一个高效的前馈框架，通过将连续跟踪分解为任意帧对之间的场景流估计，实现了在世界坐标系下对视频中“每一个像素”的稠密 3D 跟踪。

### 2. 方法论
Track4World 采用逻辑清晰的三阶段流水线：
*   **全局场景表示提取**：利用微调后的 VGGT 风格 ViT（如 Depth Anything v3）提取相机中心点云、相机位姿和几何特征。
*   **场景流解码器（核心创新）**：
    *   **稀疏到稠密策略**：在 1/8 分辨率的锚点上进行迭代更新，最后通过学习到的上采样恢复全分辨率流。
    *   **2D-to-3D 相关性机制**：这是本文的技术亮点。不同于传统方法在 3D 空间进行昂贵的 K-NN 搜索，该方法通过 2D 像素平面相关性引导 3D 流更新。它先更新 2D 流，再将其“提升”到 3D 空间，结合几何特征进行 3D 场景流的迭代修正。
    *   **2D-3D 联合监督**：利用海量的 2D 光流数据为 3D 场景流任务提供辅助信号，增强模型的泛化能力。
*   **全局轨迹融合**：将估计的任意帧对间的 3D 场景流与重建的几何结构结合，在统一的世界坐标系中链式合成所有像素的连续 3D 轨迹。

### 3. 实验设计
*   **任务与数据集**：
    *   **场景流/光流估计**：Kubric-3D（合成）、KITTI（真实）、BlinkVision。
    *   **3D 跟踪**：TAPVid-3D 衍生出的 ADT、PStudio、DriveTrack 以及 PointOdyssey。
    *   **2D 跟踪**：Kinetics、RoboTAP、RGB-Stacking。
    *   **几何与位姿**：ScanNet、Sintel、TUM、Bonn 等。
*   **对比方法**：
    *   **光流/场景流**：RAFT, RAFT-3D, ZeroMSF, POMATO, Any4D, V-DPM。
    *   **3D 跟踪**：SpatialTracker (STV2), DELTA, MASt3R, MonST3R。
    *   **2D 跟踪**：CoTracker3, TAPIR, LocoTrack。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 40GB 显存的 GPU**（具体型号未标明，推测为 A100 或 A6000 级别）。
*   **训练时长**：
    *   **第一阶段（几何编码器微调）**：100,000 步，耗时约 **1 周**。
    *   **第二阶段（运动解码器训练）**：100,000 步，耗时约 **5 天**。
*   **推理效率**：在 16 帧序列上，推理时间为 3.4 秒，显存占用 14GB，显著优于 STV2 等方法。

### 5. 实验数量与充分性
*   **实验规模**：论文在超过 **15 个** 不同的数据集上进行了测试，涵盖了从底层几何、中层流估计到高层长程跟踪的所有维度。
*   **消融实验**：非常详尽。验证了 2D 监督的必要性（不加 2D 监督会导致 3D 流性能崩塌）、迭代更新的增益、以及 2D/3D 混合公式的优越性。
*   **客观性**：通过“域外测试”（Out-of-domain）验证了模型的泛化性，并对比了参数量、内存和时间，证明了其在稠密跟踪场景下的实用性（STV2 在稠密设置下会 OOM 显存溢出，而本作不会）。

### 6. 主要结论与发现
*   **性能领先**：在 3D 跟踪指标（APD）和场景流指标（EPE3D）上，Track4World 均显著超越了现有的前馈模型和基于优化的基准。
*   **2D 引导的有效性**：实验证明，将 3D 运动更新锚定在 2D 相关性上，不仅大幅降低了计算复杂度（从 $O(N^2)$ 或 $O(N \log N)$ 降至 $O(N)$），还允许模型从 2D 数据中汲取强大的运动先验。
*   **世界坐标系的优势**：通过解耦相机运动和物体运动，能够生成物理上更连贯、背景更稳定的 3D 轨迹。

### 7. 优点（亮点）
*   **高效性**：成功绕过了 3D 空间 K-NN 搜索这一计算瓶颈，使全像素 3D 跟踪成为可能。
*   **灵活性**：支持任意帧对（Arbitrary-pair）的按需查询，不限于相邻帧。
*   **多任务统一**：一个模型同时输出高质量的深度、位姿、2D 跟踪和 3D 场景流。
*   **泛化能力**：通过 2D-3D 联合训练，在真实世界复杂场景（In-the-wild）表现稳健。

### 8. 不足与局限
*   **数据依赖**：虽然引入了 2D 监督，但核心 3D 场景流的精度仍高度依赖于现有的合成 4D 数据集，这些数据集在拓扑变化（如物体破碎、融合）方面的覆盖可能不足。
*   **极端情况**：在处理极端剧烈的相机运动或物体大幅度形变时，前馈模型可能仍存在跟踪漂移。
*   **计算开销**：尽管比现有方法快，但对于移动端实时应用（如手机 AR）来说，1.38B 的参数量和数秒的推理时间依然偏重。

（完）
