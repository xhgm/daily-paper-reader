---
title: "HAMMER: Harnessing MLLM via Cross-Modal Integration for Intention-Driven 3D Affordance Grounding"
title_zh: HAMMER：通过跨模态集成利用 MLLM 进行意图驱动的 3D 可操作性定位
authors: "Lei Yao, Yong Chen, Yuejiao Su, Yi Wang, Moyun Liu, Lap-Pui Chau"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02329v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 利用多模态大模型进行意图驱动的三维可供性定位
tldr: 本研究提出HAMMER框架，旨在利用多模态大语言模型（MLLM）解决意图驱动的3D物体可操作性（Affordance）定位问题。受人类通过观察交互学习知识的启发，该方法将图像中的交互意图聚合为接触感知嵌入，并结合分层跨模态集成机制和多粒度几何提升模块，实现了对3D物体语义和空间特征的深度挖掘。实验证明，HAMMER在公开数据集及鲁棒性基准上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有方法往往依赖显式属性描述或2D分割器，难以充分利用交互意图和多模态大模型的语义理解能力来精确识别3D物体的可操作区域。
method: 通过聚合交互意图嵌入、分层跨模态集成以及多粒度几何提升模块，将MLLM的语义知识与3D空间特征进行深度融合与细化。
result: 在公开数据集和新构建的受损数据基准测试中，HAMMER在定位准确性和鲁棒性方面均显著超越了现有的先进方法。
conclusion: HAMMER证明了结合MLLM的交互意图理解能有效提升3D可操作性定位的性能，为具身智能中的物体理解提供了新的技术路径。
---

## 摘要
人类通常通过观察图像或视频中的交互来识别 3D 物体的可操作性（affordance），且这种知识一旦形成，便可泛化至新物体。受此启发，我们提出了一种名为 HAMMER 的新型框架，利用新兴的多模态大语言模型（MLLMs）进行交互意图驱动的 3D 可操作性定位。我们没有生成显式的物体属性描述或依赖现成的 2D 分割器，而是将图像中描绘的交互意图聚合为接触感知的嵌入，并引导模型推断文本形式的可操作性标签，以确保其充分挖掘物体语义和上下文线索。此外，我们设计了一种分层跨模态集成机制，以充分利用来自 MLLM 的互补信息进行 3D 表示细化，并引入了一个多粒度几何提升模块，将空间特征注入提取的意图嵌入中，从而实现准确的 3D 可操作性定位。在公共数据集和我们新构建的受损基准测试上的大量实验证明了 HAMMER 相比于现有方法的优越性和鲁棒性。所有代码和权重均已公开。

## Abstract
Humans commonly identify 3D object affordance through observed interactions in images or videos, and once formed, such knowledge can be generically generalized to novel objects. Inspired by this principle, we advocate for a novel framework that leverages emerging multimodal large language models (MLLMs) for interaction intention-driven 3D affordance grounding, namely HAMMER. Instead of generating explicit object attribute descriptions or relying on off-the-shelf 2D segmenters, we alternatively aggregate the interaction intention depicted in the image into a contact-aware embedding and guide the model to infer textual affordance labels, ensuring it thoroughly excavates object semantics and contextual cues. We further devise a hierarchical cross-modal integration mechanism to fully exploit the complementary information from the MLLM for 3D representation refinement and introduce a multi-granular geometry lifting module that infuses spatial characteristics into the extracted intention embedding, thus facilitating accurate 3D affordance localization. Extensive experiments on public datasets and our newly constructed corrupted benchmark demonstrate the superiority and robustness of HAMMER compared to existing approaches. All code and weights are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《HAMMER: Harnessing MLLM via Cross-Modal Integration for Intention-Driven 3D Affordance Grounding》的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：人类能够通过观察他人的交互行为（如看一眼别人如何用杯子）来理解物体的“可操作性”（Affordance）。在计算机视觉中，**意图驱动的 3D 可操作性定位**旨在根据一张展示交互意图的 2D 图像，预测 3D 点云上对应的可操作区域。
*   **核心挑战**：现有的方法要么依赖繁琐的文本描述生成（如 GREAT），要么依赖 2D 到 3D 的投影（如 InteractVLM），这往往会导致细节丢失、误差累积，且未能充分挖掘多模态大模型（MLLM）深层的语义理解能力。
*   **研究目标**：提出一个名为 **HAMMER** 的框架，直接利用 MLLM 的内部状态来增强 3D 表示，实现更精准、鲁棒的定位。

### 2. 方法论
HAMMER 的核心思想是利用 MLLM 提取“接触感知意图嵌入”，并通过跨模态集成和几何提升技术将其与 3D 空间特征深度融合。
*   **意图嵌入提取（Affordance-Guided Intention Embedding）**：
    *   使用预训练的 MLLM（如 Qwen2.5-VL）处理图像。
    *   引入特殊 Token `[CONT]` 来聚合交互相关信息。
    *   设计辅助任务：引导模型预测文本形式的可操作性标签（如“抓取”、“按压”），以强化模型对语义的感知。
*   **分层跨模态集成（Hierarchical Cross-Modal Integration）**：
    *   **第一阶段（瓶颈融合）**：将 MLLM 的隐藏层状态作为 Key/Value，点云编码器的特征作为 Query，通过交叉注意力机制让点云吸收交互背景知识。
    *   **第二阶段（全局-局部细化）**：利用门控机制提取 MLLM 的全局描述符，并将其注入到点云解码器的特征中，实现语义对齐。
*   **多粒度几何提升（Multi-granular Geometry Lifting）**：
    *   针对 2D 嵌入缺乏空间信息的问题，设计了一个模块，将点云 backbone 提取的多尺度几何特征逐步注入到 `[CONT]` 嵌入中，使其具备 3D 空间感知能力。
*   **解码预测**：最后通过点对意图的注意力层（Point-to-intention attention）输出最终的 3D 可操作性概率图。

### 3. 实验设计
*   **数据集**：
    *   **PIAD**：包含 7k+ 点云，23 类物体，17 种可操作性。
    *   **PIADv2**：扩展至 38.8k 实例，43 类物体，24 种可操作性。
    *   **Corrupted Benchmark**：作者基于 PIAD 构建的新基准，引入了抖动、旋转、随机丢弃等 7 种噪声，用于测试鲁棒性。
*   **对比方法**：包括意图驱动方法（IAGNet, GREAT, InteractVLM 等）和语言驱动方法（LASO, GEAL 等）。
*   **评价指标**：aIOU（平均交并比）、AUC（曲线下面积）、SIM（相似度）、MAE（平均绝对误差）。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   采用混合精度（BF16）训练，点云 backbone 保持全精度。
    *   使用 **LoRA** 技术微调 MLLM 的语言部分（Rank=16），冻结视觉部分。
    *   训练周期为 30 个 Epoch，Batch Size 为 64。
*   **模型配置**：MLLM 选用 Qwen2.5-VL-3B，3D Backbone 选用 PointNet++。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括在两个大型数据集上的全量对比、在受损基准上的鲁棒性测试。
*   **消融实验**：针对意图嵌入策略、分层集成机制、几何提升模块、不同 MLLM 底座（LISA, Qwen 等）、不同 3D Backbone（PointNet++, Uni3D）以及微调策略（LoRA vs. Freeze）均做了独立验证。
*   **客观性**：实验涵盖了“已见（Seen）”、“未见物体（Unseen Object）”和“未见操作（Unseen Affordance）”等多种划分，证明了模型的泛化能力。可视化分析（PCA 特征分布、预测图对比）进一步增强了结果的说服力。

### 6. 主要结论与发现
*   **性能领先**：HAMMER 在所有指标上均显著优于现有 SOTA。在 PIAD 未见划分中，aIOU 提升了 **5.39%**。
*   **泛化性强**：在处理从未见过的物体类别时，HAMMER 表现出极强的适应性，证明了 MLLM 知识迁移的有效性。
*   **鲁棒性极佳**：在点云数据受损（如局部缺失或噪声）的情况下，HAMMER 的性能下降远小于竞争对手。
*   **关键组件有效性**：消融实验证明，分层集成和几何提升缺一不可，前者提供语义，后者提供空间定位精度。

### 7. 优点
*   **架构创新**：摒弃了传统的“生成文本再融合”或“2D 投影”模式，直接在特征空间进行深度的跨模态集成。
*   **多粒度提升**：通过多尺度几何特征注入，巧妙解决了 2D 意图嵌入在 3D 空间定位不准的问题。
*   **鲁棒性贡献**：构建并开源了受损数据基准，为该领域的稳健性研究提供了参考。

### 8. 不足与局限
*   **复杂场景限制**：在处理极其模糊或复杂的交互意图时（如书包拉链的细微操作），模型仍可能出现定位不全或误触。
*   **计算开销**：虽然使用了 3B 规模的模型，但相比轻量级非大模型方法，推理时的显存占用和延迟仍较高。
*   **应用范围**：目前主要集中在单个物体的定位，尚未完全扩展到复杂的室内全场景（Scene-level）交互理解。

（完）
