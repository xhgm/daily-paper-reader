---
title: "3D-RFT: Reinforcement Fine-Tuning for Video-based 3D Scene Understanding"
title_zh: 3D-RFT：基于视频的 3D 场景理解的强化微调
authors: "Xiongkun Linghu, Jiangyong Huang, Baoxiong Jia, Siyuan Huang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04976v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 用于3D场景理解的强化微调
tldr: 本研究针对视频 3D 场景理解中监督微调（SFT）训练目标与任务性能不匹配的问题，提出了首个基于可验证奖励强化学习（RLVR）的框架 3D-RFT。该框架首先通过 SFT 激活多模态大模型，随后采用 GRPO 算法，利用 3D IoU 和 F1 分数等直接评估指标作为奖励函数进行微调。实验表明，3D-RFT-4B 在多项 3D 感知与推理任务中刷新了 SOTA 记录，甚至超越了 8B 规模的模型，为 3D 场景理解提供了高效的新范式。
selection_source: fresh_fetch
motivation: 解决现有 3D 场景理解模型在监督微调中因交叉熵损失与实际评估指标不一致而导致的性能瓶颈。
method: 提出 3D-RFT 框架，通过 SFT 激活模型后，利用 GRPO 算法结合 3D IoU 和 F1 分数等可验证奖励函数进行强化学习微调。
result: 3D-RFT-4B 在 3D 视频检测、视觉定位和空间推理任务上达到 SOTA，性能显著超越了 8B 规模的更大模型。
conclusion: 3D-RFT 证明了直接优化评估指标在 3D 场景理解中的有效性，为该领域提供了一种稳健且高效的强化学习训练范式。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLMs）推理能力的变革性范式，但其在 3D 场景理解中的潜力仍有待探索。现有方法主要依赖于监督微调（SFT），其中 token 级的交叉熵损失作为优化的间接代理，导致训练目标与任务性能之间存在不一致。为了弥补这一差距，我们提出了基于视频的 3D 场景理解强化微调（3D-RFT），这是首个将 RLVR 扩展到基于视频的 3D 感知和推理的框架。3D-RFT 通过直接针对评估指标优化模型来转变范式。3D-RFT 首先通过 SFT 激活 3D 感知多模态大语言模型（MLLMs），随后使用具有严格可验证奖励函数的群体相对策略优化（GRPO）进行强化微调。我们直接根据 3D IoU 和 F1-Score 等指标设计了特定任务的奖励函数，以提供更有效的信号来指导模型训练。广泛的实验表明，3D-RFT-4B 在各种基于视频的 3D 场景理解任务上实现了最先进的性能。值得注意的是，3D-RFT-4B 在 3D 视频检测、3D 视觉定位和空间推理基准测试中显著优于更大的模型（例如 VG LLM-8B）。我们进一步揭示了 3D-RFT 的良好特性，如鲁棒的有效性，以及关于训练策略和数据影响的有价值见解。我们希望 3D-RFT 能够为 3D 场景理解的未来发展提供一个稳健且有前景的范式。

## Abstract
Reinforcement Learning with Verifiable Rewards ( RLVR ) has emerged as a transformative paradigm for enhancing the reasoning capabilities of Large Language Models ( LLMs), yet its potential in 3D scene understanding remains under-explored. Existing approaches largely rely on Supervised Fine-Tuning ( SFT), where the token-level cross-entropy loss acts as an indirect proxy for optimization, leading to a misalignment between training objectives and task performances. To bridge this gap, we present Reinforcement Fine-Tuning for Video-based 3D Scene Understanding (3D-RFT ), the first framework to extend RLVR to video-based 3D perception and reasoning. 3D-RFT shifts the paradigm by directly optimizing the model towards evaluation metrics. 3D-RFT first activates 3D-aware Multi-modal Large Language Models ( MLLM s) via SFT, followed by reinforcement fine-tuning using Group Relative Policy Optimization ( GRPO) with strictly verifiable reward functions. We design task-specific reward functions directly from metrics like 3D IoU and F1-Score to provide more effective signals to guide model training. Extensive experiments demonstrate that 3D-RFT-4B achieves state-of-the-art performance on various video-based 3D scene understanding tasks. Notably, 3D-RFT-4B significantly outperforms larger models (e.g., VG LLM-8B) on 3D video detection, 3D visual grounding, and spatial reasoning benchmarks. We further reveal good properties of 3D-RFT such as robust efficacy, and valuable insights into training strategies and data impact. We hope 3D-RFT can serve as a robust and promising paradigm for future development of 3D scene understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《3D-RFT: Reinforcement Fine-Tuning for Video-based 3D Scene Understanding》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视频 3D 场景理解模型主要依赖**监督微调（SFT）**。SFT 使用 token 级的交叉熵损失作为优化目标，但这只是评估指标（如 3D IoU、F1 分数）的间接代理。在 3D 任务中，模型输出的是表示坐标的文本浮点数，微小的 token 差异可能导致巨大的几何偏差，导致训练目标与实际任务性能之间存在严重失配。
*   **研究动机**：借鉴大语言模型（如 DeepSeek-R1）在推理任务中利用**可验证奖励强化学习（RLVR）**的成功经验，探索如何将其扩展到 3D 感知和空间推理领域，通过直接优化评估指标来突破 SFT 的性能瓶颈。

### 2. 方法论
3D-RFT 提出了一个两阶段训练框架，将学习范式从“序列模仿”转变为“指标驱动的策略优化”：
*   **核心思想**：利用可验证的几何和逻辑指标作为奖励信号，指导模型进行自我演化。
*   **关键技术细节**：
    *   **第一阶段：SFT 预热**：在基础多模态大模型（如 Qwen2.5-VL）上进行初步微调，使其具备基本的 3D 感知能力和遵循 `<think>`、`<answer>` 结构化输出格式的能力。
    *   **第二阶段：RL 强化微调**：采用 **GRPO（群体相对策略优化）** 算法。该算法通过采样一组输出并根据相对奖励计算优势函数，无需额外的 Critic 网络，节省内存。
    *   **可验证奖励设计**：
        *   **3D 检测**：结合 3D IoU 奖励（几何精度）和 F1-Score 奖励（查全/查准平衡）。
        *   **3D 视觉定位**：包含时间奖励（帧索引回归）和全局坐标系下的 3D IoU 奖励。
        *   **空间推理**：针对选择题使用准确率奖励，针对数值计算（如计数、距离）使用平均相对准确率（MRA）。
    *   **显存优化**：针对长视频输入的显存压力，引入了**损失反向分块（Loss Backward Chunking）**技术，支持在大组尺寸（Group Size）下进行训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **3D 感知**：使用 ScanNetDetection（检测）和 ScanRefer（视觉定位）。
    *   **空间推理**：使用 VSI-Bench（包含物体计数、距离、大小、方位等 9 类推理任务）。
*   **Benchmark 与对比方法**：
    *   对比了 SFT 基准模型（如 VG LLM-4B/8B）。
    *   对比了闭源模型（GPT-4o, Gemini-2.5 Pro）和开源 SOTA 模型（LLaVA-Video, InternVL3 等）。
    *   在推理任务中对比了其他强化学习尝试（如 vsGRPO, VST-RL）。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 台 Nvidia A100 GPU** 上完成。
*   **训练细节**：
    *   感知任务采用全参数微调。
    *   推理任务采用 LoRA 微调。
    *   通过损失分块技术，将微批次（micro-chunk）大小设为 1-4，以适应显存限制。
*   **时长**：文中未明确给出总训练小时数，但记录了推理任务在 4000 步左右达到饱和。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3D 场景理解的三大核心任务（检测、定位、推理），并针对不同视频帧数（4 帧、6 帧、32 帧）进行了测试。
*   **消融实验**：
    *   验证了 3D 几何先验（VGGT 骨干网络）对 RL 效果的影响。
    *   分析了不同质量的思维链（CoT）数据对强化学习起点的作用。
    *   对比了 DA（直接回答）与 TA（思考后回答）两种模式的性能差异。
*   **评价**：实验设计较为全面，通过跨规模（4B vs 8B）对比证明了算法效率，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：3D-RFT-4B 在感知任务上显著超越了 SFT 训练的 8B 模型（如在检测任务中 F1 提升约 5.5%）。
*   **推理增强**：在 VSI-Bench 空间推理基准上刷新了记录，证明 RL 能有效减少空间逻辑错误。
*   **数据洞察**：高质量的 CoT 数据是 RL 成功的基石，仅靠直接回答（DA）数据容易导致模型在分布外（OOD）场景下过拟合。
*   **训练动态**：感知任务中，模型会先优化几何紧凑度（IoU），再优化检测全率（F1）。

### 7. 优点与亮点
*   **范式创新**：首次将 RLVR 引入视频 3D 场景理解，解决了 token 损失与几何度量之间的不一致问题。
*   **高效性**：证明了较小规模的模型（4B）通过强化学习可以获得超越大规模模型（8B）的感知精度。
*   **工程优化**：提出的损失分块技术解决了长视频 RL 训练的显存瓶颈，具有实用价值。

### 8. 不足与局限
*   **任务孤立**：目前仍是针对特定任务进行微调，尚未实现一个统一的、多任务并行的 3D 强化学习模型。
*   **感知 CoT 缺失**：由于 3D 感知任务（如检测坐标）的推理过程难以标注，目前感知任务尚未引入思维链（CoT），限制了其复杂推理潜力。
*   **数据依赖**：RL 的效果高度依赖于初始 SFT 阶段提供的策略质量和奖励函数的设计，对于缺乏明确评估指标的模糊任务难以适用。

（完）
