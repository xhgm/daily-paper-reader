---
title: Observing and Controlling Features in Vision-Language-Action Models
title_zh: 观察与控制视觉-语言-动作模型中的特征
authors: "Hugo Buurmeijer, Carmen Amo Alonso, Aiden Swann, Marco Pavone"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05487v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的机理性可解释性
tldr: 本研究探讨了视觉-语言-动作模型（VLA）的内部表征与输出行为之间的关系。针对VLA多模态及混合架构带来的复杂性，作者提出了特征可观测性和可控制性概念。通过线性分类器识别表征空间中的特征，并利用基于最优控制的轻量级线性干预手段，在无需微调的情况下实现了对机器人行为的实时引导。实验证明该方法在多种VLA架构上能有效对齐用户偏好并保持闭环控制能力。
selection_source: fresh_fetch
motivation: 旨在解决大语言模型的机械可解释性研究难以直接应用于结构更复杂的视觉-语言-动作模型（VLA）的问题。
method: 引入特征可观测性与可控制性概念，利用线性分类器识别特征，并通过基于最优控制的最小线性干预来引导模型内部表征。
result: 在多种VLA架构上的仿真实验表明，轻量级干预能可靠地转向机器人行为并保留闭环操作能力。
conclusion: VLA模型具有可解释的内部结构，支持无需微调的在线适配，从而实现与用户需求和任务目标的实时对齐。
---

## 摘要
视觉-语言-动作模型 (VLAs) 在具身智能方面取得了显著进展。虽然它们的架构在一定程度上类似于大语言模型 (LLMs)，但由于其多模态输入/输出以及通常由 Transformer 和扩散头组成的混合性质，VLAs 表现出更高的复杂性。这也是为什么 LLMs 中机械可解释性（解释内部模型表示如何与其输出行为相关联）的见解无法简单地迁移到 VLA 对应模型的部分原因。在这项工作中，我们建议通过引入和分析两个主要概念来弥补这一差距：特征可观测性 (feature-observability) 和特征可控性 (feature-controllability)。具体而言，我们首先研究了在表示空间中线性编码的特征，并展示了如何通过线性分类器来观察它们。然后，我们使用一种基于最优控制的极小线性干预，来精确放置内部表示，并将 VLA 的输出引导至目标区域。我们的结果表明，有针对性的轻量级干预可以可靠地引导机器人的行为，同时保持闭环能力。我们通过仿真实验在不同的 VLA 架构（$π_{0.5}$ 和 OpenVLA）上证明，VLAs 具有可解释的内部结构，适用于无需微调的在线自适应，从而实现与用户偏好和任务要求的实时对齐。

## Abstract
Vision-Language-Action Models (VLAs) have shown remarkable progress towards embodied intelligence. While their architecture partially resembles that of Large Language Models (LLMs), VLAs exhibit higher complexity due to their multi-modal inputs/outputs and often hybrid nature of transformer and diffusion heads. This is part of the reason why insights from mechanistic interpretability in LLMs, which explain how the internal model representations relate to their output behavior, do not trivially transfer to VLA counterparts. In this work, we propose to close this gap by introducing and analyzing two main concepts: feature-observability and feature-controllability. In particular, we first study features that are linearly encoded in representation space, and show how they can be observed by means of a linear classifier. Then, we use a minimal linear intervention grounded in optimal control to accurately place internal representations and steer the VLA's output towards a desired region. Our results show that targeted, lightweight interventions can reliably steer a robot's behavior while preserving closed-loop capabilities. We demonstrate on different VLA architectures ($π_{0.5}$ and OpenVLA) through simulation experiments that VLAs possess interpretable internal structure amenable to online adaptation without fine-tuning, enabling real-time alignment with user preferences and task requirements.

---

## 论文详细总结（自动生成）

这是一份关于论文《Observing and Controlling Features in Vision-Language-Action Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究动机**：视觉-语言-动作模型（VLA）在具身智能领域表现卓越，但其内部决策过程如同“黑盒”，行为有时不可预测且难以实时修正。
*   **核心问题**：虽然大语言模型（LLM）的“机械可解释性”研究已能通过干预内部表征来引导输出，但 VLA 涉及多模态输入、连续动作输出及闭环物理交互，复杂度更高。
*   **整体含义**：本文旨在探索 VLA 内部表征与机器人行为特征之间的线性关系，并提出一种无需微调、轻量级的在线干预框架，实现对机器人行为（如速度、高度、夹爪状态）的精确控制。

### 2. 论文提出的方法论
核心思想是基于“线性表示假设”，即模型的高层语义和行为特征在线性子空间中编码。
*   **特征可观测性（Feature-Observability）**：
    *   **线性观测器**：设计一个线性回归/分类器 $f(x) = Wx + b$，从 Transformer 层的隐藏状态 $x$ 中提取机器人状态或动作特征 $\zeta$。
    *   **训练流程**：通过离线收集“表征-特征”对，利用交叉熵或均方误差损失函数训练观测器权重。
*   **特征可控制性（Feature-Controllability）**：
    *   **线性控制器**：通过向原始表征添加一个最小扰动 $u$（即 $\tilde{x} = x + u$），使得修改后的表征在观测器空间中落在目标区域 $D$ 内。
    *   **最优控制公式**：将干预建模为一个受限优化问题，目标是最小化扰动的 $L_2$ 范数，从而在改变特定行为的同时，最大限度地保留模型原始的生成能力和自然性。
*   **在线算法**：在推理过程中，算法实时监测特定层的表征，若观测值偏离目标范围，则应用闭式解形式的线性干预。

### 3. 实验设计
*   **实验模型**：选取了两种代表性架构：**OpenVLA**（基于 Llama 的自回归架构）和 **$\pi_{0.5}$**（Transformer 与流匹配混合架构）。
*   **数据集与场景**：
    *   **Libero**：用于空间任务套件的仿真评估。
    *   **BridgeData V2**：用于跨任务的表征分析。
*   **Benchmark 与对比方法**：
    *   **No Intervention**：原始模型输出。
    *   **Prompting**：通过改变输入提示词（如“打开夹爪”）来引导行为。
    *   **对比维度**：约束满足率（Constraint Satisfaction Ratio）与任务成功率（Success Rate）。

### 4. 资源与算力
*   **推理算力**：文中明确提到所有推理实验均在**单张 NVIDIA 5090 GPU** 上完成。
*   **训练时长**：文中未详细说明观测器训练的具体时长，但强调了该方法是“轻量级”的，且观测器和控制器的计算开销在推理时几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Libero 仿真器的 10 个空间任务上进行了评估，每个任务进行 10 次 Rollout（共计百余次闭环实验）。
    *   覆盖了夹爪状态（开/关）、末端执行器高度（高/低）、移动速度（快/慢）三种核心特征。
*   **充分性与客观性**：实验涵盖了当前最前沿的两种 VLA 架构，并对比了提示词引导等常规方法。通过“小提琴图”展示了分布情况，并利用标准差椭圆分析了成功率与约束满足率的权衡，实验设计较为客观、全面。

### 6. 论文的主要结论与发现
*   **线性编码存在**：机器人的动作和状态特征在 VLA 的 Transformer 层中确实呈线性编码，且可以通过简单的线性探测器高精度提取。
*   **层级差异**：干预在较早的层通常更有效，随着深度增加，表征的范数增大，固定强度的扰动效果会减弱。
*   **闭环有效性**：尽管 VLA 在闭环环境中运行（动作会改变下一步的输入），但针对单次前向传播设计的线性干预依然能稳定引导长期行为。
*   **性能保持**：相比于简单的提示词引导，本文的方法在实现极高约束满足率的同时，能更好地保持任务的成功率。

### 7. 优点
*   **非侵入性**：无需对庞大的 VLA 进行微调或重新训练，部署成本极低。
*   **实时性**：线性闭式解计算极快，满足机器人实时控制的高频率要求。
*   **自然性**：通过最小扰动原则，避免了暴力修改表征导致的模型崩溃，保留了模型的泛化能力。

### 8. 不足与局限
*   **依赖标注数据**：训练观测器仍需要带标签的数据集，对于缺乏标注的大规模机器人数据可能受限。
*   **特征局限**：目前主要集中在低级物理特征（位置、速度），对于高级语义特征（如物体亲和力、复杂逻辑关系）的可控性尚未深入探讨。
*   **架构覆盖**：主要针对 Transformer 模块，对于混合架构中的扩散头（Diffusion Heads）或流匹配模块的直接干预研究较少。
*   **安全边界**：虽然能引导行为，但尚未建立严谨的数学证明来保证干预后的行为绝对符合安全边界。

（完）
