---
title: "SFCo-Nav: Efficient Zero-Shot Visual Language Navigation via Collaboration of Slow LLM and Fast Attributed Graph Alignment"
title_zh: SFCo-Nav：通过慢速 LLM 与快速属性图对齐协作实现的高效零样本视觉语言导航
authors: "Chaoran Xiong, Litao Wei, Xinhao Hu, Kehui Ma, Ziyi Xia, Zixin Jiang, Zhen Sun, Ling Pei"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01477v1"
tags: ["query:q20"]
score: 9.0
evidence: 使用VLM和LLM的零样本视觉语言导航
tldr: SFCo-Nav 是一种高效的零样本视觉语言导航框架，旨在解决现有方法因频繁调用大模型导致的延迟和高成本问题。它受人类认知中的快慢系统启发，结合了负责战略规划的慢速 LLM 规划器和负责实时执行的快速反应式导航器。通过异步触发机制，该系统仅在必要时调用 LLM，显著提升了导航效率和实时性，并在 R2R 和 REVERIE 基准测试及真机实验中验证了其优越性。
selection_source: fresh_fetch
motivation: 现有的零样本视觉语言导航方法通常需要每步进行大模型推理，导致计算成本高且难以满足实时部署需求。
method: 提出一种快慢协作框架，利用慢速 LLM 生成子目标及想象图，配合快速导航器进行实时图构建，并通过异步桥接机制根据置信度触发 LLM。
result: "在 R2R 和 REVERIE 基准上达到或超过了现有最先进水平，同时减少了 50% 以上的 Token 消耗，运行速度提升了 3.5 倍以上。"
conclusion: SFCo-Nav 证明了通过快慢认知协作机制可以有效平衡零样本导航的智能性与效率，为实时机器人导航提供了实用方案。
---

## 摘要
最近，大型视觉语言模型（VLM）和大型语言模型（LLM）的进展使得零样本视觉语言导航（VLN）成为可能，智能体仅依靠自身感知和推理即可遵循自然语言指令。然而，现有的零样本方法通常构建简单的观测图，并对其进行逐步的 VLM-LLM 推理，导致高延迟和高计算成本，限制了实时部署。为解决这一问题，我们提出了 SFCo-Nav，这是一个受快慢认知协作原理启发的高效零样本 VLN 框架。SFCo-Nav 集成了三个关键模块：1）基于慢速 LLM 的规划器，用于生成策略性的子目标链，每个子目标都与一个想象的对象图相关联；2）快速反应式导航器，用于实时构建对象图并执行子目标；3）轻量级异步快慢桥接器，通过对齐高级结构化的属性想象图与感知图来估计导航置信度，仅在必要时触发慢速 LLM 规划器。据我们所知，SFCo-Nav 是首个支持根据内部置信度异步触发 LLM 的快慢协作零样本 VLN 系统。在公开的 R2R 和 REVERIE 基准测试上的评估结果显示，SFCo-Nav 在达到或超过现有最先进零样本 VLN 成功率的同时，将每条轨迹的总 Token 消耗降低了 50% 以上，运行速度提升了 3.5 倍以上。最后，我们在酒店套房中使用四足机器人演示了 SFCo-Nav，展示了其在室内环境中的高效性和实用性。

## Abstract
Recent advances in large vision-language models (VLMs) and large language models (LLMs) have enabled zero-shot approaches to visual language navigation (VLN), where an agent follows natural language instructions using only ego perception and reasoning. However, existing zero-shot methods typically construct a naive observation graph and perform per-step VLM-LLM inference on it, resulting in high latency and computation costs that limit real-time deployment. To address this, we present SFCo-Nav, an efficient zero-shot VLN framework inspired by the principle of slow-fast cognitive collaboration. SFCo-Nav integrates three key modules: 1) a slow LLM-based planner that produces a strategic chain of subgoals, each linked to an imagined object graph; 2) a fast reactive navigator for real-time object graph construction and subgoal execution; and 3) a lightweight asynchronous slow-fast bridge aligns advanced structured, attributed imagined and perceived graphs to estimate navigation confidence, triggering the slow LLM planner only when necessary. To the best of our knowledge, SFCo-Nav is the first slow-fast collaboration zero-shot VLN system supporting asynchronous LLM triggering according to the internal confidence. Evaluated on the public R2R and REVERIE benchmarks, SFCo-Nav matches or exceeds prior state-of-the-art zero-shot VLN success rates while cutting total token consumption per trajectory by over 50% and running more than 3.5 times faster. Finally, we demonstrate SFCo-Nav on a legged robot in a hotel suite, showcasing its efficiency and practicality in indoor environments.

---

## 论文详细总结（自动生成）

以下是对论文《SFCo-Nav: Efficient Zero-Shot Visual Language Navigation via Collaboration of Slow LLM and Fast Attributed Graph Alignment》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：零样本（Zero-shot）视觉语言导航（VLN）利用预训练的大型语言模型（LLM）和视觉语言模型（VLM）推理能力，无需任务特定训练即可部署。
*   **核心痛点**：现有方法通常采用“全慢脑”策略，即在导航的每一步都调用昂贵的 VLM/LLM 进行全景感知和推理。这导致了极高的 Token 消耗、高延迟以及无法满足实时机器人部署的需求。
*   **研究动机**：受人类认知中“快慢系统”协作的启发，旨在建立一种高效框架，让机器人平时依靠快速反应导航，仅在迷茫或置信度低时才求助于慢速的 LLM 规划。

### 2. 方法论
SFCo-Nav 框架由三个核心模块组成：
*   **慢速 LLM 规划器 (Πslow)**：
    *   **功能**：将自然语言指令分解为子目标链（目标对象 + 导航技能）。
    *   **关键创新**：生成“想象对象图”（Imagined Object Graph），作为对未来场景的语义和空间先验。
*   **快速反应式导航器 (πfast)**：
    *   **功能**：实时构建“感知对象图”（Perceived Object Graph），并执行低级动作。
    *   **技术细节**：使用轻量级目标检测（如 Grounding-DINOv2）获取物体位置和类别，构建星形拓扑图，避免了昂贵的 VLM 图像转文字过程。
*   **异步快慢桥接器 (Slow-Fast Bridge)**：
    *   **核心机制**：基于**属性图对齐（Attributed Graph Alignment）**理论计算置信度 $C_t$。
    *   **流程**：通过比较“感知图”与“想象图”的结构和属性相似性，计算对齐概率。若置信度高于阈值，则由快速导航器继续执行；若低于阈值，则触发慢速 LLM 重新规划。

### 3. 实验设计
*   **数据集与场景**：
    *   **R2R (Room-to-Room)**：侧重于遵循详细路径指令。
    *   **REVERIE**：侧重于高层级指令下的远端物体定位。
    *   **真实世界套房**：在酒店环境中使用四足机器人进行实机部署测试。
*   **对比方法 (Benchmarks)**：
    *   **NavGPT**：基于 GPT-4 的纯文本推理导航。
    *   **MapGPT**：基于地图引导的提示词方法。
    *   **NavCoT**：基于思维链（CoT）和微调 Llama 的方法。
*   **评估指标**：成功率 (SR)、路径长度加权成功率 (SPL)、导航误差 (NE)、Token 消耗量、推理耗时。

### 4. 资源与算力
*   **模型后端**：使用了 GPT-4o 作为主要的 LLM 规划器，消融实验中对比了 DeepSeek-V3.1。
*   **视觉处理**：使用了 Grounding-DINOv2 进行目标检测。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号、数量或总训练时长（由于是零样本方法，主要涉及推理开销）。文中重点给出了推理阶段的平均 Token 消耗和每条轨迹的平均墙钟时间（Wall-clock time）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 VLN 基准上进行了完整测试，并针对 R2R 的 300 条指令子集进行了深入的消融实验。
*   **充分性评价**：
    *   **多维度验证**：涵盖了性能指标、Token 效率、时间效率三个维度。
    *   **消融实验**：详细探讨了置信度阈值、不同 LLM 后端、以及“想象链”模块对结果的影响。
    *   **实机部署**：通过四足机器人的真实实验证明了系统的实用性。
    *   **客观性**：对比了当前最先进的零样本方法，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **效率飞跃**：SFCo-Nav 在 R2R 和 REVERIE 上达到了与 SOTA 相当或更高的成功率，但 **Token 消耗降低了 50% 以上**，**运行速度提升了 3.5 倍以上**。
*   **协作有效性**：异步触发机制能显著减少不必要的 LLM 调用，在环境信息匹配良好时，快速导航器表现可靠。
*   **模块贡献**：对象-技能对（Object-Skill Pair）显著提升了导航精度，而想象图链（Imagined Chain）是降低 Token 消耗的关键。

### 7. 优点
*   **极高的实时性**：通过减少 VLM/LLM 的调用频率，解决了零样本导航在机器人上部署的延迟问题。
*   **成本低廉**：大幅减少了 API 调用产生的 Token 费用。
*   **架构优雅**：将复杂的图对齐理论转化为实用的置信度触发机制，实现了认知科学理论在机器人领域的落地。
*   **兼容性强**：实验证明该框架可适配 GPT-4o 和 DeepSeek 等不同模型。

### 8. 不足与局限
*   **阈值依赖**：置信度阈值 $\tau_C$ 需要手动设定，不同环境下的最优阈值可能不同，缺乏自适应调节机制。
*   **感知精度风险**：系统高度依赖底层目标检测器的准确性，若检测器漏检关键地标，可能导致错误的图对齐和导航失败。
*   **环境复杂度限制**：虽然在室内环境表现优异，但在物体极度密集或动态变化剧烈的场景下，图对齐的鲁棒性仍有待进一步验证。

（完）
