---
title: Structural Action Transformer for 3D Dexterous Manipulation
title_zh: 用于三维灵巧操作的结构化动作 Transformer
authors: "Xiaohan Lei, Min Wang, Bohong Weng, Wengang Zhou, Houqiang Li"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03960v1"
tags: ["query:q9"]
score: 9.0
evidence: 使用结构化动作表示的三维灵巧操作策略
tldr: 针对高自由度机器人手在跨构型技能迁移中的挑战，本文提出结构化动作 Transformer (SAT)。该方法摒弃了传统的时间中心化动作表示，将动作块重构为无序的关节轨迹序列，利用 Transformer 灵活处理变长构型。通过引入具身关节码本编码运动学先验，并结合 3D 点云与流匹配目标，SAT 在异构数据集预训练及多任务微调中表现优异，显著提升了采样效率和跨构型迁移能力。
selection_source: fresh_fetch
motivation: 现有的 2D 观测和时间中心化动作表示难以捕捉 3D 空间关系，且无法有效处理不同机器人构型间的异构性。
method: 将动作建模为变长的关节轨迹序列，并引入具身关节码本编码功能角色，利用流匹配从 3D 点云生成动作。
result: 在多任务仿真和真实世界实验中，该方法在性能和采样效率上均显著优于现有基准，证明了有效的跨构型技能迁移。
conclusion: 这种以结构为中心的表示方法为实现高自由度、异构机器人的大规模策略扩展提供了新的有效路径。
---

## 摘要
通过异构数据集的模仿学习使机器人实现人类水平的灵巧性，受到跨具身技能迁移挑战的阻碍，特别是对于高自由度（high-DoF）机械手。现有方法通常依赖于二维观测和以时间为中心的动作表示，难以捕捉三维空间关系，且无法处理具身异构性。本文提出了结构化动作 Transformer (SAT)，这是一种新的三维灵巧操作策略，通过引入以结构为中心的视角来挑战这一范式。我们将每个动作块重新定义为关节级轨迹的可变长度、无序序列，而非时间序列。这种结构化公式使 Transformer 能够原生处理异构具身，将关节数量视为可变的序列长度。为了编码结构先验并消除歧义，我们引入了具身关节码本（Embodied Joint Codebook），用于嵌入每个关节的功能角色和运动学特性。我们的模型通过连续时间流匹配目标，学习从三维点云生成这些轨迹。我们通过在大规模异构数据集上进行预训练，并在仿真和真实世界的灵巧操作任务上进行微调，验证了我们的方法。我们的方法始终优于所有基线，展示了卓越的样本效率和有效的跨具身技能迁移。这种以结构为中心的表示为扩展高自由度、异构操作器的策略提供了一条新路径。

## Abstract
Achieving human-level dexterity in robots via imitation learning from heterogeneous datasets is hindered by the challenge of cross-embodiment skill transfer, particularly for high-DoF robotic hands. Existing methods, often relying on 2D observations and temporal-centric action representation, struggle to capture 3D spatial relations and fail to handle embodiment heterogeneity. This paper proposes the Structural Action Transformer (SAT), a new 3D dexterous manipulation policy that challenges this paradigm by introducing a structural-centric perspective. We reframe each action chunk not as a temporal sequence, but as a variable-length, unordered sequence of joint-wise trajectories. This structural formulation allows a Transformer to natively handle heterogeneous embodiments, treating the joint count as a variable sequence length. To encode structural priors and resolve ambiguity, we introduce an Embodied Joint Codebook that embeds each joint's functional role and kinematic properties. Our model learns to generate these trajectories from 3D point clouds via a continuous-time flow matching objective. We validate our approach by pre-training on large-scale heterogeneous datasets and fine-tuning on simulation and real-world dexterous manipulation tasks. Our method consistently outperforms all baselines, demonstrating superior sample efficiency and effective cross-embodiment skill transfer. This structural-centric representation offers a new path toward scaling policies for high-DoF, heterogeneous manipulators.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structural Action Transformer for 3D Dexterous Manipulation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：实现人类水平的机器人灵巧操作（Dexterous Manipulation）面临两大挑战：一是**跨构型迁移（Cross-embodiment transfer）**，不同机器人（如 ShadowHand, Allegro）的自由度（DoF）和运动学结构差异巨大；二是**动作表示的局限性**，现有方法多采用“以时间为中心”的视角，将动作块视为固定维度的向量序列，难以处理关节数量变化且忽略了关节间的结构关联。
*   **核心目标**：提出一种能够原生处理异构机器人构型、捕捉 3D 空间关系并实现高效跨具身技能迁移的通用策略模型。

### 2. 方法论
*   **核心思想**：将动作表示从“以时间为中心”转变为**“以结构为中心（Structural-centric）”**。
    *   **传统视角**：动作块维度为 $(T, D_a)$，即 $T$ 个时间步，每步 $D_a$ 维动作。
    *   **SAT 视角**：动作块维度为 $(D_a, T)$，将每个关节在整个预测窗口内的轨迹作为一个 Token。这样，关节数量 $D_a$ 变成了 Transformer 的序列长度，从而能自然处理变长构型。
*   **关键技术细节**：
    1.  **具身关节码本（Embodied Joint Codebook）**：为消除无序关节序列的歧义，为每个关节分配一个三元组标签 $(e, f, r)$——具身 ID、功能类别（如指根关节、指尖关节）和旋转轴。通过学习这些嵌入，模型能识别不同机器人间的功能对应关系。
    2.  **3D 观测分层 Tokenizer**：使用最远点采样（FPS）和 PointNet 提取局部几何 Token，并结合全局场景 Token 和 T5 编码的语言指令。
    3.  **连续时间流匹配（Flow Matching）**：采用条件归一化流（CNF）建模动作分布，通过学习条件速度场 $\epsilon_\theta$ 将高斯噪声转化为结构化动作轨迹，推理时使用 ODE 求解器（如 Euler 法）。
    4.  **结构化 Transformer**：使用带有因果掩码的 Diffusion Transformer (DiT) 处理观测 Token 和动作 Token 的混合序列。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：混合了人类演示（Ego-Exo4D, HOI4D, ADT）、机器人数据（Fourier ActionNet, DexCap）以及仿真专家数据（Adroit, DexArt, Bi-DexHands）。
    *   **微调/测试**：在 11 个仿真任务（涵盖物体抓取、工具使用、双臂协作）和 6 个真实世界双臂灵巧操作任务上进行评估。
*   **Benchmark 与对比方法**：
    *   **2D 基线**：Diffusion Policy, HPT, UniAct。
    *   **3D 基线**：3D Diffusion Policy (3DDP), 3D ManiFlow。

### 4. 资源与算力
*   **算力说明**：论文提到实验得到了中国科学技术大学（USTC）超级计算中心和 MCC 实验室 GPU 集群的支持。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长。但提到模型参数量仅为 **19.36M**，远小于 UniAct (1053M) 或 Diffusion Policy (266.8M)，具有极高的参数效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：在 3 个主流 Benchmark（Adroit, DexArt, Bi-DexHands）共 11 个任务上进行了定量对比。
    *   **消融实验**：针对时间压缩维度、预训练数据组成（人类 vs 机器人 vs 仿真）、模型组件（全局/局部点云、因果掩码）、关节码本构成等进行了 6 组以上的详细消融。
    *   **真实实验**：在双臂 xArm + xHand 平台上完成了 6 个复杂任务（如脱笔帽、传递玩具、刷杯子）。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集预训练和少样本（Few-shot）适配实验，有力证明了模型在处理异构构型时的优越性。

### 6. 主要结论与发现
*   **结构化表示的有效性**：将关节轨迹作为 Token 显著优于传统的时间步 Token，尤其是在高自由度机械手上。
*   **跨具身迁移**：即使只在人类数据上预训练，模型也能通过关节码本将功能性运动迁移到机器人上，表现优于仅在机器人数据上训练。
*   **参数效率**：SAT 以极小的参数量（约 20M）达到了甚至超过了参数量大十倍以上的 3D 基线模型的性能。
*   **少样本能力**：在只有 10-100 个演示样本的情况下，SAT 的收敛速度和最终成功率显著高于现有 SOTA 方法。

### 7. 优点
*   **范式创新**：首次成功实现了在结构维度（关节空间）进行 Token 化，解决了异构机器人动作空间不统一的痛点。
*   **轻量化与高效**：模型极其精简，推理速度快（支持 1-NFE 生成），适合实时控制。
*   **3D 空间感知**：原生支持点云输入，比 2D VLA 模型更擅长处理灵巧操作中的复杂空间关系。

### 8. 不足与局限
*   **遮挡问题**：在复杂双臂任务中，单视角点云容易产生严重遮挡，导致几何信息缺失。
*   **运动学匹配风险**：当演示数据与执行平台的运动学或接触几何差异过大时，仍可能出现动作分配错误。
*   **感知闭环**：目前主要依赖视觉，缺乏触觉（Tactile）反馈，在处理极高精度的接触密集型任务时可能受限。

（完）
