---
title: LiDAR Prompted Spatio-Temporal Multi-View Stereo for Autonomous Driving
title_zh: 自动驾驶中基于 LiDAR 提示的时空多视图立体视觉
authors: "Qihao Sun, Jiarun Liu, Ziqian Ni, Jianyun Xu, Tao Xie, Lijun Zhao, Ruifeng Li, Sheng Yang"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03765v1"
tags: ["query:q10"]
score: 9.0
evidence: 自动驾驶中的三维重建与绝对深度估计
tldr: 针对自动驾驶中深度估计难以兼顾绝对尺度精度、时空一致性和泛化性的挑战，本文提出DriveMVS框架。该方法创新性地将稀疏LiDAR数据作为几何提示，通过硬性锚定代价卷和软性特征引导来确保绝对尺度，并结合时空解码器融合多视图几何与时序上下文。实验证明，DriveMVS在多个基准测试中达到SOTA水平，具备极强的时空稳定性和零样本跨域迁移能力，为可靠的自动驾驶感知提供了新方案。
selection_source: fresh_fetch
motivation: 现有的自动驾驶深度估计方法在绝对尺度精度、多视图时空一致性以及跨场景泛化能力方面仍存在显著不足。
method: 提出DriveMVS框架，利用稀疏LiDAR作为几何提示锚定代价卷，并通过时空解码器深度融合几何线索与时序上下文。
result: 在多个主流基准测试中取得了最先进的性能，特别是在度量精度、时序稳定性和零样本跨域迁移方面表现优异。
conclusion: DriveMVS通过融合LiDAR提示与时空一致性建模，为构建可扩展且可靠的自动驾驶感知系统提供了有效的深度估计方案。
---

## 摘要
精确的度量深度对于自动驾驶的感知和仿真至关重要，然而目前的方法在实现高度量精度、多视图和时间一致性以及跨域泛化方面仍面临挑战。为了解决这些挑战，我们提出了 DriveMVS，这是一种新型的多视图立体视觉框架，通过两个关键见解来协调这些相互竞争的目标：(1) 稀疏但度量准确的 LiDAR 观测可以作为几何提示，将深度估计锚定在绝对尺度上；(2) 多种线索的深度融合对于消除歧义和增强鲁棒性至关重要，而时空解码器则确保了帧间的一致性。基于这些原则，DriveMVS 以两种方式嵌入 LiDAR 提示：作为锚定代价体（cost volume）的硬几何先验，以及由三线索组合器融合的软特征引导。关于时间一致性，DriveMVS 采用了一个时空解码器，共同利用来自 MVS 代价体的几何线索和来自相邻帧的时间上下文。实验表明，DriveMVS 在多个基准测试中达到了最先进的性能，在度量精度、时间稳定性和零样本跨域迁移方面表现出色，证明了其在可扩展、可靠的自动驾驶系统中的实用价值。

## Abstract
Accurate metric depth is critical for autonomous driving perception and simulation, yet current approaches struggle to achieve high metric accuracy, multi-view and temporal consistency, and cross-domain generalization. To address these challenges, we present DriveMVS, a novel multi-view stereo framework that reconciles these competing objectives through two key insights:   (1) Sparse but metrically accurate LiDAR observations can serve as geometric prompts to anchor depth estimation in absolute scale, and (2) deep fusion of diverse cues is essential for resolving ambiguities and enhancing robustness, while a spatio-temporal decoder ensures consistency across frames. Built upon these principles, DriveMVS embeds the LiDAR prompt in two ways: as a hard geometric prior that anchors the cost volume, and as soft feature-wise guidance fused by a triple-cue combiner. Regarding temporal consistency, DriveMVS employs a spatio-temporal decoder that jointly leverages geometric cues from the MVS cost volume and temporal context from neighboring frames. Experiments show that DriveMVS achieves state-of-the-art performance on multiple benchmarks, excelling in metric accuracy, temporal stability, and zero-shot cross-domain transfer, demonstrating its practical value for scalable, reliable autonomous driving systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《LiDAR Prompted Spatio-Temporal Multi-View Stereo for Autonomous Driving》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在自动驾驶领域，精确的**度量深度（Metric Depth）**估计是感知与仿真的基石。然而，现有技术存在“不可能三角”：
*   **单目基础模型**：泛化性强，但存在尺度歧义且时序不一致。
*   **通用MVS（多视图立体视觉）模型**：在低视差（如跟车静止）、无纹理区域易失效，且逐帧估计导致视频闪烁。
*   **前馈多视图模型**：速度快，但绝对精度较差。

**DriveMVS** 的研究动机在于：利用自动驾驶车辆上常见的**稀疏LiDAR**作为“几何提示（Prompt）”，结合时空上下文，构建一个既有绝对尺度精度、又能保持视频稳定、且具备强跨域泛化能力的深度估计框架。

### 2. 方法论：核心思想与关键技术
DriveMVS 的核心思想是**“提示锚定”**与**“时空融合”**，主要包含三个关键模块：

*   **提示锚定代价卷 (PACV, Prompt-Anchored Cost Volume)**：
    *   传统MVS代价卷仅依赖特征匹配（相对一致性）。PACV 引入了**绝对尺度锚定**，通过一个轻量级 MLP 将稀疏 LiDAR 提示与代价卷特征结合。
    *   它将学习过程解耦为：相对一致性学习（CV_rel）和绝对尺度锚定（CV_abs），防止在低视差场景下代价卷发生尺度坍缩。
*   **三线索组合器 (TCC, Triple-Cues Combiner)**：
    *   采用 Transformer 架构融合三类异构特征：**代价卷线索**（几何锚定）、**单目线索**（来自 DINOv2/DepthAnythingV2 的全局上下文）和**度量线索**（稀疏 LiDAR 特征）。
    *   使用 **Mask Transformer** 处理稀疏性，确保无效的 LiDAR 像素不会干扰特征传播，并通过跨帧提示增强时序鲁棒性。
*   **时空解码器 (Spatio-Temporal Decoder)**：
    *   在 DPT 解码器基础上引入**几何感知运动模块 (GMM)**。
    *   **关键细节**：将相机射线（Ray）的 6D 坐标（原点和方向）通过傅里叶特征映射嵌入到特征流中，使模型能显式感知帧间的几何位移，从而实现平滑的视频深度生成。
    *   **深度恢复公式**：通过 Sigmoid 输出 $x(t)$，结合代价卷设定的最小/最大深度范围进行对数空间插值，恢复绝对深度 $\hat{D}(t)$。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：全合成数据集（TartanAir, VKITTI2, TartanGround, MVS-Synth），共约 203 万张图像。使用合成数据是为了获得完美的真值标签，减少训练噪声。
    *   **测试集（零样本评估）**：直接在真实世界数据集 **KITTI、DDAD、Waymo** 上进行测试，不进行任何微调。
*   **Benchmark 与对比方法**：
    *   对比了 10 余种 SOTA 方法，涵盖前馈重建（VGGT, MapAnything）、单目深度（DepthPro, MoGe-2）、提示引导深度（PromptDA, PriorDA）以及 MVS 模型（MVSAnywhere）。
    *   **评价指标**：MAE（平均绝对误差）、AbsRel（绝对相对误差）、$\delta < 1.25$（在位率）以及 TAE（时序对齐误差）。

### 4. 资源与算力
*   **硬件**：使用 **4 张 NVIDIA A100 GPU**。
*   **训练参数**：Batch size 为 6，分辨率 640x480。
*   **时长**：训练 240k 步，耗时约 **1 天**。
*   **推理效率**：在 A100 上单帧推理耗时约 **65.61ms**，显存占用约 4.47GB。

### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **跨域验证**：在三个完全不同的真实驾驶数据集上验证，证明了极强的零样本泛化能力。
*   **消融实验**：逐一验证了 PACV、TCC、时空解码器及各损失函数的贡献。
*   **鲁棒性测试**：针对**雨天、黑夜、静态跟车**等极端场景进行了专项对比。
*   **传感器退化模拟**：测试了 LiDAR 线数从 64 线降至 4 线、LiDAR 存在近场遮挡、以及仅前视有 LiDAR 而侧后视无 LiDAR（盲区补偿）的情况。
*   **客观性**：实验对比涵盖了最新的 2024/2025 年方法，且在相同硬件和评估协议下进行，具有较高的客观公信力。

### 6. 主要结论与发现
*   **SOTA 性能**：DriveMVS 在所有测试数据集上的度量精度均显著优于现有方法（如在 KITTI 上 AbsRel 仅为 2.56%）。
*   **时序稳定性**：通过时空解码器，TAE 指标大幅下降，解决了视频深度估计中的闪烁问题。
*   **尺度锚定效应**：稀疏 LiDAR 提示不仅提升了有值区域的精度，还能通过 TCC 模块将尺度信息传播至无 LiDAR 覆盖的区域（如传感器盲区）。
*   **合成转真实成功**：证明了在高质量合成数据上训练的 MVS 模型，结合 LiDAR 提示后，可以完美迁移至真实复杂环境。

### 7. 优点
*   **双路径集成**：巧妙地将 LiDAR 提示同时作为代价卷的“硬约束”和特征融合的“软引导”。
*   **几何显式建模**：在解码器中嵌入相机射线几何信息，而非盲目依赖数据驱动的时序注意力。
*   **极强的鲁棒性**：在 LiDAR 极度稀疏或部分缺失的情况下，依然能保持远超单目模型的精度。

### 8. 不足与局限
*   **计算开销**：由于涉及多视图匹配和时序 Transformer 计算，其推理延迟（65ms）高于纯单目方法，对于超高帧率的实时应用可能仍需进一步优化。
*   **依赖位姿精度**：作为 MVS 框架，其性能高度依赖于相机外参和位姿估计的准确性，在位姿剧烈抖动的极端工况下可能受限。
*   **推理依赖性**：需要多帧缓存和多视图输入，增加了系统的内存管理复杂度。

（完）
