---
title: "MoECLIP: Patch-Specialized Experts for Zero-shot Anomaly Detection"
title_zh: MoECLIP：用于零样本异常检测的补丁特化专家
authors: "Jun Yeong Park, JunYoung Seo, Minji Kang, Yu Rang Park"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03101v2"
tags: ["query:q14"]
score: 9.0
evidence: 利用CLIP和补丁专家进行零样本异常检测
tldr: 本研究针对零样本异常检测（ZSAD）中现有方法对图像块处理缺乏针对性的问题，提出了MoECLIP模型。该模型利用混合专家（MoE）架构，通过动态路由将每个图像块分配给专门的LoRA专家，实现块级自适应。为避免专家功能冗余，引入了冷冻正交特征分离（FOFS）和等角紧框架（ETF）损失。实验证明，MoECLIP在14个工业和医疗基准数据集上均达到了SOTA水平，有效平衡了CLIP的泛化能力与异常检测的专业性。
selection_source: fresh_fetch
motivation: 现有的零样本异常检测方法通常对所有图像块进行统一处理，忽略了不同局部特征的独特性，限制了模型的精细化检测能力。
method: 提出MoECLIP架构，通过动态路由将图像块分配给特定的LoRA专家，并结合正交特征分离（FOFS）和等角紧框架（ETF）损失来增强专家间的差异性。
result: 在涵盖工业和医疗领域的14个基准数据集上，MoECLIP的性能均超越了现有的最先进方法。
conclusion: MoECLIP通过引入专家混合机制实现了高效的块级自适应，在保持CLIP泛化性的同时显著提升了零样本异常检测的精度。
---

## 摘要
CLIP 模型卓越的泛化能力推动了零样本异常检测（ZSAD）在检测未知类别异常方面的近期成功。ZSAD 的核心挑战在于，在保留 CLIP 强大泛化能力的同时，使模型专门化以适应异常检测任务。现有尝试解决这一挑战的方法存在一个根本局限，即采用补丁无关（patch-agnostic）的设计，不考虑每个补丁的独特特征而对其进行统一处理。为了解决这一局限，我们为 ZSAD 任务提出了一种混合专家（MoE）架构 MoECLIP，它通过根据每个图像补丁的独特特征将其动态路由到专门的低秩自适应（LoRA）专家，从而实现补丁级自适应。此外，为了防止 LoRA 专家之间的功能冗余，我们引入了：（1）冻结正交特征分离（FOFS），通过对输入特征空间进行正交分离，迫使专家关注不同的信息；（2）单纯形等角紧框架（ETF）损失，用于调节专家输出以形成最大等角的表示。在涵盖工业和医疗领域的 14 个基准数据集上的全面实验结果表明，MoECLIP 的性能优于现有的最先进方法。代码可在 https://github.com/CoCoRessa/MoECLIP 获取。

## Abstract
The CLIP model's outstanding generalization has driven recent success in Zero-Shot Anomaly Detection (ZSAD) for detecting anomalies in unseen categories. The core challenge in ZSAD is to specialize the model for anomaly detection tasks while preserving CLIP's powerful generalization capability. Existing approaches attempting to solve this challenge share the fundamental limitation of a patch-agnostic design that processes all patches monolithically without regard for their unique characteristics. To address this limitation, we propose MoECLIP, a Mixture-of-Experts (MoE) architecture for the ZSAD task, which achieves patch-level adaptation by dynamically routing each image patch to a specialized Low-Rank Adaptation (LoRA) expert based on its unique characteristics. Furthermore, to prevent functional redundancy among the LoRA experts, we introduce (1) Frozen Orthogonal Feature Separation (FOFS), which orthogonally separates the input feature space to force experts to focus on distinct information, and (2) a simplex equiangular tight frame (ETF) loss to regulate the expert outputs to form maximally equiangular representations. Comprehensive experimental results across 14 benchmark datasets spanning industrial and medical domains demonstrate that MoECLIP outperforms existing state-of-the-art methods. The code is available at https://github.com/CoCoRessa/MoECLIP.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MoECLIP**，一种基于混合专家（Mixture-of-Experts, MoE）架构的零样本异常检测（Zero-Shot Anomaly Detection, ZSAD）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：零样本异常检测（ZSAD）要求模型在未见过的类别上检测异常。虽然 CLIP 模型具备强大的泛化能力，但其预训练目标是全局语义理解，对局部异常不敏感。
*   **核心挑战**：如何在不破坏 CLIP 泛化性的前提下，使其适应精细的局部异常检测任务。
*   **现有局限**：目前的 ZSAD 方法（如 Anomaly-CLIP, AA-CLIP）多采用“补丁无关”（Patch-agnostic）设计，即对图像的所有补丁（Patches）应用统一的变换。然而，图像的不同区域（如背景、物体边缘、内部结构）具有截然不同的特征，统一处理限制了模型捕捉细微异常的能力。

### 2. 方法论
MoECLIP 的核心思想是**补丁特化自适应**，即通过动态路由让每个图像补丁选择最适合的专家模块。

*   **混合专家架构 (MoE-LoRA)**：在 CLIP 视觉编码器的多个层中集成 MoE 模块。每个专家由轻量级的 LoRA（低秩自适应）构成，保持 CLIP 原始权重冻结，仅训练专家和路由网络。
*   **专家特化机制（解决冗余问题）**：
    *   **冻结正交特征分离 (FOFS)**：在输入端，将特征空间正交切分为 $K$ 个子空间，每个专家仅处理特定的子空间。这从初始化阶段就强制专家关注不同的信息。
    *   **单纯形等角紧框架 (ETF) 损失**：在输出端，通过损失函数强制不同专家的输出向量在几何上尽可能远离（最大等角），确保专家功能的差异化。
*   **补丁平均聚合 (PAA)**：引入多尺度滑动窗口平均聚合，使模型能够感知不同尺寸的异常，增强对结构性异常的鲁棒性。
*   **深度卷积适配器 (Depth-wise Adapter)**：在图像级评分阶段，使用轻量级的深度可分离卷积提取特征，提高计算效率并防止过拟合。

### 3. 实验设计
*   **数据集**：涵盖 14 个基准数据集。
    *   **工业领域 (5个)**：MVTec-AD, VisA, BTAD, RSDD, DTD-Synthetic。
    *   **医疗领域 (9个)**：Brain MRI, Head CT, Liver CT, Retina OCT 以及 5 个结肠息肉检测数据集（如 ColonDB, Kvasir 等）。
*   **Benchmark 与对比方法**：对比了 WinCLIP, April-GAN, AnomalyCLIP, AdaCLIP, AA-CLIP, Bayes-PFL 等 6 种最先进的 SOTA 方法。
*   **实验设置**：遵循标准的 ZSAD 协议，通常在工业数据集（如 VisA）上训练，在其他所有未见过的工业和医疗数据集上直接测试。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA Tesla V100 16GB GPU**。
*   **训练细节**：训练 20 个 epoch，使用 Adam 优化器，学习率为 $5 \times 10^{-4}$。
*   **推理效率**：虽然引入了 MoE，但由于使用了轻量级 LoRA 和 Top-k 路由（仅激活部分专家），其显存占用比 AA-CLIP 降低了约 34.3%，推理延迟增加约 39ms。

### 5. 实验数量与充分性
*   **实验规模**：在 14 个数据集上进行了全面的性能评估（图像级和像素级 AUROC/AP）。
*   **消融实验**：非常充分。涵盖了 FOFS、ETF 损失、PAA 模块、适配器设计、专家数量 $K$、路由 Top-k 值、MoE 插入层位置等多个维度的验证。
*   **客观性**：通过可视化 Grad-CAM 和专家路由图，直观展示了不同专家确实在关注图像的不同语义区域（如专家 1 关注异常，专家 2 关注物体，专家 3 关注背景），证明了设计的有效性。

### 6. 主要结论与发现
*   **性能领先**：MoECLIP 在所有 14 个数据集上均达到了 SOTA 性能，尤其在医疗领域的泛化表现显著优于前人方法。
*   **补丁特化的必要性**：实验证明，动态路由到特化专家比统一的线性适配能更有效地捕捉局部异常特征。
*   **冗余抑制有效**：FOFS 和 ETF 损失的结合成功解决了 MoE 常见的“专家同质化”问题，使专家之间保持了极低的余弦相似度。

### 7. 优点
*   **创新性**：首次将 MoE 架构引入 ZSAD 任务，打破了补丁无关处理的传统范式。
*   **几何约束设计**：利用 ETF 理论来约束专家输出，具有较强的数学直觉和实际效果。
*   **跨领域泛化**：仅在工业数据上训练，却在完全不同的医疗影像上表现出色，证明了极强的鲁棒性。

### 8. 不足与局限
*   **文本端优化较少**：论文重点在于视觉特征的补丁特化，对文本提示（Prompt）的探索相对传统，主要沿用了 AA-CLIP 的设置。
*   **可解释性局限**：虽然展示了专家分工，但仍无法给出“为什么这是异常”的自然语言解释（作者在未来工作中提到将探索多模态大模型 LLM 来解决此问题）。
*   **超参数敏感性**：专家数量 $K$ 和路由策略对性能有一定影响，在不同复杂度的场景下可能需要微调。

（完）
