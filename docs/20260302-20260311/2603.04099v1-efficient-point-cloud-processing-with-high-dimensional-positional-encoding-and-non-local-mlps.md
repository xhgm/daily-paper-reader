---
title: Efficient Point Cloud Processing with High-Dimensional Positional Encoding and Non-Local MLPs
title_zh: 基于高维位置编码和非局部 MLP 的高效点云处理
authors: "Yanmei Zou, Hongshan Yu, Yaonan Wang, Zhengeng Yang, Xieyuanli Chen, Kailun Yang, Naveed Akhtar"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04099v1"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于点云特征提取的高维位置编码
tldr: 本研究针对点云处理中MLP模型架构复杂且效率受限的问题，提出了“抽象与细化”（ABS-REF）的两阶段视图。通过引入高维位置编码（HPE）模块显式利用位置信息，并利用非局部MLP替代耗时的局部聚合操作，构建了高效的HPENets。实验表明，HPENet在多个主流数据集上不仅性能优于PointNeXt等强基准，且计算开销显著降低，实现了效率与效果的极佳平衡。
selection_source: fresh_fetch
motivation: 现有的点云MLP模型架构复杂，且在捕捉局部关系时往往伴随着高昂的计算成本。
method: 提出ABS-REF框架，利用高维位置编码（HPE）强化位置表征，并结合非局部MLP进行高效的信息更新。
result: 在ScanObjectNN和S3DIS等七个数据集上，HPENet以极低的FLOPs实现了领先的分类与分割精度。
conclusion: 通过重新审视局部聚合并引入显式位置编码，可以在大幅提升点云处理效率的同时保持甚至增强模型性能。
---

## 摘要
多层感知机（MLP）模型是当代点云处理的基础。然而，它们复杂的网络架构掩盖了其优势的来源，并限制了这些模型的应用。在本文中，我们为点云处理中的模块化特征提取开发了一种两阶段的抽象与细化（ABS-REF）视角。该视角阐明了早期模型侧重于 ABS 阶段，而较新的技术则设计了复杂的 REF 阶段以获得性能优势。随后，我们提出了一种高维位置编码（HPE）模块，以显式利用内在位置信息，扩展了 Transformer 文献中的“位置编码”概念。HPE 可以轻松部署在基于 MLP 的架构中，并与基于 Transformer 的方法兼容。在我们的 ABS-REF 视角下，我们重新思考了基于 MLP 方法中的局部聚合，并建议替换用于捕获邻域间局部关系的耗时局部 MLP 操作。相反，我们使用非局部 MLP 进行高效的非局部信息更新，并结合所提出的 HPE 进行有效的局部信息表示。我们利用这些模块开发了 HPENets，这是一系列遵循 ABS-REF 范式的 MLP 网络，包含一个可扩展的基于 HPE 的 REF 阶段。在四个不同任务的七个公共数据集上进行的广泛实验表明，HPENets 在效率和有效性之间实现了强大的平衡。值得注意的是，在 ScanObjectNN、S3DIS、ScanNet 和 ShapeNetPart 数据集上，HPENet 在仅消耗 50.0%、21.5%、23.1% 和 44.4% FLOPs 的情况下，分别比强大的 MLP 对手 PointNeXt 高出 1.1% mAcc、4.0% mIoU、1.8% mIoU 和 0.2% Cls. mIoU。源代码可在 https://github.com/zouyanmei/HPENet_v2.git 获取。

## Abstract
Multi-Layer Perceptron (MLP) models are the foundation of contemporary point cloud processing. However, their complex network architectures obscure the source of their strength and limit the application of these models. In this article, we develop a two-stage abstraction and refinement (ABS-REF) view for modular feature extraction in point cloud processing. This view elucidates that whereas the early models focused on ABS stages, the more recent techniques devise sophisticated REF stages to attain performance advantages. Then, we propose a High-dimensional Positional Encoding (HPE) module to explicitly utilize intrinsic positional information, extending the ``positional encoding'' concept from Transformer literature. HPE can be readily deployed in MLP-based architectures and is compatible with transformer-based methods. Within our ABS-REF view, we rethink local aggregation in MLP-based methods and propose replacing time-consuming local MLP operations, which are used to capture local relationships among neighbors. Instead, we use non-local MLPs for efficient non-local information updates, combined with the proposed HPE for effective local information representation. We leverage our modules to develop HPENets, a suite of MLP networks that follow the ABS-REF paradigm, incorporating a scalable HPE-based REF stage. Extensive experiments on seven public datasets across four different tasks show that HPENets deliver a strong balance between efficiency and effectiveness. Notably, HPENet surpasses PointNeXt, a strong MLP-based counterpart, by 1.1% mAcc, 4.0% mIoU, 1.8% mIoU, and 0.2% Cls. mIoU, with only 50.0%, 21.5%, 23.1%, 44.4% of FLOPs on ScanObjectNN, S3DIS, ScanNet, and ShapeNetPart, respectively. Source code is available at https://github.com/zouyanmei/HPENet_v2.git.

---

## 论文详细总结（自动生成）

以下是对论文《Efficient Point Cloud Processing with High-Dimensional Positional Encoding and Non-Local MLPs》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管多层感知机（MLP）是点云处理的基石，但现有模型架构日益复杂，导致性能提升的来源变得模糊，且计算开销（FLOPs）巨大，难以在资源受限的设备上实时运行。
*   **核心问题**：如何在降低计算复杂度的同时，显式且有效地利用点云最核心的“位置信息”，并建立一个统一的视角来理解和优化点云特征提取过程。

### 2. 方法论
论文提出了 **HPENet V2**，其核心思想围绕“效率”与“几何表征”展开：
*   **ABS-REF 统一视图**：将点云处理分为**抽象（Abstraction, ABS）**和**细化（Refinement, REF）**两个阶段。ABS 负责下采样和局部特征提取，REF 负责在保持分辨率的前提下通过堆叠层来扩大感受野和增强表征。
*   **高维位置编码（HPE）**：受 Transformer 启发，将 3D 相对坐标通过 MLP 或正弦函数映射到高维空间（如通道数的 1/4），从而捕捉细粒度的几何线索。该模块具有平移不变性，能显著增强 MLP 的几何感知能力。
*   **非局部 MLP（Non-Local MLPs）策略**：重新思考局部聚合，建议在分组（Grouping）操作前使用 MLP 处理点（非局部），而在分组后仅使用简单的还原操作（如 Max Pooling）。这种设计将计算复杂度从 $O(N \times K)$ 降低到 $O(N)$，极大地减少了 FLOPs。
*   **后向融合模块（BFM）**：在解码器中引入双向通道注意力机制，利用高分辨率特征的统计信息来指导多分辨率特征的融合，弥补了传统 U-Net 结构中上下文交互不足的问题。

### 3. 实验设计
*   **数据集与场景**：
    *   **分类**：ScanObjectNN（真实世界）、ModelNet40（合成）。
    *   **语义分割**：S3DIS（室内大场景）、ScanNet V2（室内）、SemanticKITTI（室外自动驾驶）。
    *   **零件分割**：ShapeNetPart。
    *   **目标检测**：SUN RGB-D, ScanNet。
*   **Benchmark 与对比方法**：对比了包括 PointNet++、DGCNN、PointMLP、PointNeXt（强力 MLP 基准）、PointMetaBase（高效基准）以及 Point Transformer 等 SOTA 方法。

### 4. 资源与算力
*   **硬件环境**：推理速度（吞吐量）测试使用了 **NVIDIA RTX 4090 (24GB) GPU** 和 Intel Xeon Gold 6138 CPU。
*   **训练细节**：
    *   分类任务训练 250-600 epochs。
    *   分割任务使用 AdamW 优化器，初始学习率在 0.001 到 0.01 之间。
*   **算力说明**：文中未明确标注总训练时长（小时/天），但详细列出了各模型的参数量（Param）和计算量（FLOPs），强调了其模型在低算力消耗下的高性能。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流数据集、4 种核心任务上进行了广泛测试。
*   **消融实验**：非常充分。涵盖了 ABS-REF 框架的有效性、HPE 的维度影响、不同位置编码方式（绝对 vs 相对、加法 vs 乘法）的对比、BFM 模块的增益，以及将 HPE 移植到 Transformer 架构中的兼容性测试。
*   **客观性**：所有实验均采用多次运行取平均值的方式，并提供了推理速度与精度的权衡曲线（Bubble Chart），证明了其在相同算力下的领先地位。

### 6. 主要结论与发现
*   **效率极高**：HPENet V2 在 S3DIS 数据集上仅需 PointNeXt 21.5% 的 FLOPs，却实现了 4.0% mIoU 的领先。
*   **位置编码胜过复杂算子**：实验发现，显式的高维位置编码对性能的提升远比设计复杂的局部聚合算子更有效。
*   **模块通用性**：HPE 模块不仅适用于 MLP 架构，集成到 Point Transformer 中也能带来 2.5% 的 mIoU 提升，证明了其作为通用插件的潜力。

### 7. 优点（亮点）
*   **理论框架清晰**：ABS-REF 视图为点云网络的设计提供了一个模块化的标准，有助于分析不同模型的优劣。
*   **极佳的性能/功耗比**：在大幅削减计算量的前提下刷新了多个数据集的 SOTA，对工业界实时部署具有极高价值。
*   **鲁棒性强**：在点云密度变化的测试中表现稳定，优于同类高效模型。

### 8. 不足与局限
*   **合成数据提升有限**：在 ModelNet40 等分布较为规则的合成数据集上，HPE 带来的增益不如在真实扫描数据（带噪声、遮挡）中显著。
*   **长程依赖建模**：虽然 REF 阶段扩大了感受野，但核心仍基于局部聚合，对于极远距离的全局上下文建模能力可能仍逊于全注意力机制的 Transformer。
*   **应用限制**：主要针对点基（Point-based）方法，未讨论其在体素（Voxel-based）或多模态融合架构中的表现。

（完）
