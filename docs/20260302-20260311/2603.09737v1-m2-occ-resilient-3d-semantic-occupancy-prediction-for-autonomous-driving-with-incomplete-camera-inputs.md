---
title: "$M^2$-Occ: Resilient 3D Semantic Occupancy Prediction for Autonomous Driving with Incomplete Camera Inputs"
title_zh: $M^2$-Occ：针对相机输入不完整的自动驾驶鲁棒 3D 语义占据预测
authors: "Kaixin Lin, Kunyu Peng, Di Wen, Yufan Chen, Ruiping Liu, Kailun Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09737v1"
tags: ["query:3d-scn"]
score: 10.0
evidence: 自动驾驶的3D语义占据预测
tldr: 针对自动驾驶中因硬件故障或遮挡导致的相机输入缺失问题，本文提出 $M^2$-Occ 框架。该框架通过多视图掩码重建（MMR）利用相邻相机重叠区域恢复缺失特征，并结合特征记忆模块（FMM）利用全局语义先验增强体素表示。在 nuScenes 缺失视图基准测试中，该方法显著提升了 3D 语义占用预测的鲁棒性，在多视图缺失情况下表现尤为突出，且不影响全视图性能。
selection_source: fresh_fetch
motivation: 现有的 3D 占用预测方法通常假设环视输入完整，难以应对实际驾驶中常见的相机失效或遮挡等输入缺失挑战。
method: 引入多视图掩码重建模块恢复缺失特征，并利用特征记忆模块存储类级别语义原型以提供全局先验支持。
result: "在 nuScenes 缺失视图场景下，该方法在后视缺失和五视图缺失时分别实现了 4.93% 和 5.01% 的 IoU 提升。"
conclusion: M2-Occ 框架通过特征恢复和语义先验集成，显著提高了自动驾驶系统在不完整相机输入下的 3D 几何与语义感知鲁棒性。
---

## 摘要
语义占据预测为自动驾驶提供了稠密的 3D 几何与语义理解。然而，现有的基于相机的方法隐含地假设了完整的环视观测，由于遮挡、硬件故障或通信失败，这一假设在实际部署中往往难以成立。我们研究了多相机输入不完整情况下的语义占据预测，并提出了 $M^2$-Occ，这是一个旨在视角缺失时保持几何结构和语义连贯性的框架。$M^2$-Occ 解决了两个互补的挑战。首先，多视角掩码重建（MMR）模块利用相邻相机之间的空间重叠，直接在特征空间中恢复缺失视角的表示。其次，特征记忆模块（FMM）引入了一个可学习的记忆库，用于存储类别级的语义原型。通过检索和整合这些全局先验，FMM 细化了模糊的体素特征，确保即使在观测证据不完整的情况下也能保持语义一致性。我们在基于 nuScenes 的 SurroundOcc 基准测试中引入了一套系统的缺失视角评估协议，涵盖了确定性的单视角故障和随机的多视角丢弃场景。在安全至关重要的后视角缺失设置下，$M^2$-Occ 将 IoU 提高了 4.93%。随着缺失相机数量的增加，鲁棒性差距进一步扩大；例如，在缺失五个视角的设置下，我们的方法将 IoU 提升了 5.01%。这些提升是在不损害全视角性能的情况下实现的。源代码将公开发布于 https://github.com/qixi7up/M2-Occ。

## Abstract
Semantic occupancy prediction enables dense 3D geometric and semantic understanding for autonomous driving. However, existing camera-based approaches implicitly assume complete surround-view observations, an assumption that rarely holds in real-world deployment due to occlusion, hardware malfunction, or communication failures. We study semantic occupancy prediction under incomplete multi-camera inputs and introduce $M^2$-Occ, a framework designed to preserve geometric structure and semantic coherence when views are missing. $M^2$-Occ addresses two complementary challenges. First, a Multi-view Masked Reconstruction (MMR) module leverages the spatial overlap among neighboring cameras to recover missing-view representations directly in the feature space. Second, a Feature Memory Module (FMM) introduces a learnable memory bank that stores class-level semantic prototypes. By retrieving and integrating these global priors, the FMM refines ambiguous voxel features, ensuring semantic consistency even when observational evidence is incomplete. We introduce a systematic missing-view evaluation protocol on the nuScenes-based SurroundOcc benchmark, encompassing both deterministic single-view failures and stochastic multi-view dropout scenarios. Under the safety-critical missing back-view setting, $M^2$-Occ improves the IoU by 4.93%. As the number of missing cameras increases, the robustness gap further widens; for instance, under the setting with five missing views, our method boosts the IoU by 5.01%. These gains are achieved without compromising full-view performance. The source code will be publicly released at https://github.com/qixi7up/M2-Occ.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **$M^2$-Occ** 的框架，旨在解决自动驾驶中因相机故障或遮挡导致输入不完整时的 3D 语义占据预测（Semantic Occupancy Prediction, SOP）鲁棒性问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的基于相机的 3D 占据预测方法（如 SurroundOcc、TPVFormer）通常假设环视相机（如 nuScenes 的 6 个相机）始终工作正常且同步。但在实际部署中，相机常因镜头遮挡、硬件故障或通信中断而导致部分视角缺失。
*   **核心问题**：当某个或多个视角缺失时，现有的模型会出现严重的几何空洞和语义预测崩溃。论文旨在研究如何在不增加额外传感器（如 LiDAR）的情况下，利用空间冗余和全局先验来恢复缺失的感知信息。

### 2. 方法论
$M^2$-Occ 框架由两个核心模块组成，分别从特征恢复和语义正则化两个维度提升鲁棒性：

*   **多视图掩码重建模块 (MMR)**：
    *   **核心思想**：利用相邻相机视角之间的物理重叠区域（Overlap）来“修补”缺失视角的特征。
    *   **技术细节**：将相邻相机的边缘特征提取出来，与一个可学习的掩码标记（Mask Token）拼接，形成结构先验。随后通过一个轻量级的 Transformer 解码器，在特征空间中重建缺失视角的表示。
    *   **损失函数**：使用均方误差（MSE）约束重建特征与原始特征的一致性。
*   **特征记忆模块 (FMM)**：
    *   **核心思想**：引入全局“长期记忆”来纠正 MMR 恢复过程中可能产生的模糊或噪声。
    *   **技术细节**：维护一个可学习的记忆库（Memory Bank），存储各语义类别的原型（Prototypes）。
    *   **策略**：提供 **Single-Proto**（每类一个中心，更稳定）和 **Multi-Proto**（每类多个子原型，捕捉类内差异）两种模式。通过计算体素特征与原型的相似度，将全局语义先验注入 3D 体素中，增强语义一致性。

### 3. 实验设计
*   **数据集**：使用 **nuScenes** 数据集及其对应的 **SurroundOcc** 稠密占据标注。
*   **基准模型 (Benchmark)**：以代表性的 **SurroundOcc** 作为 Baseline。
*   **评估协议**：
    *   **单视角故障**：分别模拟前、后、左前、右前等 6 个相机中任意一个失效。
    *   **随机多视角丢弃**：模拟同时缺失 1、3、5 个视角的极端情况。
*   **对比指标**：几何完整性（IoU）和语义准确性（mIoU）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA RTX A6000 GPU** 上进行。
*   **训练细节**：采用 AdamW 优化器，训练 24 个 epoch，学习率为 $2 \times 10^{-4}$。
*   **开销**：相比 Baseline，内存消耗仅增加约 0.15 GB（约 2.5%）。推理延迟随缺失视角数量增加而线性增加（因为需要逐个重建特征），从 0.50s 增加到 0.77s~1.25s。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的定量分析，涵盖了所有可能的单视角缺失组合以及不同程度的多视角缺失。
*   **消融实验**：分别验证了 MMR 和 FMM（以及不同原型策略）的贡献，证明了两个模块的互补性。
*   **充分性评价**：实验设计非常系统且具有针对性，通过可视化特征图和占据网格直观展示了恢复效果。对比实验在相同的实验设置下进行，确保了客观性和公平性。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在安全关键的“后视缺失”场景下，IoU 提升了 **4.93%**。在极端“5 视角缺失”场景下，IoU 从 13.35% 提升至 **18.36%**。
*   **几何恢复优于语义细化**：MMR 在恢复道路、车辆等大尺度几何结构方面效果显著，而 FMM 在处理类别模糊性上起到辅助作用。
*   **原型策略发现**：在输入严重缺失的情况下，简单的 **Single-Proto** 策略比复杂的 Multi-Proto 更稳健，因为后者在特征稀疏时容易引入噪声。

### 7. 优点
*   **实用性强**：直接针对自动驾驶的边缘案例（Edge Cases）设计，不改变主干网络，易于集成到现有框架。
*   **特征级修复**：相比于生成原始像素，在特征空间重建效率更高且更关注语义一致性。
*   **无需额外传感器**：仅靠相机间的空间冗余实现“软修复”，降低了系统成本。

### 8. 不足与局限
*   **小物体恢复困难**：实验显示，对于行人、交通锥等小物体，由于重叠区域分辨率不足，重建效果有限，IoU 甚至可能出现小幅下降。
*   **推理延迟**：Transformer 解码器的引入增加了推理时间，对于实时性要求极高的场景可能需要进一步优化。
*   **时序信息缺失**：目前主要依赖空间冗余，未充分利用历史帧的时序信息来填补当前的视觉盲区。

（完）
