---
title: Mean-Flow based One-Step Vision-Language-Action
title_zh: 基于均值流的一步式视觉-语言-动作模型
authors: "Yang Chen, Xiaoguang Ma, Bin Zhao"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01469v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作模型的一步动作生成
tldr: 针对基于Flow-Matching的视觉-语言-动作（VLA）模型在机器人操作中因迭代采样导致的高延迟问题，本文提出了一种基于均值流（Mean-Flow）的一步生成框架。该方法通过解决动作生成中的噪声诱导问题，消除了传统流匹配的一致性约束，实现了高效的一步动作预测。实验证明，该模型在保持高性能的同时，生成速度显著优于SmolVLA和扩散策略，为实时机器人操控提供了高效的骨干架构。
selection_source: fresh_fetch
motivation: 现有的Flow-Matching VLA框架因迭代采样过程导致生成延迟较高，限制了其在实时机器人任务中的应用。
method: 提出基于均值流的一步生成方法，通过解决动作生成中的噪声诱导问题来消除传统流匹配的一致性约束。
result: 在真实机器人实验中，该方法的生成速度分别比SmolVLA和扩散策略快8.7倍和83.9倍。
conclusion: 该研究证明了均值流一步生成框架在提升VLA模型效率方面的巨大潜力，是高效机器人操作的理想选择。
---

## 摘要
基于流匹配（Flow-Matching）的视觉-语言-动作（VLA）框架的最新进展在生成高频动作块方面展现出显著优势，特别是在高灵巧性的机器人操作任务中。尽管取得了这些显著成就，但由于固有的迭代采样需求和架构限制，它们的实际应用受到生成延迟过长的制约。为了解决这一关键瓶颈，我们提出了一种基于均值流（Mean-Flow）的一步式 VLA 方法。具体而言，我们解决了动作生成过程中的噪声诱导问题，从而消除了传统流匹配方法固有的连贯性约束。这显著提高了生成效率，并实现了单步动作生成。真实世界的机器人实验表明，所提出的基于均值流的一步式 VLA 的生成速度分别是 SmolVLA 和扩散策略（Diffusion Policy）的 8.7 倍和 83.9 倍。这些结果阐明了其作为基于 VLA 的机器人操作的高效骨干网络的巨大潜力。

## Abstract
Recent advances in FlowMatching-based Vision-Language-Action (VLA) frameworks have demonstrated remarkable advantages in generating high-frequency action chunks, particularly for highly dexterous robotic manipulation tasks. Despite these notable achievements, their practical applications are constrained by prolonged generation latency, which stems from inherent iterative sampling requirements and architectural limitations. To address this critical bottleneck, we propose a Mean-Flow based One-Step VLA approach. Specifically, we resolve the noise-induced issues in the action generation process, thereby eliminating the consistency constraints inherent to conventional Flow-Matching methods. This significantly enhances generation efficiency and enables one-step action generation. Real-world robotic experiments show that the generation speed of the proposed Mean-Flow based One-Step VLA is 8.7 times and 83.9 times faster than that of SmolVLA and Diffusion Policy, respectively. These results elucidate its great potential as a high-efficiency backbone for VLA-based robotic manipulation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mean-Flow based One-Step Vision-Language-Action》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的视觉-语言-动作（VLA）模型在机器人灵巧操作中表现出色，但面临严重的**推理延迟**瓶颈。
    *   **扩散模型（Diffusion-based）**：依赖多步迭代去噪，速度慢。
    *   **流匹配（FlowMatching-based）**：虽然比扩散模型快，但仍需通过多步欧拉积分（Euler integration）来减少偏置，减少步数会导致轨迹精度大幅下降。
*   **核心问题**：如何在不牺牲生成质量的前提下，实现**一步式（One-Step）**动作生成，以满足实时高频控制的需求。

### 2. 核心方法论
论文提出了 **Mean-Flow based One-Step VLA** 框架，其核心思想是将“均值流”理论引入机器人动作预测：
*   **均值向量场（Mean Denoising Vector Field）**：不同于传统流匹配学习瞬时向量场，该方法学习给定时间区间 $[r, t]$ 内的平均变换方向。
*   **均值流恒等式（MeanFlow Identity）**：通过数学推导，建立均值向量场 $u$ 与瞬时向量场 $v$ 之间的关系。利用雅可比向量积（JVP）来处理时间导数项，使网络能够直接预测从噪声到数据的全局映射。
*   **训练策略**：
    *   **混合目标**：同时拟合瞬时向量场和均值向量场，通过 `flow-ratio` 参数平衡局部精度和全局趋势。
    *   **自适应损失函数（Adaptive Loss）**：引入参数 $\gamma$ 的自适应缩放机制，以减轻均值流目标中高方差样本（长区间样本）对优化的干扰，增强训练稳定性。
*   **推理流程**：在推理阶段，模型仅需一次前向计算（NFE=1），即可直接从随机噪声生成完整的连续动作块（Action Chunk）。

### 3. 实验设计
*   **硬件平台**：自制的 SO-101 六自由度机械臂，配备夹持器。传感器包括腕部相机（第一视角）和顶部相机（第三视角）。
*   **实验场景/任务**：
    1.  **Pick-place（抓取放置）**：随机位置抓取红块放入盒中。
    2.  **Stacking（堆叠）**：将橙色玩具堆叠在彩色方块上（高精度要求）。
    3.  **Sorting（长程排序）**：将红/绿方块分别放入对应的左右盒子。
*   **对比方法（Baselines）**：
    *   **SmolVLA**：基于流匹配的高效 VLA 模型。
    *   **Diffusion Policy**：经典的基于扩散过程的动作策略。
*   **评估指标**：任务成功率、动作生成速度（推理延迟）。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090** GPU。
    *   单任务训练：60,000 步。
    *   多任务混合训练：200,000 步。
*   **推理/部署算力**：在笔记本电脑上运行，配置为 Intel i5-10200H CPU 和 **NVIDIA GTX 1650** GPU（体现了模型对低端硬件的友好性）。
*   **模型骨干**：视觉语言模型采用 SmolVLM-2（SigLIP 视觉编码器 + SmolLM-2 语言解码器）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **参数敏感性分析**：针对 `flow-ratio`（流比例）、`Gamma`（损失函数参数）、`NFE`（推理步数）和 `Chunk Size`（动作块大小）进行了详细的消融实验。
    *   **真实机器人测试**：每个任务进行 10 轮独立评估，每轮 10-20 次尝试，总计进行了数百次实机测试。
*   **充分性评价**：实验设计较为全面，涵盖了从超参数调优到多任务泛化能力的评估。对比了当前主流的扩散和流匹配路径，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **极高的推理效率**：在生成速度上，One-Step VLA 比 SmolVLA 快 **8.7 倍**，比 Diffusion Policy 快 **83.9 倍**。
*   **一步生成的有效性**：通过均值流建模，模型在 NFE=1 时即可达到可用的成功率，而传统流匹配在 NFE=1 时轨迹会发生严重漂移。
*   **性能权衡**：虽然一步生成速度极快，但在需要极高精度的任务（如 Stacking）中，成功率略低于多步迭代的 SmolVLA（64% vs 81.5%），但在常规任务中表现持平。
*   **关键超参数**：发现 `flow-ratio=0.2` 和 `Gamma=0.5` 是平衡训练稳定性和生成精度的最优配置。

### 7. 优点
*   **推理延迟极低**：真正实现了机器人动作的“即时”生成，极大地提升了闭环控制的响应频率。
*   **无需复杂后处理**：相比于其他加速生成的方法，该方法不需要额外的模型蒸馏（Distillation）或一致性模型（Consistency Models）训练，流程更简洁。
*   **硬件友好**：能够在消费级显卡（GTX 1650）上实现高效推理，降低了 VLA 模型的部署门槛。

### 8. 不足与局限
*   **高精度任务挑战**：在极细微的操作任务中，一步生成由于缺乏迭代修正，精度仍有提升空间。
*   **泛化范围有限**：实验主要集中在自建的 300 条演示数据集上，尚未在大规模开放域数据集（如 Open X-Embodiment）上验证其极端泛化能力。
*   **动作块长度限制**：实验发现动作块过长（如 50 步）会导致环境响应性下降，说明一步生成在长程预测和实时反馈之间仍存在平衡问题。

（完）
