---
title: "OnlineX: Unified Online 3D Reconstruction and Understanding with Active-to-Stable State Evolution"
title_zh: OnlineX：基于从活跃到稳定状态演化的统一在线三维重建与理解
authors: "Chong Xia, Fangfu Liu, Yule Wang, Yize Pang, Yueqi Duan"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02134v2"
tags: ["query:q10"]
score: 9.0
evidence: 在线 3D 重建与语言场理解
tldr: OnlineX是一个针对流式图像的在线3D重建与理解框架，旨在解决现有3DGS方法在实时场景下因局部更新与全局稳定冲突导致的累积漂移问题。该框架引入了“从活跃到稳定”的状态演化范式，通过解耦并融合活跃状态与稳定状态，实现了视觉外观与语义场的同步构建。实验证明，该方法在保持实时推理速度的同时，在视图合成和语义理解任务上均达到了领先水平。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅重建方法多为离线模式，难以应对机器人和VR/AR等需要实时处理流式数据且面临累积漂移挑战的在线应用场景。
method: 提出一种解耦的活跃-稳定状态演化范式，将内存分为捕捉局部几何的活跃态和维护全局结构的稳定态，并结合隐式高斯融合模块进行联合建模。
result: 在主流数据集上的实验显示，OnlineX在视图合成和语义理解质量上均优于现有方法，且展现出对不同长度输入序列的鲁棒性和实时推理性能。
conclusion: OnlineX通过创新的状态演化机制平衡了在线重建的保真度与稳定性，为实时、统一的3D场景重建与理解提供了高效的解决方案。
---

## 摘要
近期，可泛化三维高斯泼溅（3DGS）的进展实现了秒级的快速三维场景重建，消除了对逐场景优化的需求。然而，现有方法主要遵循离线重建范式，缺乏持续重建的能力，这限制了它们在机器人和 VR/AR 等在线场景中的应用。在本文中，我们提出了 OnlineX，这是一个仅使用流式图像以在线方式重建三维视觉外观和语言场的前馈框架。在线公式化的一个关键挑战是累积漂移问题，其根源在于记忆状态两种对立角色之间的根本冲突：一种是不断刷新以捕捉高频局部几何的活跃角色，另一种是保守地积累并保留长期全局结构的稳定角色。为了解决这一问题，我们引入了一种解耦的从活跃到稳定的状态演化范式。我们的框架将记忆状态解耦为专用的活跃状态和持久的稳定状态，然后将前者的信息内聚地融合到后者中，以同时实现保真度和稳定性。此外，我们联合建模了视觉外观和语言场，并引入了一个隐式高斯融合模块来提高重建质量。在主流数据集上的实验表明，我们的方法在视角合成和语义理解方面始终优于先前的工作，在不同长度的输入序列上展现出鲁棒的性能，并具有实时推理速度。

## Abstract
Recent advances in generalizable 3D Gaussian Splatting (3DGS) have enabled rapid 3D scene reconstruction within seconds, eliminating the need for per-scene optimization. However, existing methods primarily follow an offline reconstruction paradigm, lacking the capacity for continuous reconstruction, which limits their applicability to online scenarios such as robotics and VR/AR. In this paper, we introduce OnlineX, a feed-forward framework that reconstructs both 3D visual appearance and language fields in an online manner using only streaming images. A key challenge in online formulation is the cumulative drift issue, which is rooted in the fundamental conflict between two opposing roles of the memory state: an active role that constantly refreshes to capture high-frequency local geometry, and a stable role that conservatively accumulates and preserves the long-term global structure. To address this, we introduce a decoupled active-to-stable state evolution paradigm. Our framework decouples the memory state into a dedicated active state and a persistent stable state, and then cohesively fuses the information from the former into the latter to achieve both fidelity and stability. Moreover, we jointly model visual appearance and language fields and incorporate an implicit Gaussian fusion module to enhance reconstruction quality. Experiments on mainstream datasets demonstrate that our method consistently outperforms prior work in novel view synthesis and semantic understanding, showcasing robust performance across input sequences of varying lengths with real-time inference speed.

---

## 论文详细总结（自动生成）

以下是对论文《OnlineX: Unified Online 3D Reconstruction and Understanding with Active-to-Stable State Evolution》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的可泛化三维高斯泼溅（3DGS）方法大多采用“离线”范式，即需要预先获取所有图像和相机位姿。而在机器人、AR/VR等实时应用中，图像是流式输入的。
*   **研究动机**：在线重建面临“累积漂移”挑战。这源于记忆状态的两种对立需求：**活跃角色**（需要不断刷新以捕捉局部高频几何细节）与**稳定角色**（需要保守地积累并维护长期全局结构）。单一的隐藏状态难以平衡两者，导致随着序列增长，全局结构逐渐被遗忘或发生偏移。

### 2. 方法论
OnlineX 提出了一个**从活跃到稳定（Active-to-Stable）**的状态演化范式，核心流程如下：
*   **相对几何提取器（活跃状态）**：通过双视图 ViT 解码器处理当前帧与前一帧，提取相对位姿、局部几何和外观特征。这一步负责捕捉高保真的局部细节，减轻全局状态的负担。
*   **锚点状态指导器（稳定状态）**：引入“锚点状态（Anchor State）”作为稳定内存。它通过循环更新机制（Recurrent Update），将当前帧的压缩特征与历史全局上下文融合。
*   **隐式位姿变换**：不使用显式的刚性位姿变换，而是通过交叉注意力机制，在特征空间中将局部几何特征与全局位姿特征进行隐式融合，从而提高重建的鲁棒性。
*   **统一表示**：模型同时预测高斯球的几何、颜色及低维语言特征（基于 CLIP），实现视觉重建与语义理解的统一。
*   **隐式高斯融合**：在潜在空间中识别并合并空间重叠的冗余高斯基元，通过 MLP 融合其特征，使表示更简洁。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10k (RE10K)**：用于评估有限空间范围内的视频序列重建。
    *   **ScanNet**：用于评估房间尺度的场景重建及语义分割。
    *   **DL3DV**：用于测试模型的零样本（Zero-shot）泛化能力。
*   **Benchmark 与对比方法**：
    *   **离线 3DGS 方法**：MVSplat, NoPoSplat, FLARE（提供所有视图作为输入）。
    *   **在线点图预测方法**：Spann3R, CUT3R（为公平起见，为其添加了高斯预测头并进行了微调）。
    *   **语义理解方法**：LangSplat, Gaussian Grouping。
*   **评估指标**：PSNR, SSIM, LPIPS（视角合成）；ATE, RPE（位姿估计）；mIoU, mAcc（语义分割）。

### 4. 资源与算力
*   **训练配置**：使用了 **4 张 NVIDIA RTX A6000 GPU**。
*   **训练时长**：总计进行 30,000 次迭代，有效 Batch Size 为 8。
*   **推理性能**：在单张 RTX A6000 上，处理 256×256 分辨率输入时可达到 **23 FPS**，满足实时应用需求。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流数据集上进行了测试，涵盖了从 2 帧到 30 帧不等的输入序列长度。
*   **消融实验**：设计了四组关键消融实验，分别验证了相对提取器、锚点状态、隐式变换和隐式融合模块的必要性。
*   **充分性评价**：实验设计较为全面，不仅对比了同类在线方法，还挑战了拥有“全局视角”的离线方法。通过跨数据集的零样本测试，证明了模型的泛化性能。实验对比在相同输入条件下进行，具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：OnlineX 在长序列（如 ScanNet 30 帧）下的表现显著优于现有在线方法，且在短序列下能与离线 SOTA 方法持平。
*   **有效缓解漂移**：解耦活跃与稳定状态的策略成功抑制了长期重建中的几何漂移。
*   **统一性优势**：通过联合建模语言场，模型在无需额外优化的情况下，在 3D 开放词汇语义分割任务上优于专门的优化型方法（如 LangSplat）。

### 7. 优点
*   **架构创新**：通过活跃-稳定状态的解耦，巧妙解决了在线重建中的保真度与稳定性冲突。
*   **端到端且高效**：无需每场景优化，支持实时推理，且内存占用比显式空间内存方法（如 Spann3R）更低。
*   **隐式融合机制**：隐式位姿变换和高斯融合减少了显式几何变换带来的伪影和冗余。

### 8. 不足与局限
*   **分辨率限制**：实验主要基于 256×256 分辨率，在更高分辨率下的显存压力和实时性表现尚待观察。
*   **极端运动挑战**：虽然缓解了漂移，但在相机运动极快或遮挡严重的极端情况下，在线模型仍可能面临跟踪丢失的风险。
*   **语义精度**：语义理解依赖于预训练的 CLIP 模型，对于极细粒度或长尾类别的识别精度受限于基础模型的能力。

（完）
