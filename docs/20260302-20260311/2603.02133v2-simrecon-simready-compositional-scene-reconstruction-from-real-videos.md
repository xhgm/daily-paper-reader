---
title: "SimRecon: SimReady Compositional Scene Reconstruction from Real Videos"
title_zh: SimRecon：基于真实视频的 SimReady 组合式场景重建
authors: "Chong Xia, Kai Zhu, Zizhuo Wang, Fangfu Liu, Zhizheng Zhang, Yueqi Duan"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02133v2"
tags: ["query:3d-scn"]
score: 9.0
evidence: 用于模拟的视频组合场景重建
tldr: SimRecon 提出了一种从真实视频中进行“感知-生成-仿真”的组合式场景重建框架。针对复杂场景中生成资产视觉失真和物理不合理的问题，该方法引入了主动视点优化以获取最佳投影图像，并利用场景图合成器在仿真器中指导 3D 资产组装。实验证明该方法在 ScanNet 数据集上优于现有技术，实现了高保真且可仿真的场景重建。
selection_source: fresh_fetch
motivation: 传统的组合式重建方法侧重于视觉外观，在处理复杂现实场景时往往缺乏泛化能力，且生成的资产难以直接用于物理仿真。
method: 提出包含主动视点优化（优化单物体补全条件）和场景图合成器（指导仿真器内资产组装）的感知-生成-仿真流水线。
result: 在 ScanNet 数据集上的广泛实验表明，SimRecon 在视觉保真度和物理合理性方面均优于之前的最先进方法。
conclusion: 该研究通过衔接感知、生成与仿真，为从真实视频中构建可交互、高保真的 3D 场景提供了一种有效的组合式重建方案。
---

## 摘要
组合式场景重建旨在从真实视频中创建以物体为中心的表示，而非整体场景，这天然适用于仿真和交互。传统的组合式重建方法主要强调视觉外观，在真实场景中的泛化能力有限。在本文中，我们提出了 SimRecon，这是一个实现了针对杂乱场景重建的“感知-生成-仿真”流水线的框架，该框架首先从视频输入中进行场景级语义重建，然后进行单物体生成，最后在仿真器中组装这些资产。然而，简单地结合这三个阶段会导致生成资产的视觉失真以及最终场景在物理上的不合理性，这一问题在复杂场景中尤为严重。因此，我们进一步提出了三个阶段之间的两个衔接模块来解决这一问题。具体而言，针对从感知到生成的过渡（这对视觉保真度至关重要），我们引入了主动视点优化（Active Viewpoint Optimization），它在三维空间中主动搜索以获取最佳投影图像，作为单物体补全的条件。此外，针对从生成到仿真的过渡（这对物理合理性至关重要），我们提出了场景图合成器（Scene Graph Synthesizer），它引导三维仿真器中从零开始的构建，镜像了现实世界天然的构造原理。在 ScanNet 数据集上的广泛实验验证了我们的方法优于以往的最先进方法。

## Abstract
Compositional scene reconstruction seeks to create object-centric representations rather than holistic scenes from real-world videos, which is natively applicable for simulation and interaction. Conventional compositional reconstruction approaches primarily emphasize on visual appearance and show limited generalization ability to real-world scenarios. In this paper, we propose SimRecon, a framework that realizes a "Perception-Generation-Simulation" pipeline towards cluttered scene reconstruction, which first conducts scene-level semantic reconstruction from video input, then performs single-object generation, and finally assembles these assets in the simulator. However, naively combining these three stages leads to visual infidelity of generated assets and physical implausibility of the final scene, a problem particularly severe for complex scenes. Thus, we further propose two bridging modules between the three stages to address this problem. To be specific, for the transition from Perception to Generation, critical for visual fidelity, we introduce Active Viewpoint Optimization, which actively searches in 3D space to acquire optimal projected images as conditions for single-object completion. Moreover, for the transition from Generation to Simulation, essential for physical plausibility, we propose a Scene Graph Synthesizer, which guides the construction from scratch in 3D simulators, mirroring the native, constructive principle of the real world. Extensive experiments on the ScanNet dataset validate our method's superior performance over previous state-of-the-art approaches.

---

## 论文详细总结（自动生成）

这是一份关于论文《SimRecon: SimReady Compositional Scene Reconstruction from Real Videos》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 3D 场景重建（如 NeRF 或 3DGS）通常将场景视为一个整体，缺乏明确的物体边界和完整的几何形状，难以直接用于物理仿真和交互。
*   **核心挑战**：现有的组合式重建方法在处理真实世界的杂乱视频时，常面临两个瓶颈：
    1.  **视觉失真**：由于遮挡或视角不佳，生成的单体对象几何形状不完整或变形。
    2.  **物理不合理**：重建后的物体在仿真器中往往处于“漂浮”或“穿模”状态，缺乏真实的物理支撑关系。
*   **整体含义**：SimRecon 提出了一个“感知-生成-仿真”的完整流水线，旨在将真实视频转化为可直接用于物理仿真的、以物体为中心的 3D 场景。

### 2. 方法论
该框架引入了两个关键的衔接模块：
*   **主动视点优化 (Active Viewpoint Optimization, AVO)**：
    *   **核心思想**：利用信息论方法，在 3D 空间中搜索能提供最大信息增益的视角，为生成模型提供最佳的图像条件。
    *   **技术细节**：将信息增益建模为 3D 高斯投影中的累积不透明度（Accumulated Opacity）。通过最大化渲染图像的 Alpha 混合值，并结合深度正则化项（防止视点塌陷到物体内部），利用梯度下降优化相机位姿。
    *   **迭代扩展**：通过多轮优化并衰减已观察区域的权重，获取覆盖物体全貌的多个关键视角。
*   **场景图合成器 (Scene Graph Synthesizer, SGS)**：
    *   **核心思想**：通过视觉语言模型（VLM）逐步构建全局场景图，明确物体间的支撑（Supported by）和附着（Attached to）关系。
    *   **算法流程**：
        1.  **区域划分**：使用 DBSCAN 对物体进行空间聚类。
        2.  **局部推理**：利用 VLM 识别各区域内的物理关系。
        3.  **在线合并与冲突解决**：将局部子图合并为全局图，若发现逻辑冲突（如循环支撑），则重新采样视点进行裁决。
    *   **分层物理组装**：根据场景图的层级结构，在仿真器中按“先地面/墙体、后支撑物、再被支撑物”的顺序，利用物理引擎（如重力沉降）进行组装。

### 3. 实验设计
*   **数据集**：使用了真实的 **ScanNet** 数据集中的 20 个复杂室内场景。
*   **Benchmark 与对比方法**：
    *   **场景级重建对比**：DPRecon、InstaScene。
    *   **单视图生成对比**：Gen3DSR、SceneGen。
    *   **仿真创建对比**：MetaScenes。
*   **评估指标**：
    *   **几何质量**：倒角距离 (CD)、F-Score、法线一致性 (NC)。
    *   **渲染质量**：PSNR、SSIM、LPIPS（有参考）以及 MUSIQ（无参考）。
    *   **效率**：平均处理时间。

### 4. 资源与算力
*   **硬件环境**：实验使用了单张 **NVIDIA RTX A6000** GPU。
*   **耗时**：
    *   **主动视点优化**：每个物体大约需要 30 秒。
    *   **整体流程**：全场景重建耗时约 21 分钟，显著快于基于 SDF 的方法（如 DPRecon 需 10 小时以上），与 InstaScene 处于同一量级。

### 5. 实验数量与充分性
*   **实验规模**：在 20 个真实场景上进行了测试，涵盖了多种家具和杂乱布局。
*   **消融实验**：
    *   针对 **AVO**：对比了最大化 2D 可见性、无深度约束等变体，证明了 3D 信息增益驱动的必要性。
    *   针对 **SGS**：对比了全局直接推理、无冲突解决的简单合并，验证了渐进式合成的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅涵盖了视觉指标，还通过仿真器中的可视化展示了物理合理性。对比基准涵盖了最新的 SOTA 方法，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **几何保真度提升**：AVO 模块能有效解决遮挡问题，使生成的 3D 资产在几何完整性上远超启发式采样方法。
*   **物理稳定性**：通过 SGS 引导的构造式组装，成功消除了重建场景中常见的物体漂浮和穿模现象。
*   **端到端能力**：该框架实现了从原始 RGB 视频到 Isaac Sim/Blender 仿真环境的自动化转化，无需人工干预。

### 7. 优点
*   **衔接性强**：通过 AVO 和 SGS 两个模块，巧妙地解决了感知、生成、仿真三个阶段之间的“断层”问题。
*   **主动性**：AVO 改变了被动接受输入视角的模式，利用 3D 表达的主动搜索提升了生成质量。
*   **物理原生**：SGS 模拟了现实世界的建造逻辑，使得重建结果天然具备仿真属性（Sim-Ready）。

### 8. 不足与局限
*   **依赖底层模型**：重建质量高度依赖于所使用的 3D 生成模型（如 Rodin）和 VLM（如 Qwen2.5-VL）的性能。
*   **动态场景限制**：目前主要针对静态室内场景，对于包含动态物体或复杂交互过程的视频处理能力尚待验证。
*   **计算开销**：虽然比 SDF 方法快，但对于包含数十个物体的极复杂场景，逐个物体的 AVO 优化和生成仍存在一定的累计耗时。

（完）
