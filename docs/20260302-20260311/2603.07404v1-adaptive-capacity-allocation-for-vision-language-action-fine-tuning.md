---
title: Adaptive Capacity Allocation for Vision Language Action Fine-tuning
title_zh: 视觉语言动作微调的自适应容量分配
authors: "Donghoon Kim, Minji Bae, Unghui Nam, Gyeonghun Kim, Suyun Lee, Kyuhong Shim, Byonghyo Shim"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07404v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作模型的秩自适应微调
tldr: 针对视觉语言动作模型（VLA）在机器人任务微调中固定秩难以适应不同任务复杂度的问题，本文提出LoRA-SP（Select-Prune）。该方法通过SVD风格的参数化和基于能量目标的动态路由，实现了输入和层级的自适应容量分配。在真实机器人实验中，LoRA-SP在大幅减少参数量的同时，显著提升了多任务泛化性能，解决了传统LoRA在机器人领域表现不佳的痛点。
selection_source: fresh_fetch
motivation: 传统LoRA的固定秩难以满足机器人任务中高且多变的内在秩需求，导致多任务微调时性能受限且干扰严重。
method: 提出LoRA-SP，利用SVD参数化和能量目标驱动的路由器，动态选择和剪枝共享向量库中的活跃分量以实现自适应容量分配。
result: "在真实机器人操纵任务中，LoRA-SP比标准LoRA提升了高达31.6%的多任务成功率，且性能媲美甚至超过全量微调。"
conclusion: 动态秩分配是提升VLA模型在复杂物理AI任务中适应能力、减少任务干扰并增强泛化性能的有效途径。
---

## 摘要
视觉语言动作模型（VLA）越来越多地应用于具身智能（Physical AI），但将预训练的 VLA 模型部署到未见过的环境、机器人本体或任务中仍需要进行适配。参数高效微调（PEFT），特别是 LoRA，在 VLA 策略中非常常用，然而其暴露的容量调节参数——秩（rank），在迁移过程中表现并不统一：机器人领域的迁移相比语言微调表现出更高且随任务变化的内在秩。对于大语言模型（LLM），较小的秩（如 $r \in \{4,

## Abstract
Vision language action models (VLAs) are increasingly used for Physical AI, but deploying a pre-trained VLA model to unseen environments, embodiments, or tasks still requires adaptation. Parameter-efficient fine-tuning (PEFT), especially LoRA, is common for VLA policies, yet the exposed capacity knob, the rank, does not transfer uniformly: robotics transfer exhibits a higher and task-varying intrinsic rank than language fine-tuning. Small ranks suffice for LLMs (e.g., $r \in \{4, 8\}$), while spectral analyses indicate VLAs may require much larger ranks (e.g., $r \approx 128$) or near-full rank, a mismatch that worsens in multi-task settings. We present LoRA-SP (Select-Prune), a rank-adaptive fine-tuning method that replaces fixed-rank updates with input- and layer-wise capacity. LoRA-SP uses an SVD-style parameterization with a small router whose nonnegative scores act as singular values over a shared vector bank. The active set is chosen by an energy target on the cumulative squared scores $E(k) \ge η$, providing a direct link to approximation error via our spectral analysis. During training, $η$ concentrates energy on a few directions and teaches the router to rely on fewer vectors while preserving accuracy. This yields compact adapters that reduce cross-task interference and improve generalization. On four real-robot manipulation tasks collected on an unseen AgileX PiPER arm, across two VLA backbones ($π_0$ and SmolVLA), LoRA-SP matches or exceeds full fine-tuning with far fewer trainable parameters, and improves multi-task success by up to 31.6% over standard LoRA while remaining robust to rank choice.

---

## 论文详细总结（自动生成）

以下是对论文《Adaptive Capacity Allocation for Vision Language Action Fine-tuning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**视觉-语言-动作模型（VLA）**在具身智能（Physical AI）中的微调效率与泛化问题。
*   **研究动机**：虽然 LoRA 等参数高效微调（PEFT）方法在 LLM 中表现优异，但在 VLA 模型中存在“秩错配”问题。LLM 通常只需要极小的秩（如 4 或 8）即可完成微调，但 VLA 在面对未见过的机器人本体（Embodiment）或复杂环境时，其**内在秩（Intrinsic Rank）**显著更高且随任务变化（通常需要 $r \approx 128$）。
*   **核心问题**：传统的固定秩 LoRA 在多任务场景下会导致严重的“任务干扰”，且难以平衡计算效率与模型容量。

### 2. 方法论：LoRA-SP (Select-Prune)
论文提出了一种名为 **LoRA-SP** 的秩自适应微调方法，其核心思想是根据输入和层级动态分配容量。
*   **核心思想**：将 LoRA 的权重更新参数化为类似 SVD（奇异值分解）的形式，即 $\Delta W = U \text{diag}(s(x)) V$。其中 $U$ 和 $V$ 是共享的向量库，而 $s(x)$ 是由一个轻量级**路由器（Router）**生成的、随输入 $x$ 变化的“奇异值”分数。
*   **关键技术细节**：
    *   **选择（Select）**：路由器为每个输入生成向量权重。系统计算累积能量 $E(k)$（前 $k$ 个奇异值平方和占总和的比例），并选择满足 $E(k) \ge \eta$（能量阈值，如 0.9）的最小 $k$ 个向量。
    *   **剪枝（Prune）**：在推理时，仅激活这 $k$ 个向量，其余置零，从而实现输入相关的动态秩分配。
    *   **光谱损失（Spectral Loss）**：引入 $L_{spec} = 1 - E_k(x)$，强制能量集中在少数关键方向上，促使模型在保持精度的同时学习更紧凑的表示。

### 3. 实验设计
*   **数据集/场景**：在真实的 **AgileX PiPER（7自由度）** 机器人手臂上收集了 4 个操纵任务：开锅盖（Open）、倒方块（Pour）、按按钮（Press）、抓放葡萄（Pick-Place）。共包含 480 个演示视频（双视角 RGB）。
*   **基座模型（Backbones）**：使用了高容量的 **$\pi_0$ (3.5B)** 和轻量级的 **SmolVLA**。
*   **对比方法（Benchmarks）**：
    *   全量微调（Full Fine-tuning）。
    *   标准 LoRA（不同秩 $r \in \{16, 32, 64, 128\}$）。
    *   AdaLoRA（基于重要性分数的自适应分配）。
    *   LoRA-MoE（混合专家模型风格的 LoRA）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **效率暗示**：文中提到该方法旨在实现“可负担且高效”的机器人微调，且 LoRA-SP 的可训练参数量仅占总量的 9.2% 左右（对于 $\pi_0$ 模型），暗示其对算力的需求远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单任务微调、多任务微调、跨模型基座对比以及详细的消融实验。
*   **充分性评价**：实验设计较为充分。通过对比不同秩下的性能曲线，客观地展示了 VLA 对高秩的需求；消融实验验证了光谱损失和能量阈值 $\eta$ 对性能的影响。实验在真实机器人而非仅在仿真环境中进行，增强了结果的说服力。

### 6. 主要结论与发现
*   **性能提升**：在多任务设置下，LoRA-SP 的成功率比标准 LoRA 提升了高达 **31.6%**，且在参数量极少的情况下达到了与全量微调相当甚至更好的水平。
*   **秩的异质性**：实验发现 VLA 的不同模块对容量需求不同：**视觉塔（Vision Tower）**通常需要极高的秩，而语言和动作模块需要的秩相对较低。
*   **鲁棒性**：LoRA-SP 对初始秩的选择不敏感，能够自动收敛到合适的有效秩，解决了手动调参的痛苦。

### 7. 优点
*   **动态性**：首次在 VLA 领域实现了输入相关的层级秩分配，有效缓解了多任务间的干扰。
*   **理论支撑**：利用光谱分析（Spectral Analysis）建立了能量目标与近似误差之间的直接联系，方法论具有数学严谨性。
*   **实用性**：在未见过的机器人本体（OOD Embodiment）上表现强劲，证明了其强大的迁移能力。

### 8. 不足与局限
*   **路由器开销**：虽然路由器很小，但在每一层都增加路由计算可能会在极大规模模型中引入额外的推理延迟（文中未详细量化延迟）。
*   **任务覆盖面**：实验主要集中在短程操纵任务，对于需要长程逻辑推理或更复杂交互的任务，该方法的表现尚待验证。
*   **超参数依赖**：虽然对秩不敏感，但引入了新的超参数 $\eta$（能量阈值），虽然文中给出了推荐值，但在极端不同的任务分布下可能仍需微调。

（完）
