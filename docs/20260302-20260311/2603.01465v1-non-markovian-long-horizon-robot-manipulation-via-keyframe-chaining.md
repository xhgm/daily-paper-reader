---
title: Non-Markovian Long-Horizon Robot Manipulation via Keyframe Chaining
title_zh: 通过关键帧链接实现的非马尔可夫长程机器人操作
authors: "Yipeng Chen, Wentao Tan, Lei Zhu, Fengling Li, Jingjing Li, Guoli Yang, Heng Tao Shen"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01465v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于长程机器人操作的视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在长程任务中难以处理非马尔可夫依赖的问题，本文提出了Keyframe-Chaining VLA框架。该框架通过自动关键帧选择器识别状态转换，并利用进度感知查询机制动态检索历史关键帧，将其作为视觉标记整合进模型中。在ManiSkill模拟器的非马尔可夫任务中，该方法显著提升了机器人处理长程时间依赖任务的成功率，为复杂操作任务提供了有效的时序建模方案。
selection_source: fresh_fetch
motivation: 现有VLA模型过度依赖即时观测，难以捕捉动作取决于特定过去状态而非当前状态的非马尔可夫长程依赖。
method: 提出一种关键帧链式框架，利用自动选择器识别状态转换，并通过进度感知查询机制动态检索并整合历史关键帧作为策略输入。
result: 在ManiSkill模拟器上构建的四项非马尔可夫操作任务中，该方法取得了优于现有基准模型的任务成功率。
conclusion: 通过显式链接历史关键帧，该研究证明了增强时序上下文感知能有效提升机器人处理长程复杂任务的能力。
---

## 摘要
现有的视觉-语言-动作（VLA）模型由于过度依赖即时观测，往往难以泛化到长程任务。虽然最近的研究通过引入检索机制或扩展上下文窗口来处理程序性任务，但它们通常难以捕捉非马尔可夫依赖关系，即最优动作依赖于特定的过去状态而非仅取决于当前观测。为此，我们提出了关键帧链接 VLA（Keyframe-Chaining VLA），这是一个通过提取并链接关键历史帧来建模长程依赖关系的框架。具体而言，我们提出了一种自动关键帧选择器，它通过学习判别性嵌入空间来有效识别不同的状态转换。为了捕捉任务关键信息，我们设计了一种进度感知查询机制，根据历史帧与当前执行阶段的时间相关性动态检索历史帧。这些选定的关键帧作为交错的视觉标记集成到 VLA 中，将策略显式地锚定在长程时间上下文中。最后，我们推出了一套基于 ManiSkill 模拟器构建的四个非马尔可夫操作任务，用于衡量任务成功率。实验结果表明，我们的方法取得了优异的性能，有效地解决了具有长程时间依赖特征的机器人操作任务。代码可在 https://github.com/cytoplastm/KC-VLA 获取。

## Abstract
Existing Vision-Language-Action (VLA) models often struggle to generalize to long-horizon tasks due to their heavy reliance on immediate observations. While recent studies incorporate retrieval mechanisms or extend context windows to handle procedural tasks, they often struggle to capture Non-Markovian dependencies, where optimal actions rely solely on specific past states rather than the current observation. To address this, we introduce Keyframe-Chaining VLA, a framework that extracts and links key historical frames to model long-horizon dependencies. Specifically, we propose an automatic keyframe selector that learns a discriminative embedding space, effectively identifying distinct state transitions. To capture task-critical information, we design a progress-aware query mechanism that dynamically retrieves historical frames based on their temporal relevance to the current execution phase. These selected keyframes are integrated into the VLA as interleaved visual tokens, explicitly grounding the policy in the long-horizon temporal context. Finally, we introduce a suite of four Non-Markovian manipulation tasks built upon the ManiSkill simulator to measure task success rates. Experimental results demonstrate that our method achieves superior performance, effectively tackling robot manipulation tasks characterized by long-horizon temporal dependencies. Code is available at https://github.com/cytoplastm/KC-VLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《Non-Markovian Long-Horizon Robot Manipulation via Keyframe Chaining》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型大多遵循“马尔可夫假设”，即认为当前观测足以决定下一步动作。但在长程（Long-horizon）任务中，存在**状态混淆（State Aliasing）**问题，即当前的视觉画面无法反映关键的历史信息（例如：盐是否已经加过？三个方块最初的堆叠顺序是什么？）。
*   **核心问题**：如何在不显著增加计算开销（避免长上下文注意力机制的二次方复杂度）的前提下，让机器人具备处理**非马尔可夫（Non-Markovian）**依赖的能力，使其能够“记住”数千步之前的关键状态。

### 2. 论文提出的方法论
论文提出了 **Keyframe-Chaining VLA** 框架，其核心思想是将时间抽象与动作生成解耦，通过提取稀疏的语义关键帧来构建全局时序上下文。

*   **关键帧选择模块 (KSM)**：采用两阶段训练架构。
    *   **阶段 I：统一多任务度量学习**。使用 ResNet-18 作为编码器，通过三元组损失（Triplet Margin Loss）学习判别性嵌入空间。它将关键帧作为锚点，强制模型区分时间邻居、同任务不同阶段以及不同任务的特征。
    *   **阶段 II：任务调制查询匹配**。利用 FiLM（特征线性调制）根据语言指令生成任务特定的查询（Query），通过交叉注意力机制在滑动窗口中检测语义里程碑。
*   **贪婪时间平滑机制**：为了防止实时检测中的抖动，引入验证窗口，只有在分数持续稳定后才将候选帧存入历史缓冲区。
*   **关键帧链式策略**：将检测到的稀疏关键帧序列与当前观测拼接，作为 VLA 模型（以 GR00T-N1.5 为基座）的输入。通过结构化系统提示词（Prompt），引导模型根据历史关键帧推断任务进度并生成动作。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：基于 ManiSkill 模拟器构建了专门的“长程记忆依赖基准测试”，包含四类非马尔可夫任务：空间重构（需记住初始堆叠顺序）、时序排序（严格按颜色顺序操作）、计数与延迟（需等待信号灯闪烁两次）、身份追踪（在物体交换位置后识别目标）。
    *   **真实世界**：使用 AgileX Piper 机械臂进行部署，涵盖上述四类任务的实物版本。
*   **对比方法**：
    *   **Diffusion Policy (DP)**：领先的模仿学习方法。
    *   **$\pi_0$ (Flow Matching)**：最先进的通用 VLA 模型。
    *   **GR00T-N1.5**：分别测试了无历史、短时密集历史、长时固定步长采样历史等多种配置。

### 4. 资源与算力
*   **算力设备**：实验使用了单张 **NVIDIA L40 GPU**。
*   **训练细节**：
    *   VLA 策略微调：50,000 步，Batch Size 为 16，采用 AdamW 优化器。
    *   KSM 模块：阶段 I 训练 30 轮（Epochs），阶段 II 训练 50 轮。
    *   模型规模：VLM 基座为 3B 参数，动作头（DiT）为 0.5B 参数。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖 400 个演示视频（每项任务 100 个）；真实世界实验每项任务进行 20 次测试。
*   **消融实验**：针对 KSM 的训练范式（对比端到端训练与两阶段训练）、提示词精炼（Prompt Refinement）的影响进行了详细消融。
*   **充分性评价**：实验设计较为充分。通过对比不同采样频率（Fixed Stride）的基准模型，客观地证明了固定步长采样在处理不同频率信号（如快速闪烁的灯与长期的空间记忆）时的局限性，从而突出了本文“事件驱动”采样方案的公平性与优越性。

### 6. 论文的主要结论与发现
*   **性能突破**：在非马尔可夫任务中，该方法达到了 **92.0%** 的平均成功率，远超最强基准模型（57.0%）。
*   **解决时序权衡**：传统的固定步长采样存在“视野”与“分辨率”的矛盾，而关键帧链接机制通过语义提取，实现了全局时间感受野且无冗余计算。
*   **非马尔可夫处理能力**：实验证明，显式地提供历史关键帧能有效消除状态混淆，使模型在视觉场景看似相同的情况下（如灯灭期间），能根据历史记忆做出正确决策。

### 7. 优点（亮点）
*   **架构解耦**：KSM 模块轻量且独立，无需修改 VLA 主干网络即可增强其长程推理能力。
*   **任务调制**：引入 FiLM 机制使关键帧提取具备任务感知能力，能过滤掉与当前任务无关的动作噪声。
*   **基准贡献**：填补了现有机器人数据集（如 LIBERO, CALVIN）多为马尔可夫任务的空白，提出了更具挑战性的非马尔可夫基准。

### 8. 不足与局限
*   **存储冗余**：目前直接存储原始图像关键帧，随着任务长度增加，像素空间的线性增长可能带来内存压力。
*   **关键帧标注依赖**：训练 KSM 仍需要一定程度的关键帧标注（虽然文中提到部分可通过本体感受信号自动获取），对于极度复杂的非结构化任务，标注成本可能较高。
*   **动态更新缺失**：目前采用的是简单的追加模式，缺乏对历史缓冲区进行动态压缩或遗忘的机制。

（完）
