---
title: "Critic in the Loop: A Tri-System VLA Framework for Robust Long-Horizon Manipulation"
title_zh: 环路中的评论者：一种用于鲁棒长程操作的三系统 VLA 框架
authors: "Pengfei Yi, Yingjie Ma, Wenjiang Xu, Yanan Hao, Shuai Gan, Wanting Li, Shanlin Zhong"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05185v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于鲁棒长程机器人操作的三系统VLA框架
tldr: 针对视觉机器人操作中高层推理与底层控制的平衡难题，本文提出了一种名为“Critic in the Loop”的三系统VLA框架。该框架结合了负责全局推理的VLM、负责快速执行的VLA以及负责动态调度的轻量级视觉Critic。Critic通过实时监控工作空间，在常规任务中维持VLA的高效执行，并在检测到异常时触发VLM重规划，显著提升了长程任务的鲁棒性和自主性。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型推理延迟高，而快速的动作模型在复杂长程任务中缺乏语义深度，难以兼顾实时性与鲁棒性。
method: 提出由VLM大脑、VLA小脑和视觉Critic组成的三系统架构，利用Critic动态调度控制权并在检测到失败时触发重规划。
result: 在长程操作基准测试中，该方法在显著减少昂贵VLM调用的同时，达到了最先进的性能并增强了分布外场景的鲁棒性。
conclusion: 该框架通过视觉引导的动态调度机制，成功弥合了机器人操作中高层认知规划与底层反应式控制之间的鸿沟。
---

## 摘要
在视觉机器人操作中，平衡高层语义推理与底层反应式控制仍然是一个核心挑战。虽然视觉语言模型（VLM）擅长认知规划，但其推理延迟阻碍了实时执行。相反，快速的视觉-语言-动作（VLA）模型通常缺乏复杂长程任务所需的语义深度。为了弥补这一差距，我们引入了“环路中的评论者”（Critic in the Loop），这是一种由动态 VLM-专家调度驱动的自适应分层框架。其核心是一个仿生三系统架构，包括用于全局推理的 VLM 大脑、用于反应式执行的 VLA 小脑以及一个轻量级的视觉评论者（Critic）。通过持续监控工作空间，评论者动态地路由控制权。它通过 VLA 维持常规子任务的快速闭环执行，并在检测到任务停滞或失败等执行异常时，自适应地触发 VLM 进行重新规划。此外，我们的架构无缝集成了受人类启发的规则，以直观地打破无限重试循环。这种基于视觉的调度最大限度地减少了昂贵的 VLM 查询，同时显著增强了系统在分布外（OOD）场景中的鲁棒性和自主性。在具有挑战性的长程操作基准测试上的全面实验表明，我们的方法达到了最先进的性能。

## Abstract
Balancing high-level semantic reasoning with low-level reactive control remains a core challenge in visual robotic manipulation. While Vision-Language Models (VLMs) excel at cognitive planning, their inference latency precludes real-time execution. Conversely, fast Vision-Language-Action (VLA) models often lack the semantic depth required for complex, long-horizon tasks. To bridge this gap, we introduce Critic in the Loop, an adaptive hierarchical framework driven by dynamic VLM-Expert scheduling. At its core is a bionic Tri-System architecture comprising a VLM brain for global reasoning, a VLA cerebellum for reactive execution, and a lightweight visual Critic. By continuously monitoring the workspace, the Critic dynamically routes control authority. It sustains rapid closed-loop execution via the VLA for routine subtasks, and adaptively triggers the VLM for replanning upon detecting execution anomalies such as task stagnation or failures. Furthermore, our architecture seamlessly integrates human-inspired rules to intuitively break infinite retry loops. This visually-grounded scheduling minimizes expensive VLM queries, while substantially enhancing system robustness and autonomy in out-of-distribution (OOD) scenarios. Comprehensive experiments on challenging, long-horizon manipulation benchmarks reveal that our approach achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Critic in the Loop: A Tri-System VLA Framework for Robust Long-Horizon Manipulation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在机器人操作中平衡**高层语义推理**（聪明但慢）与**底层反应式控制**（快但缺乏深度）。
*   **研究背景**：
    *   现有的视觉-语言-动作（VLA）模型通常遵循 Kahneman 的双系统理论：系统 2（VLM）负责规划，系统 1（VLA）负责执行。
    *   **瓶颈**：切换机制往往是固定的或启发式的，导致在平滑执行时浪费计算资源，而在发生干扰时反应迟钝。此外，处理罕见故障通常需要昂贵的特定任务数据。
*   **整体含义**：提出一种“三系统”架构，引入一个独立的“评论者”（Critic）作为系统 3，动态决定何时需要“思考”（调用 VLM），从而实现高效且鲁棒的长程任务处理。

### 2. 方法论
*   **核心思想**：通过异步事件驱动的调度机制，将认知推理与连续控制解耦。
*   **三系统架构**：
    1.  **系统二（大脑/VLM）**：基于 PaliGemma，负责全局推理，根据全局指令和 Critic 提供的短期记忆（如“任务完成”或“发生意外”）生成语义子任务（如“抓取蓝色杯子”）。
    2.  **系统一（小脑/VLA）**：基于流匹配（Flow Matching）的连续动作生成网络，根据子任务指令实时生成动作块（Action Chunks），实现高频闭环控制。
    3.  **系统三（评论者/Critic）**：基于轻量级 VLM（Florence-2），通过视觉问答（VQA）形式持续监控进度。
*   **关键技术细节**：
    *   **进度评估**：使用蒙特卡洛价值估计，将子任务进度离散化为 101 个 bin（-1.0 到 0.0）。
    *   **异常检测**：训练 Critic 在检测到物理失败（如物体掉落）时输出特定语义标记 `<aci>`。
    *   **动态调度算法**：当满足“子任务完成”、“检测到异常”或“执行停滞（超时）”任一条件时，触发系统二重规划。
    *   **自动标注流水线**：利用 RDP 几何算法提取关键帧，结合大模型（Qwen2-VL）自动生成子任务标签，解决长程任务标注昂贵的问题。

### 3. 实验设计
*   **硬件平台**：Cobot Magic ALOHA 双臂机器人（14 自由度），配备三个 RealSense 摄像头。
*   **实验场景**：
    1.  **整理餐具**：包括顺序放置、乱序放置、分布外（OOD）左侧杯子操作、人为干扰（撞倒杯子）。
    2.  **清理桌面**：涉及变形体操作（打开塑料袋、装入瓶子和纸巾）。
*   **对比方法（Benchmark）**：
    *   **单系统 $\pi_{0.5}$**：直接从观察预测动作。
    *   **双系统 $\pi_{0.5}$**：每个动作块都生成子任务，无 Critic 监控。
    *   **三系统 $\pi_{0.5}$（本文方法）**。

### 4. 资源与算力
*   **模型规模**：系统二使用 PaliGemma（约 3B），系统三使用 Florence-2-base（约 0.2B）。
*   **训练细节**：使用 AdamW 优化器，学习率 $1 \times 10^{-6}$，训练 50 个 epoch，Batch Size 为每张 GPU 64。
*   **算力说明**：文中提到“per GPU”，暗示使用了多 GPU 并行训练，但未明确给出具体的 GPU 型号（如 A100 或 H100）及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：每个场景进行 10 次真实世界测试。
*   **消融实验**：针对 OOD 泛化能力做了 4 组对比实验，分析了提示词格式、数据分布和启发式规则的影响。
*   **充分性评价**：实验覆盖了长程任务、变形体操作、人为干扰和 OOD 场景，具有较强的说服力。虽然每组 10 次测试在统计学上样本量较小（机器人领域的常见限制），但对比结果差异显著，客观反映了系统的优劣。

### 6. 主要结论与发现
*   **性能领先**：三系统架构在所有场景下均显著优于单系统和双系统基线，尤其在复杂长程任务（清理桌面）中优势巨大。
*   **OOD 鲁棒性**：通过 Critic 检测停滞并触发“姿态重置”规则，系统能有效打破无限重试循环，实现在未见过的位置使用未训练过的手臂完成任务。
*   **效率提升**：异步调度机制避免了 VLM 的推理延迟瓶颈，使系统能保持 20Hz 的控制频率。

### 7. 优点
*   **架构创新**：引入第三系统（Critic）作为调度核心，解决了“何时思考”的根本问题。
*   **鲁棒性强**：结合了数据驱动的策略与人类启发式的规则（如停滞重置），有效应对分布外场景。
*   **自动化程度高**：提出的自动标注流水线大幅降低了长程任务的数据准备成本。
*   **轻量化**：Critic 仅 0.2B 参数，保证了实时监控的可行性。

### 8. 不足与局限
*   **推理瓶颈**：消融实验显示，系统二（大脑）在 OOD 场景下仍存在分布过拟合，无法始终生成完美的指令，高层推理能力仍受限于基础 VLM 的性能。
*   **数据依赖**：虽然有自动标注，但仍依赖于专家演示数据，缺乏通过强化学习（RL）进行自我进化的能力。
*   **应用限制**：目前主要在受控的桌面操作环境中测试，在更复杂、动态变化的开放世界环境中的表现尚待验证。

（完）
