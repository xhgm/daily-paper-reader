---
title: "IMR-LLM: Industrial Multi-Robot Task Planning and Program Generation using Large Language Models"
title_zh: IMR-LLM：基于大语言模型的工业多机器人任务规划与程序生成
authors: "Xiangyu Su, Juzhan Xu, Oliver van Kaick, Kai Xu, Ruizhen Hu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02669v1"
tags: ["query:q20"]
score: 9.0
evidence: 大语言模型驱动的工业多机器人任务规划与操作
tldr: 针对工业多机器人协作中复杂的时序约束和任务依赖挑战，本文提出 IMR-LLM 框架。该框架利用大语言模型辅助构建析取图，结合确定性求解算法生成高效的高层任务规划，并利用过程树指导底层可执行程序的生成。此外，研究者还发布了包含三个复杂度等级的 IMR-Bench 基准测试。实验证明，该方法在多项评估指标上均显著优于现有方案，展现了在复杂工业任务中的应用潜力。
selection_source: fresh_fetch
motivation: 工业生产中多机器人协作面临严苛的时序约束和复杂的任务依赖，传统 LLM 方法难以直接应对这些挑战。
method: 提出 IMR-LLM 框架，通过 LLM 辅助构建析取图并结合确定性求解器生成高层规划，再利用过程树引导底层程序生成。
result: 在涵盖三种复杂度的 IMR-Bench 基准测试中，该方法在所有评估指标上均显著优于现有基准方法。
conclusion: IMR-LLM 成功将 LLM 的推理能力与确定性求解方法相结合，为工业多机器人任务规划和程序生成提供了高效且可靠的方案。
---

## 摘要
在现代工业生产中，多台机器人通常协作完成复杂的制造任务。大语言模型（LLMs）凭借其强大的推理能力，在协调机器人执行简单的家庭和操作任务方面展现出了潜力。然而，在工业场景中，更严格的时序约束和任务内更复杂的依赖关系给 LLMs 带来了新的挑战。为了解决这一问题，我们提出了 IMR-LLM，这是一种新型的由 LLM 驱动的工业多机器人任务规划与程序生成框架。具体而言，我们利用 LLMs 辅助构建析取图（disjunctive graphs），并采用确定性求解方法来获得可行且高效的高层任务规划。在此基础上，我们使用过程树（process tree）引导 LLMs 生成可执行的底层程序。此外，我们创建了 IMR-Bench，这是一个具有挑战性的基准测试，涵盖了三个复杂度级别的多机器人工业任务。实验结果表明，我们的方法在所有评估指标上均显著优于现有方法。

## Abstract
In modern industrial production, multiple robots often collaborate to complete complex manufacturing tasks. Large language models (LLMs), with their strong reasoning capabilities, have shown potential in coordinating robots for simple household and manipulation tasks. However, in industrial scenarios, stricter sequential constraints and more complex dependencies within tasks present new challenges for LLMs. To address this, we propose IMR-LLM, a novel LLM-driven Industrial Multi-Robot task planning and program generation framework. Specifically, we utilize LLMs to assist in constructing disjunctive graphs and employ deterministic solving methods to obtain a feasible and efficient high-level task plan. Based on this, we use a process tree to guide LLMs to generate executable low-level programs. Additionally, we create IMR-Bench, a challenging benchmark that encompasses multi-robot industrial tasks across three levels of complexity. Experimental results indicate that our method significantly surpasses existing methods across all evaluation metrics.

---

## 论文详细总结（自动生成）

这是一份关于论文《IMR-LLM: Industrial Multi-Robot Task Planning and Program Generation using Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在复杂的工业生产线环境中，利用大语言模型（LLM）实现多机器人的任务规划（分解、分配、调度）与底层可执行程序的自动生成。
*   **研究动机**：
    *   **工业场景的严苛性**：与家庭环境不同，工业任务具有极强的时序约束（如必须先焊接再打磨）和复杂的资源竞争（多个机器人竞争同一工位或设备）。
    *   **LLM 的局限性**：纯 LLM 在处理长序列依赖和资源冲突时容易产生幻觉或逻辑错误，且直接生成的代码往往难以适配具体的工业环境约束。
    *   **现有方法不足**：现有的多机器人协作研究多集中于简单的家务任务，缺乏对工业级调度优化和程序执行鲁棒性的关注。

### 2. 核心方法论
IMR-LLM 框架将任务分为两个主要阶段：

*   **高层任务规划（Task Planning）**：
    *   **析取图（Disjunctive Graph）构建**：利用 LLM 的推理能力（CoT）从自然语言指令中提取操作集合、机器人分配和工件内的操作顺序。
    *   **确定性求解**：将 LLM 提取的信息转化为析取图模型（包含顶点、顺序弧、机器冲突弧和机器人冲突弧），随后使用确定性的启发式算法（如 FIFO）求解，生成无冲突、高效率的调度图。这种方法结合了 LLM 的语义理解能力和传统算法的严谨性。
*   **底层程序生成（Program Generation）**：
    *   **操作过程树（Operation Process Tree）**：从示例程序中提取通用的操作逻辑，构建一棵树状结构。树节点包含功能描述和代码片段，分支代表不同环境下的执行变体（如固定相机 vs 手持相机）。
    *   **路径引导生成**：LLM 不再是盲目写代码，而是根据当前场景在过程树中选择合适的路径，拼接代码片段生成 Python 程序。这显著提高了代码的执行成功率和可重用性。

### 3. 实验设计
*   **基准测试（IMR-Bench）**：基于 KunWu 仿真平台创建，包含 23 个真实工业场景，50 个制造任务。
*   **任务复杂度分级**：
    1.  **单机器人任务**：1 台机器人，≤5 个操作。
    2.  **简单多机器人任务**：≤3 台机器人，≤10 个操作。
    3.  **复杂多机器人任务**：≤7 台机器人，≤24 个操作。
*   **对比方法**：
    *   **SMART-LLM**：直接生成调度和代码。
    *   **LaMMA-P**：使用 PDDL 规划器，本文将其与不同代码生成模块结合（LaMMA-S/O）。
    *   **LiP-LLM**：结合线性规划和依赖图的方法（LiP-O）。
*   **评估指标**：操作一致性（OC）、调度效率（SE）、可执行性（Exe）、目标条件召回率（GCR）和最终成功率（SR）。

### 4. 资源与算力
*   **模型使用**：实验主要使用了 **GPT-4o** 作为核心推理引擎，并对比测试了开源模型 **Qwen3-32B-Thinking**。
*   **算力细节**：文中**未明确说明**具体的 GPU 训练时长或数量。由于该框架主要基于 Prompt Engineering 和 In-context Learning，不涉及大规模模型微调，因此算力消耗主要集中在 LLM API 的推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 50 组不同难度的仿真任务，并进行了**消融实验**（验证析取图和过程树的必要性）。
*   **充分性评价**：
    *   **客观性**：通过引入多种对比基准和不同能力的 LLM（GPT vs Qwen），证明了框架的普适性。
    *   **公平性**：所有对比方法均在相同的 IMR-Bench 环境下运行，并使用了相同的底层技能库。
    *   **真实性**：除了仿真实验，还包含了一个包含 3 台机器人的**真实世界实验**，验证了从仿真到现实的迁移能力。

### 6. 主要结论与发现
*   **混合驱动更优**：将 LLM 的语义推理与确定性算法（析取图求解）结合，在处理复杂工业调度时远比纯 LLM 规划更可靠。
*   **结构化引导至关重要**：引入“过程树”后，代码生成的可执行性显著提升，尤其是在复杂场景下，它能有效减少 LLM 的逻辑幻觉。
*   **性能领先**：在复杂多机器人任务中，IMR-LLM 的成功率（SR）显著高于现有方法（如在复杂任务中 SR 达到 0.68，而对比方法最高仅为 0.24）。

### 7. 优点与亮点
*   **可解释性强**：通过析取图展示调度逻辑，方便工程师审查和验证工业流程。
*   **模块化与扩展性**：过程树允许通过简单添加节点来支持新的工业操作，无需重新训练模型。
*   **解决资源冲突**：显式建模了机器人和机器的互斥访问，这是工业场景落地的核心痛点。

### 8. 不足与局限
*   **场景描述依赖**：框架仍高度依赖于人工提供的 JSON 格式场景描述，若场景感知出现偏差，规划将失效。
*   **长上下文压力**：在极度复杂的任务中（操作数更多时），LLM 仍可能因上下文过长产生逻辑不一致，导致程序生成失败。
*   **缺乏闭环反馈**：目前的系统主要是一次性生成，虽然提到了未来将引入执行反馈，但当前版本在遇到突发执行错误时缺乏实时重规划能力。

（完）
