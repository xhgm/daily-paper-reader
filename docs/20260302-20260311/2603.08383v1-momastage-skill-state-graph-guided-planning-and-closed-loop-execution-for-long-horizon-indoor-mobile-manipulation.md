---
title: "MoMaStage: Skill-State Graph Guided Planning and Closed-Loop Execution for Long-Horizon Indoor Mobile Manipulation"
title_zh: MoMaStage：面向长程室内移动操作的技能-状态图引导规划与闭环执行
authors: "Chenxu Li, Zixuan Chen, Yetao Li, Jiapeng Xu, Hongyu Ding, Jieqi Shi, Jing Huo, Yang Gao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08383v1"
tags: ["query:q20"]
score: 10.0
evidence: 用于室内移动操作的视觉语言框架
tldr: 针对长程室内移动操作（MoMA）中存在的逻辑不一致和累积误差问题，本文提出了MoMaStage框架。该框架通过将视觉语言模型（VLM）与分层技能库及拓扑感知的技能状态图相结合，在无需显式场景建模的情况下，实现了受限且逻辑一致的任务规划。配合闭环执行机制和语义重规划，MoMaStage在仿真和现实环境中均显著提升了长程任务的成功率并降低了计算开销。
selection_source: fresh_fetch
motivation: 现有的移动操作方法在长程任务中容易出现逻辑错误，且依赖显式场景表示的方法在动态环境中缺乏灵活性。
method: 提出一种基于技能状态图引导的VLM规划框架，结合分层技能库和闭环执行机制，通过状态反馈触发语义重规划。
result: 实验表明，MoMaStage在规划成功率、任务完成率及Token效率方面均优于现有最先进的方法。
conclusion: MoMaStage通过结构化引导和闭环反馈，有效解决了长程移动操作中的逻辑一致性与鲁棒性难题。
---

## 摘要
室内移动操作（MoMA）使机器人能够将自然语言指令转化为物理动作，但由于级联误差和在多样化环境中的泛化能力有限，长程执行仍然具有挑战性。基于学习的方法通常难以在长时程内保持逻辑一致性，而依赖显式场景表示的方法则引入了僵化的结构假设，降低了在动态环境中的适应性。为了解决这些局限性，我们提出了 MoMaStage，这是一个用于长程 MoMA 的结构化视觉-语言框架，它消除了对显式场景建图的需求。MoMaStage 将视觉-语言模型（VLM）植根于分层技能库和拓扑感知的技能-状态图中，将任务分解和技能组合约束在可行的转移空间内。这种结构化植根确保了生成的规划在逻辑上保持一致，并且相对于智能体不断演变的物理状态在拓扑上是有效的。为了增强鲁棒性，MoMaStage 结合了闭环执行机制，该机制监控本体感受反馈，并在检测到偏差时触发受图约束的语义重规划，从而保持规划技能与物理结果之间的一致性。在物理丰富的仿真和真实世界环境中的广泛实验表明，MoMaStage 优于最先进的基准方法，实现了显著更高的规划成功率，减少了 token 开销，并显著提高了长程移动操作中的整体任务成功率。视频演示可在项目网站上找到：https://chenxuli-cxli.github.io/MoMaStage/。

## Abstract
Indoor mobile manipulation (MoMA) enables robots to translate natural language instructions into physical actions, yet long-horizon execution remains challenging due to cascading errors and limited generalization across diverse environments. Learning-based approaches often fail to maintain logical consistency over extended horizons, while methods relying on explicit scene representations impose rigid structural assumptions that reduce adaptability in dynamic settings. To address these limitations, we propose MoMaStage, a structured vision-language framework for long-horizon MoMA that eliminates the need for explicit scene mapping. MoMaStage grounds a Vision-Language Model (VLM) within a Hierarchical Skill Library and a topology-aware Skill-State Graph, constraining task decomposition and skill composition within a feasible transition space. This structured grounding ensures that generated plans remain logically consistent and topologically valid with respect to the agent's evolving physical state. To enhance robustness, MoMaStage incorporates a closed-loop execution mechanism that monitors proprioceptive feedback and triggers graph-constrained semantic replanning when deviations are detected, maintaining alignment between planned skills and physical outcomes. Extensive experiments in physics-rich simulations and real-world environments demonstrate that MoMaStage outperforms state-of-the-art baselines, achieving substantially higher planning success, reducing token overhead, and significantly improving overall task success rates in long-horizon mobile manipulation. Video demonstrations are available on the project website: https://chenxuli-cxli.github.io/MoMaStage/.

---

## 论文详细总结（自动生成）

这是一份关于论文《MoMaStage: Skill-State Graph Guided Planning and Closed-Loop Execution for Long-Horizon Indoor Mobile Manipulation》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于**室内长程移动操作（MoMA）**。这类任务（如“从厨房拿盘子放到餐桌”）要求机器人具备长时程的导航与操作能力。
*   **研究动机**：
    *   **VLM 的局限性**：纯视觉语言模型（VLM）驱动的智能体在长程规划时容易产生“物理幻觉”，生成语义合理但物理上不可行的指令（如在手部已抓取物体时再次尝试抓取）。
    *   **误差累积**：长程任务中，微小的执行偏差会随时间级联放大，导致最终失败。
    *   **现有方法的不足**：端到端学习难以泛化至长程任务；而基于显式场景建图的方法过于僵化，难以应对动态环境且计算开销大。
*   **核心目标**：在无需显式场景建图的前提下，通过结构化约束提升 VLM 规划的逻辑一致性，并实现鲁棒的闭环执行。

### 2. 方法论：核心思想与关键技术
MoMaStage 提出了一个结合**结构化技能植根**与**闭环反馈**的框架：
*   **分层技能库（Hierarchical Skill Library）**：
    *   **动作级技能**：底层的原子动作（如关节控制）。
    *   **语义级技能**：与场景语义耦合的技能（如“Pick”、“Navigate”），通过模仿学习（ACT）或预定义规则实现。
*   **技能-状态图（Skill-State Graph）**：
    *   **核心思想**：将技能建模为图节点，边表示可行转移。
    *   **状态建模**：每个节点包含**前置条件 $C$**（位置、左右手抓取状态）和**状态变化 $\Delta$**（位置移动、物体增减）。这使得系统能追踪累积的物理状态。
*   **图约束规划与验证**：
    *   **拓扑规划**：VLM 在简化的拓扑子图约束下生成候选技能序列。
    *   **逻辑验证**：通过递归应用状态变化 $\Delta$ 检查序列的全局一致性，剔除逻辑错误的规划。
*   **闭环执行与重规划**：
    *   **双层监控**：结合本体感受（传感器数据）和语义验证（VLM 检查环境变化）。
    *   **动态重规划**：一旦检测到偏差，以当前观测状态为起点，在技能-状态图中搜索纠正路径，而非简单重启任务。

### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：3x3m 空间，包含橱柜、餐桌、储藏室。硬件使用 Agilex Cobot Magic 平台（4 个 Piper 机械臂 + Tracer 移动底座）。
    *   **仿真环境**：基于 ManiSkill-Hab 扩展的 **mshab***，包含 Tidy House、Prepare Groceries、Set Table 三个复杂场景。
*   **Benchmark 与对比方法**：
    *   **GT Sequencing**：人工编写的最优序列（代表执行上限）。
    *   **DeCo***：一种先进的基于 VLM 的任务分解方法（无显式图约束）。
    *   **End-to-End ACT**：端到端的模仿学习基准。
*   **任务设置**：设计了 100 个不同长度的任务，最长包含 17 个子任务，涉及频繁的导航-操作切换和双臂协作。

### 4. 资源与算力
*   **推理引擎**：使用了 **Gemini 2.5 Flash** 作为主要的 VLM 推理引擎。
*   **训练细节**：语义操作技能使用 ACT 算法在 50 个演示轨迹上进行训练。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号、数量或总训练时长。主要强调了推理时的 Token 效率和响应速度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：总计执行了 **8,100 条轨迹**（3 个场景 × 3 个配置 × 100 个任务 × 3 组独立实验），样本量巨大，具有极强的统计说服力。
    *   **真实实验**：针对 100 个任务进行了测试，并详细分析了 10 条极长程（17 个子任务）轨迹的执行细节。
*   **客观性与公平性**：
    *   通过消融实验对比了全量图与精简子图的效率。
    *   对比了不同底层策略（BC、RL、RL Per Obj）对框架表现的影响，证明了框架的通用性。
    *   实验设计涵盖了从规划成功率到执行成功率的全流程指标。

### 6. 主要结论与发现
*   **规划有效性**：MoMaStage 在所有场景下的规划成功率均在 79%-94% 之间，远超 DeCo*（部分场景仅 20% 左右），证明了技能-状态图对消除“物理幻觉”的显著作用。
*   **执行鲁棒性**：在长程任务后期，MoMaStage 的成功率超过了真值序列（GT），因为闭环重规划机制能纠正物理执行中的随机误差（如物体滑落）。
*   **效率提升**：相比于直接向 VLM 输入全量信息，使用精简的技能图约束能显著减少推理时间（降低约 35%）和 Token 消耗（减少约 27%）。
*   **瓶颈识别**：实验发现，目前的失败主要源于底层物理执行（如仿真器中的碰撞计算错误），而非高层逻辑规划。

### 7. 优点与亮点
*   **轻量化建模**：不依赖沉重的 3D 场景图或显式建图，通过抽象的“技能-状态”接口实现了高效的具身智能规划。
*   **逻辑严密**：引入前置条件和状态变化函数，从根本上解决了 VLM 在长程任务中容易丢失状态追踪的问题。
*   **闭环自愈**：将本体感受与语义重规划结合，使机器人具备了在失败后自主恢复的能力。

### 8. 不足与局限
*   **底层技能依赖**：系统的最终表现仍受限于底层动作技能的鲁棒性。如果底层技能（如抓取）在特定 OOD（分布外）状态下彻底失效，高层规划也无法挽回。
*   **场景先验**：虽然不需要实时建图，但技能-状态图的构建仍需要对机器人的能力边界有预先的定义。
*   **仿真与现实差距**：论文指出仿真环境中的物理引擎错误（如模型穿模）是导致失败的主要原因，这限制了在更复杂物理交互场景下的验证。

（完）
