---
title: "Crab$^{+}$: A Scalable and Unified Audio-Visual Scene Understanding Model with Explicit Cooperation"
title_zh: "Crab$^{+}$：一种具有显式协作的可扩展统一视听场景理解模型"
authors: "Dongnuan Cai, Henghui Du, Chang Zhou, Xi Chen, Dan Guo, Hongyuan Zhang, Xuelong Li, Di Hu"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04128v1"
tags: ["query:q11"]
score: 9.0
evidence: 用于统一场景理解的音视频大语言模型
tldr: Crab+是一个旨在解决音视频大语言模型中多任务负迁移问题的统一理解模型。针对传统方法中任务异质性导致的性能下降，该研究提出了显式协作策略：在数据端构建了包含显式推理过程的AV-UIE v2数据集，在模型端设计了统一接口与交互感知LoRA（I-LoRA），通过动态路由协调不同任务的交互模式。实验表明，Crab+在多项基准上超越了专用模型，成功将负迁移转变为正向增益。
selection_source: fresh_fetch
motivation: 旨在解决音视频多任务联合训练中因任务粒度和能力需求差异导致的严重负迁移问题。
method: 通过构建包含显式推理过程的AV-UIE v2数据集，并引入基于动态路由的交互感知LoRA架构来实现任务间的显式协作。
result: "Crab+在覆盖更多任务的前提下，在近88%的任务中实现了正向迁移，性能显著优于现有的统一模型和专用模型。"
conclusion: 该研究证明了通过数据和模型层面的显式协作可以有效克服音视频任务的异质性，是迈向全面音视频场景理解的重要一步。
---

## 摘要
开发用于统一场景理解的视听大语言模型（AV-LLMs）是多模态智能的关键。虽然指令微调使预训练模型具备了多任务能力，但我们观察到传统的多任务统一方法通常会遭受严重的负迁移，与单任务训练相比，近 55% 的任务性能有所下降。我们将这一现象归因于视听任务的异构性，其特征是任务粒度迥异且能力需求不同，这导致了联合训练下的负干扰。为了解决这一问题，我们提出了 Crab$^{+}$，这是一种可扩展且统一的视听场景理解模型，通过从数据和模型两个层面的显式协作来应对任务异构性。在数据方面，我们引入了 AV-UIE v2，这是一个具有显式推理过程的综合性视听统一指令微调数据集。它包含约 22.2 万个样本，涵盖 17 个数据集和 7 项任务，使模型能够捕捉不同粒度级别的跨任务关系。在模型方面，我们设计了一个统一接口来对齐异构任务形式，并提出了交互感知 LoRA（I-LoRA），该方法通过动态路由显式建模任务间关系，以协调不同的视听交互模式，从而减轻参数干扰。大量实验表明，Crab$^{+}$ 涵盖的任务比现有统一模型更广，同时在各种基准测试中优于专用模型。我们成功扭转了负迁移趋势，实现了正迁移，在近 88% 的任务中，多任务学习的表现超过了单任务基准。这些结果在不同的 AV-LLM 范式中均成立，并通过深入的可视化得到了验证，使 Crab$^{+}$ 成为迈向全面视听场景理解的坚实一步。

## Abstract
Developing Audio-Visual Large Language Models (AV-LLMs) for unified scene understanding is pivotal in multimodal intelligence. While instruction tuning enables pre-trained models with multi-task abilities, we observe that conventional multi-task unification methods often suffer from severe negative transfer, where nearly 55% of tasks degrade compared to single-task training. We attribute this phenomenon to audio-visual task heterogeneity, characterized by disparate task granularity and divergent capability demands, which lead to negative interference under joint training. To tackle this, we present Crab$^{+}$, a scalable and unified audio-visual scene understanding model that addresses task heterogeneity through explicit cooperation from both data and model perspectives. On the data side, we introduce AV-UIE v2, a comprehensive Audio-Visual Unified Instruction-tuning dataset with Explicit reasoning processes. It contains approximately 222K samples spanning 17 datasets and 7 tasks, enabling the model to capture cross-task relationships at different levels of granularity. On the model side, we design a unified interface to align heterogeneous task formulations, and propose Interaction-aware LoRA (I-LoRA), which explicitly models inter-task relationships via dynamic routing to coordinate distinct audio-visual interaction patterns, mitigating parameter interference. Extensive experiments show Crab$^{+}$ covers broader tasks than existing unified models while outperforming specialized models on various benchmarks. We successfully reverse the negative transfer trend, achieving positive transfer where multi-task learning surpasses single-task baselines in nearly 88% of tasks. These results hold across diverse AV-LLM paradigms and are validated through in-depth visualization, positioning Crab$^{+}$ as a robust step towards holistic audio-visual scene understanding.

---

## 论文详细总结（自动生成）

以下是对论文《Crab$^{+}$: A Scalable and Unified Audio-Visual Scene Understanding Model with Explicit Cooperation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**音视频大语言模型（AV-LLMs）在多任务统一学习中的“负迁移”现象**。
*   **背景**：虽然指令微调赋予了模型多任务能力，但研究发现，简单的多任务联合训练会导致近 55% 的任务性能优于单任务训练。
*   **动机**：作者将此归因于**任务异质性**，具体表现为：
    1.  **粒度差异**：低级定位任务（时空对齐）与高级推理任务（语义/情感理解）之间存在鸿沟。
    2.  **能力需求冲突**：不同任务对音视频交互模式的要求不同，导致共享参数在优化时产生干扰。
*   **核心目标**：通过数据和模型层面的“显式协作”，消除负干扰，实现多任务的正向增益。

### 2. 核心方法论
Crab$^{+}$ 提出了两个维度的解决方案：
*   **数据层面：AV-UIE v2 数据集**
    *   构建了一个包含约 **22.2万个样本** 的大规模指令微调数据集，涵盖 7 类任务和 17 个原始数据集。
    *   **关键技术**：引入**显式推理过程（Explicit Reasoning Processes）**作为中间表示。通过 MLLM（如 Gemini 1.5 Pro）生成详细的推理链，将简单的标签转化为逻辑严密的文本，从而弥合不同粒度任务之间的语义不一致。
*   **模型层面：统一接口与 I-LoRA**
    *   **统一输入输出接口**：将所有任务（包括分割、定位、分类、QA）统一建模为序列生成任务。对于分割任务，通过预测边界框和关键点坐标，调用冻结的 SAM2 模块生成掩码。
    *   **交互感知 LoRA (I-LoRA)**：针对参数干扰问题，设计了动态路由机制。
        *   **结构**：包含一个共享的低秩矩阵 $A$ 和多个专门的 LoRA 头部 $\{B_i\}$。
        *   **算法流程**：利用一个可学习的路由器（Router）根据输入 Token 的特征，动态计算每个头部的权重，将 Token 路由到最合适的交互模式中，从而解耦冲突的音视频交互模式。

### 3. 实验设计
*   **数据集与场景**：涵盖 17 个数据集，包括动作识别（KS, UCF51）、情感识别（MELD, DFEW 等）、跨模态匹配（VGG-CM）、时空定位（AVE, UnAV-100, LLP）、分割定位（AVS, Ref-AVS）以及问答（MUSIC-AVQA, AVQA）。
*   **Benchmark 与对比方法**：
    *   **统一模型对比**：与 Video-LLaMA, PandaGPT, Crab (前作), UIO-2 等 10 余种多模态统一模型对比。
    *   **专用模型对比**：在各个子任务上与最先进的专用专家模型（如 AVSegFormer, DFER-CLIP 等）对比。
    *   **范式验证**：在三种 AV-LLM 范式（LLM+V+A, V-LLM+A, 原生 AV-LLM）上验证通用性。

### 4. 资源与算力
*   **模型规模**：实验基于 LLaMA2-7B、LLaMA3-8B、Qwen2.5-VL 和 Qwen2.5-Omni 等骨干网络。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到了超参数设置（如 LoRA rank=128/16，训练阶段分为连接器预训练和多任务指令微调），这暗示了其训练成本与主流 7B/8B 规模的多模态模型微调相当。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 17 个数据集的全量对比、单任务 vs 多任务的增益分析、针对 3 种不同架构范式的泛化性测试。
*   **消融实验**：
    1.  验证了 I-LoRA 相比标准 LoRA 的优越性（任务覆盖率提升 29%-53%）。
    2.  进行了“头部丢弃（Head-dropping）”实验，证明了不同 LoRA 头部确实学习到了互补的任务特征。
    3.  测试了 LoRA 头部数量对性能的影响。
*   **评价**：实验设计非常**充分且客观**。通过在不同底座模型上重复验证，排除了特定架构带来的偏差，有力支撑了“显式协作”能解决负迁移的论点。

### 6. 主要结论与发现
*   **扭转负迁移**：Crab$^{+}$ 成功将负迁移趋势逆转为正向增益，在 **88% 的任务**中，多任务联合训练的效果超过了单任务训练。
*   **性能领先**：在覆盖任务范围更广的前提下，Crab$^{+}$ 在多个基准测试（如 AVE, KS, Ref-AVS）中达到了 SOTA 水平，甚至超越了专门针对单一任务设计的专家模型。
*   **可视化发现**：路由器权重显示出明显的语义聚类特征，证明模型能够自动识别任务需求并分配计算资源。

### 7. 优点
*   **系统性解决负迁移**：不仅提出了模型架构（I-LoRA），还从数据表征（显式推理）入手，双管齐下解决异质性问题。
*   **高度可扩展性**：方法不依赖于特定 LLM 底座，在多种主流多模态架构上均表现出一致的提升。
*   **统一性强**：通过坐标点/框的序列化，将感知类任务（分割）完美融入生成式框架，实现了真正的单阶段统一训练。

### 8. 不足与局限
*   **推理冗余风险**：作者提到，对于简单的识别任务，强制引入长推理链可能会导致“过度解读”或引入噪声。
*   **生成任务缺失**：目前的统一框架主要集中在“理解”和“感知”类任务，尚未涵盖音视频生成（如文本转视频/音频）任务。
*   **数据构建成本**：依赖高性能 MLLM 生成推理链，对于更大规模的数据扩展可能存在 API 成本或模型偏差传递的风险。

（完）
