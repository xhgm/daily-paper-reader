---
title: Glass Segmentation with Fusion of Learned and General Visual Features
title_zh: 融合学习特征与通用视觉特征的玻璃分割
authors: "Risto Ojala, Tristan Ellison, Mo Chen"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03718v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于场景理解和机器人的玻璃表面分割
tldr: 玻璃分割因其透明性而极具挑战，但对机器人导航至关重要。本文提出一种融合通用与任务特定视觉特征的新架构，利用冻结的 DINOv3 提取通用特征，并结合监督学习的 Swin 模型提取特定特征。通过残差挤压激励通道缩减处理特征，并采用 Mask2Former 解码器生成掩码。该方法在四个主流数据集上达到 SOTA 水平，且推理速度极具竞争力。
selection_source: fresh_fetch
motivation: 玻璃由于缺乏明显的视觉特征，在 RGB 图像分割中极具挑战，但对机器人避障和场景理解至关重要。
method: 采用双骨干网络架构，融合了 DINOv3 的通用视觉特征与 Swin 模型的任务特定特征，并结合 Mask2Former 解码器。
result: 在四个常用的玻璃分割数据集上取得了最先进的准确率，且在使用轻量化骨干网络时推理速度优于现有 SOTA 方法。
conclusion: 结合通用基础模型特征与任务特定特征能显著提升玻璃分割性能，为透明物体识别提供了高效的解决方案。
---

## 摘要
从 RGB 图像中进行玻璃表面分割是一项具有挑战性的任务，因为玻璃作为一种透明材料，明显缺乏视觉特征。然而，玻璃分割对于场景理解和机器人技术至关重要，因为透明玻璃表面必须被识别为固体材料。本文提出了一种用于玻璃分割的新型架构，部署了双主干网络以生成通用视觉特征以及特定任务的学习视觉特征。通用视觉特征由冻结的 DINOv3 视觉基础模型产生，而特定任务特征则由以监督方式训练的 Swin 模型生成。生成的多尺度特征表示通过残差挤压与激励通道缩减（residual Squeeze-and-Excitation Channel Reduction）进行下采样，并输入到 Mask2Former 解码器中，从而生成最终的分割掩码。该架构在四个常用的玻璃分割数据集上进行了评估，在多项准确率指标上达到了最先进的结果。与之前的最先进方法相比，该模型还具有竞争力的推理速度，并且在使用较轻量的 DINOv3 主干变体时超过了前者。实现源代码和模型权重可在以下网址获取：https://github.com/ojalar/lgnet

## Abstract
Glass surface segmentation from RGB images is a challenging task, since glass as a transparent material distinctly lacks visual characteristics. However, glass segmentation is critical for scene understanding and robotics, as transparent glass surfaces must be identified as solid material. This paper presents a novel architecture for glass segmentation, deploying a dual-backbone producing general visual features as well as task-specific learned visual features. General visual features are produced by a frozen DINOv3 vision foundation model, and the task-specific features are generated with a Swin model trained in a supervised manner. Resulting multi-scale feature representations are downsampled with residual Squeeze-and-Excitation Channel Reduction, and fed into a Mask2Former Decoder, producing the final segmentation masks. The architecture was evaluated on four commonly used glass segmentation datasets, achieving state-of-the-art results on several accuracy metrics. The model also has a competitive inference speed compared to the previous state-of-the-art method, and surpasses it when using a lighter DINOv3 backbone variant. The implementation source code and model weights are available at: https://github.com/ojalar/lgnet

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **L+GNet** 的新型深度学习架构，旨在解决 RGB 图像中透明玻璃表面的分割难题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：玻璃具有透明性、无纹理性和反射性，其视觉外观往往与背景完全一致或受反射干扰严重，导致传统 RGB 分割方法难以准确识别。
*   **研究动机**：在机器人导航和场景理解中，准确识别玻璃（固体障碍物）对避障至关重要。
*   **核心思路**：借鉴人类直觉，通过结合**通用视觉上下文**（由大规模预训练的基础模型提供）和**任务特定特征**（通过监督学习获取），提升模型在复杂环境下的推理能力。

### 2. 方法论：核心思想与关键技术
L+GNet 采用双主干（Dual-backbone）架构，主要包含以下组件：
*   **双主干网络**：
    *   **学习特征主干 (Learned Features Backbone)**：使用 **Swin-S Transformer**，通过监督学习捕捉任务相关的局部特征和长程空间依赖，生成多尺度特征图（1/4 到 1/32 比例）。
    *   **通用特征主干 (General Features Backbone)**：使用冻结权重的 **DINOv3-L** 视觉基础模型，提取在 170 亿张图像上学到的通用语义上下文。
*   **SE 通道缩减 (SE Channel Reduction)**：
    *   由于双主干输出特征维度极高，论文设计了带有**挤压与激励 (Squeeze-and-Excitation, SE)** 模块的残差块。
    *   **关键公式**：中间通道数计算为 $C_{mid} = \max(\frac{C_{in}}{2}, C_{out})$，通过逐步降维避免信息丢失，并利用 SE 注意力机制强化重要特征、抑制噪声。
*   **解码器**：采用标准的 **Mask2Former** 解码器，利用变形注意力机制融合多尺度特征，生成最终的二值分割掩码。

### 3. 实验设计
*   **数据集**：在四个主流玻璃分割数据集上进行了验证：**GDD**、**Trans10k-Stuff**、**GSD** 和 **HSO**。
*   **Benchmark 指标**：交并比 (IoU)、F-measure ($F_\beta$)、平均绝对误差 (MAE) 和平衡错误率 (BER)。
*   **对比方法**：涵盖了从早期的 GDNet、TransLab 到最新的 SOTA 方法如 GlassWizard (2025) 和 C-LPMoE。

### 4. 资源与算力
*   **硬件平台**：实验主要在 **NVIDIA RTX 3090** GPU 上完成。
*   **训练细节**：
    *   训练 30 个 epoch，batch size 为 16。
    *   使用 AdamW 优化器，初始学习率为 $10^{-4}$，配合线性衰减调度器。
    *   使用了 PyTorch 混合精度训练以优化内存占用。
*   **推理性能**：在 FP16 精度下，L+GNet (DINOv3-L) 达到 14.2 fps；使用更轻量的 DINOv3-B 时可达 18.5 fps。

### 5. 实验数量与充分性
*   **实验规模**：论文在四个独立数据集上分别进行了训练与测试，并进行了一项“全数据集融合训练”的综合实验。
*   **消融实验**：对比了单主干（仅 Swin-S 或仅 DINOv3）、不同尺寸的 DINOv3 变体（L/B/S）以及有无 SE 模块的效果。
*   **客观性**：实验对比了多种 SOTA 方法，并提供了推理速度与参数量的详细对比，实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能领先**：L+GNet 在几乎所有数据集和指标上均刷新了纪录，尤其在 MAE 和 BER 指标上提升显著（如在 GDD 数据集上 MAE 降低了 36%）。
*   **基础模型的作用**：实验证明，即使冻结基础模型权重，其提供的通用特征也能极大增强模型对透明物体的“推理”能力。
*   **效率平衡**：虽然 DINOv3-L 效果最好，但 DINOv3-B 变体在保持极高准确率的同时，推理速度超过了之前的 SOTA 方法 GlassWizard。

### 7. 优点（亮点）
*   **双主干融合策略**：成功证明了将“冻结的基础模型特征”与“可训练的任务特定特征”结合的有效性。
*   **通用性强**：模型在不同场景（室内、室外、公共空间）的数据集上均表现稳健。
*   **SE 模块设计**：通过逐步降维和通道注意力，有效解决了多主干特征融合时的维度爆炸和冗余问题。

### 8. 不足与局限
*   **置信度校准问题**：论文指出 Mask2Former 解码器生成的预测概率分布不均（集中在 0.3-0.7 之间），导致模型无法提供可靠的预测置信度。
*   **极端场景失效**：在完全缺乏视觉线索（如极度干净且无反射的玻璃）或物体遮挡关系复杂（如玻璃上的贴纸、海报）时，模型仍会出现误判。
*   **计算开销**：尽管有轻量化版本，但完整版 L+GNet 参数量较大（4.5 亿），对于算力极度受限的嵌入式机器人平台仍有挑战。

（完）
