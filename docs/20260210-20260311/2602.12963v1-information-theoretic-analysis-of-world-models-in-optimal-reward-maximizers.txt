Title: Information-theoretic analysis of world models in optimal reward maximizers

URL Source: https://arxiv.org/pdf/2602.12963v1

Published Time: Mon, 16 Feb 2026 01:53:16 GMT

Number of Pages: 28

Markdown Content:
# Information-theoretic analysis of world models in optimal reward maximizers 

## Alfred Harwood †,1 , Jose Faustino †,1,2 , and Alex Altair 1

†Equal contribution 

1Dovetail Research 

2University of Sao Paulo February 16, 2026 

Abstract 

An important question in the field of AI is the extent to which suc-cessful behaviour requires an internal representation of the world. In this work, we quantify the amount of information an optimal policy provides about the underlying environment. We consider a Controlled Markov Process (CMP) with n states and m actions, assuming a uniform prior over the space of possible transition dynamics. We prove that observing a deterministic policy that is optimal for any non-constant reward function then conveys exactly n log m bits of information about the environment. Specifically, we show that the mutual information between the environ-ment and the optimal policy is n log m bits. This bound holds across a broad class of objectives, including finite-horizon, infinite-horizon dis-counted, and time-averaged reward maximization. These findings provide a precise information-theoretic lower bound on the “implicit world model” necessary for optimality. 

Keywords: Reinforcement Learning, Information Theory, Implicit World Models, AI Safety. 

1

> arXiv:2602.12963v1 [cs.AI] 13 Feb 2026

# 1 Introduction 

## 1.1 Does achieving goals require a world model? 

In the field of AI, it is common to refer to an AI agent as having a “world model”[1]. By this is normally meant that the agent internally contains a model of the world that the agent is interacting with, that it can use to make predic-tions about the world. Such models are generally only partial or imperfect, but still a good enough approximation of the world to be useful. If an agent has access to a perfect world model, then (tractability aside) it can achieve optimal performance by simply querying the world model for the out-come of each possible action, and picking the one with the best outcome. But we can also ask if the relationship goes both ways. Is there a sense in which a good performance requires a world model? This question is sometimes framed as one component of the ‘agent-like structure problem’ [2]. One motivation for asking this question is that the behaviour of machine learn-ing systems is notoriously fragile and hard to predict. “Out-of-distribution” generalization is a major outstanding issue. We hope that developing formal results that let us deduce world model accuracy from only observations of per-formance might help us to understand when we could or couldn’t trust our system’s performance. While it is clear that some tasks do not require a full model of the world, it also seems clear that a wide variety of goals require an agent to contain some information about its world. In this paper, we operationalize this question by asking: ‘if we are given an optimal policy for a particular goal, how much infor-mation about the world can be extracted from it?’. This framing sidesteps questions of the structure and architecture of the agent enacting the policy and focuses purely on the information-theoretic content of the policy. As such, our result makes no claims about the form or implementa-tion that an internal world model must take, and instead focuses on how much information it must contain. Following [3], we investigate this question in a setting where the environment is a controlled Markov processes. In our result the ‘goal’ which an agent must achieve is captured by a reward function over states which is summed over time. The degree to which this framing captures a fully general notion of ‘goals’ is sub-ject to some debate. For example, [4] discusses the circumstances under which this ‘reward hypothesis’ applies, comparing and relating it to the Von Neumann Morgenstern utility theorem [5]. Nonetheless, the summing of rewards over time is a very common way of framing goals in reinforcement learning,[6] and it is the approach we will use. 2Starting with maximal ignorance about the environment means not knowing any of the individual transition probabilities. We represent this state of ignorance using a uniform probability density function over all possible environments. We then consider how our subjective probability distribution over possible environ-ments changes when we learn that a particular policy is optimal for maximizing a specific value function in the environment. Any particular policy will only be optimal in a small subset of possible environments, so that learning this policy gives us information about the environment. In this sense, information about the environment is contained within the optimal policy. We quantify this using the mutual information between the environment and the policy of an optimal reward maximiser. In particular, we show that optimal reward-maximising policies contain n log m bits of information about the world, where n is the number of environment states, and m is the number of actions that the policy is able to take in each state. This holds for any non-constant reward functions and holds for all common reward aggregation methods including time-discounted, finite-time, and time-averaged. 

## 1.2 Related work 

In former decades, techniques of “Good Old-Fashioned AI” favoured inputting explicit representations of the world, as chosen and designed by the human engineers. In [7], Brooks influentially argued that such representations were ineffective and unnecessary, claiming that “the world is its own best model”. In ‘The Bitter Lesson’ [8], Sutton argued that techniques which leveraged com-pute for learning (and search) reliably beat engineers’ attempts to deliberately encode their knowledge of the domain into ML system. Both of these however leave open the possibility that successful systems are relying on implicit world models. At the same time, [9] was influential in showing several examples where learning separate world models was effective for training good policies. An early theorem that moves toward addressing our question outside of the context of AI appeared in [10]. In this paper, the authors derive a bound on the entropy reduction achievable by a controller in terms of its mutual information with the environment. While this result is illuminating, the model it considers is simplified since it only considers goals characterised by entropy reduction over a single timestep. In our work, we consider reward maximisation over multiple timesteps for a much more general framework for quantifying goal-achieving be-haviour. Recently, the Causal Incentives Working Group published two papers investi-gating versions of this problem [11, 3]. The paper [3] shows that, if a policy is general enough to achieve a wide range of goals, then one can extract from the policy’s actions an epsilon-approximate description of the world with which it is interacting. That is to say, if you have access to the policy (and the knowl-edge that it is well-performing) then you can extract a world model. In this 3sense, the policy contains an implicit world model. This result was extended to consider stochastic policies and partially observable environments in [12]. A similar result was shown in the setting of causal Bayesian networks in [11]. There are some important assumptions made in [3] which we wish to develop and relax. First, the policy is assumed to be general and ‘goal-conditioned’ meaning that it can be fed multiple goals and achieve good performance on all of them. In particular, the agent must be general enough to succeed at a particular class of goals which the authors show is equivalent to modelling the individual tran-sition probabilities of the environment. While this is valid as an assumption, the class of goals feels unnatural and must be expressed using Linear Temporal Logic. Here, we consider the more natural class of goals represented by reward maximization, which is standard in reinforcement learning. Furthermore, we only require that a policy is optimal for maximizing a single reward function, rather than requiring our policy to achieve general good performance on a wide range of goals. Since such policies do not contain the full information about the environment, we are able to discuss and quantify the amount of information about the world contained in the policy. Thus, our result finds natural applica-tion to finite agents with partial world models. 

# 2 Setup 

## 2.1 Environments 

Following [3], we consider an environment modelled by a Controlled Markov Process (CMP) with a set of n states S = {s1, s 2, . . . s n} and a set of m actions 

A = {a1, a 2, . . . a m} which can be taken in each state. The CMP environment is characterized by a set of transition probabilities of the form Psisj (ak) which specify the probability that taking an action ak ∈ A while in state si ∈ S results in a transition to state sj ∈ S. Time is taken to be discrete. At each timestep, a policy acting in the environment can take a single action, causing the system to evolve stochastically to its subsequent state. Since taking an action in a state must result in a transition to some state, we have the normalization conditions that P 

> j

Psisj (ak) = 1 ∀i, k . Since there are 

m possible actions and n possible initial states, the environment lives in a space characterized by the product of nm simplexes each with dimension ( n − 1), making it a closed subset of Rnm (n−1) . We use X to denote this space and use x to denote a point in this space, corresponding to a full list of transition probabilities which fully characterizes a particular environment. 42.2 Policies 

Acting in this environment we consider an agent using a deterministic, Marko-vian policy. We assume that the environment is fully observable, meaning that the agent can observe the exact state that it is in. The actions taken by such a policy are therefore entirely determined by the current environmental state meaning that it can be characterized by a deterministic function π : S → A

which takes the current state as an input and outputs an action. Note that there are mn possible policies of this form. We will use an indexing over these policies and label the i-th policy as πi where i can run from from 1 to mn. We will use the notation I = {1, 2, ...m n} to denote the set of indices. Notice that a deterministic, Markovian policy π acting in an environment x

causes the system to behave as a stationary Markov process characterised by a transition matrix Mπ (x) which depends both on the policy π and the en-vironment x. The elements of the transition matrix [ Mπ (x)] ij correspond to the probability that the action taken by policy π in environment x leads to state sj transitioning to state si. In the notation from before, we can write [Mπ (x)] ij = Psj si (π(sj )). 

## 2.3 Rewards 

In our setup, goals are characterised through the maximisation of the expected reward signal over states. This reward is captured using a function r : S →

(0 , 1). The only restriction we place on the reward function is that it is non-constant , meaning that there is at least one pair of states si and sj such that 

r(si) > r (sj ). We use the terminology ‘value function’ to refer to any way of accumulating this reward over multiple timesteps. There are multiple ways of doing this. We consider time-discounted summation of expected reward over both finite and infinite time-horizons and time-averaged summation of expected reward as represented in equations (13), (17) and, (19) respectively. We denote the value function evaluated in environment x with policy π as Vπ (x) and let the context denote the type of reward accumulation used. We say that a policy πo is optimal for a value function in an environment x

if Vπo (x) ≥ Vπi (x)∀i ∈ I and we say that πo is uniquely optimal if Vπo (x) >Vπi (x)∀i ∈ I . Sometimes we will refer to the evaluation of Vπ (x) as the ‘perfor-mance’ of policy π in environment x.

## 2.4 Mutual Information 

We treat the environment as a random variable X with even distribution over all possible environments in X . It is therefore characterized by a uniform prob-ability density function p(x) = 1 

> V

where V = R 

> x∈X

dx is the volume of the space 

X .5The differential entropy h will be used to quantify the amount of information we have about the environment X:

h(X) = −

Z

> x∈X

p(x) log p(x)dx . (1) Initially, when p(x) is a uniform probability density function (PDF) the differ-ential entropy of X is maximal and we have 

h(X) = −

Z

> x∈X

p(x) log p(x)dx = −

Z

> x∈X

1

V log 1

V dx = log V . (2) If Π is the random variable corresponding to the optimal policy in a particular environment then knowing the optimal policy reduces the differential entropy of the environment to h(X|Π). The expected difference between these quantities is the mutual information: 

I(X; Π) = h(X) −

> mn

X

> i=1

h(X|Π = πi)P (Π = πi) . (3) Notice that, while differential entropy can depend on the choice of coordinates [13], the mutual information between X and Π is an invariant quantity, since it can be expressed as a KL-divergence. Since all our results are in terms of mu-tual information, issues surrounding the coordinate-dependence of differential entropy are avoided. In Section 3.4 on time-averaged rewareds, we will exclude ∂X the boundary points of X and just consider the interior int( X ). This boundary of X includes all points where at least one transition probability is exactly zero. Excluding these environments allows us to assume that the environment is irreducible and aperiodic. But, since ∂X has Lebesgue measure of zero we can exclude it without affecting any of integrals over X used when calculating the differential entropy. 

# 3 Results 

A state of complete ignorance about the environment variable X is represented by a uniform PDF over X with maximal differential entropy, equal to log V . If we obtain some information about the environment, then our PDF will change, leading to a lower differential entropy. The amount of information we have obtained about the environment will be quantified by the expected change in differential entropy which is the mutual information I(X; Π). In this paper, we ask: how much information do we gain about the environment if we learn that a particular policy is optimal for some task within that environment? Suppose that we had a goal such that in every environment, except for a measure zero subset of X , there was exactly one optimal policy. Furthermore, assume that the volume of environments for which a particular policy πi was optimal 6was equal for all the mn different policies πi. This would mean that we could partition X (the space of possible environments X ) into mn equal volume sub-sets depending on which policy was optimal in each subset. In other words, if Xi ⊂ X denotes the set of environments where a policy πi is optimal then S 

> i

Xi = X and R 

> x∈X i

dx = Vmn for all i. In this situation, we have the following lemma: 

Lemma 3.1. Assume a goal represented by the maximization of a value function 

Vπ (x). We say that a policy πo is optimal for this value function in environment 

x if Vπo (x) ≥ Vπi (x)∀i. Let 

Xi = {x ∈ X | πi is optimal in x} .

If 1. (Xi)i∈{ 1,...,m n} is a partition for almost every environment - i.e, the set 

Xi ∩ X j , has measure zero for all i, j . In other words, there is exactly one optimal policy for almost all environments 2. Xi and Xj have the same volume, for any i, j ∈ I

Then, 3. h(X) − h(X | π) = n log m, i.e, knowing which optimal is policy re-duces the differential entropy of the environment from h(X) = log V to 

h(X | π) = log Vmn . The mutual information between the environment and the optimal policy is therefore 

I(X : Π) = n log m . (4) 

Proof. Let the measure zero subset where there is more than one optimal policy be labelled Z ⊂ X . Removing this subset does not affect the volume so we have 

Z

> X

dx =

Z 

> X/Z

dx = V . (5) The fact that each policy is optimal in an equal volume subset of X means that each policy is optimal for a volume Vmn , which comes from equally dividing up the volume of X /Z. In this situation, if we learned which policy was optimal in an environment, this would tell us that the environment lay somewhere within a a volume Vmn of possible environments (a more formal explanation of this process using Bayes Theorem can be found in Appendix A). This state of knowledge would be represented by PDF with value zero outside of the set Xi and a uniform distribution over the points inside Xi:

p(x|πi) = 

( mn 

> V

when x ∈ X i

0 otherwise . (6) 7This PDF would have a differential entropy given by 

h(x|πi) = −

Z

> x∈X i

mn

V log mn

V dx = log Vmn . (7) The change in differential entropy which occurs upon learning the optimal policy is, in this situation: 

h(x) − h(x|πi) = log V − log Vmn = n log m . (8) Thus, in this situation, learning the optimal policy gives us n log m bits of infor-mation about the environment. Since each policy results in the same reduction in differential entropy, we can trivially express this as mutual information be-tween the environment and optimal policy if both are represented as random variables. Since h(X|πi) is the same for each πi, the mutual information be-tween X and Π is obtained from equation (3): 

I(X; Π) = h(X) −

> mn

X

> i=1

h(X|πi)P (Π = πi) = n log m . (9) Recall that in order to get this result, we had to make two important assump-tions relating to the way in which we characterised ‘optimal’ policies, relative to some goal. First, we assumed that there is exactly one optimal policy in each environment except a measure zero subset. We will call this ‘Assumption 1’. Second, we had to assume that each of the mn deterministic polices was optimal in an equal volume subset of X . We will call this ‘Assumption 2’. As it turns out, Assumption 2 is true for a very large class of goals. In particular, it is true for any goal involving the maximisation of a value function of the form 

Vπ (x) = k(Mπ (x)) where Mπ is the transition matrix of the Markov process over states which results from enacting a policy π in environment x and k is a function which takes in a transition matrix and outputs real number. This means that the only way in which the policy influences the outcome of the value function is through its effect on the transition matrix. In this case, the following theorem applies. 

Theorem 3.2. Let Vπ (x) be a value function which is only influenced by the policy through its effect on the transition matrix Mπ (x). Let Xi be the subset of environments for which policy πi is the unique optimal policy for maximising this value function. Then: 

Z

> Xi

dx =

Z

> Xj

dx ∀i, j (10) 

ie. all policies are uniquely optimal in an equal volume of environments. 

8Proof. Here, we will sketch the proof and leave the details for the Appendix C. Consider two different policies πi and πj and let us label the actions taken by them as πi(sk) = bk and πj (sk) = ck. In an environment x, the only transition probabilities which contribute to the performance of πi are those for action bk,taken in state sk for k = 1 . . . n . Using notation from before, these transition probabilities are {Psk s′ (bk) : k = 1 . . . n, s ′ ∈ S}.Now, consider the following function gij : X → X which takes in an environment 

x and changes it so that Psk s′ (bk) and Psk s′ (ck) are swapped for all bk̸ = ck

and keeps all other transition probabilities the same. Applying policy πi in an environment x results in the same transition matrix as applying the policy πj

in an environment gij (x) so we have that Mπi (x) = Mπj (gij (x)). If πi was optimal in the environment x, then πj will be optimal in the new environment 

gij (x). Thus, if Xi is the set of environments for which πi is optimal, then 

gij (Xi) a set for which πj is optimal. Since (as proved in the appendix) gij is invertible and measure-preserving, the volumes of Xi and Xj are equal. Since this argument can be applied to any pair of policies πi and πj , we can conclude that the volumes of environments for which each policy is optimal are equal. In what follows, we will show that these Assumptions 1 and 2 hold for a very general class of goals. In particular, they hold when the goal is the maximisation of any non-constant reward function over environmental states. We consider the time-averaged, discounted, and finite-time total reward maximisation and show that all of these settings satisfy these two assumptions. Therefore, optimal policies for any goal captured by these kinds of reward maximisation contain 

n log m bits of information about their environment. 

## 3.1 Proof Strategy and Notation 

For all of these reward maximisation frameworks, our proof follows roughly the same steps. First, we ensure that the value function Vπ (x) is only influenced by the policy through its effect on the transition matrix, meaning that Theorem 3.2 applies. Then, we show that the the value function is a real analytic function of x and define a ‘difference function’ fij (x) = Vπi (x) − Vπj (x) which captures the difference in performance between policies πi and πj in an environment x.Having done this, we make use of the following standard lemma. 

Lemma 3.3. For a real analytic function f (x), either the set {x : f (x) = 0 }

has zero measure or the function is identically zero. Proof. A proof can be found in [14]. Applying this lemma to fij (x) tells us that, for any pair of policies πi and πj ,the set of environments for which their performance is the same has measure zero. This means that, in all environments except a set of measure zero, there is a unique optimal policy for maximising the value function Vπ (x). This allows 9us to apply Lemma 3.1 to the value function, yielding the result. We will now introduce some terminology and notation used when discussing reward functions throughout the paper. Let r : S → (0 , 1) be a reward function over states and let St be the random variable corresponding to the environment state at time t. The random variable St is distributed depending on the en-vironment x along with the previous state St−1 and the action taken by the policy at the previous timestep At = π(St−1). For a fixed environment and a deterministic, Markovian policy of the kind we are considering, we can write 

P (St = si) as 

P (St = si) = X 

> sj∈S

P (St = si|X = x, S t−1 = sj , A t = π(sj )) P (St−1 = sj ) .

(11) Notice that P (St = si|X = x, S t−1 = sj , A t = π(sj )) is simply equal to the transition matrix element [ Mπ (x)] ji . Now let us introduce the notation ⃗v t to write the probability distribution over states as a vector. The elements of ⃗v t

will correspond to the probabilities P (St = si). As is standard in the study of Markov processes, this allows us to write the evolution of the probability dis-tribution over states as the vector equation ⃗v t+1 = Mπ (x)⃗v t. We will use this notation to express value functions in terms of the transition matrix, allowing us to apply Theorem 3.2. As a final piece of notation, it will be convenient to introduce an ‘expected reward’ function, which is simply the expected value of reward function r when applied to a distribution over states represented by a vector ⃗v. We will denote this function R : Rn → (0 , 1) so we have: 

R(⃗v) := X

> i

r(si)vi = X

> i

r(si)P (si) , (12) where vi is the element of ⃗v corresponding to P (s i). We will now apply this framework to various forms of reward maximisation. 

## 3.2 Reward Maximisation with discount rate 

First, we consider a value function which is the time-discounted expected reward for a reward function over states. These value functions take the form: 

Vπ (x) = 

> ∞

X

> t=1

γtE[r(St)] , (13) where 0 < γ < 1 is a discount rate. Using the notation introduced in Section 3.1, we can express this in terms of the transition matrix and an initial distribution over states represented by a vector ⃗v 0:

Vπ (x) = 

> ∞

X

> t=1

γtR(Mπ (x)t⃗v 0) . (14) 10 Writing the value function in this way shows that it is only influenced by the policy through its effect on the transition matrix. We will now show that Vπ (x)is a real analytic function of x. The argument we will present here applies for any starting distribution over states ⃗v 0.

Lemma 3.4. Vπ (x) is a real analytic function over X .Proof. First, note that, due to the linearity of the expected value, we can write: 

Vπ (x) = R

 ∞X

> t=1

γtMπ (x)t⃗v 0



. (15) Since Mπ (x) is stochastic all of its eigenvalues have magnitude |λ| ≤ 1. Since 

γ < 1, we have that the matrix A = γM π (x) has all eigenvalues with |λ| < 1. As a result we can apply the matrix geometric series: 

> ∞

X

> t=1

At = A

> ∞

X

> t=0

At = A(1 − A)−1 = A C⊺

det(1 − A) , (16) where C is the matrix of cofactors of (1 − A) and det(1 − A) is the determinant. Due to the restrictions of the eigenvalues of A, we have that det (1 − A)̸ = 0 so the equation is well-defined. Since both C and det(1 − A) are polynomials in the elements of A, each element of P∞ 

> t=1

At will be a real-analytic function of the elements of A. Furthermore, each element of A is simply an element of 

Mπ (x) multiplied by γ. In turn, each element of Mπ (x) comes directly from 

x. Hence, we have that the elements of P∞ 

> t=1

At are real analytic functions of 

x. Applying this matrix to ⃗v 0 and taking the expected reward with R involves summing these elements and multiplying them by real numbers (probabilities and rewards) which preserves analyticity. Therefore Vπ (x) is a real analytic function of x.Now, we will define a ‘difference function’ fij (x) = Vπi (x) − Vπj (x). Since 

Vπ (x) is a real analytic function of x, we have that fij (x) is also a real analytic function of x. In order to apply Lemma 3.3, we will now show that fij (x) is not identically zero. 

Lemma 3.5. fij (x) is not identically zero Proof. The two policies πi and πj must take a different action in at least one state which we will call sa. Let us call the state with the highest reward sb

which may or may not be the same as sa. Consider the following environment 

x′. In this environment, in all states except sa, all actions result in the next state being a random state from a uniform distribution over states. The action taken by πi in sa causes the state to transition to sb with probability 1, but the action taken by πj in state sa leads to a uniform distribution over all states. The tran-sition matrix Mπj (x) will therefore have all elements equal to 1 

> n

. The transition 11 matrix Mπi (x) will have all elements equal to 1 

> n

except for the ath column. The 

ath column of Mπi (x) will be all zeroes except for the bth entry which will be one. Notice that, for any starting probability vector ⃗v 0, the distribution over states for all subsequent timesteps will be uniform when applying πj . However, when applying πi to distribution ⃗v, the probability that that system will transition to state sb is equal to p(sb) = va + P  

> i̸=a1
> n

vi > 1 

> n

. The probability will be uni-form for all other states when applying πi. As a result, the expected reward for policy πi at the first timestep will be larger than the expected reward achieved by πj . Furthermore, this will be true at each subsequent timestep. Thus, when summing discounted rewards, we can conclude that Vπi (x′) > V πj (x′) and hence 

fij (x′) > 0. Our example of x′ above required that one of the transitions was deterministic, and hence required that x′ was on the boundary of X , but we can use the continuity of fij to show that there exists x ∈ int( X ) which also satisfies fij (x)̸ =0. Since fij is analytic it is also continuous. Hence, since fij (x′) > 0 and fij 

is continuous on X , it’s true that there is a δ > 0 such that for any x ∈ X , if 

d(x, x ′) < δ , then fij (x) > 0. Now, consider Bδ (x′) to be the ball of radius δ and centred in x′. Since x′ ∈ ∂X , Bδ (x′)∩int( X )̸ = ∅. Then, let x ∈ Bδ (x′)∩int( X ), it is true that x ∈ int( X ) and fij (x) > 0, and hence fij (x)̸ = 0. This can be applied to prove the following. 

Lemma 3.6. The set of environments with more than one optimal policy for maximising the time-discounted reward is of measure zero. Proof. By Lemma 3.3, fij must either be identically zero or the set of its zeros must be measure zero. Since, by Lemma 3.5 it is not identically zero, the set of points for which it equals zero (ie. the set of environments for which there are two policies with identical performance) must be measure zero. Additionally, since value functions of the form given in equation (14) are func-tions of the transition matrix Mπ (x), we can apply Theorem 3.2 meaning that each policy is optimal in an equal volume of environments. This allows us to prove the following theorem. 

Theorem 3.7. An optimal deterministic policy for maximising the discount reward over states for any non-constant reward function and any discount rate 

0 < γ < 1 contains n log m bits of information about its environment. Proof. By Lemma 3.6 all environments, except those of a measure zero subset, have a unique optimal policy. By Theorem 3.2, each policy is uniquely optimal in a subset of the same volume. Therefore, by Lemma 3.1, the optimal policy for maximising the time-discounted reward contains n log m bits of information about its environment. 12 3.3 Reward Maximisation over Finite time horizons 

In the previous section, we considered summing reward in the limit of infinite timesteps. Here, we show that our argument equally applies to finite-time re-ward maximisation. When reward is summed over finite timesteps, we can also consider cases with no discount rate (ie, γ = 1) since the accumulated reward will be finite. We will consider value functions of the form: 

Vπ (x) = 

> T

X

> t=1

γtE[r(St)] , (17) for any 0 < γ ≤ 1 where γ = 1 corresponds to total reward summation with no discount rate. As before, we will re-write this in terms of the transition matrix: 

Vπ (x) = 

> T

X

> t=1

γtR(Mπ (x)t⃗v 0) . (18) Since this will be a degree T polynomial in the elements of Mπ (x), we have that every value function of this form will be a real analytic function of x. This implies that functions of the form fij (x) = Vπi (x) − Vπj (x) are also real analytic (note that since we have re-defined Vπ (x), we have similarly implicitly re-defined 

fij (x) to be in terms of these finite-time value functions). To apply Lemma 3.3 in order to show that there is a unique optimal policy in almost all environments we must therefore show that fij is not identically zero. In order to do this, we need to make a minimal qualification that either T > 1 or ⃗v 0 has full support. 

Lemma 3.8. fij is not identically zero for value functions with T > 1 or value functions where ⃗v 0 has full support. Proof. We can follow the proof used in Lemma 3.5 and construct an environ-ment where πi (where it differs from πj ) steers the environment to high reward states and πj steers the environment to a uniform distribution. However, we need to be careful since there is one situation where this does not apply. Suppose T = 1 (ie. the value function only cares about maximising reward over the first timestep). Then, let ⃗v 0 be a distribution without full support. We could then find two policies which were identical for all states where ⃗v 0

was nonzero and differed only in states assigned zero probability mass by ⃗v 0.In this situation fij would be identically zero, since both policies would only be assessed over one timestep for their performance in states for which they behaved identically. This situation can be prevented by either enforcing that ⃗v 0 has full support (since then all policy actions contribute to the final reward) or ensuring that the reward is summed over all timesteps (ie. T > 1). Since we can assume that our environment is in int( X ) without affecting the mutual information calculation, in the second timestep, we are guaranteed to have a 13 distribution over states with full support, since all actions taken in the first timestep have some probability of leading the environment to any of the other states. Thus, in this situation, fij will not be identically zero provided T > 1or that ⃗v 0 has full support. We can now prove the following lemma. 

Lemma 3.9. For all value functions of the form in equation (17), with either 

T > 1 or ⃗v 0 having full support, there is a unique optimal policy in all environ-ments, except for a measure zero subset. Proof. Value functions of the form given in equation (17) are real analytic func-tions of the environment x. By Lemma 3.3 we have that real analytic functions of the form fij (x) = Vπi (x) − Vπj (x) will either be identically zero or have a set of zeros which is measure zero. By Lemma 3.8, fij is not identically zero provided that T > 1 or ⃗v 0 has full support. This means that the set of envi-ronments for which fij (x) = 0 and two policies have the same performance is of measure zero. Therefore, there is a unique optimal policy in all environments except a measure zero subset. Now, note that since we have shown that this family of value functions depends on the policy only through Mπ (x), it is covered by Lemma 3.2. This means that we can conclude that all policies have an equal volume of environments for which they are optimal. This allows us to prove the following theorem. 

Theorem 3.10. An optimal deterministic policy for maximising the summed reward over states for a finite number of timesteps T with any discount rate 0 <γ ≤ 1 for any non-constant reward function contains n log m bits of information about its environment provided that T > 1 or ⃗v 0 has full support. Proof. By Lemma 3.9 we have that there is a unique optimal policy for this family of value functions in all environments except for a measure zero subset. By Theorem 3.2 we have that each of these policies will be optimal in an equal volume of environments. Therefore, we can apply Lemma 3.1 to this family of value functions and conclude that the optimal policy contains n log m bits of information about its environment. 

## 3.4 Time-averaged reward maximisation 

Having considered finite-time reward maximisation and time-discounted reward maximisation, we now consider time-averaged reward maximisation of a reward function. Value functions of this kind take the following form: 

Vπ (x) = lim  

> T→∞

1

T

> T

X

> t=0

E[r(St)] . (19) Again, we can re-write this in terms of the transition matrix 

Vπ (x) = lim  

> T→∞

1

T

> T

X

> t=0

R(Mπ (x)t⃗v 0) . (20) 14 Notice again that the only way in which the policy affects the value function is through the transition matrix, meaning that Theorem 3.2 applies. Again, the argument we will present applies to value functions defined in terms of any 

starting distribution ⃗v 0.First we will use the following lemma. 

Lemma 3.11. Any value function Vπ (x) of the form given in equation (19) is a real analytic function of x over int (X ).Proof. The proof is straightforward but lengthy and involves application of stan-dard results about stationary Markov processes. It can be found in Appendix D. This also means that fij (x) = Vπi (x) − Vπj (x) is a real analytic function of x.We now repeat the same process as in previous sections, first ruling out that fij 

is identically zero and then applying Lemma 3.3. 

Lemma 3.12. fij (x) is not identically zero. Proof. Again, as in Lemmas 3.5 and 3.8 we can construct an environment where 

πi has higher average reward than πj . The construction is slightly different in this case and is done explicitly in Appendix E. 

Lemma 3.13. The set of environments with more than one optimal policy for maximizing the time-averaged reward have measure zero. Proof. Since by Lemma 3.11, we have that Vπ (x) is real analytic for all policies 

π, we also have fij (x) = Vπi (x) − Vπj (x) is real analytic. By Lemma 3.3, the set 

{x : fij (x) = 0 } must have measure zero, or fij (x) is identically zero. By Lemma 3.12, we know that fij (x) is not identically zero, therefore {x : fij (x) = 0 } has measure zero. Therefore, there will be a unique policy in all environments except a measure zero subset. This justifies our requirement that there is exactly one optimal policy in all environments except a measure zero subset of X . Furthermore, equation (20) shows that our value function can be expressed as a function of the transition matrix, meaning that we can apply Theorem 3.2 in order to prove our final theorem. 

Theorem 3.14. An optimal deterministic policy for maximising the time-averaged reward over states for any non-constant reward function contains n log m bits of information about its environment. Proof. By Lemma 3.13 all environments, except those of a measure zero subset, have a unique optimal policy. By Theorem 3.2, each policy is uniquely optimal in a subset of the same volume. Therefore, by Lemma 3.1, the optimal policy for maximising the time-averaged reward contains n log m bits of information about its environment. 15 4 Conclusion 

We have shown that if we observe an optimal agent, starting with zero be-lief about the world in which the agent is (represented by a uniform prior), the knowledge that the agent is implementing a specific deterministic policy provides exactly n log m bits of information. Mathematically, this means the mutual information between the environment and the optimal policy is n log m,for a very broad class of rewards. More specifically, we’ve shown this is true for any reward that depends only on states, regardless of the regime being finite horizon, infinite discounted horizon or time-averaged. To show this result, we established some general facts about the space of environ-ments. First, we proved the deterministic optimal policies for reward maximizers induce a finite partition over the environments, for almost every environment. Each set of the partition is defined by environments that are optimal for a deterministic policy, and there is one such set for every distinct deterministic policy. Since there are mn possible deterministic policies, the induced partition contains mn sets. We highlight the fact that this result is strictly geometric and does not depend on any specific prior over environments. Thus, knowing the optimal policy lets us conclude that the environment is in a certain set of the partition. We then used this fact to show that knowledge of the optimal policy gives log mn bits of information about the environment when one uses it to update from state of maximum ignorance. Importantly, these results hold for a wide class of reward accumulation methods and are not an artifact of a specific choice of reward function. More generally, as long as the value function depends only on the transition probabilities of the environment, the result still holds. One of the limitations of the present study is that we considered the policy π

to be deterministic and without memory. Future work would involve extending this result to non-deterministic policies of the form π : S → ∆A, where ∆ A

is the set of probability distributions over actions. This would make the re-sult stronger since often real world agents are non-deterministic. Additionally, we would like to consider agents with memory, whose policies take in histories as well as the current state. Another extension of this result could consider partially observable environments, i.e, a setup such that there is a (possibly stochastic) function f : S → O and policies π : O → A. In this setup, for any state s, f (s) ∈ O represents the ‘observation’ received by the agent, which may not convey full information about the environment state. In this paper, we considered only optimal policies, but an obvious extension is to investigate world information contained by non-optimal policies. If a policy was not optimal but achieved a performance within δ of the optimal perfor-mance, could we bound the mutual information between the policy and the environment in terms of δ? Another natural extension is to consider a wider 16 class of goals, such as those involving reward functions which depend on actions as well as states. Finally, we are also interested in proving a similar result using the notion of mutual information from Algorithmic Information Theory instead Shannon information. 

# 5 Acknowledgments 

This work was funded by the Advanced Research + Invention Agency (ARIA) Safeguarded AI Programme through project code MSAI-SE01-P005. Thanks to Santiago Cifuentes, Guillermo Martin, Winter Cross for reading and comment-ing on the draft of this manuscript. 

# Appendix A Formal setup and entropy reduction 

Definition A.1. Let S be the set of states and A be the set of actions. We assume S and A are non-empty finite sets, and we denote |S| = n, |A| = m

Definition A.2. An environment x is a list of transition probabilities (P (s′|s, a )) s′,s ∈S,a ∈A (1) i.e, ∀s ∈ S, a ∈ A, we have P 

> s′∈S

P (s′|s, a ) = 1 and P (s′|s, a ) ∈ [0 , 1]. We denote P (s′|s, a ) = Pss ′ (a) and 

X = {(Pss ′ (a)) s′,s,a ∈ Rmn 2

; X

> s′∈S

P (s′|s, a ) = 1 , P (s′|s, a ) ∈ [0 , 1] , ∀s, a } (2) is the space of all environments. 

Definition A.3. Consider a probability space (Ω , F, P) and the measurable space ( X , BX ), where BX is the Borel σ-algebra over X . Then, given an abso-lutely continuous random variable X : Ω → X with density f , one can define the (differential) entropy h(X) given by 

h(X) = −

Z

> X

f (x) log f (x)dx (3) 

Remark. We can see an environment as a point x ∈ X in the space of environ-ments or as a random variable X : Ω → X 

Remark. Note that the volume of the space X is given by 

V =

Z

> X

dx (4) 17 Lemma A.1. If X ∼ U (X ), then h(X) = log V

Proof. Since X ∼ U (X ), f (x) = 1 

> V

. Then, 

h(x) = −

Z

> X

f (x) log f (x)dx = −

Z

> X

1

V log 1

V dx = log V (5) 

Lemma A.2. Consider a finite partition (Xj )Nj=1 of the space X such that each 

Xj has the same volume. Then, the differential entropy reduction achieved by the information that the environment is in Xj for some j is given by h(X) − h(X |

X ∈ X j ) = log N

Proof. Using Bayes Theorem, f (x|x ∈ X j ) = f (x)IXj (x)  

> P(x∈X j)

= NV IXj (x)Now, noting that R 

> Xj

dx = VN ,

h(x) − h(x | x ∈ X j ) = log V −

Z

> X

NV IXj (x) log( NV IXj (x)) dx (6) = log V − log NV (7) = log N (8) 

Remark. Note that, more formally, we could define the set of environments, X ,as the cartesian product of mn copies of ( n − 1) −dimensional simplexes. Then the environment X can be treated as the identity random variable on the prob-ability space ( X , BX , P), where P is a probability measure proportional to the product measure defined by the product of the Lebesgue measures in each of the individual simplexes. 

# B The entropy reduction bound 

It is possible to show result A.2 is an upper bound for any other finite partition, i.e, the entropy reduction from knowing the environment is within some set of any finite partition comprised of measurable sets is at most log N .Let X : Ω → X an absolutely continuous random variable, with density f (x). Let X1, X2, ..., XN be any finite partition of X .Define pj := P(X ∈ X j ) = R 

> Xj

f (x)dx , for j = 1 , ..., N .Define J : Ω → { 1, 2, ..., N } by J(ω) = j ⇐⇒ X(ω) ∈ X j . Note that since ( Xj )Nj=1 is a partition, for any given ω ∈ Ω, X(ω) can be in one and only one set Xj , so J is well defined. Additionally, J is a discrete random 18 variable since X is a random variable and its image is a finite set. Note that 

P(J = j) = P(X ∈ X j ) = pj

We will show that h(X) − h(X|J) = H(J), where H(J) stands for the entropy of J given by 

H(J) = 

> N

X

> j=1

pj log pj

In other words, h(X) − h(X|X ∈ X j ) = H(X ∈ X j ). Now, note that h(X) − h(X|J) = I(X; J). By symmetry of mutual infor-mation, I(X; J) = I(J; X) = H(J) − H(J|X), since J is a discrete random variable. Also, since J is a deterministic function of X, H(J|X) = 0 and thus 

h(X) − h(X|J) = I(J; X) = H(J). Now, recalling the known fact that if J is a discrete random variable over 

{1, 2, ..., N }, H(J) ≤ log N , with equality if, and only if, pj = 1 

> N

, ∀j = 1 , 2, ..., N 

(e.g, the discrete uniform is the only one with maximum entropy) the result fol-lows. In particular, consider any continuous prior over our set of environments in Definition A.2 (i.e, consider it to have any density function f (x)). Then, consider the same deterministic, stationary policies Π = {π : S → A}. Since Π is finite, for every environment there is at least one optimal policy, so we can construct a function f : X → Π that takes every environment x to a policy that is optimal 

x. Then, ker f is an equivalence relation and induces a partition X1, ..., XN

with a finite number of sets since there is a finite number of policies. Then, since the entropy reduction by knowing a certain policy is optimal is given by 

h(X) − h(X|X ∈ X j ), it will always be true that h(X) − h(X|X ∈ X j ) ≤ log N

# C Proof of Theorem 3.2 

First, we will be precise about what we mean when we say that a value func-tion “is only influenced by the policy through its effect on the transition matrix 

Mπ (x)”. Though it is fairly intuitive, we will now make it mathematically pre-cise. Let ¯M : Π × X → Rn×n be the function which takes a policy and an environ-ment and outputs the transition matrix. Let ¯V : Π × X → R be a value function which takes in a policy an environment and outputs a total reward. We say that ¯V “is only influenced by the policy through its effect on the transition matrix 

Mπ (x)” if it is possible to find a function k : Rn×n → R such that k ◦ ¯M = ¯V .In this sense, the only way in which the policy can change the total reward is through its effect on the transition matrix. The class of value functions which can be expressed this way is very broad and (as we show in the main paper) 19 applies to all of the main forms of reward maximisation. However, it is not so broad as to include all conceivable goals. For example, goals involving achieving a precise sequence of actions and states (such as the goals considered in [3])are not expressible in this way. We have defined the family of functions gij : X → X , ∀i, j ∈ { 1, 2, ..., n }. In this appendix, we will use these functions to show that, they are invertible, preserve optimality, and preserve volume, i.e, 

gij (Xi) = Xj ,

Z

> x∈X i

dx =

Z

> x∈X j

dx , 

and 

Mπi (x) = Mπj (gij (x)) .

∀i = 1 ...n, j = 1 ...n .To fix notation, we’ll index x ∈ X ⊂ Rmn 2

as x = ( xs,a,s ′ )s,s ′∈S,a ∈A, noting that xs,a,s ′ = P (s′|s, a ). Note that determining an environment is equivalent as determining mn 2 probabilities xs,a,s ′ such that P 

> s′∈S

xs,a,s ′ = 1 , ∀(s, a ) ∈ S×A.

## C.1 Characterizing gij 

In the main body of the paper, we described the functions gij as mapping from an environment x to another environment x′, where the actions of policy πj

have the same effect as the actions of policy πi do in x. Formally, we can write this as: 

gij (x)s,π j (s),s ′ := xs,π i(s),s ′ , (9) 

gij (x)s,π i(s),s ′ := xs,π j (s),s ′ . (10) For transition probabilities corresponding to actions not taken by πi or πj , the environment is left unchanged so we have for a / ∈ πi(S) ∪ πj (S), 

gij (x)s,a,s ′ := xs,a,s ′ . (11) Now note that since gij function which simply swaps some pairs of entries in a 

mn 2-dimensional vector it is its own inverse (since swapping a pair of entries twice results in the original vector). We can see that gij ◦ gij = id more explicitly by considering the transformation on all individual components of x. By construction of gij (temporarily dropping the ij subscript from gij to reduce notational clutter) we have, 

g(g(x)) s,π i(s),s ′ = g(x)s,π j (s),s ′ (12) = xs,π i(s),s ′ , (13) 20 g(g(x)) s,π j (s),s ′ = g(x)s,π i(s),s ′ (14) = xs,π j (s),s ′ . (15) Also, for a / ∈ πi(S) ∪ πj (S), 

g(g(x)) s,a,s ′ = g(x)s,a,s ′ (16) = xs,a,s ′ . (17) Thus, g(g(x)) = x, ∀x ∈ X .

## C.2 The effect on transition matrices 

For this theorem, we are concerned with the effects of the policy on the result transition matrix for that policy acting in a particular environment. Due to the construction of gij , the transition matrix induced by policy πi in environment x

is the same as the transition matrix induced by policy πj in environment gij (x). Using our notation Mπ (x) to indicate the transition matrix induced by policy 

π in environment x, we can write: 

Mπi (x) = Mπj (gij (x)) . (18) This can be seen by considering these matrices elementwise, using k, l to denote matrix indexes: [Mπi (x)] kl = P (sl|sk, π i(sk)) (19) = xsk ,π i(sk ),s l (20) = g(x)sk ,π j (sk ),s l (21) = [ Mπj (g(x))] kl . (22) Since gij is its own inverse, by symmetry, we also have 

Mπi (gij (x)) = Mπj (x) . (23) 

## C.3 Optimality 

In this section, we prove that if πi is optimal in an environment x, then πj is op-timal in environment gij (x). This is proved for any value function Vπ (x) where the contribution of the policy is determined only by the transition matrix Mπ (x)induced by policy π. As it turns out, this covers a very broad class of goals, in-cluding all forms of reward maximisation (time-averaged, time-discounted, and finite time) considered in this main paper. Now, we will show that, for any value function in this class, if πi is optimal in an environment x, then πj is optimal in environment gij (x) and achieves the same performance. This can be seen intuitively by noting the fact that, since 

gij swaps the effect of taking some actions in some states, while leaving all other 21 transition probabilities unchanged, each policy in x will have a corresponding policy with the same performance in gij (x). We will now make this claim formal. Let ˜Π = {π : S → A} = {πi|i ∈ I} be the space of all deterministic, stationary policies. Then, define ϕij : ˜Π → ˜Π be a function which maps policies to policies. We will use ϕij [ρ] : S → A to denote the policy resulting from applying ϕij to a policy ρ. We will use ϕij [ρ]( s) to indicate the action resulting from applying the policy ϕij [ρ] to a particular state s. We define ϕij in terms of its effect on a generic policy: 

ϕij [ρ]( s) = 



πj (s), if ρ(s) = πi(s)

πi(s), if ρ(s) = πj (s)

ρ(s), otherwise .

(24) In other words, for states where ρ takes the same action as either πi or πj ,we have that ϕij switches which action is taken. The behaviour of the policy is left unchanged in states where ρ behaves differently to πi or πj . Note that 

ϕij is its self-inverse. Since it only swaps the actions taken, we have ϕij ◦ϕij = id .We will now show that, for any reward function which depends only on the tran-sition matrix Mπ (x) the performance of policy ρ in environment x is equal to the performance of policy ϕij [ρ] in environment gij (x) ie. Vϕij [ρ](x) = Vρ(gij (x)). Going forward, we will use ˜ ρ = ϕij [ρ] to indicate a policy ρ transformed by ϕij .Since we are considering value functions which only depend on the transition probabilities xs,π (s),s ′ , it suffices to show xs, ˜ρ(s),s ′ = gij (x)s,ρ (s),s ′ . We will now show this is true for all s, s ′.First, we consider cases where ρ(s) = πi(s). Then, by definition of gij and ϕij ,if ρ(s) = πi(s) we have: 

xs, ˜ρ(s),s ′ = xs,π j (s),s ′ (25) = gij (x)s,π i(s),s ′ (26) = gij (x)s,ρ (s),s ′ . (27) Analogously, if ρ(s) = πj (s), it follows xs, ˜ρ(s),s ′ = gij (x)s,ρ (s),s ′ . Finally, for states s where ρ(s)̸ = πi(s) and ρ(s)̸ = πj (s), then we have that ˜ ρ(s) = ρ(s). Furthermore, the relevant environmental transition probabilities will be left un-changed so we have gij (x)s,ρ (s),s ′ = xs,ρ (s),s ′ . Meaning that for these states, we can write: 

xs, ˜ρ(s),s ′ = xs,ρ (s),s ′ (28) = gij (x)s,ρ (s),s ′ . (29) We have therefore shown that for all s, s ′, the elements of the transition matrix for a policy ρ in an environment x are the same as the elements of the transition 22 matrix for a policy ˜ ρ = ϕij (ρ) in an environment gij (x). Note that ϕij (πi) = πj , by definition of ϕij . Assume that πi is optimal in environment x. Then, it holds that Vπi (x) ≥ Vπ (x), ∀π ∈ ˜Π. Now, for any 

ρ ∈ ˜Π, since ϕij is invertible, there is ρ′ ∈ ˜Π such that ϕij (ρ′) = ρ. Then, 

Vπj (g(x)) = Vϕij (πj )(x) (30) = Vπi (x) , (31) ie. the performance of πj in environment gij (x) is equal to the performance of 

πi in environment x. Furthermore: 

Vπi (x) ≥ Vϕij (ρ′)(x) (32) = Vρ′ (gij (x)) . (33) Then, Vπj (gij (x)) ≥ Vρ(gij (x)) , ∀ρ ∈ ˜Π. Thus, if πi is optimal in environment 

x, we have that πj is optimal and with the same performance in environment 

gij (x). 

## C.4 Each gij preserves volume 

We’ll use the theorem found in [15] that if U, V ⊆ RN open sets and g : U → V

continuously differentiable bijection with differentiable inverse, then for any 

A ⊆ U measurable, 

vol (g(A)) = 

Z

> A

| det Jg (x)|dx . (34) First note that gij : X → X is a linear function. Thus, there is a mn 2 × mn 2

matrix P such that gij (x) = P x , so gij is differentiable and Jg (x) = P . Note now that since g ◦ g = id , then P T · P = Id , where Id stands for the mn 2 × mn 2

identity matrix, and hence by det( P T P ) = 1 = ⇒ det( P )2 = 1 = ⇒ | det( P )| =

| det( Jg )| = 1, since det( P T ) = det( P ) and det( AB ) = det( A) · det( B), for any 

N × N matrixes A, B .We can restrict g to the interior of Xi and then apply the theorem, since the border of Xi has measure zero and therefore doesn’t influence the integral. Then, since there is a volume-preserving transformation between all pairs of sets Xi

and Xj we can conclude that each of these sets has the same volume. 

# D Proof of Lemma 3.11 

We will first apply standard results from the study of Markov processes to show that, for any policy πi acting in any environment x ∈ int( X ) and with any initial probability distribution over states ⃗v 0, the probability distribution over states will converge to a steady state. Following this, we will show that the steady state is a real analytic function of x and that, due to the Ces` aro mean theorem, 23 this means that Vπi and fij are also real analytic functions of x.If we fix a deterministic, Markovian policy πi and let it run in an environment 

x, then we can treat the system as a stationary Markov process. Restricting to x ∈ int( X ) means that none of the transition probabilities in this Markov process are zero. This means that the Markov process is both irreducible and 

aperiodic . Irreducible means that, for any two states sa and sb, when starting from sa, there exists some number of timeteps after which there is a nonzero probability of the system eventually transitioning to sb. Aperiodicity is enforced when, for all states sa, there is some probability of remaining in that state at the subsequent timestep. Our condition x ∈ int( X ) is actually stronger than just enforcing aperiodicty and irreducibility, as it means that none of the transition probabilities are exactly zero, so for every state-action pair, there is a nonzero probability of transitioning to any state. But since restricting to int( X ) only involves excluding a measure zero subset of environments we can do so without any additional loss of generality. It is a standard result that a Markov process which is irreducible and aperiodic will eventually converge to a stationary distribution over states, regardless of the starting state. 

Theorem D.1. Convergence theorem. If a Markov chain (a Markov process with discrete states and discrete timesteps) is characterised by a transition ma-trix M which is irreducible and aperiodic then: 1. There exists a unique stationary distribution ⃗μ such that M⃗ μ =⃗μ.2. Any initial state ⃗v 0 will converge to ⃗μ with repeated applications of M .Proof. Proofs of 1. and 2. can be found in most textbooks on Markov Chains such as [16, 17]. We will use μπ (x) to denote the stationary distribution resulting from a policy π

acting in an environment x. The fact that the distribution over states converges to a stationary distribution greatly simplifies the analysis of Vπ (x) due to the following standard result. 

Lemma D.2. Ces` aro mean theorem (also known as Cauchy’s limit theorem). Let (an)n∈N a convergent sequence of real numbers such that lim n→∞ an = L.Then 

lim  

> N→∞

1

N

> N

X

> i=1

an = L . (35) 

Proof. The proof is standard and can be found in many textbooks such as [18], under the name ‘Cauchy’s limit theorem’. Applying this theorem to our value function gives: 

Vπ (x) = lim  

> T→∞

1

T

> T

X

> t=1

R(Mπ (x)t⃗v 0) = lim  

> t→∞

R(Mπ (x)t⃗v 0) = R(⃗μ π (x)) . (36) 24 The average value function simplifies down to expected reward of the stationary distribution. We will complete the proof by showing that μπ (x) and therefore 

Vπ (x) is a real analytic function of x.

Lemma D.3. Vπ (x) is a real analytic function of x.Proof. All we need to do is prove that the steady state vector is a real analytic function of the elements of Mπ (x) and hence a real analytic function of x. We will use ⃗μ =⃗μ π (x) to denote this steady state which is the solution to the equa-tion ( Mπ (x) − 1) ⃗μ = 0. However, since M π (x) has an eigenvalue equal to 1 (by virtue of being aperiodic and irreducible) we have that det( Mπ (x) − 1) = 0, meaning that we cannot apply Cramer’s rule to express ⃗μ in terms of M π (x). We will use the fact the ⃗μ must be a normalised probability vector to solve this problem. By the Perron-Frobenius theorem, we know that Mπ (x) has eigen-values |λ| ≤ 1 with exactly one eigenvalue equal to 1. As a result, the matrix 

Mπ (x) − 1 has a 1-dimensional nullspace and therefore by the rank-nullity the-orem has rank ( n − 1). This means that one of the rows of Mπ (x) − 1 is not linearly independent of the others. Without loss of generality, we will assume that this is the last row. Let us use B to denote the matrix Mπ (x) − 1 with its last row replaced with all ones. First, we will show that the all-ones row is linearly independent of the other rows, meaning that B is full rank and thus a nonzero determinant. We then apply Cramer’s rule to express ⃗μ in terms of B (and hence, in terms of 

Mπ (x)). Assume that the all ones row is not linearly independent of the other rows. This would mean that there exists a vector ⃗u̸ =⃗ 0 such that B⃗ u = 0. Let us abbreviate Mπ (x) as M . Since B and M − 1 are the same for the first n − 1rows, this means that we also have [( M − 1) u]i = 0 for i = 1 ...n − 1. Since M

is a stochastic matrix, each of the columns of M sum to one. This means that ⃗

1⊺(M − 1) = ⃗ 0 where ⃗ 0 and ⃗ 1 are the n-dimensional column vectors of all zeros and all ones respectively. As a result must have ⃗ 1⊺(M − 1) ⃗v = 0 for any vector ⃗v. Therefore: ⃗

1⊺(M − 1) ⃗u = X

> i

[( M − 1) ⃗u] i = 0 . (37) Since we have [( M − 1) u]i = 0 for i = 1 ...n − 1, this means that [( M − 1) ⃗u] i must also be zero meaning that ( M − 1) ⃗u =⃗ 0. As we noted before, the nullspace of 

M − 1 is 1-dimensional. We therefore require that ⃗u must be in the nullspace along with the steady state ⃗μ. Therefore it must be possible to express ⃗u = α⃗ μ

using some scalar α.Now, notice that the last row of B⃗ u is simply the sum of the elements of ⃗u, since the last row of B is all ones. Simultaneously, we have assumed that B⃗ u =⃗ 0 so 25 we have [ B⃗ u] n = Pni ui = 0. But, since we also have ⃗u = α⃗ μ, and P 

> i

μi = 1, the only way for this to be true is if α = 0. But this contradicts our supposition that ⃗u̸ =⃗ 0, meaning that no such vector ⃗u exists. As a result, we can conclude that all rows of B are linearly independent. Thus B is full rank and invertible. Finally, notice that, due to the normalisation of μ, we have B⃗ μ =⃗w where ⃗w = (0 , 0, .. 1) ⊺. As a result, we can express the elements of μ using Cramer’s rule: 

μi = det Bi

det B , (38) where Bi is the matrix resulting from replacing the ith column of B with ⃗w. As we have proved that B is invertible, we have that det B̸ = 0 meaning that these expressions are well-defined. Since each of these expressions for μi are simply polynomials in the elements of the matrix M − 1, the elements of μi are real analytic functions of the elements of Mπ (x) and hence real analytic functions of x.

# E Proof of Lemma 3.12 

Since we have that πi and πj must disagree on what action to take in at least one state, we pick one state where they take different actions and call it sa.Furthermore, since we have a non-constant reward function, we must have one state with the highest reward. Call this state sb. It may be the case that sa and 

sb are the same states, or they may be different. Now consider the following environment x′. In this environment, in all states except sa, all actions result in the next state being a random state from a uniform distribution over states. This means that the transition matrix elements [ Mπi (s)] kl = [ Mπj (s)] kl = 1 

> n

for all k, l except l = a.In state sa, the action taken by πi has a high probability of 1 − ϵ of transitioning the system to state sb and a ϵn−1 probability of transitioning to each of the other states. On the other hand, the action taken by state πj in state sa leads to each of the other states with equal probability 1 

> n

. This means that every element of 

Mπj (x) is 1 

> n

.For πi, on the other hand, every element of Mπi (x) is 1 

> n

except for the ath col-umn. In the ath column of Mπi (x), every element is ϵn−1 except for the element in the ath row, which is 1 − ϵ. By computing the eigenvectors of Mπi (x′) and 

Mπj (x′), it is then straightforward to verify that the steady state distribution for πj is the unform distribution over all n states. Whereas the steady state distribution for πi has probability mass 1 

> nϵ +1

on state sb and probability mass  

> 1(nϵ +1)( n−1)

on each of the other states. Since sb has a higher reward than all other states, we can conclude that policy πi achieves a higher performance than 26 πj in environment x′. This means that fij (x′) > 0, which proves that fij (x) is not identically zero. 

# References 

[1] Jingtao Ding, Yunke Zhang, Yu Shang, Yuheng Zhang, Zefang Zong, Jie Feng, Yuan Yuan, Hongyuan Su, Nian Li, Nicholas Sukiennik, et al. Un-derstanding world or predicting future? a comprehensive survey of world models. ACM Computing Surveys , 58(3):1–38, 2025. [2] Alex Altair. Towards a formalization of the agent structure problem. 

https://www.lesswrong.com/posts/oxsBpx9v3bgxraiPj/ , 2024. Ac-cessed: 2026-01-01. [3] Jonathan Richens, David Abel, Alexis Bellot, and Tom Everitt. General agents need world models. In Forty-second International Conference on Machine Learning , 2025. [4] Michael Bowling, John D Martin, David Abel, and Will Dabney. Settling the reward hypothesis. In International Conference on Machine Learning ,pages 3003–3020. PMLR, 2023. [5] John Von Neumann and Oskar Morgenstern. Theory of games and economic behavior: 60th anniversary commemorative edition. In Theory of games and economic behavior . Princeton university press, 2007. [6] Richard S. Sutton and Andrew G. Barto. Reinforcement Learning: An Introduction . The MIT Press, second edition, 2018. [7] Rodney A Brooks. Intelligence without representation. Artificial intelli-gence , 47(1-3):139–159, 1991. [8] Richard Sutton. The bitter lesson. Incomplete Ideas (blog) , 13(1):38, 2019. [9] David Ha and J¨ urgen Schmidhuber. World models. arXiv preprint arXiv:1803.10122 , 2(3), 2018. [10] Hugo Touchette and Seth Lloyd. Information-theoretic approach to the study of control systems. Physica A: Statistical Mechanics and its Appli-cations , 331(1-2):140–172, 2004. [11] Jonathan Richens and Tom Everitt. Robust agents learn causal world models. arXiv preprint arXiv:2402.10877 , 2024. [12] Santiago Cifuentes. General agents contain world models, even under par-tial observability and stochasticity. arXiv preprint arXiv:2602.03146 , 2026. [13] Thomas M Cover. Elements of information theory . John Wiley & Sons, 1999. 27 [14] Boris Mityagin. The zero set of a real analytic function. arXiv preprint arXiv:1512.07276 , 2015. [15] M. Spivak. Calculus On Manifolds: A Modern Approach To Classical The-orems Of Advanced Calculus (1st ed.) . Benjamin Cummings, 1965. [16] James R Norris. Markov chains . Number 2. Cambridge university press, 1998. [17] David A Levin and Yuval Peres. Markov chains and mixing times , volume 107. American Mathematical Soc., 2017. [18] Konrad Knopp. Infinite sequences and series . Courier Corporation, 2012. 28